---
phase: 07-workstreams-workspaces-port
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/skills/using-git-worktrees/SKILL.md
autonomous: true
requirements:
  - WF-27
must_haves:
  truths:
    - "`oto/skills/using-git-worktrees/SKILL.md` contains a `## Workflow Deference` section"
    - "The Workflow Deference section names `/oto-new-workspace` as the preferred path for creating an isolated workspace with `.oto/` state isolation"
    - "The skill's standalone form (ad-hoc worktree work) remains valid per the directive wording"
  artifacts:
    - path: "oto/skills/using-git-worktrees/SKILL.md"
      provides: "Phase-6-deferred Workflow Deference directive (ADR-03 workflow-wins routing for the workspace-creation slice)"
      contains: "## Workflow Deference"
      contains_alt: "/oto-new-workspace"
  key_links:
    - from: "oto/skills/using-git-worktrees/SKILL.md"
      to: "/oto-new-workspace command"
      via: "inline prose directive in skill body"
      pattern: "/oto-new-workspace"
---

<objective>
Wire the Phase-6-deferred ADR-03 deferral by adding a `## Workflow Deference` section to `oto/skills/using-git-worktrees/SKILL.md` (D-03, D-04). The section directs Claude to prefer `/oto-new-workspace` for creating an isolated workspace with `.oto/` state isolation, while preserving the skill's standalone usefulness for ad-hoc worktree work outside an oto workspace context.

This mirrors the Phase 6 D-03 pattern in `oto/skills/using-oto/SKILL.md` (the existing `## Workflow Deference` section at line 82 uses an `<!-- oto:state-gating-directive -->` HTML comment marker; this plan uses similar sectioning style but its own directive — D-04 explicitly says "no coupling to `.oto/STATE.md` presence — the directive applies universally").

Purpose: Closes the Phase 6 → Phase 7 deferral. WF-27 ships `/oto-new-workspace`; without this directive, an active Claude session that auto-loads `using-git-worktrees` could bypass the `/oto-new-workspace` workflow and run `git worktree add` directly, defeating ADR-03 workflow-wins routing for this specific slice.

Output: One file modified. Single section insertion near top of skill body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-workstreams-workspaces-port/07-CONTEXT.md
@.planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md
@oto/skills/using-git-worktrees/SKILL.md
@oto/skills/using-oto/SKILL.md
@decisions/ADR-03-skill-vs-command.md
</context>

<interfaces>
<!-- Pattern reference: existing Workflow Deference section in oto:using-oto -->
<!-- From oto/skills/using-oto/SKILL.md line 82-93 (read this verbatim before writing the new section): -->

```markdown
## Workflow Deference

<!-- oto:state-gating-directive -->
Before invoking any other oto skill on suspicion, read `.oto/STATE.md`.
If the `status:` frontmatter field shows an active phase
(`execute_phase`, `plan_phase`, `debug`, or `verify`), suppress
ambient skill auto-fire — only canonical agent invocations and
explicit user `Skill()` calls fire.

If `.oto/STATE.md` is missing, malformed, or shows `status: complete`,
treat as no active workflow and let skills fire normally.
<!-- /oto:state-gating-directive -->
```

<!-- Phase 7 directive is DIFFERENT — D-04 explicitly says no STATE.md coupling. -->
<!-- Phase 7 directive applies universally; ambient STATE.md gate from `oto:using-oto` already handles the active-workflow case at a different layer. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert `## Workflow Deference` section into using-git-worktrees SKILL.md</name>
  <files>oto/skills/using-git-worktrees/SKILL.md</files>
  <read_first>
    oto/skills/using-git-worktrees/SKILL.md (entire file — to find the correct insertion point near the top of the body, before procedural content),
    oto/skills/using-oto/SKILL.md (lines 80-95 — for section style reference; see <interfaces> block above),
    .planning/phases/07-workstreams-workspaces-port/07-CONTEXT.md (D-03, D-04, and `<specifics>` first bullet for suggested wording),
    decisions/ADR-03-skill-vs-command.md (workflow-wins routing rule that justifies the directive)
  </read_first>
  <action>
    Find the insertion point: the section MUST go AFTER the H1 `# Using Git Worktrees`, the `## Overview` section, and the "Announce at start" line, but BEFORE the first procedural section (`## Directory Selection Process` at the file's existing line 16). Per CONTEXT.md `<specifics>` "near the top of the skill body, ahead of any procedural content (mirrors Phase 6 D-03 placement of the Workflow Deference section)."

    Read `oto/skills/using-git-worktrees/SKILL.md` lines 1-20 to confirm the exact insertion offset; the target placement is between the `**Announce at start:**` line (currently line 14) and the `## Directory Selection Process` heading (currently line 16).

    Insert exactly this section (verbatim, including the HTML comment markers for grep-stability and the blank lines):

    ```markdown

    ## Workflow Deference

    <!-- oto:workflow-deference-directive -->
    For creating an isolated workspace with `.oto/` state isolation, prefer `/oto-new-workspace` over invoking `git worktree add` directly. The `/oto-new-workspace` workflow handles `.oto/` state initialization, repo selection, strategy resolution, and registration that raw `git worktree add` does not.

    The skill's standalone form remains valid for ad-hoc worktree work outside an oto workspace context — single-PR review, scratch worktree for debugging, or any worktree task that does not need `.oto/` state isolation.
    <!-- /oto:workflow-deference-directive -->

    ```

    Use the Edit tool with:
    - old_string: the literal text of the line(s) immediately preceding the insertion point plus the line(s) immediately following (use enough context to make the match unique — the `**Announce at start:**` line plus the blank line plus `## Directory Selection Process` heading)
    - new_string: the same prefix lines + the new section above + the same suffix lines

    Do NOT modify any other content in the file. Do NOT add `oto:state-gating-directive` markers (D-04: no STATE.md coupling — that gating already lives in `oto:using-oto` at a different layer).
  </action>
  <verify>
    <automated>grep -q "^## Workflow Deference$" oto/skills/using-git-worktrees/SKILL.md && grep -q "/oto-new-workspace" oto/skills/using-git-worktrees/SKILL.md && grep -q "<!-- oto:workflow-deference-directive -->" oto/skills/using-git-worktrees/SKILL.md && grep -q "<!-- /oto:workflow-deference-directive -->" oto/skills/using-git-worktrees/SKILL.md && grep -q "standalone form" oto/skills/using-git-worktrees/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Workflow Deference$" oto/skills/using-git-worktrees/SKILL.md` returns exactly one line
    - That line number is LESS THAN the line number of `^## Directory Selection Process$` in the same file (i.e., the new section is above the procedural content)
    - The substring `/oto-new-workspace` appears at least once in the file
    - The opening marker `<!-- oto:workflow-deference-directive -->` and closing marker `<!-- /oto:workflow-deference-directive -->` both appear exactly once
    - The substring `oto:state-gating-directive` does NOT appear (D-04 forbids STATE.md coupling here)
    - Total file line count is approximately old-line-count + 8 (the new section plus surrounding blank lines)
    - `node --test tests/phase-04-planning-leak.test.cjs` still exits 0 (the new section text contains `.oto/` — a canonical reference, not a leak; the leak test allows `.oto/` and rejects `.planning/`)
  </acceptance_criteria>
  <done>
    `oto/skills/using-git-worktrees/SKILL.md` has a new `## Workflow Deference` section near the top of the body that names `/oto-new-workspace` and preserves standalone-form usefulness. The section is bracketed by HTML comment markers for grep-stability. Phase 4 leak test still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill body → Claude Code skill loader | Claude Code reads `<configDir>/skills/using-git-worktrees/SKILL.md` and parses for ambient injection on suspicion; trust boundary is the file content itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | skill body content (model-facing prompt injection if a future contributor edits the section unsafely) | accept | The directive content is plain prose authored under user control; oto is single-developer. Risk of supply-chain prompt injection here is the same as for any skill body file — out of Phase 7 scope to add per-file content scanning |
| T-07-05 | Repudiation | section presence (regression: a future rebrand pass strips the section) | mitigate | HTML comment markers (`<!-- oto:workflow-deference-directive -->` ... `<!-- /oto:workflow-deference-directive -->`) make the section grep-stable; Plan 03 D-05 structure test will assert the section's presence by grep |
</threat_model>

<verification>
1. `grep -n "^## Workflow Deference$" oto/skills/using-git-worktrees/SKILL.md` returns one match
2. The new section appears before `## Directory Selection Process` (line ordering check)
3. `node --test tests/phase-04-planning-leak.test.cjs` exits 0
</verification>

<success_criteria>
- `## Workflow Deference` section present in skill body
- Names `/oto-new-workspace`
- Preserves standalone-form clause
- Bracketed by stable HTML comment markers
- No STATE.md coupling
</success_criteria>

<output>
After completion, create `.planning/phases/07-workstreams-workspaces-port/07-02-SUMMARY.md` recording: insertion line number, exact section text inserted, before/after total line count.
</output>
