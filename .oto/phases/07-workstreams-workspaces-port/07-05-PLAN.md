---
phase: 07-workstreams-workspaces-port
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-03-PLAN.md
  - 07-04-PLAN.md
files_modified:
  - .planning/phases/07-workstreams-workspaces-port/07-UAT.md
autonomous: false
requirements:
  - WF-26
  - WF-27
must_haves:
  truths:
    - "`07-UAT.md` exists with the documented 8-step checklist"
    - "Each step has a clear pass/fail capture slot for operator confirmation"
    - "Step 8 (the `${OTO_WS}` chaining check) is captured as its own checklist item"
    - "Rollback instructions for each step are documented (so a failed mid-UAT does not leave the repo in a broken state)"
  artifacts:
    - path: ".planning/phases/07-workstreams-workspaces-port/07-UAT.md"
      provides: "Operator-driven UAT checklist + result capture"
      min_lines: 80
      contains: "${OTO_WS}"
  key_links:
    - from: ".planning/phases/07-workstreams-workspaces-port/07-UAT.md"
      to: "operator's interactive Claude Code session"
      via: "8-step manual checklist with pass/fail slots"
      pattern: "\\[ \\]"
---

<objective>
Create the lightweight UAT checklist file that the operator runs interactively to validate WF-26 and WF-27 end-to-end on Claude Code, capturing pass/fail per step in `.planning/phases/07-workstreams-workspaces-port/07-UAT.md` (D-09, D-10).

This plan has ONE autonomous task (Claude writes the checklist file) plus ONE human-verify checkpoint (operator runs the 8 steps, captures results in the same file). The `autonomous: false` frontmatter reflects the checkpoint.

UAT runs in this repo (not a disposable fork) per CONTEXT.md `<specifics>` — workstreams/workspaces operate on `.oto/`, and oto's own `.planning/` is unaffected. Rollback for each step is documented inline.

Purpose: Final operator confirmation that the WF-26/WF-27 surfaces work end-to-end on a real Claude Code session, including the `${OTO_WS}` propagation chaining check (D-10) that no automated test in Phase 7 covers. Failure on any step blocks Phase 7 close and routes to a hand-fixup plan.

Output: `07-UAT.md` written by Claude (autonomous), then filled in with pass/fail by the operator (checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-workstreams-workspaces-port/07-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-08-PLAN.md
@oto/references/workstream-flag.md
@oto/commands/oto/workstreams.md
@oto/commands/oto/new-workspace.md
@oto/commands/oto/list-workspaces.md
@oto/commands/oto/remove-workspace.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `07-UAT.md` checklist template</name>
  <files>.planning/phases/07-workstreams-workspaces-port/07-UAT.md</files>
  <read_first>
    .planning/phases/07-workstreams-workspaces-port/07-CONTEXT.md (D-09 + D-10 spec; `<specifics>` fourth and fifth bullets for run-in-this-repo guidance and the `/oto-progress` chaining choice),
    .planning/phases/04-core-workflows-agents-port/04-08-PLAN.md if present (Phase 4 P08 UAT format reference; if not present, just produce a sensible operator checklist),
    oto/commands/oto/workstreams.md (for the `/oto-workstreams` subcommand surface to confirm exact invocation strings),
    oto/commands/oto/new-workspace.md (for `--name`, `--repos`, `--strategy` flag spec),
    oto/commands/oto/list-workspaces.md,
    oto/commands/oto/remove-workspace.md
  </read_first>
  <action>
    Create `.planning/phases/07-workstreams-workspaces-port/07-UAT.md` with this exact structure:

    ```markdown
    # Phase 7 UAT — Workstreams & Workspaces

    **Created:** YYYY-MM-DD
    **Operator:** [your name / handle]
    **Phase:** 07-workstreams-workspaces-port
    **Requirements covered:** WF-26, WF-27

    ## Preconditions

    - [ ] Repo HEAD is on the branch where Phase 7 plans 01-04 are committed
    - [ ] `node --test tests/07-*.test.cjs` exits 0 (all 4 Phase 7 tests green)
    - [ ] `node --test tests/phase-04-frontmatter-schema.test.cjs tests/phase-04-planning-leak.test.cjs` exits 0 (no Phase 4 regression)
    - [ ] No active workstream in this repo (`ls .oto/workstreams 2>/dev/null` returns nothing or already-archived names)
    - [ ] `git status` is clean before the UAT begins (so any post-UAT diff isolates the UAT's effects)

    ## Run Mode

    Open a Claude Code session in this repo. Each step below is a single slash-command invocation. Capture the output (or relevant subset) under each step's "Result" block. Mark `[x]` for pass, `[!]` for fail, `[~]` for partial / needs investigation.

    Rollback instructions for each step are inline. If any step fails, complete the documented rollback for that step before stopping the UAT.

    ## Checklist

    ### Step 1: Create a workstream

    - [ ] Run: `/oto-workstreams create demo`
    - [ ] **Expected:** New directory `.oto/workstreams/demo/` exists with at least `STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md` migrated into it
    - [ ] **Verify:** `ls .oto/workstreams/demo/` lists the migrated files
    - [ ] **Result:**
          ```
          [paste output here]
          ```
    - [ ] **Pass / Fail:** [ ]
    - [ ] **Rollback if needed:** `rm -rf .oto/workstreams/demo` and restore the migrated files to repo root if migration was destructive (see step output to determine)

    ### Step 2: Switch to the workstream

    - [ ] Run: `/oto-workstreams switch demo`
    - [ ] **Expected:** Active workstream pointer set to `demo`; subsequent oto commands route to the demo workstream's state
    - [ ] **Verify:** `node oto/bin/lib/oto-tools.cjs workstream get` returns demo
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]

    ### Step 3: List workstreams

    - [ ] Run: `/oto-workstreams list`
    - [ ] **Expected:** demo appears in the list with its status; no errors
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]

    ### Step 4: ${OTO_WS} chaining check (D-10)

    > **This is the critical chaining check.** It verifies that after `workstream switch demo`, downstream workflows actually read the demo workstream's state, not the flat-mode files.

    - [ ] With demo still active from step 2, run: `/oto-progress`
    - [ ] **Expected:** Output reflects the demo workstream's state (likely "no roadmap yet" or similar minimal output, since the migrated demo's ROADMAP.md may be empty / stub-state). It should NOT report progress for the actual oto project (Phase 6 complete, etc.). If `/oto-progress` reports the actual oto project's progress, the routing flag did NOT thread → FAIL.
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]
    - [ ] **If FAIL:** Open a hand-fixup plan to investigate `${OTO_WS}` propagation through `oto-tools.cjs` `planningPaths` / `planningRoot` resolution

    ### Step 5: Complete the workstream

    - [ ] Run: `/oto-workstreams complete demo`
    - [ ] **Expected:** demo archived; active pointer cleared (or returned to the prior active state)
    - [ ] **Verify:** `node oto/bin/lib/oto-tools.cjs workstream get` returns null / empty / "no active workstream"
    - [ ] **Verify:** `.oto/workstreams/demo/` either removed or moved to an archive subdirectory (per upstream `complete` behavior — verify against `oto/bin/lib/workstream.cjs` `complete` function)
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]
    - [ ] **Rollback if needed:** `rm -rf .oto/workstreams/demo .oto/workstreams/.archived/demo 2>/dev/null` to clear any residual state

    ### Step 6: Create a new workspace

    - [ ] Run: `/oto-new-workspace --name uat-demo --repos . --strategy worktree`
    - [ ] **Expected:** A new workspace at the path the workflow chose (likely under `.worktrees/uat-demo` or similar — confirm via output). The target contains a `WORKSPACE.md` and an initialized `.oto/`
    - [ ] **Verify:** the workspace path printed in the output exists; `WORKSPACE.md` is present at it; `.oto/` is initialized at it
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]
    - [ ] **Rollback if needed:** `git worktree remove <workspace-path>` (if worktree strategy succeeded) or `rm -rf <workspace-path>` (if clone strategy)

    ### Step 7: List workspaces

    - [ ] Run: `/oto-list-workspaces`
    - [ ] **Expected:** uat-demo listed with its path and any metadata
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]

    ### Step 8: Remove the workspace

    - [ ] Run: `/oto-remove-workspace --name uat-demo`
    - [ ] **Expected:** uat-demo removed cleanly; parent project's `.oto/` is unchanged; `git worktree list` no longer shows uat-demo
    - [ ] **Verify:** `git worktree list` does not include uat-demo
    - [ ] **Verify:** `git status` shows no unexpected modifications to the parent repo's `.oto/`
    - [ ] **Result:**
          ```
          [paste output]
          ```
    - [ ] **Pass / Fail:** [ ]

    ## Outcome

    - [ ] All 8 steps pass → Phase 7 complete; commit `07-UAT.md` and proceed to Phase 8 readiness
    - [ ] One or more steps fail → Open a `07-NN-PLAN.md` hand-fixup plan; do NOT mark Phase 7 complete

    ## Operator Notes

    [Free-form notes from the UAT run — anything observed that warrants follow-up but is not a blocker]
    ```

    Notes for the writer:
    - Keep the wording exactly as above to give the operator unambiguous instructions
    - Each step has a `[ ] **Pass / Fail:** [ ]` line that the operator changes to `[x] **Pass / Fail:** [pass]` or `[!] **Pass / Fail:** [fail]`
    - The `${OTO_WS}` substring appears at least once (in Step 4 heading) so the must_haves grep check finds it
    - Date `YYYY-MM-DD` is a placeholder; the operator fills it in
    - The "Operator" line is the operator's name placeholder
  </action>
  <verify>
    <automated>test -f .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '\${OTO_WS}' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -c '^- \[ \]' .planning/phases/07-workstreams-workspaces-port/07-UAT.md | awk '$1 >= 20 {exit 0} {exit 1}' && grep -q '/oto-workstreams create demo' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-workstreams switch demo' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-workstreams list' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-workstreams complete demo' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-new-workspace' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-list-workspaces' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-remove-workspace' .planning/phases/07-workstreams-workspaces-port/07-UAT.md && grep -q '/oto-progress' .planning/phases/07-workstreams-workspaces-port/07-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-workstreams-workspaces-port/07-UAT.md` exists
    - File mentions all 8 step invocations: `/oto-workstreams create demo`, `/oto-workstreams switch demo`, `/oto-workstreams list`, `/oto-progress`, `/oto-workstreams complete demo`, `/oto-new-workspace`, `/oto-list-workspaces`, `/oto-remove-workspace` (each verifiable via grep)
    - File contains the literal `${OTO_WS}` (D-10 chaining check naming)
    - File contains at least 20 unchecked `- [ ]` items (preconditions + per-step Verify/Pass/Fail/Rollback lines)
    - File line count >= 80
    - File contains the string "Rollback" at least once per step that mutates state (steps 1, 5, 6 minimum)
    - No leak literals (`grep -E '\bgsd-|\bGSD_|Get Shit Done|\bsuperpowers\b|\bSuperpowers\b' 07-UAT.md` returns nothing — `.planning/` IS allowed in this file's body since it lives under `.planning/` and references its own location naturally)
  </acceptance_criteria>
  <done>
    `07-UAT.md` exists with the 8-step checklist, preconditions, rollback instructions per state-mutating step, and a final outcome decision block. Operator can run the UAT against this file alone.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the UAT and captures results</name>
  <what-built>
    Plans 01-04 ship: leak-clean code surface, Workflow Deference directive on `using-git-worktrees`, 4 passing `node:test` files (smoke + behavior). The 8 user-facing slash commands `/oto-workstreams create|switch|list|complete`, `/oto-new-workspace`, `/oto-list-workspaces`, `/oto-remove-workspace` and the `${OTO_WS}` propagation are all wired and unit-covered. This checkpoint confirms they actually work end-to-end on a real Claude Code session.
  </what-built>
  <how-to-verify>
    1. Open a Claude Code session in this repo
    2. Open `.planning/phases/07-workstreams-workspaces-port/07-UAT.md` in your editor alongside the Claude Code session
    3. Run each of the 8 steps in order, exactly as written
    4. After each step, fill in the "Result" block with the relevant output and mark Pass / Fail
    5. The critical step is Step 4 (`${OTO_WS}` chaining check) — `/oto-progress` after `workstream switch demo` MUST report from the demo workstream's state, not the actual oto project's state. If it reports oto's actual progress (Phase 6 complete, etc.), the routing flag did NOT propagate → FAIL the UAT
    6. After Step 8, fill in the "Outcome" block: all-pass → mark Phase 7 complete; any-fail → open a hand-fixup plan
    7. Commit `07-UAT.md` with the filled-in results

    Resume signal:
    - Type "approved" if all 8 steps pass and `07-UAT.md` is updated and committed
    - Type "fail: <step number> <description>" if a step fails (the orchestrator will then open a hand-fixup plan)
  </how-to-verify>
  <resume-signal>
    "approved" (all pass) | "fail: <step> <desc>" (any failure)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Claude Code session | UAT runs in the actual Claude Code runtime against the real installed oto; trust boundary is the operator's authority over the session |
| UAT commands → repo's real `.oto/` | UAT mutates `.oto/workstreams/demo/` (creates, then completes/archives); rollback documented per step |
| UAT commands → working tree | `/oto-new-workspace` may create a worktree at `.worktrees/uat-demo` — git-tracked, removable via documented rollback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | UAT mid-run failure leaves repo with stale `.oto/workstreams/demo` or stale worktree | mitigate | Each state-mutating step (1, 5, 6) has explicit rollback instructions; Step 5 (`workstream complete`) auto-archives if step 1 succeeded; the operator is instructed to run rollback before stopping the UAT on any failure |
| T-07-12 | Information disclosure | UAT result file committed with sensitive paths or output | accept | UAT operates on the user's own repo; output captured in `07-UAT.md` is paths the operator already knows; no PII or secrets involved in `/oto-*` command output |
| T-07-13 | Denial of service | `/oto-new-workspace` fails partway and leaves a half-initialized worktree | mitigate | Step 6 rollback explicitly covers `git worktree remove` (worktree strategy) or `rm -rf <path>` (clone strategy); Step 8 (remove-workspace) is the documented happy-path cleanup |
</threat_model>

<verification>
1. Task 1: `test -f .planning/phases/07-workstreams-workspaces-port/07-UAT.md` and the grep checks above all pass
2. Task 2: operator returns "approved" via the resume signal
3. After "approved", `git log --oneline -5` shows a commit containing `07-UAT.md` with filled-in results
</verification>

<success_criteria>
- `07-UAT.md` written by Plan 05 Task 1
- Operator runs all 8 steps and records results
- All 8 steps pass (or any failure routes to hand-fixup plan, which is itself the documented escape valve, not a Phase 7 close blocker if the fixup lands later)
- D-10 chaining check (Step 4) explicitly passes: `/oto-progress` after `workstream switch demo` reports demo's state, not oto's actual state
</success_criteria>

<output>
After completion, create `.planning/phases/07-workstreams-workspaces-port/07-05-SUMMARY.md` recording: UAT file path, operator name, run date, per-step pass/fail (8 entries), final Outcome block content, any hand-fixup plans opened.
</output>
