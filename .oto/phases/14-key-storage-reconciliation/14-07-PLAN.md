---
phase: 14-key-storage-reconciliation
plan: 07
type: execute
wave: 2
depends_on: [14-05]
files_modified:
  - oto/bin/lib/core.cjs
  - oto/bin/lib/config.cjs
  - tests/14-migration-hardening.test.cjs
autonomous: true
gap_closure: true
requirements: [SECR-01, SECR-02, SECR-03]

must_haves:
  truths:
    - "CJS config-set on an integration key migrates any legacy string to a 0600 keyfile BEFORE overwriting it — setting exa_search=true on a legacy-string config creates ~/.oto/exa_api_key containing that string"
    - "If legacy migration cannot complete (keyfile base unusable), CJS config-set on an integration key exits non-zero WITHOUT modifying config.json (fail closed — the only credential is not overwritten)"
    - "When OTO_WORKSTREAM is set, the ROOT config layer is migrated before it is parsed: a legacy string in root .oto/config.json self-heals to a keyfile even when a workstream config exists"
    - "The CJS loadConfig return value never contains an integration string, even when file migration failed (in-memory scrub)"
  artifacts:
    - path: "oto/bin/lib/core.cjs"
      provides: "root-layer migration before root parse in loadConfig + integration string scrub"
      contains: "migrateLegacyIntegrationKeys(rootConfigPath)"
    - path: "oto/bin/lib/config.cjs"
      provides: "migrate-before-overwrite + fail-closed guard in cmdConfigSet for integration keys"
    - path: "tests/14-migration-hardening.test.cjs"
      provides: "node:test coverage: legacy-overwrite (×3), migration-failure fail-closed, root-plus-workstream migration"
      min_lines: 70
  key_links:
    - from: "oto/bin/lib/config.cjs"
      to: "oto/bin/lib/secrets.cjs"
      via: "migrateLegacyIntegrationKeys call inside cmdConfigSet before setConfigValue"
      pattern: "migrateLegacyIntegrationKeys"
    - from: "oto/bin/lib/core.cjs"
      to: "oto/bin/lib/secrets.cjs"
      via: "migrateLegacyIntegrationKeys(rootConfigPath) inside the ws branch of loadConfig"
      pattern: "rootConfigPath"
---

<objective>
Close the CJS half of verification Gap 2 (CR-04 + legacy-overwrite): "Migration is fail-open and path-incomplete." `oto/bin/lib/core.cjs` parses the ROOT config (lines 327-334) before migrating only the workstream config path (lines 337-341), so a legacy root string survives when a workstream is active. `cmdConfigSet` in `oto/bin/lib/config.cjs` overwrites a legacy integration string with a boolean WITHOUT first migrating it — the verifier reproduced that setting a legacy `exa_search` string to `true` creates no keyfile and silently destroys the only stored credential.

Purpose: SECR-03 requires every legacy string to self-heal to a keyfile; no mutation may destroy it and no loader may return it.
Output: root-layer migration before parse, migrate-before-overwrite with fail-closed guard in cmdConfigSet, loader scrub, and regression tests for all three reproduced failures.

Sync hygiene: core.cjs and config.cjs are GSD-shared — keep each hook to a few commented lines.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/14-key-storage-reconciliation/14-CONTEXT.md
@.oto/phases/14-key-storage-reconciliation/14-VERIFICATION.md
@oto/bin/lib/core.cjs
@oto/bin/lib/config.cjs
@docs/upstream-sync.md

<interfaces>
<!-- Extracted from codebase — no exploration needed. -->

From oto/bin/lib/core.cjs `loadConfig(cwd)` (defect site, lines 321-341):
```js
const ws = process.env.OTO_WORKSTREAM || null;
let rootParsed = null;
if (ws) {
  const rootConfigPath = path.join(planningRoot(cwd), 'config.json');
  try {
    const raw = fs.readFileSync(rootConfigPath, 'utf-8');   // ← parsed BEFORE any migration
    rootParsed = JSON.parse(raw);
  } catch { /* root config missing or unparseable */ }
}
const configPath = path.join(planningDir(cwd), 'config.json');
// existing Phase 14 hook migrates ONLY configPath (the workstream path when ws is set):
try { require('./secrets.cjs').migrateLegacyIntegrationKeys(configPath); } catch { }
```
`planningDir(cwd)` routes to `.oto/workstreams/<ws>/` when OTO_WORKSTREAM is set (line 912); `planningRoot(cwd)` is always root `.oto/`.

From oto/bin/lib/config.cjs `cmdConfigSet(cwd, keyPath, value, raw)` (defect site, lines 319-388):
- Phase 14 validation block at lines 339-344 (`isSecretKey` → `validateIntegrationValue` reject / `warnIfNoKeyDetected`).
- Then `setConfigValue(cwd, keyPath, parsedValue)` (line 369) captures previousValue and OVERWRITES — a legacy string previousValue is masked in output (lines 371-385) but the string is destroyed without a keyfile.
- Already imported: `planningDir` (line 7 via core.cjs require), `migrateLegacyIntegrationKeys` (line 19 from secrets.cjs), `isSecretKey`, `maskSecret`.

From oto/bin/lib/secrets.cjs:
- `migrateLegacyIntegrationKeys(configPath, baseDir)` — sync; returns `{migrated:[]}` on missing/malformed config (safe no-op); THROWS only when writeKeyfile fails (e.g. `$HOME/.oto` is a regular file → mkdirSync EEXIST/ENOTDIR); atomically rewrites config with booleans; keyfile-wins conflict; masked notices + rotation warning.

From tests/14-config-boolean.test.cjs:
- `seedFixture(t, config)` creates temp project (with `.oto/config.json` seeded) + temp home; `cleanEnv(home)` sets HOME + empty provider env vars; `runConfig(fixture, ...args)` spawns `oto/bin/lib/oto-tools.cjs`.
- This file already contains a spawned-process test that exercises the core.cjs loadConfig migration hook — reuse the SAME trigger command for the workstream test (read the file to find it; likely `resolve-model` or `state`-family).
- CJS workstream selection: set `OTO_WORKSTREAM=<name>` in the spawned env (core.cjs line 325) — no --ws flag needed for these tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cmdConfigSet — migrate legacy strings before overwrite, fail closed on migration failure</name>
  <files>oto/bin/lib/config.cjs, tests/14-migration-hardening.test.cjs</files>
  <read_first>
    - oto/bin/lib/config.cjs (cmdConfigSet lines 319-388; the Phase 14 block at 339-344 is the anchor for the new hook)
    - oto/bin/lib/secrets.cjs (migrateLegacyIntegrationKeys throw semantics — throws only on keyfile-write failure; returns {migrated:[]} on parse problems)
    - tests/14-config-boolean.test.cjs (seedFixture/cleanEnv/runConfig helpers to replicate in the new test file)
  </read_first>
  <behavior>
    - Test (×3 integrations): config `{"exa_search":"sk-test-exa-0123456789", ...}`; run `config-set exa_search true` → exit 0; `$HOME/.oto/exa_api_key` exists containing the marker with `(mode & 0o777) === 0o600`; config.json on disk has `exa_search === true` (boolean); neither stdout nor stderr contains the full marker
    - Test: same legacy config but `$HOME/.oto` pre-created as a regular FILE (so mkdirSync inside writeKeyfile throws) → `config-set exa_search true` exits non-zero; config.json on disk is byte-identical to before (string untouched, nothing overwritten); combined output contains "migration failed" and does NOT contain the marker
    - Test: non-integration key unaffected — `config-set model_profile quality` on the same broken-home fixture still exits 0 (fail-closed guard applies only to integration keys)
  </behavior>
  <action>
    In `oto/bin/lib/config.cjs` `cmdConfigSet`, extend the existing Phase 14 block (after line 343 `warnIfNoKeyDetected`, before the context-value validation) with a migrate-before-overwrite guard:
    ```js
    // OTO Phase 14 gap-closure (SECR-03): keyfile any legacy string BEFORE overwriting it; fail closed if migration cannot complete.
    if (isSecretKey(keyPath)) {
      try {
        migrateLegacyIntegrationKeys(path.join(planningDir(cwd), 'config.json'));
      } catch {
        error(`${keyPath}: legacy key migration failed — config not modified (fix ~/.oto permissions and retry)`);
      }
    }
    ```
    Notes: `migrateLegacyIntegrationKeys`, `isSecretKey`, `path`, `planningDir`, and `error` are all already imported/required in config.cjs — no new imports. `error()` exits 1 before `setConfigValue` runs, so the legacy string is never overwritten on failure. On success, the legacy string is already in the keyfile and config already holds `true` when setConfigValue runs (idempotent re-set).

    Create `tests/14-migration-hardening.test.cjs` with the behavior cases, replicating seedFixture/cleanEnv/runConfig from tests/14-config-boolean.test.cjs. For the broken-home case: `fs.writeFileSync(path.join(home, '.oto'), 'not-a-dir')` BEFORE running the command (home tmpdir exists but `.oto` is a file). Byte-identical assertion: read config.json before and after, `assert.equal(after, before)`.
  </action>
  <verify>
    <automated>node --test tests/14-migration-hardening.test.cjs tests/14-config-boolean.test.cjs && node --check oto/bin/lib/config.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "legacy key migration failed — config not modified" oto/bin/lib/config.cjs` matches exactly one line
    - In cmdConfigSet, the `migrateLegacyIntegrationKeys` call appears on a LOWER line number than the `setConfigValue(cwd, keyPath, parsedValue)` call
    - tests/14-migration-hardening.test.cjs exists and contains `exa_search`, `brave_search`, `firecrawl`, `0o600`, and a before/after byte-equality assertion on config.json
    - `node --test tests/14-migration-hardening.test.cjs` exits 0 with >= 5 passing tests
    - config.cjs diff for this task is <= 10 changed lines (sync hygiene)
  </acceptance_criteria>
  <done>Boolean config-set can no longer destroy a legacy credential: the string is keyfiled first, and if keyfiling fails the write is refused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: core.cjs loadConfig — migrate the root layer before parsing it; scrub integration strings from the loader return</name>
  <files>oto/bin/lib/core.cjs, tests/14-migration-hardening.test.cjs</files>
  <read_first>
    - oto/bin/lib/core.cjs (loadConfig lines 321-355: ws branch, rootConfigPath read at 328-334, existing migration hook at 340-341, fileData parse at 344-347; planningDir/planningRoot at ~902-930)
    - tests/14-config-boolean.test.cjs (find the exact spawned command it uses to exercise the core loadConfig migration hook — reuse that command for the workstream test)
    - .oto/phases/14-key-storage-reconciliation/14-01-SUMMARY.md (core.cjs hook was 3 changed lines — keep the same discipline)
  </read_first>
  <behavior>
    - Test (root-plus-workstream, CR-04 reproduction): project with root `.oto/config.json` = `{"exa_search":"sk-test-root-0123456789"}` and `.oto/workstreams/ws1/config.json` = `{"exa_search": false}`; spawn the loadConfig-triggering command with `OTO_WORKSTREAM=ws1` in env → after the run, root config.json has `exa_search === true` (boolean), `$HOME/.oto/exa_api_key` exists containing `sk-test-root-0123456789`, and no output contains the full marker
    - Test (missing-workstream variant): same root config, `OTO_WORKSTREAM=ws2` where `.oto/workstreams/ws2/` has no config.json → root string still migrated to keyfile + boolean
    - Test (scrub): root config legacy string + `$HOME/.oto` as a regular file (migration impossible) → the command still exits without printing the plaintext marker anywhere (loader scrub means no consumer output can carry it)
  </behavior>
  <action>
    In `oto/bin/lib/core.cjs` `loadConfig`, two small commented edits:

    1. Root-layer migration BEFORE the root read (insert immediately after `const rootConfigPath = ...` at line 328, before the `fs.readFileSync(rootConfigPath, ...)` try):
       ```js
       // OTO Phase 14 gap-closure (SECR-03): heal legacy root strings before parsing the root layer (CR-04).
       try { require('./secrets.cjs').migrateLegacyIntegrationKeys(rootConfigPath); } catch { /* never block config load */ }
       ```

    2. In-memory scrub so plaintext can never leave the loader even when file migration failed. Add a tiny local helper near `_deepMergeConfig`:
       ```js
       // OTO Phase 14 gap-closure (SECR-01): loader contract is boolean-only for integration flags.
       function _scrubIntegrationStrings(obj) {
         if (!obj || typeof obj !== 'object') return obj;
         for (const k of ['exa_search', 'brave_search', 'firecrawl']) {
           if (typeof obj[k] === 'string') obj[k] = true;
         }
         return obj;
       }
       ```
       Apply it at exactly two points: `rootParsed = _scrubIntegrationStrings(JSON.parse(raw));` (line 331) and `const fileData = _scrubIntegrationStrings(JSON.parse(raw));` (line 347). A surviving string means "a key was configured" → `true` matches the migration semantic (migrateLegacyIntegrationKeys also sets `true`).

    Extend `tests/14-migration-hardening.test.cjs` with the three behavior cases. Fixture: create `.oto/workstreams/ws1/` under the temp project and seed both config layers; spawn with `env: { ...cleanEnv(home), OTO_WORKSTREAM: 'ws1' }` using the same trigger command tests/14-config-boolean.test.cjs uses for its loadConfig hook test.
  </action>
  <verify>
    <automated>node --test tests/14-migration-hardening.test.cjs tests/14-config-boolean.test.cjs tests/14-secrets-keyfile.test.cjs && node --check oto/bin/lib/core.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "migrateLegacyIntegrationKeys" oto/bin/lib/core.cjs` == 2 (existing workstream-path hook + new root hook)
    - The new root hook appears on a LOWER line number than `rootParsed = ` (migration precedes parse)
    - `grep -c "_scrubIntegrationStrings" oto/bin/lib/core.cjs` == 3 (definition + two call sites)
    - `node --test tests/14-migration-hardening.test.cjs` exits 0, including a test whose fixture contains `.oto/workstreams/ws1/config.json`
    - core.cjs diff for this task is <= 15 changed lines (sync hygiene)
  </acceptance_criteria>
  <done>Enabling a workstream no longer defeats read-time reconciliation: every effective CJS config layer is migrated before parse, and the loader can never return an integration string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-tracked .oto/config.json → loader/mutation output | Legacy plaintext keys in tracked files must not survive reads or be destroyed by writes |
| root config layer ↔ workstream config layer | Workstream indirection must not bypass root-layer self-healing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Denial of Service (credential destruction) | cmdConfigSet on legacy string | mitigate | migrate-before-overwrite; fail-closed error() when migration cannot complete |
| T-14-07-02 | Information Disclosure | root config parsed unmigrated under OTO_WORKSTREAM | mitigate | migrateLegacyIntegrationKeys(rootConfigPath) before readFileSync |
| T-14-07-03 | Information Disclosure | loader returns string when file migration fails | mitigate | _scrubIntegrationStrings on both parsed layers — plaintext never leaves loadConfig |
| T-14-07-04 | Information Disclosure | error/notice output on failure paths | mitigate | fail-closed message contains keyPath only; tests assert marker absent from stdout+stderr |
| T-14-07-05 | Tampering | migration race with concurrent config writes (WR-02) | accept | warning-level pre-existing concern, not in this gap's scope; single-user CLI, low collision probability |
</threat_model>

<verification>
- `node --test tests/14-migration-hardening.test.cjs tests/14-config-boolean.test.cjs tests/14-secrets-keyfile.test.cjs tests/14-no-plaintext-guard.test.cjs` — all pass
- Reproduce and confirm closed: (a) legacy-overwrite probe now creates a keyfile, (b) CR-04 root-plus-workstream probe now migrates root, (c) broken-home probe refuses the write
- `node --check` clean for core.cjs and config.cjs; shared-file diffs small and commented
</verification>

<success_criteria>
- All CJS artifact issues from Gap 2 closed: core.cjs root migration, cmdConfigSet legacy overwrite
- Migration-failure, root-plus-workstream, and legacy-overwrite regression tests exist in tests/14-migration-hardening.test.cjs
- Sync-hygiene budgets respected on both shared files
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-07-SUMMARY.md`
</output>
