---
phase: 14-key-storage-reconciliation
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - .oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt
  - oto/bin/lib/core.cjs
  - sdk/src/config.ts
  - sdk/src/cli.ts
  - sdk/src/cli.test.ts
  - tests/14-loader-scrub.test.cjs
  - sdk/src/config-loader-parity.test.ts
autonomous: true
gap_closure: true
requirements: [SECR-01, SECR-04]

must_haves:
  truths:
    - "BASELINE: the full-SDK-suite failure baseline is captured to 14-SDK-BASELINE.txt from the PRE-gap-closure tree (this task runs before any production edit in this plan), recording failed/passed counts and the list of failed test files — the plan-14-19 terminal gate diffs against it"
    - "CLOSES WR-05: both effective-view scrubbers (CJS core.cjs _scrubIntegrationStrings and SDK config.ts scrubIntegrationStrings) normalize EVERY present non-boolean integration value to Boolean(value) — an object, number, null, or array value can no longer escape a loader when migration fails, and no embedded marker is ever returned verbatim"
    - "CLOSES WR-09: SDK loadConfig deep-merges root config under a present workstream config (parse + scrub BOTH layers, root then workstream, then hardcoded defaults) — root-only settings like commit_docs:false survive when a workstream config exists, matching CJS core.cjs semantics"
    - "CLOSES IR-02: importing sdk/src/cli.ts no longer executes main() — the CLI runs only when invoked directly, and cli.test.ts imports without argv parsing, usage errors, or process.exitCode pollution"
  artifacts:
    - path: ".oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt"
      provides: "Full `npx vitest run` output + extracted summary (failed/passed counts, failed-file list) from the pre-gap-closure tree"
      min_lines: 20
    - path: "oto/bin/lib/core.cjs"
      provides: "_scrubIntegrationStrings normalizing any present non-boolean to Boolean(value) on the effective view only"
      contains: "Boolean(obj[k])"
    - path: "sdk/src/config.ts"
      provides: "scrubIntegrationStrings non-boolean normalization + root→workstream deep merge in loadConfig"
      contains: "deepMergeConfig"
    - path: "sdk/src/cli.ts"
      provides: "Direct-entry guard around main()"
      contains: "pathToFileURL"
    - path: "tests/14-loader-scrub.test.cjs"
      provides: "CJS forced-migration-failure regressions: object/number/null/array integration values scrubbed from the effective view, disk bytes pristine"
      min_lines: 50
    - path: "sdk/src/config-loader-parity.test.ts"
      provides: "SDK scrub regressions + root/workstream inheritance parity tests"
      min_lines: 60
  key_links:
    - from: "sdk/src/config.ts loadConfig"
      to: "root config layer"
      via: "deep merge of scrubbed root under scrubbed workstream config when both exist"
      pattern: "deepMergeConfig"
    - from: "oto/bin/lib/core.cjs loadConfig"
      to: "_scrubIntegrationStrings"
      via: "effective-view normalization (fileData kept pristine for dirty writes)"
      pattern: "_scrubIntegrationStrings"
    - from: "bin/oto-sdk.js"
      to: "sdk/src/cli.ts main()"
      via: "spawns cli.js as process.argv[1], satisfying the direct-entry guard"
      pattern: "main\\(\\)"
---

<objective>
Three responsibilities: capture the full-SDK-suite baseline the terminal gate needs (required amendment 1), and close WR-05, WR-09, and IR-02 from 14-REVIEW.md.

BASELINE: the full SDK suite carries substantial pre-existing debt unrelated to Phase 14 (advisory numbers as of 2026-07-12: ~267 failed / ~1228 passed across ~41 failed files — treat as approximate until this task records the authoritative baseline). Phase 14 must not be gated on debt it does not own; it must instead prove it adds NO NEW failures. This plan records the authoritative pre-gap-closure baseline; plan 14-19's terminal gate re-runs the full suite and asserts no test file fails that was not already failing, and no previously-failing file gained failures.

WR-05 (verified at core.cjs:321-327 and sdk/src/config.ts:160-165): both effective-view scrubbers normalize STRINGS only (`if (typeof obj[k] === 'string') obj[k] = true`). Migration normally coerces other values, but when its rewrite fails (e.g. unwritable config), an object or number is returned unchanged despite the loaders' boolean type contract — the review reproduced the CJS path returning an object with its embedded marker verbatim.

WR-09 (verified at sdk/src/config.ts:166-235): CJS core.cjs deep-merges root config under a present workstream config (rootParsed at :334-339, merge at :418). SDK loadConfig reads root ONLY as a fallback when the workstream file is absent; when both exist, root-only settings are replaced by hardcoded defaults — reproduced with root `commit_docs:false` + workstream `model_profile:'quality'` returning `commit_docs:true`. Disposition: FIX NOW (the CJS semantics are the executable spec; deferring would leave a live SDK/CJS divergence inside Phase 14's own config surface).

IR-02 (verified at sdk/src/cli.ts:696): unconditional `main()` means importing helpers from cli.ts parses the host argv, prints usage errors, and sets process.exitCode — cli.test.ts currently emits this noise even when all assertions pass. `bin/oto-sdk.js` spawns `node sdk/dist/cli.js ...` (verified), so cli.js IS process.argv[1] in real use and a direct-entry guard preserves the CLI exactly.

Purpose: SECR-01's boolean-only loader contract must hold on every return path under every failure; SECR-04's config surface must read the config the user actually wrote (root inheritance); and the phase's terminal gate needs an honest baseline.
Output: baseline artifact, hardened scrubbers in both loaders, SDK root inheritance, guarded CLI entry, regression suites. sdk/dist rebuild deferred to plan 14-19.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/14-key-storage-reconciliation/14-REVIEW.md
@oto/bin/lib/core.cjs
@sdk/src/config.ts
@sdk/src/cli.ts
</context>

<interfaces>
Current scrubbers (verified 2026-07-12):

`oto/bin/lib/core.cjs:321-327`:
```js
function _scrubIntegrationStrings(obj) {
  if (!obj || typeof obj !== 'object') return obj;
  for (const k of ['exa_search', 'brave_search', 'firecrawl']) {
    if (typeof obj[k] === 'string') obj[k] = true;   // ← objects/numbers/null/arrays escape
  }
  return obj;
}
```
Applied to the EFFECTIVE VIEW only at core.cjs:~418 (`const parsed = _scrubIntegrationStrings(rootParsed ? _deepMergeConfig(rootParsed, fileData) : { ...fileData })`) — `fileData` stays pristine for dirty writes (the 14-10 fix). Keep that separation.

`sdk/src/config.ts:160-165`: same string-only pattern, applied at three return paths (two pre-project fallbacks + the parsed-project path at :232).

Boolean semantics to implement (matches migration's own coercion at secrets.cjs:201 / secrets.ts:307): for each of the three keys, when the key is PRESENT (`Object.prototype.hasOwnProperty.call(obj, k)`) and not boolean → `obj[k] = Boolean(obj[k])`. Notes: non-empty string → true (unchanged), `''` → false (consistent with 14-13: an empty value is not a credential), object/array → true (a value existed → enabled), null/0 → false. Never materialize absent keys.

WR-09 reference semantics (CJS core.cjs:330-421): when a workstream is active, parse + scrub ROOT config, parse + scrub WORKSTREAM config, deep-merge root under workstream (`_deepMergeConfig(rootParsed, fileData)` — overlay objects merge recursively, scalars/arrays replace), then apply hardcoded defaults. SDK loadConfig must mirror: when `workstream` is set AND both files parse, `parsed = deepMergeConfig(scrub(rootParsed), scrub(wsParsed))` before `mergeDefaults(parsed)`. Implement a local `deepMergeConfig` in config.ts mirroring `_deepMergeConfig` (core.cjs:309-318). The existing single-layer and fallback paths keep their current behavior (config.test.ts must stay green).

IR-02 fix shape (ESM):
```typescript
import { pathToFileURL } from 'node:url';
// ─── Auto-run only when invoked directly (IR-02) ───
const isDirectEntry = process.argv[1] !== undefined
  && import.meta.url === pathToFileURL(process.argv[1]).href;
if (isDirectEntry) main();
```

Forced-migration-failure seam for WR-05 tests: seed a config whose integration key holds an OBJECT (e.g. `{"exa_search":{"nested":"scrub-marker-0123456789"}}`); make the migration rewrite fail by chmod'ing the config's directory to 0o555 (read+exec, no write) so atomicWrite cannot land — loadConfig catches the migration error and proceeds to parse. Restore permissions in t.after/afterEach before rmSync. CJS loadConfig(cwd) reads `.oto/config.json` under cwd; SDK loadConfig(projectDir) likewise.

Existing fixtures to mirror: tests/14-loader-credential-survival.test.cjs (CJS loadConfig fixture + captureStderr), sdk/src/config.test.ts (SDK loadConfig tmpdir fixture).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the full-SDK-suite baseline BEFORE any edits</name>
  <files>.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt</files>
  <read_first>
    - sdk/package.json scripts block (confirm vitest invocation)
  </read_first>
  <action>
    FIRST action of this plan, before any production edit:

    1. `cd sdk && npx vitest run > /tmp/vitest-baseline-raw.txt 2>&1 || true` (the suite is expected to be red — do not treat a non-zero exit as task failure).
    2. Write `.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt` (use the Write tool) with three sections:
       - `## Summary` — the vitest tail summary lines (Test Files / Tests counts) copied verbatim, plus the capture timestamp and the HEAD commit sha (`git rev-parse HEAD`).
       - `## Failed Files` — one line per failing test file path, extracted from the run output (vitest marks failing files; `grep` the raw output for the failure markers and normalize to `src/...` paths, sorted, deduplicated). For each failed file include its failed-test count when the reporter provides it.
       - `## Raw Tail` — the last ~200 lines of the raw output for auditability.
    3. Record for context: the advisory pre-capture numbers from the planning session were ~267 failed / ~1228 passed across ~41 failed files (2026-07-12); if the captured numbers differ materially, note the delta in the file — the CAPTURED numbers are authoritative.

    This artifact is the comparison base for plan 14-19's terminal gate: "no test file fails that was not already in the baseline, and no previously-failing file's failure count increased."
  </action>
  <verify>
    <automated>test -s .oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt && grep -c "## Failed Files" .oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt</automated>
  </verify>
  <acceptance_criteria>
    - 14-SDK-BASELINE.txt exists, contains `## Summary`, `## Failed Files`, and `## Raw Tail` sections, the HEAD sha, and at least one `.test.ts` path under Failed Files (the suite is known-red) — or, if the suite is unexpectedly green, an explicit `NONE` marker under Failed Files
    - The file was created BEFORE any other file in this plan was modified (it is the plan's first commit-worthy artifact)
  </acceptance_criteria>
  <done>The authoritative pre-gap-closure baseline exists and is diffable by plan 14-19.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-05 — normalize every non-boolean integration value in both effective-view scrubbers</name>
  <files>oto/bin/lib/core.cjs, sdk/src/config.ts, tests/14-loader-scrub.test.cjs, sdk/src/config-loader-parity.test.ts</files>
  <read_first>
    - oto/bin/lib/core.cjs lines 305-430 (_deepMergeConfig, _scrubIntegrationStrings, loadConfig's migration calls at 339/352 and the effective-view scrub at ~418; fileData/dirty-write separation)
    - sdk/src/config.ts lines 130-260 (loadUserDefaults, scrubIntegrationStrings, loadConfig's three return paths, mergeDefaults)
    - tests/14-loader-credential-survival.test.cjs (CJS loadConfig fixture conventions)
    - sdk/src/config.test.ts (SDK fixture conventions — do NOT edit this file)
  </read_first>
  <behavior>
    CJS (tests/14-loader-scrub.test.cjs), each with migration forced to fail via a 0o555 config directory:
    - Test 1: `exa_search: {"nested":"scrub-marker-0123456789"}` → `loadConfig(cwd).exa_search === true`; JSON.stringify of the returned config does NOT contain `scrub-marker`; on-disk bytes unchanged
    - Test 2: `brave_search: 42` → `true`; Test 3: `firecrawl: null` → `false`; Test 4: `exa_search: ["scrub-marker-0123456789"]` → `true`, marker absent from the returned view
    - Test 5 (string regression guard): `exa_search: "scrub-marker-0123456789"` → `true`, marker absent (existing behavior preserved)
    - Test 6 (absent key untouched): config without `firecrawl` → the returned view's `firecrawl` comes from defaults, and the on-disk file still has no `firecrawl` key

    SDK (sdk/src/config-loader-parity.test.ts, scrub describe block): mirror Tests 1-5 against `loadConfig(projectDir)` with the same 0o555 forced-failure seam and stubbed HOME.
  </behavior>
  <action>
    **oto/bin/lib/core.cjs** — replace `_scrubIntegrationStrings`'s loop body (GSD-shared file, keep the diff commented):

    ```js
    // OTO Phase 14 gap-closure (WR-05 / SECR-01): the loader contract is
    // boolean-only for integration flags on the EFFECTIVE view. Migration
    // normally coerces non-booleans, but if its rewrite fails the raw value
    // would escape — normalize every present non-boolean to Boolean(value).
    // (Non-empty string → true, '' → false, object/array → true, null/0 → false.)
    for (const k of ['exa_search', 'brave_search', 'firecrawl']) {
      if (Object.prototype.hasOwnProperty.call(obj, k) && typeof obj[k] !== 'boolean') {
        obj[k] = Boolean(obj[k]);
      }
    }
    ```

    Note: `Boolean('')` is false while the old string-only scrub mapped `''` to true — the new mapping matches migration's own coercion (secrets.cjs:201) and 14-13's "empty is not a credential" rule; pin it in Test 5's family if an `''` case is added. Do NOT touch the fileData/dirty-write separation or the scrub call site.

    **sdk/src/config.ts** — apply the identical normalization inside `scrubIntegrationStrings` (same comment, TS syntax). All three existing call sites inherit it.

    **Tests** — implement the behaviors above. CJS fixture: mkdtemp cwd with `.oto/config.json`; `fs.chmodSync(otoDir, 0o555)` before loadConfig, `t.after(() => fs.chmodSync(otoDir, 0o755))` BEFORE the rmSync cleanup registered by the fixture. Assert disk bytes with a pre-captured string compare. SDK: same seam under vitest with `vi.stubEnv('HOME', tmpHome)` and the three integration env vars stubbed `''`.
  </action>
  <verify>
    <automated>node --test tests/14-loader-scrub.test.cjs && cd sdk && npx vitest run src/config-loader-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/_scrubIntegrationStrings/,/^}/p' oto/bin/lib/core.cjs | grep -c "Boolean(obj\[k\])"` returns 1; same check passes for the TS scrubber in sdk/src/config.ts
    - `node --test tests/14-loader-scrub.test.cjs` exits 0 with ≥ 6 passing tests; the file contains `scrub-marker-0123456789` and `0o555`
    - `node --test tests/14-loader-credential-survival.test.cjs tests/14-migration-hardening.test.cjs` exits 0 (fileData pristineness unbroken)
    - `cd sdk && npx vitest run src/config.test.ts` exits 0 (existing loader suite unbroken)
  </acceptance_criteria>
  <done>No non-boolean integration value — object, number, null, array, or string — can escape either loader's effective view, even under forced migration failure; disk bytes stay pristine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-09 — SDK root→workstream inheritance + IR-02 — guard cli.ts auto-run</name>
  <files>sdk/src/config.ts, sdk/src/cli.ts, sdk/src/cli.test.ts, sdk/src/config-loader-parity.test.ts</files>
  <read_first>
    - sdk/src/config.ts loadConfig (full function — the workstream/root read paths at 166-235) and mergeDefaults
    - oto/bin/lib/core.cjs lines 309-318 (_deepMergeConfig — the reference implementation) and 330-345 + ~410-420 (rootParsed load + merge ordering)
    - sdk/src/cli.ts lines 680-700 (main() invocation at :696) and the top-of-file imports
    - sdk/src/cli.test.ts lines 1-40 (how the module is imported; the current noise the fix removes)
    - bin/oto-sdk.js (confirms cli.js is spawned as process.argv[1] — the direct-entry guard preserves the real CLI)
  </read_first>
  <behavior>
    Inheritance describe block (config-loader-parity.test.ts):
    - Test I1 (the WR-09 reproduction): root `.oto/config.json` = `{"commit_docs":false}`, workstream `.oto/workstreams/ws1/config.json` = `{"model_profile":"quality"}` → `await loadConfig(projectDir, 'ws1')` returns `commit_docs === false` AND `model_profile === 'quality'`
    - Test I2 (nested inheritance): root `{"workflow":{"research":false}}`, ws `{"workflow":{"plan_check":false}}` → both `workflow.research === false` and `workflow.plan_check === false`
    - Test I3 (workstream override wins): root `{"model_profile":"budget"}`, ws `{"model_profile":"quality"}` → `'quality'`
    - Test I4 (fallback regression): workstream named but its config file absent → root config used (existing behavior preserved)
    - Test I5 (scrub on both layers): root `{"exa_search":"root-marker-0123456789"}` (migration force-failed), ws `{}` → effective `exa_search === true`, marker absent

    IR-02 (cli.test.ts additions):
    - Test C1: after importing the cli module, `process.exitCode` is unset/0 (no auto-run on import)
    - Test C2 (guard shape): the module exports/behavior used by existing cli.test.ts specs still pass unchanged
  </behavior>
  <action>
    **sdk/src/config.ts**:
    1. Add a local `deepMergeConfig(base, overlay)` mirroring core.cjs `_deepMergeConfig` (309-318): plain-object overlay values merge recursively; scalars/arrays/null replace.
    2. In `loadConfig`, restructure the workstream branch: when `workstream` is set, ALSO attempt to read + parse the ROOT config (it is already migration-healed at :173-175). When both parse to plain objects: `parsed = deepMergeConfig(scrubIntegrationStrings(rootParsed), scrubIntegrationStrings(wsParsed))`. When only one exists: current behavior. When neither: current pre-project fallback. Keep the existing malformed-JSON throw semantics for whichever file fails to parse (do not widen fail-open behavior). `mergeDefaults(parsed)` stays the final step.
    3. Do NOT change the no-workstream path.

    **sdk/src/cli.ts** (IR-02): add `pathToFileURL` to the `node:url` import (or add the import), and replace the unconditional `main();` at :696 with:

    ```typescript
    // ─── Auto-run only when invoked directly (IR-02) ─────────────────────────
    // bin/oto-sdk.js spawns this file as process.argv[1]; importing it (tests,
    // programmatic use) must not parse argv or set process.exitCode.
    const isDirectEntry = process.argv[1] !== undefined
      && import.meta.url === pathToFileURL(process.argv[1]).href;
    if (isDirectEntry) main();
    ```

    **sdk/src/cli.test.ts**: add Test C1 (`expect(process.exitCode ?? 0).toBe(0)` in a spec that runs after the top-level import) and confirm the pre-existing usage-error noise no longer appears in the run output. Do not restructure existing specs.

    Add the inheritance describe block (I1-I5) to `sdk/src/config-loader-parity.test.ts` (same file as Task 2 — single new SDK test file for this plan).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/config-loader-parity.test.ts src/config.test.ts src/cli.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deepMergeConfig" sdk/src/config.ts` ≥ 2 (definition + loadConfig call)
    - `grep -n "pathToFileURL" sdk/src/cli.ts` matches; `grep -vn "//" sdk/src/cli.ts | grep -c "^.*main();$"` shows main() invoked only behind the isDirectEntry guard (manual inspection: the only unconditional top-level `main()` call is gone)
    - `cd sdk && npx vitest run src/config-loader-parity.test.ts` exits 0 with ≥ 11 passing tests total (Task 2 scrub block + I1-I5)
    - config-loader-parity.test.ts contains `commit_docs` and `root-marker-0123456789`
    - `cd sdk && npx vitest run src/config.test.ts src/cli.test.ts` exits 0 and the cli.test.ts run output contains no `Usage` error noise
    - `cd sdk && npx tsc --noEmit` exits 0
    - Real-CLI smoke unchanged: `node bin/oto-sdk.js query config-path` (from the repo root, cwd = a mkdtemp dir) still exits 0 and prints a path — NOTE this runs the OLD dist; the smoke only proves the bin wrapper contract, the guarded entry ships via plan 14-19's rebuild
    - `git diff --name-only` shows NO changes under sdk/dist
  </acceptance_criteria>
  <done>SDK workstream loads inherit root config exactly like CJS; importing the CLI module is side-effect-free while the spawned CLI is unchanged; all parity behaviors pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .oto/config.json (hand-edited/corrupt values) → loader effective views | Non-boolean integration values cross into code that promises booleans (WR-05) |
| Root config → workstream effective config | Root-only settings cross the workstream boundary; dropping them silently changes behavior (WR-09) |
| Module import → process state | Importing cli.ts previously executed the CLI (IR-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-16-01 | Information Disclosure (embedded marker escapes via object/array value) | _scrubIntegrationStrings / scrubIntegrationStrings | mitigate | Normalize every present non-boolean to Boolean(value) under forced migration failure (WR-05; Tests 1-5 both layers) |
| T-14-16-02 | Tampering (loader mutates disk on scrub) | CJS fileData separation | mitigate | Scrub applies to the effective view only; disk-byte assertions in every scrub test |
| T-14-16-03 | Tampering (root settings silently replaced by defaults) | SDK loadConfig workstream path | mitigate | Root→workstream deep merge mirroring CJS (WR-09; Tests I1-I5) |
| T-14-16-04 | Denial of Service (test/programmatic import runs the CLI) | sdk/src/cli.ts | mitigate | Direct-entry guard; bin wrapper spawns cli.js as argv[1] so real invocation is unchanged (IR-02) |
| T-14-16-05 | Repudiation (gate without a baseline blames Phase 14 for pre-existing debt) | terminal verification gate | mitigate | Authoritative pre-gap-closure baseline captured first (Task 1), diffed by plan 14-19 |
</threat_model>

<verification>
- `node --test tests/14-loader-scrub.test.cjs tests/14-loader-credential-survival.test.cjs` exits 0
- `cd sdk && npx vitest run src/config-loader-parity.test.ts src/config.test.ts src/cli.test.ts` exits 0
- `cd sdk && npx tsc --noEmit` exits 0
- 14-SDK-BASELINE.txt committed with summary + failed-file list from the pre-edit tree
</verification>

<success_criteria>
- WR-05 closed in both layers with forced-failure coverage for object/number/null/array values
- WR-09 closed: SDK and CJS agree on root inheritance under an active workstream
- IR-02 closed: cli import is side-effect-free, CLI behavior unchanged
- The terminal gate's baseline artifact exists and is authoritative
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-16-SUMMARY.md`
</output>
