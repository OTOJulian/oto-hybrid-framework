---
phase: 14-key-storage-reconciliation
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - oto/workflows/settings-integrations.md
  - tests/14-no-plaintext-guard.test.cjs
autonomous: false
requirements: [SECR-01, SECR-03, SECR-04]

must_haves:
  truths:
    - "D-09: /oto-settings-integrations instructs the user to run `! oto-sdk query secret-set <slug>` for Set/Replace — the key never enters the conversation, argv, or config.json"
    - "The workflow's security block states keys live in ~/.oto/<integration>_api_key (0600) and config.json holds booleans only — the old 'plaintext in config.json' contract is gone"
    - "D-04: A regression test proves the three integration config keys are boolean-typed and no key-shaped string exists in tracked .oto/ files"
    - "D-10: Status display in the workflow uses secret-status masked output (flag + source + ****<last-4>)"
    - "D-11: Clear in the workflow runs secret-clear, showing its keyfile-removed / env-still-set one-liner"
  artifacts:
    - path: "oto/workflows/settings-integrations.md"
      provides: "Rewritten Set/Replace/Clear flow via secret-* commands, corrected <security> block, D-10 status table"
      contains: "secret-set"
    - path: "tests/14-no-plaintext-guard.test.cjs"
      provides: "D-04 no-plaintext regression guard (boolean-typed keys + key-shaped-string scan of tracked .oto/ files)"
      min_lines: 40
  key_links:
    - from: "oto/workflows/settings-integrations.md"
      to: "oto-sdk secret commands"
      via: "bang-prefixed user-run command instruction"
      pattern: "oto-sdk query secret-set"
    - from: "tests/14-no-plaintext-guard.test.cjs"
      to: ".oto/config.json"
      via: "boolean-type assertion on the repo's own tracked config"
      pattern: "config\\.json"
---

<objective>
Rewrite `/oto-settings-integrations` around the keyfile mechanism (SECR-04): Set/Replace via the `!`-prefixed `oto-sdk query secret-set <slug>` TTY flow (D-09), Clear via `secret-clear`, status via `secret-status` masked lines (D-10), and a corrected `<security>` block. Add the D-04 no-plaintext regression guard over tracked `.oto/` files, then human-verify the full set/replace/clear loop.

Purpose: the workflow is the only user-facing surface — and it is the site of the original defect (it wrote raw keys into git-tracked config.json via `config-set exa_search "<value>"`). This plan removes the defective instructions and locks the phase's invariant in with a permanent test.
Output: rewritten workflow markdown, no-plaintext guard test, human-verified end-to-end flow.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/PROJECT.md
@.oto/ROADMAP.md
@.oto/STATE.md
@.oto/phases/14-key-storage-reconciliation/14-CONTEXT.md
@.oto/phases/14-key-storage-reconciliation/14-01-SUMMARY.md
@.oto/phases/14-key-storage-reconciliation/14-03-SUMMARY.md
@oto/workflows/settings-integrations.md
@docs/upstream-sync.md

<interfaces>
<!-- What Plans 01/03 delivered, which this plan consumes. -->

Live SDK commands (Plan 03, registered and dist-rebuilt):
- `oto-sdk query secret-set <exa|brave|firecrawl>` — piped stdin or silent TTY prompt; writes ~/.oto/<slug>_api_key (0600); sets `<configKey>: true`; rejects key-as-argv; output masked.
- `oto-sdk query secret-clear <slug>` — deletes keyfile, sets flag false, notes still-set env var.
- `oto-sdk query secret-status [slug]` — per-integration line: `Exa: enabled — key from env EXA_API_KEY (****4f2a)` / `Brave: enabled — key from ~/.oto/brave_api_key (****9c1e)` / `Firecrawl: disabled — no key detected`; env wins with `[shadows ...]` note; heals keyfile perms.

CJS enforcement (Plan 01): `config-set exa_search <string>` now exits non-zero ("booleans only — ... secret-set"); legacy strings self-heal on load.

Config keys still written via config-set by this workflow (non-secret): `search_gitignored`, `review.models.<cli>`, `agent_skills.<agent-type>`.

Current workflow defect sites (oto/workflows/settings-integrations.md):
- `<security>` block lines 13-33: claims "written as plaintext to .oto/config.json — that is where secrets live on disk" — now FALSE.
- `<purpose>` lines 1-11: "Writes to .oto/config.json" framing.
- step `read_current` lines 59-77: reads keys via `config-get brave_search --default null`.
- step `section_1_search_integrations` lines 79-145: writes `oto-sdk query config-set brave_search "<value>"` and clears via `config-set brave_search null` — the defect.
- step `confirm` lines 229-270: notes block says "API keys are stored plaintext in .oto/config.json".
- success_criteria line 277: "API keys written plaintext only to config.json".

git-tracked .oto file listing: `git ls-files .oto` (run from repo root).
Repo config current state: `.oto/config.json` has `brave_search: false, firecrawl: false, exa_search: false` (booleans — the guard must keep it that way).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite settings-integrations.md around the keyfile mechanism</name>
  <files>oto/workflows/settings-integrations.md</files>
  <read_first>
    - oto/workflows/settings-integrations.md (full current file — sections 2 and 3 [review models, agent skills] must survive UNCHANGED; sync hygiene: this file is GSD-shared, keep the diff confined to the key-handling sections)
    - .oto/phases/14-key-storage-reconciliation/14-CONTEXT.md (D-09 `!`-prefix flow in <specifics>, D-10 status format, D-11 clear semantics)
    - .oto/phases/14-key-storage-reconciliation/14-03-SUMMARY.md (exact command names/output shapes as shipped)
  </read_first>
  <action>
    Edit oto/workflows/settings-integrations.md. Leave `section_2_review_models` and `section_3_agent_skills` byte-identical. Change:

    1. `<purpose>`: replace the "Writes to .oto/config.json" sentence for search keys with: search API keys are stored in `~/.oto/<integration>_api_key` (mode 0600) via `oto-sdk query secret-*` commands; `.oto/config.json` holds only boolean enable flags. Review-model routing and agent skills still write config.json via config-set.

    2. `<security>` block — rewrite to state (keep the heading, keep masking convention paragraph):
       - Keys live ONLY in `~/.oto/<integration>_api_key` (mode 0600) or env vars (`EXA_API_KEY`, `BRAVE_API_KEY`, `FIRECRAWL_API_KEY`). They are NEVER written to `.oto/config.json` — the three config keys are boolean-only and both write paths hard-reject strings.
       - Key entry is stdin-only: the workflow directs the user to run `! oto-sdk query secret-set <slug>` themselves so the key travels terminal → process directly — never through argv, shell history, the conversation transcript, or Claude's context. Claude must NEVER ask for, receive, or echo a key value in chat.
       - Masking convention paragraph: keep as-is (`****<last-4>`, short strings `****`, unset `(unset)`).
       - Keep the agent-type/CLI slug validation paragraph as-is.

    3. Step `read_current` → rename to `read_status`: replace the four config-get lines with:
       ```bash
       oto-sdk query secret-status
       SEARCH_GITIGNORED=$(oto-sdk query config-get search_gitignored --default false)
       ```
       Show the secret-status lines verbatim (they are pre-masked, D-10 format: `Exa: enabled — key from env EXA_API_KEY (****4f2a)` etc.).

    4. Step `section_1_search_integrations`: keep the AskUserQuestion structure (Leave/Replace/Clear for keyed integrations, Skip/Set for unkeyed — keyed-ness comes from secret-status source, not config value). Replace the write mechanics:
       - For "Set"/"Replace": DO NOT prompt for the key in chat. The workflow tells the user to run the command themselves with the `!` bash prefix, e.g.:
         ```text
         Run this yourself (the ! prefix runs it in your terminal; the key is
         entered at a hidden prompt and never appears in this conversation):

           ! oto-sdk query secret-set exa

         Then reply "done" (or "skip") to continue.
         ```
         After the user replies, run `oto-sdk query secret-status exa` to confirm and show the masked line. Same pattern for brave/firecrawl.
       - For "Clear": run `oto-sdk query secret-clear <slug>` directly (no secret material involved) and show its one-line result (including the "env still set — integration remains available" note when applicable, D-11).
       - `search_gitignored` keeps its existing `oto-sdk query config-set search_gitignored true|false` write.
       - Delete the `config-set brave_search "<value>"` / `config-set ... null` command examples entirely.

    5. Step `confirm`: Search Integrations table now sourced from a final `oto-sdk query secret-status` — columns `Integration | Enabled | Key source | Key`, values like `Exa | true | env EXA_API_KEY | ****4f2a` or `Firecrawl | false | — | (unset)`. Replace the Notes block with:
       ```text
       Notes:
       - API keys live in ~/.oto/<integration>_api_key (mode 0600) or env vars —
         never in .oto/config.json (boolean flags only).
       - Keys are entered via a hidden terminal prompt (secret-set), never through
         this conversation, argv, or shell history; displays are always masked.
       ```

    6. `<success_criteria>`: replace the plaintext-to-config line with: `- [ ] API keys written only to ~/.oto/<integration>_api_key (0600) via secret-set stdin/TTY; config.json holds booleans only; nothing plaintext ever echoed, logged, or displayed`. Add: `- [ ] Set/Replace guidance uses the ! oto-sdk query secret-set flow; Clear uses secret-clear`.
  </action>
  <verify>
    <automated>grep -q "! oto-sdk query secret-set" oto/workflows/settings-integrations.md && ! grep -q "config-set brave_search\|config-set firecrawl\|config-set exa_search" oto/workflows/settings-integrations.md && ! grep -qi "stored plaintext in .oto/config.json\|written as plaintext to" oto/workflows/settings-integrations.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "secret-set" oto/workflows/settings-integrations.md` >= 3 (security block, section 1, success criteria)
    - `grep -n "! oto-sdk query secret-set" oto/workflows/settings-integrations.md` matches (bang-prefix instruction)
    - `grep -n "secret-clear" oto/workflows/settings-integrations.md` and `grep -n "secret-status" oto/workflows/settings-integrations.md` both match
    - `grep -n "config-set brave_search\|config-set firecrawl\|config-set exa_search" oto/workflows/settings-integrations.md` returns nothing (defect instructions removed)
    - `grep -n "0600" oto/workflows/settings-integrations.md` matches in the security block
    - `git diff oto/workflows/settings-integrations.md` shows section_2_review_models and section_3_agent_skills untouched
  </acceptance_criteria>
  <done>The workflow's only key paths are secret-set (user-run, hidden prompt) and secret-clear; every plaintext-in-config claim is gone; sections 2/3 untouched for sync hygiene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 no-plaintext regression guard over tracked .oto/ files</name>
  <files>tests/14-no-plaintext-guard.test.cjs</files>
  <read_first>
    - .oto/phases/14-key-storage-reconciliation/14-CONTEXT.md (D-04 scope: boolean-typed keys + key-shaped-string scan of tracked .oto/ files ONLY — not whole-repo, by decision)
    - tests/14-config-boolean.test.cjs (Plan 01's test conventions)
    - .oto/config.json (current clean boolean state the guard protects)
  </read_first>
  <behavior>
    - Test 1: parse the repo's own .oto/config.json — for each of exa_search, brave_search, firecrawl: key absent OR typeof value === 'boolean' (SECR-03 "including this repo's own config")
    - Test 2: for every file from `git ls-files .oto` (spawnSync from repo root), no line matches the string-typed-integration-field pattern /"(exa_search|brave_search|firecrawl)"\s*:\s*"/
    - Test 3: same file set, no match for key-shaped tokens on key-ish fields: /(api[_-]?key|_key)["']?\s*[:=]\s*["'][A-Za-z0-9_\-]{20,}["']/i and no provider-prefix tokens /\b(sk-[A-Za-z0-9]{24,}|fc-[A-Za-z0-9]{24,})\b/
    - On failure, the assertion message names the offending file and line number but renders the matched token MASKED via the maskSecret convention (never the full match)
  </behavior>
  <action>
    Create tests/14-no-plaintext-guard.test.cjs (node:test). Enumerate tracked files via `spawnSync('git', ['ls-files', '-z', '.oto'], { cwd: REPO_ROOT })` split on NUL; skip binaries by extension if any. Implement the three tests from the behavior block. Scope is exactly tracked `.oto/**` — do NOT scan the whole repo (D-04 rejected that: fixture/docs false positives). The regexes above are the starting heuristics (Claude's discretion per CONTEXT to tune thresholds); if a legitimate tracked .oto file trips a pattern (e.g. masked `****abcd` examples in planning docs are fine — they're < 20 chars), tighten the pattern rather than allowlisting files. Require '../oto/bin/lib/secrets.cjs' for maskSecret in failure messages.
  </action>
  <verify>
    <automated>node --test tests/14-no-plaintext-guard.test.cjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/14-no-plaintext-guard.test.cjs` exits 0 with 3 passing tests
    - `grep -n "ls-files" tests/14-no-plaintext-guard.test.cjs` matches (scans tracked files, not working-tree strays)
    - `grep -n "exa_search|brave_search|firecrawl" tests/14-no-plaintext-guard.test.cjs` matches (field-typed scan)
    - `npm test` (full suite: tooling + any wired SDK tests) exits 0
  </acceptance_criteria>
  <done>A permanent guard fails CI the moment a key-shaped string or string-typed integration flag enters any tracked .oto/ file — the boolean-validation + no-plaintext test families Phase 15 confirms under HARD-02 are both in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the set/replace/clear loop and masked displays</name>
  <files>none (verification only)</files>
  <read_first>
    - .oto/phases/14-key-storage-reconciliation/14-CONTEXT.md (phase success criteria being demonstrated)
  </read_first>
  <what-built>The complete key-storage reconciliation: stdin/TTY-only key entry to 0600 keyfiles, boolean-only tracked config enforced on both write paths, self-heal migration, masked status, and the rewritten /oto-settings-integrations workflow.</what-built>
  <action>
    Present the verification steps below to the user (including step 0's backup caution), then pause for their signal. Do not run the secret-set TTY steps on the user's behalf — the point is the user experiencing the hidden prompt in their own terminal.
  </action>
  <how-to-verify>
    CAUTION (do step 0 first): these steps overwrite and delete `~/.oto/exa_api_key`. If you have a REAL Exa key stored there, back it up before starting and restore it at the end — otherwise it will be destroyed.

    0. Preserve any real key and note the current flag value:
       `[ -f ~/.oto/exa_api_key ] && cp -p ~/.oto/exa_api_key ~/.oto/exa_api_key.verify-backup`
       `oto-sdk query config-get exa_search --default false`   # remember this value for step 7

    Then, using a DUMMY key value (e.g. `dummy-key-for-verify-1234`), in your terminal at the repo root:
    1. Run `oto-sdk query secret-set exa` (no pipe). Confirm the prompt hides your typing and nothing you type appears on screen or in scrollback.
    2. Run `ls -l ~/.oto/exa_api_key` — expect `-rw-------` (0600). Run `oto-sdk query secret-status` — expect `Exa: enabled — key from ~/.oto/exa_api_key (****1234)` (masked, last-4 only) and `git diff .oto/config.json` showing only `"exa_search": false` → `true`.
    3. Replace: run `oto-sdk query secret-set exa` again with a different dummy (`replacement-key-verify-9999`); secret-status should now show `****9999`.
    4. Reject check: run `oto-sdk query config-set exa_search some-string-key` — expect a non-zero exit and the "booleans only — ... secret-set" error; run `oto-sdk query secret-set exa another-key-in-argv` — expect the argv-rejection error.
    5. Clear: run `oto-sdk query secret-clear exa` — expect keyfile gone (`ls ~/.oto/exa_api_key` fails), `secret-status` shows `Exa: disabled — no key detected`, and `git diff .oto/config.json` is clean again.
    6. Skim the rewritten workflow: `sed -n '1,60p' oto/workflows/settings-integrations.md` — confirm the security block describes keyfiles/0600/stdin and the `!`-prefix flow, with no "plaintext in config.json" language.
    7. Restore your real key if you backed one up in step 0:
       `[ -f ~/.oto/exa_api_key.verify-backup ] && mv ~/.oto/exa_api_key.verify-backup ~/.oto/exa_api_key && chmod 600 ~/.oto/exa_api_key`
       and if the step-0 flag value was `true`, run `oto-sdk query config-set exa_search true` to restore it (then confirm with `oto-sdk query secret-status exa`).
  </how-to-verify>
  <verify>User confirms all checks pass (hidden prompt, 0600 mode, masked status, both rejections, clean clear) and any backed-up real key from step 0 has been restored (no ~/.oto/exa_api_key.verify-backup left behind)</verify>
  <done>User has typed "approved" after completing the checks (including step 0 backup and step 7 restore when a real key existed), or issues raised have been fixed and re-verified</done>
  <resume-signal>Type "approved" if all checks pass, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ↔ Claude conversation | anything typed in chat enters the transcript and Claude's context — keys must never cross here |
| workflow markdown → user's shell | the workflow instructs commands; wrong instructions reintroduce the defect |
| git-tracked .oto/** → remote | committed planning artifacts are the exfiltration surface the guard test watches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-23 | I | key entered into the chat transcript when the workflow asks for it | mitigate | Set/Replace never prompts in chat — `!`-prefixed user-run secret-set with hidden TTY prompt (D-09); security block forbids Claude receiving key values |
| T-14-24 | I | regression: future edit reintroduces `config-set exa_search "<key>"` guidance or a key lands in a tracked .oto file | mitigate | tests/14-no-plaintext-guard.test.cjs runs in `npm test` permanently (D-04); write-path rejection is defense-in-depth |
| T-14-25 | I | confirmation table or status display echoing plaintext | mitigate | all displays sourced from secret-status masked output; acceptance greps verify no plaintext-claims remain |
| T-14-26 | I/D | verification steps destroying a pre-existing real key, or leaving dummy artifacts behind | mitigate | step 0 backup + step 7 restore of ~/.oto/exa_api_key; secret-clear + clean `git diff .oto/config.json` check; no .verify-backup file left behind |
| T-14-27 | T | GSD upstream sync clobbering the rewritten shared workflow | accept | sync hygiene contract (docs/upstream-sync.md) + HARD-05 dry-run check at milestone close; diff confined to key-handling sections |
</threat_model>

<verification>
- `npm test` exits 0 (tooling suite incl. all four 14-* test files)
- `cd sdk && npx vitest run` exits 0
- `grep -rn "config-set \(brave_search\|firecrawl\|exa_search\)" oto/workflows/` returns nothing
- Human checkpoint approved: hidden prompt, 0600 keyfile, masked status, argv + string rejections, clean clear, pre-existing key (if any) restored
</verification>

<success_criteria>
- Phase success criteria 1 and 4 verified end-to-end by a human: set/replace/clear via the workflow's instructed commands with masked (`****<last-4>`) displays throughout
- Phase success criterion 3's "including this repo's own config" is locked in by the boolean-type guard on .oto/config.json
- No key material can enter any tracked file through either write path or the workflow surface
- Verification leaves the user's machine as found (real key preserved via backup/restore)
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-04-SUMMARY.md`
</output>
