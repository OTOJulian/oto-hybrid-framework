---
phase: 14-key-storage-reconciliation
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/workflows/settings-integrations.md
  - tests/14-settings-workflow-contract.test.cjs
  - tests/14-no-plaintext-guard.test.cjs
autonomous: true
gap_closure: true
requirements: [SECR-01, SECR-04]

must_haves:
  truths:
    - "CLOSES WR-02: the settings workflow resolves the active workstream through the canonical session-aware/root-aware resolver (oto-tools' dispatch-level resolution: --ws flag > OTO_WORKSTREAM > session-scoped tmpdir pointer > shared pointer) instead of reading only the hardcoded .oto/active-workstream file — a session-scoped workstream (e.g. under CODEX_THREAD_ID) and a migrated .planning root both route every workflow command to the correct config"
    - "CLOSES WR-02 (config path): OTO_CONFIG_PATH comes from the workstream-aware, root-aware config-path command, never from a hardcoded '.oto/...' literal"
    - "CLOSES IR-01: the no-plaintext provider-token scan catches keys with internal separators (sk-proj-*-style, underscores/hyphens), pinned by positive and negative control fixtures"
    - "The workflow contract test covers the session-pointer case and the migrated .planning-root case, both against real processes"
  artifacts:
    - path: "oto/workflows/settings-integrations.md"
      provides: "Canonical workstream + config-path resolution in ensure_and_load_config; no direct active-workstream file read"
      contains: "workstream get"
    - path: "tests/14-settings-workflow-contract.test.cjs"
      provides: "Session-pointer and migrated-root resolution regressions + static pin that the workflow no longer greps the shared pointer file"
      min_lines: 80
    - path: "tests/14-no-plaintext-guard.test.cjs"
      provides: "Broadened PROVIDER_PREFIX_TOKEN / KEY_SHAPED_FIELD character classes + positive/negative control assertions"
      contains: "sk-proj-"
  key_links:
    - from: "oto/workflows/settings-integrations.md ensure_and_load_config"
      to: "oto/bin/lib/oto-tools.cjs workstream get"
      via: "shell resolution of WS before building WS_ARGS"
      pattern: "workstream get --raw"
    - from: "oto/workflows/settings-integrations.md"
      to: "oto-tools config-path"
      via: "OTO_CONFIG_PATH command substitution"
      pattern: "config-path"
---

<objective>
Close WR-02 and IR-01 from 14-REVIEW.md.

WR-02 (verified): the workflow's `ensure_and_load_config` step (oto/workflows/settings-integrations.md:52-66) checks ONLY `[[ -f .oto/active-workstream ]]`. Core routing (oto/bin/lib/core.cjs getActiveWorkstream, :1113-1121) uses a tmpdir-backed session-scoped pointer whenever a stable session key exists (e.g. CODEX_THREAD_ID) and then intentionally ignores the shared pointer; the workflow also hardcodes `.oto`, so a migrated `.planning` root is missed. Reproduced by the reviewer: core resolved `ws1` while `.oto/active-workstream` did not exist — every workflow command would read or mutate ROOT config.

The canonical resolver already exists and needs no new code: `oto/bin/lib/oto-tools.cjs` resolves the workstream at dispatch (verified at :272-297: `--ws` flag > `OTO_WORKSTREAM` env > session-scoped pointer > shared legacy pointer > null) and `workstream get --raw` prints the active name or `none` (workstream.cjs:367-371). `config-path` (oto-tools :664 → config.cjs cmdConfigPath) prints the resolved config path through the same dispatch resolution and the root-aware `planningDir`. The fix is a workflow-markdown change: resolve WS and OTO_CONFIG_PATH through those commands and build `WS_ARGS` from the result.

IR-01 (verified at tests/14-no-plaintext-guard.test.cjs:18-22): `PROVIDER_PREFIX_TOKEN = /\b(sk-[A-Za-z0-9]{24,}|fc-[A-Za-z0-9]{24,})\b/g` allows only alphanumerics after the prefix — long tokens containing internal `-` or `_` (e.g. `sk-proj-...`) are missed unless the narrower integration-field pattern happens to catch them. Broaden the character classes and add positive/negative controls.

WR-04 note (dispositioned DEFER — see 14-DISPOSITIONS.md): the comma-separated agent-skills write at settings-integrations.md:259-264 is NOT changed in this phase. Owner: Phase 16 (agent guidance — it owns the agent_skills consumers), tracked in .oto/STATE.md Pending Todos. Do not "fix" it opportunistically here; changing the persisted shape without updating the Phase 16 consumers would break skill resolution.

Purpose: SECR-04 — the workflow must set/replace/clear/inspect the config the user is actually operating in; SECR-01 — the D-04 regression net must not have token-shaped blind spots.
Output: canonical-resolution workflow step, extended contract test, broadened guard regexes.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/14-key-storage-reconciliation/14-REVIEW.md
@oto/workflows/settings-integrations.md
@tests/14-settings-workflow-contract.test.cjs
@tests/14-no-plaintext-guard.test.cjs
</context>

<interfaces>
Current defective workflow block (settings-integrations.md:52-66):
```bash
WS_ARGS=()
if [[ -f .oto/active-workstream ]]; then
  WS=$(tr -d '\n\r' < .oto/active-workstream)
  WS_ARGS=(--ws "$WS")
  OTO_CONFIG_PATH=".oto/workstreams/${WS}/config.json"
else
  WS=""
  OTO_CONFIG_PATH=".oto/config.json"
fi
```

Canonical resolver surfaces (verified):
- `node <oto-tools> workstream get --raw` → prints active workstream name, or `none` (workstream.cjs cmdWorkstreamGet: `process.env.OTO_WORKSTREAM || getActiveWorkstream(cwd)`; dispatch pre-resolution at oto-tools:272-297 already consulted the session pointer and set OTO_WORKSTREAM).
- `node <oto-tools> config-path` → prints the resolved config.json path (plain text), workstream-aware (dispatch resolution) and root-aware (`planningDir` → `planningRootName` handles `.oto` vs migrated `.planning`).
- Installed path convention used by other workflows (e.g. plan-review-convergence.md:65): `node "$HOME/.claude/oto/bin/oto-tools.cjs"`. NOTE: the installed layout puts oto-tools at `bin/oto-tools.cjs` under the install root while the repo keeps it at `oto/bin/lib/oto-tools.cjs` — follow the existing workflow convention exactly as other oto workflows reference it (verify with `grep -rn "oto-tools.cjs" oto/workflows/*.md` and match the dominant pattern).

Session-pointer test seam (core.cjs 1033-1121): a session key comes from env (e.g. `CODEX_THREAD_ID`); `workstream set <name>` with that env writes the tmpdir session pointer (per-project dir hashed from the realpath of the planning root), and `workstream get` with the same env reads it back while `.oto/active-workstream` stays absent. Fixture projects are mkdtemp-unique, so tmpdir slots never collide; use a UUID-ish CODEX_THREAD_ID per test.

Contract test conventions: tests/14-settings-workflow-contract.test.cjs already has makeFixture/cleanEnv/run helpers spawning `bin/oto-sdk.js`; add a parallel `runTools(fixture, args, extraEnv)` helper spawning `oto/bin/lib/oto-tools.cjs`.

IR-01 current regexes (tests/14-no-plaintext-guard.test.cjs:18-22):
```js
const KEY_SHAPED_FIELD =
  /(api[_-]?key|_key)["']?\s*[:=]\s*["']([A-Za-z0-9_-]{20,})["']/gi;
const PROVIDER_PREFIX_TOKEN = /\b(sk-[A-Za-z0-9]{24,}|fc-[A-Za-z0-9]{24,})\b/g;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-02 — canonical workstream/config-path resolution in the settings workflow + contract-test coverage</name>
  <files>oto/workflows/settings-integrations.md, tests/14-settings-workflow-contract.test.cjs</files>
  <read_first>
    - oto/workflows/settings-integrations.md (whole file — every `WS_ARGS`/`OTO_CONFIG_PATH` consumer must keep working; the guarded `${WS_ARGS[@]+"${WS_ARGS[@]}"}` expansion convention is retained)
    - oto/bin/lib/oto-tools.cjs lines 272-330 (dispatch workstream resolution + --raw handling) and the `workstream`/`config-path` cases
    - oto/bin/lib/core.cjs lines 1033-1125 (getWorkstreamSessionKey/getSessionScopedWorkstreamFile/getActiveWorkstream — the resolver the workflow now rides)
    - oto/bin/lib/workstream.cjs lines 360-372 (cmdWorkstreamSet/cmdWorkstreamGet raw output shapes)
    - tests/14-settings-workflow-contract.test.cjs (existing helpers and static workflow pins)
    - `grep -rn "oto-tools.cjs" oto/workflows/*.md` output (the installed-path reference convention to copy)
  </read_first>
  <behavior>
    - Test 1 (session pointer, the WR-02 reproduction): fixture project with `.oto/` and a created workstream `ws1` (`workstream create ws1` then `workstream set ws1`, both run via oto-tools with `CODEX_THREAD_ID=<unique>` in env) → `.oto/active-workstream` does NOT exist, yet `workstream get --raw` with the same env prints `ws1`; and the workflow's documented resolution sequence (executed as a bash snippet extracted from the workflow, or re-implemented verbatim in the test) yields `WS=ws1` and an OTO_CONFIG_PATH ending in `workstreams/ws1/config.json`
    - Test 2 (no workstream): fresh fixture, no session env → `workstream get --raw` prints `none`; resolution yields empty WS_ARGS and a root config path
    - Test 3 (migrated root): fixture using `.planning/` instead of `.oto/` (create `.planning/config.json`; ensure no `.oto/`) → `config-path` prints a path containing `.planning/config.json` (root-awareness), and the workflow text contains NO hardcoded `.oto/active-workstream` or `.oto/config.json` literals in the ensure_and_load_config step
    - Test 4 (static pin): the workflow file contains `workstream get --raw` and `config-path`, retains the guarded `${WS_ARGS[@]+"${WS_ARGS[@]}"}` expansion on every oto-sdk invocation, and no longer contains the string `active-workstream`
  </behavior>
  <action>
    **oto/workflows/settings-integrations.md** (GSD-shared file — keep the edit scoped to the `ensure_and_load_config` step and any hardcoded-path mentions):

    Replace the resolution block (lines 52-66) with the canonical sequence (match the repo's dominant oto-tools path convention found in read_first):

    ```bash
    OTO_TOOLS="${OTO_TOOLS:-$HOME/.claude/oto/bin/oto-tools.cjs}"
    # Canonical session-aware/root-aware resolution (WR-02): oto-tools resolves
    # --ws > OTO_WORKSTREAM > session-scoped pointer > shared pointer at dispatch.
    WS=$(node "$OTO_TOOLS" workstream get --raw 2>/dev/null || echo none)
    WS_ARGS=()
    if [[ -n "$WS" && "$WS" != "none" ]]; then
      WS_ARGS=(--ws "$WS")
    fi
    OTO_CONFIG_PATH=$(node "$OTO_TOOLS" config-path ${WS_ARGS[@]+"${WS_ARGS[@]}"})
    oto-sdk query config-new-project ${WS_ARGS[@]+"${WS_ARGS[@]}"}
    ```

    Keep the surrounding prose but update it: resolution is delegated to the canonical resolver; never read pointer files directly; `.oto` vs `.planning` is resolved by the tool. Sweep the REST of the workflow for hardcoded `.oto/` config-path literals in prose/examples tied to this step (the display examples like `~/.oto/brave_api_key` are keyfile paths, NOT project config paths — leave those). If the oto-tools path convention in other workflows differs from `$HOME/.claude/oto/bin/oto-tools.cjs`, use theirs.

    **tests/14-settings-workflow-contract.test.cjs** — add a `runTools(fixture, args, extraEnv)` helper spawning `process.execPath [path.join(REPO_ROOT, 'oto/bin/lib/oto-tools.cjs'), ...args]` with `cwd: fixture.project` and `env: { ...cleanEnv(fixture.home), ...extraEnv }`. Implement Tests 1-4. For Test 1, generate `CODEX_THREAD_ID` as `'oto-test-' + Date.now() + '-' + Math.random().toString(36).slice(2)` and clean up by running `workstream set` with no name (or unlinking via the same env) in `t.after` — verify the clear subcommand shape in workstream.cjs first. For the workflow-sequence assertion, read the workflow file, extract the fenced bash block of ensure_and_load_config, and run it with `bash -c` in the fixture (OTO_TOOLS overridden to the repo path via env) asserting `$WS` and `$OTO_CONFIG_PATH`.
  </action>
  <verify>
    <automated>node --test tests/14-settings-workflow-contract.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "active-workstream" oto/workflows/settings-integrations.md` returns 0
    - `grep -n "workstream get --raw" oto/workflows/settings-integrations.md` matches inside the ensure_and_load_config step; `grep -n "config-path" oto/workflows/settings-integrations.md` matches in the same step
    - `grep -c '\${WS_ARGS\[@\]+' oto/workflows/settings-integrations.md` is unchanged or higher than before the edit (the guarded expansion convention is retained — capture the pre-edit count first)
    - `node --test tests/14-settings-workflow-contract.test.cjs` exits 0 with ≥ 4 new passing tests alongside the existing ones
    - The contract test contains `CODEX_THREAD_ID` and `.planning` (session + migrated-root cases are real, not prose)
  </acceptance_criteria>
  <done>Every settings-workflow command routes through the canonical session-aware, root-aware resolution; session-scoped and migrated-root projects are covered by real-process regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IR-01 — broaden the no-plaintext token scan + control fixtures</name>
  <files>tests/14-no-plaintext-guard.test.cjs</files>
  <read_first>
    - tests/14-no-plaintext-guard.test.cjs (whole file — the three regexes at 18-22, the tracked-file walk, and the existing assertion structure)
  </read_first>
  <behavior>
    - Test R1 (positive controls): each of `sk-proj-AbC123_def-456789012345678901234567`, `sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXX`, `fc-abc_123-XYZ7890123456789012345` matches the broadened PROVIDER_PREFIX_TOKEN
    - Test R2 (negative controls): `sk-` followed by fewer than 20 token chars, prose like `task-123`, and a hyphenated English phrase (`well-known-longer-than-twenty-chars-word`) do NOT match
    - Test R3 (KEY_SHAPED_FIELD): `"exa_api_key": "AbC-123_def-4567890123"` matches; `"api_key": "true"` does not
    - The existing tracked-`.oto`-file scan still passes against the repo (no real key material present)
  </behavior>
  <action>
    In `tests/14-no-plaintext-guard.test.cjs`:
    1. Broaden the provider regex (IR-01):
       ```js
       const PROVIDER_PREFIX_TOKEN = /\b(sk-[A-Za-z0-9_-]{20,}|fc-[A-Za-z0-9_-]{20,})\b/g;
       ```
       (allows internal `-`/`_` so `sk-proj-*` and segmented tokens match; threshold lowered 24→20 to keep segmented tokens in range — verify against the repo scan for false positives and, if any tracked `.oto` file legitimately matches, tighten with a negative lookahead for that documented literal rather than weakening the class).
    2. Keep KEY_SHAPED_FIELD's `[A-Za-z0-9_-]{20,}` class (already separator-tolerant) but confirm with R3-style unit assertions.
    3. Add a `describe`/subtest block of pure regex unit assertions (R1-R3) exercising the patterns against literal strings — reset `lastIndex` or use fresh regex instances per assertion (the `g` flag makes `.test()` stateful).
  </action>
  <verify>
    <automated>node --test tests/14-no-plaintext-guard.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sk-\[A-Za-z0-9_-\]{20,}" tests/14-no-plaintext-guard.test.cjs` matches (broadened class present)
    - The file contains `sk-proj-` (positive control fixture)
    - `node --test tests/14-no-plaintext-guard.test.cjs` exits 0 — both the repo scan and the new unit assertions pass
  </acceptance_criteria>
  <done>The D-04 guard catches separator-containing provider tokens with pinned positive/negative controls, and still passes against the clean repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow shell context → project config selection | The workstream identity crosses from session/env/pointer state into every config mutation the workflow issues (WR-02) |
| Tracked .oto files → git remote | The D-04 scan is the last net before push; token-shaped blind spots are disclosure risk (IR-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17-01 | Tampering (mutating the wrong config) | settings workflow WS resolution | mitigate | Canonical resolver (session pointer + migrated root) with real-process regressions (WR-02; Tests 1-3) |
| T-14-17-02 | Information Disclosure (separator-containing token evades scan) | no-plaintext guard | mitigate | Broadened character classes + positive/negative controls (IR-01; R1-R3) |
| T-14-17-03 | Denial of Service (workflow breaks under set -u / bash 3.2) | WS_ARGS expansion | mitigate | Guarded `${WS_ARGS[@]+...}` convention retained and grep-pinned |
| T-14-17-04 | Tampering (comma-joined skills resolve to a nonexistent skill) | agent-skills write (WR-04) | transfer | DEFERRED to Phase 16 with tracked owner (STATE.md Pending Todos + 14-DISPOSITIONS.md) — consumers live in Phase 16 scope |
</threat_model>

<verification>
- `node --test tests/14-settings-workflow-contract.test.cjs tests/14-no-plaintext-guard.test.cjs` exits 0
- `grep -c "active-workstream" oto/workflows/settings-integrations.md` returns 0
</verification>

<success_criteria>
- WR-02 closed: session-scoped and migrated-root projects route every settings command to the correct config, pinned by real-process tests
- IR-01 closed: the token scan has no separator blind spot and still passes on the clean repo
- WR-04 remains explicitly deferred with a named owner (no silent omission)
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-17-SUMMARY.md`
</output>
