---
phase: 14-key-storage-reconciliation
plan: 18
type: execute
wave: 3
depends_on: ["14-13", "14-14", "14-15", "14-16"]
files_modified:
  - oto/bin/lib/secrets.cjs
  - oto/bin/lib/config.cjs
  - sdk/src/query/secrets.ts
  - sdk/src/query/config-mutation.ts
  - tests/14-migration-lock.test.cjs
  - sdk/src/query/migration-lock.test.ts
autonomous: true
gap_closure: true
requirements: [SECR-03, SECR-04]

must_haves:
  truths:
    - "CLOSES WR-01 (write path): integration config-set runs legacy migration and the flag mutation inside ONE lock-protected transaction in BOTH layers — a concurrent locked writer's unrelated update can no longer be silently removed by a stale migration write"
    - "CLOSES WR-01 (read paths): read-triggered migration (config-get, loaders, secret-status) acquires the same per-config lock before its read/modify/write; while another process holds the lock, migration does not write (CJS read paths skip best-effort after a short wait instead of clobbering)"
    - "CLOSES WR-01 (reproduction inverted): with a fresh .oto/.lock present, the CJS migration helper does NOT rewrite config — the reviewer's confirmed clobber is now a pinned regression"
    - "A multi-process interleave regression proves an unrelated setting update and a legacy-key migration both survive when they contend"
    - "No lock is leaked on any path (throw inside the locked region still releases; no stray .lock/.tmp files after any test)"
  artifacts:
    - path: "oto/bin/lib/secrets.cjs"
      provides: "withConfigDirLock helper (wx-create, stale>30s, timeout+skip option) + self-locking migrateLegacyIntegrationKeys with alreadyLocked opt-out"
      contains: "withConfigDirLock"
    - path: "oto/bin/lib/config.cjs"
      provides: "cmdConfigSet single-transaction (withPlanningLock wrapping migration + setConfigValueInLock); setConfigValue split into locked wrapper + inLock body; cmdConfigGet treats a lock-skipped migration as failure for sensitive keys only"
      contains: "setConfigValueInLock"
    - path: "sdk/src/query/secrets.ts"
      provides: "Self-locking migrateLegacyIntegrationKeys via acquireStateLock(configPath) with alreadyLocked opt-out"
      contains: "alreadyLocked"
    - path: "sdk/src/query/config-mutation.ts"
      provides: "configSet migration moved INSIDE the acquireStateLock region"
      contains: "alreadyLocked: true"
    - path: "tests/14-migration-lock.test.cjs"
      provides: "Lock-respect, interleave, and single-transaction real-process regressions"
      min_lines: 80
    - path: "sdk/src/query/migration-lock.test.ts"
      provides: "SDK lock-acquisition and transaction-ordering regressions"
      min_lines: 40
  key_links:
    - from: "oto/bin/lib/config.cjs cmdConfigSet"
      to: "migrateLegacyIntegrationKeys + setConfigValueInLock"
      via: "one withPlanningLock region"
      pattern: "alreadyLocked: true"
    - from: "sdk/src/query/config-mutation.ts configSet"
      to: "sdk/src/query/secrets.ts migrateLegacyIntegrationKeys"
      via: "migration call relocated inside the acquireStateLock try block"
      pattern: "migrateLegacyIntegrationKeys\\(paths.config, undefined, \\{ alreadyLocked: true \\}\\)"
    - from: "oto/bin/lib/secrets.cjs migrateLegacyIntegrationKeys"
      to: "withConfigDirLock"
      via: "self-locking default when no caller lock is held"
      pattern: "withConfigDirLock\\("
---

<objective>
Close WR-01 from 14-REVIEW.md: both migration helpers perform an unlocked whole-file read/modify/write. Verified against live source:
- CJS `cmdConfigSet` (config.cjs:346-357) calls `migrateLegacyIntegrationKeys` BEFORE `setConfigValue` acquires `withPlanningLock` (config.cjs:284) — migration and mutation are two separate windows, and migration itself takes no lock.
- SDK `configSet` (config-mutation.ts:225-234) migrates before `acquireStateLock` at :250.
- Read paths migrate with no lock at all: CJS `cmdConfigGet` (config.cjs:411), `core.cjs loadConfig` (:339, :352), SDK `config.ts loadConfig`, `secret-commands.ts secretStatus` (:284).
- The reviewer confirmed the CJS helper rewrites config even while `.oto/.lock` exists — a stale migration write can race a locked writer and silently remove the writer's unrelated update.

Fix, per layer:
- **CJS:** add `withConfigDirLock(configPath, fn, { timeoutMs, onTimeout })` to secrets.cjs implementing the same protocol as core's `withPlanningLock` (wx-create of `path.join(path.dirname(configPath), '.lock')`, stale >30s eviction, retry loop) — SAME lock file as withPlanningLock uses for that config's directory, so migration and ordinary writers contend on one lock. `migrateLegacyIntegrationKeys(configPath, baseDir, opts)` self-locks by default; `opts.alreadyLocked` opts out for callers that already hold the planning lock. Read paths get `onTimeout:'skip'` semantics (short wait, then return `{ migrated: [], skipped: true }` — best-effort reads must not deadlock or clobber). `cmdConfigSet` becomes one transaction: `withPlanningLock(cwd, () => { migrate(configPath, undefined, { alreadyLocked: true }); …; return setConfigValueInLock(cwd, keyPath, parsedValue); })` — which requires splitting `setConfigValue` into the locked wrapper (kept, same signature, still used by other callers) and an `setConfigValueInLock` body.
- **SDK:** `migrateLegacyIntegrationKeys(configPath, baseDir?, opts?)` self-locks via `acquireStateLock(configPath)`/`releaseStateLock` in try/finally unless `opts.alreadyLocked`; `configSet` moves its migration call INSIDE the existing lock region (the lock file is `config.json.lock` — same one the mutators already use, so contention is real). Read-path call sites (config.ts loadConfig, secretStatus) need no signature change — they inherit self-locking. Note the SDK lock force-acquires after ~2s of retries against a live lock (state-mutation.ts:206-209, existing semantics) — document this in the SUMMARY; do not change lock internals in this plan.

Lock-safety constraints (why this is Wave 3): touches files owned by 14-13/14-14/14-15 in earlier waves; runs against their merged results. No import cycle: verified `state-mutation.ts` does not import `secrets.ts`.

Purpose: SECR-03/SECR-04 — self-healing must never destroy a concurrent writer's update; a migration and its triggering mutation must be atomic with respect to other oto processes.
Output: one-lock transactions in both layers, self-locking migration with skip-on-contention read semantics, multi-process regressions. sdk/dist rebuild deferred to plan 14-19.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/14-key-storage-reconciliation/14-REVIEW.md
@oto/bin/lib/secrets.cjs
@oto/bin/lib/config.cjs
@oto/bin/lib/core.cjs
@sdk/src/query/secrets.ts
@sdk/src/query/config-mutation.ts
@sdk/src/query/state-mutation.ts
</context>

<interfaces>
CJS lock protocol to replicate (core.cjs withPlanningLock, 856-905): `writeFileSync(lockPath, json, { flag: 'wx' })` → run fn in try/finally with unlink → on EEXIST, evict if mtime older than 30s, else retry every 100ms up to 10s. `_heldPlanningLocks` exit-cleanup is core-internal; the secrets.cjs helper should register its own `process.on('exit')` cleanup set (small, self-contained — do NOT import core.cjs from secrets.cjs; core already requires secrets, and a static require back would create a load cycle).

Lock identity: for a config at `<dir>/config.json`, BOTH withPlanningLock (via `planningDir(cwd)`) and the new withConfigDirLock (via `dirname(configPath)`) must use `<dir>/.lock`. That identity is what makes the transaction real. Workstream configs get their own `<wsdir>/.lock` — matching planningDir(cwd) when OTO_WORKSTREAM is set.

CJS call sites (verified):
- config.cjs cmdConfigSet :346-357 (migrate + warn) and :382 (setConfigValue) — becomes one withPlanningLock region; keep the D-05 validation and VALID_* checks OUTSIDE the lock (pure), keep `warnIfNoKeyDetected` AFTER migration (WR-02 ordering from 14-11) — it reads keyfiles/env only, safe inside or after the lock; place it after the lock releases to keep the region minimal.
- config.cjs cmdConfigGet :411 — `try { migrate } catch { migrationFailed = true }`; extend: `const m = migrate(...); if (m && m.skipped) migrationFailed = isSecretKey(keyPath);` semantics — a lock-skipped migration withholds SENSITIVE reads only (same sanitized message), unrelated reads proceed (fail-open posture from 14-11 preserved).
- core.cjs loadConfig :339/:352 — unchanged call sites; self-locking with skip applies automatically. Do NOT edit core.cjs in this plan.
- secrets.cjs reconcileNewProjectIntegrations — out of scope (operates pre-write on a fresh config; 14-15 owns it).

SDK call sites (verified): config-mutation.ts configSet :225-234 (move inside lock region at :250-266); config.ts loadConfig :173/:176 and secret-commands.ts secretStatus :284 (unchanged — inherit self-locking).

SDK lock (state-mutation.ts:179-215): `acquireStateLock(statePath)` locks `statePath + '.lock'`; 10 retries × 200ms; evicts dead-pid or >10s-stale locks; FORCE-acquires on final retry. `releaseStateLock(lockPath)` in finally. Import into secrets.ts: `import { acquireStateLock, releaseStateLock } from './state-mutation.js';` (no cycle — verified).

Existing test fixtures: tests/14-migration-hardening.test.cjs (CJS migration fixtures), sdk/src/query/config-mutation.test.ts (SDK mutation fixtures).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CJS — withConfigDirLock + self-locking migration + single-transaction cmdConfigSet</name>
  <files>oto/bin/lib/secrets.cjs, oto/bin/lib/config.cjs, tests/14-migration-lock.test.cjs</files>
  <read_first>
    - oto/bin/lib/core.cjs lines 850-910 (withPlanningLock — the protocol to replicate; note the exit-cleanup set at 876-883)
    - oto/bin/lib/config.cjs lines 275-401 (setConfigValue + cmdConfigSet post-14-14 state) and 403-457 (cmdConfigGet post-14-14 state)
    - oto/bin/lib/secrets.cjs (post-14-15 state — migrateLegacyIntegrationKeys and the module.exports block)
    - tests/14-migration-hardening.test.cjs (fixture conventions)
  </read_first>
  <behavior>
    - Test L1 (the WR-01 reproduction, inverted): seed config `{"exa_search":"sk-legacy-lock-0123456789","commit_docs":false}`; create `<dir>/.lock` with CURRENT mtime → `migrateLegacyIntegrationKeys(configPath, base)` returns `{ migrated: [], skipped: true }` within ~3s; config bytes UNCHANGED; no keyfile written
    - Test L2: remove the lock → same call now migrates: keyfile written at 0600, config `exa_search === true`, `commit_docs === false` preserved
    - Test L3 (stale lock evicted): lock file with mtime forced >30s old (fs.utimesSync) → migration proceeds
    - Test L4 (interleave): parent process runs `withConfigDirLock(configPath, fn)`-equivalent by holding the lock (create `.lock`), inside that window spawns a CHILD `node -e` that calls `migrateLegacyIntegrationKeys` synchronously → child output shows `skipped: true` and config is untouched; parent then modifies an unrelated key (`{"commit_docs":true}` direct JSON edit) and releases; a fresh migration call then succeeds → final config has `commit_docs === true` AND `exa_search === true` AND the keyfile holds the legacy value (both changes survive)
    - Test L5 (single transaction, real process): seed config `{"brave_search":"sk-legacy-brave-0123456789","commit_docs":false}` → spawnSync `oto-tools config-set exa_search true` → exit 0; final config: `exa_search === true`, `brave_search === true` (migrated), `commit_docs === false` (preserved); `$HOME/.oto/brave_api_key` holds the legacy value at 0600; NO `.lock` or `.tmp.` files remain
    - Test L6 (sensitive read under contention): fresh `.lock` held + legacy string in config → spawnSync `oto-tools config-get exa_search` exits non-zero with the sanitized withheld message; `oto-tools config-get commit_docs` (unrelated) exits 0
  </behavior>
  <action>
    **oto/bin/lib/secrets.cjs** (GSD-shared — commented, self-contained):
    1. Add `withConfigDirLock(configPath, fn, opts = {})`: lockPath = `path.join(path.dirname(configPath), '.lock')`; loop with `writeFileSync(lockPath, JSON.stringify({ pid, acquired }), { flag: 'wx' })`; on success add to a module-level `_heldLocks` Set with a lazily-registered `process.on('exit')` unlink sweep, run `fn()` in try/finally (delete + unlink); on EEXIST evict if `Date.now() - statSync(lockPath).mtimeMs > 30000`, else retry every 100ms until `opts.timeoutMs ?? 10000`; on timeout: if `opts.onTimeout === 'skip'` return `{ skipped: true }` sentinel, else throw `new Error('config lock timeout: ' + lockPath)`.
    2. Change `migrateLegacyIntegrationKeys(configPath, baseDir)` to `(configPath, baseDir, opts = {})`: when `opts.alreadyLocked` → run the existing body directly; otherwise wrap the body in `withConfigDirLock(configPath, body, { timeoutMs: 2000, onTimeout: 'skip' })` and, when skipped, return `{ migrated: [], skipped: true }`. Keep the return shape `{ migrated, conflicts }` on success. Export `withConfigDirLock`.

    **oto/bin/lib/config.cjs**:
    1. Split `setConfigValue` (281-316): extract the body inside the `withPlanningLock` callback into `setConfigValueInLock(cwd, keyPath, parsedValue)` (module-private, exported only if tests need it — prefer testing through commands); `setConfigValue` becomes `return withPlanningLock(cwd, () => setConfigValueInLock(cwd, keyPath, parsedValue));` (all other callers unchanged).
    2. In `cmdConfigSet`, replace the migrate-then-set sequence: keep key validation, value parsing, D-05 rejection, and the context/drift/post_planning_gaps checks where they are (pure, pre-lock); then:
       ```js
       let setConfigValueResult;
       if (isSecretKey(keyPath)) {
         setConfigValueResult = withPlanningLock(cwd, () => {
           // WR-01: migration + mutation are ONE transaction under the planning lock.
           try {
             migrateLegacyIntegrationKeys(path.join(planningDir(cwd), 'config.json'), undefined, { alreadyLocked: true });
           } catch {
             error(`${keyPath}: legacy key migration failed — config not modified (fix ~/.oto permissions and retry)`);
           }
           return setConfigValueInLock(cwd, keyPath, parsedValue);
         });
         // WR-02 ordering preserved: warn AFTER migration (and after the lock releases).
         if (parsedValue === true) warnIfNoKeyDetected(keyPath);
       } else {
         setConfigValueResult = setConfigValue(cwd, keyPath, parsedValue);
       }
       ```
       (Adjust to the actual post-14-14 shape on read; the invariant is: ONE lock region containing migration + write for integration keys; unchanged path for others.)
    3. In `cmdConfigGet` (~411), capture the migration result: `let migrationFailed = false; let migrationSkipped = false; try { const m = migrateLegacyIntegrationKeys(configPath); migrationSkipped = Boolean(m && m.skipped); } catch { migrationFailed = true; }` then `if ((migrationFailed || migrationSkipped) && isSecretKey(keyPath)) error(...same sanitized message...)`. Unrelated keys keep failing open.

    **tests/14-migration-lock.test.cjs** — implement L1-L6 with the 14-migration-hardening fixture conventions; real-process cases spawn `oto/bin/lib/oto-tools.cjs` with HOME-overridden cleanEnv. Timing: L1's skip window is ≤2s + margin; keep assertions on OUTCOMES (bytes unchanged / skipped flag), never on precise timing.
  </action>
  <verify>
    <automated>node --test tests/14-migration-lock.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "withConfigDirLock" oto/bin/lib/secrets.cjs` ≥ 3 (definition, migrate wrapper, export)
    - `grep -n "alreadyLocked" oto/bin/lib/config.cjs` matches inside cmdConfigSet; `grep -c "setConfigValueInLock" oto/bin/lib/config.cjs` ≥ 3 (definition, setConfigValue wrapper, cmdConfigSet transaction)
    - Transaction pinned structurally: in the cmdConfigSet body, the `migrateLegacyIntegrationKeys` call and the `setConfigValueInLock` call both appear inside the same `withPlanningLock(` callback (sed the function range and verify both strings occur after the `withPlanningLock(` line and before its closing)
    - `node --test tests/14-migration-lock.test.cjs` exits 0 with ≥ 6 passing tests; the file contains `skipped`, `utimesSync`, and `sk-legacy-lock-0123456789`
    - `node --test tests/14-migration-hardening.test.cjs tests/14-config-boolean.test.cjs tests/14-configget-guard.test.cjs tests/14-secrets-keyfile.test.cjs` exits 0 (existing migration/config suites unbroken by the signature extension — it is backward-compatible)
  </acceptance_criteria>
  <done>CJS: migration cannot write while another process holds the config lock; integration config-set is one atomic lock region; sensitive reads withhold under contention while unrelated reads fail open; interleave regression proves both writers' changes survive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SDK — self-locking migration + configSet transaction + Vitest regressions</name>
  <files>sdk/src/query/secrets.ts, sdk/src/query/config-mutation.ts, sdk/src/query/migration-lock.test.ts</files>
  <read_first>
    - sdk/src/query/state-mutation.ts lines 175-230 (acquireStateLock/releaseStateLock semantics incl. the force-acquire-on-final-retry behavior)
    - sdk/src/query/config-mutation.ts configSet (post-14-14 state — the lock region and the pre-lock migration call at ~225-234)
    - sdk/src/query/secrets.ts migrateLegacyIntegrationKeys (post-14-13/14-15 state)
    - sdk/src/query/secret-commands.ts secretStatus :284 and sdk/src/config.ts loadConfig :173-176 (call sites that inherit self-locking — NOT modified)
  </read_first>
  <behavior>
    - Test S1 (transaction): seed config `{"brave_search":"sk-legacy-brave-0123456789","commit_docs":false}` → `await configSet(['exa_search','true'], projectDir)` → config has `exa_search === true`, `brave_search === true`, `commit_docs === false`; keyfile holds the legacy value; NO `config.json.lock` remains
    - Test S2 (self-locking standalone): with `vi.mock`-spied state-mutation, `await migrateLegacyIntegrationKeys(configPath)` (no opts) → `acquireStateLock` was called with `configPath` and `releaseStateLock` with the returned lock path; with `{ alreadyLocked: true }` → neither spy called
    - Test S3 (ordering inside configSet): with the same spies, `await configSet(['exa_search','true'], projectDir)` → exactly ONE acquireStateLock call (the mutator's own) — migration did not double-acquire (it received alreadyLocked)
    - Test S4 (no lock leak on failure): malformed config `{bad json` → `configSet(['exa_search','true'])` rejects (14-14 fail-closed) and no `.lock` file remains
  </behavior>
  <action>
    **sdk/src/query/secrets.ts**: import `acquireStateLock`, `releaseStateLock` from `./state-mutation.js` (no cycle — verified). Change `migrateLegacyIntegrationKeys(configPath, baseDir?)` to `(configPath, baseDir?, opts?: { alreadyLocked?: boolean })`: when not `alreadyLocked`, wrap the existing body in

    ```typescript
    const lockPath = await acquireStateLock(configPath);
    try {
      return await migrateBody();
    } finally {
      await releaseStateLock(lockPath);
    }
    ```

    (extract the current body into a local `migrateBody` closure; return shape unchanged). Document in the doc comment: the SDK lock force-acquires after ~2s against a live lock (existing state-mutation semantics — contention windows shrink from unbounded to ≤2s; changing lock internals is out of scope, noted in SUMMARY).

    **sdk/src/query/config-mutation.ts**: in `configSet`, DELETE the pre-lock migration block (~225-234) and re-issue it as the FIRST statement inside the lock's try block:

    ```typescript
    const lockPath = await acquireStateLock(paths.config);
    let previousValue: unknown;
    try {
      if (integrationKey) {
        // WR-01: migration + mutation are one transaction under the config lock.
        try {
          await migrateLegacyIntegrationKeys(paths.config, undefined, { alreadyLocked: true });
        } catch {
          throw new GSDError(
            `${keyPath}: legacy key migration failed — config not modified (fix ~/.oto permissions and retry)`,
            ErrorClassification.Execution,
          );
        }
      }
      const config = await readConfigForMutation(paths.config);
      ...
    ```

    Keep `warnIfNoKeyDetected` where 14-11 placed it relative to migration (after), moving it after the lock region if it currently sits between the old migration site and the lock. configSetModelProfile/configEnsureSection are NOT integration paths — unchanged.

    **sdk/src/query/migration-lock.test.ts** — implement S1-S4. For spy tests use `vi.mock('./state-mutation.js', async (importOriginal) => { const actual = await importOriginal(); return { ...actual, acquireStateLock: vi.fn(actual.acquireStateLock), releaseStateLock: vi.fn(actual.releaseStateLock) }; })` and import the mocked module to read the spies. Fixture: mkdtemp + stubbed HOME + integration env vars `''`.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/query/migration-lock.test.ts src/query/config-mutation.test.ts src/query/config-mutation-failclosed.test.ts src/query/secrets.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "alreadyLocked: true" sdk/src/query/config-mutation.ts` matches inside configSet's lock try block (line number GREATER than the `acquireStateLock` line in the same function)
    - `grep -c "alreadyLocked" sdk/src/query/secrets.ts` ≥ 2 (option type + branch)
    - `cd sdk && npx vitest run src/query/migration-lock.test.ts` exits 0 with ≥ 4 passing tests
    - `cd sdk && npx vitest run src/query/config-mutation.test.ts src/query/config-mutation-failclosed.test.ts src/query/secrets.test.ts src/query/secrets-empty-keyfile.test.ts src/query/secret-commands.test.ts` exits 0 (all prior-wave suites green on the merged tree)
    - `cd sdk && npx tsc --noEmit` exits 0
    - `git diff --name-only` shows NO changes under sdk/dist (rebuild deferred to plan 14-19)
  </acceptance_criteria>
  <done>SDK: migration self-locks on every read path and joins the mutator's transaction on the write path; no double-acquire, no lock leaks, all prior suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent oto processes → shared config.json | Two writers (one migrating, one setting) race on a whole-file read/modify/write (WR-01) |
| Lock files on disk → liveness decisions | Stale/dead locks must be evicted without letting a live writer be clobbered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-18-01 | Tampering (lost update) | migrateLegacyIntegrationKeys (both layers) | mitigate | Same-lock transaction with writers; lock-respect + interleave regressions (L1/L4/S1) |
| T-14-18-02 | Denial of Service (read paths deadlock on a held lock) | loaders/config-get/secret-status | mitigate | CJS skip-after-2s best-effort semantics; sensitive reads withhold, unrelated reads fail open (L6) |
| T-14-18-03 | Denial of Service (leaked lock blocks all writers) | withConfigDirLock / acquireStateLock usage | mitigate | try/finally release + exit-cleanup sweep + stale eviction; no-leak assertions in every test |
| T-14-18-04 | Tampering (SDK force-acquire steals a live lock after ~2s) | state-mutation acquireStateLock | accept | Pre-existing lock-internal semantics; contention window shrinks from unbounded to ≤2s; changing lock internals is out of Phase 14 scope — recorded in SUMMARY and 14-DISPOSITIONS.md |
</threat_model>

<verification>
- `node --test tests/14-migration-lock.test.cjs tests/14-migration-hardening.test.cjs tests/14-configget-guard.test.cjs` exits 0
- `cd sdk && npx vitest run src/query/migration-lock.test.ts src/query/config-mutation.test.ts src/query/config-mutation-failclosed.test.ts` exits 0
- `cd sdk && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- WR-01 closed: the reviewer's while-locked clobber is a pinned, passing regression in CJS; both layers run migration+mutation as one transaction; read-triggered migration cannot destroy a concurrent writer's update
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-18-SUMMARY.md`
</output>
