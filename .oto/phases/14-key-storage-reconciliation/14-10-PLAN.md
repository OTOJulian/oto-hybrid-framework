---
phase: 14-key-storage-reconciliation
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/bin/lib/core.cjs
  - tests/14-loader-credential-survival.test.cjs
autonomous: true
gap_closure: true
requirements: [SECR-01, SECR-03]

must_haves:
  truths:
    - "A legacy integration string in .oto/config.json survives on disk when self-heal migration fails, even when loadConfig performs an unrelated dirty write (depth→granularity, multiRepo, sub_repos sync)"
    - "loadConfig never returns an integration string (in-memory boolean-only contract stays intact after the fix)"
    - "The happy-path migration (writable ~/.oto) still heals the string to a 0600 keyfile with boolean true in config.json"
  artifacts:
    - path: "oto/bin/lib/core.cjs"
      provides: "loadConfig with pristine fileData for disk writes; scrub applied only to the merged in-memory view"
      contains: "_scrubIntegrationStrings(rootParsed"
    - path: "tests/14-loader-credential-survival.test.cjs"
      provides: "Regression test pinning the exact Gap 1 reproduction (legacy string + broken ~/.oto + depth key)"
      min_lines: 40
  key_links:
    - from: "oto/bin/lib/core.cjs loadConfig"
      to: "in-memory effective config (parsed)"
      via: "scrub applied at the merge point AFTER all configDirty write-backs"
      pattern: "_scrubIntegrationStrings\\(rootParsed \\? _deepMergeConfig"
    - from: "oto/bin/lib/core.cjs fileData"
      to: "config.json disk writes"
      via: "unscrubbed JSON.parse(raw) — dirty-write paths persist the pristine object"
      pattern: "const fileData = JSON\\.parse\\(raw\\);"
---

<objective>
Close Gap 1 from 14-VERIFICATION.md (regression introduced by 14-07): the CJS loader scrub at `oto/bin/lib/core.cjs:358` mutates `fileData` — the object the loader's own dirty-write paths (depth→granularity at ~365, multiRepo/sub_repos/configDirty at ~375-414) persist back to disk. When `migrateLegacyIntegrationKeys` fails (unusable `~/.oto`), the legacy string is flipped to `true` in `fileData` and written to config.json with NO keyfile ever created — the only stored credential is destroyed.

Purpose: restore the fail-closed invariant that `tests/14-migration-hardening.test.cjs` ("the only stored credential is never overwritten") pins for the config-set path, on the loader path. Verifier confirmed the review's suggested patch (14-REVIEW.md CR-01) is sound.
Output: fixed `loadConfig` in core.cjs + a regression test pinning the exact reproduction.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/14-key-storage-reconciliation/14-VERIFICATION.md
@.oto/phases/14-key-storage-reconciliation/14-REVIEW.md
@oto/bin/lib/core.cjs
@tests/14-migration-hardening.test.cjs
</context>

<interfaces>
Current defective code in `oto/bin/lib/core.cjs` (post-14-07):

```js
// line ~358 (inside loadConfig try block) — THE BUG: scrub mutates the disk-write object
const fileData = _scrubIntegrationStrings(JSON.parse(raw));
...
// line ~418 — parsed aliases fileData in the non-workstream case
const parsed = rootParsed ? _deepMergeConfig(rootParsed, fileData) : fileData;
```

`_deepMergeConfig(base, overlay)` (core.cjs:307-319) returns a FRESH object (`const result = { ...base }`) — it never mutates `overlay`. `_scrubIntegrationStrings(obj)` (core.cjs:322-328) mutates `obj` in place for top-level keys `exa_search`, `brave_search`, `firecrawl`.

Root-layer scrub at core.cjs:342 (`rootParsed = _scrubIntegrationStrings(JSON.parse(raw))`) is SAFE — `rootParsed` is never written back to disk. Leave it unchanged.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep fileData pristine; scrub only the merged in-memory view</name>
  <files>oto/bin/lib/core.cjs</files>
  <read_first>
    - oto/bin/lib/core.cjs lines 320-420 (the `_scrubIntegrationStrings` helper, `loadConfig` from the workstream branch through the `parsed` merge point)
    - tests/14-migration-hardening.test.cjs lines 159-186 (existing loader-scrub tests that MUST keep passing after this change)
  </read_first>
  <behavior>
    - Test 1 (new, Task 2): legacy string + broken ~/.oto + depth key → config.json still contains the string after loadConfig()
    - Test 2 (existing, must stay green): loadConfig returns `true` (not the string) when migration is impossible — with and without OTO_WORKSTREAM (tests/14-migration-hardening.test.cjs lines 159-186)
  </behavior>
  <action>
    Two exact edits inside `loadConfig` in `oto/bin/lib/core.cjs` (this file is GSD-shared: keep the diff minimal and commented, per sync hygiene):

    1. Line ~358 — remove the scrub from the disk-write object, restoring the documented "fileData is kept for disk writes only" contract:

       BEFORE: `const fileData = _scrubIntegrationStrings(JSON.parse(raw));`
       AFTER:  `const fileData = JSON.parse(raw);`

    2. Line ~418 — apply the scrub to the merged/effective in-memory view instead, AFTER all configDirty/depth write-backs have completed. Use a shallow copy in the non-workstream branch so the scrub cannot mutate `fileData` through aliasing (the scrub only touches top-level keys, so a shallow copy is sufficient; `_deepMergeConfig` already returns a fresh object). Write the assignment as ONE line — the downstream verifier's must_haves grep patterns line-match `_scrubIntegrationStrings(rootParsed ? _deepMergeConfig`, so do NOT wrap it across lines:

       BEFORE: `const parsed = rootParsed ? _deepMergeConfig(rootParsed, fileData) : fileData;`
       AFTER:
       ```js
       // OTO Phase 14 gap-closure (SECR-01/SECR-03, Gap 1): scrub ONLY the effective
       // in-memory view — never fileData, which the dirty-write paths above persist
       // to disk. A failed migration must leave the legacy string on disk intact.
       const parsed = _scrubIntegrationStrings(rootParsed ? _deepMergeConfig(rootParsed, fileData) : { ...fileData });
       ```

    Do NOT touch the root-layer scrub at line ~342 (`rootParsed` is never written back). Do NOT change `_scrubIntegrationStrings` itself. Do NOT reorder the depth/multiRepo/sub_repos migration blocks.
  </action>
  <verify>
    <automated>node --test tests/14-migration-hardening.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_scrubIntegrationStrings(JSON.parse(raw))' oto/bin/lib/core.cjs` returns 1 (only the rootParsed occurrence at ~342 remains; the fileData occurrence is gone)
    - `grep -n 'const fileData = JSON.parse(raw);' oto/bin/lib/core.cjs` matches exactly one line
    - `grep -n '_scrubIntegrationStrings(rootParsed ? _deepMergeConfig' oto/bin/lib/core.cjs` matches exactly one line (the new single-line `parsed` assignment)
    - `node --test tests/14-migration-hardening.test.cjs` → all existing tests pass (including the two "scrubs ... when migration is impossible" cases at lines 159-186)
  </acceptance_criteria>
  <done>fileData is never scrubbed; the scrub runs once on the merged in-memory view after all disk write-backs; all pre-existing Phase 14 loader tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test — credential survives a failed migration plus a loader dirty write</name>
  <files>tests/14-loader-credential-survival.test.cjs</files>
  <read_first>
    - tests/14-migration-hardening.test.cjs (copy its harness verbatim: `seedFixture`, `cleanEnv`, `runLoadConfig` spawnSync pattern with HOME override and the `node -e` LOAD_CONFIG_SCRIPT that requires oto/bin/lib/core.cjs)
    - oto/bin/lib/core.cjs lines 360-366 (depth→granularity dirty-write path the test must trigger)
  </read_first>
  <action>
    Create `tests/14-loader-credential-survival.test.cjs` (node:test, CJS) pinning the exact Gap 1 reproduction from 14-VERIFICATION.md. Reuse the fixture pattern from tests/14-migration-hardening.test.cjs (mkdtemp project + home, spawnSync `node -e` script requiring `oto/bin/lib/core.cjs`, env `{ HOME: home, EXA_API_KEY: '', BRAVE_API_KEY: '', FIRECRAWL_API_KEY: '' }`).

    Test 1 — "failed migration + depth dirty-write leaves the legacy string on disk":
    - Seed `.oto/config.json` with `{"exa_search": "sk-test-survival-0123456789", "depth": "standard"}`
    - Make migration impossible: `fs.writeFileSync(path.join(home, '.oto'), 'not-a-dir')` (regular file where the dir should be)
    - Run loadConfig once via the spawned script; assert exit 0
    - Assert `JSON.parse(fs.readFileSync(configPath)).exa_search === 'sk-test-survival-0123456789'` (credential SURVIVES on disk)
    - Assert the dirty write itself happened: config has `granularity: "standard"` and no `depth` key (proves the write-back path ran and still preserved the string)
    - Assert no keyfile exists (`fs.existsSync(path.join(home, '.oto', 'exa_api_key')) === false` — home/.oto is a file)
    - Assert the spawned script's stdout shows the IN-MEMORY value is `true` (script prints `JSON.stringify(loadConfig(process.cwd()).exa_search)`) — boolean-only loader contract intact
    - Assert stdout+stderr never contain the marker string

    Test 2 — "happy path still heals": same seed but with a writable home; after loadConfig, config.json has `exa_search: true`, keyfile `home/.oto/exa_api_key` contains the marker with mode 0600, and `granularity` migrated.

    Test 3 — "failed migration + sub_repos/configDirty write-back also preserves the string": seed `{"exa_search": "sk-test-survival-0123456789", "sub_repos": ["child"]}` (stale top-level sub_repos triggers the configDirty strip at core.cjs ~387-394), broken home/.oto; after loadConfig, config.json still contains the string and `planning.sub_repos` exists while top-level `sub_repos` is gone.
  </action>
  <verify>
    <automated>node --test tests/14-loader-credential-survival.test.cjs && node --test tests/14-migration-hardening.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/14-loader-credential-survival.test.cjs` → all 3 tests pass
    - `grep -c "sk-test-survival" tests/14-loader-credential-survival.test.cjs` ≥ 3 (marker used in every survival assertion)
    - `grep -n "granularity" tests/14-loader-credential-survival.test.cjs` matches (dirty-write co-trigger asserted, not just the string)
    - Reverting Task 1's core.cjs edit makes Test 1 fail (spot-check: the test genuinely pins the regression — verify by inspection that Test 1 asserts the on-disk string, which the pre-fix code flips to `true`)
  </acceptance_criteria>
  <done>Regression test exists and passes; the exact verifier reproduction (legacy string + broken ~/.oto + depth key) is pinned; the full Phase 14 CJS suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json (git-tracked) → loader | Legacy plaintext credentials may cross here; loader must heal or preserve, never destroy |
| loader → disk write-back | Self-heal writes must never persist a lossy transformation of the credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10-01 | Tampering (data loss) | core.cjs loadConfig dirty-write paths | mitigate | Scrub only the in-memory view; fileData stays byte-faithful for all disk writes (Task 1) |
| T-14-10-02 | Information Disclosure | core.cjs loadConfig return value | mitigate | Scrub retained on merged `parsed` — no integration string ever leaves loadConfig (existing tests re-pinned) |
</threat_model>

<verification>
- `node --test tests/14-*.test.cjs` → full Phase 14 CJS suite passes (46 pre-existing + 3 new)
- Manual trace: `grep -n '_scrubIntegrationStrings' oto/bin/lib/core.cjs` shows exactly two call sites — rootParsed (~342) and the merged `parsed` (~418) — and zero on `fileData`
</verification>

<success_criteria>
- Gap 1 truth holds: legacy string + broken ~/.oto + any loader dirty write → config.json still contains the string; no credential destruction path remains in loadConfig
- Boolean-only in-memory contract unchanged (existing 14-migration-hardening scrub tests pass)
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-10-SUMMARY.md`
</output>
