---
phase: 14-key-storage-reconciliation
plan: 19
type: execute
wave: 4
depends_on: ["14-13", "14-14", "14-15", "14-16", "14-17", "14-18"]
files_modified:
  - sdk/dist/query/secrets.js
  - sdk/dist/query/secrets.js.map
  - sdk/dist/query/secrets.d.ts
  - sdk/dist/query/secrets.d.ts.map
  - sdk/dist/query/config-mutation.js
  - sdk/dist/query/config-mutation.js.map
  - sdk/dist/query/config-mutation.d.ts
  - sdk/dist/query/config-mutation.d.ts.map
  - sdk/dist/query/secret-commands.js
  - sdk/dist/query/secret-commands.js.map
  - sdk/dist/query/index.js
  - sdk/dist/query/index.js.map
  - sdk/dist/config.js
  - sdk/dist/config.js.map
  - sdk/dist/cli.js
  - sdk/dist/cli.js.map
  - .oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt
autonomous: true
gap_closure: true
requirements: [SECR-01, SECR-02, SECR-03, SECR-04]

must_haves:
  truths:
    - "GATE PRECONDITION (hard gate, not a suggestion): this plan MUST NOT start until every earlier-wave plan (14-13..14-18) has passed its own <verify> chain — Wave N+1 begins only after Wave N verification PASSES"
    - "sdk/dist is rebuilt exactly once, carrying ALL SDK source changes from plans 14-13 through 14-18, and every regenerated dist artifact is committed"
    - "The verifier's exact Gap 1/2/3 reproductions fail closed or heal correctly against BOTH the CJS CLI and the rebuilt SDK dist CLI, recorded as SIX machine-checkable verdict lines (GAP1-CJS, GAP1-SDK, GAP2-SDK, GAP3-CONFIGSET, GAP3-SECRETSET, GAP3-SECRETCLEAR — each ': PASS') in 14-SDK-BASELINE-DELTA.txt"
    - "FOUR-PART TERMINAL GATE holds: (1) all focused Phase 14 CJS suites pass; (2) the enumerated focused Phase 14 SDK suites pass; (3) npx tsc --noEmit passes from sdk/; (4) the full SDK suite introduces NO PERSISTENT failure beyond the two-run-union baseline in 14-SDK-BASELINE.txt — every apparent offender (newly-failing file or count increase over the recorded per-file max) is adjudicated via two isolated re-runs, FLAKEs are recorded with evidence and excluded, and only persistent failures fail the gate or consume the cycle budget"
    - "BOUNDED CONVERGENCE: Phase 14 completes only per the convergence contract mirrored in 14-DISPOSITIONS.md — fresh verifier pass (4/4 requirements), fresh review with zero unresolved Criticals, every Warning dispositioned with evidence; no more than TWO plan-review revision cycles remain permitted, and if the blocker count does not decrease between cycles, STOP and report the unresolved contradiction instead of generating another gap-plan loop"
  artifacts:
    - path: "sdk/dist/query/secrets.js"
      provides: "Built artifact carrying 14-13 (empty-keyfile + symlink guards), 14-15 (deep scan + two-phase reconcile), 14-18 (self-locking migration)"
      contains: "findNestedIntegrationString"
    - path: "sdk/dist/query/config-mutation.js"
      provides: "Built artifact carrying 14-14 (readConfigForMutation), 14-15 (shape guard), 14-18 (in-lock migration)"
      contains: "readConfigForMutation"
    - path: "sdk/dist/query/secret-commands.js"
      provides: "Built artifact carrying 14-14's fail-closed hidden input"
      contains: "secure key entry unavailable"
    - path: "sdk/dist/config.js"
      provides: "Built artifact carrying 14-16's scrub + root inheritance"
      contains: "deepMergeConfig"
    - path: ".oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt"
      provides: "Six gap-reproduction verdict lines + post-fix full-suite run + machine-checked comparison against the 14-SDK-BASELINE.txt two-run union with per-offender FLAKE/PERSISTENT adjudication evidence"
      min_lines: 20
  key_links:
    - from: "sdk/src (all Phase 14 changes)"
      to: "sdk/dist (shipped surface)"
      via: "single tsc build"
      pattern: "readConfigForMutation"
    - from: "14-SDK-BASELINE.txt"
      to: "14-SDK-BASELINE-DELTA.txt"
      via: "union failed-file set comparison with isolated-re-run flake adjudication (no new PERSISTENT failures rule)"
      pattern: "NO NEW FAILURES"
---

<objective>
Terminal convergence plan: ship every prior wave's SDK fix through the phase's SINGLE sdk/dist rebuild (14-08/14-12 pattern), re-run the verifier's real-process reproductions for all three 2026-07-12 blockers against the shipped surfaces, and enforce the four-part terminal gate plus the bounded convergence contract (required amendments 1 and 7).

**Wave gating (explicit, per amendment 6):** Wave 1 (14-13, 14-14, 14-16, 14-17), Wave 2 (14-15), and Wave 3 (14-18) verification must PASS before this plan begins. This is a gate, not a suggestion — if any earlier plan's verify chain is red, stop and fix that plan first; do not rebuild dist over a red tree.

**Why the SDK gate is baseline-relative AND flake-adjudicated (amendment 1):** the full SDK suite carries pre-existing debt unrelated to Phase 14 (advisory context from 2026-07-12 planning: ~267 failed / ~1228 passed across ~41 failed files; the AUTHORITATIVE numbers are whatever plan 14-16 captured in 14-SDK-BASELINE.txt), and it has MEASURED inter-run flake drift on an unchanged tree (267 failed/41 files vs 259 failed/39 files across two same-tree runs). Requiring full-suite green would make the gate impossible without owning debt this phase must not own; a naive single-run set-comparison would false-fail on flakes and burn the bounded convergence budget on noise. The gate is therefore: focused suites green + typecheck green + full suite no worse than the TWO-RUN-UNION baseline, with every apparent offender adjudicated via isolated re-runs before it may be called a regression.

**Commit-evidence convention (amendment 6):** NEVER use `git status --porcelain` to prove files were committed. Use `git diff --name-only <sha-before>..<sha-after>` (capture `git rev-parse HEAD` before and after the commit) or `git log --stat -1`.

Purpose: SECR-01..04 verified against what actually ships, with an honest, bounded, flake-tolerant definition of done.
Output: rebuilt committed dist, machine-checkable reproduction verdicts, baseline-delta artifact with adjudication evidence, convergence-contract record.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/14-key-storage-reconciliation/14-VERIFICATION.md
@.oto/phases/14-key-storage-reconciliation/14-DISPOSITIONS.md
@.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt
@sdk/package.json
@bin/oto-sdk.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-gate check + single sdk/dist rebuild</name>
  <files>sdk/dist/query/secrets.js, sdk/dist/query/secrets.js.map, sdk/dist/query/secrets.d.ts, sdk/dist/query/secrets.d.ts.map, sdk/dist/query/config-mutation.js, sdk/dist/query/config-mutation.js.map, sdk/dist/query/config-mutation.d.ts, sdk/dist/query/config-mutation.d.ts.map, sdk/dist/query/secret-commands.js, sdk/dist/query/secret-commands.js.map, sdk/dist/query/index.js, sdk/dist/query/index.js.map, sdk/dist/config.js, sdk/dist/config.js.map, sdk/dist/cli.js, sdk/dist/cli.js.map</files>
  <read_first>
    - .oto/phases/14-key-storage-reconciliation/14-13-SUMMARY.md through 14-18-SUMMARY.md (confirm every earlier plan completed and its verify chain passed — the WAVE GATE; if any SUMMARY is missing or reports failure, STOP and report instead of proceeding)
    - sdk/package.json scripts block (build = tsc)
    - .oto/phases/14-key-storage-reconciliation/14-12-SUMMARY.md (the prior single-rebuild + dist-smoke procedure this task repeats)
  </read_first>
  <action>
    1. WAVE GATE: verify all six prior SUMMARYs exist and report success. Record the pre-commit sha: `SHA_BEFORE=$(git rev-parse HEAD)`.
    2. `cd sdk && npm run build`. The rebuild picks up ALL SDK source changes from plans 14-13 (secrets.ts), 14-14 (config-mutation.ts, index.ts, secret-commands.ts), 14-15 (config-mutation.ts, secrets.ts), 14-16 (config.ts, cli.ts), and 14-18 (secrets.ts, config-mutation.ts). Commit every regenerated dist file — dist is committed by project convention. If tsc regenerates dist files beyond those listed in frontmatter (index barrels, helpers, .d.ts siblings), commit those too — the invariant is "dist matches src", not the file list.
    3. Evidence of commit (NOT git status --porcelain): `git diff --name-only "$SHA_BEFORE"..HEAD` lists the dist files; include that listing in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c "findNestedIntegrationString" sdk/dist/query/secrets.js && grep -c "readConfigForMutation" sdk/dist/query/config-mutation.js && grep -c "secure key entry unavailable" sdk/dist/query/secret-commands.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "findNestedIntegrationString" sdk/dist/query/secrets.js` ≥ 2 and `grep -n "if (value === '') return null" sdk/dist/query/secrets.js` matches (14-13/14-15 shipped)
    - `grep -c "readConfigForMutation" sdk/dist/query/config-mutation.js` ≥ 4 and `grep -c "expected a JSON object" sdk/dist/query/config-mutation.js` ≥ 1 (14-14/14-15 shipped)
    - `grep -c "secure key entry unavailable" sdk/dist/query/secret-commands.js` ≥ 1 (14-14 WR-08 shipped); `grep -c "deepMergeConfig" sdk/dist/config.js` ≥ 1 (14-16 WR-09 shipped); `grep -c "alreadyLocked" sdk/dist/query/config-mutation.js` ≥ 1 (14-18 shipped)
    - `grep -c "findNestedIntegrationString" sdk/dist/query/secrets.d.ts` ≥ 1 (regenerated declarations carry the new export)
    - `git diff --name-only "$SHA_BEFORE"..HEAD` includes sdk/dist files (js + maps + d.ts) — recorded in the SUMMARY; `git status --porcelain` is NOT used as commit evidence anywhere in this plan's outputs
  </acceptance_criteria>
  <done>The committed dist byte-for-byte reflects the merged Phase 14 sources; commit evidence is sha-ranged.</done>
</task>

<task type="auto">
  <name>Task 2: Real-process reproductions for all three gaps against CJS and rebuilt dist, with machine-checkable verdict lines</name>
  <files>.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt</files>
  <read_first>
    - .oto/phases/14-key-storage-reconciliation/14-VERIFICATION.md (the exact original reproductions being inverted)
    - bin/oto-sdk.js (confirm arg passing to sdk/dist/cli.js)
  </read_first>
  <action>
    All commands use an isolated `HOME=$(mktemp -d)` and a `mktemp -d` project dir with EXA_API_KEY/BRAVE_API_KEY/FIRECRAWL_API_KEY set to empty strings. Record every command + outcome in the SUMMARY (they also seed the DELTA file's evidence section).

    **Gap 1 — CJS CLI** (was: exit 0 + plaintext at 0.exa_search):
    ```bash
    node <repo>/oto/bin/lib/oto-tools.cjs config-new-project '[{"exa_search":"sk-gap1-marker-0123456789"}]'
    ```
    cwd = tmp project. Expected: non-zero exit; `<project>/.oto` absent entirely (mkdir-after-validation); `grep -r "sk-gap1-marker" <project>` finds nothing. → verdict `GAP1-CJS`.

    **Gap 1 — SDK dist CLI**:
    ```bash
    node <repo>/bin/oto-sdk.js query config-new-project '[{"exa_search":"sk-gap1-marker-0123456789"}]'
    ```
    fresh tmp project. Expected: non-zero exit; no `.oto` dir; no marker anywhere under the project. → verdict `GAP1-SDK`.

    **Gap 2 — SDK dist** (was: valid legacy credential destroyed; status showed `((unset))`): seed `<project>/.oto/config.json` = `{"exa_search":"sk-legacy-gap2-0123456789"}` and a ZERO-BYTE `$HOME/.oto/exa_api_key`, then:
    ```bash
    node <repo>/bin/oto-sdk.js query secret-status exa
    ```
    Expected: `$HOME/.oto/exa_api_key` now contains `sk-legacy-gap2-0123456789`; keyfile mode asserted PORTABLY via Node (never `stat -f`):
    ```bash
    node -e "process.exit(((require('fs').statSync(process.argv[1]).mode & 0o777) === 0o600) ? 0 : 1)" "$HOME/.oto/exa_api_key"
    ```
    config `exa_search` is `true`; output contains `key from ~/.oto/exa_api_key (****6789)` — NOT `((unset))`. → verdict `GAP2-SDK`.

    **Gap 3 — SDK dist** (was: exit 0 + file replaced): seed `<project>/.oto/config.json` with the literal bytes `{bad json`, then run each of:
    ```bash
    node <repo>/bin/oto-sdk.js query config-set model_profile quality          # expect non-zero → verdict GAP3-CONFIGSET
    printf 'sk-gap3-key-abcd1234\n' | node <repo>/bin/oto-sdk.js query secret-set exa   # expect non-zero → verdict GAP3-SECRETSET
    node <repo>/bin/oto-sdk.js query secret-clear exa                          # expect non-zero (pre-seed a keyfile first) → verdict GAP3-SECRETCLEAR
    ```
    After each: `cat <project>/.oto/config.json` byte-identical to `{bad json`. After secret-set: no `$HOME/.oto/exa_api_key`. After secret-clear (pre-seeded keyfile `sk-prior-key-9999wxyz`): keyfile still exists with identical content.

    **Verdict recording (machine-checkable):** create `.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt` (Write tool) opening with a `## Gap Reproductions` section. For each of the six reproductions record the command, exit code, and post-state checks, then exactly one structured verdict line per reproduction, each on its own line:
    ```
    GAP1-CJS: PASS
    GAP1-SDK: PASS
    GAP2-SDK: PASS
    GAP3-CONFIGSET: PASS
    GAP3-SECRETSET: PASS
    GAP3-SECRETCLEAR: PASS
    ```
    A verdict line may read `PASS` ONLY when every expected post-fix outcome for that reproduction actually held; otherwise write `FAIL` — never hand-edit a verdict to PASS. Task 3 APPENDS to this file and must preserve this section.

    If ANY reproduction fails: fix the SOURCE (not the repro), rebuild, re-run — never commit a dist that fails a reproduction. If the fix requires re-opening an earlier plan's file, record it in the SUMMARY and count it toward the convergence-cycle budget (Task 3).
  </action>
  <verify>
    <automated>for v in "GAP1-CJS: PASS" "GAP1-SDK: PASS" "GAP2-SDK: PASS" "GAP3-CONFIGSET: PASS" "GAP3-SECRETSET: PASS" "GAP3-SECRETCLEAR: PASS"; do grep -q "^${v}$" .oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt || { echo "MISSING VERDICT: ${v}"; exit 1; }; done; echo "all six gap verdicts PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All six reproduction commands produce the expected post-fix outcomes (non-zero exits, byte preservation, healed keyfile at Node-asserted mode 0600, no marker in any project file, no `.oto` dir on rejected new-project)
    - 14-SDK-BASELINE-DELTA.txt exists, opens with `## Gap Reproductions`, and contains all six verdict lines exactly (`GAP1-CJS: PASS`, `GAP1-SDK: PASS`, `GAP2-SDK: PASS`, `GAP3-CONFIGSET: PASS`, `GAP3-SECRETSET: PASS`, `GAP3-SECRETCLEAR: PASS`), each preceded by its command + exit code + post-state evidence
    - The Gap 2 mode check uses the Node statSync one-liner — `stat -f` appears nowhere in this plan's outputs or SUMMARY
    - Reproduction evidence (command + exit code + post-state) recorded in the SUMMARY
  </acceptance_criteria>
  <done>The three 14-VERIFICATION blockers are irreproducible against both shipped surfaces, and the six verdicts are grep-gateable in the DELTA artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Four-part terminal gate + flake-adjudicated baseline delta + convergence contract</name>
  <files>.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt</files>
  <read_first>
    - .oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE.txt (the authoritative two-run-union baseline from plan 14-16: `## Failed Files (union)` with per-file `max_failed=` counts)
    - .oto/phases/14-key-storage-reconciliation/14-DISPOSITIONS.md (the convergence contract this gate enforces)
  </read_first>
  <action>
    Run the four-part gate, recorded verbatim (amendment 1):

    **Part 1 — focused CJS:**
    ```bash
    node --test --test-reporter=dot tests/14-*.test.cjs
    ```
    Must exit 0. (Covers all Phase 14 CJS suites including the new 14-empty-keyfile, 14-keyfile-symlink, 14-newproject-shape-guard, 14-loader-scrub, 14-migration-lock, 14-settings-workflow-contract, 14-no-plaintext-guard files.)

    **Part 2 — focused SDK (exact enumerated list — every Phase-14-relevant test file, including all files added by plans 14-13..14-18):**
    ```bash
    cd sdk && npx vitest run \
      src/query/secrets.test.ts \
      src/query/secrets-empty-keyfile.test.ts \
      src/query/secrets-symlink.test.ts \
      src/query/secret-commands.test.ts \
      src/query/secret-input-failclosed.test.ts \
      src/query/config-mutation.test.ts \
      src/query/config-mutation-failclosed.test.ts \
      src/query/config-newproject-shape.test.ts \
      src/query/config-query.test.ts \
      src/query/registry.test.ts \
      src/query/migration-lock.test.ts \
      src/config.test.ts \
      src/config-loader-parity.test.ts \
      src/cli.test.ts
    ```
    Must exit 0 with zero failures. (If any earlier plan renamed/added a test file, adjust the list to the ACTUAL file set and record the final list in the SUMMARY.)

    **Part 3 — typecheck:**
    ```bash
    cd sdk && npx tsc --noEmit
    ```
    Must exit 0.

    **Part 4 — full-suite baseline delta with mandatory flake adjudication:**
    ```bash
    cd sdk && npx vitest run > /tmp/vitest-final-raw.txt 2>&1 || true
    ```
    Extract the final run's failed-file set and per-file failure counts the same way plan 14-16 did, then compare against 14-SDK-BASELINE.txt's `## Failed Files (union)` section (the baseline is the UNION of two pre-fix runs with per-file MAX counts):
    - Rule (a): every finally-failing file must appear in the baseline union.
    - Rule (b): for files in the union, the final failure count must be ≤ the recorded `max_failed` value.

    **ADJUDICATION (mandatory BEFORE declaring any regression — the suite has documented inter-run flake drift):** for EACH rule-offending file (a new failing file, or a union file exceeding its max), re-run that file in isolation TWICE (`npx vitest run <file>`, two separate invocations). Classify:
    - `FLAKE`: at least one isolated re-run meets the file's baseline criterion (full pass for a file absent from the union; failure count ≤ its `max_failed` for a union file). Record `FLAKE: <file>` in the DELTA with the evidence (both isolated-run summaries) and EXCLUDE it from the verdict.
    - `PERSISTENT`: BOTH isolated re-runs exceed the baseline criterion. This is a Phase 14 regression.

    APPEND to `.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt` (preserving Task 2's `## Gap Reproductions` section and its six verdict lines): the baseline union summary, the final-run summary, both failed-file sets, the per-file count comparison, an `## Adjudication` section (each offender with its FLAKE/PERSISTENT classification and both isolated-run summaries, or the literal `NONE` when there were no offenders), and the verdict line `NO NEW FAILURES: PASS` (zero PERSISTENT offenders) or `NO NEW FAILURES: FAIL` naming the persistent files. Advisory planning-time numbers (2026-07-12: ~267 failed / ~1228 passed / ~41 failed files) are context only — the comparison is baseline-file vs final-run.

    Only PERSISTENT failures fail the gate and count toward the convergence-cycle budget; FLAKE classifications do not. If Part 4 fails (persistent offenders): those files are Phase 14 regressions — fix source, rebuild (return to Task 1), re-run. Count the cycle.

    **Convergence contract (amendment 7 — record this verbatim in the SUMMARY and confirm it matches 14-DISPOSITIONS.md):**
    Phase 14 completes ONLY when:
    1. Fresh independent verification (oto-verifier) reports `passed` with 4/4 requirements (SECR-01..04) satisfied.
    2. A fresh code review reports zero unresolved Critical findings.
    3. Every Warning is explicitly FIX / ACCEPT / DEFER with evidence (14-DISPOSITIONS.md current).
    4. Focused Phase 14 tests (CJS + SDK, Parts 1-2) and `npx tsc --noEmit` (Part 3) pass.
    5. The full SDK suite introduces no PERSISTENT failures beyond the recorded two-run-union baseline (Part 4, after flake adjudication).

    **Bounded convergence:** no more than TWO further plan-review revision cycles are permitted after this plan. If the blocker count does not decrease between consecutive cycles, STOP — report the unresolved contradiction to the developer rather than generating another gap-plan loop. Record the current cycle number in the SUMMARY (this revision is cycle 1 of the bound). Adjudicated FLAKEs never consume a cycle.
  </action>
  <verify>
    <automated>node --test --test-reporter=dot tests/14-*.test.cjs && cd sdk && npx tsc --noEmit && grep -c "## Adjudication" ../.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt && grep -c "NO NEW FAILURES: PASS" ../.oto/phases/14-key-storage-reconciliation/14-SDK-BASELINE-DELTA.txt</automated>
  </verify>
  <acceptance_criteria>
    - Part 1 exits 0; Part 2 exits 0 with the enumerated list (final list recorded); Part 3 exits 0
    - 14-SDK-BASELINE-DELTA.txt contains both failed-file sets, the per-file comparison against the union's `max_failed` values, an `## Adjudication` section (offenders classified FLAKE/PERSISTENT with both isolated-run summaries each, or `NONE`), and the line `NO NEW FAILURES: PASS`
    - Every FLAKE entry carries isolated-re-run evidence; no offender was excluded from the verdict without a recorded `FLAKE: <file>` line and evidence
    - Task 2's `## Gap Reproductions` section and all six `GAP*: PASS` verdict lines are still present in the final DELTA file (append-only)
    - The SUMMARY records the convergence contract verbatim, the cycle count, and the sha-ranged commit evidence from Task 1
    - Next step stated in the SUMMARY: run fresh independent verification (`/oto-verify-phase 14` or orchestrator equivalent) and a fresh code review — phase completion is gated on contract items 1-2, which this plan cannot self-certify
  </acceptance_criteria>
  <done>The four-part gate passes, the baseline-delta artifact proves no new PERSISTENT SDK debt (flakes adjudicated and evidenced, not miscounted as regressions), and the bounded convergence contract is on record — Phase 14 is ready for fresh independent verification and review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built sdk/dist (shipped surface) → live oto-sdk symlink | All source fixes are inert until the rebuild lands; the verifier reproduces against dist |
| Test-gate definition → phase completion decision | An impossible, dishonest, or flake-blind gate either blocks forever, ships regressions, or burns the convergence budget on noise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-19-01 | Elevation of Privilege (stale shipped build bypasses all fixes) | sdk/dist | mitigate | Single rebuild + dist-level grep criteria + real-process reproductions for all three gaps with grep-gateable verdict lines |
| T-14-19-02 | Repudiation (claiming completion on a red or unowned-debt suite) | terminal gate | mitigate | Four-part gate with two-run-union baseline rule; DELTA artifact is machine-checkable (six GAP verdicts + NO NEW FAILURES line) |
| T-14-19-03 | Denial of Service (endless gap-plan loops, incl. cycles burned on flake noise) | convergence process | mitigate | Bounded convergence: ≤2 further cycles, mandatory STOP on non-decreasing blocker count; mandatory isolated-re-run adjudication so only PERSISTENT failures consume cycles |
| T-14-19-04 | Tampering (self-certified completion or hand-edited verdicts) | verification/review independence | mitigate | Contract items 1-2 require FRESH oto-verifier and code-review runs outside this plan; verdict lines must trace to recorded command evidence |
</threat_model>

<verification>
- Wave gate honored: 14-13..14-18 SUMMARYs all report success before Task 1 ran
- All six reproductions inverted against CJS + dist with six `GAP*: PASS` verdict lines grep-gated in the DELTA; four-part gate green; adjudicated DELTA verdict `NO NEW FAILURES: PASS`
- Commit evidence is `git diff --name-only <sha>..<sha>` / `git log --stat` — never `git status --porcelain`
</verification>

<success_criteria>
- Phase goal restored end-to-end on the shipped surfaces: the three 14-VERIFICATION blockers are irreproducible (machine-checked), every review finding is dispositioned, and completion is defined by the bounded convergence contract with flake-adjudicated regression accounting — not by another open-ended gap loop and not by cycles burned on inter-run flake drift
</success_criteria>

<output>
After completion, create `.oto/phases/14-key-storage-reconciliation/14-19-SUMMARY.md`
</output>
