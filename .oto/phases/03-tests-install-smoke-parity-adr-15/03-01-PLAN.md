---
phase: 3
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - decisions/file-inventory.json
  - tests/ingest-docs.test.cjs
  - tests/eval-review.test.cjs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
tags: [tests, node-test, rebrand, regression-guard]

must_haves:
  truths:
    - "`tests/ingest-docs.test.cjs` exists and is the rebrand-ported upstream file (no `gsd-` tokens, no `.planning/` paths)."
    - "Classifier agent assertion in `tests/ingest-docs.test.cjs` does NOT require `Write` in the tools list (D-04 read-only-agent reshape locked)."
    - "Upstream `import command adopts shared conflict-engine` describe block is NOT present in the port (out-of-scope for v0.3.0)."
    - "`tests/eval-review.test.cjs` exists and locks workflow-shape + agent-shape + EVAL-REVIEW.md vocabulary for `/oto-eval-review`."
    - "`tests/eval-review.test.cjs` asserts `oto-eval-auditor.md` tools list does NOT contain `Write` (read-only sandbox per D-04)."
    - "`tests/eval-review.test.cjs` locks the SDK-DEFER-01 fallback pattern via two literal regex assertions against the workflow body — fuzziness/discretionary relaxation is forbidden."
    - "`npm test` exits 0 with new total ≥ 562 passing (561 v0.2.0 baseline + ≥ ~34 new tests), 1 expected skip, 0 failures."
    - "`decisions/file-inventory.json` row for `tests/ingest-docs.test.cjs` has `verdict: keep` and `rebrand_required: true` so the engine produces output for this file."
  artifacts:
    - path: "decisions/file-inventory.json"
      provides: "Inventory verdict flip for tests/ingest-docs.test.cjs"
      contains: "\"path\": \"tests/ingest-docs.test.cjs\""
    - path: "tests/ingest-docs.test.cjs"
      provides: "TEST-01 ported regression suite"
      min_lines: 200
    - path: "tests/eval-review.test.cjs"
      provides: "TEST-02 fresh-authored regression suite"
      min_lines: 80
  key_links:
    - from: "tests/ingest-docs.test.cjs"
      to: "oto/agents/oto-doc-classifier.md"
      via: "fs.readFileSync + regex assertions"
      pattern: "oto-doc-classifier"
    - from: "tests/ingest-docs.test.cjs"
      to: "oto/workflows/ingest-docs.md"
      via: "fs.readFileSync + content assertions"
      pattern: "oto/workflows/ingest-docs"
    - from: "tests/eval-review.test.cjs"
      to: "oto/agents/oto-eval-auditor.md"
      via: "fs.readFileSync + tools-list absence-of-Write assertion"
      pattern: "oto-eval-auditor"
    - from: "tests/eval-review.test.cjs"
      to: "oto/workflows/eval-review.md"
      via: "fs.readFileSync + content assertions"
      pattern: "EVAL-REVIEW.md"
---

<objective>
Land TEST-01, TEST-02, and TEST-03 by porting the upstream `tests/ingest-docs.test.cjs` through the rebrand engine (with two surgical hand-fixups), authoring a fresh `tests/eval-review.test.cjs` against the already-shipped eval-review surface, and keeping `npm test` green at the new baseline.

Purpose: Lock the v0.3.0 `/oto-ingest-docs` and `/oto-eval-review` contracts with regression tests matching the v0.1.0/v0.2.0 coverage bar, and prove `EXPECTED_AGENTS` staleness (Plan 02's territory) is the only install-smoke gap.
Output: Two new `tests/*.test.cjs` files plus one inventory row flip.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-VALIDATION.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-01-SUMMARY.md
@.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-03-SUMMARY.md
@CLAUDE.md
@foundation-frameworks/get-shit-done-main/tests/ingest-docs.test.cjs
@oto/workflows/ingest-docs.md
@oto/workflows/eval-review.md
@oto/commands/oto/ingest-docs.md
@oto/commands/oto/eval-review.md
@oto/agents/oto-doc-classifier.md
@oto/agents/oto-doc-synthesizer.md
@oto/agents/oto-eval-auditor.md
@scripts/rebrand.cjs
@decisions/file-inventory.json
@tests/workflow-ingest-docs.test.cjs
@tests/workflow-eval-review.test.cjs

<interfaces>
<!-- Required vocabulary the executor must preserve verbatim in eval-review.test.cjs assertions -->

eval-review workflow content vocabulary (from oto/workflows/eval-review.md — already shipped):
- Scoring tokens: COVERED, PARTIAL, MISSING (must all three appear in workflow body)
- Verdict tokens: PRODUCTION READY, NEEDS WORK, SIGNIFICANT GAPS, NOT IMPLEMENTED
- Output target: EVAL-REVIEW.md (under .oto/phases/<phase>/)
- SDK fallback pattern (mandatory per SDK-DEFER-01): `oto-sdk query … 2>/dev/null || …`
  - Verified literal lines in `oto/workflows/eval-review.md`:
    - Line 16: `oto-sdk query init.phase-op "${PHASE_ARG}" 2>/dev/null || echo ""`
    - Line 23: `oto-sdk query resolve-model oto-eval-auditor 2>/dev/null | jq -r '.model' 2>/dev/null || true`
- Agent dispatch: oto-eval-auditor (subagent_type)
- Persistence model: orchestrator writes auditor return value (Phase 1 D-04 read-only-agent reshape)

ingest-docs port hand-fixups (lines/locations may differ slightly after rebrand):
- Stale assertion to fix: classifier "has Read and Write tools" — change regex from `/^tools:\s*.*Read.*Write.*/m` to `/^tools:\s*.*Read.*Grep.*Glob/m` AND add a negative assertion that Write is NOT in the classifier tools line. Test name should become: 'has Read, Grep, Glob tools (read-only per Phase 1 D-04)'.
- Block to drop: `describe('import command adopts shared conflict-engine', …)` (lines 285-307 of upstream — `/oto-import` is not in v0.3.0 scope).
- Synthesizer assertion (`Read.*Write.*Bash`) is correct as-ported — synthesizer KEEPS Write per D-04 (workspace-write sandbox).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip inventory row + run rebrand engine + hand-fixup TEST-01 port</name>
  <files>decisions/file-inventory.json, tests/ingest-docs.test.cjs</files>
  <read_first>
    - decisions/file-inventory.json (lines 8136-8144 — current row for `tests/ingest-docs.test.cjs` is `verdict: drop, rebrand_required: false`)
    - foundation-frameworks/get-shit-done-main/tests/ingest-docs.test.cjs (upstream source, 307 LOC — the file being ported)
    - scripts/rebrand.cjs (the engine that performs token substitution)
    - oto/agents/oto-doc-classifier.md (tools list — confirm it is `Read, Grep, Glob` only, no Write, per D-04)
    - oto/agents/oto-doc-synthesizer.md (tools list — confirm Read, Write, Grep, Glob, Bash)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-01-SUMMARY.md (Phase 2 precedent for rebrand-engine usage + staging dir pattern)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md sections "Pattern 1" and "Common Pitfalls" #2, #3, #9
  </read_first>
  <action>
    Step A — Flip the inventory row. In `decisions/file-inventory.json`, locate the entry whose `"path": "tests/ingest-docs.test.cjs"` (currently at line 8137). Update it to:
    ```json
    {
      "path": "tests/ingest-docs.test.cjs",
      "upstream": "gsd",
      "verdict": "keep",
      "target_path": "tests/ingest-docs.test.cjs",
      "reason": "v0.3.0 Phase 3 TEST-01 — port upstream ingest-docs regression suite (rebrand engine + D-04 hand-fixups)",
      "rebrand_required": true,
      "phase_owner": 3,
      "category": "test"
    }
    ```
    Preserve surrounding JSON shape (commas, indentation). Do NOT touch other entries.

    Step B — Run the rebrand engine into a staging dir, then copy the ported file into place:
    ```bash
    node scripts/rebrand.cjs --apply \
      --target foundation-frameworks/get-shit-done-main \
      --owner OTOJulian \
      --out .oto-rebrand-stage
    cp .oto-rebrand-stage/tests/ingest-docs.test.cjs tests/ingest-docs.test.cjs
    rm -rf .oto-rebrand-stage
    ```

    Step C — Hand-fixup #1 (stale classifier `Write` assertion per D-04). Inside the ported `tests/ingest-docs.test.cjs`, find the describe block for the classifier agent (approximately lines 180-200 — search for `oto-doc-classifier` describe). Locate the test `test('has Read and Write tools', () => { assert.match(content, /^tools:\s*.*Read.*Write.*/m); });` and replace it with:
    ```javascript
    test('has Read, Grep, Glob tools (read-only per Phase 1 D-04)', () => {
      assert.match(content, /^tools:\s*.*Read.*Grep.*Glob/m);
      const toolsLine = content.match(/^tools:.*$/m)?.[0] || '';
      assert.ok(!/\bWrite\b/.test(toolsLine), 'oto-doc-classifier must not declare Write (read-only sandbox per D-04)');
    });
    ```

    Step D — Hand-fixup #2 (drop out-of-scope import block). Find `describe('import command adopts shared conflict-engine'` (the block exists in the upstream at lines 285-307; after rebrand the line numbers will shift but the describe-block name is unique). Delete the entire describe block including its body and trailing closing `});` — this block tests `/oto-import` which is NOT in v0.3.0 scope. Also remove any preceding `const cmdContent`/`const wfContent` constants that exist solely to serve this block.

    Step E — Sanity sweep. Run `grep -n "gsd-\|\.planning/\|get-shit-done/" tests/ingest-docs.test.cjs` — output MUST be empty. If any hit remains, fix the token (rebrand engine should have handled all, but spot-check).

    Step F — Verify locally: `node --test tests/ingest-docs.test.cjs` exits 0.
  </action>
  <verify>
    <automated>node --test tests/ingest-docs.test.cjs &amp;&amp; grep -c 'oto-doc-classifier' tests/ingest-docs.test.cjs &amp;&amp; ! grep -q "import command adopts shared conflict-engine" tests/ingest-docs.test.cjs &amp;&amp; ! grep -qE 'gsd-|\.planning/|get-shit-done/' tests/ingest-docs.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/ingest-docs.test.cjs` exists.
    - `grep -c "oto-doc-classifier" tests/ingest-docs.test.cjs` returns >= 1.
    - `grep -q "has Read, Grep, Glob tools (read-only per Phase 1 D-04)" tests/ingest-docs.test.cjs` exits 0.
    - `grep -q "import command adopts shared conflict-engine" tests/ingest-docs.test.cjs` exits 1 (block deleted).
    - `grep -qE 'gsd-|\.planning/|get-shit-done/' tests/ingest-docs.test.cjs` exits 1 (no token leakage).
    - `node --test tests/ingest-docs.test.cjs` exits 0.
    - `grep -A1 '"path": "tests/ingest-docs.test.cjs"' decisions/file-inventory.json` shows `"verdict": "keep"` immediately below the path line, and `grep -A6 '"path": "tests/ingest-docs.test.cjs"' decisions/file-inventory.json` shows `"rebrand_required": true` and `"phase_owner": 3`.
    - `node -e "JSON.parse(require('fs').readFileSync('decisions/file-inventory.json','utf8'))"` exits 0 (JSON remains valid).
  </acceptance_criteria>
  <done>
    `tests/ingest-docs.test.cjs` ports cleanly, both hand-fixups applied, the inventory row reflects keep+rebrand_required, no `gsd-` / `.planning/` / `get-shit-done/` tokens leak through, and the focused test run is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/eval-review.test.cjs (TEST-02)</name>
  <files>tests/eval-review.test.cjs</files>
  <behavior>
    - Test: file structure — four required eval-review files exist on disk.
    - Test: command frontmatter — `oto:eval-review` name, phase argument-hint, allowed-tools entry, @-references to workflow + ai-evals reference.
    - Test: workflow content — locks SDK-DEFER-01 fallback via TWO literal regexes (init.phase-op + resolve-model oto-eval-auditor); locks State A/B/C markers; locks orchestrator-persistence wording near oto-eval-auditor mention; locks full scoring + verdict vocabulary; rejects deferral markers and `gsd-eval-auditor` token leakage.
    - Test: auditor agent shape — tools list declares Read, does NOT declare Write (D-04 negative assertion); scoring vocabulary echoed in agent prompt.
  </behavior>
  <read_first>
    - oto/workflows/eval-review.md (already shipped — source of truth for content assertions: scoring vocab, verdict tokens, SDK fallback pattern, orchestrator-persistence wording, EVAL-REVIEW.md target. Lines 16 and 23 are the SDK-fallback anchors and have been verified by the planner; the test regexes below MUST match those exact lines verbatim.)
    - oto/commands/oto/eval-review.md (already shipped — source of truth for command frontmatter: name, argument-hint, allowed-tools, @-references)
    - oto/agents/oto-eval-auditor.md (already shipped — tools list must be asserted as containing Read but NOT Write per D-04)
    - oto/references/ai-evals.md (already shipped — referenced by command file)
    - tests/workflow-eval-review.test.cjs (Phase 2 skeleton — pattern to mirror for content assertions)
    - tests/workflow-ingest-docs.test.cjs (Phase 2 sibling — assertion style precedent)
    - foundation-frameworks/get-shit-done-main/tests/ingest-docs.test.cjs (style precedent for describe/test breakdown)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 2" (sketch with ≥10 assertions to land)
  </read_first>
  <action>
    Create `tests/eval-review.test.cjs` (new file). Use `node:test` + `node:assert/strict` + `node:fs` + `node:path` (project standard per CLAUDE.md — NO Vitest, NO Jest). The file must define exactly these describe blocks with at least the listed test cases (executor may add more, but these are mandatory):

    1. `describe('eval-review file structure (TEST-02)')` — test that each of these files exists via `fs.existsSync`:
       - `oto/commands/oto/eval-review.md`
       - `oto/workflows/eval-review.md`
       - `oto/agents/oto-eval-auditor.md`
       - `oto/references/ai-evals.md`

    2. `describe('eval-review command frontmatter')` — read `oto/commands/oto/eval-review.md` and assert:
       - `assert.match(content, /^name:\s*oto:eval-review$/m)`
       - `argument-hint` line exists and its value string contains `phase` (regex: `/^argument-hint:\s*"(.+)"$/m` then check captured group includes `phase`)
       - `content.includes('- Task')` is true (allowed-tools entry)
       - `content.includes('@~/.claude/oto/workflows/eval-review.md')` is true
       - `content.includes('@~/.claude/oto/references/ai-evals.md')` is true

    3. `describe('eval-review workflow content')` — read `oto/workflows/eval-review.md` and assert:
       - **LOCKED SDK-DEFER-01 fallback assertions (B3 — these regexes are NON-NEGOTIABLE; do not relax, do not "adjust to be less brittle", do not match the workflow against fuzzier patterns).** Include exactly these two assertions verbatim:
         ```javascript
         assert.match(content, /oto-sdk query init\.phase-op[^\n]*2>\/dev\/null \|\| /);
         assert.match(content, /oto-sdk query resolve-model oto-eval-auditor[^\n]*2>\/dev\/null/);
         ```
         These match the verified lines at `oto/workflows/eval-review.md:16` and `:23`. If a future workflow refactor changes those lines, the test SHOULD fail loudly — that is the lock-in behavior we want.
       - State A / State B / State C detection markers all present: `assert.ok(content.includes('State A')); assert.ok(content.includes('State B')); assert.ok(content.includes('State C'));`
       - `assert.ok(content.includes('oto-eval-auditor'))` (agent dispatch reference)
       - Orchestrator-persistence wording (Phase 1 D-04): within a 25-line window around the `oto-eval-auditor` mention, at least one line containing `orchestrator` (case-insensitive). Implement as the sliding-window check from research Pattern 2.
       - `assert.ok(content.includes('EVAL-REVIEW.md'))` (output target)
       - Scoring vocabulary: `assert.ok(content.includes('COVERED')); assert.ok(content.includes('PARTIAL')); assert.ok(content.includes('MISSING'));`
       - Verdict tokens: `assert.ok(content.includes('PRODUCTION READY')); assert.ok(content.includes('NEEDS WORK')); assert.ok(content.includes('SIGNIFICANT GAPS')); assert.ok(content.includes('NOT IMPLEMENTED'));`
       - No deferral markers: `assert.ok(!/intentionally non-executable/i.test(content)); assert.ok(!/\bDEFERRED\b/.test(content));`
       - No gsd-token leakage: `assert.ok(!content.includes('gsd-eval-auditor'));`

    4. `describe('oto-eval-auditor agent (D-04 read-only locked)')` — read `oto/agents/oto-eval-auditor.md` and assert:
       - `assert.match(content, /^tools:\s*.*Read/m)` (Read is declared)
       - `const toolsLine = content.match(/^tools:.*$/m)?.[0] || '';`
       - `assert.ok(!/\bWrite\b/.test(toolsLine), 'oto-eval-auditor must not declare Write (read-only sandbox per Phase 1 D-04)')`
       - Scoring vocabulary echoed in agent prompt: `assert.ok(content.includes('COVERED')); assert.ok(content.includes('PARTIAL')); assert.ok(content.includes('MISSING'));`

    Path constants must be derived via `path.resolve(__dirname, '..')` (consistent with sibling tests). NO calls to `oto-sdk` from inside the test (SDK-DEFER-01 — assertions read shipped artifacts only).

    After authoring, run: `node --test tests/eval-review.test.cjs`. All tests MUST pass green.

    **IF an SDK-DEFER-01 assertion fails:** the workflow body has drifted from the verified Phase 2 shape. DO NOT relax the assertion — instead, STOP and surface the drift. The workflow at `oto/workflows/eval-review.md` is locked by Phase 1/Phase 2 verification and any change to lines 16 or 23 is a regression that requires its own ADR or a Phase 3 amendment. Likewise NEVER modify `oto/workflows/eval-review.md` or `oto/agents/oto-eval-auditor.md` to make the test pass.
  </action>
  <verify>
    <automated>node --test tests/eval-review.test.cjs &amp;&amp; grep -c 'oto-eval-auditor' tests/eval-review.test.cjs &amp;&amp; grep -q 'must not declare Write' tests/eval-review.test.cjs &amp;&amp; [ "$(grep -cE 'oto-sdk query .* 2>/dev/null' tests/eval-review.test.cjs)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/eval-review.test.cjs` exists with `require('node:test')` and `require('node:assert/strict')`.
    - `node --test tests/eval-review.test.cjs` exits 0 with all tests passing.
    - `grep -c "describe(" tests/eval-review.test.cjs` returns >= 4 (four mandated describe blocks).
    - `grep -q "must not declare Write" tests/eval-review.test.cjs` exits 0 (D-04 negative assertion present).
    - `grep -q "COVERED" tests/eval-review.test.cjs` AND `grep -q "PARTIAL" tests/eval-review.test.cjs` AND `grep -q "MISSING" tests/eval-review.test.cjs` all exit 0.
    - `grep -q "PRODUCTION READY" tests/eval-review.test.cjs` AND `grep -q "NEEDS WORK" tests/eval-review.test.cjs` AND `grep -q "SIGNIFICANT GAPS" tests/eval-review.test.cjs` AND `grep -q "NOT IMPLEMENTED" tests/eval-review.test.cjs` all exit 0.
    - `grep -q "EVAL-REVIEW.md" tests/eval-review.test.cjs` exits 0.
    - `grep -qE 'vitest|jest' tests/eval-review.test.cjs` exits 1 (no forbidden frameworks).
    - **B3 lock — `grep -cE 'oto-sdk query .* 2>/dev/null' tests/eval-review.test.cjs` returns >= 2** (proves both SDK-DEFER-01 fallback regexes are present verbatim).
    - `grep -q 'init\.phase-op' tests/eval-review.test.cjs` exits 0 (first fallback regex anchor).
    - `grep -q 'resolve-model oto-eval-auditor' tests/eval-review.test.cjs` exits 0 (second fallback regex anchor).
  </acceptance_criteria>
  <done>
    `tests/eval-review.test.cjs` ships with ≥4 describe blocks covering file structure, command frontmatter, workflow content (including TWO literal SDK-DEFER-01 fallback regex assertions matching workflow lines 16 + 23, D-04 orchestrator-persistence wording, full scoring + verdict vocabulary, no-deferral guard), and auditor agent shape with explicit no-Write D-04 lock. Test passes green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full npm test and confirm TEST-03 regression baseline</name>
  <files>(no file modifications — regression check)</files>
  <read_first>
    - .planning/STATE.md (baseline: 562 tests, 561 pass, 1 expected skip, 0 fail per 2026-05-18 Phase 2 verification)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-VALIDATION.md (per-task verification map — Task 3-01-03 row)
  </read_first>
  <action>
    Run `npm test` from the repo root. Capture the final summary line (e.g., `tests 596`, `pass 595`, `fail 0`, `skipped 1`).

    Expected new baseline after Tasks 1+2 in this plan:
    - Pre-plan baseline: 562 tests, 561 pass, 1 skip, 0 fail.
    - `tests/ingest-docs.test.cjs` adds approximately 24 tests (12 describe blocks × ~2 tests each, minus the dropped `import command` block which was ~2 tests).
    - `tests/eval-review.test.cjs` adds approximately 13 tests (4 describe blocks).
    - Expected new total: approximately 595-600 tests, 594-599 pass, 1 skip, 0 fail.

    Required outcomes:
    1. `pass` count INCREASES from 561 by at least 30 (proving both new test files run and pass).
    2. `fail` count is exactly 0 (no regressions).
    3. `skipped` count remains exactly 1 (the same pre-existing expected skip — verify by diffing skip reasons if needed).

    If `fail > 0`, identify which tests fail. If a failure is in either new file, fix the assertion (return to Task 1 or 2). If a failure is in a pre-existing test that did NOT fail at the v0.2.0 baseline, the change has caused a regression elsewhere — STOP and diagnose; do not paper over.

    Record the exact final numbers (tests, pass, fail, skipped) in the plan SUMMARY so 03-VALIDATION.md can be updated with the new locked baseline.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -20 | grep -E '^# (pass|fail|skipped|tests) [0-9]+' | tee /tmp/oto-phase3-plan01-npmtest.log &amp;&amp; grep -q '^# fail 0$' /tmp/oto-phase3-plan01-npmtest.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - Final summary line `# fail 0` appears in stdout.
    - Final summary `# pass <N>` shows N >= 591 (561 baseline + ≥30 new).
    - Final summary `# skipped 1` (unchanged).
    - `grep -q '^# tests [0-9]' /tmp/oto-phase3-plan01-npmtest.log` exits 0 (summary captured).
  </acceptance_criteria>
  <done>
    Full `npm test` is green at the new baseline (≥591 pass, 0 fail, 1 skip), with the exact final numbers recorded in the plan SUMMARY for downstream phase-VALIDATION updates.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → repo files | Tests read shipped agent/workflow/command markdown via `fs.readFileSync`. Read-only; no test mutates production files. |
| Rebrand engine → tests/ | Engine writes to staging dir; explicit `cp` step lands the file under `tests/`. Engine is repo-local, no network. |
| `decisions/file-inventory.json` → engine | Engine reads inventory verdicts to decide which files to emit. A wrong verdict (drop instead of keep) silently skips the file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | `decisions/file-inventory.json` row edit | mitigate | Task 1 acceptance asserts JSON.parse round-trips AND that the specific row has verdict: keep + rebrand_required: true via grep. |
| T-3-02 | Information disclosure | `tests/eval-review.test.cjs` SDK fallback assertion | mitigate | Two literal regex assertions lock SDK-DEFER-01 fallback at workflow lines 16 + 23; if a future regression removes the `2>/dev/null` clause OR changes the subcommand name, the test fails loudly. Discretionary relaxation explicitly forbidden in Task 2 action. |
| T-3-03 | Repudiation / silent test pass | Stale upstream `import command` block | mitigate | Task 1 acceptance greps for absence of the dropped describe block, so a careless port that leaves it in fails the gate. |
| T-3-04 | Spoofing | False-pass classifier `Write` assertion | mitigate | Task 1 reshapes assertion to require absence of Write (negative assertion); the ONLY way to pass is to actually keep Write out of the classifier tools line. |

All threats are medium severity; phase scope is test + inventory edits, no new runtime code paths.
</threat_model>

<verification>
- All three tasks complete with green automated verify commands.
- `npm test` reports `fail 0` at the new baseline.
- No `gsd-`, `.planning/`, or `get-shit-done/` token leakage in either new test file.
- D-04 read-only locks are encoded as negative assertions (`!/\bWrite\b/.test(toolsLine)`) in both classifier and auditor tests.
- B3 lock: both SDK-DEFER-01 fallback regexes present verbatim in `tests/eval-review.test.cjs` (count >= 2 via grep gate).
</verification>

<success_criteria>
- `tests/ingest-docs.test.cjs` and `tests/eval-review.test.cjs` exist and pass under `node:test`.
- `decisions/file-inventory.json` row for the test file is flipped to keep+rebrand_required.
- `npm test` baseline locks at ≥591 pass, 0 fail, 1 skip with the exact final numbers recorded in the SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-install-smoke-parity-adr-15/03-01-SUMMARY.md` recording:
- Final `npm test` numbers (tests, pass, fail, skipped).
- Exact lines of the two hand-fixups in `tests/ingest-docs.test.cjs` (line numbers + brief diff).
- The describe-block count in `tests/eval-review.test.cjs` and the count of tests added.
- Confirmation the inventory row was flipped (verdict, rebrand_required, phase_owner).
</output>
</content>
</invoke>