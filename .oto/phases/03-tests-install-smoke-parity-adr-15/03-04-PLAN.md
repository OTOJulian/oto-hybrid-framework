---
phase: 3
plan: 4
type: execute
wave: 2
depends_on: ['03-01', '03-02', '03-03']
files_modified:
  - decisions/ADR-15-restore-doc-and-eval-agents.md
autonomous: true
requirements: [ADR-01]
tags: [adr, decision-record, governance]

must_haves:
  truths:
    - "`decisions/ADR-15-restore-doc-and-eval-agents.md` exists and follows the ADR-09 format (Status, Date, Implements, ## Context, ## Decision, ## Rationale, ## Consequences)."
    - "ADR-15 names exactly the three restored agents (`oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor`) in the Decision section, with their respective Codex sandbox modes."
    - "ADR-15 explicitly enumerates the seven still-dropped ADR-07 agents (`oto-ai-researcher`, `oto-eval-planner`, `oto-framework-selector`, `oto-pattern-mapper`, `oto-intel-updater`, `oto-user-profiler`, `oto-debug-session-manager`) and cites AGNT-DEFER-01 as the binding deferral."
    - "ADR-15 paraphrases (not literally quotes) ADR-07's reactivation framing — ADR-07 contains no literal 'reactivation criterion' sentence."
    - "ADR-15's `Implements:` line is exactly `Implements: D-24` (B1 — a new global decision number registering the partial reversal of ADR-07). ADR-15's `## Context` body cites Phase 1 D-04 as the locked sandbox decisions being formalized."
    - "ADR-15 body includes a `## Decision D-24` (or equivalent) section that registers D-24 in prose so future readers can see what global decision was minted by this ADR."
    - "`tests/phase-01-adr-structure.test.cjs` passes against the new ADR-15 (auto-discovers all `decisions/ADR-*.md` files and enforces the `^Implements: D-\\d{2}` regex)."
  artifacts:
    - path: "decisions/ADR-15-restore-doc-and-eval-agents.md"
      provides: "Formal record of the partial ADR-07 reversal for the three restored agents; mints global decision D-24"
      contains: "AGNT-DEFER-01"
      min_lines: 50
  key_links:
    - from: "decisions/ADR-15-restore-doc-and-eval-agents.md"
      to: "ADR-07 (preceding decision being partially reversed)"
      via: "narrative reference in ## Context"
      pattern: "ADR-07"
    - from: "decisions/ADR-15-restore-doc-and-eval-agents.md"
      to: "REQUIREMENTS.md v2 AGNT-DEFER-01"
      via: "explicit enumeration of still-dropped agents"
      pattern: "AGNT-DEFER-01"
    - from: "decisions/ADR-15-restore-doc-and-eval-agents.md"
      to: "Phase 1 D-04 (Codex sandbox locks)"
      via: "Context paragraph"
      pattern: "D-04"
---

<objective>
Write `decisions/ADR-15-restore-doc-and-eval-agents.md` to formally record the partial reversal of ADR-07 for the three agents restored in v0.3.0, against the criterion ADR-07 itself named (paraphrased — ADR-07 contains no literal "reactivation criterion" sentence). Explicitly affirm that AGNT-DEFER-01 still binds for the remaining seven dropped agents. Mint a new global decision number `D-24` to register the partial reversal so it is traceable in the same `Implements: D-NN` registry as every other ADR.

Purpose: Close the governance loop on v0.3.0. The ADR is the human-readable contract that explains why three agents came back and why the others did not. It also locks the Codex sandbox decisions per agent so future readers don't have to reverse-engineer them from `bin/lib/runtime-codex.cjs`.

Output: One new ADR file. No code changes.

**Wave 2 placement:** This plan runs AFTER Plans 01, 02, and 03 land because ADR-15 documents the verified outcome (tests green, install-smoke asserting, runtimes parity-locked). Writing the ADR before the work lands risks describing aspirational state instead of shipped state.

**B1 lock:** `Implements:` line MUST be `Implements: D-24` (matching the `/^Implements: D-\d{2}/m` regex enforced by `tests/phase-01-adr-structure.test.cjs:12`). D-24 is the next free global decision number — verified by grep against existing ADRs (D-23 is the current high-water mark in ADR-14). Free-form prose suffix on the same line (e.g., " — v0.3.0 Phase 3 ADR-01") is allowed ONLY if the regex still matches; verify in acceptance.

**B2 lock:** All references to ADR-07 use the correct filename `ADR-07-agent-trim.md` (NOT `ADR-07-agent-rationalization.md` — that filename does not exist and the typo would break docs cross-linking).
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-VALIDATION.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@.planning/phases/01-agent-ports-installer-wiring/01-VERIFICATION.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-01-SUMMARY.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-02-SUMMARY.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-03-SUMMARY.md
@decisions/ADR-07-agent-trim.md
@decisions/ADR-09-adr-format.md
@decisions/ADR-14-inventory-scope.md
@decisions/agent-audit.md
@tests/phase-01-adr-structure.test.cjs
@bin/lib/runtime-codex.cjs

<interfaces>
<!-- ADR-09 format (verified — enforced by tests/phase-01-adr-structure.test.cjs) -->

Required fields (one per line, near top — these EXACT regex patterns are enforced):
- `^# ADR-\d{2}: ` (title line)
- `^Status: (Accepted|Deferred|Superseded by ADR-\d{2})$`
- `^Date: \d{4}-\d{2}-\d{2}$`
- `^Implements: D-\d{2}` — **B1 lock: must be `Implements: D-24`** (D-24 is the next free decision number; existing ADRs use D-01..D-23 per grep of `^Implements: D-` across all `decisions/ADR-*.md`).

Required section headers (Markdown H2):
- `## Context`
- `## Decision`
- `## Rationale`
- `## Consequences`

Title line at top: `# ADR-15: Restore doc-intake and eval-review agents`

Phase 1 D-04 sandbox locks (authoritative, from `bin/lib/runtime-codex.cjs:90-92`):
| Agent | Codex sandbox_mode | Tools | Persistence model |
|-------|---------------------|-------|-------------------|
| oto-doc-classifier | read-only | Read, Grep, Glob | Workflow orchestrator persists return value |
| oto-doc-synthesizer | workspace-write | Read, Write, Grep, Glob, Bash | Agent writes `.oto/INGEST-CONFLICTS.md` and `.oto/intel/SYNTHESIS.md` directly |
| oto-eval-auditor | read-only | Read, Bash, Grep, Glob | Workflow orchestrator persists return value to `.oto/phases/<phase>/EVAL-REVIEW.md` |

Seven still-dropped agents (per AGNT-DEFER-01, from REQUIREMENTS.md v2):
- oto-ai-researcher
- oto-eval-planner
- oto-framework-selector
- oto-pattern-mapper
- oto-intel-updater
- oto-user-profiler
- oto-debug-session-manager

ADR-07 framing (paraphrased — no literal "reactivation criterion" sentence exists in ADR-07):
- ADR-07 (filename: `decisions/ADR-07-agent-trim.md` — **B2 lock**, NOT `ADR-07-agent-rationalization.md` which is a typo that does not exist on disk)
- ADR-07 categorized the dropped agents as "DROP - AI/eval" and "DROP - redundant doc"
- ADR-07 implicitly left room for restoration via a "future milestone that restores document intake"
- ADR-15 affirms v0.3.0 IS that milestone — specifically for the two `[deferred]` commands (`/oto-ingest-docs`, `/oto-eval-review`), nothing more.

Test enforcement: `tests/phase-01-adr-structure.test.cjs` auto-discovers every `decisions/ADR-*.md` and validates the required fields/headers — so ADR-15 must conform precisely or the test fails red. **The `Implements:` regex requires `^Implements: D-\d{2}` (two digits). `D-24` matches; `D-04` would also match but is wrong because D-04 is already implemented by ADR-02; `v0.3.0 Phase 1 D-04` would NOT match because it does not start with `Implements: D-`.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author decisions/ADR-15-restore-doc-and-eval-agents.md (B1 D-24 lock + B2 ADR-07 filename fix)</name>
  <files>decisions/ADR-15-restore-doc-and-eval-agents.md</files>
  <read_first>
    - decisions/ADR-07-agent-trim.md (**B2 — correct filename**; FULL read — to confirm there is no literal "reactivation criterion" sentence, and to identify the exact agent-drop framing ADR-15 must reference)
    - decisions/ADR-09-adr-format.md (FULL — format template; this is the ADR-09 format ADR-15 mirrors)
    - decisions/ADR-14-inventory-scope.md (the immediately preceding ADR, for style continuity — and to confirm D-23 is the most recent `Implements:` number used)
    - tests/phase-01-adr-structure.test.cjs (FULL — read what fields/headers it requires so ADR-15 passes the auto-discovery test; lines 8-17 contain `REQUIRED_PATTERNS` with the `^Implements: D-\d{2}` regex)
    - **B1 verification step** — before writing, run:
      ```bash
      grep -h "^Implements: D-" decisions/ADR-*.md | sort -u
      ```
      Confirm the highest existing D-NN is D-23 (so D-24 is free). If a higher D-NN exists (e.g., another ADR landed in parallel), use the next free number AND update this plan's truths/acceptance accordingly. Record the chosen D-NN in the SUMMARY.
    - bin/lib/runtime-codex.cjs (lines 88-95 — confirm the three sandbox_mode values currently shipping)
    - .planning/REQUIREMENTS.md (the AGNT-DEFER-01 entry under v2+ Requirements — confirm the seven still-dropped agent names enumerated)
    - .planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md (Phase 1 D-04 locked decisions — confirm read-only-agent reshape and persistence model)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md (the verified Wave 1 outcomes that ADR-15 documents — read these AFTER Wave 1 lands so ADR-15 describes shipped state)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 5" (the full ADR-15 skeleton starting at line 393)
  </read_first>
  <action>
    Create `decisions/ADR-15-restore-doc-and-eval-agents.md` using the ADR-09 format. **B1 + B2 + content-lock requirements (NON-NEGOTIABLE):**

    Required structure (executor MUST customize prose but preserve the section shape, the `Implements: D-24` literal, and the ADR-07 filename references):

    ```markdown
    # ADR-15: Restore doc-intake and eval-review agents

    Status: Accepted
    Date: <current date in YYYY-MM-DD>
    Implements: D-24

    ## Context

    ADR-07 (see `decisions/ADR-07-agent-trim.md`) dropped ten GSD agents during the v0.1.0 rationalization. Three of those (`gsd-doc-classifier`, `gsd-doc-synthesizer`, `gsd-eval-auditor`) powered two `/oto-help` commands that v0.1.0 had to mark `[deferred]` to ship on time: `/oto-ingest-docs` and `/oto-eval-review`. Those two dead commands violated v0.3.0's north-star goal — "no dead commands in `/oto-help`."

    ADR-07 did not contain a literal "reactivation criterion" sentence; instead it categorized agents by drop-rationale and left the door open for "a future milestone that restores document intake." v0.3.0 is that milestone. The minimal reversal that retires both stubs requires exactly the three named agents — restoring more would re-inflate the agent maintenance surface against the personal-use cost ceiling defined in PROJECT.md.

    Phase 1 of v0.3.0 (see Phase 1 D-04) landed the three agents with a deliberate read-only-agent reshape: classifier and auditor declare a Codex `read-only` sandbox and tools that exclude `Write`; the synthesizer keeps `workspace-write` because it must produce `.oto/INGEST-CONFLICTS.md` and synthesized context during its own run. Phase 2 de-deferred both commands and shipped executable workflow bodies. Phase 3 (this ADR) closes the loop with test coverage, per-runtime parity smoke, and this decision record.

    ## Decision D-24

    Partially reverse `decisions/ADR-07-agent-trim.md` by restoring exactly three of the ten dropped agents. This ADR mints global decision **D-24** ("Partial reversal of ADR-07 — restore the three doc-intake / eval-review agents") so the reversal is traceable in the same `Implements: D-NN` registry as every other locked decision:

    | Agent | Codex sandbox | Tools | Persistence model |
    |-------|---------------|-------|-------------------|
    | `oto-doc-classifier` | `read-only` | `Read, Grep, Glob` | Workflow orchestrator persists the classifier's returned classification record to `.oto/intel/classifications/` |
    | `oto-doc-synthesizer` | `workspace-write` | `Read, Write, Grep, Glob, Bash` | Agent writes `.oto/INGEST-CONFLICTS.md` and `.oto/intel/SYNTHESIS.md` directly during its run |
    | `oto-eval-auditor` | `read-only` | `Read, Bash, Grep, Glob` | Workflow orchestrator persists the auditor's returned markdown to `.oto/phases/<phase>/EVAL-REVIEW.md` |

    The remaining seven ADR-07-dropped agents stay dropped per AGNT-DEFER-01 (REQUIREMENTS.md v2+):

    - `oto-ai-researcher`
    - `oto-eval-planner`
    - `oto-framework-selector`
    - `oto-pattern-mapper`
    - `oto-intel-updater`
    - `oto-user-profiler`
    - `oto-debug-session-manager`

    Restoring any of these seven requires a future ADR superseding the AGNT-DEFER-01 deferral and minting its own decision number.

    ## Rationale

    The retained-agent set grows from 23 to 26 — well under GSD's original 33 — and both `[deferred]` commands become live. The classifier and auditor are deliberately `read-only` to (a) honor INST-02's Codex sandbox declarations, (b) align with the established `oto-plan-checker` / `oto-integration-checker` read-only convention, and (c) keep the orchestrator-persistence model uniform with how other checkers integrate. The synthesizer keeps `workspace-write` because its single-pass output requires writing the conflict report mid-run — splitting it would force a two-agent dance for one logical operation.

    Minting D-24 (rather than naming this ADR's `Implements:` line as `D-04` or some Phase-1 backreference) keeps the decision registry monotonic and one-decision-per-ADR. D-04 is already implemented by ADR-02; reusing it would violate the registry invariant. The body of this ADR still cites Phase 1 D-04 in `## Context` for traceability — the Implements line is the registry pointer, the Context paragraph is the cross-reference.

    The seven still-dropped agents serve workflows that are not in v0.3.0 scope. Re-adding them now would re-inflate the agent maintenance surface (rebrand-engine coverage, per-runtime install adapters, sandbox declarations, test fixtures) for capabilities the user does not currently need. Future milestones can revisit individually.

    ## Consequences

    - The rebrand engine's coverage manifest carries three additional target paths (`oto/agents/oto-doc-classifier.md`, `oto/agents/oto-doc-synthesizer.md`, `oto/agents/oto-eval-auditor.md`) plus the shared `oto/references/doc-conflict-engine.md` reference. Verified by `tests/rebrand-coverage.test.cjs` (Phase 1).
    - `decisions/file-inventory.json` and `decisions/agent-audit.md` carry the three rows flipped from DROP to KEEP (Phase 1 outcome), and the inventory row for `tests/ingest-docs.test.cjs` is now `keep` + `rebrand_required: true` (Phase 3 Plan 01 outcome).
    - `bin/lib/runtime-codex.cjs` `agentSandboxes` carries 26 entries; the three new entries declare the sandbox modes recorded in this ADR.
    - `oto/bin/lib/model-profiles.cjs` `EXPECTED_AGENTS` carries 26 entries (updated in Phase 3 Plan 02 — was stale at 23 since Phase 1).
    - `tests/ingest-docs.test.cjs` (ported from GSD in Phase 3 Plan 01) and `tests/eval-review.test.cjs` (fresh-authored in Phase 3 Plan 01) lock the two restored commands at the regression-suite surface.
    - `.github/workflows/install-smoke.yml` asserts the three new agent files install per runtime under both tarball and unpacked install paths (Phase 3 Plan 02).
    - `tests/phase-04-mr01-install-smoke.test.cjs`, `tests/phase-08-smoke-codex.integration.test.cjs`, and `tests/phase-08-smoke-gemini.integration.test.cjs` cover per-runtime parity (Phase 3 Plans 02 + 03).
    - Decision D-24 is now consumed by this ADR; the next free global decision number is D-25.
    - Future restoration of any of the seven still-dropped agents requires a new ADR superseding AGNT-DEFER-01 and minting its own decision number. This ADR does not pre-authorize any such restoration.
    ```

    **Locks to preserve verbatim (executor cannot relax these):**
    - Title line exactly as shown.
    - Frontmatter lines `Status: Accepted`, `Date: <today>`, and **`Implements: D-24`** (B1 lock — must satisfy `/^Implements: D-\d{2}/m`).
    - All four section headers (`## Context`, `## Decision D-24`, `## Rationale`, `## Consequences`). The `## Decision D-24` header satisfies the `^## Decision$` regex enforced by the test because the regex uses `m` flag without `$` anchor — verify by reading the test file, and if the test's regex is strict `^## Decision$` (with `$`), use BOTH `## Decision` AND a `## Decision D-24` subsection (or rename Decision to plain `## Decision` and put `### D-24:` underneath).
    - All references to ADR-07 use the filename `ADR-07-agent-trim.md` (**B2 lock**) — do NOT use `ADR-07-agent-rationalization.md`.
    - Verbatim mention of all three restored agent names AND all seven still-dropped agent names.
    - Verbatim mention of `AGNT-DEFER-01`, `ADR-07`, `D-04`, and `D-24`.
    - The three Codex sandbox modes (classifier read-only, synthesizer workspace-write, auditor read-only).

    **Strict header check:** Re-read `tests/phase-01-adr-structure.test.cjs` lines 8-17. The `## Decision` pattern is `/^## Decision$/m` — meaning the line must be EXACTLY `## Decision` (no trailing text on that line). If the test's regex is strict, change the Decision header in the skeleton above to plain `## Decision` and embed D-24 in the body of that section as the first paragraph: `**Decision D-24:** Partially reverse…`. Adjust your draft to whichever form satisfies BOTH the test regex AND the in-body D-24 registration requirement.

    After writing, immediately run:
    ```bash
    grep -E '^Implements: D-24$' decisions/ADR-15-restore-doc-and-eval-agents.md
    node --test tests/phase-01-adr-structure.test.cjs
    ```
    The grep MUST return exactly one line. The test MUST exit 0 (auto-discovers ADR-15 and validates). If either fails, fix and retest.
  </action>
  <verify>
    <automated>test -f decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -qE '^Implements: D-24$' decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "ADR-07-agent-trim.md" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; ! grep -q "ADR-07-agent-rationalization" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "AGNT-DEFER-01" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "oto-doc-classifier" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "oto-doc-synthesizer" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "oto-eval-auditor" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "oto-pattern-mapper" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "oto-debug-session-manager" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "^## Context$" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -qE "^## Decision( D-24)?$" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "^## Rationale$" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "^## Consequences$" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "^Status:" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "^Date:" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; grep -q "D-24" decisions/ADR-15-restore-doc-and-eval-agents.md &amp;&amp; node --test tests/phase-01-adr-structure.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `decisions/ADR-15-restore-doc-and-eval-agents.md` exists.
    - **B1 LOCK:** `grep -E "^Implements: D-24" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 0 (Implements line starts with `Implements: D-24`). If a free-form suffix is appended on the same line, the line still matches the regex.
    - **B1 LOCK:** `node --test tests/phase-01-adr-structure.test.cjs` exits 0 (auto-discovery test passes; this is the load-bearing regex lock).
    - **B2 LOCK:** `grep -q "ADR-07-agent-trim.md" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 0 (correct filename used).
    - **B2 LOCK:** `grep -q "ADR-07-agent-rationalization" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 1 (the typo filename must NOT appear).
    - File has all three frontmatter lines: `grep -q "^Status:" …` AND `grep -q "^Date:" …` AND `grep -q "^Implements:" …`.
    - File has all four section headers: `## Context`, `## Decision` (with or without ` D-24` suffix per the test regex investigation), `## Rationale`, `## Consequences`.
    - All three restored agent names present: `oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor`.
    - All seven still-dropped agent names present: `oto-ai-researcher`, `oto-eval-planner`, `oto-framework-selector`, `oto-pattern-mapper`, `oto-intel-updater`, `oto-user-profiler`, `oto-debug-session-manager`. Grep each name individually.
    - Both critical tokens present: `ADR-07` AND `AGNT-DEFER-01`.
    - Both Codex sandbox modes present in the file: `grep -q "read-only" …` AND `grep -q "workspace-write" …`.
    - Phase 1 D-04 referenced in body: `grep -q "D-04" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 0.
    - D-24 registered in body: `grep -q "D-24" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 0 (already covered by the Implements grep, but explicit body check confirms D-24 is introduced as a decision, not just an Implements pointer).
    - File length is at least 50 lines: `wc -l decisions/ADR-15-restore-doc-and-eval-agents.md` reports >= 50.
    - Full suite check: `npm test` exits 0 with `# fail 0`.
  </acceptance_criteria>
  <done>
    ADR-15 ships in ADR-09 format, names all three restored agents with their sandbox modes, enumerates all seven still-dropped agents with AGNT-DEFER-01 citation, mints D-24 in the `Implements:` line (B1) and registers it in the body, references Phase 1 D-04 in Context, cites ADR-07 by correct filename `ADR-07-agent-trim.md` (B2), and passes the auto-discovery structure test. Full `npm test` stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR-15 prose → future reader interpretation | A documentation artifact; the only "trust" surface is whether the prose accurately reflects shipped state. |
| ADR-15 ↔ `tests/phase-01-adr-structure.test.cjs` | The test auto-discovers all ADR-*.md files; if ADR-15 omits a required field or fails the `^Implements: D-\d{2}` regex, the test fails. Single load-bearing assertion surface. |
| ADR-15 → decision registry (D-NN namespace) | Each ADR claims one D-NN. Reusing an already-claimed number would silently confuse traceability; D-24 was verified free by grep. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-13 | Spoofing | Misnaming a restored agent (e.g., typo `oto-doc-classifer`) | mitigate | Acceptance grep checks for each of the three exact agent names; a typo causes failure. |
| T-3-14 | Repudiation | Forgetting to enumerate all seven still-dropped agents | mitigate | Acceptance greps for ALL SEVEN names individually; AGNT-DEFER-01 affirmation is therefore machine-checkable. |
| T-3-15 | Information disclosure | Quoting a literal "reactivation criterion" sentence that doesn't exist in ADR-07 | mitigate | Task action explicitly forbids verbatim quotation of a nonexistent phrase; research confirmed ADR-07 contains no such literal text (see RESEARCH.md Assumption A4). |
| T-3-16 | Tampering | Wrong sandbox_mode recorded in ADR-15 (e.g., listing synthesizer as read-only) | mitigate | Acceptance requires both `read-only` AND `workspace-write` to appear in the file. Plan 03's Codex test asserts the actual sandbox_mode in shipped TOML — double-coverage. Drift between ADR text and shipping code would be caught by Plan 03 but not by Plan 04 acceptance alone; SUMMARY should cite the verified TOML values. |
| T-3-B1 | Repudiation / silent test failure | `Implements:` line drifts from `D-\d{2}` regex (e.g., free-form `v0.3.0 Phase 1 D-04`) | mitigate | B1 fix: literal `Implements: D-24` enforced via grep AND via the `phase-01-adr-structure` test run in verify; the test itself is the lock. |
| T-3-B2 | Spoofing | ADR-15 cites non-existent ADR-07 filename | mitigate | B2 fix: positive grep for `ADR-07-agent-trim.md` AND negative grep rejecting `ADR-07-agent-rationalization`. |

All threats are medium severity.
</threat_model>

<verification>
- `decisions/ADR-15-restore-doc-and-eval-agents.md` exists with all ADR-09 structural elements.
- `grep -E "^Implements: D-24" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 0 (B1).
- `grep -q "ADR-07-agent-trim.md" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 0 AND `grep -q "ADR-07-agent-rationalization" decisions/ADR-15-restore-doc-and-eval-agents.md` exits 1 (B2).
- `node --test tests/phase-01-adr-structure.test.cjs` exits 0.
- Full `npm test` stays green at the post-Wave-1 baseline.
- The ADR cites ADR-07, AGNT-DEFER-01, Phase 1 D-04, and mints D-24 explicitly.
</verification>

<success_criteria>
- ADR-01 requirement satisfied: ADR-15 file shipped in conformant format.
- Auto-discovery test (`tests/phase-01-adr-structure.test.cjs`) passes against the new file (B1 enforcement).
- ADR-07 filename used correctly throughout (B2 enforcement).
- All three restored agents AND all seven still-dropped agents enumerated by name.
- D-24 minted in `Implements:` line AND registered in body.
- Phase 3 closure is now complete: tests + install-smoke + parity + decision record all in place.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-install-smoke-parity-adr-15/03-04-SUMMARY.md` recording:
- The final `Date:` value used in ADR-15.
- The exact `Implements:` line value (must start with `Implements: D-24`).
- The result of `grep -h "^Implements: D-" decisions/ADR-*.md | sort -u` run before authoring (proves D-24 was the next free number at write time).
- Confirmation that the ADR-07 filename `ADR-07-agent-trim.md` is used (and the typo `ADR-07-agent-rationalization` does NOT appear).
- Confirmation that all 3 restored + 7 still-dropped agent names appear by name (10 grep results recorded).
- Final `npm test` numbers (tests, pass, fail, skipped) after ADR-15 ships.
- Any deviations from the recommended ADR skeleton.
</output>
</content>
</invoke>