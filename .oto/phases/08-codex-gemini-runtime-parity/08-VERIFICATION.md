---
phase: 08-codex-gemini-runtime-parity
verified: 2026-05-02T21:34:24Z
status: passed
score: 3/3 roadmap success criteria verified
requirements_completed:
  - MR-02
  - MR-03
  - MR-04
must_haves:
  total: 18
  verified: 18
  failed: 0
  uncertain: 0
findings:
  blockers: 0
  warnings: 0
human_verification: []
gaps: []
---

# Phase 8: Codex & Gemini Runtime Parity Verification Report

**Phase Goal:** Bring Codex and Gemini CLI runtimes up to parity after Claude daily-use stability, with generated instruction files, documented/tested runtime divergences, and per-runtime smoke coverage.

**Status:** passed

## Goal Achievement

Phase 8 achieved its roadmap goal and the stricter user-locked daily-peer bar from `08-CONTEXT.md`. The project-root instruction files are generated from one template, Codex and Gemini runtime transforms are implemented and fixture-locked, `decisions/runtime-tool-matrix.md` is generated from code, and the per-command matrix reports 100% PASS for Claude, Codex, and Gemini.

The runtime smoke layer verifies real tmpdir installs through `bin/install.js`. Codex live invocation ran locally on `codex-cli 0.128.0`; Gemini install-shape tests passed and the live invocation test skipped with the expected version gate because local `gemini` is `0.26.0`, below the `0.38` minimum captured in the plan. This is the intended CI-friendly behavior for Phase 8; Phase 10 owns provisioning newer runtime binaries in CI.

## Roadmap Success Criteria

| Criterion | Status | Evidence |
|---|---|---|
| `CLAUDE.md`, `AGENTS.md`, and `GEMINI.md` are generated from a single source-of-truth template with per-runtime transformations. | VERIFIED | `oto/templates/instruction-file.md`, `bin/lib/instruction-file.cjs`, and `scripts/render-instruction-files.cjs` exist; `tests/phase-08-instruction-file-render.test.cjs` enforces byte-equality regen diff for all three root files. |
| Runtime-specific instruction-file divergences are documented in `runtime-tool-matrix.md` and verified by per-runtime fixture tests. | VERIFIED | `decisions/runtime-tool-matrix.md` is generated by `scripts/render-runtime-matrix.cjs`; `tests/phase-08-runtime-matrix-render.test.cjs` enforces regen diff, Gemini tool-map consistency, Codex sandbox consistency, and 100% Codex/Gemini command support. Codex/Gemini/Claude fixture tests pass. |
| Per-runtime smoke test passes for install, representative command surface, state-file write, and no tool-shape errors. | VERIFIED | `tests/phase-08-smoke-codex.integration.test.cjs` verifies command files, install state, AGENTS.md marker, config.toml hook marker, per-agent TOML sandbox mode, and local Codex live invocation. `tests/phase-08-smoke-gemini.integration.test.cjs` verifies command files, install state, GEMINI.md marker, settings hook dialect, enableAgents, and converted matchers; live Gemini invocation skips clearly until local/CI Gemini is >= 0.38. |

## Requirement Verification

| Requirement | Status | Evidence |
|---|---|---|
| MR-02 | VERIFIED | Single-source template and renderer are implemented; `node --test tests/phase-08-instruction-file-render.test.cjs` passes and fails on generated-file drift. |
| MR-03 | VERIFIED | Codex transform/TOML/profile tests, Gemini transform/toolmap/settings tests, runtime matrix regen tests, and per-runtime fixture goldens all pass. `decisions/runtime-tool-matrix.md` includes all required matrix sections and a 100% PASS per-command support table. |
| MR-04 | VERIFIED | Codex and Gemini tmpdir smoke tests run through `bin/install.js` and assert runtime-specific state/config outputs. Codex live CLI path ran locally; Gemini live CLI path correctly version-skipped. |

## Key Links

- `scripts/render-instruction-files.cjs` -> `bin/lib/instruction-file.cjs::render()` -> `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`.
- `bin/lib/runtime-codex.cjs` -> `bin/lib/codex-transform.cjs`, `bin/lib/codex-toml.cjs`, `bin/lib/codex-profile.cjs`, and `adapter.emitDerivedFiles()`.
- `bin/lib/runtime-gemini.cjs` -> `bin/lib/gemini-transform.cjs` for agent conversion, command TOML emit, Task rewrite, hook event names, and matcher conversion.
- `bin/lib/runtime-matrix.cjs` -> `bin/lib/gemini-transform.cjs::claudeToGeminiTools` and `bin/lib/runtime-codex.cjs::agentSandboxes`.
- `tests/phase-08-smoke-codex.integration.test.cjs` and `tests/phase-08-smoke-gemini.integration.test.cjs` -> `bin/install.js`.

## Automated Checks

| Check | Result |
|---|---|
| `gsd-sdk query phase-plan-index 08` | 6 plans, 6 summaries, `incomplete: []` |
| `gsd-sdk query verify.schema-drift 08` | `valid: true`, 0 issues, 6 checked |
| `node --test tests/phase-08-*.test.cjs` | 61 tests, 60 pass, 1 skipped, 0 fail, 0 todo |
| `node --test tests/phase-08-claude-identity.test.cjs tests/phase-08-smoke-codex.integration.test.cjs tests/phase-08-smoke-gemini.integration.test.cjs` | 11 pass, 1 skipped, 0 fail, 0 todo |
| `npm test` | 349 tests, 348 pass, 1 skipped, 0 fail, 0 todo |
| `rg -n "test\\.todo" tests/phase-08-*.test.cjs` | no matches |
| Runtime smoke tmpdir leftover check | no `oto-smoke-codex-*` or `oto-smoke-gemini-*` leftovers |

## Residual Notes

- The one skipped test is intentional in this local environment: `gemini v0.26.0 < required v0.38`. The install-shape Gemini smoke still runs and passes. Phase 10 owns CI/runtime provisioning for an unskipped Gemini live invocation.
- `reports/rebrand-dryrun.*` are rewritten by the full suite; they were restored after each run and are not Phase 8 deliverables.
- No human verification is required for Phase 8 closeout. The optional Phase-4-style operator dogfood for Codex/Gemini is explicitly deferred in `08-CONTEXT.md`.

## Gaps Summary

No phase-blocking gaps remain.

---

_Verified: 2026-05-02T21:34:24Z_
_Verifier: Codex (inline fallback; subagent unavailable in this runtime)_
