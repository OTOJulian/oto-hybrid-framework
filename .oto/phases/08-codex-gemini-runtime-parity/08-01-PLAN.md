---
phase: 08
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-08-instruction-file-render.test.cjs
  - tests/phase-08-runtime-matrix-render.test.cjs
  - tests/phase-08-codex-transform.test.cjs
  - tests/phase-08-codex-toml.test.cjs
  - tests/phase-08-codex-profile.test.cjs
  - tests/phase-08-gemini-transform.test.cjs
  - tests/phase-08-gemini-toolmap.test.cjs
  - tests/phase-08-gemini-settings.test.cjs
  - tests/phase-08-smoke-codex.integration.test.cjs
  - tests/phase-08-smoke-gemini.integration.test.cjs
  - tests/phase-08-claude-identity.test.cjs
  - tests/fixtures/runtime-parity/claude/.gitkeep
  - tests/fixtures/runtime-parity/codex/.gitkeep
  - tests/fixtures/runtime-parity/gemini/.gitkeep
autonomous: true
requirements:
  - MR-02
  - MR-03
  - MR-04
must_haves:
  truths:
    - "All 11 phase-08-*.test.cjs files exist on disk"
    - "Each test file uses node:test t.todo() placeholders so the suite stays green"
    - "Fixture skeleton directories exist for the three runtimes"
    - "No phase-08 test passes a real assertion yet (every behavior gated behind t.todo)"
  artifacts:
    - path: "tests/phase-08-instruction-file-render.test.cjs"
      provides: "D-03 regen-diff scaffold"
      min_lines: 8
    - path: "tests/phase-08-runtime-matrix-render.test.cjs"
      provides: "D-05/D-08 regen-diff + matrix-vs-code byte-equality scaffold"
      min_lines: 8
    - path: "tests/phase-08-codex-transform.test.cjs"
      provides: "D-11 fixture-golden scaffold"
      min_lines: 8
    - path: "tests/phase-08-codex-toml.test.cjs"
      provides: "D-10 hook merge round-trip scaffold"
      min_lines: 8
    - path: "tests/phase-08-codex-profile.test.cjs"
      provides: "D-10 resolveAgentModel scaffold"
      min_lines: 8
    - path: "tests/phase-08-gemini-transform.test.cjs"
      provides: "D-13/D-14 fixture-golden scaffold"
      min_lines: 8
    - path: "tests/phase-08-gemini-toolmap.test.cjs"
      provides: "D-13 convertGeminiToolName unit scaffold"
      min_lines: 8
    - path: "tests/phase-08-gemini-settings.test.cjs"
      provides: "D-16 mergeSettings + experimental.enableAgents scaffold"
      min_lines: 8
    - path: "tests/phase-08-smoke-codex.integration.test.cjs"
      provides: "D-17/D-18 spine smoke scaffold"
      min_lines: 8
    - path: "tests/phase-08-smoke-gemini.integration.test.cjs"
      provides: "D-17/D-18 spine smoke scaffold"
      min_lines: 8
    - path: "tests/phase-08-claude-identity.test.cjs"
      provides: "Pitfall 5 sanity baseline scaffold"
      min_lines: 8
    - path: "tests/fixtures/runtime-parity/claude/.gitkeep"
      provides: "Fixture skeleton placeholder"
    - path: "tests/fixtures/runtime-parity/codex/.gitkeep"
      provides: "Fixture skeleton placeholder"
    - path: "tests/fixtures/runtime-parity/gemini/.gitkeep"
      provides: "Fixture skeleton placeholder"
  key_links:
    - from: "tests/phase-08-*.test.cjs"
      to: "node:test"
      via: "require('node:test')"
      pattern: "require\\('node:test'\\)"
---

<objective>
Wave 0: Create all Phase 8 test-stub files and fixture skeleton directories so downstream plans (Plans 02–06) can fill in real behavior against existing test files instead of inventing new names mid-implementation.

Purpose: Lock the test surface (11 files + 3 fixture dirs from `08-VALIDATION.md` "Wave 0 Requirements") with `t.todo()` placeholders. Plans in Waves 1–4 replace the t.todo bodies with real assertions as transforms land. Matches Phase 4 D-01 / Phase 5 D-01 / Phase 6 D-01 scaffold pattern.

Output: 11 `tests/phase-08-*.test.cjs` files + 3 fixture skeleton dirs. Zero behavior changes elsewhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-codex-gemini-runtime-parity/08-CONTEXT.md
@.planning/phases/08-codex-gemini-runtime-parity/08-RESEARCH.md
@.planning/phases/08-codex-gemini-runtime-parity/08-VALIDATION.md

<interfaces>
Existing scaffold convention from Phase 4 / 5 / 6:
- `require('node:test')` named exports `test` (which has `.todo`)
- Each test file: `'use strict';` header, optional `const { test } = require('node:test');`, then one or more `test.todo('...')` calls.
- Wave 0 stubs MUST stay GREEN (no failing assertions) so the test suite remains passable while implementation lands.

Reference shapes (from prior wave-0 scaffolds):
```javascript
'use strict';
const { test } = require('node:test');
test.todo('phase-08 instruction-file-render: regen-diff against committed CLAUDE.md/AGENTS.md/GEMINI.md (D-03)');
test.todo('phase-08 instruction-file-render: SoT renderer strips wrong-runtime fences (D-02)');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 11 phase-08 test stub files</name>
  <files>tests/phase-08-instruction-file-render.test.cjs, tests/phase-08-runtime-matrix-render.test.cjs, tests/phase-08-codex-transform.test.cjs, tests/phase-08-codex-toml.test.cjs, tests/phase-08-codex-profile.test.cjs, tests/phase-08-gemini-transform.test.cjs, tests/phase-08-gemini-toolmap.test.cjs, tests/phase-08-gemini-settings.test.cjs, tests/phase-08-smoke-codex.integration.test.cjs, tests/phase-08-smoke-gemini.integration.test.cjs, tests/phase-08-claude-identity.test.cjs</files>
  <read_first>
    - tests/04-frontmatter-validation.test.cjs (Phase 4 scaffold pattern)
    - tests/05-session-start-fixture.test.cjs (Phase 5 fixture-test scaffold pattern)
    - .planning/phases/08-codex-gemini-runtime-parity/08-VALIDATION.md "Wave 0 Requirements" list (lines 63–80)
    - .planning/phases/08-codex-gemini-runtime-parity/08-CONTEXT.md "Test Surface Summary" section (lines 115–129)
  </read_first>
  <action>
    Create all 11 test stub files with `'use strict';` header + `const { test } = require('node:test');` and ONE `test.todo(...)` per planned coverage row from `08-VALIDATION.md` Per-Task Verification Map (lines 41–55). Each `test.todo()` description must reference the decision ID it covers (D-02, D-03, D-08, D-10, D-11, D-13, D-14, D-16, D-17, D-18).

    Exact `test.todo()` strings per file (ONE PER LINE):

    `tests/phase-08-instruction-file-render.test.cjs`:
      - `test.todo('D-03 regen-diff: in-memory render(\'claude\', ctx) byte-equals committed CLAUDE.md');`
      - `test.todo('D-03 regen-diff: in-memory render(\'codex\', ctx) byte-equals committed AGENTS.md');`
      - `test.todo('D-03 regen-diff: in-memory render(\'gemini\', ctx) byte-equals committed GEMINI.md');`
      - `test.todo('D-02 fence pairing: validateFencePairing throws on unclosed runtime fence (Pitfall 7)');`
      - `test.todo('D-02 token substitution: {{otoVersion}} replaced from ctx');`

    `tests/phase-08-runtime-matrix-render.test.cjs`:
      - `test.todo('D-05 regen-diff: in-memory render byte-equals committed decisions/runtime-tool-matrix.md');`
      - `test.todo('D-08 matrix-vs-code: Claude→Gemini map column matches convertGeminiToolName table byte-for-byte');`
      - `test.todo('D-08 matrix-vs-code: Codex sandbox column matches runtime-codex.cjs::agentSandboxes byte-for-byte');`
      - `test.todo('D-09 per-command runtime support: Codex column 100% green');`
      - `test.todo('D-09 per-command runtime support: Gemini column 100% green');`

    `tests/phase-08-codex-transform.test.cjs`:
      - `test.todo('D-11 oto-executor agent transformed via runtime-codex.transformAgent matches tests/fixtures/runtime-parity/codex/oto-executor.expected.md');`
      - `test.todo('D-11 oto-executor per-agent .toml emit matches tests/fixtures/runtime-parity/codex/oto-executor.expected.toml');`
      - `test.todo('D-11 transformed agent body contains zero literal `gsd-` or `superpowers-` strings (Pitfall 1 / Pitfall 15)');`
      - `test.todo('D-08 /oto-progress command transform output matches tests/fixtures/runtime-parity/codex/oto-progress.expected.md');`
      - `test.todo('D-08 oto:test-driven-development skill transform output matches tests/fixtures/runtime-parity/codex/test-driven-development.expected.md');`

    `tests/phase-08-codex-toml.test.cjs`:
      - `test.todo('D-10 mergeHooksBlock injects [[hooks]] array between BEGIN OTO HOOKS / END OTO HOOKS markers');`
      - `test.todo('D-10 idempotent rewrite: re-merging strips prior block first, then injects fresh content (no duplication)');`
      - `test.todo('D-10 user-content preservation: user-authored entries above and below oto marker block survive merge → unmerge round-trip');`
      - `test.todo('D-10 unmerge removes ONLY oto-marked entries; non-oto [[hooks]] entries untouched');`
      - `test.todo('Pitfall 2 guard: refuses to merge when both legacy [hooks.X] and modern [[hooks]] formats coexist');`

    `tests/phase-08-codex-profile.test.cjs`:
      - `test.todo('D-10 resolveAgentModel: per-project model_overrides wins over global ~/.oto/defaults.json');`
      - `test.todo('D-10 resolveAgentModel: model_profile from .oto/config.json drives RUNTIME_PROFILE_MAP[codex] tier resolution');`
      - `test.todo('D-10 resolveAgentModel: returns { model, reasoning_effort } for codex');`
      - `test.todo('D-10 resolveAgentModel: omits reasoning_effort for non-codex runtimes');`
      - `test.todo('D-10 loadDefaults: hand-rolled validation rejects malformed ~/.oto/defaults.json');`

    `tests/phase-08-gemini-transform.test.cjs`:
      - `test.todo('D-13 oto-executor agent transformed via convertClaudeToGeminiAgent matches tests/fixtures/runtime-parity/gemini/oto-executor.expected.md');`
      - `test.todo('D-13 ${VAR} → $VAR escape: oto-executor body retains zero unresolved ${WORD} patterns');`
      - `test.todo('D-13 frontmatter: color: and skills: stripped, tools: emitted as YAML array');`
      - `test.todo('D-14 Task() rewrite: bare Task(subagent_type=\'oto-foo\', prompt=\'...\') rewritten to Gemini agent-as-tool invocation');`
      - `test.todo('D-14 Task() rewrite: fenced-code Task() examples preserved verbatim (Open Question 2)');`
      - `test.todo('D-15 parallel Task() block: three adjacent Tasks rewritten as one parallel-tool-use instruction');`
      - `test.todo('D-13 oto:test-driven-development skill transform output matches tests/fixtures/runtime-parity/gemini/test-driven-development.expected.md');`
      - `test.todo('D-13 /oto-progress command rewritten as TOML at tests/fixtures/runtime-parity/gemini/oto-progress.expected.toml');`
      - `test.todo('Pitfall 1 / Pitfall 15: transformed agent body contains zero literal `gsd-` or `superpowers-` strings');`

    `tests/phase-08-gemini-toolmap.test.cjs`:
      - `test.todo('D-13 convertGeminiToolName: every entry in claudeToGeminiTools map (Read/Write/Edit/Bash/Glob/Grep/WebSearch/WebFetch/TodoWrite/AskUserQuestion)');`
      - `test.todo('D-13 convertGeminiToolName: mcp__* prefix returns null (auto-discovered from mcpServers)');`
      - `test.todo('D-13 convertGeminiToolName: Task returns null (agents auto-registered as callable tools)');`
      - `test.todo('D-13 convertGeminiToolName: unknown tool falls back to lowercase');`

    `tests/phase-08-gemini-settings.test.cjs`:
      - `test.todo('D-16 mergeSettings: PreToolUse → BeforeTool, PostToolUse → AfterTool event-name renames');`
      - `test.todo('D-16 mergeSettings: matchers run through convertGeminiToolName (Bash → run_shell_command, etc., Pitfall 3)');`
      - `test.todo('D-16 experimental.enableAgents = true written when missing');`
      - `test.todo('D-16 T-08-tampering-enableAgents: existing experimental.enableAgents=false honored — installer warns + skips agent emit');`
      - `test.todo('D-16 statusLine field NOT written on Gemini (no statusLine equivalent)');`

    `tests/phase-08-smoke-codex.integration.test.cjs`:
      - `test.todo('D-17 codex spine: install → /oto-help → /oto-progress → /oto-new-project tmpdir flow → state file written under .oto/');`
      - `test.todo('D-17 codex per-agent .toml present at <configDir>/agents/oto-executor.toml with sandbox_mode = workspace-write (T-08-access-control-sandbox)');`
      - `test.todo('D-17 codex spine smoke skips with clear message if codex --version < 0.120 or codex not on PATH');`

    `tests/phase-08-smoke-gemini.integration.test.cjs`:
      - `test.todo('D-17 gemini spine: install → /oto-help → /oto-progress → /oto-new-project tmpdir flow → state file written under .oto/');`
      - `test.todo('D-17 gemini per-runtime hook present in settings.json under BeforeTool / AfterTool events');`
      - `test.todo('D-17 gemini spine smoke skips with clear message if gemini --version < 0.38 or gemini not on PATH');`

    `tests/phase-08-claude-identity.test.cjs`:
      - `test.todo('Pitfall 5 baseline: oto-executor agent through runtime-claude.transformAgent is byte-identical to source');`
      - `test.todo('Pitfall 5 baseline: /oto-progress command through runtime-claude.transformCommand is byte-identical to source');`
      - `test.todo('Pitfall 5 baseline: oto:test-driven-development skill through runtime-claude.transformSkill is byte-identical to source');`

    NO `test()` real bodies. NO assertions. ONLY `test.todo(...)` calls. The suite must remain green after this task.
  </action>
  <verify>
    <automated>node --test tests/phase-08-*.test.cjs 2>&1 | grep -E "^(# pass|# fail|# todo)" && test $(ls tests/phase-08-*.test.cjs | wc -l) -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/phase-08-*.test.cjs | wc -l` returns `11`
    - `node --test tests/phase-08-*.test.cjs` exits 0 (suite green; all marked todo)
    - `grep -l "test.todo" tests/phase-08-*.test.cjs | wc -l` returns `11`
    - `grep -L "require('node:test')" tests/phase-08-*.test.cjs` returns empty (every file requires node:test)
    - No `test('...', ...)` calls (only `test.todo(...)`) — `grep -E "^test\\(" tests/phase-08-*.test.cjs` returns empty
  </acceptance_criteria>
  <done>11 phase-08-*.test.cjs files exist; suite runs green with all behaviors marked t.todo(); no real assertions executed</done>
</task>

<task type="auto">
  <name>Task 2: Create fixture skeleton directories for the three runtimes</name>
  <files>tests/fixtures/runtime-parity/claude/.gitkeep, tests/fixtures/runtime-parity/codex/.gitkeep, tests/fixtures/runtime-parity/gemini/.gitkeep</files>
  <read_first>
    - .planning/phases/08-codex-gemini-runtime-parity/08-RESEARCH.md "Recommended Project Structure" section (lines 160–174)
    - .planning/phases/08-codex-gemini-runtime-parity/08-CONTEXT.md D-08 fixture trio definition
    - tests/fixtures/phase-05/ (existing fixture dir convention)
  </read_first>
  <action>
    Create three empty directories under `tests/fixtures/runtime-parity/`: `claude/`, `codex/`, `gemini/`. Each gets a `.gitkeep` placeholder file (zero bytes is fine; can also contain a single comment line).

    Wave 1 / Wave 2 plans will populate these dirs with `.expected.md` / `.expected.toml` golden files. Wave 0 only creates the directory shells so subsequent plans don't need to mkdir.

    Exact file paths to create:
    - `tests/fixtures/runtime-parity/claude/.gitkeep`
    - `tests/fixtures/runtime-parity/codex/.gitkeep`
    - `tests/fixtures/runtime-parity/gemini/.gitkeep`

    `.gitkeep` content (each file): `# Phase 8 fixture skeleton (Wave 0 stub). Goldens land in Wave 2.\n`

    No code changes elsewhere. No test changes (Task 1 already covers).
  </action>
  <verify>
    <automated>test -d tests/fixtures/runtime-parity/claude && test -d tests/fixtures/runtime-parity/codex && test -d tests/fixtures/runtime-parity/gemini && test -f tests/fixtures/runtime-parity/claude/.gitkeep && test -f tests/fixtures/runtime-parity/codex/.gitkeep && test -f tests/fixtures/runtime-parity/gemini/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d tests/fixtures/runtime-parity/claude` exits 0
    - `test -d tests/fixtures/runtime-parity/codex` exits 0
    - `test -d tests/fixtures/runtime-parity/gemini` exits 0
    - `test -f tests/fixtures/runtime-parity/claude/.gitkeep` exits 0 (and same for codex/gemini)
    - `git ls-files tests/fixtures/runtime-parity/` lists all three .gitkeep files (after add)
  </acceptance_criteria>
  <done>Three fixture-parity directories exist with .gitkeep placeholders; ready for golden files in Wave 2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ test files | `node:test` parses test files; malformed test syntax could crash runner — mitigated by `'use strict'` + standard `require('node:test')` pattern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-tampering | Tampering | Test stub files | accept | Wave 0 stubs are scaffolds with no assertions; cannot tamper with state — only `test.todo()` placeholders. |
| T-08-01-info | Information Disclosure | Fixture dirs | accept | Empty `.gitkeep` placeholders contain no secrets; commit history is the only "information" surface. |

No `high`-severity threats applicable to scaffold-only work. The substantive threat surface (T-08-tampering-marker, T-08-tampering-shellquote, T-08-tampering-toml, T-08-info-disclosure-identity, T-08-tampering-enableagents, T-08-access-control-sandbox) is mitigated by Plans 02–06 which contain the actual transform logic.
</threat_model>

<verification>
- `node --test tests/phase-08-*.test.cjs` exits 0
- 11 test files + 3 fixture dirs present
- Suite remains green (no failing assertions); all behavior marked t.todo
</verification>

<success_criteria>
Wave 0 is complete when all 11 phase-08 test stubs and 3 fixture skeleton dirs exist and `npm test` stays green. Plans 02–06 may then replace `test.todo()` with real `test('...', () => { ... })` bodies.
</success_criteria>

<output>
After completion, create `.planning/phases/08-codex-gemini-runtime-parity/08-01-SUMMARY.md`.
</output>
</content>
</invoke>