---
phase: 06
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/06-skill-structure.test.cjs
  - tests/06-installer-skill-copy.test.cjs
  - tests/06-using-oto-state-gating.test.cjs
  - tests/skills/__fixtures__/STATE-active.md
autonomous: true
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
  - SKL-04
  - SKL-05
  - SKL-06
  - SKL-07
  - SKL-08
must_haves:
  truths:
    - "Three failing test files exist at tests/06-*.test.cjs and exit non-zero when run against the current tree (no oto/skills/ yet)"
    - "Test files encode every D-07/D-08/D-09 axis listed in 06-VALIDATION.md as t.todo() or live test() blocks"
    - "Wave 0 contract holds: tests are red, no production code exists yet, Wave 1 plans fill in artifacts that turn each axis green"
  artifacts:
    - path: "tests/06-skill-structure.test.cjs"
      provides: "D-07 structure + collision + agent-invocation static-analysis assertions"
      contains: "test('"
    - path: "tests/06-installer-skill-copy.test.cjs"
      provides: "D-08 installer copy fidelity (sha256 + exec bit) + install-state JSON marker"
      contains: "test('"
    - path: "tests/06-using-oto-state-gating.test.cjs"
      provides: "D-09 STATE.md gating directive presence + Pitfall 15 literal-string-leak defense"
      contains: "test('"
    - path: "tests/skills/__fixtures__/STATE-active.md"
      provides: "Reference STATE fixture with status: execute_phase for D-09 prose anchor"
  key_links:
    - from: "tests/06-skill-structure.test.cjs"
      to: "oto/skills/<name>/SKILL.md (created by Plan 02)"
      via: "fs.readFileSync + frontmatter regex parse"
      pattern: "fs\\.readFileSync.*oto/skills"
    - from: "tests/06-installer-skill-copy.test.cjs"
      to: "bin/lib/install.cjs::installRuntime + bin/lib/runtime-claude.cjs"
      via: "require + invoke against temp configDir"
      pattern: "require.*install\\.cjs|require.*runtime-claude"
    - from: "tests/06-using-oto-state-gating.test.cjs"
      to: "oto/skills/using-oto/SKILL.md (created/hand-fixed by Plan 02)"
      via: "fs.readFileSync + substring/marker grep"
      pattern: "oto:state-gating-directive|\\.oto/STATE\\.md"
---

<objective>
Seed Wave 0 test scaffolds for Phase 6. Three node:test files plus an optional STATE fixture establish the Nyquist sampling contract for SKL-01..08 before any skill content lands. Tests MUST fail on first run because oto/skills/ does not yet exist — that failure is the proof Wave 1 work is doing something.

Purpose: Make the test surface authoritative before content. Mirrors Phase 5 Wave 0 (tests/05-*.test.cjs scaffolds shipped before hooks rewritten). Eliminates "implementation is complete because nothing tested it" failure mode.
Output: 3 *.test.cjs files in tests/, 1 fixture file under tests/skills/__fixtures__/, all encoding the assertions from 06-VALIDATION.md per-task verification map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md
@.planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md
@.planning/phases/06-skills-port-cross-system-integration/06-VALIDATION.md

# Reference test (Phase 5 pattern — copy structure, not content)
@tests/05-session-start-fixture.test.cjs

# Reference test (Phase 5 install / copy pattern)
@tests/05-token-substitution.test.cjs

# Production wiring the install test will exercise
@bin/lib/install.cjs
@bin/lib/runtime-claude.cjs
@bin/lib/copy-files.cjs
@bin/lib/install-state.cjs

# Inventory drives expected file lists
@decisions/file-inventory.json

<interfaces>
<!-- Key APIs the test files will call. Extracted from production code. -->

From bin/lib/install.cjs:
```javascript
async function installRuntime(opts) // opts: { repoRoot, adapter, configDir, otoVersion }
// Walks SRC_KEYS = ['commands', 'agents', 'skills', 'hooks', 'workflows',
//                   'references', 'templates', 'contexts']
// Copies each to <configDir>/<adapter.targetSubdirs[key]>
// Writes install-state JSON at <configDir>/oto/.install.json with shape:
//   { version, oto_version, installed_at, runtime, config_dir,
//     files: [{ path, sha256 }, ...], instruction_file, hooks }
module.exports = { installRuntime, /* ... */ };
```

From bin/lib/runtime-claude.cjs:
```javascript
// Adapter shape (already wired for skills):
{
  name: 'claude',
  sourceDirs: { skills: 'oto/skills', /* ... */ },
  targetSubdirs: { skills: 'skills', /* ... */ },
  transformSkill: (content) => content,  // identity
}
```

From bin/lib/copy-files.cjs:
```javascript
async function sha256File(absPath) // returns hex digest
async function copyTree(srcAbs, dstAbs) // returns { files: [{ relPath, sha256, mode }] }
```

From bin/lib/install-state.cjs:
```javascript
function readState(statePath) // returns parsed JSON or null
function writeState(statePath, state)
```
</interfaces>

<expected_skill_inventory>
<!-- The 7 retained skill directories and their expected file counts (from 06-RESEARCH.md
     "Phase Requirements" table, cross-checked against decisions/file-inventory.json
     phase_owner: 6 rows). Test fixtures should hardcode this list. -->

oto/skills/test-driven-development/         → 2 files: SKILL.md, testing-anti-patterns.md
oto/skills/systematic-debugging/             → 11 files: SKILL.md, CREATION-LOG.md,
                                                condition-based-waiting.md,
                                                condition-based-waiting-example.ts,
                                                defense-in-depth.md, find-polluter.sh,
                                                root-cause-tracing.md, test-academic.md,
                                                test-pressure-1.md, test-pressure-2.md,
                                                test-pressure-3.md
oto/skills/verification-before-completion/   → 1 file: SKILL.md
oto/skills/dispatching-parallel-agents/      → 1 file: SKILL.md
oto/skills/using-git-worktrees/              → 1 file: SKILL.md
oto/skills/writing-skills/                   → 7 files: SKILL.md,
                                                anthropic-best-practices.md,
                                                examples/CLAUDE_MD_TESTING.md,
                                                graphviz-conventions.dot,
                                                persuasion-principles.md,
                                                render-graphs.js,
                                                testing-skills-with-subagents.md
oto/skills/using-oto/                         → 4 files: SKILL.md,
                                                references/codex-tools.md,
                                                references/copilot-tools.md,
                                                references/gemini-tools.md

Total: 27 files. find-polluter.sh must be mode 0o755 (mode & 0o111 != 0).
</expected_skill_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/06-skill-structure.test.cjs (D-07 + SKL-08 static analysis)</name>
  <files>tests/06-skill-structure.test.cjs</files>
  <read_first>
    - tests/05-session-start-fixture.test.cjs (Phase 5 pattern — node:test + node:fs + node:path; literal-string-scan idiom; banned-substring loop on lines 28–34)
    - .planning/phases/06-skills-port-cross-system-integration/06-VALIDATION.md (Per-Task Verification Map rows for SKL-01..07 structure and SKL-08 axes 1–4)
    - .planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md ("Phase Requirements" table for skill file counts; "Code Examples" Example 3 for fixture-test pattern)
    - decisions/file-inventory.json (sanity-check the 27 phase_owner: 6 rows enumerated in <expected_skill_inventory> above)
    - oto/agents/oto-executor.md (line 58 — current generic prose to be replaced in Plan 03)
    - oto/agents/oto-verifier.md (line 54 — current generic prose to be replaced in Plan 03)
    - oto/agents/oto-debugger.md (line 41 — current generic prose to be replaced in Plan 03)
  </read_first>
  <action>
    Create `tests/06-skill-structure.test.cjs` as a CommonJS node:test file mirroring the structure of `tests/05-session-start-fixture.test.cjs`. Use exactly this header preamble:

    ```javascript
    'use strict';
    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');
    const REPO_ROOT = path.resolve(__dirname, '..');
    const SKILLS_ROOT = path.join(REPO_ROOT, 'oto/skills');
    const COMMANDS_ROOT = path.join(REPO_ROOT, 'oto/commands/oto');
    const AGENTS_ROOT = path.join(REPO_ROOT, 'oto/agents');

    const SKILL_INVENTORY = {
      'test-driven-development': ['SKILL.md', 'testing-anti-patterns.md'],
      'systematic-debugging': [
        'SKILL.md', 'CREATION-LOG.md',
        'condition-based-waiting.md', 'condition-based-waiting-example.ts',
        'defense-in-depth.md', 'find-polluter.sh',
        'root-cause-tracing.md', 'test-academic.md',
        'test-pressure-1.md', 'test-pressure-2.md', 'test-pressure-3.md',
      ],
      'verification-before-completion': ['SKILL.md'],
      'dispatching-parallel-agents': ['SKILL.md'],
      'using-git-worktrees': ['SKILL.md'],
      'writing-skills': [
        'SKILL.md', 'anthropic-best-practices.md',
        'examples/CLAUDE_MD_TESTING.md',
        'graphviz-conventions.dot', 'persuasion-principles.md',
        'render-graphs.js', 'testing-skills-with-subagents.md',
      ],
      'using-oto': [
        'SKILL.md',
        'references/codex-tools.md',
        'references/copilot-tools.md',
        'references/gemini-tools.md',
      ],
    };

    function parseFrontmatter(md) {
      // Returns plain { name, description, ... } object or null.
      // Hand-rolled per "Don't Hand-Roll" table allowance: regex extract of
      // `^([a-z_-]+):\s*(.+)$` between leading `---` fences. No YAML lib.
      if (!md.startsWith('---\n')) return null;
      const end = md.indexOf('\n---', 4);
      if (end === -1) return null;
      const body = md.slice(4, end);
      const out = {};
      for (const line of body.split('\n')) {
        const m = line.match(/^([a-z_-]+):\s*(.+)$/);
        if (m) out[m[1]] = m[2].trim();
      }
      return out;
    }
    ```

    Then add the following live `test()` blocks (NOT t.todo() — these are real assertions that MUST FAIL until Plan 02/03 complete):

    1. `test('phase-06 D-07: all 7 skill directories exist with SKILL.md', ...)` — Loop over `Object.keys(SKILL_INVENTORY)`; for each, assert `fs.existsSync(path.join(SKILLS_ROOT, name, 'SKILL.md'))`.

    2. `test('phase-06 D-07: every skill SKILL.md frontmatter parses with name and description', ...)` — Loop over skill names; read SKILL.md; parse frontmatter; assert `fm !== null`, `typeof fm.name === 'string'`, `typeof fm.description === 'string'`, `fm.name.length > 0`, `fm.description.length > 0`.

    3. `test('phase-06 D-07: every skill ships its complete payload from inventory', ...)` — Loop over `[name, files]` of `SKILL_INVENTORY`; for each `file`, assert `fs.existsSync(path.join(SKILLS_ROOT, name, file))` with a message that names the missing file.

    4. `test('phase-06 D-07: skill name does not collide with any /oto-<name> command', ...)` — For each skill name, assert that `fs.existsSync(path.join(COMMANDS_ROOT, name + '.md'))` is FALSE. This is the Pitfall B / SKL-08 axis 4 collision check.

    5. `test('phase-06 SKL-08 axis 1: oto-executor invokes oto:test-driven-development before write', ...)` — Read `oto/agents/oto-executor.md`; assert `body.includes("Skill('oto:test-driven-development')")` AND `body.includes("Skill('oto:verification-before-completion')")`. Add a literal-string-scan defense: assert `body.indexOf("Follow skill rules relevant to the task you are about to commit.") === -1` (the placeholder must be gone — no double-write).

    6. `test('phase-06 SKL-08 axis 2: oto-verifier invokes oto:verification-before-completion at start', ...)` — Read `oto/agents/oto-verifier.md`; assert `body.includes("Skill('oto:verification-before-completion')")`; assert the placeholder `"Apply skill rules when scanning for anti-patterns and verifying quality."` is gone.

    7. `test('phase-06 SKL-08 axis 3: oto-debugger invokes oto:systematic-debugging at session start', ...)` — Read `oto/agents/oto-debugger.md`; assert `body.includes("Skill('oto:systematic-debugging')")`; assert the placeholder `"Follow skill rules relevant to the bug being investigated and the fix being applied."` is gone.

    Save the file. Do NOT use `t.todo()` — every block above must be a real `test()` so the Wave 0 red→green transition is observable when Plans 02/03 land.
  </action>
  <verify>
    <automated>node --test tests/06-skill-structure.test.cjs ; test $? -ne 0 # Wave 0: MUST exit non-zero (oto/skills/ does not exist yet)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/06-skill-structure.test.cjs` exists.
    - `grep -F "require('node:test')" tests/06-skill-structure.test.cjs` returns 1 match.
    - `grep -cE "^test\(" tests/06-skill-structure.test.cjs` returns 7 (seven live test blocks, no t.todo).
    - `grep -F "SKILL_INVENTORY" tests/06-skill-structure.test.cjs` returns ≥3 matches (declared once, looped over multiple times).
    - `grep -F "Skill('oto:test-driven-development')" tests/06-skill-structure.test.cjs` returns ≥1 match.
    - `grep -F "Skill('oto:verification-before-completion')" tests/06-skill-structure.test.cjs` returns ≥2 matches (executor + verifier axes).
    - `grep -F "Skill('oto:systematic-debugging')" tests/06-skill-structure.test.cjs` returns ≥1 match.
    - `grep -F "oto/commands/oto" tests/06-skill-structure.test.cjs` returns ≥1 match (collision check uses this path).
    - Running `node --test tests/06-skill-structure.test.cjs` exits non-zero (Wave 0 contract: test must fail because oto/skills/ does not exist yet).
    - File contains no `t.todo(` substring (`grep -c "t\.todo(" tests/06-skill-structure.test.cjs` returns 0).
  </acceptance_criteria>
  <done>
    Test file shipped, all seven test blocks live, file fails on current tree, ready for Plan 02/03 to flip green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/06-installer-skill-copy.test.cjs (D-08 install fidelity)</name>
  <files>tests/06-installer-skill-copy.test.cjs</files>
  <read_first>
    - bin/lib/install.cjs (full file — read the `installRuntime` signature and SRC_KEYS list at top, plus state JSON write at lines 128–142)
    - bin/lib/runtime-claude.cjs (full file — confirm sourceDirs.skills + targetSubdirs.skills mapping)
    - bin/lib/copy-files.cjs (sha256File + copyTree signatures)
    - bin/lib/install-state.cjs (readState/writeState signatures)
    - tests/05-token-substitution.test.cjs (Phase 5 pattern: temp-dir setup, run install logic against fixture, assert state)
    - .planning/phases/06-skills-port-cross-system-integration/06-VALIDATION.md ("Install copy fidelity" sampling row)
    - .planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md ("Pitfall C: Executable-Bit Loss on find-polluter.sh"; "Code Examples" Example 4 for state JSON contract)
  </read_first>
  <action>
    Create `tests/06-installer-skill-copy.test.cjs` as a CommonJS node:test file. Use this header:

    ```javascript
    'use strict';
    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const fsp = require('node:fs/promises');
    const os = require('node:os');
    const path = require('node:path');
    const crypto = require('node:crypto');

    const REPO_ROOT = path.resolve(__dirname, '..');
    const SKILLS_SRC = path.join(REPO_ROOT, 'oto/skills');

    const { installRuntime } = require(path.join(REPO_ROOT, 'bin/lib/install.cjs'));
    const adapter = require(path.join(REPO_ROOT, 'bin/lib/runtime-claude.cjs'));
    const { readState } = require(path.join(REPO_ROOT, 'bin/lib/install-state.cjs'));

    function sha256(absPath) {
      const buf = fs.readFileSync(absPath);
      return crypto.createHash('sha256').update(buf).digest('hex');
    }

    async function makeTempDir(prefix) {
      return fsp.mkdtemp(path.join(os.tmpdir(), prefix));
    }
    ```

    Add three live `test()` blocks:

    1. `test('phase-06 D-08: installer copies all skill files byte-identically', async (t) => { ... })`
       - Create a temp configDir with `await makeTempDir('oto-test-skills-')`.
       - Register `t.after(() => fs.rmSync(configDir, { recursive: true, force: true }))`.
       - Call `await installRuntime({ repoRoot: REPO_ROOT, adapter, configDir, otoVersion: '0.0.0-test' })`.
       - Walk `oto/skills/` recursively. For each file, compute `sha256(srcAbs)` and `sha256(path.join(configDir, 'skills', relPath))`. Assert hashes match.
       - This catches any transformSkill mutation, copyTree corruption, or path-mapping drift.

    2. `test('phase-06 D-08: find-polluter.sh exec bit preserved (Pitfall C)', async (t) => { ... })`
       - Same temp-dir setup as Task 1.
       - Run `installRuntime`.
       - Stat the destination: `const mode = fs.statSync(path.join(configDir, 'skills/systematic-debugging/find-polluter.sh')).mode;`
       - Assert `(mode & 0o111) !== 0` with message `'find-polluter.sh lost executable bit (mode-644 trap)'`.

    3. `test('phase-06 D-08: install-state JSON records every skill file with sha256', async (t) => { ... })`
       - Same setup.
       - Run `installRuntime`.
       - Read state via `readState(path.join(configDir, 'oto/.install.json'))`.
       - Assert `state !== null`.
       - Filter `state.files` to those whose `path` starts with `'skills/'`.
       - Build expected file list by walking `oto/skills/` recursively and converting absolute paths to `skills/<relPath>` form.
       - Assert the filtered count equals the expected count.
       - For each entry, assert `entry.sha256 === sha256(path.join(SKILLS_SRC, entry.path.slice('skills/'.length)))`.

    Wave 0 contract: this test MUST fail because `oto/skills/` does not exist yet — the recursive walk yields zero files and the install copies nothing useful, so the byte-equality and exec-bit assertions throw `ENOENT`. Plan 02 makes Tasks 1 and 3 green; the exec-bit assertion turns green when find-polluter.sh lands with its 0o755 mode.
  </action>
  <verify>
    <automated>node --test tests/06-installer-skill-copy.test.cjs ; test $? -ne 0 # Wave 0: MUST exit non-zero (oto/skills/ does not exist yet)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/06-installer-skill-copy.test.cjs` exists.
    - `grep -F "require('node:test')" tests/06-installer-skill-copy.test.cjs` returns 1 match.
    - `grep -cE "^test\(" tests/06-installer-skill-copy.test.cjs` returns 3.
    - `grep -F "installRuntime" tests/06-installer-skill-copy.test.cjs` returns ≥2 matches (require + invocation).
    - `grep -F "find-polluter.sh" tests/06-installer-skill-copy.test.cjs` returns ≥1 match.
    - `grep -F "0o111" tests/06-installer-skill-copy.test.cjs` returns ≥1 match (exec-bit assertion).
    - `grep -F "readState" tests/06-installer-skill-copy.test.cjs` returns ≥2 matches (require + invocation).
    - `grep -F "createHash('sha256')" tests/06-installer-skill-copy.test.cjs` returns ≥1 match.
    - `grep -F "fs.rmSync" tests/06-installer-skill-copy.test.cjs` returns ≥1 match (temp-dir cleanup wired).
    - Running `node --test tests/06-installer-skill-copy.test.cjs` exits non-zero (Wave 0 contract).
    - File contains no `t.todo(` substring.
  </acceptance_criteria>
  <done>
    Test file shipped, three live test blocks, exec-bit and state-JSON axes encoded, fails on current tree.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/06-using-oto-state-gating.test.cjs and tests/skills/__fixtures__/STATE-active.md (D-09 + Pitfall 15 defense)</name>
  <files>tests/06-using-oto-state-gating.test.cjs, tests/skills/__fixtures__/STATE-active.md</files>
  <read_first>
    - tests/05-session-start-fixture.test.cjs (lines 28–34 — exact banned-substring loop pattern; replicate verbatim then extend)
    - oto/hooks/__fixtures__/session-start-claude.json (the locked Phase 5 D-05 identity sentence — `using-oto/SKILL.md` body must align with this string at runtime)
    - .planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md (D-03 directive sentence wording; "Specific Ideas" item 2 — suggested grep-able sentence)
    - .planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md ("Pitfall A: Upstream Identity Leakage"; "Pitfall E: STATE.md Gating Sentence Drift" — recommended marker-comment style; "Code Examples" Example 3 — fixture-test pattern)
    - .planning/phases/06-skills-port-cross-system-integration/06-VALIDATION.md (D-09 sampling row)
  </read_first>
  <action>
    Create two files.

    File 1: `tests/skills/__fixtures__/STATE-active.md` — verbatim content:

    ```markdown
    ---
    status: execute_phase
    phase: 06
    plan: 02
    ---

    # State (active fixture)

    Sample STATE.md used by tests/06-using-oto-state-gating.test.cjs to assert
    that oto:using-oto's gating directive references the same `status:` values
    the skill body claims to recognize.
    ```

    File 2: `tests/06-using-oto-state-gating.test.cjs` — CommonJS node:test file with this exact structure:

    ```javascript
    'use strict';
    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');

    const REPO_ROOT = path.resolve(__dirname, '..');
    const SKILL_PATH = path.join(REPO_ROOT, 'oto/skills/using-oto/SKILL.md');
    const STATE_FIXTURE = path.join(__dirname, 'skills/__fixtures__/STATE-active.md');

    // Banned substrings — Pitfall 15 / T-15. Mirrors tests/05-session-start-fixture.test.cjs.
    const BANNED_LITERALS = [
      'superpowers',
      'Superpowers',
      'using-superpowers',
      'You have superpowers',
      '94% PR rejection',
      'fabricated content',
      'protect your human partner from PR rejection',
    ];

    // STATE.md status values the gating directive claims to recognize.
    const RECOGNIZED_STATUSES = ['execute_phase', 'plan_phase', 'debug', 'verify'];
    ```

    Add four live `test()` blocks:

    1. `test('phase-06 D-09: using-oto/SKILL.md contains STATE.md gating directive marker', () => { ... })`
       - Read SKILL.md; assert the file contains both marker comments `<!-- oto:state-gating-directive -->` AND `<!-- /oto:state-gating-directive -->`.
       - Assert `body.includes('.oto/STATE.md')`.
       - Assert `body.toLowerCase().includes('suppress')` (the suppression rule keyword).
       - Assert each value in `RECOGNIZED_STATUSES` appears at least once in the body (anchors the directive to the same status values the fixture uses).

    2. `test('phase-06 Pitfall 15: using-oto/SKILL.md contains no upstream-identity literals', () => { ... })`
       - Read SKILL.md.
       - Loop over `BANNED_LITERALS`; for each, assert `body.indexOf(banned) === -1` with message `\`Pitfall 15: upstream identity leaked: \${banned}\``.

    3. `test('phase-06 D-09: using-oto/SKILL.md preserves Phase 5 D-05 locked identity sentence', () => { ... })`
       - Read SKILL.md.
       - Assert `body.includes('You are using oto.')` (the Phase 5 D-05 locked sentence — keeps SessionStart fixture and skill body aligned per CONTEXT.md "Specific Ideas" item 1).

    4. `test('phase-06 D-09: STATE-active fixture file is well-formed', () => { ... })`
       - Read `STATE_FIXTURE`; assert it starts with `'---\n'`; assert it contains `'status: execute_phase'`. Sanity check that the fixture has not drifted away from the gating prose anchors.

    Wave 0 contract: blocks 1–3 MUST fail (the file `oto/skills/using-oto/SKILL.md` does not exist yet — `fs.readFileSync` throws `ENOENT`). Block 4 turns green as soon as this task lands. Plan 02 hand-fix turns blocks 1–3 green.
  </action>
  <verify>
    <automated>node --test tests/06-using-oto-state-gating.test.cjs ; test $? -ne 0 # Wave 0: MUST exit non-zero (oto/skills/using-oto/SKILL.md does not exist yet)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/06-using-oto-state-gating.test.cjs` exists.
    - File `tests/skills/__fixtures__/STATE-active.md` exists.
    - `grep -F "status: execute_phase" tests/skills/__fixtures__/STATE-active.md` returns 1 match.
    - `grep -cE "^test\(" tests/06-using-oto-state-gating.test.cjs` returns 4.
    - `grep -F "oto:state-gating-directive" tests/06-using-oto-state-gating.test.cjs` returns ≥2 matches (open + close marker assertions).
    - `grep -F ".oto/STATE.md" tests/06-using-oto-state-gating.test.cjs` returns ≥1 match.
    - `grep -F "BANNED_LITERALS" tests/06-using-oto-state-gating.test.cjs` returns ≥2 matches (declared + iterated).
    - `grep -F "You are using oto." tests/06-using-oto-state-gating.test.cjs` returns ≥1 match.
    - `grep -F "RECOGNIZED_STATUSES" tests/06-using-oto-state-gating.test.cjs` returns ≥2 matches.
    - `grep -F "execute_phase" tests/06-using-oto-state-gating.test.cjs` returns ≥1 match.
    - Running `node --test tests/06-using-oto-state-gating.test.cjs` exits non-zero (Wave 0 contract: blocks 1–3 fail because using-oto/SKILL.md does not exist).
    - File contains no `t.todo(` substring.
    - The literal substring `superpowers` does NOT appear in the test source EXCEPT inside the BANNED_LITERALS array (exactly 3 occurrences expected: 'superpowers', 'Superpowers', 'using-superpowers' as ARRAY ELEMENTS — verify by `grep -c "superpowers" tests/06-using-oto-state-gating.test.cjs` returns ≤ 6).
  </acceptance_criteria>
  <done>
    Test file plus fixture shipped, four live test blocks, banned-literals defense active, marker-comment anchor encoded, fails on current tree because using-oto/SKILL.md does not yet exist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → `oto/skills/` payload | Tests assert against committed payload; no untrusted input crosses |
| Test file → installer (`installRuntime`) | Tests invoke installer against repo-root + temp configDir; install logic runs with full filesystem access in temp dir only |
| Temp configDir → host filesystem | Each install test creates a fresh `os.tmpdir()/oto-test-skills-*` dir and removes it via `t.after()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (T-15) | tests/06-using-oto-state-gating.test.cjs banned-literal scan | mitigate | Loop asserts SKILL.md contains none of `superpowers`, `Superpowers`, `using-superpowers`, `You have superpowers`, `94% PR rejection`, `fabricated content`, `protect your human partner from PR rejection`. Failing test blocks Wave 1 hand-fix from drifting. |
| T-06-02 | Tampering (Pitfall E) | tests/06-using-oto-state-gating.test.cjs gating-directive marker | mitigate | Test asserts presence of BOTH `<!-- oto:state-gating-directive -->` AND closing marker AND `.oto/STATE.md` substring AND `suppress` substring. Future drift that paraphrases the directive away breaks the test. |
| T-06-03 | Repudiation | Test contract (red→green visible) | mitigate | All Wave 0 tests must FAIL on first run (assertions reference oto/skills/ that does not yet exist). Acceptance criteria explicitly require non-zero exit. Prevents "shipped but never tested" silent regressions. |
| T-06-04 | Information Disclosure | Tests reading source files | accept | Tests read public repo content via `fs.readFileSync`. No secrets; CI runs in same trust boundary. |
| T-06-05 | DoS | Temp-dir bloat from install test | mitigate | Each install test registers `t.after()` cleanup with `fs.rmSync({ recursive: true, force: true })`. Acceptance criterion checks `fs.rmSync` is wired. |
| T-06-06 | Elevation of Privilege | exec-bit on `find-polluter.sh` after copy | mitigate | Test 2 of installer-copy file asserts `(mode & 0o111) !== 0` after install. Catches a copy mechanism that strips the bit before users see the regression. |
| T-06-07 | Tampering | Test file itself contains banned literals (it must reference them in BANNED_LITERALS array) | accept | The strings appear ONLY as array literals in the banned-list, not in narrative prose. Acceptance criterion bounds total occurrence count. |
</threat_model>

<verification>
- All 3 test files exist and contain `'use strict'` + `require('node:test')`.
- Each test file fails on the current tree (oto/skills/ does not yet exist).
- Run `node scripts/run-tests.cjs --filter "06-"` and confirm 3 test files match the filter.
- Confirm no `t.todo(` calls anywhere — every block is a real test.
- The fixture file `tests/skills/__fixtures__/STATE-active.md` is well-formed (starts with frontmatter, contains `status: execute_phase`).
- The banned-literals array in the gating test contains the exact 7 strings listed in <action>.
</verification>

<success_criteria>
- [ ] `tests/06-skill-structure.test.cjs` exists with 7 live test() blocks covering D-07 (4 blocks) + SKL-08 axes 1–3 (3 blocks).
- [ ] `tests/06-installer-skill-copy.test.cjs` exists with 3 live test() blocks covering byte-identity copy, exec-bit preservation, install-state JSON marker.
- [ ] `tests/06-using-oto-state-gating.test.cjs` exists with 4 live test() blocks covering D-09 directive marker + Pitfall 15 banned literals + Phase 5 D-05 identity-sentence alignment + STATE fixture sanity check.
- [ ] `tests/skills/__fixtures__/STATE-active.md` exists with `status: execute_phase` frontmatter.
- [ ] All three test files exit non-zero on the current tree (Wave 0 red contract).
- [ ] No file uses `t.todo()` (`grep -rc "t\.todo(" tests/06-*.test.cjs` returns 0 across all three).
- [ ] All requirement IDs SKL-01..08 mapped to ≥1 assertion in the test corpus (SKL-01..06 covered by structure block 1+3, SKL-07 covered by structure blocks 1+2+3 plus all installer tests plus all gating tests, SKL-08 covered by structure blocks 4+5+6+7).
</success_criteria>

<output>
After completion, create `.planning/phases/06-skills-port-cross-system-integration/06-01-SUMMARY.md` documenting:
- Three test files created with line counts and test() block counts
- Initial test run output (all three failing, exact failure messages)
- Confirmation that Plan 02 / Plan 03 will turn each axis green
</output>
