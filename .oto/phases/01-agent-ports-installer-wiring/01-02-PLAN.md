---
phase: 01-agent-ports-installer-wiring
phase_number: 1
plan: 02
plan_id: 01-02
type: execute
wave: 2
depends_on: [01-01]
autonomous: true
requirements: [INST-01, INST-02]
requirements_addressed: [INST-01, INST-02]
files_modified:
  - oto/bin/install.js

must_haves:
  truths:
    - "CODEX_AGENT_SANDBOX in oto/bin/install.js contains an entry for oto-doc-classifier mapped to read-only"
    - "CODEX_AGENT_SANDBOX in oto/bin/install.js contains an entry for oto-eval-auditor mapped to read-only"
    - "CODEX_AGENT_SANDBOX in oto/bin/install.js contains an entry for oto-doc-synthesizer mapped to workspace-write"
    - "Running `oto install --claude --config-dir <tmp>` copies oto-doc-classifier.md, oto-doc-synthesizer.md, and oto-eval-auditor.md into <tmp>/agents/ (Phase 1 success criterion 1)"
    - "Running `oto install --codex --config-dir <tmp>` produces three per-agent .toml files under <tmp>/agents/ with sandbox_mode lines matching D-04 — read-only for classifier+auditor, workspace-write for synthesizer (Phase 1 success criterion 2)"
    - "Running `oto install --gemini --config-dir <tmp>` copies the three agent files into <tmp>/agents/ (Phase 1 success criterion 3)"
    - "Each agent is dispatchable in Claude Code via Task(subagent_type=\"oto-doc-classifier\" | \"oto-doc-synthesizer\" | \"oto-eval-auditor\") and the agent file frontmatter is valid (Phase 1 success criterion 4)"
  artifacts:
    - path: "oto/bin/install.js"
      provides: "Three new CODEX_AGENT_SANDBOX entries; per-runtime install paths now ship the three new agents"
      contains: "'oto-doc-classifier': 'read-only'"
  key_links:
    - from: "oto/bin/install.js CODEX_AGENT_SANDBOX (line ~26)"
      to: "generateCodexAgentToml (line ~2017)"
      via: "lookup by agent name to pick sandbox_mode; emits literal `sandbox_mode = \"<mode>\"` into <config_dir>/agents/<name>.toml"
      pattern: "CODEX_AGENT_SANDBOX\\[agentName\\]"
    - from: "oto/agents/oto-*.md (Plan 01 output)"
      to: "<config_dir>/agents/oto-*.md"
      via: "per-runtime install adapter copy step (Claude/Codex/Gemini)"
      pattern: "oto-doc-(classifier|synthesizer)|oto-eval-auditor"
---

<objective>
Wire the three Plan-01 agent files into the per-runtime installer by extending `CODEX_AGENT_SANDBOX` in `oto/bin/install.js` with three new entries (per D-05, D-09: no new code paths — sandbox map extension only). Then smoke-test all three runtime targets (`--claude`, `--codex`, `--gemini`) against an isolated `--config-dir` to confirm Phase 1 success criteria 1, 2, 3, and 4. Codex sandbox emission must match D-04 locks: read-only for classifier + auditor, workspace-write for synthesizer.

Purpose: Close Phase 1. After this plan, all three new agents are installable, dispatchable, and per-runtime parity-equivalent with the 23 retained agents.
Output: One source-file edit (sandbox map extension); three smoke-tested install paths confirming agent presence + sandbox shape; one Claude Code dispatch sanity check per agent.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@.planning/phases/01-agent-ports-installer-wiring/01-01-PLAN.md
@CLAUDE.md

# The installer is the only source file edited in this plan
@oto/bin/install.js

# Plan 01 outputs (now on disk in oto/agents/) — read frontmatter to confirm shape pre-install
@oto/agents/oto-doc-classifier.md
@oto/agents/oto-doc-synthesizer.md
@oto/agents/oto-eval-auditor.md

# Existing read-only-agent CODEX_AGENT_SANDBOX precedent (D-04)
@oto/agents/oto-plan-checker.md
@oto/agents/oto-integration-checker.md

<interfaces>
<!-- Current CODEX_AGENT_SANDBOX const (oto/bin/install.js lines 26-38) — read this exact block: -->

```javascript
const CODEX_AGENT_SANDBOX = {
  'oto-executor': 'workspace-write',
  'oto-planner': 'workspace-write',
  'oto-phase-researcher': 'workspace-write',
  'oto-project-researcher': 'workspace-write',
  'oto-research-synthesizer': 'workspace-write',
  'oto-verifier': 'workspace-write',
  'oto-codebase-mapper': 'workspace-write',
  'oto-roadmapper': 'workspace-write',
  'oto-debugger': 'workspace-write',
  'oto-plan-checker': 'read-only',
  'oto-integration-checker': 'read-only',
};
```

<!-- After edit (the THREE NEW LINES added — keep grouping consistent: -->
<!-- synthesizer goes with the workspace-write group, classifier+auditor with the read-only group): -->

```javascript
const CODEX_AGENT_SANDBOX = {
  'oto-executor': 'workspace-write',
  'oto-planner': 'workspace-write',
  'oto-phase-researcher': 'workspace-write',
  'oto-project-researcher': 'workspace-write',
  'oto-research-synthesizer': 'workspace-write',
  'oto-verifier': 'workspace-write',
  'oto-codebase-mapper': 'workspace-write',
  'oto-roadmapper': 'workspace-write',
  'oto-debugger': 'workspace-write',
  'oto-doc-synthesizer': 'workspace-write',
  'oto-plan-checker': 'read-only',
  'oto-integration-checker': 'read-only',
  'oto-doc-classifier': 'read-only',
  'oto-eval-auditor': 'read-only',
};
```

<!-- Deterministic Codex emission shape (confirmed by reading oto/bin/install.js:2017-2058, generateCodexAgentToml): -->
<!-- - Per-agent .toml file written to: <config_dir>/agents/<agentName>.toml -->
<!-- - First three lines of each .toml are: -->
<!--     name = "<resolved-name>" -->
<!--     description = "<resolved-description>" -->
<!--     sandbox_mode = "<read-only|workspace-write>"   (literal, double-quoted) -->
<!-- - There is NO `sandbox:` field added to agent markdown frontmatter — the installer does not rewrite agent markdown. -->
<!-- - There is no `config.toml` agent-sandbox struct used for sandbox_mode (config.toml carries other agent metadata via generateCodexConfigBlock, but the sandbox lock lives in the per-agent .toml). -->
<!-- - Conclusion: smoke gates MUST check the per-agent .toml at <config_dir>/agents/<name>.toml for the literal `sandbox_mode = "<mode>"` line. No alternative shapes. -->

<!-- Downstream readers of CODEX_AGENT_SANDBOX (no edits needed — they autoload by name): -->
<!-- - generateCodexAgentToml (oto/bin/install.js line 2017) — reads CODEX_AGENT_SANDBOX[agentName] || 'read-only' (silent default — co-located per-agent checks below guard against this default masking a missing-key bug) -->
<!-- - generateCodexConfigBlock (oto/bin/install.js ~line 2065) — emits agent struct entries -->
<!-- Per D-09: no other installer code paths need touching. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CODEX_AGENT_SANDBOX with three entries (one per new agent)</name>
  <files>oto/bin/install.js</files>
  <read_first>
    - oto/bin/install.js (lines 26-38 — the CODEX_AGENT_SANDBOX const)
    - oto/bin/install.js (lines 2010-2080 — generateCodexAgentToml + generateCodexConfigBlock; confirm they read from the const by name lookup, no other edits needed)
    - oto/agents/oto-plan-checker.md frontmatter (read-only convention reference)
    - oto/agents/oto-doc-classifier.md frontmatter (Plan 01 output — confirm name matches the key we add)
    - oto/agents/oto-doc-synthesizer.md frontmatter (Plan 01 output)
    - oto/agents/oto-eval-auditor.md frontmatter (Plan 01 output)
    - .planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md (D-04, D-05, D-09)
  </read_first>
  <action>
    Edit `oto/bin/install.js` at the `CODEX_AGENT_SANDBOX` const declaration (lines 26-38). Add exactly three new key/value entries:

    - `'oto-doc-synthesizer': 'workspace-write',` — insert after `'oto-debugger': 'workspace-write',` (grouped with the workspace-write block).
    - `'oto-doc-classifier': 'read-only',` — insert after `'oto-integration-checker': 'read-only',` (grouped with the read-only block).
    - `'oto-eval-auditor': 'read-only',` — insert after the new `'oto-doc-classifier'` line (alphabetically adjacent within the read-only block per D-05 convention).

    The final block should match the post-edit shape shown in the `<interfaces>` block of this plan. Use the Edit tool with a precise old/new replacement targeting the full const literal — do NOT do three separate single-line inserts (use one multi-line Edit so the diff is reviewable as a single coherent change).

    Per D-09: do NOT modify `generateCodexAgentToml`, `generateCodexConfigBlock`, or any per-runtime install adapter function. The const map is the only insertion point. The existing logic discovers agents by globbing `oto/agents/*.md`, so the three new files (created in Plan 01) are picked up automatically.

    Note on the silent default: `generateCodexAgentToml` at line 2018 reads `CODEX_AGENT_SANDBOX[agentName] || 'read-only'`. If any of the three new keys is mistyped or omitted, the agent's `.toml` will silently emit `sandbox_mode = "read-only"`. Task 2's verification uses per-agent .toml co-located checks to catch this — a broad `grep -rq workspace-write` over the smoke dir would still pass (matching other workspace-write agents like `oto-executor`) and mask the bug for the synthesizer.
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('oto/bin/install.js','utf8'); const m=src.match(/const CODEX_AGENT_SANDBOX = \{([\s\S]*?)\};/); if(!m) throw new Error('const not found'); const body=m[1]; const must=[[\"'oto-doc-classifier': 'read-only'\",'classifier'],[\"'oto-doc-synthesizer': 'workspace-write'\",'synthesizer'],[\"'oto-eval-auditor': 'read-only'\",'auditor']]; for(const [needle,label] of must){if(!body.includes(needle)){console.error('MISSING',label,needle);process.exit(1);}} console.log('sandbox map OK');" && node -c oto/bin/install.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'oto-doc-classifier': 'read-only'" oto/bin/install.js` exits 0
    - `grep -q "'oto-doc-synthesizer': 'workspace-write'" oto/bin/install.js` exits 0
    - `grep -q "'oto-eval-auditor': 'read-only'" oto/bin/install.js` exits 0
    - `node -c oto/bin/install.js` exits 0 (syntax-valid JavaScript after edit)
    - The three new keys appear inside the `CODEX_AGENT_SANDBOX = { ... }` literal (not elsewhere): the node script above verifies this by matching the regex and inspecting body
    - No other functions touched: `git diff oto/bin/install.js | grep '^+' | grep -v '^+++' | wc -l` ≤ 5 (three lines added, possibly a trailing comma adjustment)
  </acceptance_criteria>
  <done>
    `CODEX_AGENT_SANDBOX` contains three new entries with the exact sandbox modes locked by D-04. File parses as valid Node.js. No other installer code paths edited (D-09 satisfied).
  </done>
</task>

<task type="auto">
  <name>Task 2: Per-runtime install smoke against isolated --config-dir (Claude / Codex / Gemini); verify agent files present + Codex sandbox shape</name>
  <files>(no source file mutations; this task runs installer and inspects outputs)</files>
  <read_first>
    - oto/bin/install.js (CLI arg parsing — locate `--config-dir`, `--claude`, `--codex`, `--gemini` flag handling)
    - oto/bin/install.js (lines 2017-2058 — generateCodexAgentToml — confirm per-agent .toml shape: `name`, `description`, `sandbox_mode = "<mode>"` lines emitted to <config_dir>/agents/<name>.toml)
    - oto/agents/oto-doc-classifier.md, oto-doc-synthesizer.md, oto-eval-auditor.md (Plan 01 outputs — sanity-check they exist before invoking installer)
    - .planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md (D-04 — sandbox lock per agent; D-09 — per-runtime install discovery)
  </read_first>
  <action>
    Run three smoke invocations against three isolated temp directories. For each, confirm the three new agent files are present in the runtime's agents dir, and (for Codex) confirm the sandbox_mode line in each generated per-agent `.toml` matches D-04 locks.

    **Setup — Create three isolated temp dirs:**

    ```
    SMOKE_CLAUDE=$(mktemp -d /tmp/oto-smoke-claude.XXXXXX)
    SMOKE_CODEX=$(mktemp -d /tmp/oto-smoke-codex.XXXXXX)
    SMOKE_GEMINI=$(mktemp -d /tmp/oto-smoke-gemini.XXXXXX)
    ```

    **Part A — Claude smoke (Phase 1 success criterion 1):**

    ```
    node oto/bin/install.js --claude --config-dir "$SMOKE_CLAUDE"
    test -f "$SMOKE_CLAUDE/agents/oto-doc-classifier.md"
    test -f "$SMOKE_CLAUDE/agents/oto-doc-synthesizer.md"
    test -f "$SMOKE_CLAUDE/agents/oto-eval-auditor.md"
    ls "$SMOKE_CLAUDE/agents/" | wc -l   # should be 26 (23 retained + 3 new), match expected count
    ```

    **Part B — Codex smoke (Phase 1 success criterion 2 + D-04 sandbox shape):**

    The installer's `generateCodexAgentToml` (oto/bin/install.js:2017-2058) deterministically emits one `.toml` file per agent at `<config_dir>/agents/<agentName>.toml`, with `sandbox_mode = "<mode>"` as a literal line (the mode value is double-quoted). There is no alternative emission shape — no frontmatter `sandbox:` rewrite, no `config.toml` agent-sandbox struct for the lock. Verify against the deterministic per-agent .toml shape only:

    ```
    node oto/bin/install.js --codex --config-dir "$SMOKE_CODEX"

    # Step 1 — Agent markdown files copied:
    test -f "$SMOKE_CODEX/agents/oto-doc-classifier.md"
    test -f "$SMOKE_CODEX/agents/oto-doc-synthesizer.md"
    test -f "$SMOKE_CODEX/agents/oto-eval-auditor.md"

    # Step 2 — Per-agent .toml files exist (guards against silent CODEX_AGENT_SANDBOX[name] || 'read-only' default
    # masking a missing key — if the .toml file itself is absent, the agent was never wired for Codex):
    test -f "$SMOKE_CODEX/agents/oto-doc-classifier.toml"
    test -f "$SMOKE_CODEX/agents/oto-doc-synthesizer.toml"
    test -f "$SMOKE_CODEX/agents/oto-eval-auditor.toml"

    # Step 3 — sandbox_mode line matches D-04 locks, co-located per-agent (NOT a broad grep over the smoke dir —
    # that would let oto-executor's workspace-write line satisfy a synthesizer check and mask a missing-key bug):
    grep -q 'sandbox_mode = "read-only"'       "$SMOKE_CODEX/agents/oto-doc-classifier.toml"
    grep -q 'sandbox_mode = "workspace-write"' "$SMOKE_CODEX/agents/oto-doc-synthesizer.toml"
    grep -q 'sandbox_mode = "read-only"'       "$SMOKE_CODEX/agents/oto-eval-auditor.toml"
    ```

    If any of the three `sandbox_mode = "<mode>"` lines is missing from its co-located `.toml`, Task 1's `CODEX_AGENT_SANDBOX` entries did not reach the emission path (or were mistyped). Halt and re-inspect the const before re-running.

    **Part C — Gemini smoke (Phase 1 success criterion 3):**

    ```
    node oto/bin/install.js --gemini --config-dir "$SMOKE_GEMINI"
    test -f "$SMOKE_GEMINI/agents/oto-doc-classifier.md"
    test -f "$SMOKE_GEMINI/agents/oto-doc-synthesizer.md"
    test -f "$SMOKE_GEMINI/agents/oto-eval-auditor.md"
    ```

    **Part D — Claude Code Task dispatch sanity check (Phase 1 success criterion 4):**

    The success criterion is "dispatchable via `Task(subagent_type=...)` and emits canonical output shape". Full canonical-output verification belongs to Phase 2 (workflow wiring) and Phase 3 (test ports). For Phase 1, the dispatch-clean check is: frontmatter is parseable + agent file is in the runtime's agents/ dir + no startup error on dispatch.

    Run a frontmatter validation against each new agent file in the Claude smoke dir:

    ```
    for agent in oto-doc-classifier oto-doc-synthesizer oto-eval-auditor; do
      node -e "
        const fs=require('fs');
        const src=fs.readFileSync('$SMOKE_CLAUDE/agents/'+'$agent'+'.md','utf8');
        const m=src.match(/^---\n([\s\S]*?)\n---/);
        if(!m){console.error('NO FRONTMATTER',  '$agent'); process.exit(1);}
        if(!m[1].includes('name: '+ '$agent')){console.error('BAD NAME', '$agent'); process.exit(1);}
        if(!m[1].includes('tools: ')){console.error('NO TOOLS', '$agent'); process.exit(1);}
        if(!m[1].includes('description: ')){console.error('NO DESCRIPTION', '$agent'); process.exit(1);}
        console.log('OK '+'$agent');
      "
    done
    ```

    The actual `Task(subagent_type=...)` dispatch test runs implicitly in Phase 2 / Phase 3 when the workflows exercise these agents. For Phase 1, frontmatter + presence is the dispatchable contract.

    **Cleanup:** Leave the smoke dirs in place at end of run for human inspection if needed; do not `rm -rf` until SUMMARY notes confirm health. (The temp dirs auto-clean on reboot.)
  </action>
  <verify>
    <automated>SMOKE_CLAUDE=$(mktemp -d /tmp/oto-smoke-claude.XXXXXX); SMOKE_CODEX=$(mktemp -d /tmp/oto-smoke-codex.XXXXXX); SMOKE_GEMINI=$(mktemp -d /tmp/oto-smoke-gemini.XXXXXX); node oto/bin/install.js --claude --config-dir "$SMOKE_CLAUDE" >/dev/null 2>&1 && test -f "$SMOKE_CLAUDE/agents/oto-doc-classifier.md" && test -f "$SMOKE_CLAUDE/agents/oto-doc-synthesizer.md" && test -f "$SMOKE_CLAUDE/agents/oto-eval-auditor.md" && node oto/bin/install.js --codex --config-dir "$SMOKE_CODEX" >/dev/null 2>&1 && test -f "$SMOKE_CODEX/agents/oto-doc-classifier.md" && test -f "$SMOKE_CODEX/agents/oto-doc-synthesizer.md" && test -f "$SMOKE_CODEX/agents/oto-eval-auditor.md" && test -f "$SMOKE_CODEX/agents/oto-doc-classifier.toml" && test -f "$SMOKE_CODEX/agents/oto-doc-synthesizer.toml" && test -f "$SMOKE_CODEX/agents/oto-eval-auditor.toml" && grep -q 'sandbox_mode = "read-only"' "$SMOKE_CODEX/agents/oto-doc-classifier.toml" && grep -q 'sandbox_mode = "workspace-write"' "$SMOKE_CODEX/agents/oto-doc-synthesizer.toml" && grep -q 'sandbox_mode = "read-only"' "$SMOKE_CODEX/agents/oto-eval-auditor.toml" && node oto/bin/install.js --gemini --config-dir "$SMOKE_GEMINI" >/dev/null 2>&1 && test -f "$SMOKE_GEMINI/agents/oto-doc-classifier.md" && test -f "$SMOKE_GEMINI/agents/oto-doc-synthesizer.md" && test -f "$SMOKE_GEMINI/agents/oto-eval-auditor.md" && echo "smoke OK"</automated>
  </verify>
  <acceptance_criteria>
    - After `node oto/bin/install.js --claude --config-dir <tmp>`: `test -f <tmp>/agents/oto-doc-classifier.md && test -f <tmp>/agents/oto-doc-synthesizer.md && test -f <tmp>/agents/oto-eval-auditor.md` exits 0 (success criterion 1)
    - After `node oto/bin/install.js --codex --config-dir <tmp>`:
      - `test -f <tmp>/agents/oto-doc-classifier.toml && test -f <tmp>/agents/oto-doc-synthesizer.toml && test -f <tmp>/agents/oto-eval-auditor.toml` exits 0 (per-agent .toml emitted)
      - `grep -q 'sandbox_mode = "read-only"' <tmp>/agents/oto-doc-classifier.toml` exits 0
      - `grep -q 'sandbox_mode = "workspace-write"' <tmp>/agents/oto-doc-synthesizer.toml` exits 0
      - `grep -q 'sandbox_mode = "read-only"' <tmp>/agents/oto-eval-auditor.toml` exits 0
      (success criterion 2 + D-04 enforcement; co-located per-agent checks defeat the `CODEX_AGENT_SANDBOX[name] || 'read-only'` silent-default masking risk)
    - After `node oto/bin/install.js --gemini --config-dir <tmp>`: the three agent files exist in `<tmp>/agents/` (success criterion 3)
    - Frontmatter validity for each new agent in `<tmp>/agents/`: name, tools, description all present and parseable (success criterion 4 — dispatchable contract)
    - No installer error output (stderr empty) for any of the three runtime invocations
    - Smoke dirs scoped to `/tmp/oto-smoke-*` — no global side effects on `~/.claude/`, `~/.codex/`, `~/.gemini/`
  </acceptance_criteria>
  <done>
    All three runtimes (Claude, Codex, Gemini) install the three new agents into their respective `<config-dir>/agents/` paths. Codex install emits per-agent `.toml` files with `sandbox_mode` lines matching D-04 (read-only for classifier+auditor, workspace-write for synthesizer) — verified co-located per agent. Frontmatter is valid in each output (dispatchable in Claude Code via `Task(subagent_type=...)`). Phase 1 success criteria 1, 2, 3, 4 all satisfied. Combined with Plan 01's success criterion 5 (coverage manifest), Phase 1 is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer → user fs | `oto install --<runtime> --config-dir <dir>` writes to the given dir. In this plan, scoped to `/tmp/oto-smoke-*` — no `~/.claude`/`~/.codex`/`~/.gemini` mutation. |
| Codex sandbox emission | Codex installer emits `sandbox_mode` per agent — this is the runtime security gate enforcing D-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | CODEX_AGENT_SANDBOX map | mitigate | Task 1's literal key/value entries are reviewed against D-04 in the acceptance gate (exact strings `'oto-doc-classifier': 'read-only'`, `'oto-eval-auditor': 'read-only'`, `'oto-doc-synthesizer': 'workspace-write'` are grep-asserted). Task 2 adds co-located per-agent .toml verification to defeat the `CODEX_AGENT_SANDBOX[name] || 'read-only'` silent-default that could otherwise mask a missing-key bug (broad `grep -rq workspace-write` over the smoke dir would still pass via other workspace-write agents). |
| T-01-05 | Tampering | smoke install paths | mitigate | All smoke runs use `--config-dir /tmp/oto-smoke-*` — no global installer side effects. Acceptance gate asserts files land under `/tmp/oto-smoke-*` only. |
| T-01-06 | Information Disclosure | agent frontmatter post-install | accept | Agent frontmatter is public-by-design; no secrets surface. |

Note: No new auth/network/secrets surface in this plan. The sandbox declarations Task 1 wires (per D-04) are the security gate for the new agents — read-only for the two non-writing agents, workspace-write for the one that writes `.oto/INGEST-CONFLICTS.md` during its own run (synthesizer). Per-runtime smoke confirms emission matches lock via co-located per-agent .toml grep — not a broad smoke-dir grep that the silent default could mask.
</threat_model>

<verification>
- `CODEX_AGENT_SANDBOX` in `oto/bin/install.js` contains the three new entries with D-04-correct sandbox modes.
- `oto install --claude --config-dir <tmp>` produces the three agent files in `<tmp>/agents/`.
- `oto install --codex --config-dir <tmp>` produces the three agent files AND three per-agent `.toml` files at `<tmp>/agents/<name>.toml`, each containing the D-04-correct literal `sandbox_mode = "<mode>"` line (co-located per agent — read-only for classifier + auditor; workspace-write for synthesizer).
- `oto install --gemini --config-dir <tmp>` produces the three agent files in `<tmp>/agents/`.
- All three agent files have valid frontmatter (name, tools, description present) — dispatch-clean under Claude Code's Task subagent path.
- No global install side effects (`~/.claude`, `~/.codex`, `~/.gemini` untouched).
- All five Phase 1 success criteria satisfied across Plans 01 + 02.
</verification>

<success_criteria>
1. `oto/bin/install.js` `CODEX_AGENT_SANDBOX` has three new entries — exact strings: `'oto-doc-classifier': 'read-only'`, `'oto-doc-synthesizer': 'workspace-write'`, `'oto-eval-auditor': 'read-only'`.
2. `oto install --claude --config-dir <tmp>` ships all three agents to `<tmp>/agents/` (Phase 1 success criterion 1).
3. `oto install --codex --config-dir <tmp>` ships all three agents AND emits three per-agent `.toml` files at `<tmp>/agents/<name>.toml` with the D-04-correct `sandbox_mode = "<mode>"` line co-located in each (Phase 1 success criterion 2).
4. `oto install --gemini --config-dir <tmp>` ships all three agents to `<tmp>/agents/` (Phase 1 success criterion 3).
5. Each agent's installed frontmatter is parseable + name-matched + tools-line present (Phase 1 success criterion 4 — dispatchable contract; full canonical-output check deferred to Phase 2/3).
6. No global install side effects — smoke runs confined to `/tmp/oto-smoke-*` temp dirs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-ports-installer-wiring/01-02-SUMMARY.md` capturing:
- Exact three lines added to `CODEX_AGENT_SANDBOX` (with surrounding context lines)
- Smoke-dir paths and the actual file counts under each `<smoke>/agents/`
- The three per-agent `.toml` paths under `$SMOKE_CODEX/agents/` and the exact `sandbox_mode = "<mode>"` line extracted from each (co-located confirmation of D-04 enforcement)
- Confirmation that none of `~/.claude/`, `~/.codex/`, `~/.gemini/` were touched during the smoke
- Phase 1 closeout note: which of the five phase success criteria each plan addressed
</output>
</content>
</invoke>