---
phase: 01-agent-ports-installer-wiring
phase_number: 1
plan: 01
plan_id: 01-01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03]
requirements_addressed: [AGNT-01, AGNT-02, AGNT-03]
files_modified:
  - decisions/file-inventory.json
  - decisions/agent-audit.md
  - oto/agents/oto-doc-classifier.md
  - oto/agents/oto-doc-synthesizer.md
  - oto/agents/oto-eval-auditor.md
  - oto/references/doc-conflict-engine.md
  - reports/rebrand-dryrun.json
  - reports/rebrand-dryrun.md

must_haves:
  truths:
    - "oto/agents/oto-doc-classifier.md exists with frontmatter name: oto-doc-classifier, tools list excludes Write (per D-04), color: yellow preserved"
    - "oto/agents/oto-doc-synthesizer.md exists with frontmatter name: oto-doc-synthesizer, tools include Write (synthesizer keeps Write per D-04), color: orange preserved"
    - "oto/agents/oto-eval-auditor.md exists with frontmatter name: oto-eval-auditor, tools list excludes Write (per D-04), color: \"#EF4444\" preserved"
    - "oto/references/doc-conflict-engine.md exists (synthesizer's required_reading per D-03; absence hard-fails AGNT-02 dispatch)"
    - "decisions/file-inventory.json shows verdict: keep + target_path + rebrand_required: true + phase_owner: 1 for the three agent rows"
    - "decisions/agent-audit.md verdict counts read KEEP: 26 / DROP: 7, and the three agent rows show KEEP"
    - "reports/rebrand-dryrun.json reports zero orphan upstream paths and zero orphan target paths after regeneration (Phase 1 success criterion 5)"
  artifacts:
    - path: "oto/agents/oto-doc-classifier.md"
      provides: "Rebrand-ported doc classifier agent (AGNT-01)"
      contains: "name: oto-doc-classifier"
    - path: "oto/agents/oto-doc-synthesizer.md"
      provides: "Rebrand-ported doc synthesizer agent (AGNT-02)"
      contains: "name: oto-doc-synthesizer"
    - path: "oto/agents/oto-eval-auditor.md"
      provides: "Rebrand-ported eval auditor agent (AGNT-03)"
      contains: "name: oto-eval-auditor"
    - path: "oto/references/doc-conflict-engine.md"
      provides: "Synthesizer required_reading (D-03) — without this, AGNT-02 dispatch hard-fails"
      contains: "conflict"
    - path: "decisions/file-inventory.json"
      provides: "Three flipped rows (drop -> keep) per D-06"
      contains: "agents/oto-doc-classifier.md"
    - path: "decisions/agent-audit.md"
      provides: "Updated verdict counts (KEEP: 26, DROP: 7) and three flipped rows"
      contains: "KEEP: 26"
    - path: "reports/rebrand-dryrun.json"
      provides: "Coverage manifest regenerated with three new target paths"
      contains: "oto-doc-classifier.md"
  key_links:
    - from: "decisions/file-inventory.json"
      to: "oto/agents/oto-*.md"
      via: "scripts/rebrand.cjs reads inventory, applies rename-map, writes to target_path"
      pattern: "\"target_path\": \"agents/oto-doc-(classifier|synthesizer)|oto-eval-auditor"
    - from: "rebrand engine run"
      to: "reports/rebrand-dryrun.json"
      via: "manifest.cjs regenerates after engine apply"
      pattern: "oto-doc-classifier"
---

<objective>
Port three upstream GSD agents (`gsd-doc-classifier`, `gsd-doc-synthesizer`, `gsd-eval-auditor`) into `oto/agents/` through the existing rebrand engine, flipping their `decisions/file-inventory.json` rows from `drop` to `keep`, updating `decisions/agent-audit.md` verdict counts, and regenerating the coverage manifest. Reconcile tool lists with INST-02 sandbox locks (per D-04) so the classifier and auditor drop `Write` from their tools strings while the synthesizer keeps it. No hand-porting — the rebrand engine remains the single source of truth (D-01).

Purpose: Phase 1 foundation. Plan 02 wires these files into the installer; nothing can be wired until they exist on disk in `oto/agents/`.
Output: Three new agent files in `oto/agents/`, one reference file at `oto/references/doc-conflict-engine.md` (D-03), two updated decisions files, and a regenerated coverage manifest with zero orphan paths.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@CLAUDE.md

# Source files (read these — do NOT hand-port; rebrand engine handles tokens)
@foundation-frameworks/get-shit-done-main/agents/gsd-doc-classifier.md
@foundation-frameworks/get-shit-done-main/agents/gsd-doc-synthesizer.md
@foundation-frameworks/get-shit-done-main/agents/gsd-eval-auditor.md

# Read-only-agent tool-list precedent (D-04 reconciliation reference)
@oto/agents/oto-plan-checker.md
@oto/agents/oto-integration-checker.md

# Canonical keep-row shape for inventory edits (D-06)
@decisions/file-inventory.json
@decisions/agent-audit.md

# Rebrand engine entry points
@scripts/rebrand.cjs
@scripts/rebrand/lib/engine.cjs
@scripts/rebrand/lib/manifest.cjs
@rename-map.json
@decisions/sync-allowlist.json

<interfaces>
<!-- Canonical "keep" row shape from file-inventory.json (line 488-507, gsd-doc-writer entry). -->
<!-- Apply this shape to the three flipped agent rows. -->

```json
{
  "path": "agents/gsd-doc-classifier.md",
  "upstream": "gsd",
  "verdict": "keep",
  "reason": "v0.3.0 Phase 1 agent port — partial ADR-07 reversal",
  "target_path": "agents/oto-doc-classifier.md",
  "rebrand_required": true,
  "phase_owner": 1,
  "category": "agent"
}
```

<!-- Read-only-agent tool list convention (D-04). Classifier + auditor mirror these: -->
<!-- - oto-plan-checker tools: Read, Bash, Glob, Grep (no Write) -->
<!-- - oto-integration-checker tools: Read, Bash, Glob, Grep (no Write) -->

<!-- Upstream tools-line locations to be modified during rebrand: -->
<!-- - gsd-doc-classifier.md:4: "tools: Read, Write, Grep, Glob" -> target should be "tools: Read, Grep, Glob" (drop Write) -->
<!-- - gsd-doc-synthesizer.md:4: "tools: Read, Write, Grep, Glob, Bash" -> KEEP verbatim (synthesizer is workspace-write) -->
<!-- - gsd-eval-auditor.md:4: "tools: Read, Write, Bash, Grep, Glob" -> target should be "tools: Read, Bash, Grep, Glob" (drop Write) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip three inventory rows from drop to keep + update agent-audit.md verdict counts</name>
  <files>decisions/file-inventory.json, decisions/agent-audit.md</files>
  <read_first>
    - decisions/file-inventory.json (lines 460-545 — the three target rows and the canonical keep-shape neighbors)
    - decisions/agent-audit.md (full file — verdict counts at lines 14-18, per-agent rows at lines 22-57)
    - .planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md (D-06, D-07 — reason text and ADR-15 omission)
  </read_first>
  <action>
    **Part A — file-inventory.json:** Edit the three drop rows (gsd-doc-classifier.md at line ~470, gsd-doc-synthesizer.md at line ~479, gsd-eval-auditor.md at line ~518) to match the canonical keep shape. For each row, replace the existing object body with this exact shape (substituting the agent slug):

    ```json
    {
      "path": "agents/gsd-doc-classifier.md",
      "upstream": "gsd",
      "verdict": "keep",
      "reason": "v0.3.0 Phase 1 agent port — partial ADR-07 reversal",
      "target_path": "agents/oto-doc-classifier.md",
      "rebrand_required": true,
      "phase_owner": 1,
      "category": "agent"
    }
    ```

    Use the slug `oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor` respectively. Do NOT add an ADR-15 reference (per D-07 — Phase 3 owns ADR-15).

    **Part B — agent-audit.md:**
    1. Update verdict-count block (lines 14-18):
       - Change `- KEEP: 23` → `- KEEP: 26`
       - Change `- DROP: 10` → `- DROP: 7`
       - Leave `MERGE: 0` and `Total: 33` unchanged.
    2. Update per-agent table rows (lines 32, 33, 37) to flip verdicts and rewrite rationale:
       - `gsd-doc-classifier` row: change `DROP | redundant doc | Consolidated into oto-doc-writer per ADR-07.` → `KEEP | phase spine | Restored in v0.3.0 Phase 1 — powers /oto-ingest-docs classification arm (partial ADR-07 reversal). Rebrands to oto-doc-classifier.`
       - `gsd-doc-synthesizer` row: change `DROP | redundant doc | Consolidated into oto-doc-writer per ADR-07.` → `KEEP | phase spine | Restored in v0.3.0 Phase 1 — powers /oto-ingest-docs synthesis arm (partial ADR-07 reversal). Rebrands to oto-doc-synthesizer.`
       - `gsd-eval-auditor` row: change `DROP | AI/eval | AI/eval workflow deferred.` → `KEEP | phase spine | Restored in v0.3.0 Phase 1 — powers /oto-eval-review (partial ADR-07 reversal). Rebrands to oto-eval-auditor.`

    Use the Edit tool (not Write) for both files to preserve surrounding content.
  </action>
  <verify>
    <automated>node -e "const j=require('./decisions/file-inventory.json');const slugs=['agents/gsd-doc-classifier.md','agents/gsd-doc-synthesizer.md','agents/gsd-eval-auditor.md'];for(const s of slugs){const e=j.entries.find(x=>x.path===s);if(!e||e.verdict!=='keep'||!e.target_path||e.rebrand_required!==true||e.phase_owner!==1){console.error('FAIL',s,e);process.exit(1);}}console.log('inventory OK');" && grep -q "^- KEEP: 26$" decisions/agent-audit.md && grep -q "^- DROP: 7$" decisions/agent-audit.md && grep -q "gsd-doc-classifier\` | KEEP" decisions/agent-audit.md && grep -q "gsd-doc-synthesizer\` | KEEP" decisions/agent-audit.md && grep -q "gsd-eval-auditor\` | KEEP" decisions/agent-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const j=require('./decisions/file-inventory.json'); ['agents/gsd-doc-classifier.md','agents/gsd-doc-synthesizer.md','agents/gsd-eval-auditor.md'].forEach(p=>{const e=j.entries.find(x=>x.path===p); if(e.verdict!=='keep'||e.target_path!==p.replace('gsd-','oto-')||e.phase_owner!==1) throw new Error(p);});"` exits 0
    - `grep -c '^- KEEP: 26$' decisions/agent-audit.md` equals `1`
    - `grep -c '^- DROP: 7$' decisions/agent-audit.md` equals `1`
    - All three per-agent rows in agent-audit.md show `KEEP` and the rationale string contains "v0.3.0 Phase 1"
    - No occurrence of `ADR-15` anywhere in either edited file (per D-07): `grep -c ADR-15 decisions/agent-audit.md decisions/file-inventory.json` equals `0`
  </acceptance_criteria>
  <done>
    Three inventory rows show verdict: keep with correct target_path, rebrand_required: true, phase_owner: 1, and the "v0.3.0 Phase 1 agent port" reason string. agent-audit.md verdict counts read KEEP: 26 / DROP: 7 and the three per-agent rows have been re-classified to KEEP with restoration rationale.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run rebrand engine, verify three agent files land in oto/agents/, regenerate coverage manifest, reconcile tool lists per D-04, confirm doc-conflict-engine.md present per D-03</name>
  <files>oto/agents/oto-doc-classifier.md, oto/agents/oto-doc-synthesizer.md, oto/agents/oto-eval-auditor.md, oto/references/doc-conflict-engine.md, reports/rebrand-dryrun.json, reports/rebrand-dryrun.md</files>
  <read_first>
    - foundation-frameworks/get-shit-done-main/agents/gsd-doc-classifier.md (full file — upstream source; LOC ~168)
    - foundation-frameworks/get-shit-done-main/agents/gsd-doc-synthesizer.md (full file — upstream source; LOC ~204)
    - foundation-frameworks/get-shit-done-main/agents/gsd-eval-auditor.md (full file — upstream source; LOC ~191)
    - oto/agents/oto-plan-checker.md (frontmatter only — read-only agent tools-line precedent for D-04)
    - oto/agents/oto-integration-checker.md (frontmatter only — read-only agent tools-line precedent for D-04)
    - scripts/rebrand.cjs (entry point; understand --apply / --dry-run flags)
    - package.json scripts block (lines 26-28 — `rebrand`, `rebrand:dry-run` aliases)
    - decisions/sync-allowlist.json (verify the new agents need no allowlist entries; D-08 expects pass without edits)
  </read_first>
  <action>
    **Part A — Run the rebrand engine in apply mode** to produce the three target files. Invoke:

    ```
    npm run rebrand
    ```

    (resolves to `node scripts/rebrand.cjs --apply --force`). This reads `decisions/file-inventory.json` (now flipped in Task 1), applies `rename-map.json` token substitutions (`gsd-` → `oto-`, `.planning/` → `.oto/`, etc.), and writes the three target files to `oto/agents/`. If the engine errors with allowlist or content gates, halt and surface the engine output; do NOT hand-edit to bypass (D-01: rebrand engine is single source of truth).

    **Part B — Reconcile tool lists per D-04.** The rebrand engine does token substitution, not semantic edits. After it runs, the frontmatter `tools:` lines need adjustment in two of the three agents (the synthesizer keeps Write verbatim — workspace-write sandbox):

    - `oto/agents/oto-doc-classifier.md` — change the frontmatter line from `tools: Read, Write, Grep, Glob` → `tools: Read, Grep, Glob` (drop `Write, `). Mirrors `oto-plan-checker`'s tools shape.
    - `oto/agents/oto-eval-auditor.md` — change the frontmatter line from `tools: Read, Write, Bash, Grep, Glob` → `tools: Read, Bash, Grep, Glob` (drop `Write, `). Mirrors `oto-integration-checker`'s tools shape.
    - `oto/agents/oto-doc-synthesizer.md` — leave `tools: Read, Write, Grep, Glob, Bash` UNCHANGED (this is the only agent of the three with workspace-write sandbox; D-04 keeps Write here).

    Use the Edit tool with the exact old/new strings above. Preserve every other frontmatter field (name, description, color) verbatim — the rebrand engine already handled `gsd-` → `oto-` substitutions in name/description.

    **Part C — Regenerate the coverage manifest** in dry-run mode to confirm zero orphans:

    ```
    npm run rebrand:dry-run
    ```

    This runs `node scripts/rebrand.cjs --dry-run` and re-emits `reports/rebrand-dryrun.json` + `reports/rebrand-dryrun.md`. The dry-run output must show: every flipped inventory row resolves to an existing target file in `oto/agents/`, and every target file traces back to its inventory `target_path` (no orphan upstream, no orphan target).

    Per D-08: zero orphan upstream paths AND zero orphan target paths is the Phase 1 success criterion 5 gate. If the manifest reports any orphans involving the three new agents, halt — the rebrand engine output is inconsistent with the inventory edit and needs investigation, not a hand-fix.

    **Part D — Validate frontmatter integrity.** Confirm each new agent file has the canonical color field preserved (D-02: yellow / orange / `"#EF4444"`) and the name field is rebranded (`gsd-` → `oto-`).

    **Part E — Confirm D-03 required_reading port: `oto/references/doc-conflict-engine.md` is present.** The synthesizer's `<required_reading>` cites this file; if absent, AGNT-02 dispatch hard-fails. Run:

    ```
    test -f oto/references/doc-conflict-engine.md
    ```

    Expected state at execution time: the file is already present in the repo (ported in an earlier session). If `test -f` returns non-zero (file missing), the executor MUST halt and port it via the rebrand engine — DO NOT hand-copy. Steps to recover via the engine:

    1. Confirm `decisions/file-inventory.json` has a `keep`-verdict row for `get-shit-done/references/doc-conflict-engine.md` with `target_path: "references/doc-conflict-engine.md"`, `rebrand_required: true`, `phase_owner: 1`. If absent, add it using the canonical keep shape (mirror the agent keep shape from the `<interfaces>` block; category becomes `reference`).
    2. Re-run `npm run rebrand` (Part A). The engine will pick up the new keep row and emit `oto/references/doc-conflict-engine.md` from `foundation-frameworks/get-shit-done-main/get-shit-done/references/doc-conflict-engine.md`.
    3. Re-run `npm run rebrand:dry-run` (Part C). Manifest must show zero orphans including the reference port.

    This is a guard, not a rebuild — the file is expected to already exist. The gate ensures Phase 1 cannot pass while D-03's required_reading is unmet.
  </action>
  <verify>
    <automated>test -f oto/agents/oto-doc-classifier.md && test -f oto/agents/oto-doc-synthesizer.md && test -f oto/agents/oto-eval-auditor.md && test -f oto/references/doc-conflict-engine.md && grep -q "^name: oto-doc-classifier$" oto/agents/oto-doc-classifier.md && grep -q "^name: oto-doc-synthesizer$" oto/agents/oto-doc-synthesizer.md && grep -q "^name: oto-eval-auditor$" oto/agents/oto-eval-auditor.md && grep -q "^color: yellow$" oto/agents/oto-doc-classifier.md && grep -q "^color: orange$" oto/agents/oto-doc-synthesizer.md && grep -q '^color: "#EF4444"$' oto/agents/oto-eval-auditor.md && grep -q "^tools: Read, Grep, Glob$" oto/agents/oto-doc-classifier.md && grep -q "^tools: Read, Bash, Grep, Glob$" oto/agents/oto-eval-auditor.md && grep -q "^tools: Read, Write, Grep, Glob, Bash$" oto/agents/oto-doc-synthesizer.md && ! grep -q "Write" <(sed -n '/^---$/,/^---$/p' oto/agents/oto-doc-classifier.md | grep '^tools:') && ! grep -q "Write" <(sed -n '/^---$/,/^---$/p' oto/agents/oto-eval-auditor.md | grep '^tools:') && test -f reports/rebrand-dryrun.json && node -e "const r=require('./reports/rebrand-dryrun.json'); const s=JSON.stringify(r); if(!s.includes('oto-doc-classifier')||!s.includes('oto-doc-synthesizer')||!s.includes('oto-eval-auditor')){console.error('manifest missing new agents');process.exit(1);}"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f oto/agents/oto-doc-classifier.md && test -f oto/agents/oto-doc-synthesizer.md && test -f oto/agents/oto-eval-auditor.md` exits 0
    - `test -f oto/references/doc-conflict-engine.md` exits 0 (synthesizer required_reading present per D-03)
    - `grep -q "^name: oto-doc-classifier$" oto/agents/oto-doc-classifier.md` (and analogous for synthesizer + auditor) exits 0
    - `grep -q "^color: yellow$" oto/agents/oto-doc-classifier.md` exits 0
    - `grep -q "^color: orange$" oto/agents/oto-doc-synthesizer.md` exits 0
    - `grep -q '^color: "#EF4444"$' oto/agents/oto-eval-auditor.md` exits 0
    - `grep -q "^tools: Read, Grep, Glob$" oto/agents/oto-doc-classifier.md` exits 0 (Write dropped per D-04)
    - `grep -q "^tools: Read, Bash, Grep, Glob$" oto/agents/oto-eval-auditor.md` exits 0 (Write dropped per D-04)
    - `grep -q "^tools: Read, Write, Grep, Glob, Bash$" oto/agents/oto-doc-synthesizer.md` exits 0 (Write kept per D-04)
    - In the classifier/auditor frontmatter blocks (between the two `---` lines), the tools line does NOT contain the substring `Write`
    - `reports/rebrand-dryrun.json` and `reports/rebrand-dryrun.md` regenerated and contain the substring `oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor`
    - No occurrence of `gsd-doc-classifier`, `gsd-doc-synthesizer`, or `gsd-eval-auditor` in any new `oto/agents/oto-*.md` file (the rebrand engine swapped all tokens): `grep -l "gsd-" oto/agents/oto-doc-classifier.md oto/agents/oto-doc-synthesizer.md oto/agents/oto-eval-auditor.md` returns nothing
  </acceptance_criteria>
  <done>
    All three new agent files exist in `oto/agents/` with rebranded names, preserved colors, and D-04-reconciled tool lists. `oto/references/doc-conflict-engine.md` is present (D-03 — synthesizer's required_reading). Coverage manifest regenerated with the new target paths recognized. No orphan upstream/target paths. AGNT-01, AGNT-02, AGNT-03 file-port requirements satisfied for Plan 02 to consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo only | All mutations are to files inside the project root; no network, no user data, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | decisions/file-inventory.json | mitigate | Sandbox declarations per D-04 (tool list reconciliation) are the security gate — agent CAN'T write outside its declared sandbox at runtime. JSON shape validated by Task 1 acceptance gate (`node -e` script). |
| T-01-02 | Information Disclosure | oto/agents/oto-*.md frontmatter | accept | Agent files are public-by-design (shipped in `npm install -g`); no secrets surface. |
| T-01-03 | Elevation of Privilege | Codex sandbox assignment | mitigate | Plan 02 wires the sandbox map. This plan only ensures tools-line shape matches the locked sandbox intent (D-04) so Plan 02's wiring is internally consistent. |

Note: No new auth/network/secrets surface in this plan. Per CONTEXT.md security threat model: the sandbox declarations finalized in Plan 02 (depending on Plan 01's tool-list reconciliation) are the security gate for the new agents.
</threat_model>

<verification>
- All three agent files exist at the canonical target paths under `oto/agents/`.
- `oto/references/doc-conflict-engine.md` is present (D-03 synthesizer required_reading gate).
- All three inventory rows show `verdict: keep` with the correct `target_path`, `rebrand_required: true`, `phase_owner: 1`, and the "v0.3.0 Phase 1 agent port" reason.
- `decisions/agent-audit.md` verdict counts: KEEP: 26 / DROP: 7. Three per-agent table rows flipped to KEEP.
- Coverage manifest (`reports/rebrand-dryrun.json` and `.md`) regenerated; references the three new target paths; zero orphan paths.
- Frontmatter tool lists reconcile with D-04 sandbox semantics (classifier + auditor drop Write; synthesizer keeps it).
- Phase 1 success criterion 5 is satisfied by the regenerated coverage manifest.
</verification>

<success_criteria>
1. `oto/agents/oto-doc-classifier.md`, `oto/agents/oto-doc-synthesizer.md`, `oto/agents/oto-eval-auditor.md` exist with rebranded `name:` fields, preserved color values, and D-04-reconciled `tools:` lines.
2. `oto/references/doc-conflict-engine.md` exists (D-03 — synthesizer's required_reading; absence hard-fails AGNT-02 dispatch).
3. `decisions/file-inventory.json` shows three flipped rows (drop → keep) matching the canonical keep shape.
4. `decisions/agent-audit.md` shows verdict counts KEEP: 26 / DROP: 7 and three KEEP-rewritten per-agent rows.
5. `reports/rebrand-dryrun.json` and `reports/rebrand-dryrun.md` regenerated post-engine-run; reference the three new agents; zero orphan paths (Phase 1 success criterion 5).
6. No `gsd-doc-classifier`, `gsd-doc-synthesizer`, `gsd-eval-auditor` tokens remain in the new agent files (rebrand engine swapped them).
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-ports-installer-wiring/01-01-SUMMARY.md` capturing:
- Which three inventory rows flipped (with line numbers)
- Which rebrand-engine invocation produced the target files (apply mode, command used)
- Tool-list reconciliation edits made post-rebrand (classifier + auditor — Write dropped)
- D-03 reference port confirmation (`oto/references/doc-conflict-engine.md` present; whether re-port via engine was needed)
- Coverage manifest delta (orphan count before/after)
- Any allowlist or rename-map edits needed (expected: none — per D-08)
</output>
</content>
</invoke>