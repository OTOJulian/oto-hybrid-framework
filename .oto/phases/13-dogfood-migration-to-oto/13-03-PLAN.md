---
phase: 13-dogfood-migration-to-oto
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - oto/templates/instruction-file.md
  - CLAUDE.md
  - AGENTS.md
  - GEMINI.md
  - decisions/ADR-01-state-root.md
  - .oto/STATE.md
  - .oto/ROADMAP.md
  - .oto/REQUIREMENTS.md
  - .oto/PROJECT.md
  - .oto/MILESTONES.md
autonomous: false
requirements: [DOG-03]
must_haves:
  truths:
    - "CLAUDE.md (and AGENTS.md, GEMINI.md) point at /oto-* entry points, not /gsd-*"
    - "Every genuine live self-reference to .planning/ on the enumerated allowlist is rewritten to .oto/"
    - "ADR-01-state-root.md carries a single forward-note that this repo's root is now .oto/ (D-11)"
    - "Active live-artifact path citations that point at moved files now resolve to .oto/ (D-10)"
    - "Frozen surfaces still say .planning/: foundation-frameworks/, the resolver fallback, migrate.cjs, rename-map.json, reports/, fixtures, sdk/ tooling, completed-phase dirs"
  artifacts:
    - path: "oto/templates/instruction-file.md"
      provides: "OTO Workflow Enforcement block (source of CLAUDE/AGENTS/GEMINI generation)"
      contains: "OTO Workflow Enforcement"
    - path: "CLAUDE.md"
      provides: "Rendered instruction file with /oto-* enforcement"
      contains: "oto-quick"
    - path: "decisions/ADR-01-state-root.md"
      provides: "Forward-note breadcrumb (D-11)"
      contains: ".oto/"
  key_links:
    - from: "oto/templates/instruction-file.md"
      to: "CLAUDE.md / AGENTS.md / GEMINI.md"
      via: "scripts/render-instruction-files.cjs"
      pattern: "OTO Workflow Enforcement"
---

<objective>
Update every GENUINE live self-reference in this repo so nothing points at the dead `.planning/` location, and flip the daily-flow enforcement from `/gsd-*` to `/oto-*` — the literal point of dogfooding (DOG-03, D-07). This is governed by an ALLOWLIST of real operational self-references (D-03/D-05): rewrite ONLY the enumerated entries below; everything else stays `.planning/` by default. Frozen surfaces (D-04) are protected by grep-verifiable assertions.

This plan runs AFTER the move (Plan 13-02), so all planning artifacts are at `.oto/...`. Read plan files from `.oto/phases/13-dogfood-migration-to-oto/`.

This plan is `autonomous: false`: the D-10 live-path-citation rewrite requires per-line judgment (distinguish *path citations an agent will open* from *prose that legitimately discusses the `.planning/` resolver-fallback contract*). A human reviews the allowlist diff before trust, because an over-broad find-replace here is the highest-blast-radius failure mode in the phase (corrupting the frozen resolver-contract prose or the upstream baseline).

Purpose: End the GSD/oto split-brain so this repo self-manages via `/oto-*` (DOG-03). After this plan, all remaining and future work runs through `/oto-*` commands.
Output: Re-rendered instruction files, an ADR forward-note, and surgically-rewritten live path citations — with frozen surfaces provably untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.oto/PROJECT.md
@.oto/ROADMAP.md
@.oto/STATE.md
@.oto/phases/13-dogfood-migration-to-oto/13-CONTEXT.md
@oto/templates/instruction-file.md
@decisions/ADR-01-state-root.md

<interfaces>
CLAUDE.md / AGENTS.md / GEMINI.md are GENERATED, not hand-edited. The top line of each says:
`<!-- Generated by scripts/render-instruction-files.cjs from oto/templates/instruction-file.md. Do not edit. -->`

Generation pipeline (read for understanding — DO NOT change the renderer):
- Source template: `oto/templates/instruction-file.md` (the "GSD Workflow Enforcement" block lives at ~line 203, inside `<!-- GSD:workflow-start --> ... <!-- GSD:workflow-end -->`)
- Renderer: `scripts/render-instruction-files.cjs` → calls `render(runtime, ctx)` from `bin/lib/instruction-file.cjs` → writes CLAUDE.md (claude), AGENTS.md (codex), GEMINI.md (gemini)
- Therefore: edit the TEMPLATE, then RUN the renderer; do NOT hand-edit the three generated files.
- Re-render command: `node scripts/render-instruction-files.cjs`
</interfaces>

<allowlist>
## THE ALLOWLIST (D-03/D-05) — rewrite ONLY these; everything else stays `.planning/`

Each entry is a GENUINE live self-reference (a file this repo's daily operation reads that names this repo's own state path or daily-flow commands). Justification per entry:

1. **`oto/templates/instruction-file.md`** — the "GSD Workflow Enforcement" block + `/gsd-*` entry points → "OTO Workflow Enforcement" + `/oto-*`. JUSTIFY: this is the source-of-truth for this repo's own instruction files; flipping it is the dogfood (D-07). (Note: this template currently contains `/gsd-*`, NOT `.planning/` path strings — the rewrite here is command-surface, not path.)
2. **`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`** — regenerated from #1 via the renderer. JUSTIFY: the live instruction files this repo's runtimes read at session start.
3. **`decisions/ADR-01-state-root.md`** — append ONE forward-note line (D-11). JUSTIFY: the canonical record of the state-root decision; deserves a breadcrumb. Exception to the "preserve ADRs as point-in-time records" rule (D-10), explicitly carved out by D-11. Do NOT rewrite its existing body `.planning/` strings — only ADD the note.
4. **Live-artifact ACTIVE PATH CITATIONS (D-10), surgically, in:**
   - `.oto/STATE.md` — rewrite path-citation lines that point at MOVED files an agent will open: `See: .planning/PROJECT.md` → `.oto/PROJECT.md`; `Resume File: .planning/phases/13-...` → `.oto/phases/13-...`; the `Archive:` bullets `.planning/milestones/v*.md` → `.oto/milestones/v*.md` (those archives moved too). PRESERVE historical-narrative lines ("archived to `.planning/milestones/...`", "PLAN.md files written in `.planning/phases/03-...`") and semantic lines discussing the contract ("clean cutover to `.oto/`... keeping `.planning/` working", "marking this repo's `.planning/STATE.md` as migrated").
   - `.oto/ROADMAP.md` — rewrite active path citations (e.g. the footer `For prior milestone archives, see .planning/milestones/` and any `milestones/...` link that now lives under `.oto/`). PRESERVE prose describing past phases.
   - `.oto/REQUIREMENTS.md` — rewrite any active path citation to a moved file. PRESERVE traceability prose.
   - `.oto/PROJECT.md` — rewrite active path citations to moved files; PRESERVE historical narrative.
   - `.oto/MILESTONES.md` — rewrite active path citations to moved files; PRESERVE historical narrative.

## FROZEN (D-04) — MUST keep saying `.planning/`. Do NOT touch:
- `foundation-frameworks/**` — vendored upstream byte-baseline (478+ files).
- The resolver fallback: `oto/bin/lib/core.cjs` (`planningRootName`/`hasMigratedPlanningRoot`), `sdk/src/query/helpers.ts` (`findProjectRoot` + `.planning` doc comments/literals), `sdk/src/planning-root.ts`, `sdk/dist/**`.
- `oto/bin/lib/migrate.cjs` — `/oto-migrate` GSD-handling engine.
- `rename-map.json`, `reports/rebrand-dryrun.json`, and migration test fixtures under `tests/fixtures/**` and `sdk/src/golden/fixtures/**`.
- The CJS tooling's GENERIC `.planning/config.json` handling: `oto/bin/install.js`, `oto/bin/lib/{init,state,drift,commands,milestone,verify,gsd2-import}.cjs`, `scripts/install-smoke.cjs` (throwaway `.planning` test project). These handle `.planning/` as a concept for END-USER/un-migrated projects (the migration-window contract), NOT this repo's path.
- `docs/rebrand-engine.md` (line 29 rename-rule example), `decisions/ADR-10-rename-map-schema.md` (line 17 rule example) — describe the rename rule by design.
- Markdown links into COMPLETED-phase dirs (`docs/rebrand-engine.md` → `02-...`, `docs/upstream-sync.md` → `09-...`): these point at preserved point-in-time records. Per D-10, completed-phase dirs (v0.1.0–v0.3.0) are PRESERVED. BUT note: those dirs physically moved to `.oto/...` in Plan 13-02. The LINK TARGET still exists under `.oto/` now. DECISION: these two doc links are NOT on the allowlist (they reference completed-phase research as historical citation, not active operational paths an agent opens during normal flow). Leave them as `../.planning/...` — they are documentation breadcrumbs to point-in-time records, consistent with D-10's "preserve completed-phase directories." If a future reader follows them, that is acceptable historical drift; rewriting them risks scope creep into the preserve-set. Default-exclude when uncertain (D-05).
- `.oto/phases/01-*` … `.oto/phases/10-*`, `.oto/phases/11-*`, `.oto/phases/12-*` SUMMARY/PLAN/RESEARCH/CONTEXT docs — completed/historical; their `.planning/` mentions are historical narrative + resolver-contract discussion. PRESERVE (not on allowlist). Exception: if a phase-13 doc has an ACTIVE canonical_ref to a moved file, that is in-scope, but phase-13 CONTEXT/DISCUSSION-LOG are themselves historical records of the planning conversation — PRESERVE them.
</allowlist>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the workflow-enforcement block to OTO in the template, then re-render CLAUDE/AGENTS/GEMINI (D-07)</name>
  <read_first>
    - oto/templates/instruction-file.md (lines ~202-213 — the GSD:workflow block)
    - scripts/render-instruction-files.cjs (the renderer — confirm it writes CLAUDE.md/AGENTS.md/GEMINI.md)
    - .oto/phases/13-dogfood-migration-to-oto/13-CONTEXT.md (D-07; the user's standing "use /gsd-* until v0.4.0 ships the SDK + .oto/ migration" note that this retires)
  </read_first>
  <files>oto/templates/instruction-file.md, CLAUDE.md, AGENTS.md, GEMINI.md</files>
  <action>
1. In `oto/templates/instruction-file.md`, replace the block between `<!-- GSD:workflow-start source:GSD defaults -->` and `<!-- GSD:workflow-end -->` (currently the "GSD Workflow Enforcement" text) with EXACTLY:

```
<!-- GSD:workflow-start source:GSD defaults -->
## OTO Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through an OTO command so planning artifacts and execution context stay in sync.

Use these entry points:
- `/oto-quick` for small fixes, doc updates, and ad-hoc tasks
- `/oto-debug` for investigation and bug fixing
- `/oto-execute-phase` for planned phase work

Do not make direct repo edits outside an OTO workflow unless the user explicitly asks to bypass it.
<!-- GSD:workflow-end -->
```

   Keep the `<!-- GSD:workflow-start ... -->` / `<!-- GSD:workflow-end -->` marker comments byte-identical (the renderer / future tooling keys on them — do NOT rename the markers). Change ONLY the heading and the body text inside them. Fix the grammar `a OTO` → `an OTO` as shown.

2. Re-render the three instruction files from the template:
```bash
node scripts/render-instruction-files.cjs
```
   This regenerates `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`. Do NOT hand-edit those three files.

3. Confirm all three now carry the OTO block.
  </action>
  <verify>
    <automated>grep -q "OTO Workflow Enforcement" oto/templates/instruction-file.md && grep -q "/oto-quick" CLAUDE.md && grep -q "/oto-quick" AGENTS.md && grep -q "/oto-quick" GEMINI.md && ! grep -q "GSD Workflow Enforcement" CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## OTO Workflow Enforcement" oto/templates/instruction-file.md` exits 0
    - `grep -q "GSD Workflow Enforcement" oto/templates/instruction-file.md` exits 1 (old heading gone from the source)
    - Each of CLAUDE.md, AGENTS.md, GEMINI.md: `grep -q "/oto-quick"` exits 0 AND `grep -q "/gsd-quick"` exits 1 AND `grep -q "GSD Workflow Enforcement"` exits 1
    - The marker comments survive: `grep -q "GSD:workflow-start" CLAUDE.md` and `grep -q "GSD:workflow-end" CLAUDE.md` both exit 0 (markers are structural; only the inner text changed)
    - The renderer was used, not hand-edits: `git diff --name-only` includes all four of `oto/templates/instruction-file.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`
  </acceptance_criteria>
  <done>The template declares "OTO Workflow Enforcement" with `/oto-*` entry points; all three instruction files are re-rendered to match; no `/gsd-*` enforcement remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add the ADR-01 forward-note (D-11)</name>
  <read_first>
    - decisions/ADR-01-state-root.md (read whole file — it documents the .oto/ state-root decision)
    - .oto/phases/13-dogfood-migration-to-oto/13-CONTEXT.md (D-11 — single forward-note line; D-10 — ADRs otherwise preserved as point-in-time records)
  </read_first>
  <files>decisions/ADR-01-state-root.md</files>
  <action>
Append a single forward-note to the END of `decisions/ADR-01-state-root.md` (after the "## Consequences" section). Add exactly:

```

## Forward note (Phase 13)

Paths in this ADR are historical (point-in-time record). As of Phase 13 (v0.4.0 dogfood migration), this repo's own state root is now `.oto/` — `.planning/` no longer exists in this repository. The `.planning/`-with-marker fallback described in the resolver remains intentional for un-migrated end-user projects.
```

Do NOT rewrite the existing `.planning/` strings in the ADR body — they recorded what was true when the rename rule was authored (D-10 preserve). Only ADD the note.
  </action>
  <verify>
    <automated>grep -q "Forward note (Phase 13)" decisions/ADR-01-state-root.md && grep -q "this repo's own state root is now \`.oto/\`" decisions/ADR-01-state-root.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Forward note (Phase 13)" decisions/ADR-01-state-root.md` exits 0
    - `grep -q "now \`.oto/\`" decisions/ADR-01-state-root.md` exits 0
    - The original body is intact: `grep -c "\.planning/" decisions/ADR-01-state-root.md` is still >= the pre-edit count (no body `.planning/` was rewritten — only the note added)
    - `git diff decisions/ADR-01-state-root.md` shows ONLY additions (no `-` content lines)
  </acceptance_criteria>
  <done>ADR-01 carries a single forward-note breadcrumb; its historical body is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Surgically rewrite ACTIVE path citations in moved live artifacts (D-10)</name>
  <read_first>
    - .oto/STATE.md (read fully — identify path-citation lines vs historical-narrative vs contract-prose; see the allowlist for the exact line classification)
    - .oto/ROADMAP.md, .oto/REQUIREMENTS.md, .oto/PROJECT.md, .oto/MILESTONES.md (grep `.planning/` in each; classify each hit)
    - .oto/phases/13-dogfood-migration-to-oto/13-CONTEXT.md (D-10 — rewrite ONLY active path citations to moved files; PRESERVE historical narrative + resolver-contract prose)
  </read_first>
  <files>.oto/STATE.md, .oto/ROADMAP.md, .oto/REQUIREMENTS.md, .oto/PROJECT.md, .oto/MILESTONES.md</files>
  <action>
For EACH file, grep `\.planning/` and classify every hit into one of three buckets, then act:

A. ACTIVE PATH CITATION pointing at a MOVED file an agent will Read → REWRITE `.planning/` → `.oto/`. Signals: lines starting `See: .planning/...`, `Resume File: .planning/...`, `Archive:`/bullet lists of `.planning/milestones/...` or `.planning/phases/...`, footer links like `For prior milestone archives, see .planning/milestones/`, markdown links `[...](.planning/...)` to files that now live at `.oto/...`.

B. HISTORICAL NARRATIVE describing a past action → PRESERVE. Signals: past-tense sentences like "archived to `.planning/milestones/v0.3.0-*`", "4 PLAN.md files written in `.planning/phases/03-...`", "v0.3.0 archived on 2026-05-18: ... archived to `.planning/...`". These recorded what was true at the time.

C. RESOLVER-CONTRACT / SEMANTIC PROSE about the `.planning/` fallback → PRESERVE. Signals: "clean cutover to `.oto/` — no dual-location shim keeping `.planning/` working", "marking this repo's `.planning/STATE.md` as migrated", anything discussing the migration-window fallback. Rewriting these would CORRUPT their meaning.

Concrete known targets (verify against the live files — line numbers may shift):
- `.oto/STATE.md`:
  - `See: .planning/PROJECT.md (updated ...)` → `See: .oto/PROJECT.md (updated ...)` [bucket A]
  - The `Archive (prior milestones):` bullet list `- \`.planning/milestones/v*.md\`` → `- \`.oto/milestones/v*.md\`` (8 bullets) [bucket A — those archives moved]
  - `**Resume File:** .planning/phases/13-dogfood-migration-to-oto/13-CONTEXT.md` → `.oto/phases/13-dogfood-migration-to-oto/13-CONTEXT.md` [bucket A]
  - `**Current focus:** ... Phase 12 ...` and the "v0.3.0 archived on 2026-05-18: ... archived to `.planning/milestones/...`" Roadmap-Evolution bullets → PRESERVE [bucket B]
  - "clean cutover to `.oto/` ... keeping `.planning/` working", "marking this repo's `.planning/STATE.md` as migrated" → PRESERVE [bucket C]
- `.oto/ROADMAP.md`:
  - Footer `_For prior milestone archives, see \`.planning/milestones/\`._` → `.oto/milestones/` [bucket A]
  - `[milestones/v0.3.0-ROADMAP.md](milestones/v0.3.0-ROADMAP.md)` style links are already relative (no `.planning/` prefix) — leave as-is.
  - Any prose describing phase derivation → PRESERVE [bucket B].
- `.oto/REQUIREMENTS.md`, `.oto/PROJECT.md`, `.oto/MILESTONES.md`: apply the same A/B/C classification. Most hits in REQUIREMENTS/MILESTONES are historical/traceability prose [B] — rewrite only genuine active path citations [A].

When uncertain whether a hit is A vs B/C → DEFAULT to PRESERVE (D-05 default-exclude). Bias toward leaving `.planning/` in place; only rewrite when the line is unambiguously an active citation to a file that physically moved.

Do NOT use a blanket `sed 's/.planning/.oto/g'` — that would clobber buckets B and C. Edit hit-by-hit.

After edits, re-run STATE.md's `oto_state_version` check is unaffected (frontmatter untouched by this task — the marker flip happened in Plan 13-02).
  </action>
  <verify>
    <automated>test ! -d .planning && grep -q "\.oto/PROJECT.md" .oto/STATE.md && grep -q "\.oto/phases/13-dogfood-migration-to-oto/13-CONTEXT.md" .oto/STATE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "See: \.oto/PROJECT.md" .oto/STATE.md` exits 0 (active citation rewritten)
    - `grep -q "Resume File:\*\* \.oto/phases/13" .oto/STATE.md` exits 0
    - `grep -q "\.oto/milestones/v0.3.0-ROADMAP.md" .oto/STATE.md` exits 0 (archive bullets followed the move)
    - HISTORICAL/CONTRACT prose preserved: `grep -q "marking this repo's .planning/STATE.md as migrated" .oto/STATE.md` STILL exits 0 (bucket C untouched), and the past-tense "archived to .planning/milestones" Roadmap-Evolution bullet is still present
    - `.oto/ROADMAP.md` footer rewritten: `grep -q "prior milestone archives, see \`.oto/milestones/\`" .oto/ROADMAP.md` exits 0
    - No blanket replace occurred: at least one `.planning/` (a bucket B/C mention) still exists in `.oto/STATE.md` — `grep -c "\.planning/" .oto/STATE.md` is > 0 (proves the rewrite was surgical, not global)
  </acceptance_criteria>
  <done>Active path citations to moved files resolve to `.oto/`; historical-narrative and resolver-contract `.planning/` mentions are preserved verbatim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human reviews the reference-hygiene diff for over-broad rewrites and frozen-surface integrity</name>
  <what-built>
The full reference-hygiene change set: the OTO enforcement flip (template + 3 rendered files), the ADR-01 forward-note, and the surgical D-10 path-citation rewrites in `.oto/{STATE,ROADMAP,REQUIREMENTS,PROJECT,MILESTONES}.md`. All governed by the enumerated allowlist.
  </what-built>
  <how-to-verify>
1. Review the full diff is allowlist-only (no unexpected files):
   ```bash
   git status --short
   ```
   Expect ONLY: `oto/templates/instruction-file.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `decisions/ADR-01-state-root.md`, and the five `.oto/*.md` live artifacts. NO other file should appear.
2. Confirm the daily-flow flip:
   ```bash
   grep -n "oto-quick\|oto-debug\|oto-execute-phase" CLAUDE.md
   ```
3. Confirm the D-10 rewrite stayed surgical (NOT a global replace):
   ```bash
   grep -n "\.planning/" .oto/STATE.md
   ```
   Expect: remaining `.planning/` lines are HISTORICAL NARRATIVE / resolver-contract prose only (e.g. "archived to .planning/milestones", "marking this repo's .planning/STATE.md as migrated") — NOT active `See:`/`Resume File:`/`Archive:` citations.
4. Confirm FROZEN surfaces are untouched:
   ```bash
   git diff --name-only | grep -E "foundation-frameworks/|oto/bin/lib/(core|migrate|init|state|drift|commands|milestone|verify|gsd2-import)\.cjs|sdk/src/query/helpers\.ts|sdk/src/planning-root\.ts|rename-map\.json|tests/fixtures/" || echo "FROZEN-SURFACES-UNTOUCHED-OK"
   ```
   Expect: `FROZEN-SURFACES-UNTOUCHED-OK` (none of those frozen paths appear in the diff).
  </how-to-verify>
  <resume-signal>Type "approved" if the diff is allowlist-only, the flip is correct, D-10 rewrites are surgical, and frozen surfaces are untouched — or describe what overreached.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit the reference-hygiene change set</name>
  <read_first>
    - Run `git status --short` to confirm only allowlist files are staged
  </read_first>
  <files>oto/templates/instruction-file.md, CLAUDE.md, AGENTS.md, GEMINI.md, decisions/ADR-01-state-root.md, .oto/STATE.md, .oto/ROADMAP.md, .oto/REQUIREMENTS.md, .oto/PROJECT.md, .oto/MILESTONES.md</files>
  <action>
Stage explicitly (no `git add -A`) and commit:
```bash
git add oto/templates/instruction-file.md CLAUDE.md AGENTS.md GEMINI.md \
        decisions/ADR-01-state-root.md \
        .oto/STATE.md .oto/ROADMAP.md .oto/REQUIREMENTS.md .oto/PROJECT.md .oto/MILESTONES.md
git commit -m "docs(13): flip enforcement to /oto-* and rewrite live .planning self-refs (DOG-03)

D-07: OTO Workflow Enforcement block (template + rendered CLAUDE/AGENTS/GEMINI).
D-11: ADR-01 forward-note.
D-10: surgical active-path-citation rewrites in live artifacts; historical
narrative and resolver-contract prose preserved. Allowlist-only; frozen
surfaces (foundation-frameworks, resolver, migrate, fixtures) untouched.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && git log --oneline -1 | grep -q "flip enforcement" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` is empty after commit
    - `git show --stat HEAD` lists ONLY the 10 allowlist files (template, 3 instruction files, ADR-01, 5 live artifacts) — no frozen path appears
    - `git log --oneline -1` shows the reference-hygiene commit
  </acceptance_criteria>
  <done>The allowlist-only reference-hygiene change set is committed; tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allowlist scope → repo content | The find-replace must touch ONLY allowlisted live self-refs; over-reach corrupts frozen surfaces |
| template → generated files | Editing the wrong layer (hand-editing generated files vs the template) drifts the generation contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | frozen reference surfaces (foundation-frameworks, resolver fallback, migrate.cjs, rename-map, fixtures, sdk tooling) | mitigate | Allowlist discipline (rewrite only enumerated entries); hit-by-hit edits (never blanket `sed`); human-verify `git diff --name-only` excludes all frozen paths; grep assertion `FROZEN-SURFACES-UNTOUCHED-OK`. |
| T-13-09 | Tampering | meaning of resolver-contract prose (bucket C) | mitigate | D-10 three-bucket classification; default-exclude when uncertain; assert a bucket-C line ("marking this repo's .planning/STATE.md as migrated") survives verbatim. |
| T-13-10 | Tampering | generation contract (CLAUDE/AGENTS/GEMINI) | mitigate | Edit the template + run the renderer; never hand-edit generated files; assert marker comments survive and all four files appear in the diff together. |

No high-severity threats: text/markdown edits + a deterministic local renderer; no untrusted input, no network, no privileged ops. The dominant risk is correctness (over-broad rewrite), mitigated by allowlist + human review + grep freeze-assertions.
</threat_model>

<verification>
- `oto/templates/instruction-file.md` and all three instruction files declare OTO enforcement with `/oto-*`; no `/gsd-*` enforcement remains.
- ADR-01 carries the forward-note; its body is unchanged.
- Active path citations in live artifacts resolve to `.oto/`; historical/contract `.planning/` prose preserved.
- Frozen surfaces (foundation-frameworks, resolver, migrate.cjs, rename-map, fixtures, sdk tooling) untouched — proven by `git diff --name-only`.
- Tree clean after the single allowlist-only commit.
</verification>

<success_criteria>
- DOG-03: every genuine in-repo `.planning/` self-reference on the allowlist is updated; nothing operational points at the stale location.
- Daily flow flipped to `/oto-*` (D-07).
- Allowlist discipline provably held (frozen surfaces untouched).
</success_criteria>

<output>
After completion, create `.oto/phases/13-dogfood-migration-to-oto/13-03-SUMMARY.md`.
</output>
