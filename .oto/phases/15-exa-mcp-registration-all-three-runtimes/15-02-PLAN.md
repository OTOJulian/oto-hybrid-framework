---
phase: 15-exa-mcp-registration-all-three-runtimes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/bin/lib/secrets.cjs
  - oto/bin/lib/config.cjs
  - sdk/src/query/secrets.ts
  - sdk/src/query/config-mutation.ts
  - sdk/src/query/init-complex.ts
  - tests/15-key-usability.test.cjs
  - tests/14-keyfile-symlink.test.cjs
  - sdk/dist/** (rebuild)
autonomous: true
requirements: [MCP-07]
must_haves:
  truths:
    - "A keyfile that resolves through symlinks to a regular readable file with non-empty trimmed content counts as a usable key (D-15)"
    - "Empty, whitespace-only, dangling-symlink, and non-regular keyfiles count as 'no key detected' in BOTH the CJS and SDK paths"
    - "Keyfile WRITES still refuse symlinks (WR-07 unchanged)"
    - "The bare-existsSync defect sites no longer treat an empty or dangling keyfile as 'key present'"
  artifacts:
    - path: "oto/bin/lib/secrets.cjs"
      provides: "D-15 read-follow in readKeyfile; detectKeySource inherits it"
      contains: "D-15"
    - path: "sdk/src/query/secrets.ts"
      provides: "SDK mirror of the D-15 read-follow rule"
      contains: "D-15"
    - path: "tests/15-key-usability.test.cjs"
      provides: "D-15 usability matrix (env/plain/empty/whitespace/symlink-to-regular/symlink-to-dir/dangling)"
  key_links:
    - from: "oto/bin/lib/config.cjs"
      to: "oto/bin/lib/secrets.cjs"
      via: "detectKeySource instead of bare existsSync"
      pattern: "detectKeySource"
    - from: "sdk/src/query/config-mutation.ts"
      to: "sdk/src/query/secrets.ts"
      via: "detectKeySource instead of bare existsSync"
      pattern: "detectKeySource"
---

<objective>
Implement the D-15 key-usability rule (FRESH-WR-04) identically in the CJS and SDK secret helpers: read/detect follows symlinks to regular files, empty/whitespace/non-regular = no key; write path keeps WR-07 O_NOFOLLOW refusal. Fix the bare-`existsSync` defect sites so registration gating, config warnings, and status all use one canonical answer.

Purpose: MCP-07 gates registration on a *detected usable key*; Pitfall 7 (three divergent detection implementations) is prevented by fixing the two canonical helpers first — the launcher (Plan 03) inlines the same table.
Output: Updated secrets helpers in both paths, defect sites routed through `detectKeySource`, D-15 test matrix green, `sdk/dist` rebuilt.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-CONTEXT.md
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-RESEARCH.md

<interfaces>
Current CJS behavior (`oto/bin/lib/secrets.cjs`):
```js
// readKeyfile(slug, baseDir) — lstat first; REFUSES any non-regular file (WR-07),
// then heals loose perms (chmod 0600), reads, trims, empty → null.
function readKeyfile(slug, baseDir)  // returns { value, healed } | null
function writeKeyfile(slug, value, baseDir)  // O_NOFOLLOW — DO NOT CHANGE
function detectKeySource(slug, baseDir)
// returns { source:'env', envVar, masked, shadowedKeyfile } | { source:'keyfile', path, masked } | { source:null, masked:'(unset)' }
function keyfilePath(slug, baseDir), keyfileBase(baseDir)
```
SDK mirror (`sdk/src/query/secrets.ts`): `readKeyfile(slug, baseDir?): KeyfileValue | null`, `detectKeySource(slug, baseDir?): KeySource`, `writeKeyfile` — same semantics.

Defect sites (bare `existsSync` treats empty/dangling keyfile as "key present"):
- `oto/bin/lib/config.cjs:88-93` (approx — locate the `existsSync(keyfilePath...)` check)
- `sdk/src/query/config-mutation.ts:464-466`
- `sdk/src/query/init-complex.ts` (same pattern — grep `existsSync` near keyfile usage)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-15 read-follow in CJS secrets.cjs + config.cjs defect site + test matrix</name>
  <files>oto/bin/lib/secrets.cjs, oto/bin/lib/config.cjs, tests/15-key-usability.test.cjs, tests/14-keyfile-symlink.test.cjs</files>
  <read_first>oto/bin/lib/secrets.cjs (full file — readKeyfile lines ~109-142, detectKeySource ~151-172, writeKeyfile ~81-108), oto/bin/lib/config.cjs lines 80-130 (locate the existsSync keyfile check), tests/14-keyfile-symlink.test.cjs (current WR-07 assertions that must be updated), tests/14-secrets-keyfile.test.cjs (temp-baseDir test pattern)</read_first>
  <behavior>
    In tests/15-key-usability.test.cjs (all against temp baseDir, never real ~/.oto):
    - env `EXA_API_KEY="  key123  "` → source 'env' (trimmed non-empty wins)
    - env whitespace-only + no keyfile → source null
    - plain regular keyfile "sk-abc" → source 'keyfile'
    - empty keyfile → source null; whitespace-only keyfile → source null
    - symlink → regular non-empty file → source 'keyfile' (NEW under D-15; was refused)
    - symlink → directory → source null (stderr one-line refusal, no throw)
    - dangling symlink → source null
    - writeKeyfile onto an existing symlink → still throws/refuses (WR-07 unchanged)
  </behavior>
  <action>
In `oto/bin/lib/secrets.cjs` `readKeyfile`:
- Keep `fs.lstatSync(target)`; ENOENT → null (unchanged).
- If `st.isFile()` → existing behavior unchanged (heal perms, read, trim, empty → null).
- NEW: if `st.isSymbolicLink()` → `fs.statSync(target)` inside try/catch (dangling → stderr one line `refusing to read ${target}: dangling symlink — remove it and re-set via /oto-settings-integrations`, return null). If the followed stat is NOT a regular file → stderr one-line refusal (reuse existing wording pattern), return null. If regular → `fs.readFileSync(target,'utf8').trim()`; empty → null; else return `{ value, healed: false }`. Do NOT chmod symlink targets (oto does not own password-manager-managed files).
- Any other lstat type (dir, FIFO, socket directly at the path) → existing refusal path unchanged.
- Add comment: `// OTO Phase 15 (D-15/FRESH-WR-04): read path follows symlinks to regular files; write path keeps O_NOFOLLOW (WR-07). See decisions/ADR-16.` Keep the diff small and commented — this file is GSD-shared (sync hygiene).
- `writeKeyfile` and `deleteKeyfile`: NO changes.

In `oto/bin/lib/config.cjs` (~lines 88-93): replace the bare `fs.existsSync(<keyfile path>)` key-presence check with `require('./secrets.cjs').detectKeySource(slug, baseDir).source !== null` (adapt to the actual variable names at the site; secrets.cjs is already required in this file for other helpers — reuse the existing import if present). Add comment `// OTO Phase 15 (FRESH-WR-04): canonical usability check — empty/dangling keyfiles are not keys.`

Update `tests/14-keyfile-symlink.test.cjs`: the read-side assertion that a symlink is refused now asserts the D-15 semantics (symlink→regular non-empty = usable; symlink→non-regular = null). Keep the write-side O_NOFOLLOW assertions exactly as they are.

Create `tests/15-key-usability.test.cjs` implementing the behavior matrix above with `node:test`, temp dirs via `fs.mkdtempSync(path.join(os.tmpdir(), 'oto-d15-'))`, and env manipulation via save/restore of `process.env.EXA_API_KEY` in before/after hooks. FIFO case: attempt `child_process.execSync('mkfifo …')` inside try/catch and `t.skip()` when unavailable (win32).
  </action>
  <verify>
    <automated>node --test tests/15-key-usability.test.cjs tests/14-keyfile-symlink.test.cjs tests/14-secrets-keyfile.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `oto/bin/lib/secrets.cjs` contains `isSymbolicLink()` and a comment containing `D-15`
    - `oto/bin/lib/secrets.cjs` writeKeyfile still contains `O_NOFOLLOW` (grep `O_NOFOLLOW` ≥ 1)
    - `oto/bin/lib/config.cjs` no longer key-gates via bare existsSync at the defect site; contains `detectKeySource` at that site
    - `node --test tests/15-key-usability.test.cjs` exits 0 with the 8-case matrix passing
    - `node --test tests/14-*.test.cjs` exits 0 (no Phase 14 regressions)
  </acceptance_criteria>
  <done>CJS detection implements the D-15 table exactly; symlink-to-regular reads work; writes still refuse symlinks; defect site fixed; matrix green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror D-15 in SDK secrets.ts + fix SDK defect sites + rebuild dist</name>
  <files>sdk/src/query/secrets.ts, sdk/src/query/config-mutation.ts, sdk/src/query/init-complex.ts, sdk/dist/**</files>
  <read_first>sdk/src/query/secrets.ts (readKeyfile ~120-154, detectKeySource ~170-204), sdk/src/query/config-mutation.ts lines 455-475 (the existsSync site at ~464-466), sdk/src/query/init-complex.ts (grep `existsSync` near keyfile paths), sdk/package.json (build/test scripts), oto/bin/lib/secrets.cjs (the just-changed CJS implementation to mirror exactly)</read_first>
  <behavior>
    - SDK `detectKeySource('exa', tmpBase)` returns source 'keyfile' for symlink→regular non-empty, null for empty/whitespace/dangling/symlink-to-dir
    - SDK `writeKeyfile` still refuses symlinks (existing vitest assertions stay green)
  </behavior>
  <action>
Mirror Task 1's `readKeyfile` change in `sdk/src/query/secrets.ts` with identical semantics and the same `// OTO Phase 15 (D-15/FRESH-WR-04)` comment: lstat → regular unchanged; symlink → statSync-follow → regular file → read/trim/non-empty (no chmod heal on symlink targets); dangling/non-regular → stderr one line + null. `writeKeyfile` untouched.

Replace the bare `existsSync` key-presence checks in `sdk/src/query/config-mutation.ts` (~464-466) and `sdk/src/query/init-complex.ts` with `detectKeySource(slug, baseDir).source !== null` (import from `./secrets.js` — match the module's existing import specifier style). Add the same one-line comment at each site.

Extend the SDK secrets test file (locate the existing vitest file covering secrets — e.g. `sdk/src/query/secrets.test.ts` or the Phase 14 equivalent; create `sdk/src/query/secrets.test.ts` if none exists) with the D-15 matrix cases from Task 1 (temp-dir based).

Rebuild: run the SDK build per `sdk/package.json` scripts (`cd sdk && npm run build`, or `npx tsc -p tsconfig.json` if no build script) so `sdk/dist` reflects the change. Then run `cd sdk && npx tsc --noEmit` and the focused vitest file.
  </action>
  <verify>
    <automated>cd sdk && npx tsc --noEmit && npx vitest run src/query/secrets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/query/secrets.ts` contains `isSymbolicLink()` and a comment containing `D-15`
    - `sdk/src/query/config-mutation.ts` contains `detectKeySource` at the former existsSync site (grep confirms; `grep -n "existsSync" sdk/src/query/config-mutation.ts` shows no keyfile-presence gating remaining at ~464)
    - `cd sdk && npx tsc --noEmit` exits 0
    - `cd sdk && npx vitest run src/query/secrets.test.ts` exits 0
    - `sdk/dist` rebuilt (git status shows dist changes; `grep -rl "isSymbolicLink" sdk/dist/query/ | grep -c secrets` ≥ 1)
    - `npm test` exits 0 (root suite unaffected)
  </acceptance_criteria>
  <done>SDK path implements the identical D-15 table; both defect sites fixed; dist rebuilt; typecheck + focused vitest + root suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.oto` keyfiles → detection helpers | Attacker-plantable filesystem objects (symlinks, FIFOs) are read by privileged helpers |
| Detection result → registration gate | A wrong "key present" answer causes registration of a server that fails every session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering/Elevation | `writeKeyfile` symlink plant | mitigate | WRITE path unchanged: lstat + O_NOFOLLOW (WR-07); regression asserted in tests/14-keyfile-symlink.test.cjs |
| T-15-04 | Information Disclosure | read-follow of symlinks | accept | Read-follow to a regular file is disclosure-neutral for the same user (ADR-16 rationale); non-regular targets still refused |
| T-15-05 | Denial of Service | FIFO planted at keyfile path blocking reads | mitigate | statSync + regular-file check happens before any read; FIFO → "no key detected", never opened for read |
| T-15-06 | Spoofing | Empty/dangling keyfile counted as credential (defect sites) | mitigate | All key-presence gates routed through `detectKeySource`; matrix tests pin empty/whitespace/dangling → null |
</threat_model>

<verification>
- `node --test tests/15-key-usability.test.cjs tests/14-keyfile-symlink.test.cjs` exits 0
- `cd sdk && npx tsc --noEmit` exits 0; focused vitest green
- `npm test` exits 0
- No test writes to the real `~/.oto` (all cases use mkdtemp baseDir)
</verification>

<success_criteria>
- D-15 rule implemented identically in CJS and SDK; write path untouched
- Bare-existsSync defect sites fixed in config.cjs, config-mutation.ts, init-complex.ts
- Full matrix green in both test frameworks; sdk/dist rebuilt
</success_criteria>

<output>
After completion, create `.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-02-SUMMARY.md`
</output>
