---
phase: 15-exa-mcp-registration-all-three-runtimes
plan: 08
type: execute
wave: 4
depends_on: ["15-07", "15-02"]
files_modified:
  - bin/lib/args.cjs
  - bin/lib/mcp-consent.cjs
  - bin/install.js
  - tests/15-consent.test.cjs
autonomous: true
requirements: [MCP-01]
must_haves:
  truths:
    - "Nothing registers without explicit consent; the default answer is No; non-TTY installs never prompt and never register (one-line log points at /oto-settings-integrations)"
    - "Multi-runtime installs (--all, multiple --<runtime> flags) issue at most ONE consent prompt per install command, naming every needs-prompt targeted runtime (D-06); the single answer persists per-runtime (D-07); per-runtime silent paths (flag/no-key/recorded) never prompt"
    - "No usable key detected → silent skip, no prompt, no registration"
    - "Consent answers (Yes AND No) persist per integration+runtime in ~/.oto/mcp-consent.json; later installs honor them silently"
    - "--register-exa-mcp / --unregister-exa-mcp provide the scripted path; both flags together is an argument error"
    - "After a Yes, the npx cache is pre-warmed with the empty-stdin mechanism; pre-warm failure warns but does not block registration"
  artifacts:
    - path: "bin/lib/mcp-consent.cjs"
      provides: "consent read/write, single aggregated TTY prompt per install command, per-runtime decisions, pre-warm"
      exports: ["decideExaMcpAction", "readConsent", "writeConsent", "preWarmExaMcp"]
    - path: "bin/lib/args.cjs"
      provides: "register-exa-mcp / unregister-exa-mcp flags in the strict parser"
      contains: "register-exa-mcp"
    - path: "bin/install.js"
      provides: "consent gate before the adapter loop, threading opts.exaMcp"
      contains: "decideExaMcpAction"
    - path: "tests/15-consent.test.cjs"
      provides: "MCP-01 decision matrix (flag/no-key/recorded/non-TTY/prompt) incl. multi-runtime single-prompt aggregation"
  key_links:
    - from: "bin/install.js"
      to: "bin/lib/install.cjs installRuntime(opts.exaMcp)"
      via: "per-runtime decisions map (one decideExaMcpAction call per install command) threaded into each installRuntime call"
      pattern: "exaMcp"
    - from: "bin/lib/mcp-consent.cjs"
      to: "oto/bin/lib/secrets.cjs detectKeySource"
      via: "require('../../oto/bin/lib/secrets.cjs')"
      pattern: "detectKeySource"
---

<objective>
Build the consent gate (D-05..D-08): flags in the strict arg parser, the decision function with persisted per-runtime answers in `~/.oto/mcp-consent.json`, TTY-guarded default-No prompt issued at most ONCE per install command covering all needs-prompt targeted runtimes (D-06), key-usability gating via the Plan 02 canonical helper, and the corrected empty-stdin pre-warm — wired into `bin/install.js` before the adapter loop.

Purpose: MCP-01 — consent before any registration, nothing silent, default No. D-06 — one consent covers the runtimes the current install command targets; no per-runtime checklist ceremony. Pitfall 6 (CI hang) is prevented by the TTY guard.
Output: `oto install --<runtime>` (and multi-runtime `--all` / `--claude --codex`) prompts exactly per the D-05..D-08 matrix — at most one prompt per command — and threads `opts.exaMcp` into `installRuntime`.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-CONTEXT.md
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-RESEARCH.md

<interfaces>
`bin/lib/args.cjs`: `FLAG_CONFIG.options` object (strict: true — unknown flags THROW; new flags MUST be declared); `parseCliArgs(argv)` returns `{ action: 'install'|'uninstall', runtimes: [...], all, configDir, force, purge, verbose, help }`; `ArgError(message, exitCode)`.

`bin/install.js` `main(argv)`: `parsed = parseCliArgs(argv)`; install branch loops `for (const rt of parsed.runtimes) await installRuntime(ADAPTERS[rt], opts)`; there is also an `installAll` path — read the file to see how `--all` dispatches, resolve BOTH dispatches to a single targeted-runtimes array, and thread the same opts there.

From Plan 07: `installRuntime(adapter, opts)` consumes `opts.exaMcp` ('register' | 'unregister' | null) and `opts.env`.
From Plan 02: `require('../../oto/bin/lib/secrets.cjs').detectKeySource('exa', baseDir)` → `{ source: 'env'|'keyfile'|null, ... }` with D-15 usability semantics (relative path from bin/lib/ → repo root → oto/bin/lib/).

Consent file (~/.oto/mcp-consent.json — dedicated file, NOT defaults.json, because loadDefaults validates that file's shape):
```json
{ "exa": { "claude": { "answer": "yes", "recorded_at": "<iso>" }, "codex": { "answer": "no", "recorded_at": "<iso>" } } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flags in args.cjs + mcp-consent.cjs decision module</name>
  <files>bin/lib/args.cjs, bin/lib/mcp-consent.cjs, tests/15-consent.test.cjs</files>
  <read_first>bin/lib/args.cjs (FULL — FLAG_CONFIG, parseCliArgs validation style, ArgError exit codes), oto/bin/lib/secrets.cjs (detectKeySource signature post-Plan-02), decisions/ADR-16-exa-mcp-transport.md (pre-warm mechanism)</read_first>
  <behavior>
    parseCliArgs:
    - `['install','--claude','--register-exa-mcp']` → `registerExaMcp: true`
    - `['install','--claude','--register-exa-mcp','--unregister-exa-mcp']` → ArgError
    - `['uninstall','--claude','--register-exa-mcp']` → ArgError (flags are install-only)
    decideExaMcpAction({ runtimes, parsed, env, keyBaseDir, consentBaseDir, io }) → `{ [runtime]: { action, reason } }` (one call per install command; per-runtime decisions, at most ONE prompt — D-06):
    - registerExaMcp flag → every targeted runtime `{ action: 'register', reason: 'flag' }`, consent 'yes' persisted per runtime; io prompt never called
    - unregisterExaMcp flag → every targeted runtime `{ action: 'unregister', reason: 'flag' }`, consent 'no' persisted per runtime
    - no usable key (empty consentless tmp dirs) → every runtime `{ action: null, reason: 'no-key' }`, no prompt (io.prompt never called)
    - recorded answers honored silently per runtime (D-07): 'yes' → `{ action: 'register', reason: 'recorded' }`; 'no' → `{ action: null, reason: 'declined' }`
    - runtimes with a usable key and NO recorded answer form the needs-prompt set; io.isTTY false → each gets `{ action: null, reason: 'non-interactive' }` and exactly ONE line written via io.log
    - io.isTTY true → EXACTLY ONE prompt naming all needs-prompt runtimes (`Register the Exa MCP server for claude, codex? [y/N]`); '' (default) or 'n' → 'no' persisted for EACH prompted runtime; 'y' → 'register' + 'yes' persisted for each
    - mixed state (`['claude','codex']`, claude recorded 'yes', codex unrecorded, key present, TTY) → claude registers silently ('recorded'); ONE prompt naming only codex; answer persisted for codex only
    - single-runtime call (`['claude']`) → behavior identical to a per-runtime gate; prompt names the one runtime
  </behavior>
  <action>
In `bin/lib/args.cjs`: add to FLAG_CONFIG.options `'register-exa-mcp': { type: 'boolean', default: false }` and `'unregister-exa-mcp': { type: 'boolean', default: false }`. In `parseCliArgs`: throw `ArgError('--register-exa-mcp and --unregister-exa-mcp are mutually exclusive', 3)` when both set; throw ArgError when either is set with `action === 'uninstall'`; return `registerExaMcp: values['register-exa-mcp']` and `unregisterExaMcp: values['unregister-exa-mcp']`.

Create NEW oto-only `bin/lib/mcp-consent.cjs` (comment header referencing D-05..D-08 and ADR-16):
- `consentPath(baseDir)` → `path.join(baseDir || path.join(os.homedir(), '.oto'), 'mcp-consent.json')`
- `readConsent(slug, runtime, baseDir)` → 'yes' | 'no' | null (missing file, unparseable JSON, or missing keys all → null — never throw)
- `writeConsent(slug, runtime, answer, baseDir)` → merge-preserving read-modify-write of the file shape in interfaces; `fs.mkdirSync(dir, { recursive: true, mode: 0o700 })` first
- `promptYesNo(question, io)` → uses `io.input`/`io.output` (injected; default `process.stdin`/`process.stdout`) with `node:readline`; empty answer = No; accepts y/yes case-insensitive
- `decideExaMcpAction({ runtimes, parsed, env, keyBaseDir, consentBaseDir, io })` → returns `{ [runtime]: { action, reason } }`. Called ONCE per install command with the full targeted-runtimes array. Two-phase order implementing D-05..D-08:
  PHASE A — per-runtime silent decisions (no prompt, preserving D-07 per-runtime persistence): (1) unregister flag → persist 'no' per runtime, decide unregister for all; (2) register flag → persist 'yes' per runtime, decide register for all; (3) `detectKeySource('exa', keyBaseDir).source === null` (ONE call — the key is global, not per-runtime) → all runtimes `{ action: null, reason: 'no-key' }` (io.log one line only when parsed.verbose); (4) per runtime, recorded answer → honor silently ('recorded'/'declined'); runtimes left undecided form the needs-prompt set.
  PHASE B — aggregate prompt (at most ONE per call — D-06, no per-runtime checklist ceremony): if needs-prompt set empty → return; if `!io.isTTY` → io.log(`oto: exa MCP registration skipped (non-interactive) — run /oto-settings-integrations to register later`) once and each needs-prompt runtime gets null/'non-interactive'; else prompt ONCE `Register the Exa MCP server for ${needsPrompt.join(', ')}? [y/N] `, persist the single answer per-runtime into mcp-consent.json (D-07), and decide register/null for each accordingly.
  `io` defaults: `{ isTTY: process.stdin.isTTY && process.stdout.isTTY, log: console.log, input: process.stdin, output: process.stdout }`
- `preWarmExaMcp({ timeoutMs = 120000, spawnImpl = require('node:child_process').spawnSync } = {})` → prints `oto: warming npx cache for exa-mcp-server@3.2.1 (first run may take a while)…`, calls `spawnImpl(process.platform === 'win32' ? 'npx.cmd' : 'npx', ['-y','exa-mcp-server@3.2.1','tools=web_search_exa,web_fetch_exa,web_search_advanced_exa'], { input: '', timeout: timeoutMs, encoding: 'utf8' })`, returns `{ ok: res.status === 0, status: res.status }`; on !ok the CALLER prints a warning and continues (Open Question 4 resolution: still register — the launcher surfaces the same failure per-session)

Write `tests/15-consent.test.cjs` FIRST covering the behavior matrix with injected `io` (scripted prompt answers, spy log, spy prompt counting invocations), tmp `keyBaseDir`/`consentBaseDir` dirs, and a seeded keyfile for key-present cases. MUST include multi-runtime aggregation cases: two unrecorded runtimes → exactly one prompt naming both, single answer persisted for both; mixed recorded/unrecorded → prompt names only the unrecorded runtime; flags/no-key/recorded paths never invoke the prompt spy. `preWarmExaMcp` tested only via an injected `spawnImpl` spy (assert argv + input: '' + never called during decide-only paths) — tests must never spawn real npx.
  </action>
  <verify>
    <automated>node --test tests/15-consent.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `bin/lib/args.cjs` FLAG_CONFIG contains `'register-exa-mcp'` and `'unregister-exa-mcp'`; mutual-exclusion + uninstall-rejection ArgErrors implemented
    - `bin/lib/mcp-consent.cjs` exports `decideExaMcpAction`, `readConsent`, `writeConsent`, `promptYesNo`, `preWarmExaMcp`
    - Decision tests cover every PHASE A silent branch, both non-TTY and TTY prompt outcomes, AND the multi-runtime aggregation cases (one prompt for two unrecorded runtimes; mixed recorded/unrecorded prompts only the unrecorded; prompt spy called at most once per decideExaMcpAction call) with named tests; no test spawns npx or prompts a real TTY
    - `grep -c "exaApiKey" bin/lib/mcp-consent.cjs` = 0 (no key material handled — only detectKeySource booleans)
    - `node --test tests/15-consent.test.cjs` exits 0
  </acceptance_criteria>
  <done>Full consent decision matrix implemented and pinned, including D-06 single-prompt aggregation; flags parse strictly; pre-warm mechanism injectable and correct.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the gate into bin/install.js</name>
  <files>bin/install.js</files>
  <read_first>bin/install.js (FULL — main(), the install loop, the --all path, opts construction), bin/lib/mcp-consent.cjs (Task 1), bin/lib/install.cjs (opts.exaMcp/opts.env consumption from Plan 07)</read_first>
  <action>
In `bin/install.js` `main()` install branch (D-06: ONE consent per install command covering all targeted runtimes — NOT one prompt per runtime):
1. Resolve the full targeted-runtimes array first: both the explicit `parsed.runtimes` loop and the `--all` path must yield the same array shape — read the file and route both dispatches through the same consent call.
2. Call the decision function EXACTLY ONCE per install command: `const decisions = decideExaMcpAction({ runtimes, parsed, env: process.env })` (defaults for keyBaseDir/consentBaseDir/io). Per-runtime silent paths (flags/no-key/recorded) and per-runtime persistence (D-07) are handled inside the module; at most one prompt is issued, naming all needs-prompt runtimes.
3. If ANY decision in the map is 'register', call `preWarmExaMcp()` ONCE before the install loop (D-03: consent → warm → write); on `!ok` print `oto: warning — could not pre-warm exa-mcp-server (offline?); registration proceeds, first session will retry` and continue.
4. Thread the per-runtime decision into each install: `installRuntime(ADAPTERS[rt], { ...opts, exaMcp: decisions[rt].action })`.
Uninstall branch: unchanged (state-driven unmerge from Plan 07 handles it).
Add `require('./lib/mcp-consent.cjs')` at top. Keep every message one line. Do NOT modify the vestigial `oto/bin/install.js`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('bin/install.js','utf8'); if(!/decideExaMcpAction/.test(s)||!/preWarmExaMcp/.test(s)||!/exaMcp:/.test(s)) process.exit(1)" && printf '' | node bin/install.js install --help >/dev/null 2>&1; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `bin/install.js` contains `decideExaMcpAction`, `preWarmExaMcp`, and threads `exaMcp:` into installRuntime opts
    - `decideExaMcpAction` is invoked exactly once per install command (single call site fed the full targeted-runtimes array — covers both `parsed.runtimes` and `--all` dispatch), never inside the per-runtime loop
    - `git diff --name-only` does NOT include `oto/bin/install.js`
    - `node bin/install.js --help` still exits 0 and prints usage (no parse regression)
    - Non-TTY safety: `printf '' | node bin/install.js install --claude --config-dir <tmpdir>` completes without hanging (may be exercised in tests/15-consent.test.cjs as a spawn with stdin non-TTY and a tmp config dir; acceptable to assert completion + 'skipped (non-interactive)' absence/presence depending on key state)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Consent gate live in the real installer entry point with one consent per install command (D-06); pre-warm fires once after any Yes; CI-safe non-TTY behavior verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv/TTY → registration decision | User intent must be explicit before oto mutates runtime configs |
| npm registry → pre-warm spawn | Network + third-party code at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-25 | Repudiation | silent registration (consent bypass) | mitigate | Default No everywhere; non-TTY = No; ordered decision function test-pinned; the single aggregated prompt names every runtime it covers, so consent scope is explicit; install report line states exactly what was registered where |
| T-15-26 | Elevation | consent file tampering flipping No→Yes | accept | `~/.oto` is same-user 0700; consent grants no secret access — worst case is a registration the user can see in status and remove |
| T-15-27 | Denial of Service | prompt hanging CI / non-interactive installs (Pitfall 6) | mitigate | `isTTY` guard before any readline; non-TTY path test-pinned; pre-warm has a hard timeout |
| T-15-28 | Information Disclosure | key bytes entering consent/prompt/pre-warm code | mitigate | Module consumes only `detectKeySource(...).source` booleans; grep gate asserts no `exaApiKey` handling; pre-warm argv contains no key |
</threat_model>

<verification>
- `node --test tests/15-consent.test.cjs` green (full matrix incl. multi-runtime single-prompt aggregation)
- `npm test` green
- Manual spot-check available: `printf '' | node bin/install.js install --claude --config-dir $(mktemp -d)` returns without prompting
</verification>

<success_criteria>
- MCP-01 satisfied: explicit consent, default No, persisted both ways per runtime, scripted opt-in/opt-out flags, CI-safe, pre-warm after Yes
- D-06 satisfied: one consent prompt per install command covering all needs-prompt targeted runtimes; no per-runtime checklist ceremony; single-runtime installs behave exactly as before
</success_criteria>

<output>
After completion, create `.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-08-SUMMARY.md`
</output>
