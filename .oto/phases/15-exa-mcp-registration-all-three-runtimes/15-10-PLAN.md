---
phase: 15-exa-mcp-registration-all-three-runtimes
plan: 10
type: execute
wave: 5
depends_on: ["15-08", "15-09"]
files_modified:
  - oto/workflows/settings-integrations.md
  - tests/14-settings-workflow-contract.test.cjs
autonomous: false
requirements: [MCP-09, MCP-01]
user_setup:
  - service: exa
    why: "Manual end-to-end verification of a real consented registration (dev machine has no key — RESEARCH Pitfall 9)"
    env_vars:
      - name: EXA_API_KEY
        source: "dashboard.exa.ai → API Keys (export in the shell used for the checkpoint; not required for any automated test)"
must_haves:
  truths:
    - "/oto-settings-integrations shows per-runtime exa registration status (claude / codex / gemini) sourced from `oto-sdk query mcp-status` — the broken $OTO_TOOLS path is gone from this workflow"
    - "The workflow offers register/unregister for detected runtimes only (D-11), driving the installer's shared code path via --register-exa-mcp / --unregister-exa-mcp"
    - "Replace and Clear disclose the keyfile's global scope plainly and require explicit confirmation before any secret mutation (D-16 / FRESH-WR-05), then reconcile enabled flags across root/workstream configs"
  artifacts:
    - path: "oto/workflows/settings-integrations.md"
      provides: "status summary, register/unregister flow, global-scope confirmation"
      contains: "mcp-status"
  key_links:
    - from: "oto/workflows/settings-integrations.md"
      to: "oto-sdk query mcp-status"
      via: "workflow shell step"
      pattern: "oto-sdk query mcp-status"
    - from: "oto/workflows/settings-integrations.md"
      to: "oto install --<runtime> --register-exa-mcp"
      via: "register/unregister menu actions"
      pattern: "register-exa-mcp"
---

<objective>
Rewrite the relevant sections of `/oto-settings-integrations`: per-runtime MCP status summary via `oto-sdk query mcp-status`, register/unregister actions through the installer's shared consent path, the D-16 global-scope disclosure + confirmation on Replace/Clear, and removal of the doubly-broken `$OTO_TOOLS` dependency (FRESH-CR-02). Close with a human-verify checkpoint covering the live happy path the automated tests cannot exercise (no key on this machine; live CLIs banned from the install path).

Purpose: MCP-09's user-facing surface + the register-later path of MCP-01 (D-05).
Output: Updated workflow + human-verified live registration in at least Claude Code.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-CONTEXT.md
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-RESEARCH.md

<interfaces>
Available after Plans 08/09:
- `oto-sdk query mcp-status` → `{ runtimes: [{ runtime, status, target }], warnings, raw }`; statuses: not-installed / not-registered / oto-managed / user-owned / drifted / missing-but-expected
- `oto install --claude|--codex|--gemini --register-exa-mcp` (scripted consent yes + registration) and `--unregister-exa-mcp` (consent no + removal) — the D-05 shared code path
- `oto-sdk query workstream get --raw` and `oto-sdk query config-path` are ALREADY registered SDK commands (index.ts ~511-512, ~286) — Assumption A6: verify the `--raw` flag surface empirically while editing and adapt the workflow text to actual output
- Phase 14 flows kept as-is: `! oto-sdk query secret-set <slug>`, `secret-clear`, `secret-status` (masked)

Current defect being removed (FRESH-CR-02): the workflow's `OTO_TOOLS="${OTO_TOOLS:-$HOME/.claude/oto/bin/oto-tools.cjs}"` default is hardcoded to Claude AND points at a path the live installer never creates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite settings-integrations workflow sections</name>
  <files>oto/workflows/settings-integrations.md</files>
  <read_first>oto/workflows/settings-integrations.md (FULL — the OTO_TOOLS block at ~55-63, the status display at ~77-102, the secret-set flows at ~150-170), .oto/phases/14-key-storage-reconciliation/14-DISPOSITIONS.md (FRESH-WR-05 obligation verbatim)</read_first>
  <action>
Edit `oto/workflows/settings-integrations.md` (GSD-shared file — keep the diff as small as the feature allows, comment new blocks with `<!-- OTO Phase 15 -->` markers where the workflow format permits):

1. **Drop $OTO_TOOLS (FRESH-CR-02):** replace `WS=$(node "$OTO_TOOLS" workstream get --raw …)` with `WS=$(oto-sdk query workstream get --raw 2>/dev/null || echo none)` and `OTO_CONFIG_PATH=$(node "$OTO_TOOLS" config-path …)` with the `oto-sdk query config-path` equivalent, preserving the existing `${WS_ARGS[@]+…}` threading. Delete the `OTO_TOOLS=` default line entirely. Before finalizing, RUN both replacement commands in this repo to confirm flags/output (Assumption A6) and adapt if `--raw` differs.

2. **Per-runtime MCP status section (MCP-09/D-09/D-11):** after the existing `secret-status` display, add a step running `oto-sdk query mcp-status` and rendering one line per runtime from `raw` (or from the structured runtimes array), e.g. `exa MCP — claude: oto-managed | codex: not-registered | gemini: not-installed`, plus any `warnings` lines verbatim (they are pre-formatted one-liners).

3. **Register/Unregister actions (D-05/D-11):** add menu options shown ONLY for runtimes whose status is not `not-installed`: register → run `oto install --<runtime> --register-exa-mcp`; unregister (offered when status is oto-managed/drifted/missing-but-expected) → run `oto install --<runtime> --unregister-exa-mcp`. After either action, re-run `mcp-status` and show the new line. For `user-owned` status, display the D-13 refusal explanation (existing entry not oto-managed; resolve manually) instead of offering registration.

4. **D-16 global-scope confirmation (FRESH-WR-05):** in the Replace and Clear flows, BEFORE invoking `! oto-sdk query secret-set <slug>` / `secret-clear`, display exactly: `This key is shared by the root project and all workstreams — replacing/clearing it affects every context that uses <slug>.` and require an explicit yes/no confirmation (default No). After a completed Clear, check integration flags across root and workstream configs (`config-get` on each layer the workflow already resolves) and when any layer still has `<config_key>: true`, surface the coherence warning from `mcp-status`/offer to disable. No root-only lockout: the flow works from any workstream context.
  </action>
  <verify>
    <automated>bash -c 'f=oto/workflows/settings-integrations.md; grep -q "oto-sdk query mcp-status" $f && grep -q "register-exa-mcp" $f && grep -q "unregister-exa-mcp" $f && grep -q "shared by the root project and all workstreams" $f && ! grep -q "OTO_TOOLS" $f'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OTO_TOOLS" oto/workflows/settings-integrations.md` = 0
    - File contains `oto-sdk query mcp-status`, `--register-exa-mcp`, `--unregister-exa-mcp`, and the exact scope-disclosure sentence fragment `shared by the root project and all workstreams`
    - Replace/Clear sections contain an explicit confirmation step with default No
    - `node --test tests/14-settings-workflow-contract.test.cjs` exits 0 (update the contract test expectations if they pin the old $OTO_TOOLS lines — the contract is the workflow's command surface, which this plan intentionally changes)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Workflow shows truthful per-runtime status, offers register/unregister via the shared installer path, and gates Replace/Clear behind the global-scope confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end verification (consented registration, 3-tool surface, uninstall symmetry)</name>
  <what-built>Consent-gated, fingerprint-tracked Exa MCP registration across Claude Code, Codex, and Gemini, with status surface and uninstall symmetry (Plans 01-10).</what-built>
  <action>Pause for developer verification. Present the how-to-verify steps below, wait for the resume signal, and record the outcome (approved / issues) in the plan SUMMARY. Do not proceed to phase completion without an explicit "approved".</action>
  <how-to-verify>
Prerequisite: `export EXA_API_KEY=<your key>` in the verification shell (or set the keyfile via /oto-settings-integrations first).

1. From the repo: `node bin/install.js install --claude` → expect the consent prompt `Register the Exa MCP server for claude? [y/N]`; answer `y`. Expect the `warming npx cache…` line, then `oto: registered exa MCP server for claude (…/.claude.json)`.
2. `claude mcp list` (or open Claude Code → /mcp) → `exa` present; its tool list shows exactly `web_search_exa`, `web_fetch_exa`, `web_search_advanced_exa` — no deprecated tools (MCP-06).
3. `oto-sdk query mcp-status` → `claude: oto-managed`; `~/.gemini` (no oto install) shows `not-installed` or `not-registered`.
4. Re-run `node bin/install.js install --claude` → NO prompt (recorded yes honored), still exactly one `exa` entry in `~/.claude.json` (MCP-07).
5. Assumption A1 probe: `mkdir -p /tmp/ccd && CLAUDE_CONFIG_DIR=/tmp/ccd node bin/install.js install --claude --config-dir /tmp/ccd --register-exa-mcp` then confirm `/tmp/ccd/.claude.json` contains the exa entry and, if convenient, `CLAUDE_CONFIG_DIR=/tmp/ccd claude mcp list` shows it.
6. `node bin/install.js uninstall --claude` on the /tmp/ccd install → exa entry and launcher removed from /tmp/ccd (MCP-08). (Keep your real ~/.claude registration if you want it.)
7. Run `/oto-settings-integrations` in a session → per-runtime status line renders; Replace flow shows the global-scope confirmation before any secret command.
8. Grep-audit (secret hygiene): `grep -i "$EXA_API_KEY" ~/.claude.json ~/.codex/config.toml ~/.gemini/settings.json 2>/dev/null` returns nothing.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human checkpoint; automated coverage for this surface lives in tests/15-*.test.cjs (live-CLI verification is manual-only per 15-VALIDATION.md)</automated>
  </verify>
  <done>Developer replied "approved" after completing steps 1-8, including the grep-audit showing no key bytes in any runtime config.</done>
  <resume-signal>Type "approved" or describe issues (include which step failed and its output)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent-executed workflow → secret/registration mutations | The workflow orchestrates but must never handle key bytes; mutations must be explicitly confirmed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-33 | Information Disclosure | key bytes entering the workflow/chat transcript | mitigate | Register/unregister/status commands carry no key material; secret entry stays on Phase 14's `!`-prefix hidden-terminal path; checkpoint includes the grep-audit for keys in runtime configs |
| T-15-34 | Repudiation | destructive Replace/Clear without informed consent (global scope) | mitigate | D-16 disclosure sentence + explicit default-No confirmation, grep-pinned in acceptance criteria |
| T-15-35 | Tampering | workflow offering registration into absent runtimes or over user-owned entries | mitigate | Menu gated on mcp-status (D-11); user-owned status renders the refusal explanation instead of an action |
</threat_model>

<verification>
- Workflow grep gates pass (no OTO_TOOLS; mcp-status + flags + disclosure present)
- `npm test` green
- Human checkpoint approved: live registration, 3-tool surface, idempotent re-install, uninstall symmetry, A1 probe, no key bytes in any runtime config
</verification>

<success_criteria>
- `/oto-settings-integrations` shows per-runtime registration status and offers register/unregister (MCP-09)
- D-16/FRESH-WR-05 confirmation live; FRESH-CR-02 $OTO_TOOLS dependency gone
- Live e2e approved by the developer
</success_criteria>

<output>
After completion, create `.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-10-SUMMARY.md`
</output>
