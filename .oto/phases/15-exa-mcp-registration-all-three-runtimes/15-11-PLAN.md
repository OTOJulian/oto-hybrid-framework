---
phase: 15-exa-mcp-registration-all-three-runtimes
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/runtime-claude.cjs
  - bin/lib/runtime-gemini.cjs
  - tests/15-claude-mcp-merge.test.cjs
  - tests/15-gemini-mcp-merge.test.cjs
  - tests/15-mcp-state.test.cjs
autonomous: true
gap_closure: true
requirements: [MCP-03, MCP-05]
must_haves:
  truths:
    - "Claude mergeMcp refuses with { registered: false, refused: { reason: 'incompatible-shape' } } when the parsed JSON root or an existing mcpServers container is not a plain object, and the target file bytes are byte-identical after the refusal"
    - "Gemini mergeMcp does the same, and additionally refuses with reason 'unparseable' (instead of throwing) on unparseable settings.json"
    - "For every incompatible-shape fixture, a full installRuntime with exaMcp: 'register' completes without throwing, records NO .install.json mcp fingerprint, and leaves the runtime config bytes exactly as seeded"
    - "unmergeMcp never throws on non-object roots/containers — it returns { removed: false, skipped: { reason: 'absent' } } and never writes"
  artifacts:
    - path: "bin/lib/runtime-claude.cjs"
      provides: "Plain-object root/container guard in mergeMcp + unmergeMcp"
      contains: "incompatible-shape"
    - path: "bin/lib/runtime-gemini.cjs"
      provides: "Plain-object root/container guard + unparseable refusal in mergeMcp + unmergeMcp"
      contains: "incompatible-shape"
    - path: "tests/15-claude-mcp-merge.test.cjs"
      provides: "Adapter regressions: array/string/null container, null/array/primitive root — refusal + byte preservation"
    - path: "tests/15-gemini-mcp-merge.test.cjs"
      provides: "Adapter regressions: same fixture matrix + unparseable refusal"
    - path: "tests/15-mcp-state.test.cjs"
      provides: "Installer-lifecycle regressions asserting no mcp fingerprint is recorded on refusal"
  key_links:
    - from: "bin/lib/install.cjs"
      to: "bin/lib/runtime-claude.cjs / bin/lib/runtime-gemini.cjs"
      via: "existing result.registered gate before buildMcpStateRecord (unchanged — refusal must flow through it)"
      pattern: "result\\.registered"
    - from: "tests/15-mcp-state.test.cjs"
      to: "bin/lib/install.cjs"
      via: "installRuntime with exaMcp: 'register' against seeded incompatible fixtures"
      pattern: "exaMcp"
---

<objective>
Close verification blocker CR-01 (VERIFICATION.md, adjudicated `confirmed`, blocking MCP-03 and MCP-05): the Claude and Gemini MCP adapters must fail closed when valid JSON has an incompatible shape — non-plain-object root, or a `mcpServers` container that is an array/string/null/primitive. Today Claude destroys user content (array/string `mcpServers` replaced with `{}`) and returns `registered: true`; both adapters throw on `null` roots; both report phantom success on `[]` roots, which lets the installer record an ownership fingerprint for a registration that does not exist.

Purpose: restore the phase's core preservation/refusal guarantee — "oto never touches config it didn't write and can't prove it still owns" (15-CONTEXT.md D-13 theme).
Output: guarded adapters + adapter and installer-lifecycle regressions for every reproduced failure mode, all green under `node --test`.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-VERIFICATION.md

**Re-review scope (state verbatim in SUMMARY):** The follow-up review for this gap-closure run is limited to files changed by gap plans 15-11 and 15-12 only: `bin/lib/runtime-claude.cjs`, `bin/lib/runtime-gemini.cjs`, `tests/15-claude-mcp-merge.test.cjs`, `tests/15-gemini-mcp-merge.test.cjs`, `tests/15-mcp-state.test.cjs`, `sdk/src/query/mcp-status.ts`, `sdk/src/query/mcp-status.test.ts`, `tests/15-mcp-status.test.cjs`, plus generated `sdk/dist/**`. Plans 15-01..15-10 are executed and MUST NOT be modified.

<interfaces>
Current defect sites (read before editing):

`bin/lib/runtime-claude.cjs` `mergeMcp` (lines 203-239):
- Line 208: `claudeState = JSON.parse(existingText)` — root never validated as plain object; `null` root throws at line 219 (`claudeState.mcpServers?.exa` on `null`).
- Lines 233-235: `if (!claudeState.mcpServers || typeof claudeState.mcpServers !== 'object' || Array.isArray(claudeState.mcpServers)) { claudeState.mcpServers = {}; }` — this DESTROYS a user array/string/null container and proceeds to `registered: true`.
- Existing refusal vocabulary: `{ registered: false, refused: { reason: 'unparseable' }, entry: null, target }` (line 210-215) and `reason: 'user-owned'` (line 225-230). The new refusal reuses this exact result shape.

`bin/lib/runtime-claude.cjs` `unmergeMcp` (lines 241-263): `claudeState.mcpServers?.exa` at line 250 throws on `null` root.

`bin/lib/runtime-gemini.cjs` `mergeMcp` (lines 55-81):
- Line 58: `parseSettings(existingText || '{}')` — throws `Error('mergeSettings: cannot parse settings.json: …')` on unparseable text (no refusal path), and returns arrays/primitives/null untouched.
- Line 74: `settings.mcpServers = settings.mcpServers || {}` — on array root/container the `.exa` assignment is not serialized yet the function returns `registered: true`; on string container the assignment throws; on `[]` root the write at line 77 rewrites bytes to `[]\n`.

`bin/lib/runtime-gemini.cjs` `unmergeMcp` (lines 83-107): same parse/shape exposure at lines 89-90.

Installer gate (NO change needed — verify only): `bin/lib/install.cjs` lines 346-362 — `if (result.registered)` records `buildMcpStateRecord(...)` into `.install.json`; `else if (result.refused)` logs a skip line including `result.refused.reason`. Refusals therefore automatically prevent fingerprint recording.

Test helpers to reuse:
- `tests/15-claude-mcp-merge.test.cjs`: `tempConfig(t)` → `{ configDir, env, target }`; `mergeContext(configDir, launcherPath)` builds the ctx with `env: { CLAUDE_CONFIG_DIR: configDir }`.
- `tests/15-gemini-mcp-merge.test.cjs`: `tempConfig(t)` → `{ configDir, target }`; `mergeContext(configDir)` sets `ctx.configDir` directly (Gemini resolves target from `ctx.configDir`).
- `tests/15-mcp-state.test.cjs`: `tempRuntime(t, adapter)` → `{ configDir, env, opts }` where `opts = { flags: { configDir, runtimes: [adapter.name] }, repoRoot, env }`; `targetPath(adapter, configDir)`; register via `installRuntime(adapter, { ...opts, exaMcp: 'register' })`; state at `path.join(configDir, 'oto', '.install.json')` read with `readState`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plain-object guards in Claude + Gemini mergeMcp/unmergeMcp with byte-preserving refusal, plus adapter regressions</name>
  <files>bin/lib/runtime-claude.cjs, bin/lib/runtime-gemini.cjs, tests/15-claude-mcp-merge.test.cjs, tests/15-gemini-mcp-merge.test.cjs</files>
  <read_first>bin/lib/runtime-claude.cjs (mergeMcp/unmergeMcp, lines 185-263), bin/lib/runtime-gemini.cjs (parseSettings + mergeMcp/unmergeMcp, lines 16-107), tests/15-claude-mcp-merge.test.cjs (FULL — tempConfig/mergeContext helpers and assertion style), tests/15-gemini-mcp-merge.test.cjs (FULL — same), .oto/phases/15-exa-mcp-registration-all-three-runtimes/15-VERIFICATION.md (CR-01 fixture table)</read_first>
  <behavior>
    Write the failing tests FIRST (RED), then implement (GREEN). Fixture matrix — for EACH of these seeded target-file contents (written verbatim as the full file text):
    - `{"mcpServers":["user-entry"],"keep":true}` (array container)
    - `{"mcpServers":"user-entry","keep":true}` (string container)
    - `{"mcpServers":null,"keep":true}` (null container)
    - `null` (null root)
    - `[]` (array root)
    - `42` (primitive root)
    - `"user-text"` (string root)
    assert for BOTH adapters:
    - Test 1: `mergeMcp` resolves to exactly `{ registered: false, refused: { reason: 'incompatible-shape' }, entry: null, target }` (assert.deepEqual)
    - Test 2: file bytes after `mergeMcp` are byte-identical to the seeded string (read with `fs.readFileSync(target, 'utf8')` and `assert.equal` against the seeded literal)
    - Test 3: `unmergeMcp` (same ctx, `priorEntry: { command: 'node', args: ['/x'] }`) resolves to `{ removed: false, skipped: { reason: 'absent' }, target }` without throwing, and bytes remain identical
    Gemini only, additionally:
    - Test 4: seeded content `{ not json` → `mergeMcp` resolves to `{ registered: false, refused: { reason: 'unparseable' }, entry: null, target }` and bytes unchanged; `unmergeMcp` resolves to `{ removed: false, skipped: { reason: 'absent' }, target }` without throwing
    Happy-path guard (both adapters):
    - Test 5: all pre-existing tests in both files still pass unchanged (absent file and absent `mcpServers` key still register normally)
  </behavior>
  <action>
Add to BOTH `bin/lib/runtime-claude.cjs` and `bin/lib/runtime-gemini.cjs` (module-level helper, near their respective `buildExaEntry`), with the comment `// OTO Phase 15 gap closure (CR-01): fail closed on non-object roots/containers — refuse, preserve bytes, never record ownership.`:

```js
function isPlainObject(value) {
  return value !== null && typeof value === 'object' && !Array.isArray(value);
}
```

**Claude `mergeMcp`** — insert immediately AFTER the existing `JSON.parse` try/catch (after line 216) and BEFORE `const desired = buildExaEntry(...)`:

```js
if (!isPlainObject(claudeState) ||
    (claudeState.mcpServers !== undefined && !isPlainObject(claudeState.mcpServers))) {
  return { registered: false, refused: { reason: 'incompatible-shape' }, entry: null, target };
}
```

Then REPLACE the destructive container replacement at lines 233-235 with:

```js
if (claudeState.mcpServers === undefined) claudeState.mcpServers = {};
```

**Claude `unmergeMcp`** — insert the same shape guard immediately after its `JSON.parse` try/catch (after line 248), returning `{ removed: false, skipped: { reason: 'absent' }, target };` (exa cannot exist in a non-object container; nothing to remove, nothing written).

**Gemini `mergeMcp`** — replace line 58 (`const settings = parseSettings(existingText || '{}');`) with a try/catch refusal plus shape guard:

```js
let settings;
try {
  settings = parseSettings(existingText || '{}');
} catch {
  return { registered: false, refused: { reason: 'unparseable' }, entry: null, target };
}
if (!isPlainObject(settings) ||
    (settings.mcpServers !== undefined && !isPlainObject(settings.mcpServers))) {
  return { registered: false, refused: { reason: 'incompatible-shape' }, entry: null, target };
}
```

Then REPLACE line 74 (`settings.mcpServers = settings.mcpServers || {};`) with `if (settings.mcpServers === undefined) settings.mcpServers = {};`.

**Gemini `unmergeMcp`** — replace line 89 (`const settings = parseSettings(...)`) with the same try/catch, catch returning `{ removed: false, skipped: { reason: 'absent' }, target };`, followed by the same shape guard also returning `{ removed: false, skipped: { reason: 'absent' }, target };`.

Do NOT touch `mergeSettings`/`unmergeSettings`/`parseSettings` themselves, and do NOT change `bin/lib/install.cjs` — its existing `result.registered` / `result.refused` branches already handle the new refusal (prints `oto: exa MCP registration skipped … (incompatible-shape)` and records no fingerprint).

Tests: append the fixture matrix from `<behavior>` to `tests/15-claude-mcp-merge.test.cjs` and `tests/15-gemini-mcp-merge.test.cjs` using the files' existing `tempConfig`/`mergeContext` helpers, iterating a `const INCOMPATIBLE_FIXTURES = [ ... ]` array of `{ name, text }` entries so each fixture is its own named subtest. Seed via `fs.writeFileSync(target, fixture.text)` (no added trailing newline — byte-identity assertions compare against the exact literal).
  </action>
  <verify>
    <automated>node --test tests/15-claude-mcp-merge.test.cjs tests/15-gemini-mcp-merge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "incompatible-shape" bin/lib/runtime-claude.cjs` ≥ 1 and `grep -c "incompatible-shape" bin/lib/runtime-gemini.cjs` ≥ 1
    - `grep -c "isPlainObject" bin/lib/runtime-claude.cjs` ≥ 3 (definition + merge guard + unmerge guard); same for `bin/lib/runtime-gemini.cjs`
    - The destructive line is gone: `grep -c "claudeState.mcpServers = {}" bin/lib/runtime-claude.cjs` returns exactly 1 and that occurrence is guarded by `=== undefined` (grep the line: `grep -n "mcpServers === undefined" bin/lib/runtime-claude.cjs` non-empty); `grep -c "settings.mcpServers || {}" bin/lib/runtime-gemini.cjs` returns 0
    - `grep -c "user-entry" tests/15-claude-mcp-merge.test.cjs` ≥ 2 and `grep -c "user-entry" tests/15-gemini-mcp-merge.test.cjs` ≥ 2 (array + string container fixtures present verbatim)
    - `node --test tests/15-claude-mcp-merge.test.cjs tests/15-gemini-mcp-merge.test.cjs` exits 0 with zero failures (pre-existing happy-path tests untouched and green)
  </acceptance_criteria>
  <done>Both adapters refuse all seven incompatible-shape fixtures (plus Gemini unparseable) with byte-identical file preservation and never throw; all existing adapter tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Installer-lifecycle regressions — refusal records no .install.json mcp fingerprint</name>
  <files>tests/15-mcp-state.test.cjs</files>
  <read_first>tests/15-mcp-state.test.cjs (FULL — tempRuntime/targetPath helpers, the existing "refuses a user-owned entry without recording MCP state" test at line 169 as the template, and the output-capture pattern in the drift test at lines 146-167), bin/lib/install.cjs (lines 342-395 — mcp dispatch, refusal log line, COMMIT POINT writeState)</read_first>
  <behavior>
    New lifecycle family, claude + gemini adapters only (codex TOML path is refusal-safe already — do NOT add codex cases):
    - For each fixture in `[{ name: 'array-container', text: '{"mcpServers":["user-entry"],"keep":true}' }, { name: 'string-container', text: '{"mcpServers":"user-entry","keep":true}' }, { name: 'null-root', text: 'null' }, { name: 'array-root', text: '[]' }, { name: 'primitive-root', text: '42' }]`:
      1. seed the runtime target file with `fixture.text`, then `await installRuntime(adapter, { ...opts, exaMcp: 'register' })` completes WITHOUT throwing
      2. `readState(statePath).mcp === undefined` (no fingerprint recorded; overall state still schema-valid: `validateState(readState(statePath))` deep-equals `[]`)
      3. target file bytes read back equal `fixture.text` exactly
      4. captured stdout matches `/exa MCP registration skipped/` (reuse the console.log capture pattern from the drift test)
  </behavior>
  <action>
Append to `tests/15-mcp-state.test.cjs` a new loop `for (const adapter of [claudeAdapter, geminiAdapter])` over the fixture array above (declare it as `const INCOMPATIBLE_LIFECYCLE_FIXTURES = [...]` verbatim from `<behavior>`), one `test(\`${adapter.name} lifecycle refuses ${fixture.name} without recording MCP state or touching bytes\`, ...)` per adapter×fixture. Inside each test:

1. `const { configDir, opts } = tempRuntime(t, adapter);`
2. `const target = targetPath(adapter, configDir); fs.mkdirSync(path.dirname(target), { recursive: true }); fs.writeFileSync(target, fixture.text);`
3. Capture `console.log` into an array (copy the try/finally restore pattern from the drift test at lines 151-164; stderr capture not needed here).
4. `await installRuntime(adapter, { ...opts, exaMcp: 'register' });` inside the try.
5. Assert `readState(path.join(configDir, 'oto', '.install.json')).mcp === undefined` via `assert.equal(state.mcp, undefined)` and `assert.deepEqual(validateState(state), [])`.
6. Assert `assert.equal(fs.readFileSync(target, 'utf8'), fixture.text)` (byte identity).
7. Assert `assert.match(output.join('\n'), /exa MCP registration skipped/)`.

This test file already imports `installRuntime`, `readState`, `validateState`, and the adapters — no new imports beyond what exists. Note the mcp dispatch in `install.cjs` runs BEFORE the COMMIT POINT `writeState`, so a throwing adapter would previously have aborted the whole install; these tests also pin the no-throw behavior.
  </action>
  <verify>
    <automated>node --test tests/15-mcp-state.test.cjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INCOMPATIBLE_LIFECYCLE_FIXTURES" tests/15-mcp-state.test.cjs` ≥ 2 (declaration + loop use)
    - `grep -c "registration skipped" tests/15-mcp-state.test.cjs` ≥ 1
    - 10 new lifecycle tests exist (2 adapters × 5 fixtures): `node --test tests/15-mcp-state.test.cjs` output shows them passing, exits 0
    - `npm test` exits 0 (full CJS suite green — no regression anywhere in the installer path)
  </acceptance_criteria>
  <done>Full installer lifecycle proven to refuse every CR-01 fixture without throwing, without recording an mcp fingerprint, and without changing a single byte of the seeded runtime config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-editable runtime config → adapter mutation | `.claude.json` / `settings.json` are user-owned files; oto writes into them only under proven-safe shapes |
| Adapter result → installer ownership state | `registered: true` authorizes a fingerprint in `.install.json` that later authorizes deletion at uninstall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-33 | Tampering | destructive overwrite of user `mcpServers` content (array/string/null replaced by `{}`) in runtime-claude.cjs | mitigate | Plain-object guard refuses before any mutation; byte-identity regressions per fixture |
| T-15-34 | Spoofing | phantom `registered: true` recording an `.install.json` fingerprint for a registration that does not exist (later enabling unintended uninstall action) | mitigate | Refusal path returns `registered: false`; lifecycle tests assert `state.mcp === undefined` on every fixture |
| T-15-35 | Denial of Service | adapter throw on `null`/primitive roots aborting the entire install before the COMMIT POINT | mitigate | Guards return structured refusals instead of throwing; lifecycle tests pin no-throw + schema-valid state |
| T-15-36 | Repudiation | Claude/Gemini refusal behavior diverging from the Codex refusal contract (D-13 uniform-refusal decision) | mitigate | Identical result shapes (`refused.reason` strings) and identical fixture matrix across both JSON adapters |
</threat_model>

<verification>
- `node --test tests/15-claude-mcp-merge.test.cjs tests/15-gemini-mcp-merge.test.cjs tests/15-mcp-state.test.cjs` green
- `npm test` green
- Manual spot-check of the exact CR-01 repro: seed a temp dir's `.claude.json` with `{"mcpServers":["user-entry"],"keep":true}`, call `mergeMcp`, confirm refusal + identical bytes
</verification>

<success_criteria>
- CR-01 closed: no valid-JSON shape can cause the Claude or Gemini adapter to destroy user content, throw, or report a registration that did not happen; the installer records ownership only for real registrations. VERIFICATION.md's eight reproduced failure modes all covered by regressions.
</success_criteria>

<output>
After completion, create `.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-11-SUMMARY.md`
</output>
