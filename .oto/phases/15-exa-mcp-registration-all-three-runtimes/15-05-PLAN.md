---
phase: 15-exa-mcp-registration-all-three-runtimes
plan: 05
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - bin/lib/runtime-claude.cjs
  - tests/15-claude-mcp-merge.test.cjs
autonomous: true
requirements: [MCP-03, HARD-02]
must_haves:
  truths:
    - "After merge, exa appears in the resolved .claude.json ($CLAUDE_CONFIG_DIR/.claude.json when the env var is set, else $HOME/.claude.json) via additive JSON merge — every other key preserved"
    - "A user-owned mcpServers.exa entry causes refusal; the file is left untouched"
    - "Re-running merge never duplicates or mutates anything beyond mcpServers.exa (idempotent)"
    - "An unparseable .claude.json is never rewritten"
  artifacts:
    - path: "bin/lib/runtime-claude.cjs"
      provides: "claudeJsonPath resolution + mergeMcp/unmergeMcp adapter hooks"
      exports: ["mergeMcp", "unmergeMcp", "claudeJsonPath"]
    - path: "tests/15-claude-mcp-merge.test.cjs"
      provides: "HARD-02 round-trip family for Claude (both path branches, additive preservation, refusal, drift)"
  key_links:
    - from: "bin/lib/runtime-claude.cjs"
      to: "$CLAUDE_CONFIG_DIR/.claude.json | $HOME/.claude.json"
      via: "env-based resolution (NOT ctx.configDir)"
      pattern: "CLAUDE_CONFIG_DIR"
---

<objective>
Add `mergeMcp`/`unmergeMcp` to the Claude adapter targeting the resolved `.claude.json` — a DIFFERENT file than `settingsFilename` (which is why this is a new effectful hook, not a `mergeSettings` extension). Additive JSON merge touching only `mcpServers.exa`, refusal on user-owned entries, drift-safe unmerge.

Purpose: MCP-03 — user-scope registration in `~/.claude.json` via additive merge, never CLI shell-outs. Implements the confirmed D-12 resolution: `$CLAUDE_CONFIG_DIR/.claude.json` when set, else `$HOME/.claude.json`.
Output: Claude adapter MCP hooks + green round-trip tests.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@decisions/ADR-16-exa-mcp-transport.md
@.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-RESEARCH.md

<interfaces>
`bin/lib/runtime-claude.cjs` adapter export: `{ name: 'claude', configDirEnvVar: 'CLAUDE_CONFIG_DIR', defaultConfigDirSegment: '.claude', settingsFilename: 'settings.json', mergeSettings, unmergeSettings, ... }`. It has a JSONC-tolerant `parseSettings(text)` at ~52-66 — but `.claude.json` is strict JSON written by Claude Code itself; use strict `JSON.parse` for it.

`bin/lib/args.cjs` exports `expandTilde(p)` — reuse for `CLAUDE_CONFIG_DIR` values like `~/custom`.

Entry to write (the fingerprint unit for Claude):
```json
{ "type": "stdio", "command": "node", "args": ["<ctx.launcherPath>"] }
```

CRITICAL resolution nuance (verified, claude-code#14313): when `CLAUDE_CONFIG_DIR` is unset, `.claude.json` sits at `$HOME/.claude.json` — OUTSIDE the configDir the installer resolved. So `mergeMcp` MUST resolve from `ctx.env`, never from `ctx.configDir`. When the user passes `--config-dir` without the env var, Claude Code itself still reads `$HOME/.claude.json` — resolve from env only (Open Question 3 resolution; the install report may note this).

Ownership rule: NO `_oto` breadcrumb inside `.claude.json` (it is Claude Code's live mutable state file, holding project history/onboarding). Ownership lives exclusively in `.install.json` (Plan 07).

## Shared mergeMcp/unmergeMcp adapter contract (identical wording in Plans 04/05/06 — Plan 07's dispatcher relies on it)

```
async mergeMcp(ctx) -> { registered: boolean, refused: null | { reason: string, existing?: any },
                         entry: <fingerprint unit> | null, target: <absolute path written/inspected> }
ctx = { configDir, env, otoVersion, launcherPath, priorEntry }

async unmergeMcp(ctx) -> { removed: boolean, skipped: null | { reason: 'absent' | 'user-owned' | 'drifted' }, target }
ctx = { configDir, env, otoVersion, priorEntry }
```
Merge decision matrix (D-13 / MCP-07):
- no existing exa entry → write, `registered: true`, `entry` = what was written
- existing entry matches priorEntry OR matches what oto would write → rewrite in place (idempotent refresh), `registered: true`
- existing entry, no match → `refused: { reason: 'user-owned' }`, file untouched
Unmerge decision matrix (D-14 / MCP-08):
- no live entry → `{ removed: false, skipped: { reason: 'absent' } }`
- live entry equals priorEntry → remove it, `{ removed: true }`
- live entry differs (or priorEntry null) → skip, reason 'drifted' / 'user-owned', one stderr line: `oto: exa entry was modified since oto registered it — left in place.`

For Claude/Gemini the fingerprint unit is the parsed entry object; equality = deep-equal via key-sorted stable stringify.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: claudeJsonPath resolution + mergeMcp</name>
  <files>bin/lib/runtime-claude.cjs, tests/15-claude-mcp-merge.test.cjs</files>
  <read_first>bin/lib/runtime-claude.cjs (FULL file), bin/lib/args.cjs (expandTilde), tests/05-merge-settings.test.cjs (test shape), decisions/ADR-16-exa-mcp-transport.md</read_first>
  <behavior>
    - `claudeJsonPath({ CLAUDE_CONFIG_DIR: '/tmp/x' })` → `/tmp/x/.claude.json`; `claudeJsonPath({}, '/home/u')` → `/home/u/.claude.json`; `~/custom` values expand via expandTilde
    - merge into an absent file creates strict JSON `{ "mcpServers": { "exa": { "type": "stdio", "command": "node", "args": [launcherPath] } } }`
    - merge into a rich fixture (top-level `projects`, `onboarding`, `numStartups`, existing `mcpServers.other`) preserves every other key deep-equal; only `mcpServers.exa` added
    - existing user-owned exa entry (different shape) → `refused.reason === 'user-owned'`, file bytes unchanged
    - existing entry deep-equal to what oto would write → `registered: true`, no duplicate, second run stable
    - corrupt JSON file → refused (reason 'unparseable'), file bytes unchanged
  </behavior>
  <action>
In `bin/lib/runtime-claude.cjs`:
1. Add `function claudeJsonPath(env, homeDir = os.homedir())`: `env.CLAUDE_CONFIG_DIR` set → `path.join(expandTilde(env.CLAUDE_CONFIG_DIR), '.claude.json')`; else `path.join(homeDir, '.claude.json')`. Require `expandTilde` from `./args.cjs`.
2. Add `function stableStringify(value)` (key-sorted recursive JSON.stringify, ~8 lines) and `entriesEqual(a, b)` = `stableStringify(a) === stableStringify(b)`.
3. Add `function buildExaEntry(launcherPath)` → `{ type: 'stdio', command: 'node', args: [launcherPath] }`.
4. Add `async mergeMcp(ctx)` implementing the contract: `target = claudeJsonPath(ctx.env || process.env)`; read (absent → `'{}'`); STRICT `JSON.parse` in try/catch — parse failure → `{ registered: false, refused: { reason: 'unparseable' }, entry: null, target }` with NO write (never rewrite a file we cannot parse); `desired = buildExaEntry(ctx.launcherPath)`; existing `obj.mcpServers?.exa`: absent → set (creating `mcpServers` if needed); equals desired or equals ctx.priorEntry → set to desired (idempotent refresh); else → refused 'user-owned' with `existing` included, no write. On write: read-modify-write in one tick, touch ONLY `mcpServers.exa`, serialize `JSON.stringify(obj, null, 2)` (matches Claude Code's own format — Pitfall 4).
5. Add `async unmergeMcp(ctx)` per the contract matrix: parse target (absent/unparseable → skipped 'absent'); live entry equals `ctx.priorEntry` → `delete obj.mcpServers.exa` (leave the `mcpServers` object itself in place), write, `{ removed: true }`; else skip with the one-line stderr notice.
6. Export `mergeMcp`, `unmergeMcp`, `claudeJsonPath` on the adapter object. Comment: `// OTO Phase 15 (MCP-03): MCP hooks target .claude.json (NOT settingsFilename) — resolution is env-based, see decisions/ADR-16 + claude-code#14313.`

Write `tests/15-claude-mcp-merge.test.cjs` FIRST covering the behavior list; use temp dirs with `ctx.env = { CLAUDE_CONFIG_DIR: tmpDir }` (never touch the real `~/.claude.json`), and one homeDir-branch unit test against `claudeJsonPath` directly.
  </action>
  <verify>
    <automated>node --test tests/15-claude-mcp-merge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `bin/lib/runtime-claude.cjs` exports `mergeMcp`, `unmergeMcp`, `claudeJsonPath`; contains `CLAUDE_CONFIG_DIR` and `'.claude.json'`
    - `grep -c "_oto" bin/lib/runtime-claude.cjs` = 0 in the new MCP code (no breadcrumb in .claude.json)
    - Preservation test asserts deep-equality of all non-mcpServers keys after merge
    - Refusal + unparseable tests assert the file bytes are unchanged (`assert.equal(fs.readFileSync(target,'utf8'), before)`)
    - `node --test tests/15-claude-mcp-merge.test.cjs` exits 0
  </acceptance_criteria>
  <done>Claude mergeMcp/unmergeMcp implement the shared contract with env-based path resolution; both branches and all conflict behaviors test-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-trip + drift family completes the Claude HARD-02 coverage</name>
  <files>tests/15-claude-mcp-merge.test.cjs</files>
  <read_first>bin/lib/runtime-claude.cjs (implementation from Task 1)</read_first>
  <behavior>
    - round-trip: merge → unmerge(priorEntry = returned entry) restores an object graph deep-equal to the original fixture
    - drift: merge, then hand-edit the exa entry (change args), then unmerge → `skipped.reason === 'drifted'`, entry left in place
    - user-owned unmerge: live exa entry present but priorEntry null → `skipped.reason === 'user-owned'`
    - double-merge stability: two consecutive merges → byte-identical file after each
  </behavior>
  <action>
Extend `tests/15-claude-mcp-merge.test.cjs` with the four cases above (temp dirs, env-branch). Assert the drift notice goes to stderr by capturing `process.stderr.write` or by spawning — capturing via a temporary monkey-patch in the test is acceptable and matches existing suite conventions (check how tests/14-* capture stderr and reuse that pattern).
  </action>
  <verify>
    <automated>node --test tests/15-claude-mcp-merge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains named tests matching /round-trip/, /drift/, /user-owned/, /idempotent|double-merge/
    - `node --test tests/15-claude-mcp-merge.test.cjs` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Claude HARD-02 round-trip family complete and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oto installer → user's `.claude.json` | Claude Code's live mutable state file (project history, onboarding) — data loss here is user-visible and unrecoverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | clobbering unknown keys / user MCP servers in .claude.json | mitigate | Additive merge touching only `mcpServers.exa`; strict-parse-or-refuse (never rewrite unparseable); preservation test asserts all other keys survive deep-equal |
| T-15-15 | Tampering | removing a user-owned/modified exa entry on unmerge | mitigate | Deep-equal fingerprint match required before delete; drift/user-owned → skip + report (D-14) |
| T-15-16 | Repudiation | registration landing in a file Claude ignores (CLAUDE_CONFIG_DIR relocation) | mitigate | Env-based resolution per verified #14313 behavior; both branches unit-tested; manual probe scheduled in Plan 10 checkpoint (Assumption A1) |
| T-15-17 | Denial of Service | concurrent Claude session rewriting .claude.json mid-merge | accept | Read-modify-write in one tick; known small window documented (personal-tool accept, Pitfall 4) |
</threat_model>

<verification>
- `node --test tests/15-claude-mcp-merge.test.cjs` green (both path branches, preservation, refusal, drift, round-trip)
- `npm test` green
- No test touches the real `~/.claude.json`
</verification>

<success_criteria>
- Claude registration is additive, env-resolved, idempotent, refusal-safe, drift-safe; HARD-02 Claude family green
</success_criteria>

<output>
After completion, create `.oto/phases/15-exa-mcp-registration-all-three-runtimes/15-05-SUMMARY.md`
</output>
