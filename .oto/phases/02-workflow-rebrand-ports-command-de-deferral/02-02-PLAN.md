---
phase: 02-workflow-rebrand-ports-command-de-deferral
phase_number: 2
plan: 02
plan_id: 02-02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03]
requirements_addressed: [CMD-01, CMD-02, CMD-03]
files_modified:
  - tests/workflow-no-deferral-marker.test.cjs

must_haves:
  truths:
    - "oto/commands/oto/ingest-docs.md contains zero `deferred` / `DEFERRED` / `intentionally non-executable` markers (CMD-01 — already clean per research; assertion test locks the contract)"
    - "oto/commands/oto/eval-review.md contains zero `deferred` / `DEFERRED` / `intentionally non-executable` markers (CMD-02 — already clean per research; assertion test locks the contract)"
    - "oto/commands/INDEX.md rows for `/oto-ingest-docs` and `/oto-eval-review` do not contain the substring `[deferred]` (CMD-03 part A — already clean per research; assertion test locks the contract)"
    - "oto/workflows/help.md does not contain command-level deferral framing (e.g., `intentionally non-executable`) anywhere, and has no `[deferred]` tag or v2-reactivation footnote adjacent to either `/oto-ingest-docs` or `/oto-eval-review` mention (CMD-03 part B/C — already clean per research; global+conditional assertion test locks the contract)"
    - "tests/workflow-no-deferral-marker.test.cjs runs under `node --test` and asserts all four conditions above; if any future edit reintroduces a deferral marker for either command, this test fails"
  artifacts:
    - path: "tests/workflow-no-deferral-marker.test.cjs"
      provides: "Regression-guard test for CMD-01, CMD-02, CMD-03 — fails if any deferral marker reappears for either command"
      contains: "no deferral framing"
      min_lines: 40
  key_links:
    - from: "tests/workflow-no-deferral-marker.test.cjs"
      to: "oto/commands/oto/ingest-docs.md"
      via: "fs.readFileSync + assert no deferral markers (CMD-01)"
      pattern: "ingest-docs"
    - from: "tests/workflow-no-deferral-marker.test.cjs"
      to: "oto/commands/oto/eval-review.md"
      via: "fs.readFileSync + assert no deferral markers (CMD-02)"
      pattern: "eval-review"
    - from: "tests/workflow-no-deferral-marker.test.cjs"
      to: "oto/commands/INDEX.md"
      via: "fs.readFileSync + assert no `[deferred]` on lines mentioning either command (CMD-03 part A)"
      pattern: "INDEX\\.md"
    - from: "tests/workflow-no-deferral-marker.test.cjs"
      to: "oto/workflows/help.md"
      via: "fs.readFileSync + assert no command-level deferral framing globally + no `[deferred]` tag adjacent to either command (CMD-03 part B)"
      pattern: "help\\.md"
---

<objective>
Lock the CMD-01 / CMD-02 / CMD-03 contracts with a regression-guard test. The command files (`oto/commands/oto/ingest-docs.md`, `oto/commands/oto/eval-review.md`) and the listing files (`oto/commands/INDEX.md`, `oto/workflows/help.md`) are **already clean** of deferral framing for these two commands per the research probe — no source-tree edits required. The risk is regression: a future plan, rebrand sync, or hand-edit could reintroduce a `[deferred]` tag, an "intentionally non-executable" refusal, or a v2-reactivation footnote. A single `node --test` file asserting all four contracts will fail-fast on any such regression.

Purpose: Lock CMD-01/02/03 against silent regression. Parallel to Plan 02-01 (this plan does not touch the workflow bodies; can run in Wave 1).
Output: One new test file (`tests/workflow-no-deferral-marker.test.cjs`) wiring into `npm test`.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md
@CLAUDE.md

# Files under assertion (audited clean during research; this plan locks them)
@oto/commands/oto/ingest-docs.md
@oto/commands/oto/eval-review.md
@oto/commands/INDEX.md
@oto/workflows/help.md

# Test-style precedent (node:test built-in pattern)
@tests/phase-04-frontmatter-schema.test.cjs
@tests/phase-04-no-dropped-agents.test.cjs

<interfaces>
<!-- Existing repo test pattern (node:test built-in, CJS, zero deps): -->

```javascript
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = path.resolve(__dirname, '..');
const FILE_CONTENT = fs.readFileSync(path.join(ROOT, 'some/file.md'), 'utf8');

test('TEST-ID: description', () => {
  assert.ok(condition, 'failure message');
});
```

<!-- Known unrelated `deferred` hits to NOT trip on (research-confirmed): -->
<!--   - oto/workflows/help.md line 428: `/oto-plant-seed` description (unrelated v2 command) -->
<!--   - oto/commands/INDEX.md line 11: `/oto-ai-integration-phase` description (unrelated) -->
<!-- The test must scope its line-iterator assertions to text adjacent to `/oto-ingest-docs` and `/oto-eval-review` references, NOT global file-wide grep for "deferred". -->
<!-- BUT: a NON-VACUOUS global assertion on command-level deferral framing -->
<!-- ("intentionally non-executable") is added in addition, to guard help.md regardless -->
<!-- of whether the two scoped commands are currently listed. -->

<!-- Deferral markers to forbid (research's list, CMD-01/02 scope): -->
<!--   - "deferred" (case-insensitive) -->
<!--   - "DEFERRED" -->
<!--   - "intentionally non-executable" (case-insensitive) -->
<!--   - "[deferred]" — specifically the bracketed tag form, for CMD-03 line-adjacent check -->
<!--   - "v2 reactivation" / "reactivation criterion" — footnote indicator -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit-confirm clean state, then write tests/workflow-no-deferral-marker.test.cjs locking CMD-01, CMD-02, CMD-03</name>
  <files>tests/workflow-no-deferral-marker.test.cjs</files>
  <read_first>
    - oto/commands/oto/ingest-docs.md (full file — confirm no `deferred`/`DEFERRED`/`intentionally non-executable` markers before writing the assertion)
    - oto/commands/oto/eval-review.md (full file — confirm no markers)
    - oto/commands/INDEX.md (full file — note lines 27 and 35 are the rows for `/oto-eval-review` and `/oto-ingest-docs` per research; line 11 has an unrelated `deferred` for `/oto-ai-integration-phase` to NOT trip on)
    - oto/workflows/help.md (full file — research notes line 428 has unrelated `/oto-plant-seed` `deferred` mention; the two scoped commands are NOT listed per-command in help.md, so CMD-03B/C use a NON-VACUOUS global assertion on "intentionally non-executable" PLUS conditional forward-compat guards on per-command rows if either is ever added)
    - tests/phase-04-frontmatter-schema.test.cjs (test-style precedent — `require('node:test')`, `require('node:assert/strict')`, `fs.readFileSync`)
    - tests/phase-04-no-dropped-agents.test.cjs (test-style precedent — similar shape)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md (Pitfall 6 — CMD-03 mis-scoped; treat as assertion-only)
  </read_first>
  <action>
    **Part A — Pre-flight audit.** Confirm the four files are already clean. Run:

    ```bash
    grep -ciE 'deferred|intentionally non-executable' oto/commands/oto/ingest-docs.md oto/commands/oto/eval-review.md
    ```

    Expected: both file counts equal `0`. If either is non-zero, the research-claimed clean state is wrong — HALT and surface the finding. Do NOT proceed to write the assertion test against a known-failing baseline.

    Then check the two listing files for `[deferred]` adjacent to the scoped commands. The two scoped commands are `/oto-ingest-docs` and `/oto-eval-review`. The two known unrelated `deferred` hits (per research) are at INDEX.md line 11 (`/oto-ai-integration-phase`) and help.md line 428 (`/oto-plant-seed`). Confirm there is no `[deferred]` on the same line as either scoped command:

    ```bash
    grep -n '/oto-ingest-docs\|/oto-eval-review' oto/commands/INDEX.md oto/workflows/help.md | grep -i 'deferred\|\[deferred\]'
    ```

    Expected: no output. If output appears, HALT — that's a new regression and the test would fail on first run.

    Confirm help.md does not currently contain the command-level deferral phrase anywhere (global assertion baseline):

    ```bash
    grep -c -i 'intentionally non-executable' oto/workflows/help.md
    # must equal 0 — the global assertion in CMD-03B will lock this contract
    ```

    **Part B — Write the assertion test.** Create `tests/workflow-no-deferral-marker.test.cjs` with the following structure. Use the exact code below as the implementation, modeled on `tests/phase-04-frontmatter-schema.test.cjs` style (require `node:test` + `node:assert/strict` + `node:fs` + `node:path`; no external deps).

    File body (write verbatim — these are the locked assertion strings, derived from research's Phase Requirements → Test Map):

    ```javascript
    // tests/workflow-no-deferral-marker.test.cjs
    // Locks CMD-01, CMD-02, CMD-03 from .planning/REQUIREMENTS.md (v0.3.0).
    // Phase 2 made no source-tree edits to these files — they were already clean per
    // .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md.
    // This test fails if any future change reintroduces a deferral marker for either command.
    'use strict';

    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');

    const ROOT = path.resolve(__dirname, '..');

    const INGEST_CMD = fs.readFileSync(path.join(ROOT, 'oto/commands/oto/ingest-docs.md'), 'utf8');
    const EVAL_CMD = fs.readFileSync(path.join(ROOT, 'oto/commands/oto/eval-review.md'), 'utf8');
    const INDEX_MD = fs.readFileSync(path.join(ROOT, 'oto/commands/INDEX.md'), 'utf8');
    const HELP_MD = fs.readFileSync(path.join(ROOT, 'oto/workflows/help.md'), 'utf8');

    // ---- CMD-01: /oto-ingest-docs command file has no deferral framing ----

    test('CMD-01: oto/commands/oto/ingest-docs.md has no deferral framing', () => {
      assert.ok(
        !/intentionally non-executable/i.test(INGEST_CMD),
        'ingest-docs.md must not contain "intentionally non-executable" refusal'
      );
      assert.ok(
        !/\bdeferred\b/i.test(INGEST_CMD),
        'ingest-docs.md must not contain the word "deferred"'
      );
      assert.ok(
        !/DEFERRED/.test(INGEST_CMD),
        'ingest-docs.md must not contain the uppercase DEFERRED marker'
      );
    });

    // ---- CMD-02: /oto-eval-review command file has no deferral framing ----

    test('CMD-02: oto/commands/oto/eval-review.md has no deferral framing', () => {
      assert.ok(
        !/intentionally non-executable/i.test(EVAL_CMD),
        'eval-review.md must not contain "intentionally non-executable" refusal'
      );
      assert.ok(
        !/\bdeferred\b/i.test(EVAL_CMD),
        'eval-review.md must not contain the word "deferred"'
      );
      assert.ok(
        !/DEFERRED/.test(EVAL_CMD),
        'eval-review.md must not contain the uppercase DEFERRED marker'
      );
    });

    // ---- CMD-03 part A: INDEX.md rows for /oto-ingest-docs and /oto-eval-review have no [deferred] tag ----

    test('CMD-03A: oto/commands/INDEX.md rows for the two commands have no [deferred] tag', () => {
      const lines = INDEX_MD.split('\n');
      const scoped = lines.filter(
        (l) => l.includes('/oto-ingest-docs') || l.includes('/oto-eval-review')
      );
      assert.ok(
        scoped.length >= 2,
        `expected at least 2 INDEX.md rows mentioning /oto-ingest-docs or /oto-eval-review, got ${scoped.length}`
      );
      for (const line of scoped) {
        assert.ok(
          !/\[deferred\]/i.test(line),
          `INDEX.md row must not contain [deferred] tag: ${line}`
        );
      }
    });

    // ---- CMD-03 part B: help.md has no command-level deferral framing (global, non-vacuous) ----
    // ---- and no [deferred] tag adjacent to either scoped command (forward-compat guard).  ----
    //
    // DESIGN NOTE: help.md by design lists no per-command rows for /oto-ingest-docs
    // or /oto-eval-review (research-confirmed during Phase 2 audit). The line-iterator
    // assertions below are conditional and would only fire if either command is ever
    // added as a help.md row in a future commit. To make THIS test non-vacuous today,
    // we ALSO assert globally on help.md content that the upstream
    // "intentionally non-executable" command-level deferral framing never reappears
    // anywhere in help.md, regardless of which commands are currently listed.

    test('CMD-03B: oto/workflows/help.md has no command-level deferral framing (global) and no [deferred] tag adjacent to either scoped command (forward-compat guard)', () => {
      // Non-vacuous global assertion — fires regardless of whether the two commands
      // are listed in help.md. Locks help.md against regaining upstream deferral framing.
      assert.ok(
        !/intentionally non-executable/i.test(HELP_MD),
        'help.md must not contain "intentionally non-executable" command-level deferral framing'
      );

      // Forward-compatibility guard: if either scoped command is ever added as a
      // help.md row, fail on adjacent [deferred] tags. Today this loop body does not
      // execute because help.md has no /oto-ingest-docs or /oto-eval-review rows.
      const lines = HELP_MD.split('\n');
      lines.forEach((line, idx) => {
        if (line.includes('/oto-ingest-docs') || line.includes('/oto-eval-review')) {
          // Same line check
          assert.ok(
            !/\[deferred\]/i.test(line),
            `help.md line ${idx + 1} mentions a scoped command and contains [deferred]: ${line}`
          );
          // Window check: 5 lines before / 5 after must not contain [deferred] tag
          const start = Math.max(0, idx - 5);
          const end = Math.min(lines.length, idx + 6);
          const window = lines.slice(start, end).join('\n');
          assert.ok(
            !/\[deferred\]/i.test(window),
            `help.md lines ${start + 1}-${end} include a scoped command mention and a [deferred] tag within 5 lines`
          );
        }
      });
    });

    // ---- CMD-03 part C: help.md has no v2-reactivation footnote tied to either command ----
    //
    // DESIGN NOTE: same as CMD-03B — the per-command line-iterator block below is
    // conditional and acts as a forward-compatibility guard. A non-vacuous global
    // assertion (string-co-occurrence within 500 chars) guards against a
    // "reactivation criterion" footnote getting tied to either scoped command's
    // framing anywhere in help.md, regardless of whether the commands are listed
    // line-by-line. We deliberately allow "reactivation criterion" to appear in
    // help.md for unrelated commands (e.g., /oto-plant-seed line 428) — the
    // co-occurrence check only fires when paired with /oto-ingest-docs or
    // /oto-eval-review in the same 500-char window.

    test('CMD-03C: oto/workflows/help.md has no v2-reactivation footnote co-occurring with either scoped command (global) or adjacent to a per-command row (forward-compat guard)', () => {
      // Non-vacuous global co-occurrence assertion: no "reactivation criterion"
      // footnote should appear within 500 chars of either scoped command mention.
      assert.ok(
        !/(oto-ingest-docs|oto-eval-review)[\s\S]{0,500}reactivation criterion/i.test(HELP_MD),
        'help.md must not contain a "reactivation criterion" footnote within 500 chars after an ingest-docs/eval-review mention'
      );
      assert.ok(
        !/reactivation criterion[\s\S]{0,500}(oto-ingest-docs|oto-eval-review)/i.test(HELP_MD),
        'help.md must not contain a "reactivation criterion" footnote within 500 chars before an ingest-docs/eval-review mention'
      );

      // Forward-compatibility guard, line-window scoped. Conditional — fires only
      // if either scoped command is ever added as a help.md row.
      const lines = HELP_MD.split('\n');
      lines.forEach((line, idx) => {
        if (line.includes('/oto-ingest-docs') || line.includes('/oto-eval-review')) {
          const start = Math.max(0, idx - 5);
          const end = Math.min(lines.length, idx + 11);
          const window = lines.slice(start, end).join('\n');
          assert.ok(
            !/reactivation criterion/i.test(window),
            `help.md lines ${start + 1}-${end} mention a scoped command and contain a "reactivation criterion" footnote`
          );
          assert.ok(
            !/\bv2\s+reactivation\b/i.test(window),
            `help.md lines ${start + 1}-${end} mention a scoped command and contain a "v2 reactivation" footnote`
          );
        }
      });
    });
    ```

    Write this file verbatim using the Write tool. Do NOT modify the assertion strings — they map 1:1 to research's "Phase Requirements → Test Map" Wave 0 gap list (with CMD-03B/C upgraded to non-vacuous global + conditional forward-compat shape per Phase 2 revision iter 1, fix W1).

    **Part C — Run the test in isolation to confirm green.** Execute:

    ```bash
    node --test tests/workflow-no-deferral-marker.test.cjs
    ```

    Expected output: `pass: 5` (or `tests: 5` with `pass: 5` and `fail: 0`). If any test fails, the pre-flight audit missed something — HALT, re-audit, fix the source-tree state if needed, and re-run.

    **Part D — Confirm `npm test` integration.** The repo's `scripts/run-tests.cjs` auto-discovers `tests/*.test.cjs` via `node --test`; no additional wiring needed. Confirm:

    ```bash
    npm test 2>&1 | tail -20
    ```

    Expected: `fail: 0` remains. (Absolute pass-count deltas are not asserted — `npm test`'s baseline drifts as other phases land, and tying acceptance to a specific number is fragile.)
  </action>
  <verify>
    <automated>test -f tests/workflow-no-deferral-marker.test.cjs && [ $(wc -l < tests/workflow-no-deferral-marker.test.cjs) -gt 40 ] && grep -q "CMD-01" tests/workflow-no-deferral-marker.test.cjs && grep -q "CMD-02" tests/workflow-no-deferral-marker.test.cjs && grep -q "CMD-03A" tests/workflow-no-deferral-marker.test.cjs && grep -q "CMD-03B" tests/workflow-no-deferral-marker.test.cjs && grep -q "CMD-03C" tests/workflow-no-deferral-marker.test.cjs && grep -q "intentionally non-executable" tests/workflow-no-deferral-marker.test.cjs && grep -q "HELP_MD" tests/workflow-no-deferral-marker.test.cjs && grep -q "must not contain \"intentionally non-executable\" command-level deferral framing" tests/workflow-no-deferral-marker.test.cjs && grep -q "require('node:test')" tests/workflow-no-deferral-marker.test.cjs && node --test tests/workflow-no-deferral-marker.test.cjs 2>&1 | grep -q "fail 0\|fail: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/workflow-no-deferral-marker.test.cjs` exits 0
    - `wc -l < tests/workflow-no-deferral-marker.test.cjs` ≥ 40
    - `grep -c "test('CMD-01" tests/workflow-no-deferral-marker.test.cjs` equals `1`
    - `grep -c "test('CMD-02" tests/workflow-no-deferral-marker.test.cjs` equals `1`
    - `grep -c "test('CMD-03A" tests/workflow-no-deferral-marker.test.cjs` equals `1`
    - `grep -c "test('CMD-03B" tests/workflow-no-deferral-marker.test.cjs` equals `1`
    - `grep -c "test('CMD-03C" tests/workflow-no-deferral-marker.test.cjs` equals `1`
    - `grep -q "intentionally non-executable" tests/workflow-no-deferral-marker.test.cjs` exits 0 (assertion string present)
    - `grep -q "must not contain \"intentionally non-executable\" command-level deferral framing" tests/workflow-no-deferral-marker.test.cjs` exits 0 (CMD-03B global non-vacuous assertion present — locks W1 fix)
    - `grep -q "DESIGN NOTE" tests/workflow-no-deferral-marker.test.cjs` exits 0 (design rationale documented in test file per W1 fix)
    - `grep -q "require('node:test')" tests/workflow-no-deferral-marker.test.cjs` exits 0 (built-in test runner)
    - `grep -q "require('node:assert/strict')" tests/workflow-no-deferral-marker.test.cjs` exits 0
    - `node --test tests/workflow-no-deferral-marker.test.cjs` exits 0 (all 5 sub-tests pass)
    - `npm test` exits 0 and the summary shows `fail 0` (full suite still green)
    - No edits to `oto/commands/oto/ingest-docs.md`, `oto/commands/oto/eval-review.md`, `oto/commands/INDEX.md`, or `oto/workflows/help.md` — `git status --porcelain oto/commands oto/workflows/help.md` lists no modified files in those paths after Task 1 completes (the test is the only artifact)
  </acceptance_criteria>
  <done>
    `tests/workflow-no-deferral-marker.test.cjs` exists with five named tests (CMD-01, CMD-02, CMD-03A, CMD-03B, CMD-03C); all five pass under `node --test`; the full `npm test` suite stays green. CMD-01, CMD-02, CMD-03 are locked: any future regression that reintroduces a deferral marker for either command fails this test on the next `npm test` run. CMD-03B and CMD-03C both include a NON-VACUOUS global assertion (per W1 fix) plus a conditional forward-compat guard documented with a DESIGN NOTE comment block. No source-tree edits to command files or listing files — the research-confirmed clean state is preserved and locked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo only | This plan writes a single test file. No network, no user input, no secrets, no runtime surface. The test reads four shipped markdown files synchronously and runs assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Deferral marker reintroduction in command files or listing files | mitigate | This plan IS the mitigation. The test asserts absence on every `npm test` run; any future commit that reintroduces a deferral marker for either scoped command fails the test before merge. CMD-03B's global non-vacuous assertion also locks help.md against regaining upstream "intentionally non-executable" framing even if the two scoped commands are never re-added as help.md rows. |
| T-02-02-02 | Information Disclosure | tests/workflow-no-deferral-marker.test.cjs shipped contents | accept | Test file is public-by-design (npm install -g would ship it if `files` allowlist permitted; in practice tests are not shipped). No secrets surface. |

Note: No new auth/network/secrets/UI surface. The test itself is the security artifact (regression-guard against contract drift).
</threat_model>

<verification>
- `tests/workflow-no-deferral-marker.test.cjs` exists with five named test cases mapping to CMD-01, CMD-02, CMD-03A, CMD-03B, CMD-03C.
- Test file uses `node:test` and `node:assert/strict` (zero external deps, CJS, matches repo convention).
- CMD-03B contains a NON-VACUOUS global assertion (`!/intentionally non-executable/i.test(HELP_MD)`) that fires regardless of whether either scoped command is currently listed in help.md.
- CMD-03C contains a NON-VACUOUS global co-occurrence assertion that fires if "reactivation criterion" appears within 500 chars of either scoped command anywhere in help.md.
- Both CMD-03B and CMD-03C include a `DESIGN NOTE` comment block documenting why the per-command line-iterator block is conditional (forward-compat guard) and how the global assertion compensates.
- All five tests pass under `node --test tests/workflow-no-deferral-marker.test.cjs`.
- Full `npm test` suite remains green; only `fail: 0` is asserted (absolute pass-count deltas are not gated — fragile to other-phase drift).
- No edits to `oto/commands/oto/{ingest-docs,eval-review}.md`, `oto/commands/INDEX.md`, or `oto/workflows/help.md` — the audited clean state is preserved unchanged.
- Test scopes its per-command line-iterator assertions to text adjacent to `/oto-ingest-docs` and `/oto-eval-review` mentions only (does not trip on unrelated `deferred` hits at INDEX.md line 11 or help.md line 428).
</verification>

<success_criteria>
1. `tests/workflow-no-deferral-marker.test.cjs` exists with five tests (CMD-01, CMD-02, CMD-03A, CMD-03B, CMD-03C) and passes under `node --test`.
2. Full `npm test` suite remains green; `fail: 0` reported.
3. CMD-01, CMD-02, CMD-03 are locked: the test fails on any future reintroduction of a deferral marker for either scoped command. CMD-03B's global non-vacuous assertion locks help.md against regaining "intentionally non-executable" framing regardless of which commands are currently listed.
4. No source-tree edits to command files or listing files (the research-confirmed clean state is preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-02-SUMMARY.md` capturing:
- Pre-flight audit results (grep counts for deferral markers in the four files — all 0)
- Five test names and their corresponding requirement IDs
- Confirmation that CMD-03B contains the non-vacuous global assertion on help.md "intentionally non-executable" framing (per W1 fix in revision iter 1)
- Confirmation that CMD-03B and CMD-03C both contain DESIGN NOTE comment blocks documenting the conditional forward-compat guard pattern (per W1 fix in revision iter 1)
- `node --test tests/workflow-no-deferral-marker.test.cjs` output (pass count)
- `npm test` summary (`fail: 0` confirmed)
- Confirmation no source-tree edits to command/listing files were needed
</output>
</content>
</invoke>