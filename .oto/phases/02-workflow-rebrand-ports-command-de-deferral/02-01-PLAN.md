---
phase: 02-workflow-rebrand-ports-command-de-deferral
phase_number: 2
plan: 01
plan_id: 02-01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WF-ING-01, WF-ING-02, WF-ING-03, WF-ING-04, WF-EVAL-01, WF-EVAL-02]
requirements_addressed: [WF-ING-01, WF-ING-02, WF-ING-03, WF-ING-04, WF-EVAL-01, WF-EVAL-02]
files_modified:
  - oto/workflows/ingest-docs.md
  - oto/workflows/eval-review.md
  - reports/rebrand-dryrun.json
  - reports/rebrand-dryrun.md

must_haves:
  truths:
    - "oto/workflows/ingest-docs.md is the rebrand-ported executable body (~332 LOC), not the ~27-LOC deferral stub"
    - "oto/workflows/eval-review.md is the rebrand-ported executable body (~155 LOC), not the ~25-LOC deferral stub"
    - "Both workflow bodies contain zero `intentionally non-executable` or `DEFERRED` markers"
    - "oto/workflows/ingest-docs.md contains zero `.planning/` or bare `.planning` token occurrences (hand-fixup pass 1 closed the rebrand engine blind spot for prose-embedded paths)"
    - "Every `oto-sdk query` call in both workflow bodies has a `2>/dev/null` or `|| ` fallback (hand-fixup pass 2 — SDK-DEFER-01 tolerance per plan-phase.md precedent)"
    - "The classify_parallel step instructs the orchestrator (workflow itself) to persist oto-doc-classifier return JSON to .oto/intel/classifications/, NOT the agent to write directly (Phase 1 D-04 read-only-agent reshape — classifier is read-only). The upstream agent-writes-directly phrasing has been REPLACED (not augmented) — a regression-guard test in Plan 02-03 asserts ABSENCE of the upstream phrasing."
    - "The eval-review.md auditor-dispatch step instructs the orchestrator to persist oto-eval-auditor return content to .oto/phases/<phase>/EVAL-REVIEW.md, NOT the agent to write directly (Phase 1 D-04 — auditor is read-only). The upstream agent-writes-directly phrasing has been REPLACED (not augmented) — a regression-guard test in Plan 02-03 asserts ABSENCE of the upstream phrasing."
    - "Pre-existing security checks survived: path-traversal `*..*` rejection, realpath containment of SCAN_PATH, BLOCKER-gate `Exit WITHOUT writing` clause"
    - "reports/rebrand-dryrun.json and reports/rebrand-dryrun.md regenerated; zero orphan upstream paths and zero orphan target paths"
  artifacts:
    - path: "oto/workflows/ingest-docs.md"
      provides: "Executable /oto-ingest-docs workflow body (WF-ING-01..04)"
      contains: "<step name=\"discover_docs\">"
      min_lines: 300
    - path: "oto/workflows/eval-review.md"
      provides: "Executable /oto-eval-review workflow body (WF-EVAL-01..02)"
      contains: "oto-eval-auditor"
      min_lines: 130
    - path: "reports/rebrand-dryrun.json"
      provides: "Regenerated coverage manifest with both workflow target paths recognized"
      contains: "oto/workflows/ingest-docs.md"
    - path: "reports/rebrand-dryrun.md"
      provides: "Human-readable coverage manifest (regenerated companion to .json)"
      contains: "workflows/ingest-docs"
  key_links:
    - from: "oto/workflows/ingest-docs.md (classify_parallel step)"
      to: ".oto/intel/classifications/<slug>.json"
      via: "orchestrator (Bash/Write) persists oto-doc-classifier Task return value"
      pattern: "intel/classifications"
    - from: "oto/workflows/ingest-docs.md (synthesize step)"
      to: "oto-doc-synthesizer agent (workspace-write)"
      via: "Task(subagent_type=\"oto-doc-synthesizer\") — synthesizer writes .oto/INGEST-CONFLICTS.md and .oto/intel/SYNTHESIS.md directly"
      pattern: "oto-doc-synthesizer"
    - from: "oto/workflows/eval-review.md (audit step)"
      to: ".oto/phases/<phase>/EVAL-REVIEW.md"
      via: "orchestrator (Write) persists oto-eval-auditor Task return value"
      pattern: "EVAL-REVIEW.md"
    - from: "decisions/file-inventory.json (workflow rows verdict: keep + target_path)"
      to: "oto/workflows/{ingest-docs,eval-review}.md"
      via: "scripts/rebrand.cjs --apply reads inventory, applies rename-map, writes target_path"
      pattern: "workflows/oto"
---

<objective>
Replace the two deferral stubs at `oto/workflows/ingest-docs.md` and `oto/workflows/eval-review.md` with the rebrand-engine-produced executable workflow bodies from upstream GSD (`gsd-doc-classifier`/`gsd-doc-synthesizer`/`gsd-eval-auditor`-dispatching workflows, ~332 LOC and ~155 LOC respectively). Apply three hand-fixup passes that close engine-known blind spots: (1) prose-embedded `.planning/` → `.oto/` in ingest-docs.md (multiple hits — count varies with engine output), (2) `oto-sdk query` calls made tolerant of SDK absence per the `plan-phase.md` precedent (SDK-DEFER-01), (3) classify_parallel and audit steps reshaped so the workflow orchestrator persists classifier/auditor return values (Phase 1 D-04 read-only-agent reshape). Regenerate the coverage manifest. No hand-porting of body content — the rebrand engine remains the single source of truth (CLAUDE.md, Phase 1 D-01).

Purpose: Phase 2 foundation. Plans 02-02 and 02-03 consume these bodies (assertion tests + fixture-tree smoke). Plan 02-03 has `depends_on: [02-01]` for that reason.
Output: Two executable workflow bodies in `oto/workflows/`, regenerated coverage manifest, no orphan paths.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md
@CLAUDE.md

# Phase 1 precedents (read-only-agent reshape; rebrand-then-fixup pattern; D-01 engine-is-truth)
@.planning/phases/01-agent-ports-installer-wiring/01-VERIFICATION.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@.planning/phases/01-agent-ports-installer-wiring/01-01-PLAN.md

# Upstream source (do NOT hand-port; rebrand engine handles tokens)
@foundation-frameworks/get-shit-done-main/get-shit-done/workflows/ingest-docs.md
@foundation-frameworks/get-shit-done-main/get-shit-done/workflows/eval-review.md

# Existing deferral stubs (will be replaced)
@oto/workflows/ingest-docs.md
@oto/workflows/eval-review.md

# SDK-tolerant fallback precedent
@oto/workflows/plan-phase.md

# Rebrand engine entry points
@scripts/rebrand.cjs
@scripts/rebrand/lib/engine.cjs
@scripts/rebrand/lib/manifest.cjs
@rename-map.json
@decisions/file-inventory.json

<interfaces>
<!-- Canonical engine invocation (from research empirical probe; engine refuses to write outside repo root or os.tmpdir). -->
<!-- Use an in-repo --out dir for staging, then copy two target files into oto/workflows/. -->

```bash
node scripts/rebrand.cjs --apply \
  --target foundation-frameworks/get-shit-done-main \
  --owner OTOJulian \
  --out .oto-rebrand-stage
```

<!-- Expected stage output paths after engine apply: -->
<!--   .oto-rebrand-stage/oto/workflows/ingest-docs.md  (~332 LOC) -->
<!--   .oto-rebrand-stage/oto/workflows/eval-review.md  (~155 LOC) -->

<!-- Hand-fixup pass 1: prose .planning/ → .oto/  (multiple hits in ingest-docs.md). -->
<!-- Safe via sed because all hits are inside backticks / path strings in markdown prose; no false positives verified during research. -->
<!-- eval-review.md has zero such hits (upstream doesn't reference .planning/ in its prose). -->
<!-- We do NOT pre-commit to a specific hit count — engine output may vary slightly and a literal-count expectation would anchor the executor toward rubber-stamping a wrong count. The lock is `grep -c '\.planning\b' == 0` after the sed pass. -->

```bash
sed -i '' 's|\.planning/|.oto/|g' oto/workflows/ingest-docs.md
# Lock with: grep -c '\.planning\b' oto/workflows/ingest-docs.md  → must equal 0
```

<!-- Hand-fixup pass 2: SDK-tolerant fallback. Existing precedent from oto/workflows/plan-phase.md: -->
<!--   CONTEXT_WINDOW=$(oto-sdk query config-get context_window 2>/dev/null || echo "200000") -->
<!-- Every oto-sdk query call in both workflows must have `2>/dev/null` or `|| ` tail. -->
<!-- Audit by: grep -n 'oto-sdk query' oto/workflows/{ingest-docs,eval-review}.md -->

<!-- Hand-fixup pass 3 (Phase 1 D-04 read-only-agent reshape, per 01-VERIFICATION.md "Code Review" finding): -->
<!--   Upstream classify_parallel step instructs the gsd-doc-classifier agent itself to write JSON to OUTPUT_DIR. -->
<!--   Phase 1 stripped Write from oto-doc-classifier (Codex sandbox: read-only). -->
<!--   Reshape: agent returns JSON in its Task response; orchestrator writes it to .oto/intel/classifications/<slug>.json. -->
<!-- Same reshape for eval-review.md audit step (oto-eval-auditor is also read-only): -->
<!--   Agent returns EVAL-REVIEW.md content as Task response; orchestrator writes to .oto/phases/<phase>/EVAL-REVIEW.md. -->
<!-- oto-doc-synthesizer is workspace-write (Phase 1 D-04) — synthesize step keeps direct-agent-write semantics for INGEST-CONFLICTS.md and SYNTHESIS.md. -->
<!-- CRITICAL (W3 fix): The reshape REPLACES the upstream agent-writes-directly phrasing, not augments it. -->
<!-- Task 2 Part C pins ONE exact pre/post string per file (run engine first, find the verbatim line). -->
<!-- A regression-guard test in Plan 02-03 asserts ABSENCE of the upstream phrasing in both files. -->
</interfaces>

## Planner notes

This plan follows the researcher's recommended 3-plan breakdown verbatim. Hand-fixup scope is strictly bounded to the three engine blind spots identified in research (prose `.planning/`, SDK-absence tolerance, read-only-agent persistence reshape). All token substitution remains the rebrand engine's responsibility (CLAUDE.md, Phase 1 D-01).

The `oto-sdk` CLI is unavailable in this environment (broken symlink per planning context; STATE.md Phase 1 last-activity note). Tasks therefore use direct file operations + `git` for state changes; no `oto-sdk query ...` invocations appear in any task body.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Run rebrand engine, stage output, copy two workflow bodies into oto/workflows/, regenerate coverage manifest, clean up stage</name>
  <files>oto/workflows/ingest-docs.md, oto/workflows/eval-review.md, reports/rebrand-dryrun.json, reports/rebrand-dryrun.md</files>
  <read_first>
    - oto/workflows/ingest-docs.md (current ~27-LOC deferral stub — confirm before replacement)
    - oto/workflows/eval-review.md (current ~25-LOC deferral stub — confirm before replacement)
    - foundation-frameworks/get-shit-done-main/get-shit-done/workflows/ingest-docs.md (upstream source ~332 LOC)
    - foundation-frameworks/get-shit-done-main/get-shit-done/workflows/eval-review.md (upstream source ~155 LOC)
    - scripts/rebrand.cjs (entry point — confirm --apply / --dry-run / --out flags)
    - decisions/file-inventory.json (confirm rows for `get-shit-done/workflows/ingest-docs.md` and `get-shit-done/workflows/eval-review.md` show `verdict: keep`, `target_path: oto/workflows/...md`, `rebrand_required: true`; research verified these are clean)
  </read_first>
  <action>
    **Part A — Pre-flight: confirm inventory rows are clean.** Run:

    ```bash
    node -e "const j=require('./decisions/file-inventory.json');const want=['get-shit-done/workflows/ingest-docs.md','get-shit-done/workflows/eval-review.md'];for(const p of want){const e=j.entries.find(x=>x.path===p);if(!e||e.verdict!=='keep'||!e.target_path||e.rebrand_required!==true){console.error('FAIL pre-flight inventory row:',p,e);process.exit(1);}}console.log('inventory pre-flight OK');"
    ```

    Expected output: `inventory pre-flight OK`. If this exits non-zero, halt — the inventory rows are not in the expected state and the engine apply will not produce the target files. Do NOT hand-edit inventory in this plan; surface the gap to the user.

    **Part B — Run the rebrand engine in apply mode** with an in-repo `--out` staging dir (engine safety: refuses to write outside repo root or `os.tmpdir()`):

    ```bash
    node scripts/rebrand.cjs --apply \
      --target foundation-frameworks/get-shit-done-main \
      --owner OTOJulian \
      --out .oto-rebrand-stage
    ```

    Expected output (per research empirical probe): `engine: apply — 341 files, 5098 matches, 0 unclassified`. If `0 unclassified` is not reported or the engine errors with `unclassified token` / `allowlist gate`, halt and surface the engine output. Do NOT hand-edit to bypass (D-01 — engine is the single source of truth).

    Confirm the two staged workflow files exist:

    ```bash
    test -f .oto-rebrand-stage/oto/workflows/ingest-docs.md && test -f .oto-rebrand-stage/oto/workflows/eval-review.md
    ```

    **Part C — Copy staged output → shipped location** (Pitfall 3: engine writes to `--out`, not into `oto/` directly; the shipped deferral stubs persist unless explicitly copied):

    ```bash
    cp .oto-rebrand-stage/oto/workflows/ingest-docs.md oto/workflows/ingest-docs.md
    cp .oto-rebrand-stage/oto/workflows/eval-review.md oto/workflows/eval-review.md
    ```

    Verify the deferral stub strings are GONE post-copy (the executable body has zero deferral framing in upstream — pre-Task-2 sanity check before hand-fixup):

    ```bash
    grep -ci 'intentionally non-executable\|DEFERRED\|deferral stub' oto/workflows/ingest-docs.md oto/workflows/eval-review.md
    # expected: 0 (per file, summed across files: 0)
    ```

    **Part D — Regenerate coverage manifest** in dry-run mode (Phase 1 D-08 precedent, applied to Phase 2):

    ```bash
    node scripts/rebrand.cjs --dry-run \
      --target foundation-frameworks/get-shit-done-main \
      --owner OTOJulian
    ```

    This rewrites `reports/rebrand-dryrun.json` and `reports/rebrand-dryrun.md` to include the two new workflow target paths. Confirm:

    ```bash
    grep -q 'oto/workflows/ingest-docs.md' reports/rebrand-dryrun.json && grep -q 'oto/workflows/eval-review.md' reports/rebrand-dryrun.json
    ```

    Then verify zero orphan paths involving the new workflows. Per Phase 1's manifest schema, orphans are reported under `orphans.upstream` and `orphans.target` arrays:

    ```bash
    node -e "const r=require('./reports/rebrand-dryrun.json');const o=r.orphans||{};const wfHits=JSON.stringify(o).match(/workflows\/(ingest-docs|eval-review)/g)||[];if(wfHits.length){console.error('FAIL: workflow orphans:',wfHits);process.exit(1);}console.log('no workflow orphans');"
    ```

    **Part E — Clean up staging dir** (do not leave engine output in the repo tree — Pitfall 5 hygiene):

    ```bash
    rm -rf .oto-rebrand-stage
    ```

    Confirm gone:

    ```bash
    test ! -e .oto-rebrand-stage
    ```

    **Do NOT** modify the workflow body content in this task. Hand-fixups (prose `.planning/`, SDK fallback, read-only-agent reshape) happen in Task 2. Task 1's sole responsibility is engine apply + shipped-tree replacement + manifest regen.
  </action>
  <verify>
    <automated>test -f oto/workflows/ingest-docs.md && test -f oto/workflows/eval-review.md && [ $(wc -l < oto/workflows/ingest-docs.md) -gt 200 ] && [ $(wc -l < oto/workflows/eval-review.md) -gt 100 ] && ! grep -qi 'intentionally non-executable' oto/workflows/ingest-docs.md && ! grep -qi 'intentionally non-executable' oto/workflows/eval-review.md && grep -q '<step name="discover_docs">' oto/workflows/ingest-docs.md && grep -q 'oto-eval-auditor' oto/workflows/eval-review.md && test -f reports/rebrand-dryrun.json && grep -q 'oto/workflows/ingest-docs.md' reports/rebrand-dryrun.json && grep -q 'oto/workflows/eval-review.md' reports/rebrand-dryrun.json && test ! -e .oto-rebrand-stage</automated>
  </verify>
  <acceptance_criteria>
    - `test -f oto/workflows/ingest-docs.md && test -f oto/workflows/eval-review.md` exits 0
    - `wc -l < oto/workflows/ingest-docs.md` ≥ 200 (was ~27 as a stub; rebranded body is ~332)
    - `wc -l < oto/workflows/eval-review.md` ≥ 100 (was ~25 as a stub; rebranded body is ~155)
    - `grep -qi 'intentionally non-executable' oto/workflows/ingest-docs.md` exits non-zero (string ABSENT)
    - `grep -qi 'intentionally non-executable' oto/workflows/eval-review.md` exits non-zero (string ABSENT)
    - `grep -q '<step name="discover_docs">' oto/workflows/ingest-docs.md` exits 0
    - `grep -q '<step name="conflict_gate">' oto/workflows/ingest-docs.md` exits 0
    - `grep -q 'oto-eval-auditor' oto/workflows/eval-review.md` exits 0
    - `grep -q 'oto/workflows/ingest-docs.md' reports/rebrand-dryrun.json` exits 0
    - `grep -q 'oto/workflows/eval-review.md' reports/rebrand-dryrun.json` exits 0
    - `node -e "const r=require('./reports/rebrand-dryrun.json');const o=r.orphans||{};const h=JSON.stringify(o).match(/workflows\/(ingest-docs|eval-review)/g)||[];process.exit(h.length===0?0:1);"` exits 0
    - `test ! -e .oto-rebrand-stage` exits 0 (staging dir cleaned)
    - `grep -c 'gsd-doc-classifier\|gsd-doc-synthesizer\|gsd-eval-auditor' oto/workflows/ingest-docs.md oto/workflows/eval-review.md` equals `0` (rebrand engine swapped all `gsd-` tokens)
  </acceptance_criteria>
  <done>
    Both shipped workflow files have been replaced with the rebrand-engine-produced executable bodies (~332 / ~155 LOC). The two deferral stubs are gone. Coverage manifest regenerated; no orphan paths for either workflow. Staging dir cleaned up. The bodies still need three hand-fixup passes (Task 2) before they're correct — but the rebrand engine's contribution is complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply three hand-fixup passes (prose .planning/ → .oto/; SDK-tolerant fallback; classifier+auditor read-only-agent persistence reshape with REPLACE semantics)</name>
  <files>oto/workflows/ingest-docs.md, oto/workflows/eval-review.md</files>
  <read_first>
    - oto/workflows/ingest-docs.md (full file post-Task-1 — find and review every .planning/ hit and every oto-sdk query call and the classify_parallel step verbatim before editing)
    - oto/workflows/eval-review.md (full file post-Task-1 — find every oto-sdk query call and the audit step verbatim before editing)
    - oto/workflows/plan-phase.md (SDK-tolerant fallback precedent: `oto-sdk query config-get context_window 2>/dev/null || echo "200000"` — search for `2>/dev/null` to see the established pattern)
    - .planning/phases/01-agent-ports-installer-wiring/01-VERIFICATION.md ("Code Review" section — confirms `read-only agents return output for orchestrator persistence instead of writing directly`)
    - .planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md (D-04 sandbox-tools reconciliation: classifier + auditor are `read-only`, no Write; synthesizer is `workspace-write`, keeps Write)
    - oto/agents/oto-doc-classifier.md (frontmatter tools line: `Read, Grep, Glob` — confirms classifier cannot Write at runtime)
    - oto/agents/oto-eval-auditor.md (frontmatter tools line: `Read, Bash, Grep, Glob` — confirms auditor cannot Write at runtime)
    - oto/agents/oto-doc-synthesizer.md (frontmatter tools line: `Read, Write, Grep, Glob, Bash` — synthesizer CAN Write; synthesize step keeps direct-agent-write)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md ("Common Pitfalls" Pitfalls 1, 2, 4; "Code Examples" section for sed templates)
  </read_first>
  <action>
    **Part A — Hand-fixup pass 1: prose-embedded `.planning/` → `.oto/` in ingest-docs.md.**

    Inspect every hit first (no blind sed — per Assumption A3 mitigation: confirm no false-positive contexts):

    ```bash
    grep -n '\.planning\b' oto/workflows/ingest-docs.md
    ```

    Expected: multiple hits (engine output count varies — research's empirical probe saw ~33 hits across the file; do NOT anchor on a specific number). For each hit, inspect context; HALT if any hit references this repo's own `.planning/` state (e.g., "this repo still uses .planning/ per DOG-01") rather than a doc/path token in the workflow's prose. Otherwise confirm the context is one of (a) a path string inside backticks (e.g., `` `.planning/intel/classifications/` ``), (b) a path string in markdown prose, or (c) inside an XML attribute value pointing at a planning artifact. The research probe confirmed no false-positive hits exist; this is a guard.

    Apply the substitution:

    ```bash
    sed -i '' 's|\.planning/|.oto/|g' oto/workflows/ingest-docs.md
    sed -i '' 's|\.planning\b|.oto|g' oto/workflows/ingest-docs.md
    ```

    Lock:

    ```bash
    grep -c '\.planning\b' oto/workflows/ingest-docs.md
    # must equal 0
    ```

    Confirm eval-review.md was untouched and has zero `.planning/` hits (upstream doesn't reference it in prose; research verified):

    ```bash
    grep -c '\.planning\b' oto/workflows/eval-review.md
    # must equal 0
    ```

    **Part B — Hand-fixup pass 2: SDK-tolerant fallback on every `oto-sdk query` call.**

    Find every call in both files:

    ```bash
    grep -n 'oto-sdk query' oto/workflows/ingest-docs.md oto/workflows/eval-review.md
    ```

    For EACH call, review the surrounding line. The line is one of three shapes:

    1. **Already tolerant (no edit needed):** e.g., `VAR=$(oto-sdk query foo 2>/dev/null || echo "default")` — leave as-is.
    2. **Inline assignment without fallback:** e.g., `INIT=$(oto-sdk query init.ingest-docs)` — edit to append ` 2>/dev/null || echo ""` before the closing paren: `INIT=$(oto-sdk query init.ingest-docs 2>/dev/null || echo "")`.
    3. **Direct invocation in a pipeline:** e.g., `oto-sdk query commit | tee log.txt` — edit to add `|| true` after the SDK call: `oto-sdk query commit 2>/dev/null || true`.

    Use the Edit tool (NOT sed) — exact old/new strings, one call at a time. Mass-sed is forbidden here because some calls may already have partial fallbacks from upstream and a global sed could double-append (Pitfall 5 — hand-fixup drift).

    Lock with this check, which counts SDK calls that lack a fallback:

    ```bash
    # Every line containing 'oto-sdk query' must also contain either '2>/dev/null' or '|| ' on that same line OR within the same $() / `` `` expression.
    # Simpler lock: every oto-sdk query line should have 2>/dev/null adjacent.
    grep -n 'oto-sdk query' oto/workflows/ingest-docs.md oto/workflows/eval-review.md | grep -v '2>/dev/null\|||' | wc -l
    # must equal 0
    ```

    If the lock returns non-zero (some `oto-sdk query` line lacks a fallback), edit those lines.

    **Part C — Hand-fixup pass 3: read-only-agent persistence reshape (Phase 1 D-04, 01-VERIFICATION.md code-review precedent). REPLACE — DO NOT AUGMENT.**

    The upstream `<step name="classify_parallel">` block in ingest-docs.md tells the agent to write classification JSON to `OUTPUT_DIR — .oto/intel/classifications/` (after Part A's path fix). But `oto-doc-classifier` has `tools: Read, Grep, Glob` (no Write) — Phase 1 D-04 reconciliation locked this with Codex sandbox `read-only`. If the agent tries to Write under Codex, it gets sandbox-rejected; under Claude Code, the tools-list omission means the Write tool isn't available.

    **W3 fix — pinning ONE exact pre/post string per file.** Before editing, run the rebrand engine in Task 1 and inspect the post-Part-A workflow body to find the verbatim line. The substitution must REPLACE the upstream agent-writes-directly phrasing, not append to it. Both a regression-guard test in Plan 02-03 and the executor's diff review must confirm the upstream phrasing is GONE.

    Procedure for ingest-docs.md classify_parallel step:

    1. Locate the step: `grep -n '<step name="classify_parallel">' oto/workflows/ingest-docs.md` (capture line number).
    2. Read the step block verbatim:
       ```bash
       awk '/<step name="classify_parallel">/,/<\/step>/' oto/workflows/ingest-docs.md
       ```
    3. Identify the single line (or contiguous lines) describing where the classification is written. The upstream phrasing variants you may see post-rebrand include (engine substitutes `gsd-` → `oto-`):
       - "the oto-doc-classifier agent writes the classification JSON to OUTPUT_DIR"
       - "the agent persists the JSON to .oto/intel/classifications/<slug>.json"
       - "OUTPUT_DIR — `.oto/intel/classifications/`"

       Pick ONE exact verbatim line as the pre-string. Do NOT guess — read the file and copy the literal characters (whitespace, punctuation included).
    4. Apply the Edit tool with that EXACT pre-string and replace with this post-string (whichever wording maps best to your pre-string's grammar — only ONE replacement total):

       Post-string template (adapt verbiage to match upstream's tone):
       > "Spawn `Task(subagent_type=\"oto-doc-classifier\")` and capture the agent's JSON response. The classifier is a read-only agent (Phase 1 D-04). After the Task returns, the workflow orchestrator writes the returned JSON to `.oto/intel/classifications/<doc-slug>.json` using its own Write tool."

    5. Re-read the block and confirm:
       - The post-string is present.
       - The pre-string is GONE (not present alongside the post-string).

    6. If after the edit `grep` still finds the pre-string in the file, the Edit tool's match anchored to a different occurrence — re-run with more surrounding context until the upstream phrasing is fully replaced.

    Apply the same procedure to the audit step in `oto/workflows/eval-review.md`:

    1. Locate via `grep -n 'oto-eval-auditor' oto/workflows/eval-review.md` and read the surrounding step block (`grep -B5 -A30 'oto-eval-auditor' oto/workflows/eval-review.md`).
    2. The auditor has `tools: Read, Bash, Grep, Glob` (no Write). The upstream phrasing variants you may see post-rebrand:
       - "the auditor writes EVAL-REVIEW.md to .oto/phases/<phase>/"
       - "the oto-eval-auditor agent persists the review document"
       - "agent writes the classification" (less likely for eval-review, but possible)

       Pick ONE exact verbatim line as the pre-string.
    3. Replace with post-string:
       > "Spawn `Task(subagent_type=\"oto-eval-auditor\")` and capture the agent's response body. The auditor is a read-only agent (Phase 1 D-04). After the Task returns, the workflow orchestrator writes the returned markdown to `.oto/phases/<phase>/EVAL-REVIEW.md` using its own Write tool."

    4. Confirm pre-string is GONE post-edit.

    Do NOT reshape the synthesize step (`<step name="synthesize">`) — the synthesizer (`oto-doc-synthesizer`) has `tools: Read, Write, Grep, Glob, Bash` and Codex sandbox `workspace-write`. It writes `.oto/INGEST-CONFLICTS.md` and `.oto/intel/SYNTHESIS.md` directly. This is the only sandbox/tool combination of the three new agents that permits direct-agent-write.

    **Local lock (in this plan) — assert the new orchestrator-persistence phrasing is present:**

    ```bash
    awk '/<step name="classify_parallel">/,/<\/step>/' oto/workflows/ingest-docs.md | grep -q 'orchestrator'
    grep -B5 -A20 'oto-eval-auditor' oto/workflows/eval-review.md | grep -q 'orchestrator'
    ```

    Both must exit 0. The ABSENCE-of-upstream-phrasing regression guard is asserted in Plan 02-03 (where `tests/workflow-ingest-docs.test.cjs` and `tests/workflow-eval-review.test.cjs` greps for the upstream phrasing fragments and fails if any appear).

    **Part D — Verify pre-existing security checks survived.** Per RESEARCH.md "Security Domain" section, the workflow body must retain:

    - Path-traversal rejection: `*..*` case statement on `SCAN_PATH`.
    - Realpath containment: `realpath` check that `SCAN_PATH` is under `realpath("$REPO_ROOT")`.
    - BLOCKER-gate exit clause: "Exit WITHOUT writing PROJECT.md, REQUIREMENTS.md, ROADMAP.md, or STATE.md" when `BLOCKERS > 0`.

    Confirm with:

    ```bash
    grep -q '\*\.\.\*' oto/workflows/ingest-docs.md
    grep -q 'realpath' oto/workflows/ingest-docs.md
    grep -q 'Exit WITHOUT' oto/workflows/ingest-docs.md
    ```

    All three must exit 0. If any fails, the rebrand engine altered a security check (unexpected) — halt and report; do NOT hand-restore (that would mask an engine regression).

    **Part E — Confirm Phase 1 agents are still referenced.** The rebranded bodies should mention all three Phase 1 agents by their oto-namespaced names:

    ```bash
    grep -q 'oto-doc-classifier' oto/workflows/ingest-docs.md
    grep -q 'oto-doc-synthesizer' oto/workflows/ingest-docs.md
    grep -q 'oto-eval-auditor' oto/workflows/eval-review.md
    ```

    All three must exit 0. The rebrand engine handled `gsd-` → `oto-` for these tokens; this is a final sanity check.
  </action>
  <verify>
    <automated>[ $(grep -c '\.planning\b' oto/workflows/ingest-docs.md) -eq 0 ] && [ $(grep -c '\.planning\b' oto/workflows/eval-review.md) -eq 0 ] && [ $(grep -n 'oto-sdk query' oto/workflows/ingest-docs.md oto/workflows/eval-review.md | grep -v '2>/dev/null\|||' | wc -l | tr -d ' ') -eq 0 ] && grep -q '\*\.\.\*' oto/workflows/ingest-docs.md && grep -q 'realpath' oto/workflows/ingest-docs.md && grep -q 'Exit WITHOUT' oto/workflows/ingest-docs.md && grep -q 'oto-doc-classifier' oto/workflows/ingest-docs.md && grep -q 'oto-doc-synthesizer' oto/workflows/ingest-docs.md && grep -q 'oto-eval-auditor' oto/workflows/eval-review.md && grep -q '\.oto/intel/classifications' oto/workflows/ingest-docs.md && grep -q 'EVAL-REVIEW\.md' oto/workflows/eval-review.md && awk '/<step name="classify_parallel">/,/<\/step>/' oto/workflows/ingest-docs.md | grep -q 'orchestrator' && grep -B5 -A20 'oto-eval-auditor' oto/workflows/eval-review.md | grep -q 'orchestrator'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.planning\b' oto/workflows/ingest-docs.md` equals `0` (Pass 1 closed; engine blind spot fixed)
    - `grep -c '\.planning\b' oto/workflows/eval-review.md` equals `0` (no regression — upstream had no hits)
    - `grep -n 'oto-sdk query' oto/workflows/ingest-docs.md oto/workflows/eval-review.md | grep -v '2>/dev/null\|||' | wc -l` equals `0` (every SDK call has a fallback)
    - `grep -q '\*\.\.\*' oto/workflows/ingest-docs.md` exits 0 (path-traversal check preserved)
    - `grep -q 'realpath' oto/workflows/ingest-docs.md` exits 0 (realpath containment preserved)
    - `grep -q 'Exit WITHOUT' oto/workflows/ingest-docs.md` exits 0 (BLOCKER-gate clause preserved)
    - `grep -q 'oto-doc-classifier' oto/workflows/ingest-docs.md` exits 0
    - `grep -q 'oto-doc-synthesizer' oto/workflows/ingest-docs.md` exits 0
    - `grep -q 'oto-eval-auditor' oto/workflows/eval-review.md` exits 0
    - `grep -q '\.oto/intel/classifications' oto/workflows/ingest-docs.md` exits 0 (classify step still references the canonical output dir, now namespaced .oto/)
    - `grep -q 'EVAL-REVIEW\.md' oto/workflows/eval-review.md` exits 0 (auditor output target name preserved)
    - The classify_parallel step in `oto/workflows/ingest-docs.md` contains the substring `orchestrator`: `awk '/<step name="classify_parallel">/,/<\/step>/' oto/workflows/ingest-docs.md | grep -q 'orchestrator'` exits 0
    - The audit step in `oto/workflows/eval-review.md` contains `orchestrator` within ~30 lines of any auditor mention: `grep -B5 -A20 'oto-eval-auditor' oto/workflows/eval-review.md | grep -q 'orchestrator'` exits 0
    - REPLACE (not augment) semantics: the executor's Task 2 Part C SUMMARY entry MUST document (a) the verbatim pre-string used for each file, (b) the verbatim post-string written, (c) confirmation that the pre-string is GONE from the file after the edit (`grep -c "<exact pre-string>" oto/workflows/<file>.md` equals `0`). Plan 02-03 adds an ABSENCE regression-guard that locks this across future syncs.
  </acceptance_criteria>
  <done>
    All three hand-fixup passes complete. Prose-embedded `.planning/` paths in ingest-docs.md are rewritten to `.oto/`. Every `oto-sdk query` call in both workflows has a fallback (`2>/dev/null` or `||`). The classify_parallel step and the audit step both make the workflow orchestrator the writer (Phase 1 D-04 read-only-agent persistence reshape) via REPLACE semantics — the upstream agent-writes-directly phrasing is GONE from both files, not merely accompanied by new orchestrator phrasing. Pre-existing security checks (`*..*`, `realpath`, BLOCKER-gate `Exit WITHOUT`) survived the rebrand and the hand-fixups. WF-ING-01..04 and WF-EVAL-01..02 are satisfied at the file-content level; assertion tests (Plan 02-02) and fixture smoke + ABSENCE regression-guard (Plan 02-03) will lock them.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo only | All mutations are to files inside the project root; no network, no user data, no auth, no secrets. The shipped workflows themselves DO have a trust boundary at runtime (user-supplied `SCAN_PATH` and `MANIFEST_PATH` cross into untrusted territory) — Task 2 Part D verifies that boundary's existing controls survived the rebrand+fixup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | oto/workflows/ingest-docs.md path inputs (SCAN_PATH, MANIFEST_PATH) | mitigate | Pre-existing `*..*` case rejection + `realpath` containment under `$REPO_ROOT` survive rebrand+fixup. Task 2 Part D grep-locks all three security checks. If the rebrand engine altered any check, Task 2 halts (no hand-restore — that would mask an engine regression). |
| T-02-02 | Tampering | oto/workflows/ingest-docs.md BLOCKER-gate bypass | mitigate | The workflow's "Exit WITHOUT writing PROJECT.md, REQUIREMENTS.md, ROADMAP.md, or STATE.md" clause when `BLOCKERS > 0` is asserted-preserved by Task 2 Part D (`grep -q 'Exit WITHOUT'`). |
| T-02-03 | Elevation of Privilege | oto-doc-classifier / oto-eval-auditor sandbox bypass | mitigate | Phase 1 D-04 stripped Write from both agents (Codex sandbox: read-only). Task 2 Part C reshapes the workflow body so the agents return data and the orchestrator persists it — eliminating any path where a read-only agent attempts Write. REPLACE semantics (not augment) ensure the upstream agent-writes-directly phrasing is GONE, not coexisting alongside the new wording. Plan 02-03's ABSENCE regression-guard locks this across future syncs. |
| T-02-04 | Information Disclosure | oto/workflows/*.md shipped content | accept | Workflow bodies are public-by-design (npm install -g). No secrets surface. |
| T-02-05 | Repudiation | reports/rebrand-dryrun.json provenance | accept | Manifest is regenerated by `scripts/rebrand.cjs --dry-run`; the engine writes a deterministic shape. Git commits record the regen invocation; no audit-log requirement for this personal-use tool. |

Note: No new auth/network/secrets/UI surface in this plan. The security gate is preservation of existing controls (T-02-01, T-02-02) and the sandbox-alignment reshape (T-02-03) via REPLACE semantics.
</threat_model>

<verification>
- Both shipped workflow files are rebrand-engine-produced executable bodies (~332 / ~155 LOC), not deferral stubs.
- Zero `intentionally non-executable` / `DEFERRED` / `deferral stub` markers in either body.
- Zero prose-embedded `.planning/` hits in either workflow body (engine blind spot 1 closed).
- Every `oto-sdk query` call in both workflows has a `2>/dev/null` or `|| ` fallback (engine blind spot 2 closed; SDK-DEFER-01 tolerance).
- classify_parallel and audit steps reshape to orchestrator-persistence (engine blind spot 3 closed; Phase 1 D-04 alignment). REPLACE — not augment: the upstream agent-writes-directly phrasing is GONE from both files (Plan 02-03's ABSENCE regression-guard locks this).
- Synthesize step preserves direct-agent-write (`oto-doc-synthesizer` has Write + `workspace-write` sandbox — the only agent of the three that may write directly).
- Pre-existing security checks (`*..*` path-traversal rejection; `realpath` containment; BLOCKER-gate `Exit WITHOUT` clause) survived rebrand+fixup.
- All three Phase 1 agent names (`oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor`) are referenced by their rebranded names; no `gsd-*` agent tokens remain in either body.
- `reports/rebrand-dryrun.json` and `reports/rebrand-dryrun.md` regenerated; both workflow target paths present; no workflow orphans.
- `.oto-rebrand-stage/` staging dir cleaned up after engine apply.
</verification>

<success_criteria>
1. `oto/workflows/ingest-docs.md` is the rebrand-ported executable body (~332 LOC) with all prose-embedded `.planning/` hits rewritten to `.oto/`, every `oto-sdk query` call fallback-tolerant, and the classify_parallel step reshaped for orchestrator-persistence (REPLACE — upstream phrasing GONE).
2. `oto/workflows/eval-review.md` is the rebrand-ported executable body (~155 LOC) with every `oto-sdk query` call fallback-tolerant and the audit step reshaped for orchestrator-persistence (REPLACE — upstream phrasing GONE).
3. Pre-existing security controls (path-traversal rejection, realpath containment, BLOCKER-gate `Exit WITHOUT`) survive the rebrand and the hand-fixups.
4. `reports/rebrand-dryrun.json` and `reports/rebrand-dryrun.md` regenerated; both workflow target paths recognized; no orphan paths involving the new workflows.
5. WF-ING-01..04 and WF-EVAL-01..02 file-content requirements satisfied; Plan 02-02 (assertion tests) and Plan 02-03 (fixture smoke + ABSENCE regression-guard) can now run.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-01-SUMMARY.md` capturing:
- Pre-flight inventory-row confirmation result
- Rebrand engine invocation command + reported matches/unclassified counts
- Two shipped-file replacements (line counts before → after for each)
- Hand-fixup pass 1 result: exact list of line numbers / contexts touched by the `.planning/` → `.oto/` substitution in ingest-docs.md (per Pitfall 5 — document each hand-fixup)
- Hand-fixup pass 2 result: list of every `oto-sdk query` call edited (file:line + the fallback shape added)
- Hand-fixup pass 3 result: REPLACE-semantics record — for each file (ingest-docs.md classify_parallel + eval-review.md audit step):
  - the verbatim pre-string (single line) used as the Edit tool's `old_string`
  - the verbatim post-string written
  - confirmation that `grep -c "<verbatim pre-string>" oto/workflows/<file>.md` equals `0` after the edit (upstream phrasing GONE, not augmented)
- Security-check grep results (path-traversal, realpath, BLOCKER-gate — all PRESENT)
- Coverage manifest delta: orphan counts before/after the regen
- Confirmation `.oto-rebrand-stage/` is gone
</output>
</content>
</invoke>