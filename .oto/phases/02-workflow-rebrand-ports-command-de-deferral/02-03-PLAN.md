---
phase: 02-workflow-rebrand-ports-command-de-deferral
phase_number: 2
plan: 03
plan_id: 02-03
type: execute
wave: 2
depends_on: [02-01]
autonomous: true
requirements: [WF-ING-01, WF-ING-02, WF-ING-03, WF-ING-04, WF-EVAL-01, WF-EVAL-02]
requirements_addressed: [WF-ING-01, WF-ING-02, WF-ING-03, WF-ING-04, WF-EVAL-01, WF-EVAL-02]
files_modified:
  - tests/workflow-ingest-docs.test.cjs
  - tests/workflow-eval-review.test.cjs
  - tests/workflow-ingest-docs-fixture.test.cjs
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-001-database-choice.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-002-auth-strategy.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-003-deployment.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/prd/PRD-001-user-onboarding.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/prd/PRD-002-billing.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/prd/PRD-003-analytics.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/specs/SPEC-001-api-contract.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/specs/SPEC-002-data-model.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/rfc/RFC-001-feature-flags.md
  - tests/fixtures/ingest-docs/new-mode-mixed/docs/rfc/RFC-002-rate-limits.md
  - tests/fixtures/ingest-docs/merge-mode-existing/.oto/PROJECT.md
  - tests/fixtures/ingest-docs/merge-mode-existing/.oto/REQUIREMENTS.md
  - tests/fixtures/ingest-docs/merge-mode-existing/.oto/ROADMAP.md
  - tests/fixtures/ingest-docs/merge-mode-existing/.oto/STATE.md
  - tests/fixtures/ingest-docs/merge-mode-existing/docs/adr/ADR-004-cache-layer.md
  - tests/fixtures/ingest-docs/merge-mode-existing/docs/prd/PRD-004-search.md
  - tests/fixtures/ingest-docs/merge-mode-existing/docs/specs/SPEC-003-search-index.md
  - tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-005-database-postgres.md
  - tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-006-database-mongo.md
  - tests/fixtures/ingest-docs/conflict-block/docs/prd/PRD-005-platform.md

must_haves:
  truths:
    - "tests/workflow-ingest-docs.test.cjs asserts workflow-shape invariants for oto/workflows/ingest-docs.md: banner step, discover_docs step, classify_parallel step, synthesize step, conflict_gate step; no deferral framing; all three Phase 1 agent names referenced; 50-doc cap text present; BLOCKER gate `Exit WITHOUT` clause present; directory-conventions discovery (docs/adr/, docs/prd/, docs/specs/, docs/rfc/) and --manifest flag both present; --mode new and --mode merge both present; zero prose-embedded .planning/ hits (regression-guard for engine blind spot 1); every oto-sdk query call has a 2>/dev/null or || fallback (regression-guard for engine blind spot 2); classify_parallel step contains 'orchestrator' wording (regression-guard for engine blind spot 3 — Phase 1 D-04 reshape); ABSENCE of upstream agent-writes-directly phrasing fragments in both workflow bodies (W3 fix — locks REPLACE semantics from Plan 02-01 Task 2 Part C)"
    - "tests/workflow-eval-review.test.cjs asserts workflow-shape invariants for oto/workflows/eval-review.md: oto-eval-auditor referenced; State A / State B detection (AI-SPEC.md presence/absence) present; EVAL-REVIEW.md output target named; COVERED / PARTIAL / MISSING scoring vocabulary present; no deferral framing; zero prose-embedded .planning/ hits; SDK fallback regression-guard; auditor step contains 'orchestrator' wording (Phase 1 D-04 reshape regression-guard); ABSENCE of upstream agent-writes-directly phrasing (W3 fix)"
    - "tests/workflow-ingest-docs-fixture.test.cjs asserts fixture-tree well-formedness: new-mode-mixed has 10 docs distributed across docs/adr/ (3), docs/prd/ (3), docs/specs/ (2), docs/rfc/ (2); merge-mode-existing has a pre-existing .oto/ skeleton (4 files) plus 3 new docs; conflict-block has 2 ADRs with directly contradictory locked decisions plus 1 PRD; the over-cap fixture is generated by a setup hook (51 minimal .md files in os.tmpdir()) and the cap-enforcement assertion passes against that tmp tree"
    - "All three new test files pass under `node --test`; full `npm test` suite stays green at v0.2.0 + Phase 1 baseline + the new tests; no regressions"
  artifacts:
    - path: "tests/workflow-ingest-docs.test.cjs"
      provides: "Workflow-shape assertions for /oto-ingest-docs (WF-ING-01..04 + Phase 1 D-04 + engine-blind-spot regression guards + W3 ABSENCE guard)"
      contains: "<step name=\"discover_docs\">"
      min_lines: 80
    - path: "tests/workflow-eval-review.test.cjs"
      provides: "Workflow-shape assertions for /oto-eval-review (WF-EVAL-01..02 + engine-blind-spot regression guards + W3 ABSENCE guard)"
      contains: "oto-eval-auditor"
      min_lines: 60
    - path: "tests/workflow-ingest-docs-fixture.test.cjs"
      provides: "Fixture-tree well-formedness assertions"
      contains: "new-mode-mixed"
      min_lines: 50
    - path: "tests/fixtures/ingest-docs/new-mode-mixed"
      provides: "10-doc mixed-type fixture for /oto-ingest-docs new-mode smoke (3 ADR + 3 PRD + 2 SPEC + 2 RFC)"
      contains: "ADR-001"
    - path: "tests/fixtures/ingest-docs/merge-mode-existing"
      provides: "Pre-existing .oto/ + 3 new docs fixture for /oto-ingest-docs merge-mode smoke"
      contains: "PROJECT.md"
    - path: "tests/fixtures/ingest-docs/conflict-block"
      provides: "Two-contradictory-ADR fixture for BLOCKER-gate smoke (WF-ING-04)"
      contains: "postgres"
  key_links:
    - from: "tests/workflow-ingest-docs.test.cjs"
      to: "oto/workflows/ingest-docs.md"
      via: "fs.readFileSync + assert step names + agent references + regression-guard greps + W3 absence guard"
      pattern: "workflows/ingest-docs"
    - from: "tests/workflow-eval-review.test.cjs"
      to: "oto/workflows/eval-review.md"
      via: "fs.readFileSync + assert auditor reference + State A/B detection + EVAL-REVIEW.md target name + W3 absence guard"
      pattern: "workflows/eval-review"
    - from: "tests/workflow-ingest-docs-fixture.test.cjs"
      to: "tests/fixtures/ingest-docs/{new-mode-mixed,merge-mode-existing,conflict-block}"
      via: "fs.readdirSync + assert doc counts, types, distribution"
      pattern: "fixtures/ingest-docs"
    - from: "tests/workflow-ingest-docs-fixture.test.cjs (setup hook)"
      to: "os.tmpdir()/oto-over-cap-fixture-*/"
      via: "fs.mkdtempSync + 51 fs.writeFileSync calls (lightweight minimal .md files; not committed)"
      pattern: "tmpdir"
---

<objective>
Add workflow-shape assertion tests and fixture-tree well-formedness assertions for the two restored commands. Three new test files plus four committed fixture trees + one tmpdir-generated over-cap fixture verify that Plan 02-01's rebrand+fixup work satisfied WF-ING-01..04 and WF-EVAL-01..02, and lock the three engine blind-spot fixes (prose `.planning/`, SDK-fallback, classifier/auditor read-only-agent reshape) against regression. The W3 ABSENCE regression-guard (added per Phase 2 revision iter 1) locks REPLACE semantics — fails if any future rebrand sync reintroduces the upstream agent-writes-directly phrasing alongside (or instead of) the orchestrator-persistence wording. Tests assert markdown **shape** of the workflow body (the executable contract — step names, agent references, gate clauses), not runtime behavior (workflows are LLM prompts, not executable code — Pitfall 7).

Purpose: Phase 2 verification surface. Phase 3 will later port the upstream `tests/ingest-docs.test.cjs` (TEST-01) and write `tests/eval-review.test.cjs` (TEST-02); this plan's tests are scoped to Phase 2's success criteria — proving the rebrand+fixup landed correctly and guarding the engine blind-spot regressions.
Output: Three new test files in `tests/`, three committed fixture trees in `tests/fixtures/ingest-docs/`, one tmpdir-generated over-cap fixture (not committed — generated in test setup).

W4 fix (Phase 2 revision iter 1): The original Task 1 modified 23 files in one task (above 15-file threshold, driven by 20 fixture markdown files). It is SPLIT into Task 1a (new-mode-mixed fixtures, 10 docs) and Task 1b (merge-mode-existing + conflict-block fixtures, 10 files total: 4-file .oto skeleton + 3 merge docs + 3 conflict-block docs). Both stay in this plan (cohesive — fixture seeding is one concern). Tasks 2 and 3 (test files) are renumbered to Tasks 3 and 4 accordingly.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md
@CLAUDE.md

# This plan asserts against Plan 02-01's outputs (must run AFTER 02-01)
@oto/workflows/ingest-docs.md
@oto/workflows/eval-review.md

# Phase 1 D-04 precedent — read-only agents' workflow integration shape
@.planning/phases/01-agent-ports-installer-wiring/01-VERIFICATION.md
@oto/agents/oto-doc-classifier.md
@oto/agents/oto-eval-auditor.md
@oto/agents/oto-doc-synthesizer.md

# Upstream test-style precedent (Phase 3 will port; we mirror its shape for Phase 2's scoped smoke)
@foundation-frameworks/get-shit-done-main/tests/ingest-docs.test.cjs

# Repo test-style precedents
@tests/phase-04-frontmatter-schema.test.cjs
@tests/phase-04-no-dropped-agents.test.cjs

<interfaces>
<!-- Workflow-shape assertion pattern (research's Code Examples + Pattern 2): -->
<!--   1. fs.readFileSync the workflow body once at module top -->
<!--   2. test() per requirement ID with assert.ok(substring) or assert.match(regex) -->
<!--   3. Regression-guards for the three engine blind spots use grep-style negation -->
<!--   4. W3 ABSENCE regression-guard: assert upstream agent-writes-directly phrasing is GONE -->

<!-- Required substrings/patterns in oto/workflows/ingest-docs.md (post-Plan-02-01): -->
<!--   - <step name="banner"> -->
<!--   - <step name="discover_docs"> -->
<!--   - <step name="classify_parallel"> -->
<!--   - <step name="synthesize"> -->
<!--   - <step name="conflict_gate"> -->
<!--   - <step name="route_new_mode"> AND <step name="route_merge_mode"> -->
<!--   - docs/adr/ AND docs/prd/ AND docs/specs/ AND docs/rfc/  (discovery conventions) -->
<!--   - --manifest -->
<!--   - --mode new AND --mode merge -->
<!--   - 50  (the cap) -->
<!--   - exceeds the v1 cap of 50  (cap-text per upstream) -->
<!--   - BLOCKERS  AND  Exit WITHOUT  (BLOCKER gate) -->
<!--   - oto-doc-classifier AND oto-doc-synthesizer AND oto-roadmapper  (Task targets) -->

<!-- Required substrings/patterns in oto/workflows/eval-review.md: -->
<!--   - oto-eval-auditor -->
<!--   - AI-SPEC.md  (State A/B trigger) -->
<!--   - EVAL-REVIEW.md  (output target) -->
<!--   - COVERED AND PARTIAL AND MISSING  (scoring vocabulary) -->

<!-- Regression guards (lock the three Plan 02-01 hand-fixup passes): -->
<!--   - Both workflows: zero `.planning\b` regex matches -->
<!--   - Both workflows: every line containing 'oto-sdk query' also has 2>/dev/null or || on same line -->
<!--   - ingest-docs.md: classify_parallel step block contains 'orchestrator' wording -->
<!--   - eval-review.md: auditor-dispatch block contains 'orchestrator' wording -->

<!-- W3 ABSENCE regression-guard fragments (forbidden phrasing, fails test if present in either workflow): -->
<!--   - "agent writes the classification" (case-insensitive) -->
<!--   - "agent persists the JSON" / "agent persists the classification" -->
<!--   - "classifier writes" / "auditor writes" (followed by an output path) -->
<!--   - "the agent writes EVAL-REVIEW" / "auditor persists EVAL-REVIEW" -->
<!-- These map to the variants Plan 02-01 Task 2 Part C identifies as candidate pre-strings to REPLACE. -->
<!-- If any of these substrings remain in the workflow body, Plan 02-01's REPLACE semantics were violated (augment-instead-of-replace bug). -->

<!-- Fixture-tree layout (committed): -->
<!--   tests/fixtures/ingest-docs/new-mode-mixed/docs/ -->
<!--     adr/ADR-001-database-choice.md, ADR-002-auth-strategy.md, ADR-003-deployment.md -->
<!--     prd/PRD-001-user-onboarding.md, PRD-002-billing.md, PRD-003-analytics.md -->
<!--     specs/SPEC-001-api-contract.md, SPEC-002-data-model.md -->
<!--     rfc/RFC-001-feature-flags.md, RFC-002-rate-limits.md -->
<!--   tests/fixtures/ingest-docs/merge-mode-existing/.oto/{PROJECT,REQUIREMENTS,ROADMAP,STATE}.md (skeleton) -->
<!--   tests/fixtures/ingest-docs/merge-mode-existing/docs/{adr/ADR-004-cache-layer.md, prd/PRD-004-search.md, specs/SPEC-003-search-index.md} -->
<!--   tests/fixtures/ingest-docs/conflict-block/docs/{adr/ADR-005-database-postgres.md, adr/ADR-006-database-mongo.md, prd/PRD-005-platform.md} -->

<!-- Over-cap fixture: NOT committed. Generated in setup hook in tests/workflow-ingest-docs-fixture.test.cjs via fs.mkdtempSync(os.tmpdir() + path.sep + 'oto-over-cap-'). 51 minimal .md files (~50 bytes each). Cleaned up in t.after(). -->

<!-- Each committed fixture .md file is minimal but well-formed: -->
<!--   - Frontmatter with `id:` (e.g., ADR-001), `title:`, `status: accepted` (for ADRs) or appropriate status -->
<!--   - One paragraph of body content -->
<!--   - Total file: ~10-20 lines, ~300-500 bytes -->

<!-- The conflict-block fixture's two ADRs (ADR-005 / ADR-006) must declare directly contradictory locked decisions: -->
<!--   - ADR-005 frontmatter: status: accepted; body: "Selected database: PostgreSQL" -->
<!--   - ADR-006 frontmatter: status: accepted; body: "Selected database: MongoDB" -->
<!-- Synthesizer should classify this as a BLOCKER (LOCKED-vs-LOCKED contradiction). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Seed the new-mode-mixed fixture tree (10 docs across 4 type dirs) under tests/fixtures/ingest-docs/new-mode-mixed/</name>
  <files>tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-001-database-choice.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-002-auth-strategy.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-003-deployment.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/prd/PRD-001-user-onboarding.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/prd/PRD-002-billing.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/prd/PRD-003-analytics.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/specs/SPEC-001-api-contract.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/specs/SPEC-002-data-model.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/rfc/RFC-001-feature-flags.md, tests/fixtures/ingest-docs/new-mode-mixed/docs/rfc/RFC-002-rate-limits.md</files>
  <read_first>
    - tests/fixtures/ (top-level — confirm directory exists; reuse if any helpers already present)
    - tests/helpers/ (if present — check for temp-dir or fixture-seeder utilities to reuse before rolling new ones per Open Question 2)
    - oto/workflows/ingest-docs.md (post-Plan-02-01 — read the discover_docs step to confirm the directory conventions `docs/adr/`, `docs/prd/`, `docs/specs/`, `docs/rfc/` that the fixtures must mirror)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md (Wave 0 Gaps fixture layout for new-mode-mixed: 3 ADR + 3 PRD + 2 SPEC + 2 RFC)
  </read_first>
  <action>
    **Create the new-mode-mixed fixture tree using the Write tool (one file per Write call).** Each file is minimal but well-formed: YAML frontmatter (id, title, status) + 5–10 lines of body. Use exact filenames listed in `<files>`. Do NOT use heredocs or bash redirection — use the Write tool.

    **Fixture A — `tests/fixtures/ingest-docs/new-mode-mixed/` (10 docs):**

    For each ADR file (`docs/adr/ADR-NNN-*.md`), write:

    ```markdown
    ---
    id: ADR-001
    title: Database choice
    status: accepted
    date: 2026-01-15
    ---

    # ADR-001: Database choice

    ## Context

    The platform needs durable storage for user records and event logs.

    ## Decision

    Use PostgreSQL 16 as the primary relational store. Single-region for v1.

    ## Consequences

    Backup strategy must align with PostgreSQL's WAL + pg_basebackup.
    ```

    Use distinct `id:` / `title:` / `Decision` content per file:
    - ADR-001-database-choice: id `ADR-001`, title "Database choice", Decision "Use PostgreSQL 16 as the primary relational store."
    - ADR-002-auth-strategy: id `ADR-002`, title "Authentication strategy", Decision "Use OIDC with Auth0; no self-hosted IdP for v1."
    - ADR-003-deployment: id `ADR-003`, title "Deployment target", Decision "Deploy to AWS Fargate; no Kubernetes for v1."

    For each PRD file (`docs/prd/PRD-NNN-*.md`), write:

    ```markdown
    ---
    id: PRD-001
    title: User onboarding
    status: draft
    owner: product
    ---

    # PRD-001: User onboarding

    ## Problem

    New users churn within the first session if signup takes more than 30 seconds.

    ## Goal

    Reduce signup to <15 seconds via OAuth + magic-link fallback.

    ## Requirements

    - PRD-REQ-001: Support Google OAuth on signup screen.
    - PRD-REQ-002: Fallback to magic-link email if OAuth fails.
    ```

    Distinct `id:` / `title:` / Problem content per:
    - PRD-001-user-onboarding: id `PRD-001`, title "User onboarding", Problem "New users churn within the first session if signup takes more than 30 seconds."
    - PRD-002-billing: id `PRD-002`, title "Billing", Problem "Customers can't self-serve plan upgrades."
    - PRD-003-analytics: id `PRD-003`, title "Analytics dashboard", Problem "Operators have no visibility into per-tenant usage."

    For each SPEC file (`docs/specs/SPEC-NNN-*.md`):

    ```markdown
    ---
    id: SPEC-001
    title: API contract v1
    status: review
    ---

    # SPEC-001: API contract v1

    ## Endpoints

    - POST /v1/users  → 201, returns User
    - GET  /v1/users/:id → 200 | 404

    ## Auth

    Bearer token in `Authorization` header; tokens issued by Auth0.
    ```

    - SPEC-001-api-contract: id `SPEC-001`, title "API contract v1"
    - SPEC-002-data-model: id `SPEC-002`, title "Data model v1"

    For each RFC file (`docs/rfc/RFC-NNN-*.md`):

    ```markdown
    ---
    id: RFC-001
    title: Feature flag rollout
    status: discussion
    ---

    # RFC-001: Feature flag rollout

    ## Summary

    Propose using LaunchDarkly for percentage-based feature rollouts.

    ## Alternatives

    - Self-hosted Unleash (rejected: ops overhead)
    - In-house flags (rejected: build cost)
    ```

    - RFC-001-feature-flags: id `RFC-001`, title "Feature flag rollout"
    - RFC-002-rate-limits: id `RFC-002`, title "API rate limit strategy"
  </action>
  <verify>
    <automated>[ $(find tests/fixtures/ingest-docs/new-mode-mixed/docs -name '*.md' | wc -l | tr -d ' ') -eq 10 ] && [ $(find tests/fixtures/ingest-docs/new-mode-mixed/docs/adr -name '*.md' | wc -l | tr -d ' ') -eq 3 ] && [ $(find tests/fixtures/ingest-docs/new-mode-mixed/docs/prd -name '*.md' | wc -l | tr -d ' ') -eq 3 ] && [ $(find tests/fixtures/ingest-docs/new-mode-mixed/docs/specs -name '*.md' | wc -l | tr -d ' ') -eq 2 ] && [ $(find tests/fixtures/ingest-docs/new-mode-mixed/docs/rfc -name '*.md' | wc -l | tr -d ' ') -eq 2 ] && for f in $(find tests/fixtures/ingest-docs/new-mode-mixed -name '*.md'); do head -1 "$f" | grep -q '^---$' || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `find tests/fixtures/ingest-docs/new-mode-mixed/docs -name '*.md' | wc -l` equals `10`
    - `find tests/fixtures/ingest-docs/new-mode-mixed/docs/adr -name '*.md' | wc -l` equals `3`
    - `find tests/fixtures/ingest-docs/new-mode-mixed/docs/prd -name '*.md' | wc -l` equals `3`
    - `find tests/fixtures/ingest-docs/new-mode-mixed/docs/specs -name '*.md' | wc -l` equals `2`
    - `find tests/fixtures/ingest-docs/new-mode-mixed/docs/rfc -name '*.md' | wc -l` equals `2`
    - Every committed fixture .md has YAML frontmatter (starts with `---`): `for f in $(find tests/fixtures/ingest-docs/new-mode-mixed -name '*.md'); do head -1 "$f" | grep -q '^---$' || (echo "missing frontmatter: $f"; exit 1); done` exits 0
  </acceptance_criteria>
  <done>
    The new-mode-mixed fixture tree is seeded under `tests/fixtures/ingest-docs/new-mode-mixed/` with 10 well-formed docs distributed 3 ADR + 3 PRD + 2 SPEC + 2 RFC. All committed files have YAML frontmatter and minimal but valid body content. Task 1b will seed the remaining two fixture trees.
  </done>
</task>

<task type="auto">
  <name>Task 1b: Seed the merge-mode-existing (.oto skeleton + 3 docs) and conflict-block (2 contradictory ADRs + 1 PRD) fixture trees</name>
  <files>tests/fixtures/ingest-docs/merge-mode-existing/.oto/PROJECT.md, tests/fixtures/ingest-docs/merge-mode-existing/.oto/REQUIREMENTS.md, tests/fixtures/ingest-docs/merge-mode-existing/.oto/ROADMAP.md, tests/fixtures/ingest-docs/merge-mode-existing/.oto/STATE.md, tests/fixtures/ingest-docs/merge-mode-existing/docs/adr/ADR-004-cache-layer.md, tests/fixtures/ingest-docs/merge-mode-existing/docs/prd/PRD-004-search.md, tests/fixtures/ingest-docs/merge-mode-existing/docs/specs/SPEC-003-search-index.md, tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-005-database-postgres.md, tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-006-database-mongo.md, tests/fixtures/ingest-docs/conflict-block/docs/prd/PRD-005-platform.md</files>
  <read_first>
    - tests/fixtures/ingest-docs/new-mode-mixed/docs/adr/ADR-001-database-choice.md (post-Task-1a — confirm doc-shape template to mirror)
    - foundation-frameworks/get-shit-done-main/get-shit-done/references/doc-conflict-engine.md (BLOCKER classification rules — LOCKED-vs-LOCKED contradiction; needed for conflict-block fixture authoring)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md (Wave 0 Gaps fixture layout: merge-mode-existing = 4-file .oto skeleton + 3 new docs; conflict-block = 2 contradictory ADRs + 1 PRD)
  </read_first>
  <action>
    **Create the remaining two fixture trees using the Write tool (one file per Write call).**

    **Fixture B — `tests/fixtures/ingest-docs/merge-mode-existing/` (4-file .oto/ skeleton + 3 new docs):**

    `.oto/PROJECT.md`:

    ```markdown
    # Fixture Project (merge-mode)

    ## Overview

    Pre-existing synthesized project for /oto-ingest-docs --mode merge fixture.

    ## Tech Stack

    Node 22, PostgreSQL 16, AWS Fargate.
    ```

    `.oto/REQUIREMENTS.md`:

    ```markdown
    # Requirements

    ## v1
    - [x] REQ-001: User registration via OAuth.
    - [x] REQ-002: Billing self-service upgrade flow.
    ```

    `.oto/ROADMAP.md`:

    ```markdown
    # Roadmap

    ## Phase 1: Foundation (shipped)
    - Auth + billing live.

    ## Phase 2: Search (planned)
    - Postgres FTS, frontend search bar.
    ```

    `.oto/STATE.md`:

    ```markdown
    ---
    status: executing
    ---

    # State

    ## Current Position

    Phase: 2 — Search.
    ```

    Then the three new docs (which `--mode merge` should append), using the same shape as Task 1a's ADR/PRD/SPEC templates:

    - `docs/adr/ADR-004-cache-layer.md`: id `ADR-004`, title "Cache layer", Decision "Add Redis 7 for hot-path caching."
    - `docs/prd/PRD-004-search.md`: id `PRD-004`, title "Search", Problem "Users can't find content older than 30 days."
    - `docs/specs/SPEC-003-search-index.md`: id `SPEC-003`, title "Search index spec"

    **Fixture C — `tests/fixtures/ingest-docs/conflict-block/` (2 contradictory ADRs + 1 PRD):**

    `docs/adr/ADR-005-database-postgres.md`:

    ```markdown
    ---
    id: ADR-005
    title: Database — PostgreSQL
    status: accepted
    date: 2026-02-10
    ---

    # ADR-005: Database — PostgreSQL

    ## Decision

    Selected database: PostgreSQL 16. This is a LOCKED architectural commitment.

    ## Rationale

    Strong ACID guarantees, mature operational tooling, team familiarity.
    ```

    `docs/adr/ADR-006-database-mongo.md`:

    ```markdown
    ---
    id: ADR-006
    title: Database — MongoDB
    status: accepted
    date: 2026-02-12
    ---

    # ADR-006: Database — MongoDB

    ## Decision

    Selected database: MongoDB 7. This is a LOCKED architectural commitment.

    ## Rationale

    Schema flexibility for evolving document shapes.
    ```

    `docs/prd/PRD-005-platform.md`:

    ```markdown
    ---
    id: PRD-005
    title: Platform v2
    status: draft
    ---

    # PRD-005: Platform v2

    ## Goal

    Ship v2 by Q3 with whichever database the ADRs settle.
    ```

    These two ADRs declare directly contradictory locked decisions on the same architectural choice (database). When the synthesizer runs, it must classify this as a BLOCKER per `oto/references/doc-conflict-engine.md` (LOCKED-vs-LOCKED contradiction).

    **Do NOT seed the over-cap fixture (51 docs) on disk** — that's generated by the test's setup hook in `os.tmpdir()` (Task 4). Per Open Question 3 resolution: ≤50-doc fixtures are committed; the over-cap fixture is generated to avoid repo bloat.
  </action>
  <verify>
    <automated>test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/PROJECT.md && test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/REQUIREMENTS.md && test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/ROADMAP.md && test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/STATE.md && [ $(find tests/fixtures/ingest-docs/merge-mode-existing/docs -name '*.md' | wc -l | tr -d ' ') -eq 3 ] && [ $(find tests/fixtures/ingest-docs/conflict-block/docs -name '*.md' | wc -l | tr -d ' ') -eq 3 ] && grep -q 'PostgreSQL' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-005-database-postgres.md && grep -q 'MongoDB' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-006-database-mongo.md && grep -q 'LOCKED' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-005-database-postgres.md && grep -q 'LOCKED' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-006-database-mongo.md && for f in $(find tests/fixtures/ingest-docs/merge-mode-existing tests/fixtures/ingest-docs/conflict-block -name '*.md'); do head -1 "$f" | grep -q '^---$\|^#' || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/PROJECT.md` exits 0
    - `test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/REQUIREMENTS.md` exits 0
    - `test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/ROADMAP.md` exits 0
    - `test -f tests/fixtures/ingest-docs/merge-mode-existing/.oto/STATE.md` exits 0
    - `find tests/fixtures/ingest-docs/merge-mode-existing/docs -name '*.md' | wc -l` equals `3`
    - `find tests/fixtures/ingest-docs/conflict-block/docs -name '*.md' | wc -l` equals `3`
    - `grep -q 'PostgreSQL' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-005-database-postgres.md` exits 0
    - `grep -q 'MongoDB' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-006-database-mongo.md` exits 0
    - `grep -q 'LOCKED' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-005-database-postgres.md` exits 0 (contradiction marker for BLOCKER classification)
    - `grep -q 'LOCKED' tests/fixtures/ingest-docs/conflict-block/docs/adr/ADR-006-database-mongo.md` exits 0
    - Every committed merge-mode-existing/conflict-block fixture file (.oto/*.md and docs/**/*.md) is well-formed (starts with `---` for frontmatter docs, or `#` for the .oto/*.md skeleton files which don't all use frontmatter): `for f in $(find tests/fixtures/ingest-docs/merge-mode-existing tests/fixtures/ingest-docs/conflict-block -name '*.md'); do head -1 "$f" | grep -q '^---$\|^#' || (echo "malformed first line: $f"; exit 1); done` exits 0
  </acceptance_criteria>
  <done>
    Both remaining fixture trees are seeded: merge-mode-existing (4-file .oto skeleton + 3 new docs in adr/prd/specs) and conflict-block (2 contradictory ADRs declaring LOCKED PostgreSQL vs LOCKED MongoDB, plus 1 PRD). Combined with Task 1a, all three committed fixture trees are now in place. Over-cap fixture remains NOT committed (Task 4 generates it in tmpdir).
  </done>
</task>

<task type="auto">
  <name>Task 3: Write tests/workflow-ingest-docs.test.cjs and tests/workflow-eval-review.test.cjs (workflow-shape assertions + Plan-02-01 regression guards + W3 ABSENCE guard)</name>
  <files>tests/workflow-ingest-docs.test.cjs, tests/workflow-eval-review.test.cjs</files>
  <read_first>
    - oto/workflows/ingest-docs.md (post-Plan-02-01 — re-read to confirm the exact substrings used in assertions actually appear in the rebrand+fixup output; the substrings come from research's empirical probe but the Plan 02-01 hand-fixups may have nudged a few)
    - oto/workflows/eval-review.md (post-Plan-02-01 — same confirmation)
    - foundation-frameworks/get-shit-done-main/tests/ingest-docs.test.cjs (upstream test-style precedent; Phase 3 will port the full thing, this plan ports the workflow-shape subset)
    - tests/phase-04-frontmatter-schema.test.cjs (repo style; `node:test` + `node:assert/strict` + CJS)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Code Examples → smoke test sample)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-01-PLAN.md (Task 2 Part C — the list of upstream agent-writes-directly phrasing fragments that this test's W3 ABSENCE guard must forbid)
  </read_first>
  <action>
    **Part A — Write `tests/workflow-ingest-docs.test.cjs`.** Use the Write tool. The test must cover WF-ING-01..04 plus the three Plan-02-01 regression guards PLUS the W3 ABSENCE regression-guard (locks REPLACE semantics from Plan 02-01 Task 2 Part C). Write the file verbatim using this structure (the test names and assertion strings are the locked verification contract):

    ```javascript
    // tests/workflow-ingest-docs.test.cjs
    // Workflow-shape assertions for oto/workflows/ingest-docs.md (Plan 02-03).
    // Covers WF-ING-01..04 and locks the three Plan-02-01 hand-fixup regression guards
    // (.planning/ → .oto/ prose, SDK-fallback, classifier read-only-agent reshape).
    // Additionally adds the W3 ABSENCE regression-guard (Phase 2 revision iter 1):
    // asserts upstream agent-writes-directly phrasing fragments are GONE from the
    // workflow body — locks REPLACE semantics from Plan 02-01 Task 2 Part C.
    'use strict';

    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');

    const ROOT = path.resolve(__dirname, '..');
    const WF = fs.readFileSync(path.join(ROOT, 'oto/workflows/ingest-docs.md'), 'utf8');

    test('WF-ING-01: workflow body is the rebrand-ported executable (not deferral stub)', () => {
      assert.ok(WF.includes('<step name="banner">'), 'must have banner step');
      assert.ok(WF.includes('<step name="discover_docs">'), 'must have discovery step');
      assert.ok(WF.includes('<step name="classify_parallel">'), 'must have parallel classification step');
      assert.ok(WF.includes('<step name="synthesize">'), 'must have synthesize step');
      assert.ok(WF.includes('<step name="conflict_gate">'), 'must have conflict gate step');
      assert.ok(!/intentionally non-executable/i.test(WF), 'no deferral framing');
      assert.ok(!/DEFERRED/.test(WF), 'no DEFERRED marker');
      // Sanity: rebrand engine handled gsd- → oto- substitutions
      assert.ok(!WF.includes('gsd-doc-classifier'), 'no gsd-doc-classifier token leaked');
      assert.ok(!WF.includes('gsd-doc-synthesizer'), 'no gsd-doc-synthesizer token leaked');
    });

    test('WF-ING-02: discovery covers directory conventions + --manifest', () => {
      assert.ok(WF.includes('docs/adr/'), 'must cover docs/adr/');
      assert.ok(WF.includes('docs/prd/'), 'must cover docs/prd/');
      assert.ok(WF.includes('docs/specs/'), 'must cover docs/specs/');
      assert.ok(WF.includes('docs/rfc/'), 'must cover docs/rfc/');
      assert.match(WF, /--manifest/, 'must support --manifest flag');
    });

    test('WF-ING-03: --mode new and --mode merge both present with auto-detect', () => {
      assert.match(WF, /--mode\s+new/, 'must support --mode new');
      assert.match(WF, /--mode\s+merge/, 'must support --mode merge');
      assert.ok(WF.includes('<step name="route_new_mode">'), 'must have route_new_mode step');
      assert.ok(WF.includes('<step name="route_merge_mode">'), 'must have route_merge_mode step');
    });

    test('WF-ING-04: 50-doc cap and BLOCKER gate are wired', () => {
      assert.ok(WF.includes('50'), 'cap value 50 referenced');
      assert.ok(/exceeds the v1 cap of 50/i.test(WF), 'cap text present');
      assert.ok(WF.includes('BLOCKERS'), 'BLOCKER gate present');
      assert.ok(WF.includes('Exit WITHOUT'), 'gate exits without writing on blocker');
    });

    test('Phase 1 agents are referenced by oto- names', () => {
      assert.ok(WF.includes('oto-doc-classifier'), 'classifier referenced');
      assert.ok(WF.includes('oto-doc-synthesizer'), 'synthesizer referenced');
      // route_new_mode delegates to oto-roadmapper
      assert.ok(WF.includes('oto-roadmapper'), 'roadmapper referenced for new-mode bootstrap');
    });

    test('Plan-02-01 regression guard 1: no prose-embedded .planning/ leaked from rebrand', () => {
      const planningHits = WF.match(/\.planning\b/g) || [];
      assert.equal(planningHits.length, 0,
        `found .planning/ in workflow body: ${planningHits.length} hits — Plan 02-01 hand-fixup pass 1 incomplete`);
    });

    test('Plan-02-01 regression guard 2: every oto-sdk query call has 2>/dev/null or || fallback', () => {
      const lines = WF.split('\n');
      const sdkLinesWithoutFallback = lines.filter((l) =>
        l.includes('oto-sdk query') && !/2>\/dev\/null/.test(l) && !/\|\|/.test(l)
      );
      assert.equal(sdkLinesWithoutFallback.length, 0,
        `found oto-sdk query call(s) without fallback:\n${sdkLinesWithoutFallback.join('\n')}`);
    });

    test('Plan-02-01 regression guard 3: classify_parallel step uses orchestrator-persistence (Phase 1 D-04)', () => {
      // Extract the <step name="classify_parallel">...</step> block
      const match = WF.match(/<step name="classify_parallel">[\s\S]*?<\/step>/);
      assert.ok(match, 'classify_parallel step block must be findable');
      const block = match[0];
      assert.ok(/orchestrator/i.test(block),
        'classify_parallel block must contain "orchestrator" wording (Phase 1 D-04 read-only-agent persistence reshape)');
    });

    test('W3 ABSENCE regression-guard: upstream agent-writes-directly phrasing is GONE (REPLACE semantics from Plan 02-01 Task 2 Part C)', () => {
      // The Plan 02-01 hand-fixup pass 3 REPLACES the upstream phrasing — does not augment it.
      // If any of these fragments appear in the workflow body, REPLACE semantics were violated
      // (the executor appended new orchestrator wording but left the upstream agent-writes-directly
      // wording in place). This locks the contract that Plan 02-01 must enforce.
      const forbiddenFragments = [
        /agent writes the classification/i,
        /agent persists the classification/i,
        /agent persists the JSON/i,
        /the classifier writes/i,
        /classifier writes the JSON/i,
        /classifier writes\s+to\s+OUTPUT_DIR/i,
      ];
      for (const re of forbiddenFragments) {
        assert.ok(!re.test(WF),
          `forbidden upstream agent-writes-directly phrasing found in workflow body (matches /${re.source}/${re.flags}): Plan 02-01 Task 2 Part C must REPLACE this with orchestrator-persistence wording, not augment it.`);
      }
    });

    test('Pre-existing security checks survived rebrand+fixup', () => {
      assert.match(WF, /\*\.\.\*/, 'path-traversal *..* rejection preserved');
      assert.ok(WF.includes('realpath'), 'realpath containment preserved');
      assert.ok(WF.includes('Exit WITHOUT'), 'BLOCKER-gate exit clause preserved');
    });

    test('CMD-01 sanity: command file has no deferral framing (also asserted in workflow-no-deferral-marker.test.cjs)', () => {
      const cmd = fs.readFileSync(path.join(ROOT, 'oto/commands/oto/ingest-docs.md'), 'utf8');
      assert.ok(!/intentionally non-executable/i.test(cmd));
      assert.ok(!/\bdeferred\b/i.test(cmd));
    });
    ```

    **Part B — Write `tests/workflow-eval-review.test.cjs`.** Same shape, scoped to eval-review:

    ```javascript
    // tests/workflow-eval-review.test.cjs
    // Workflow-shape assertions for oto/workflows/eval-review.md (Plan 02-03).
    // Covers WF-EVAL-01..02 and locks the Plan-02-01 hand-fixup regression guards.
    // Additionally adds the W3 ABSENCE regression-guard (Phase 2 revision iter 1):
    // asserts upstream auditor-writes-directly phrasing fragments are GONE from the
    // workflow body — locks REPLACE semantics from Plan 02-01 Task 2 Part C.
    'use strict';

    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');

    const ROOT = path.resolve(__dirname, '..');
    const WF = fs.readFileSync(path.join(ROOT, 'oto/workflows/eval-review.md'), 'utf8');

    test('WF-EVAL-01: workflow body is the rebrand-ported executable (not deferral stub)', () => {
      assert.ok(WF.includes('oto-eval-auditor'), 'must reference the eval auditor agent');
      assert.ok(!/intentionally non-executable/i.test(WF), 'no deferral framing');
      assert.ok(!/DEFERRED/.test(WF), 'no DEFERRED marker');
      assert.ok(!WF.includes('gsd-eval-auditor'), 'no gsd- token leaked');
      // The rebranded body is ~155 LOC; a stub would be ~25
      const lines = WF.split('\n').length;
      assert.ok(lines >= 100, `expected ≥100 lines for rebranded body, got ${lines}`);
    });

    test('WF-EVAL-02: workflow produces EVAL-REVIEW.md with COVERED/PARTIAL/MISSING scoring', () => {
      assert.ok(WF.includes('EVAL-REVIEW.md'), 'output target named');
      assert.ok(WF.includes('COVERED'), 'COVERED scoring vocabulary');
      assert.ok(WF.includes('PARTIAL'), 'PARTIAL scoring vocabulary');
      assert.ok(WF.includes('MISSING'), 'MISSING scoring vocabulary');
      assert.ok(WF.includes('AI-SPEC.md'), 'State A/B trigger (AI-SPEC.md presence) referenced');
    });

    test('Plan-02-01 regression guard 1: no prose-embedded .planning/ in eval-review workflow', () => {
      const planningHits = WF.match(/\.planning\b/g) || [];
      assert.equal(planningHits.length, 0,
        `found .planning/ in workflow body: ${planningHits.length} hits`);
    });

    test('Plan-02-01 regression guard 2: every oto-sdk query call has 2>/dev/null or || fallback', () => {
      const lines = WF.split('\n');
      const sdkLinesWithoutFallback = lines.filter((l) =>
        l.includes('oto-sdk query') && !/2>\/dev\/null/.test(l) && !/\|\|/.test(l)
      );
      assert.equal(sdkLinesWithoutFallback.length, 0,
        `found oto-sdk query call(s) without fallback:\n${sdkLinesWithoutFallback.join('\n')}`);
    });

    test('Plan-02-01 regression guard 3: auditor dispatch uses orchestrator-persistence (Phase 1 D-04)', () => {
      // Find the block around oto-eval-auditor reference; assert 'orchestrator' wording appears
      // within a 30-line window (forward or backward) of any auditor mention.
      const lines = WF.split('\n');
      let foundReshape = false;
      lines.forEach((line, idx) => {
        if (line.includes('oto-eval-auditor')) {
          const start = Math.max(0, idx - 10);
          const end = Math.min(lines.length, idx + 20);
          const window = lines.slice(start, end).join('\n');
          if (/orchestrator/i.test(window)) foundReshape = true;
        }
      });
      assert.ok(foundReshape,
        'audit step must contain "orchestrator" wording within ~30 lines of an oto-eval-auditor reference (Phase 1 D-04 read-only-agent persistence reshape)');
    });

    test('W3 ABSENCE regression-guard: upstream auditor-writes-directly phrasing is GONE (REPLACE semantics from Plan 02-01 Task 2 Part C)', () => {
      // Same intent as the ingest-docs equivalent: locks the contract that Plan 02-01's hand-fixup
      // pass 3 REPLACES the upstream agent-writes-directly phrasing for the auditor step,
      // rather than appending the new orchestrator wording alongside it.
      const forbiddenFragments = [
        /auditor writes EVAL-REVIEW/i,
        /the auditor writes/i,
        /auditor persists EVAL-REVIEW/i,
        /agent writes EVAL-REVIEW/i,
        /agent persists EVAL-REVIEW/i,
      ];
      for (const re of forbiddenFragments) {
        assert.ok(!re.test(WF),
          `forbidden upstream auditor-writes-directly phrasing found in workflow body (matches /${re.source}/${re.flags}): Plan 02-01 Task 2 Part C must REPLACE this with orchestrator-persistence wording, not augment it.`);
      }
    });

    test('CMD-02 sanity: command file has no deferral framing', () => {
      const cmd = fs.readFileSync(path.join(ROOT, 'oto/commands/oto/eval-review.md'), 'utf8');
      assert.ok(!/intentionally non-executable/i.test(cmd));
      assert.ok(!/\bdeferred\b/i.test(cmd));
    });
    ```

    **Part C — Run both files in isolation:**

    ```bash
    node --test tests/workflow-ingest-docs.test.cjs tests/workflow-eval-review.test.cjs
    ```

    Expected: all named tests pass (`fail: 0`). If any assertion fails, the failure indicates a real gap in Plan 02-01's output — HALT and surface to the user. Do NOT weaken the assertion to make it pass (the assertion is the contract per research's "Phase Requirements → Test Map" + the W3 fix's REPLACE-semantics contract).

    **Part D — Confirm full suite green:**

    ```bash
    npm test 2>&1 | tail -10
    ```

    Expected: `fail: 0`.
  </action>
  <verify>
    <automated>test -f tests/workflow-ingest-docs.test.cjs && test -f tests/workflow-eval-review.test.cjs && [ $(wc -l < tests/workflow-ingest-docs.test.cjs) -gt 80 ] && [ $(wc -l < tests/workflow-eval-review.test.cjs) -gt 60 ] && grep -q "WF-ING-01" tests/workflow-ingest-docs.test.cjs && grep -q "WF-ING-02" tests/workflow-ingest-docs.test.cjs && grep -q "WF-ING-03" tests/workflow-ingest-docs.test.cjs && grep -q "WF-ING-04" tests/workflow-ingest-docs.test.cjs && grep -q "WF-EVAL-01" tests/workflow-eval-review.test.cjs && grep -q "WF-EVAL-02" tests/workflow-eval-review.test.cjs && grep -q "regression guard 1" tests/workflow-ingest-docs.test.cjs && grep -q "regression guard 2" tests/workflow-ingest-docs.test.cjs && grep -q "regression guard 3" tests/workflow-ingest-docs.test.cjs && grep -q "regression guard 1" tests/workflow-eval-review.test.cjs && grep -q "W3 ABSENCE regression-guard" tests/workflow-ingest-docs.test.cjs && grep -q "W3 ABSENCE regression-guard" tests/workflow-eval-review.test.cjs && grep -q "agent writes the classification" tests/workflow-ingest-docs.test.cjs && grep -q "auditor writes EVAL-REVIEW" tests/workflow-eval-review.test.cjs && node --test tests/workflow-ingest-docs.test.cjs tests/workflow-eval-review.test.cjs 2>&1 | grep -q "fail 0\|fail: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/workflow-ingest-docs.test.cjs && test -f tests/workflow-eval-review.test.cjs` exits 0
    - `wc -l < tests/workflow-ingest-docs.test.cjs` ≥ 80
    - `wc -l < tests/workflow-eval-review.test.cjs` ≥ 60
    - `grep -c "test('WF-ING-01" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "test('WF-ING-02" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "test('WF-ING-03" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "test('WF-ING-04" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "test('WF-EVAL-01" tests/workflow-eval-review.test.cjs` equals `1`
    - `grep -c "test('WF-EVAL-02" tests/workflow-eval-review.test.cjs` equals `1`
    - `grep -c "regression guard 1" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "regression guard 2" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "regression guard 3" tests/workflow-ingest-docs.test.cjs` equals `1`
    - `grep -c "regression guard 1" tests/workflow-eval-review.test.cjs` equals `1`
    - `grep -c "regression guard 2" tests/workflow-eval-review.test.cjs` equals `1`
    - `grep -c "regression guard 3" tests/workflow-eval-review.test.cjs` equals `1`
    - `grep -c "W3 ABSENCE regression-guard" tests/workflow-ingest-docs.test.cjs` equals `1` (W3 fix present)
    - `grep -c "W3 ABSENCE regression-guard" tests/workflow-eval-review.test.cjs` equals `1` (W3 fix present)
    - `grep -q "agent writes the classification" tests/workflow-ingest-docs.test.cjs` exits 0 (W3 fragment list present)
    - `grep -q "auditor writes EVAL-REVIEW" tests/workflow-eval-review.test.cjs` exits 0 (W3 fragment list present)
    - `node --test tests/workflow-ingest-docs.test.cjs tests/workflow-eval-review.test.cjs` exits 0 (all tests pass)
    - `npm test` exits 0 and the summary shows `fail 0`
  </acceptance_criteria>
  <done>
    Two new workflow-shape test files exist and pass under `node --test`. Together they cover WF-ING-01..04, WF-EVAL-01..02, lock the three Plan-02-01 hand-fixup regression guards (no prose `.planning/`, SDK fallback, classifier/auditor read-only-agent reshape), AND lock the W3 ABSENCE regression-guard (upstream agent-writes-directly phrasing fragments are forbidden — REPLACE semantics from Plan 02-01 Task 2 Part C). Full `npm test` suite stays green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write tests/workflow-ingest-docs-fixture.test.cjs (fixture-tree well-formedness + os.tmpdir over-cap fixture generation)</name>
  <files>tests/workflow-ingest-docs-fixture.test.cjs</files>
  <read_first>
    - tests/fixtures/ingest-docs/new-mode-mixed (post-Task-1a — confirm directory structure exists; tree should be 10 .md files across 4 type dirs)
    - tests/fixtures/ingest-docs/merge-mode-existing (post-Task-1b — confirm .oto/ skeleton + 3 docs)
    - tests/fixtures/ingest-docs/conflict-block (post-Task-1b — confirm 3 docs)
    - tests/phase-04-frontmatter-schema.test.cjs (repo style precedent)
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-RESEARCH.md ("Code Examples" smoke-test sample for the fixture-validity test shape; Open Question 3 resolution: over-cap is tmpdir-generated)
  </read_first>
  <action>
    Write `tests/workflow-ingest-docs-fixture.test.cjs` verbatim with the Write tool. The test asserts fixture well-formedness for the three committed trees and generates a 51-doc over-cap fixture in `os.tmpdir()` via `fs.mkdtempSync` (cleaned up in `after`). Per Open Question 3 resolution, the over-cap fixture is NOT committed to avoid repo bloat.

    File body (write verbatim):

    ```javascript
    // tests/workflow-ingest-docs-fixture.test.cjs
    // Asserts fixture-tree well-formedness for the three committed fixtures and
    // generates an over-cap fixture (51 docs) in os.tmpdir() to verify the cap
    // assertion can be made. Workflows themselves run inside an LLM runtime —
    // these tests verify only that the fixtures the workflow would consume are
    // shaped correctly.
    'use strict';

    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');
    const os = require('node:os');

    const ROOT = path.resolve(__dirname, '..');
    const FIXTURES = path.join(ROOT, 'tests/fixtures/ingest-docs');

    function listMd(dir) {
      const out = [];
      function walk(d) {
        for (const entry of fs.readdirSync(d, { withFileTypes: true })) {
          const full = path.join(d, entry.name);
          if (entry.isDirectory()) walk(full);
          else if (entry.isFile() && entry.name.endsWith('.md')) out.push(full);
        }
      }
      walk(dir);
      return out;
    }

    test('fixture new-mode-mixed: 10 docs distributed 3 ADR + 3 PRD + 2 SPEC + 2 RFC', () => {
      const base = path.join(FIXTURES, 'new-mode-mixed/docs');
      assert.equal(listMd(path.join(base, 'adr')).length, 3, 'expected 3 ADRs');
      assert.equal(listMd(path.join(base, 'prd')).length, 3, 'expected 3 PRDs');
      assert.equal(listMd(path.join(base, 'specs')).length, 2, 'expected 2 SPECs');
      assert.equal(listMd(path.join(base, 'rfc')).length, 2, 'expected 2 RFCs');
      assert.equal(listMd(base).length, 10, 'total 10 docs');
      // Every doc has YAML frontmatter
      for (const f of listMd(base)) {
        const head = fs.readFileSync(f, 'utf8').slice(0, 4);
        assert.equal(head, '---\n', `missing frontmatter: ${path.relative(ROOT, f)}`);
      }
      // Each doc count is under the 50-doc cap
      assert.ok(listMd(base).length <= 50, 'must be under 50-doc cap');
    });

    test('fixture merge-mode-existing: pre-existing .oto/ skeleton (4 files) + 3 new docs', () => {
      const base = path.join(FIXTURES, 'merge-mode-existing');
      assert.ok(fs.existsSync(path.join(base, '.oto/PROJECT.md')), 'PROJECT.md exists');
      assert.ok(fs.existsSync(path.join(base, '.oto/REQUIREMENTS.md')), 'REQUIREMENTS.md exists');
      assert.ok(fs.existsSync(path.join(base, '.oto/ROADMAP.md')), 'ROADMAP.md exists');
      assert.ok(fs.existsSync(path.join(base, '.oto/STATE.md')), 'STATE.md exists');
      const newDocs = listMd(path.join(base, 'docs'));
      assert.equal(newDocs.length, 3, 'expected 3 new docs to merge');
    });

    test('fixture conflict-block: 2 contradictory ADRs + 1 PRD', () => {
      const base = path.join(FIXTURES, 'conflict-block/docs');
      const adrs = listMd(path.join(base, 'adr'));
      assert.equal(adrs.length, 2, 'expected 2 ADRs');
      const prds = listMd(path.join(base, 'prd'));
      assert.equal(prds.length, 1, 'expected 1 PRD');
      // The two ADRs must declare directly contradictory locked decisions
      const adrTexts = adrs.map((f) => fs.readFileSync(f, 'utf8'));
      const hasPostgres = adrTexts.some((t) => /PostgreSQL/i.test(t));
      const hasMongo = adrTexts.some((t) => /MongoDB/i.test(t));
      assert.ok(hasPostgres, 'one ADR must lock PostgreSQL');
      assert.ok(hasMongo, 'one ADR must lock MongoDB');
      const hasLocked = adrTexts.every((t) => /LOCKED/i.test(t));
      assert.ok(hasLocked, 'both ADRs must declare LOCKED status (BLOCKER trigger)');
    });

    test('over-cap fixture (tmpdir-generated): 51 minimal docs triggers cap enforcement', async (t) => {
      const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'oto-over-cap-'));
      t.after(() => fs.rmSync(tmp, { recursive: true, force: true }));

      const adrDir = path.join(tmp, 'docs/adr');
      fs.mkdirSync(adrDir, { recursive: true });
      for (let i = 1; i <= 51; i += 1) {
        const id = String(i).padStart(3, '0');
        const body = `---\nid: ADR-${id}\ntitle: Decision ${id}\nstatus: accepted\n---\n\n# ADR-${id}\n\nMinimal fixture doc for over-cap test.\n`;
        fs.writeFileSync(path.join(adrDir, `ADR-${id}-minimal.md`), body);
      }

      const docs = listMd(path.join(tmp, 'docs'));
      assert.equal(docs.length, 51, `expected 51 docs in tmpdir, got ${docs.length}`);
      assert.ok(docs.length > 50, 'over-cap fixture must exceed the 50-doc cap');
    });
    ```

    Run the test in isolation:

    ```bash
    node --test tests/workflow-ingest-docs-fixture.test.cjs
    ```

    Expected: 4 tests pass; the tmpdir from the over-cap test is cleaned up by `t.after`.

    Full suite confirmation:

    ```bash
    npm test 2>&1 | tail -10
    ```

    Expected: `fail: 0`.
  </action>
  <verify>
    <automated>test -f tests/workflow-ingest-docs-fixture.test.cjs && [ $(wc -l < tests/workflow-ingest-docs-fixture.test.cjs) -gt 50 ] && grep -q "fixture new-mode-mixed" tests/workflow-ingest-docs-fixture.test.cjs && grep -q "fixture merge-mode-existing" tests/workflow-ingest-docs-fixture.test.cjs && grep -q "fixture conflict-block" tests/workflow-ingest-docs-fixture.test.cjs && grep -q "over-cap fixture" tests/workflow-ingest-docs-fixture.test.cjs && grep -q "mkdtempSync" tests/workflow-ingest-docs-fixture.test.cjs && grep -q "t.after" tests/workflow-ingest-docs-fixture.test.cjs && node --test tests/workflow-ingest-docs-fixture.test.cjs 2>&1 | grep -q "fail 0\|fail: 0" && npm test 2>&1 | tail -20 | grep -q "fail 0\|fail: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/workflow-ingest-docs-fixture.test.cjs` exits 0
    - `wc -l < tests/workflow-ingest-docs-fixture.test.cjs` ≥ 50
    - File requires `node:test`, `node:assert/strict`, `node:fs`, `node:path`, `node:os` (zero external deps)
    - `grep -c "fixture new-mode-mixed" tests/workflow-ingest-docs-fixture.test.cjs` equals `1`
    - `grep -c "fixture merge-mode-existing" tests/workflow-ingest-docs-fixture.test.cjs` equals `1`
    - `grep -c "fixture conflict-block" tests/workflow-ingest-docs-fixture.test.cjs` equals `1`
    - `grep -c "over-cap fixture" tests/workflow-ingest-docs-fixture.test.cjs` equals `1`
    - `grep -q "mkdtempSync" tests/workflow-ingest-docs-fixture.test.cjs` exits 0 (tmpdir generation)
    - `grep -q "t.after" tests/workflow-ingest-docs-fixture.test.cjs` exits 0 (cleanup hook present)
    - `node --test tests/workflow-ingest-docs-fixture.test.cjs` exits 0 (4 tests pass)
    - `npm test` exits 0; summary shows `fail 0`
    - After test run, no `oto-over-cap-*` directories remain in `os.tmpdir()`: `ls $(node -e "console.log(require('os').tmpdir())") | grep -c '^oto-over-cap-'` equals `0` (the t.after hook cleaned up)
  </acceptance_criteria>
  <done>
    `tests/workflow-ingest-docs-fixture.test.cjs` exists and passes (4 tests). The three committed fixture trees are asserted well-formed; the over-cap fixture is generated in `os.tmpdir()` at test-time (51 docs) and cleaned up. Full `npm test` suite green. Phase 2 verification surface complete: WF-ING-01..04 and WF-EVAL-01..02 are locked by Plan 02-01 (file content), Plan 02-02 (no-deferral assertion), and Plan 02-03 (workflow-shape + fixture-tree assertion + regression guards for the three engine blind spots + W3 ABSENCE guard for REPLACE semantics).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo only (committed fixtures) | Fixture .md files are checked into the repo. They are public-by-design (no secrets, no PII; synthetic ADR/PRD/SPEC/RFC content). |
| os.tmpdir() (over-cap fixture) | The over-cap fixture is generated inside `os.tmpdir()` via `fs.mkdtempSync` (unique subdir per run). Cleaned up by `t.after`. No data crosses this boundary at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Engine blind-spot regression (prose .planning/, SDK fallback, classifier read-only reshape) | mitigate | Three regression-guard tests in tests/workflow-ingest-docs.test.cjs + three in tests/workflow-eval-review.test.cjs fail on any future reintroduction of the three blind spots. Locks Plan 02-01's hand-fixup passes. |
| T-02-03-02 | Tampering | Pre-existing security-check removal during future rebrand sync | mitigate | The "Pre-existing security checks survived rebrand+fixup" test in tests/workflow-ingest-docs.test.cjs asserts presence of `*..*`, `realpath`, `Exit WITHOUT`. Fails on any future sync that loses one. |
| T-02-03-03 | Tampering | Augment-instead-of-replace bug in agent-writes-directly reshape (REPLACE semantics violation) | mitigate | W3 ABSENCE regression-guard tests in both tests/workflow-ingest-docs.test.cjs and tests/workflow-eval-review.test.cjs assert the upstream agent-writes-directly phrasing fragments ("agent writes the classification", "auditor writes EVAL-REVIEW", etc.) are GONE from the workflow body. Fails if Plan 02-01 Task 2 Part C ever appends new wording without removing the upstream wording. |
| T-02-03-04 | Information Disclosure | Fixture content shipped with package | accept | Synthetic content; no secrets, no PII. The `files` allowlist in package.json determines whether `tests/` ships in `npm install -g`; in practice tests are usually excluded. |
| T-02-03-05 | Resource Exhaustion | tmpdir over-cap fixture not cleaned up | mitigate | `t.after(() => fs.rmSync(tmp, { recursive: true, force: true }))` guarantees cleanup even on test failure. Acceptance criterion `ls tmpdir | grep -c '^oto-over-cap-' == 0` verifies. |

Note: No new auth/network/secrets/UI surface. The tests themselves are the security artifact (regression-guards + invariant assertions + W3 ABSENCE guard).
</threat_model>

<verification>
- `tests/workflow-ingest-docs.test.cjs` passes (≥11 named tests covering WF-ING-01..04 + 3 regression guards + W3 ABSENCE guard + security-check preservation + CMD-01 sanity).
- `tests/workflow-eval-review.test.cjs` passes (≥7 named tests covering WF-EVAL-01..02 + 3 regression guards + W3 ABSENCE guard + CMD-02 sanity).
- `tests/workflow-ingest-docs-fixture.test.cjs` passes (4 tests covering 3 committed fixtures + 1 tmpdir-generated over-cap fixture).
- Three committed fixture trees seeded with correct doc counts, types, distribution, frontmatter, and contradictory-ADR content (conflict-block). Seeding is split across Task 1a (new-mode-mixed, 10 docs) and Task 1b (merge-mode-existing + conflict-block, 10 docs) per W4 fix.
- Over-cap fixture generated in `os.tmpdir()` at test time (51 docs); cleaned up after.
- Full `npm test` suite green; no regressions.
- All three Plan-02-01 hand-fixup passes locked against regression (`.planning/` → `.oto/`, SDK fallback, classifier/auditor read-only-agent reshape).
- W3 ABSENCE regression-guard locks REPLACE semantics from Plan 02-01 Task 2 Part C — upstream agent-writes-directly phrasing fragments forbidden in either workflow body.
- Pre-existing security checks (`*..*`, `realpath`, `Exit WITHOUT`) locked against regression.
</verification>

<success_criteria>
1. Three new test files exist and pass under `node --test`: `tests/workflow-ingest-docs.test.cjs`, `tests/workflow-eval-review.test.cjs`, `tests/workflow-ingest-docs-fixture.test.cjs`.
2. Three committed fixture trees seeded under `tests/fixtures/ingest-docs/` across Task 1a + Task 1b (W4 split): `new-mode-mixed` (10 docs, Task 1a), `merge-mode-existing` (4-file .oto skeleton + 3 docs, Task 1b), `conflict-block` (2 contradictory ADRs + 1 PRD, Task 1b).
3. Over-cap fixture (51 docs) generated and cleaned up in `os.tmpdir()` per test run.
4. Full `npm test` suite stays green at the Phase 2 baseline + the new tests; no regressions.
5. The three Plan-02-01 hand-fixup passes are locked by regression-guard tests; any future regression fails the suite.
6. The W3 ABSENCE regression-guard locks REPLACE semantics from Plan 02-01 Task 2 Part C; the test fails if any future rebrand sync reintroduces the upstream agent-writes-directly phrasing alongside (or instead of) the orchestrator-persistence wording.
7. WF-ING-01..04 and WF-EVAL-01..02 are end-to-end locked: file content (Plan 02-01) + assertion tests (Plan 02-02 for CMD-01/02/03; this plan for WF-* shape + regression guards) + fixture-tree shape (this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-03-SUMMARY.md` capturing:
- The three committed fixture trees' final file counts (per directory, matching the planned 3/3/2/2 + 4-skeleton + 3 + 2/1 distribution), with Task 1a vs Task 1b attribution
- Test files written with their test counts and pass counts (note the new W3 ABSENCE regression-guard tests in both workflow-shape test files)
- `node --test` output for each test file (`tests/workflow-ingest-docs.test.cjs`, `tests/workflow-eval-review.test.cjs`, `tests/workflow-ingest-docs-fixture.test.cjs`)
- `npm test` summary (`fail: 0` confirmed)
- Confirmation `os.tmpdir()` contains no `oto-over-cap-*` directories after `npm test` completes (cleanup verified)
- List of which engine blind spots are now regression-guarded (prose .planning/, SDK fallback, classifier/auditor read-only-agent reshape, pre-existing security checks, W3 ABSENCE REPLACE-semantics guard)
</output>
</content>
</invoke>