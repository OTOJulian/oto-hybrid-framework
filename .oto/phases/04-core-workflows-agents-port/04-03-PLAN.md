---
phase: 04-core-workflows-agents-port
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - oto/workflows/plan-phase.md
  - oto/commands/oto/debug.md
autonomous: true
requirements_addressed:
  - WF-03
  - WF-10
must_haves:
  truths:
    - "oto/workflows/plan-phase.md no longer references oto-pattern-mapper as an executable Task call"
    - "oto/commands/oto/debug.md no longer references oto-debug-session-manager as an executable Task call"
    - "Both files retain their core flow; only the dropped-agent invocations are rewritten or marked deferred per D-09/D-11"
  artifacts:
    - path: "oto/workflows/plan-phase.md"
      provides: "WF-03 workflow without dropped-agent reference"
    - path: "oto/commands/oto/debug.md"
      provides: "WF-10 command using only oto-debugger (which absorbs debug-session-manager per agent-audit.md)"
  key_links:
    - from: "oto/commands/oto/debug.md"
      to: "oto-debugger agent"
      via: "Task(subagent_type=...)"
      pattern: "subagent_type[\"']?\\s*[:=]\\s*[\"']oto-debugger"
---

<objective>
Apply the two core dropped-agent rewrites identified in 04-RESEARCH § "Category A — Dropped-agent invocations (BLOCKERS)" items 1 and 2:

1. `oto/workflows/plan-phase.md` line ~685: rewrite or defer the `oto-pattern-mapper` step (the upstream "Optional" §7.8 codebase-pattern-mapping step references a dropped agent).
2. `oto/commands/oto/debug.md` (5 invocations): replace `oto-debug-session-manager` references with `oto-debugger` per agent-audit.md ("oto-debugger absorbs debug-session-manager responsibilities").

These are D-09 hard failures if shipped as-is. This plan unblocks WF-03 and WF-10 for the dogfood in Wave 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflows-agents-port/04-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-RESEARCH.md
@decisions/agent-audit.md
@decisions/ADR-07-agent-trim.md
@oto/workflows/plan-phase.md
@oto/commands/oto/debug.md

<interfaces>
<!-- Agent-audit verdicts that govern these rewrites -->

From `decisions/agent-audit.md`:
- `gsd-pattern-mapper` → DROP (ADR-07 trim verdict). No retained replacement; closest analogue is `oto-codebase-mapper` but the audit notes their roles differ.
- `gsd-debug-session-manager` → DROP, with explicit note: "responsibilities absorbed by oto-debugger". Direct rewrite is sanctioned.

From RESEARCH § "Category A":
- `oto/workflows/plan-phase.md` references the pattern-mapper inside an "Optional §7.8" step. Recommended action: convert to a deferred TODO block, NOT a rewrite to `oto-codebase-mapper` (because the role differs — see Assumption A2).
- `oto/commands/oto/debug.md` invocations at lines 30, 163, 220, 241, 260: rewrite to `oto-debugger`. There is also a `gsd-phase-researcher` reference (a KEEP agent) which the engine already rebranded — leave alone.

Generic allowlist (NOT a violation): `general-purpose`, `generalPurpose`, `general`, `X`, `general_purpose_task` — leave as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite oto/commands/oto/debug.md to use oto-debugger only</name>
  <files>oto/commands/oto/debug.md</files>
  <read_first>
    - oto/commands/oto/debug.md (full file — to see every `subagent_type=` occurrence and surrounding context)
    - decisions/agent-audit.md (the row for gsd-debug-session-manager confirming oto-debugger absorbs the role)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Category A" item 2 (lines ~482-484)
  </read_first>
  <action>
    Open `oto/commands/oto/debug.md`. Find every occurrence of the substring `oto-debug-session-manager` (RESEARCH cites 5 occurrences at lines 30, 163, 220, 241, 260 — verify exact count and locations in the rebranded file, which may differ slightly).

    For each occurrence, apply the following rewrite rule:

    - If the occurrence is inside a `Task(subagent_type="..."...)` call: replace the agent name with `oto-debugger`.
    - If the occurrence is inside prose describing the workflow (e.g., "Spawn oto-debug-session-manager to ..."): rewrite the prose to reference `oto-debugger` directly. The agent-audit.md justification ("absorbs debug-session-manager responsibilities") is the warrant.
    - If the occurrence is a section heading (e.g., `## Debug Session Manager`), rename to `## Debugger` or fold the section into the surrounding flow.

    DO NOT introduce a new helper agent, do NOT mark these as deferred (D-09 says executable refs MUST point to retained agents; oto-debugger is retained), and do NOT preserve the dropped name in any form. Even comments referencing `oto-debug-session-manager` should be removed.

    Leave any `gsd-phase-researcher` / `oto-phase-researcher` reference untouched — that agent is retained.

    After the edit, run `grep -n "debug-session-manager" oto/commands/oto/debug.md` and confirm zero matches.
  </action>
  <acceptance_criteria>
    - `grep -c "debug-session-manager" oto/commands/oto/debug.md` returns `0`
    - `grep -c "oto-debugger" oto/commands/oto/debug.md` returns >= 1 (at least one Task call now points at oto-debugger)
    - File is still valid markdown (preserves frontmatter, headings, code fences)
    - `grep -E "subagent_type\\s*[:=]\\s*[\"']oto-debugger" oto/commands/oto/debug.md` returns at least one match (proves the rewrite landed in a Task call)
    - File still has its original `<objective>` block and overall structure (don't accidentally truncate)
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "debug-session-manager" oto/commands/oto/debug.md && grep -q "oto-debugger" oto/commands/oto/debug.md</automated>
  </verify>
  <done>debug-session-manager removed from oto/commands/oto/debug.md; oto-debugger is the sole debug agent referenced</done>
</task>

<task type="auto">
  <name>Task 2: Defer oto-pattern-mapper step in oto/workflows/plan-phase.md</name>
  <files>oto/workflows/plan-phase.md</files>
  <read_first>
    - oto/workflows/plan-phase.md (find every line containing `pattern-mapper`; RESEARCH cites line ~685 in upstream — line number in rebranded file may differ. Use grep to locate.)
    - decisions/agent-audit.md (confirms pattern-mapper is DROP with no direct retained replacement)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Category A" item 1 (lines ~479-480)
  </read_first>
  <action>
    Open `oto/workflows/plan-phase.md`. Locate every occurrence of `pattern-mapper` (the engine renames `gsd-pattern-mapper` → `oto-pattern-mapper`). RESEARCH says this is in an "Optional §7.8" step.

    For each occurrence:

    - If inside a `Task(subagent_type="oto-pattern-mapper"...)` call: replace the entire Task call (or the entire optional step block containing it) with a deferred TODO block:
      ```
      <!-- DEFERRED: oto-pattern-mapper agent dropped per ADR-07 (decisions/agent-audit.md). The original optional codebase-pattern-mapping step is deferred until a v2 replacement agent is designed. To approximate this step manually, run `/oto-map-codebase` (oto-codebase-mapper) and feed the output into your planning context. See .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Category A" item 1. -->
      ```

    - If inside prose ("Spawn oto-pattern-mapper to ..." or section text): rewrite the prose to say the step is deferred and recommend `/oto-map-codebase` as a manual approximation, with the same DEFERRED comment marker.

    - If inside a numbered section heading (e.g., `### 7.8 Pattern Mapper (Optional)`): change the heading to `### 7.8 Pattern Mapper (Deferred — see ADR-07)` and put the DEFERRED comment immediately under it.

    DO NOT rewrite to `oto-codebase-mapper` directly via Task call — Assumption A2 in RESEARCH flags that the roles differ and a silent substitution would mislead the executor.

    After the edit, confirm zero `pattern-mapper` substring matches in the file.
  </action>
  <acceptance_criteria>
    - `grep -c "pattern-mapper" oto/workflows/plan-phase.md` returns `0`
    - `grep -c "DEFERRED" oto/workflows/plan-phase.md` returns >= 1
    - The DEFERRED block references "ADR-07" (the trim decision warrant)
    - File remains valid markdown (frontmatter intact, no truncation)
    - File size after edit is within 10% of the size before edit (no accidental large deletions); use `wc -l` before/after to sanity-check
    - The rest of `oto/workflows/plan-phase.md` retains its core planning steps unchanged
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "pattern-mapper" oto/workflows/plan-phase.md && grep -q "DEFERRED" oto/workflows/plan-phase.md</automated>
  </verify>
  <done>oto-pattern-mapper step deferred with explicit DEFERRED marker; ADR-07 referenced; manual approximation suggested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — content-only edits to two markdown files within `oto/`) | These are local file edits to already-rebranded markdown. No code paths execute, no external services involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | I (Integrity — supply-chain) | shipped workflow/command files | mitigate | Wave 3 verification (`phase-04-no-dropped-agents.test.cjs`) asserts ZERO occurrences of dropped-agent substrings in shipped payload, including these two files. If a stray reference remains after this plan, the test fails and Phase 4 cannot close. |
</threat_model>

<verification>
- `grep -c "debug-session-manager" oto/commands/oto/debug.md` returns 0
- `grep -c "pattern-mapper" oto/workflows/plan-phase.md` returns 0
- `grep -c "DEFERRED" oto/workflows/plan-phase.md` returns >= 1
- Both files remain valid markdown with frontmatter intact
</verification>

<success_criteria>
- WF-03 (`/oto-plan-phase`) workflow no longer references the dropped pattern-mapper agent
- WF-10 (`/oto-debug`) command exclusively uses `oto-debugger` for debug Task calls
- Both fixes survive Wave 3 verification (no-dropped-agents test passes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflows-agents-port/04-03-SUMMARY.md` listing the exact line numbers and before/after snippets for every edit (so Phase 9 sync can replay them).
</output>
