---
phase: 05-hooks-port-consolidation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/05-token-substitution.test.cjs
  - tests/05-session-start.test.cjs
  - tests/05-merge-settings.test.cjs
  - tests/05-build-hooks.test.cjs
  - tests/05-session-start-fixture.test.cjs
  - tests/fixtures/phase-05/settings-existing.json
  - tests/fixtures/phase-05/settings-empty.json
autonomous: true
requirements: [HK-01, HK-02, HK-03, HK-04, HK-05, HK-06, HK-07]
must_haves:
  truths:
    - "5 Phase 5 verification test files exist as scaffolds with `t.todo()` placeholders that map 1:1 to tests/05-*.test.cjs files in 05-VALIDATION.md Wave 0 list"
    - "Fixture files for mergeSettings round-trip exist (one with user-authored entries, one empty `{}`) so Wave 3 can wire merge tests deterministically"
    - "`node --test tests/05-*.test.cjs` exits 0 (all todos, no failures, no productional code touched)"
  artifacts:
    - path: "tests/05-token-substitution.test.cjs"
      provides: "Wave 0 placeholder for HK-07 token substitution"
    - path: "tests/05-session-start.test.cjs"
      provides: "Wave 0 placeholder for HK-01 SessionStart output"
    - path: "tests/05-merge-settings.test.cjs"
      provides: "Wave 0 placeholder for HK-01..06 mergeSettings round-trip"
    - path: "tests/05-build-hooks.test.cjs"
      provides: "Wave 0 placeholder for HK-07 build-hooks retarget"
    - path: "tests/05-session-start-fixture.test.cjs"
      provides: "Wave 0 placeholder for HK-01 fixture snapshot"
    - path: "tests/fixtures/phase-05/settings-existing.json"
      provides: "Round-trip baseline: settings.json with user-authored entries that mergeSettings must preserve"
    - path: "tests/fixtures/phase-05/settings-empty.json"
      provides: "Initial-install baseline: empty settings to merge into"
  key_links:
    - from: "tests/05-merge-settings.test.cjs"
      to: "tests/fixtures/phase-05/settings-existing.json"
      via: "require"
      pattern: "fixtures/phase-05/settings-existing"
---

<objective>
Create Wave 0 test scaffolds for Phase 5. Five `node:test` files map 1:1 to the Wave 0 list in 05-VALIDATION.md. Two fixture files (existing + empty `settings.json`) seed the mergeSettings round-trip test (Wave 3). All tests use `t.todo()` placeholders so the suite passes green while downstream waves implement the bodies.

Purpose: Mirrors Phase 4's Wave 0 precedent (`04-01-PLAN.md`). Per CLAUDE.md / Validation Architecture: every task must have an automated `<verify>` command — Wave 0 creates the test files those commands target.
Output: 5 test scaffolds + 2 fixture JSON files (no production code touched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hooks-port-consolidation/05-CONTEXT.md
@.planning/phases/05-hooks-port-consolidation/05-RESEARCH.md
@.planning/phases/05-hooks-port-consolidation/05-VALIDATION.md
@CLAUDE.md
@tests/phase-04-codex-sandbox-coverage.test.cjs
@.planning/phases/04-core-workflows-agents-port/04-01-PLAN.md

<interfaces>
<!-- Phase test pattern (from existing tests/phase-NN-*.test.cjs files). Use this exact shape. -->

```js
'use strict';
// Phase 5 Wave 0 scaffold — implemented in Wave N (plan 05-NN)
// Covers: HK-XX (...short description...)
// Will: <one-paragraph description of the eventual assertions>

const test = require('node:test');
const path = require('node:path');
const REPO_ROOT = path.resolve(__dirname, '..');

test('phase-05 <suite>: <short description>', (t) => {
  t.todo('Implemented in Wave N (plan 05-NN) after <prerequisite>');
});
```

<!-- Fixture file shape: tiny stable settings.json snippets. Round-trip test will:
     1. read fixture
     2. call mergeSettings(text, otoBlock)
     3. assert oto entries injected, user entries preserved verbatim
     4. call mergeSettings on the result again — must be byte-identical (idempotency)
     5. call uninstall (TBD helper) — must restore original. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the two settings.json fixtures under tests/fixtures/phase-05/</name>
  <files>tests/fixtures/phase-05/settings-existing.json, tests/fixtures/phase-05/settings-empty.json</files>
  <read_first>
    - .planning/phases/05-hooks-port-consolidation/05-RESEARCH.md § "Pattern 2: mergeSettings Round-Trip Pattern" (the JSON shape contract for the `_oto` block) and § "Pitfall E" (entries must have `hooks: [...]` arrays — Claude Code silently discards malformed `settings.json`)
    - tests/fixtures/phase-04/retained-agents.json (reference for fixture file layout convention under tests/fixtures/phase-NN/)
  </read_first>
  <action>
    Create directory `tests/fixtures/phase-05/` (use `fs.mkdirSync({ recursive: true })` if directly authoring; otherwise just write the files which will create the dir).

    File 1 — `tests/fixtures/phase-05/settings-existing.json` — represents a settings.json that already has user-authored entries plus one third-party hook. mergeSettings MUST preserve every key/entry below verbatim AND add the `_oto` block + oto's hook entries. Exact content (write byte-for-byte):

    ```json
    {
      "model": "claude-3-5-sonnet-20241022",
      "permissions": { "allow": ["Bash(git:*)"] },
      "hooks": {
        "PreToolUse": [
          { "matcher": "Bash", "hooks": [{ "type": "command", "command": "/Users/julian/.local/bin/user-pretool.sh" }] }
        ]
      }
    }
    ```

    File 2 — `tests/fixtures/phase-05/settings-empty.json` — represents a fresh settings.json. Exact content:

    ```json
    {}
    ```

    Each file MUST end with a single trailing newline (so `git diff` is clean).
  </action>
  <acceptance_criteria>
    - File `tests/fixtures/phase-05/settings-existing.json` exists and parses: `node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/phase-05/settings-existing.json','utf8'))"` exits 0
    - File `tests/fixtures/phase-05/settings-empty.json` exists and equals `{}\n` exactly: `node -e "const t=require('fs').readFileSync('tests/fixtures/phase-05/settings-empty.json','utf8'); if(t!=='{}\n')throw new Error('expected {}\\\\n got '+JSON.stringify(t))"` exits 0
    - `settings-existing.json` parses to an object with `model === 'claude-3-5-sonnet-20241022'`, `permissions.allow[0] === 'Bash(git:*)'`, and `hooks.PreToolUse[0].hooks[0].command === '/Users/julian/.local/bin/user-pretool.sh'`
    - `settings-existing.json` does NOT contain the substring `_oto` (Wave 3's mergeSettings will add it)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const a=JSON.parse(fs.readFileSync('tests/fixtures/phase-05/settings-existing.json','utf8'));if(a.model!=='claude-3-5-sonnet-20241022')throw new Error('model');if(a.permissions.allow[0]!=='Bash(git:*)')throw new Error('perm');if(a.hooks.PreToolUse[0].hooks[0].command!=='/Users/julian/.local/bin/user-pretool.sh')throw new Error('hook');const e=fs.readFileSync('tests/fixtures/phase-05/settings-empty.json','utf8');if(e!=='{}\n')throw new Error('empty wrong: '+JSON.stringify(e));if(fs.readFileSync('tests/fixtures/phase-05/settings-existing.json','utf8').includes('_oto'))throw new Error('should not contain _oto');"</automated>
  </verify>
  <done>Both fixture files exist with the exact content specified; existing parses correctly and contains no `_oto` substring; empty equals `{}\n` exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 5 Phase 5 test scaffolds with t.todo() placeholders</name>
  <files>tests/05-token-substitution.test.cjs, tests/05-session-start.test.cjs, tests/05-merge-settings.test.cjs, tests/05-build-hooks.test.cjs, tests/05-session-start-fixture.test.cjs</files>
  <read_first>
    - tests/phase-04-codex-sandbox-coverage.test.cjs (template — same shape and conventions as Phase 4 scaffolds; use the exact `'use strict'` + `node:test` + REPO_ROOT pattern)
    - .planning/phases/05-hooks-port-consolidation/05-VALIDATION.md (Per-Task Verification Map and Wave 0 Requirements lists — exact filenames already specified there)
    - .planning/phases/05-hooks-port-consolidation/05-RESEARCH.md § "Validation Architecture > Phase Requirements → Test Map" (lines ~599-615; specifies what each test will eventually assert)
  </read_first>
  <behavior>
    - Each scaffold file is syntactically valid JS executable by `node --test`.
    - Running `node --test tests/05-*.test.cjs` exits 0 with 5 (or more) tests, all marked `todo`, zero failures.
    - Each scaffold has a header comment block naming: (1) the requirement(s) it covers (HK-NN), (2) the Wave-N plan that implements it, (3) a one-paragraph description of the eventual assertions, sourced from 05-RESEARCH.md.
    - No production code is touched.
  </behavior>
  <action>
    Create each of the 5 test files. Each file MUST begin with `'use strict';`, require `node:test` and `node:path`, define `const REPO_ROOT = path.resolve(__dirname, '..');`, and contain at least one `test(name, (t) => { t.todo(reason); })` call. Per-file content:

    **`tests/05-token-substitution.test.cjs`** (Wave 1, plan 05-02):
    Header comment must say: "Covers HK-07. Implemented in Wave 1 (plan 05-02). Will require a `tokenReplace` helper from `bin/lib/copy-files.cjs` (or sibling), feed it a fixture string containing `{{OTO_VERSION}}`, assert output substitutes the token to a semver string matching `^\\d+\\.\\d+\\.\\d+(-[a-z0-9.]+)?$`. Will also assert allowlist exclusion: a fixture path containing `foundation-frameworks/` returns the input untouched. Round-trip: re-substitute the literal token back into the substituted output yields the original template (per 05-RESEARCH.md Pitfall D)."
    Test name: `phase-05 token-substitution: {{OTO_VERSION}} substitutes to package version`
    Todo reason: `Implemented in Wave 1 (plan 05-02) once bin/lib/copy-files.cjs::tokenReplace exists`

    **`tests/05-session-start.test.cjs`** (Wave 2, plan 05-03):
    Header comment must say: "Covers HK-01. Implemented in Wave 2 (plan 05-03). Will spawn `oto/hooks/oto-session-start` via `child_process.spawnSync('bash', [hookPath], { env: { ...process.env, CLAUDE_PLUGIN_ROOT: '/tmp/fake', COPILOT_CLI: '' } })`, parse stdout as JSON, assert: (1) exactly one `<EXTREMELY_IMPORTANT>` block via `(ctx.match(/<EXTREMELY_IMPORTANT>/g) || []).length === 1` (Pitfalls 8 + C); (2) zero matches for `superpowers`, `Superpowers`, `gsd-`, `\\bGSD\\b`, `Get Shit Done` in the additionalContext (Pitfall 15 — D-05 hand-rebrand verification); (3) string `You are using oto.` and `'oto:using-oto'` both present; (4) top-level shape `{ hookSpecificOutput: { hookEventName: 'SessionStart', additionalContext: ... } }` (D-08 Claude branch)."
    Test name: `phase-05 session-start: emits exactly one rebranded identity block on Claude runtime`
    Todo reason: `Implemented in Wave 2 (plan 05-03) once oto-session-start is rewritten per D-04..D-09`

    **`tests/05-merge-settings.test.cjs`** (Wave 3, plan 05-04):
    Header comment must say: "Covers HK-01..06 + D-12/D-13/D-14. Implemented in Wave 3 (plan 05-04). Will: (1) read `tests/fixtures/phase-05/settings-existing.json`, call `runtimeClaude.mergeSettings(existingText, { otoVersion: '0.1.0-alpha.1', configDir: '/tmp/fake-claude' })`, parse result, assert user `model`, `permissions`, and `hooks.PreToolUse[0]` entries preserved verbatim; (2) assert `_oto` top-level key exists with `version`, `installed_at`, and a `hooks` array containing exactly 6 entries (SessionStart, PreToolUse Write|Edit, PostToolUse Read, PostToolUse Bash, PostToolUse Bash|Edit|Write|MultiEdit|Agent|Task, statusLine — per 05-RESEARCH.md Code Example 1 + Pitfall A: context-monitor on PostToolUse NOT Stop, + 05-VALIDATION.md HK-06 note: validate-commit on PreToolUse Bash NOT PostToolUse); (3) idempotency — calling mergeSettings on the merged output yields byte-identical result; (4) uninstall — call hypothetical `runtimeClaude.unmergeSettings(merged)`, assert result is byte-identical to original existing fixture (`_oto` block + oto entries removed, user entries untouched)."
    Test name: `phase-05 merge-settings: round-trip preserves user content; idempotent; uninstall removes only _oto`
    Todo reason: `Implemented in Wave 3 (plan 05-04) once runtime-claude.cjs::mergeSettings is filled in`

    **`tests/05-build-hooks.test.cjs`** (Wave 1, plan 05-02):
    Header comment must say: "Covers HK-07 build leg + Pitfall F (build reads correct dir) + Pitfall G (executable bit on bash hooks). Implemented in Wave 1 (plan 05-02). Will spawn `node scripts/build-hooks.js` from a clean working tree, assert exit 0; assert `oto/hooks/dist/` contains exactly 6 files matching the inventory keep set: `oto-session-start`, `oto-statusline.js`, `oto-context-monitor.js`, `oto-prompt-guard.js`, `oto-read-injection-scanner.js`, `oto-validate-commit.sh`; assert `(fs.statSync('oto/hooks/dist/oto-session-start').mode & 0o111) !== 0` (executable); same for `oto-validate-commit.sh`; assert `oto-session-start` content begins with `#!` shebang on line 1 and `# oto-hook-version: ` on line 2."
    Test name: `phase-05 build-hooks: emits 6 files into oto/hooks/dist with exec bits intact`
    Todo reason: `Implemented in Wave 1 (plan 05-02) once scripts/build-hooks.js is retargeted to oto/hooks/`

    **`tests/05-session-start-fixture.test.cjs`** (Wave 4, plan 05-05):
    Header comment must say: "Covers HK-01 snapshot lock per D-09 + ADR-04 Consequences. Implemented in Wave 4 (plan 05-05). Will: spawn `oto/hooks/oto-session-start` with stubbed `CLAUDE_PLUGIN_ROOT=/tmp/fake-claude` and no `.oto/config.json` (opt-in OFF, default), capture stdout, JSON-parse, compare deep-equal against `oto/hooks/__fixtures__/session-start-claude.json`; explicit substring scan for the 5 banned upstream identity strings (`superpowers`, `Superpowers`, `gsd-`, `GSD`, `Get Shit Done`) inside the captured `hookSpecificOutput.additionalContext` per Pitfall 15. Phase 10 promotes this from a static-file compare to a CI snapshot check."
    Test name: `phase-05 session-start-fixture: stdout deep-equals __fixtures__/session-start-claude.json with no upstream-identity leakage`
    Todo reason: `Implemented in Wave 4 (plan 05-05) once oto/hooks/__fixtures__/session-start-claude.json exists`

    Use this exact scaffold body shape per file, adjusting only the comment block, test name, and todo reason:
    ```js
    'use strict';
    // Phase 5 Wave 0 scaffold — implemented in Wave N (plan 05-NN)
    // Covers: HK-NN (...short description...)
    // <full eventual-assertion description from above>
    const test = require('node:test');
    const path = require('node:path');
    const REPO_ROOT = path.resolve(__dirname, '..');

    test('<test name from above>', (t) => {
      t.todo('<todo reason from above>');
    });
    ```

    Do NOT write any actual implementation logic. Do NOT touch production files. Wave 0 ownership per 05-VALIDATION.md is test scaffolding ONLY.
  </action>
  <acceptance_criteria>
    - All 5 files exist under `tests/`
    - `node --test --test-concurrency=4 tests/05-*.test.cjs` exits 0
    - Output of that command shows `# todo 5` (or more) and `# fail 0`
    - Each file starts with `'use strict';` (verify: `head -1 tests/05-token-substitution.test.cjs` equals `'use strict';`)
    - Each file contains at least one `t.todo(` call: `grep -l "t.todo" tests/05-*.test.cjs | wc -l` returns 5
    - Each file contains a `// Covers: HK-` header comment: `grep -l "Covers: HK-" tests/05-*.test.cjs | wc -l` returns 5
    - No production files are modified by this task: `git status --porcelain | grep -v '^??' | grep -v 'tests/' | grep -v '.planning/' | wc -l` returns 0 (only untracked tests/ + .planning/ allowed)
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-concurrency=4 tests/05-*.test.cjs 2>&1 | tee /tmp/05-w0.out && grep -E "# tests|# todo|# fail" /tmp/05-w0.out && test "$(grep -l 't.todo' tests/05-*.test.cjs | wc -l | tr -d ' ')" = "5" && test "$(grep -l 'Covers: HK-' tests/05-*.test.cjs | wc -l | tr -d ' ')" = "5"</automated>
  </verify>
  <done>5 test scaffolds exist, all run as `t.todo()` placeholders; `node --test tests/05-*.test.cjs` exits 0; each file has the required header comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Wave 0 only writes test scaffolds and static JSON fixtures. No production code paths execute, no user input is parsed, no settings.json on disk is mutated, no network or external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | tests/fixtures/phase-05/settings-existing.json | accept | Fixture contains a fake home path (`/Users/julian/.local/bin/user-pretool.sh`) used as a string token; no real credential or secret. ASVS L1 V14.3.3: documentation/test fixtures may contain placeholder data. Reviewed: no real secret. |
| T-05-01-02 | Tampering | tests/05-*.test.cjs | accept | `t.todo()` placeholders execute no production code; a corrupted scaffold can only fail node:test parsing, surfacing immediately at acceptance time. No persistent damage possible. |
</threat_model>

<verification>
- All 5 Phase 5 test scaffold files exist
- Both fixture files parse as valid JSON; `settings-existing.json` contains the exact 4 user-content keys, `settings-empty.json` is exactly `{}\n`
- `node --test --test-concurrency=4 tests/05-*.test.cjs` exits 0 with 5 todos and 0 failures
- `npm test` (full suite) still passes (Phase 1-4 tests + Phase 5 todos all green)
- No production code changes (only `tests/` and `.planning/` modifications)
</verification>

<success_criteria>
- 5 scaffold files + 2 fixture files committed
- All 5 scaffolds run as `t.todo()` placeholders against `node --test`
- Wave 1, 2, 3, 4 plans can fill in test bodies without creating new files (filenames already match 05-VALIDATION.md Per-Task Verification Map)
- The fixtures unblock Wave 3 mergeSettings round-trip without further setup
</success_criteria>

<output>
After completion, create `.planning/phases/05-hooks-port-consolidation/05-01-SUMMARY.md`.
</output>
