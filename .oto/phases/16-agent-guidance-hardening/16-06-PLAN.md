---
phase: 16-agent-guidance-hardening
plan: 06
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03", "16-04", "16-05"]
files_modified:
  - .oto/phases/16-agent-guidance-hardening/16-VALIDATION.md
  - oto/agents/oto-phase-researcher.md
  - oto/agents/oto-project-researcher.md
  - oto/agents/oto-ui-researcher.md
  - oto/agents/oto-debugger.md
  - oto/agents/oto-advisor-researcher.md
autonomous: false
requirements: [HARD-04, HARD-05, HARD-01]
must_haves:
  truths:
    - "A live tools-restricted subagent enumerated mcp__exa__* tools and completed one real Exa call with results (claude-code#13898 regression class guarded)"
    - "With the key removed, a research-flavored flow completed with zero user-facing errors via the fallback floor (HARD-01 live leg)"
    - "oto sync --dry-run ran at milestone close and its conflict surface is recorded, with new conflicts in milestone-touched files dispositioned"
    - "Full npm test suite and SDK vitest suite are green at phase close"
  artifacts:
    - path: ".oto/phases/16-agent-guidance-hardening/16-VALIDATION.md"
      provides: "Per-task verification map with final statuses; nyquist sign-off"
      contains: "HARD-04"
  key_links:
    - from: "live Claude Code session"
      to: "exa MCP server"
      via: "Task(oto-debugger) → mcp__exa__web_search_exa"
      pattern: "mcp__exa__"
---

<objective>
Close the phase with the two verifications that cannot be automated in CI: the live subagent e2e proving `mcp__exa__*` tools reach a tools-restricted subagent (HARD-04, guarding the claude-code#13898 regression class and empirically resolving the wildcard-vs-enumeration question from issue #53865), and the milestone-close `oto sync --dry-run` conflict-surface check (HARD-05). Includes the pre-planned contingency: if the wildcard frontmatter fails live, switch all five agents to exact 3-tool enumeration.

Purpose: Sources conflict on wildcard `tools:` behavior (current docs say yes; closed issue #53865 said no on v2.1.119; installed CLI is 2.1.212) — only an empirical check on the installed version resolves it. Phase 15 P10's human-verify checkpoint is the proven shape.

Output: approved checkpoint evidence, recorded sync-dry-run results, completed 16-VALIDATION.md, green full suites.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/16-agent-guidance-hardening/16-RESEARCH.md
@.oto/phases/16-agent-guidance-hardening/16-VALIDATION.md

HARD-04 facts: installed Claude Code 2.1.212; an Exa key was configured for Phase 15's checkpoint (15-USER-SETUP.md) — re-confirm at checkpoint time. Since v2.1.208 a `tools:` list resolving to ZERO tools errors at launch, but a list where only the mcp entries fail resolves silently to the rest — therefore the probe MUST have the subagent enumerate its actual tools, not just attempt a call.

HARD-05 facts: sync pipeline is `oto sync --upstream all --to latest --dry-run` (bin/lib/sync-cli.cjs; docs/upstream-sync.md); requires network + upstream GitHub. Treat NEW conflicts in files this milestone touched (researcher/debugger/advisor agents, init.cjs, config.cjs, secrets.cjs, settings-integrations.md) as findings to disposition, not auto-failures — a pre-existing conflict baseline may exist. Phase 16 is the last v0.5.0 phase, so phase close == milestone close.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint gate — full suites green + probe prep</name>
  <files>.oto/phases/16-agent-guidance-hardening/16-VALIDATION.md</files>
  <read_first>
    - .oto/phases/16-agent-guidance-hardening/16-VALIDATION.md (per-task map to update)
    - .oto/phases/15-exa-mcp-registration-all-three-runtimes/15-USER-SETUP.md (key/checkpoint environment from Phase 15, if present)
    - oto/agents/oto-debugger.md (final frontmatter form going into the live probe)
  </read_first>
  <action>
    Run and record (all must pass before the checkpoint):
    1. `npm test` — full CJS suite including all tests/16-* files
    2. `cd sdk && npx vitest run` — full SDK suite
    3. `node scripts/check-runtime-sync.cjs` — exit 0
    4. Verify the installed copies the live session will actually load: `grep -c 'mcp__exa__\*' ~/.claude/agents/oto-debugger.md` == 1 and `diff oto/references/search-tools.md ~/.claude/oto/references/search-tools.md` produces no output
    5. Confirm a usable Exa key is detectable: `node -e "const{detectKeySource}=require('./oto/bin/lib/secrets.cjs');console.log(detectKeySource('exa').source)"` prints `env` or `keyfile` (NEVER print the key itself)

    Update `.oto/phases/16-agent-guidance-hardening/16-VALIDATION.md`: fill the Test Infrastructure table (framework `node:test` + vitest(sdk); quick run `node --test tests/16-*.test.cjs`; full `npm test`), mark all automated per-task rows green with their commands, and list HARD-04/HARD-05 under Manual-Only Verifications.
  </action>
  <verify>
    <automated>npm test && node scripts/check-runtime-sync.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0; `cd sdk && npx vitest run` exits 0
    - `grep -c 'mcp__exa__' ~/.claude/agents/oto-debugger.md` >= 1
    - `diff oto/references/search-tools.md ~/.claude/oto/references/search-tools.md` produces no output
    - detectKeySource('exa').source is non-null (probe environment ready)
    - 16-VALIDATION.md contains a filled Test Infrastructure table and per-task statuses (no `{placeholder}` braces remain in filled sections)
  </acceptance_criteria>
  <done>All automated gates green; installed-root state verified; key detectable; validation doc current</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HARD-04 live subagent e2e (keyed + keyless legs)</name>
  <files>none — verification-only checkpoint (evidence recorded in 16-06-SUMMARY.md)</files>
  <action>Present the two verification legs in <how-to-verify> to the developer and PAUSE. Do not run the probe yourself — the developer runs it in a live session. Record both legs' outcomes verbatim in 16-06-SUMMARY.md under `## HARD-04 Live E2E Evidence`. Never echo the key or its file contents.</action>
  <what-built>Phases 14-16 delivered: Exa MCP registered via oto's adapters with the key from the 0600 keyfile/env, five agents carrying mcp__exa__* access through one shared runtime-neutral reference, and a fallback ladder that must hold with no key present.</what-built>
  <how-to-verify>
    **Leg A — keyed run (in a live Claude Code session in this repo, key present):**
    1. Start a fresh Claude Code session (so ~/.claude.json MCP registration and updated agents load).
    2. Run this exact probe:
       ```
       Task(subagent_type="oto-debugger", prompt:
         "1. List every tool name you can currently call.
          2. Call the Exa web search tool once with query 'node lts release'.
          3. Report the tool name you used and whether the call returned results.")
       ```
    3. PASS criteria (BOTH required):
       - The enumeration in step 1 includes `mcp__exa__web_search_exa`, `mcp__exa__web_fetch_exa`, `mcp__exa__web_search_advanced_exa`
       - The step-2 call used an `mcp__exa__*` tool and returned real results
    4. If the enumeration is missing the Exa tools (wildcard unrecognized — issue #53865 behavior) or `/agents` shows `⚠ Unrecognized`: report "wildcard-failed" — Task 3's contingency will switch to exact enumeration, then re-run this leg.

    **Leg B — keyless fallback floor (HARD-01 live leg):**
    1. Temporarily disable the key: `mv ~/.oto/exa_api_key ~/.oto/exa_api_key.bak 2>/dev/null; unset EXA_API_KEY` (in the shell that launches the session), start a fresh session.
    2. Ask a research-flavored question that would normally trigger search (e.g. "research current Node LTS guidance using your search tools").
    3. PASS criteria: the flow completes with ZERO user-facing errors — the agent uses WebSearch (or Brave if configured) without attempting/retrying Exa.
    4. Restore: `mv ~/.oto/exa_api_key.bak ~/.oto/exa_api_key`.

    Never paste the key or its file contents into the session transcript.
  </how-to-verify>
  <resume-signal>Type "approved" (both legs passed), "wildcard-failed" (Leg A enumeration missing Exa tools), or describe other issues</resume-signal>
  <verify>Human resume signal received; Leg A tool enumeration + call result and Leg B zero-error fallback outcome recorded in 16-06-SUMMARY.md (manual-only per 16-VALIDATION.md — no automated command exists for a live session)</verify>
  <done>Resume signal "approved" received (or "wildcard-failed" routed to Task 3 contingency and Leg A re-approved after the switch)</done>
</task>

<task type="auto">
  <name>Task 3: Contingency (if needed) + HARD-05 sync dry-run + phase close</name>
  <files>oto/agents/oto-phase-researcher.md, oto/agents/oto-project-researcher.md, oto/agents/oto-ui-researcher.md, oto/agents/oto-debugger.md, oto/agents/oto-advisor-researcher.md, .oto/phases/16-agent-guidance-hardening/16-VALIDATION.md</files>
  <read_first>
    - docs/upstream-sync.md (sync CLI invocation and output interpretation)
    - bin/lib/sync-cli.cjs (header — flags accepted, dry-run semantics)
    - .oto/phases/16-agent-guidance-hardening/16-VALIDATION.md (sign-off section)
  </read_first>
  <action>
    **Contingency (ONLY if checkpoint returned "wildcard-failed"):** In all five agents' frontmatter, replace `mcp__exa__*` with the exact enumeration `mcp__exa__web_search_exa, mcp__exa__web_fetch_exa, mcp__exa__web_search_advanced_exa`. Re-sync both runtime roots per the 16-01/16-05 procedure (Claude copy; Codex edit-in-place .md tools line + any role-prose tools mention), run `node --test tests/16-search-reference.test.cjs tests/16-transformed-tool-names.test.cjs` (update the frontmatter regex assertion to accept the enumerated form), run `node scripts/check-runtime-sync.cjs`, then re-run checkpoint Leg A and obtain "approved" before continuing.

    **HARD-05 — milestone-close conflict-surface check:**
    1. Run `oto sync --upstream all --to latest --dry-run` (if `oto` is not on PATH: `node bin/install.js sync --upstream all --to latest --dry-run`). Requires network; if offline, STOP and flag — this gate cannot be skipped silently.
    2. Record the full conflict list in the 16-06-SUMMARY under a `## HARD-05 Sync Dry-Run` heading: total conflicts, and specifically whether any conflicts are NEW in files this milestone touched (oto/agents/oto-{phase,project,ui}-researcher.md, oto-debugger.md, oto-advisor-researcher.md, oto/bin/lib/init.cjs, config.cjs, secrets.cjs, oto/workflows/settings-integrations.md, sdk/src/query/*).
    3. For each NEW conflict in a milestone-touched file: record a disposition line (expected-and-block-shaped vs needs-follow-up). Pre-existing baseline conflicts are noted, not dispositioned.

    **Phase close:** finalize `.oto/phases/16-agent-guidance-hardening/16-VALIDATION.md` — all statuses green/recorded, Manual-Only rows marked verified with checkpoint evidence, tick the Validation Sign-Off checklist, set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`, set Approval to the checkpoint approval reference. Re-run `npm test` one final time.
  </action>
  <verify>
    <automated>npm test && node scripts/check-runtime-sync.cjs && grep -q 'nyquist_compliant: true' .oto/phases/16-agent-guidance-hardening/16-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 16-06-SUMMARY.md contains a `## HARD-05 Sync Dry-Run` section with the recorded conflict surface and per-file dispositions for milestone-touched files
    - If contingency ran: `grep -c 'mcp__exa__web_search_exa, mcp__exa__web_fetch_exa, mcp__exa__web_search_advanced_exa' oto/agents/oto-debugger.md` == 1 and all 16-* tests green; if not: agents unchanged from 16-05 state
    - `.oto/phases/16-agent-guidance-hardening/16-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
    - `npm test` exits 0 at phase close
  </acceptance_criteria>
  <done>Checkpoint approved (post-contingency if needed); sync dry-run recorded and dispositioned; validation doc signed off; suites green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint shell → session transcript | Key material exists in the checkpoint environment |
| upstream GSD repo → local merge surface | sync dry-run fetches remote content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Information Disclosure | Exa key echoed into checkpoint transcript/summary | mitigate | Instructions never cat/echo the key; detectKeySource prints source label only; explicit "never paste the key" rule in the checkpoint |
| T-16-06-02 | Tampering | Future upstream sync silently reverting this phase's guards | mitigate | HARD-05 dry-run records the conflict baseline for milestone-touched files; block-shaped commented diffs minimize the surface |
| T-16-06-03 | Denial of Service | Keyless leg leaving the developer's keyfile disabled | mitigate | Leg B includes the explicit restore step; Task 3 acceptance requires suites green after restore |
</threat_model>

<verification>
- Checkpoint approved with both legs passing (post-contingency if applicable)
- `## HARD-05 Sync Dry-Run` recorded in 16-06-SUMMARY.md
- `npm test`, `cd sdk && npx vitest run`, `node scripts/check-runtime-sync.cjs` all green at close
</verification>

<success_criteria>
- HARD-04: live proof that Exa tools reach a tools-restricted subagent on the installed CLI, with the wildcard question empirically settled
- HARD-01 live leg: keyless research flow completes with zero user-facing errors
- HARD-05: conflict surface measured and recorded at milestone close
- 16-VALIDATION.md complete and signed off
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-06-SUMMARY.md`
</output>
