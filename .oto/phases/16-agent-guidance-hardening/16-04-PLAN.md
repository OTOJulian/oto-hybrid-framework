---
phase: 16-agent-guidance-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/runtime-matrix.cjs
  - decisions/runtime-tool-matrix.md
  - README.md
  - docs/search-integrations.md
autonomous: true
requirements: [HARD-03]
must_haves:
  truths:
    - "The generated runtime matrix has an Exa MCP row covering all three runtimes (registration location + shape per runtime)"
    - "The committed matrix byte-equals the generator output (regen-diff test green)"
    - "User-facing docs cover Exa setup (key entry, consent-gated registration, fallback behavior) with qualitative rate-limit phrasing — zero hard-coded quota numbers"
  artifacts:
    - path: "decisions/runtime-tool-matrix.md"
      provides: "Regenerated matrix with MCP servers section"
      contains: "mcp_servers.exa"
    - path: "docs/search-integrations.md"
      provides: "Setup + behavior docs for search integrations"
      min_lines: 40
  key_links:
    - from: "bin/lib/runtime-matrix.cjs"
      to: "decisions/runtime-tool-matrix.md"
      via: "scripts/render-runtime-matrix.cjs regeneration"
      pattern: "MCP Servers"
    - from: "README.md"
      to: "docs/search-integrations.md"
      via: "Documentation section link"
      pattern: "docs/search-integrations\\.md"
---

<objective>
Add the Exa MCP row to the generated runtime matrix and write user-facing setup docs with qualitative rate-limit phrasing (HARD-03).

Purpose: The matrix is oto's canonical per-runtime capability record and is guarded by a byte-equality regen-diff test — editing the generator without regenerating fails `npm test`, which is a free hard gate. Docs must not hard-code quota numbers: exa.ai publishes per-endpoint QPS but NO free-tier monthly quota (verified 2026-07-17 in research); the roadmap explicitly requires qualitative phrasing.

Output: extended generator, regenerated matrix, new docs/search-integrations.md, README link.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/16-agent-guidance-hardening/16-RESEARCH.md

Verified pipeline: `bin/lib/runtime-matrix.cjs` `renderMatrix()` → `scripts/render-runtime-matrix.cjs` writes `decisions/runtime-tool-matrix.md` → `tests/phase-08-runtime-matrix-render.test.cjs` (D-05) asserts the in-memory render byte-equals the committed file. NEVER hand-edit `decisions/runtime-tool-matrix.md`.

Registration facts (Phase 15, shipped): Claude = `~/.claude.json` `mcpServers.exa`, user scope, oto-fingerprinted, additive JSON merge. Codex = `~/.codex/config.toml` OTO-marker `[mcp_servers.exa]` block (separate marker from hooks). Gemini = `~/.gemini/settings.json` `mcpServers.exa`, stdio shape (command+args only — no url/httpUrl). All three run the shipped launcher `oto/hooks/oto-exa-mcp.js` pinned to `exa-mcp-server@3.2.1` exposing exactly `web_search_exa`, `web_fetch_exa`, `web_search_advanced_exa`. Registration is consent-gated (default No, persisted in `~/.oto/mcp-consent.json`) and conditional on a detected key (detectKeySource). Uninstall removes only oto-fingerprinted entries.
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP servers section in runtime-matrix generator + regeneration</name>
  <files>bin/lib/runtime-matrix.cjs, decisions/runtime-tool-matrix.md</files>
  <read_first>
    - bin/lib/runtime-matrix.cjs (entire file, 309 lines — table constants at top, renderMatrix section list at lines 258-303, renderTable helper)
    - scripts/render-runtime-matrix.cjs (how regeneration is invoked)
    - tests/phase-08-runtime-matrix-render.test.cjs (the D-05 byte-equality gate this must satisfy)
    - decisions/runtime-tool-matrix.md (current generated output — section numbering to extend)
  </read_first>
  <action>
    In `bin/lib/runtime-matrix.cjs`:

    1. Add a table constant near the other row constants (after `FRONTMATTER_ROWS`):

    ```js
    // OTO Phase 16 (HARD-03): MCP server registration per runtime (Phase 15 adapters).
    const MCP_SERVER_ROWS = [
      [
        '`exa` (launcher-stdio, `oto/hooks/oto-exa-mcp.js`, pinned `exa-mcp-server@3.2.1`)',
        '`~/.claude.json` `mcpServers.exa` (user scope, additive JSON merge, oto-fingerprinted)',
        '`~/.codex/config.toml` OTO-marker `[mcp_servers.exa]` block',
        '`~/.gemini/settings.json` `mcpServers.exa` (stdio `command`+`args`, no `url`/`httpUrl`)',
      ],
    ];
    ```

    2. In `renderMatrix()`'s returned array, after the section-5 lines (`'**Codex column at v0.1.0 close: 100% green (D-09).**'` / `'**Gemini column per D-12 daily-peer bar: 100% green.**'`), insert:

    ```js
    '## 6. MCP Servers per Runtime',
    renderTable(['MCP server', 'Claude', 'Codex', 'Gemini'], MCP_SERVER_ROWS),
    'Tool surface: exactly `web_search_exa`, `web_fetch_exa`, `web_search_advanced_exa` (MCP-06). Registration is consent-gated (default No) and conditional on a detected key (MCP-01/MCP-07); uninstall removes only oto-fingerprinted entries (MCP-08).',
    '',
    ```

    (Match the existing pattern of trailing `''` so the joined output keeps its final newline — verify against the current renderMatrix tail before finalizing placement.)

    3. Export `MCP_SERVER_ROWS` only if the module.exports pattern requires it for tests; otherwise leave exports unchanged.

    4. Regenerate: `node scripts/render-runtime-matrix.cjs`, then run the byte-equality gate: `node --test tests/phase-08-runtime-matrix-render.test.cjs` — must pass. Commit generator + regenerated matrix together.
  </action>
  <verify>
    <automated>node scripts/render-runtime-matrix.cjs && node --test tests/phase-08-runtime-matrix-render.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MCP Servers per Runtime' bin/lib/runtime-matrix.cjs` >= 1
    - `grep -c 'mcp_servers.exa' decisions/runtime-tool-matrix.md` == 1
    - `grep -c 'web_search_advanced_exa' decisions/runtime-tool-matrix.md` == 1
    - `grep -c 'exa-mcp-server@3.2.1' decisions/runtime-tool-matrix.md` == 1
    - `node --test tests/phase-08-runtime-matrix-render.test.cjs` exits 0
    - `git diff --stat` shows bin/lib/runtime-matrix.cjs and decisions/runtime-tool-matrix.md changed together
  </acceptance_criteria>
  <done>Matrix has an Exa MCP row per runtime; committed file byte-equals the render; D-05 test green</done>
</task>

<task type="auto">
  <name>Task 2: docs/search-integrations.md + README link (qualitative rate-limit phrasing)</name>
  <files>docs/search-integrations.md, README.md</files>
  <read_first>
    - README.md (lines 60-130 — Install, Supported runtimes, Documentation sections; style/tone to match)
    - docs/upstream-sync.md (docs/ style precedent — heading structure, depth)
    - oto/references/search-tools.md (if 16-01 already merged — keep docs consistent with shipped guidance; if absent, use the ladder facts from 16-RESEARCH.md)
    - .oto/phases/15-exa-mcp-registration-all-three-runtimes/15-RESEARCH.md (registration behavior facts, if detail needed)
  </read_first>
  <action>
    Create `docs/search-integrations.md` covering, in this order:

    1. **Overview** — oto's three search integrations (Exa semantic search via MCP, Brave via `oto-sdk query websearch`, built-in runtime search as the always-present floor) and the fallback ladder agents follow.
    2. **Exa setup** — steps: (a) get an Exa API key from the Exa dashboard; (b) run `/oto-settings-integrations` — the key is entered via a hidden terminal prompt and stored ONLY in `~/.oto/exa_api_key` (mode 0600) or supplied via the `EXA_API_KEY` env var; never in tracked config; (c) registration: oto asks for explicit consent (default No) before registering the `exa` MCP server in each installed runtime (Claude Code `~/.claude.json`, Codex `~/.codex/config.toml`, Gemini `~/.gemini/settings.json`); (d) exactly three tools are exposed: `web_search_exa`, `web_fetch_exa`, `web_search_advanced_exa`.
    3. **Brave setup** — key via `/oto-settings-integrations` (env `BRAVE_API_KEY` or `~/.oto/brave_api_key`); used through `oto-sdk query websearch`.
    4. **Fallback behavior** — with no key or server present, research flows complete using Brave or the runtime's built-in search with zero user-facing errors; agents never retry Exa after a rate-limit error.
    5. **Rate limits (qualitative ONLY)** — phrasing to use verbatim-strength: "Exa's free tier is rate-limited. When Exa returns a rate-limit (429) error, oto's agents switch to Brave or built-in search for the remainder of the session rather than retrying. Check Exa's pricing page for current limits." Do NOT write any numeric quota (no "1,000 requests", no "150 calls/day", no QPS figures).
    6. **Uninstall** — `oto uninstall`/unregistration removes only oto-fingerprinted `exa` entries; user-owned entries are skipped and reported.

    Then in `README.md`'s `## Documentation` section, add a list entry linking `docs/search-integrations.md` with a one-line description (e.g. "Search integrations — Exa MCP setup, Brave, and the fallback ladder"). Match the existing link style in that section exactly.

    Never include any real or example API key value anywhere in the docs.
  </action>
  <verify>
    <automated>test -f docs/search-integrations.md && grep -q 'docs/search-integrations.md' README.md && ! grep -nE '[0-9,]+ ?(requests|req|calls|QPS)' docs/search-integrations.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/search-integrations.md` exists; contains `web_search_exa`, `web_fetch_exa`, `web_search_advanced_exa`, `~/.oto/exa_api_key`, `/oto-settings-integrations`, and `429`
    - `grep -cE '[0-9,]+ ?(requests|req|calls|QPS)' docs/search-integrations.md` == 0 (no hard-coded quota/QPS numbers)
    - `grep -c 'docs/search-integrations.md' README.md` == 1 (inside the Documentation section)
    - `grep -ciE '(sk-|api[_-]?key ?= ?[A-Za-z0-9])' docs/search-integrations.md` == 0 (no key-looking values)
  </acceptance_criteria>
  <done>Docs cover setup, consent-gated registration, fallback, uninstall — rate limits phrased qualitatively; README links the new page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → public GitHub repo | Everything written here is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Information Disclosure | Key material or key-shaped examples in docs | mitigate | Docs reference the keyfile PATH and env var NAME only; acceptance criterion greps for key-looking values |
| T-16-04-02 | Tampering | Hand-edit of the generated matrix drifting from generator | mitigate | Only the generator is edited; D-05 byte-equality test enforces regeneration |
</threat_model>

<verification>
- `node --test tests/phase-08-runtime-matrix-render.test.cjs` green
- `npm test` full suite green
</verification>

<success_criteria>
- Matrix documents the Exa MCP registration per runtime, generated not hand-written
- Setup docs exist, linked from README, with qualitative-only rate-limit language
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-04-SUMMARY.md`
</output>
