---
phase: 16-agent-guidance-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/references/search-tools.md
  - oto/agents/oto-phase-researcher.md
  - oto/agents/oto-project-researcher.md
  - oto/agents/oto-ui-researcher.md
  - tests/16-search-reference.test.cjs
autonomous: true
requirements: [GUID-01, GUID-02]
must_haves:
  truths:
    - "One runtime-neutral search-tools reference exists with the Exa → Brave → WebSearch fallback ladder and the never-retry-on-429 rule"
    - "The three researcher agents consume the shared reference via include pointer; their drifted Brave/Exa/Firecrawl sections are gone"
    - "The reference contains zero mcp__ Claude-namespace tokens (it ships byte-identical to Codex/Gemini)"
    - "Installed runtime roots (~/.claude, ~/.codex) carry the new reference and updated researcher agents; check-runtime-sync passes"
  artifacts:
    - path: "oto/references/search-tools.md"
      provides: "Shared runtime-neutral search guidance (ladder, 429 rule, tool-not-found rule, Exa tool table, Brave CLI, Firecrawl)"
      min_lines: 30
    - path: "tests/16-search-reference.test.cjs"
      provides: "Structural guards for GUID-01/02"
      min_lines: 40
  key_links:
    - from: "oto/agents/oto-phase-researcher.md"
      to: "oto/references/search-tools.md"
      via: "include pointer"
      pattern: "@~/.claude/oto/references/search-tools\\.md"
    - from: "oto/agents/oto-project-researcher.md"
      to: "oto/references/search-tools.md"
      via: "include pointer"
      pattern: "@~/.claude/oto/references/search-tools\\.md"
    - from: "oto/agents/oto-ui-researcher.md"
      to: "oto/references/search-tools.md"
      via: "include pointer"
      pattern: "@~/.claude/oto/references/search-tools\\.md"
---

<objective>
Create the single shared, runtime-neutral search-tools reference (GUID-01) and make the three researcher agents consume it by include pointer, deleting their three mutually drifted Brave/Exa/Firecrawl guidance copies (GUID-02).

Purpose: The three researchers currently disagree ("init context" vs "orchestrator context" vs a different priority ordering). One reference file, included by pointer, ends the drift permanently. This plan MUST land before debugger/advisor get guidance (16-05) — extending first would create a 4th and 5th drifted copy (research Pitfall 2).

Output: `oto/references/search-tools.md` (new, oto-only file — zero upstream-sync conflict surface), three consolidated researcher agents, structural test, synced runtime roots.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/ROADMAP.md
@.oto/phases/16-agent-guidance-hardening/16-RESEARCH.md

Critical mechanism (verified in 16-RESEARCH.md): `bin/lib/install.cjs` TRANSFORM_KEY transforms only commands/agents/skills/hooks — **references are copied byte-identical to every runtime root** and byte-checked by `scripts/check-runtime-sync.cjs`. The reference therefore must contain NO `mcp__` Claude-namespace tokens. Agent BODIES do get path transforms (`~/.claude/` → `~/.codex/`), so the include pointer resolves correctly per runtime.

Sync precedent (quick task 260714-nzr): `~/.claude` = straight copy (installed Claude copies are byte-identical for these agents); `~/.codex` = edit-in-place on both `.md` and `.toml` (`developer_instructions` embeds the full agent body) using `~/.codex`-rooted paths — NEVER regenerate the `.toml` with `generateCodexAgentToml` defaults (it would lose the installed `model`/`model_reasoning_effort`/`sandbox_mode` values). `~/.gemini` has no oto install — skip with the notice "gemini: no install, skipped".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create oto/references/search-tools.md + structural test</name>
  <files>oto/references/search-tools.md, tests/16-search-reference.test.cjs</files>
  <read_first>
    - oto/agents/oto-phase-researcher.md (lines 136-195 — current drifted guidance being consolidated; source of Brave CLI options and Exa/Firecrawl "best for" prose)
    - oto/agents/oto-project-researcher.md (lines 120-170 — second drifted copy)
    - oto/agents/oto-ui-researcher.md (lines 105-135 — third drifted copy, different ordering)
    - tests/14-newproject-boolean.test.cjs (test file conventions: 'use strict', node:test, assert/strict)
    - oto/references/mandatory-initial-read.md (reference file style example)
  </read_first>
  <behavior>
    - Test: reference file exists and contains all three pinned tool names: `web_search_exa`, `web_fetch_exa`, `web_search_advanced_exa`
    - Test: reference contains the exact sentence "Never retry Exa after a rate-limit error" and the token "429"
    - Test: reference contains "oto-sdk query websearch" (Brave rung command)
    - Test: reference contains ZERO occurrences of the substring "mcp__" (runtime-neutrality guard)
    - Test: ladder ordering — indexOf("Exa semantic search") < indexOf("Brave web search") < indexOf("Built-in web search")
    - Test: reference contains "tool-not-found" fallback rule
  </behavior>
  <action>
    Create `oto/references/search-tools.md` with EXACTLY this content structure (prose may be lightly polished but every quoted string below must appear verbatim because the test greps for it):

    ```markdown
    # Search Tools Reference

    Runtime-neutral search guidance for all search-capable oto agents. This file ships byte-identical to every runtime root — never add runtime-specific tool namespace prefixes to this file.

    ## Availability Gates

    Your provided context (init or orchestrator) carries three booleans: `exa_search`, `brave_search`, `firecrawl`. Trust these booleans — never attempt a tool your context says is unavailable.

    ## Fallback Ladder

    Work down this ladder; each rung is used only when the rung above is unavailable or has failed for the session:

    1. **Exa semantic search** — if `exa_search` is `true`. Use the Exa search tool (`web_search_exa`) for semantic, research-heavy queries where keyword search fails: "best approaches to X", technical/academic content, niche library discovery. Exa is the semantic-query tier, not the default for every lookup — use built-in search or Brave for simple keyword lookups even when Exa is available.
    2. **Brave web search** — if `brave_search` is `true`. Run:

       ```bash
       oto-sdk query websearch "your query" --limit 10
       ```

       Options: `--limit N` (default 10), `--freshness day|week|month`. Independent index (not Google/Bing dependent), less SEO spam.
    3. **Built-in web search** — always present. Use your runtime's built-in web search tool (WebSearch in Claude Code, or your runtime's equivalent).

    ## Exa Tools (when `exa_search` is true)

    Exactly three Exa tools are registered:

    | Tool | Use For |
    |------|---------|
    | `web_search_exa` | Semantic web search — research questions, discovery |
    | `web_fetch_exa` | Fetch full content from a specific URL |
    | `web_search_advanced_exa` | Search with domain/date filters |

    ## Rate-Limit Rule (429)

    On any Exa rate-limit/429 error, switch to Brave/WebSearch for the remainder of the session. Never retry Exa after a rate-limit error.

    ## Tool-Not-Found Rule

    If an Exa tool call fails with tool-not-found, fall back immediately to the next rung — never retry.

    ## Firecrawl Deep Scraping (when `firecrawl` is true)

    Use the Firecrawl scrape tool to extract clean markdown from a specific URL (documentation, blog posts, READMEs), or the Firecrawl search tool for web search with auto-scraped results. Use after finding a URL via Exa, Brave, or built-in search.

    If `firecrawl` is `false` (or unset), use your runtime's built-in URL fetch tool (WebFetch in Claude Code, or equivalent) instead.
    ```

    Do NOT name Firecrawl tools with exact identifiers (`mcp__firecrawl__scrape` is a Claude-namespace token and its server-side name is unverified) — the descriptive phrasing above is deliberate.

    Then create `tests/16-search-reference.test.cjs` (node:test, `'use strict'`, `require('node:assert/strict')`) implementing every assertion in <behavior>, reading `oto/references/search-tools.md` relative to `path.resolve(__dirname, '..')`. Also add a researcher-consumption test block that loops over `['oto-phase-researcher', 'oto-project-researcher', 'oto-ui-researcher']` and asserts for each `oto/agents/<name>.md`:
    - contains the include line `@~/.claude/oto/references/search-tools.md`
    - does NOT contain "Enhanced Web Search (Brave API)"
    - does NOT contain "Exa Semantic Search (MCP)"
    - does NOT contain "Firecrawl Deep Scraping (MCP)"
    - does NOT contain "mcp__exa__web_search_exa"
    - does NOT contain "Check `exa_search`" and does NOT contain "Check `brave_search`" (the drifted per-agent gate phrasings)

    Run the test BEFORE Task 2's agent edits: the reference assertions must pass (GREEN) and the researcher-consumption assertions must fail (RED) — confirming the test detects the current drift. Structure the test file so the researcher block is present from the start (Task 2 turns it green).
  </action>
  <verify>
    <automated>node --test tests/16-search-reference.test.cjs 2>&1 | tail -5 (reference-file assertions pass; researcher assertions expected RED until Task 2)</automated>
  </verify>
  <acceptance_criteria>
    - `oto/references/search-tools.md` exists; `grep -c 'web_search_exa' oto/references/search-tools.md` >= 2; contains `web_fetch_exa` and `web_search_advanced_exa`
    - `grep -c 'Never retry Exa after a rate-limit error' oto/references/search-tools.md` == 1
    - `grep -c 'mcp__' oto/references/search-tools.md` == 0
    - `grep -c 'oto-sdk query websearch' oto/references/search-tools.md` >= 1
    - `tests/16-search-reference.test.cjs` exists and contains `web_search_advanced_exa`, `Never retry Exa`, and a loop over the three researcher names
  </acceptance_criteria>
  <done>Reference file exists with all mandated content; structural test file exists; reference-side tests green</done>
</task>

<task type="auto">
  <name>Task 2: Replace drifted sections in three researchers with include + sync runtime roots</name>
  <files>oto/agents/oto-phase-researcher.md, oto/agents/oto-project-researcher.md, oto/agents/oto-ui-researcher.md</files>
  <read_first>
    - oto/agents/oto-phase-researcher.md (lines 136-195 — exact section boundaries before deleting)
    - oto/agents/oto-project-researcher.md (lines 116-170)
    - oto/agents/oto-ui-researcher.md (lines 105-135)
    - scripts/check-runtime-sync.cjs (header comment — what the drift guard byte-checks vs presence-checks)
    - ~/.codex/agents/oto-phase-researcher.md and ~/.codex/agents/oto-phase-researcher.toml (installed Codex dialect — quoted frontmatter, developer_instructions body, ~/.codex-rooted paths)
  </read_first>
  <action>
    **Edit 1 — oto/agents/oto-phase-researcher.md:** Delete the contiguous block from the heading `## Enhanced Web Search (Brave API)` (line ~150) through the final Firecrawl line `If \`firecrawl: false\` (or not set), fall back to WebFetch.` (line ~189, the line immediately before `## Verification Protocol`). Replace with exactly:

    ```markdown
    ## Search Tools

    @~/.claude/oto/references/search-tools.md
    ```

    Keep `## Verification Protocol` and everything after untouched.

    **Edit 2 — oto/agents/oto-project-researcher.md:** Delete the contiguous block from `### Enhanced Web Search (Brave API)` (line ~126) through `If \`firecrawl: false\` (or not set), fall back to WebFetch.` (line ~165, immediately before `## Verification Protocol`). Replace with the same 3-line `## Search Tools` include block.

    **Edit 3 — oto/agents/oto-ui-researcher.md:** In the `## Tool Priority` table (lines ~111-117), replace the three rows
    `| 3rd | Exa (MCP) | ... |`, `| 4th | Firecrawl (MCP) | ... |`, `| 5th | WebSearch | ... |`
    with the single row:
    `| 3rd | Web search ladder (see Search Tools below) | Pattern references, accessibility standards, ecosystem discovery, deep scraping | Needs verification |`
    Then replace the paragraph starting `**Exa/Firecrawl:** Check \`exa_search\` and \`firecrawl\` from orchestrator context...` (line ~119) with the same 3-line `## Search Tools` include block. Keep `**Codebase first:**` and everything after untouched.

    **Sync (same task, mandatory — CLAUDE.md Runtime Sync Guardrail):**
    1. `cp oto/references/search-tools.md ~/.claude/oto/references/search-tools.md` and `cp oto/references/search-tools.md ~/.codex/oto/references/search-tools.md` (references are byte-checked).
    2. For each of the three researchers: `cp oto/agents/<name>.md ~/.claude/agents/<name>.md` (straight copy — verify first with `diff` that the pre-change installed copy matched the pre-change repo file; if it did not, apply the section replacement as an edit instead of copying).
    3. For each of the three researchers in `~/.codex/agents/`: apply the SAME section replacement edit-in-place to `<name>.md` AND inside the `developer_instructions = '''...'''` body of `<name>.toml`, using the Codex-rooted include path `@~/.codex/oto/references/search-tools.md`. Do NOT regenerate the .toml (would lose installed model/sandbox values). Zero `~/.claude` paths may leak into Codex files.
    4. `~/.gemini`: no oto install — record "gemini: no install, skipped" in the summary.
    5. Run `node scripts/check-runtime-sync.cjs` — must exit 0.
  </action>
  <verify>
    <automated>node --test tests/16-search-reference.test.cjs && node scripts/check-runtime-sync.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@~/.claude/oto/references/search-tools.md' oto/agents/oto-phase-researcher.md` == 1 (same for project- and ui-researcher)
    - `grep -c 'Enhanced Web Search' oto/agents/oto-phase-researcher.md oto/agents/oto-project-researcher.md` → 0 in each
    - `grep -c 'mcp__exa__web_search_exa' oto/agents/oto-phase-researcher.md oto/agents/oto-project-researcher.md` → 0 in each
    - `grep -c 'Exa (MCP)' oto/agents/oto-ui-researcher.md` == 0
    - `grep -c '@~/.codex/oto/references/search-tools.md' ~/.codex/agents/oto-phase-researcher.md` == 1 and `grep -c '@~/.codex/oto/references/search-tools.md' ~/.codex/agents/oto-phase-researcher.toml` == 1 (same for the other two researchers)
    - `grep -rc '~/.claude' ~/.codex/agents/oto-phase-researcher.md` == 0
    - `node scripts/check-runtime-sync.cjs` exits 0
    - `node --test tests/16-search-reference.test.cjs` exits 0 (all assertions green including researcher block)
  </acceptance_criteria>
  <done>All three researchers consume the shared reference; drifted sections deleted; both runtime roots synced (.md + .toml); drift guard green; full structural test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → installed runtime roots | Shipped guidance content crosses into live agent sessions on this machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | Installed runtime copies drifting from repo (stale guidance in live sessions) | mitigate | Same-task sync + `node scripts/check-runtime-sync.cjs` byte-check for references, presence/invariant check for agents |
| T-16-01-02 | Information Disclosure | Reference/docs content leaking key material | accept | No secret values exist in guidance content; reference only names the keyfile path pattern, never values |
| T-16-04-XX (cross-ref) | Tampering | Future upstream sync mis-merging shared researcher files | mitigate | Edits are block-shaped (single contiguous section → 3-line include); HARD-05 dry-run in 16-06 measures the surface |
</threat_model>

<verification>
- `node --test tests/16-search-reference.test.cjs` — all GUID-01/02 structural guards green
- `node scripts/check-runtime-sync.cjs` — exit 0
- `npm test` — full suite green (no regressions from agent edits)
</verification>

<success_criteria>
- One reference file carries the ladder, 429 rule, tool-not-found rule, Exa tool table, Brave CLI usage, Firecrawl guidance — with zero `mcp__` tokens
- Three researchers each carry exactly one include line where three divergent guidance copies used to be
- Installed `~/.claude` and `~/.codex` roots match; gemini skipped with notice
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-01-SUMMARY.md`
</output>
