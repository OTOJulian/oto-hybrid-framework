---
phase: 16-agent-guidance-hardening
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync-upstream/merge.cjs
  - bin/lib/sync-cli.cjs
  - tests/phase-09-cli.integration.test.cjs
  - tests/16-07-sync-all-provenance.test.cjs
  - docs/upstream-sync.md
autonomous: true
gap_closure: true
requirements: [HARD-05]
tags: [upstream-sync, conflict-artifacts, provenance]

must_haves:
  truths:
    - "After `oto sync --upstream all` with a target present in both upstreams, BOTH upstreams' conflict sidecars and BOTH per-upstream REPORT.md files exist on disk — the Superpowers leg no longer overwrites the GSD leg's durable evidence"
    - "`oto sync --status` counts pending sidecars across both upstream namespaces (and legacy flat leftovers) while skipping every REPORT.md"
    - "`oto sync --accept <path>` resolves the sidecar from the correct upstream namespace: auto-detects when unambiguous, fails loud when both upstreams recorded the same path, and accepts `--upstream gsd|superpowers` to disambiguate"
  artifacts:
    - path: "scripts/sync-upstream/merge.cjs"
      provides: "Per-upstream conflicts dir: sidecars and REPORT.md written under .oto-sync-conflicts/<upstream>/"
      contains: "values['conflicts-dir'], upstream"
    - path: "bin/lib/sync-cli.cjs"
      provides: "Namespace-aware --status counting and --accept/--accept-deletion/--keep-deleted namespace resolution"
    - path: "tests/16-07-sync-all-provenance.test.cjs"
      provides: "End-to-end --upstream all test with an overlapping target proving both records survive the second leg"
    - path: "docs/upstream-sync.md"
      provides: "Documented per-upstream conflict layout and accept disambiguation"
  key_links:
    - from: "scripts/sync-upstream/merge.cjs"
      to: ".oto-sync-conflicts/<upstream>/"
      via: "conflictsDir resolved as path.resolve(values['conflicts-dir'], upstream)"
      pattern: "conflicts-dir.*upstream"
    - from: "bin/lib/sync-cli.cjs"
      to: ".oto-sync-conflicts/{gsd,superpowers}/<relPath><suffix>"
      via: "accept-mode namespace resolution before calling sync-accept helpers"
      pattern: "gsd.*superpowers"
---

<objective>
Close verification blocking gap 1 (HARD-05 / WR-01): `oto sync --upstream all` currently writes both upstream legs into one flat `.oto-sync-conflicts` namespace, so the Superpowers leg overwrites overlapping GSD sidecars and rewrites `REPORT.md` to Superpowers-only. Namespace all durable conflict artifacts by upstream, keep `--status`/`--accept` provenance-safe against the new layout, and prove with an end-to-end `--upstream all` test that both upstreams' records survive the second leg.

Purpose: The milestone-close sync hygiene command must leave a durable record of the FULL conflict surface it inspects. Re-verification of HARD-05 depends on this.
Output: Namespaced conflict layout (`.oto-sync-conflicts/gsd/`, `.oto-sync-conflicts/superpowers/`), provenance-safe status/accept, new e2e regression test, updated docs.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/STATE.md
@.oto/phases/16-agent-guidance-hardening/16-VERIFICATION.md
@.oto/phases/16-agent-guidance-hardening/16-REVIEW.md
@bin/lib/sync-cli.cjs
@bin/lib/sync-merge.cjs
@bin/lib/sync-accept.cjs
@scripts/sync-upstream/merge.cjs
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — no exploration required. -->

From scripts/sync-upstream/merge.cjs (stage script, receives --upstream, defaults --conflicts-dir '.oto-sync-conflicts'):
```js
const conflictsDir = path.resolve(values['conflicts-dir']);   // line 58 — the flat namespace to fix
await writeReport(conflictsDir, summary);                      // line 96 — REPORT.md lands in conflictsDir
```

From bin/lib/sync-merge.cjs (library — DO NOT MODIFY in this plan):
```js
async function mergeAll({ inventoryPath, allowlistPath, otoDir, conflictsDir, priorRebrandedDir, currentRebrandedDir, upstream, ... }) // writes sidecars to given conflictsDir
async function writeReport(conflictsDir, summary)              // writes <conflictsDir>/REPORT.md
// sidecar suffixes: conflict -> '<target>.md', added/binary -> '<target>.added.md', deleted -> '<target>.deleted.md'
// each sidecar YAML header includes `upstream: <gsd|superpowers>` (emitYamlHeader)
```

From bin/lib/sync-accept.cjs (library — DO NOT MODIFY; sync-cli passes the resolved conflictsDir):
```js
async function acceptConflict({ relPath, otoDir, conflictsDir })                 // sidecar: <conflictsDir>/<relPath>.md
async function acceptDeletion({ relPath, otoDir, conflictsDir, inventoryPath })  // sidecar: <conflictsDir>/<relPath>.deleted.md
async function keepDeleted({ relPath, conflictsDir, allowlistPath })             // sidecar: <conflictsDir>/<relPath>.deleted.md
function assertSafeAcceptPath(rootDir, relPath)                                  // exported traversal guard — reuse it
```

From bin/lib/sync-cli.cjs:
```js
// parseSyncArgs currently throws: "--accept / --accept-deletion / --keep-deleted cannot be combined with --upstream/--to" (line 61-63)
// countConflicts skips only root REPORT.md: `else if (nextRel === 'REPORT.md') continue;` (line 165)
// runSync accept modes pass conflictsDir = path.resolve('.oto-sync-conflicts') (line 212)
// runFullSync expands 'all' -> ['gsd', 'superpowers'] and runs runUpstreamSequence (lines 139-156)
```

From tests/fixtures/phase-09/build-bare-upstream.cjs:
```js
// buildBareUpstream({ rootDir }) -> { bareUrl, workDir, tags: ['v1.0.0','v1.1.0','v1.2.0'] }
// v1.0.0 tree: README.md ('line1\nline2\nline3\n'), oto/workflows/sample.md ('# sample\n\nbody\n')
```
</interfaces>

<re_review_scope>
The subsequent re-review is limited to files changed by the Phase 16 gap plans (16-07 and 16-08). For this plan that is exactly: `scripts/sync-upstream/merge.cjs`, `bin/lib/sync-cli.cjs`, `tests/phase-09-cli.integration.test.cjs`, `tests/16-07-sync-all-provenance.test.cjs`, `docs/upstream-sync.md`. The re-reviewer must not re-open passed requirements (GUID-02..05, HARD-01, HARD-03, HARD-04) or the developer-approved WR-02 DEFER disposition.
</re_review_scope>

<constraints>
- Do NOT modify `bin/lib/sync-merge.cjs` or `bin/lib/sync-accept.cjs` — the namespacing lives at the stage boundary (merge.cjs) and the CLI resolution layer (sync-cli.cjs). This keeps the 13/13 merge and accept-helper library tests valid unchanged.
- Do NOT touch `sdk/`, any SDK baseline, golden row, or `16-SDK-BASELINE-DELTA.txt`. No SDK dist rebuild is needed for this plan.
- Do NOT implement anything from WR-02 (rebrand `do_not_rename` allowlist / planning-root migration) — developer-approved DEFER.
- Fail-loud semantics from Plan 16-06 stay intact: `--upstream all` still runs both legs and returns the first nonzero status.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Namespace conflict artifacts by upstream and prove both records survive `--upstream all`</name>
  <files>tests/16-07-sync-all-provenance.test.cjs, scripts/sync-upstream/merge.cjs, tests/phase-09-cli.integration.test.cjs</files>
  <read_first>
    - scripts/sync-upstream/merge.cjs (the file being modified — conflictsDir resolution at line 58, writeReport call at line 96)
    - bin/lib/sync-merge.cjs (source of truth for sidecar suffixes and the `upstream:` YAML header field — do not modify)
    - tests/phase-09-cli.integration.test.cjs (existing fixture/setup helpers to replicate: makeTempRoot, setupSyncProject, runOtoSync, inventoryEntry, writeJson; REPORT.md path assertions at lines 156 and 183)
    - tests/fixtures/phase-09/build-bare-upstream.cjs (fixture repo shape — both legs of `--upstream all` can use the same `--fixture-url`)
    - scripts/sync-upstream/pull-superpowers.cjs (confirms the superpowers pull stage accepts `--url`, so one bare fixture serves both legs)
  </read_first>
  <behavior>
    - Test 1 (WR-01 regression, end-to-end): running `node bin/install.js sync --upstream all --to v1.0.0 --fixture-url <bareUrl>` (default dry-run) in a project whose `decisions/file-inventory.json` contains entries for BOTH upstreams with the SAME `target_path: 'oto/workflows/sample.md'`, and whose local `oto/workflows/sample.md` diverges from the fixture, leaves BOTH sidecars on disk after the run completes: `.oto-sync-conflicts/gsd/oto/workflows/sample.md.md` with YAML header line `upstream: gsd` AND `.oto-sync-conflicts/superpowers/oto/workflows/sample.md.md` with YAML header line `upstream: superpowers`. The GSD sidecar must still contain the local divergent body marker after the second (superpowers) leg — this is the exact overwrite the verifier reproduced.
    - Test 2: both `.oto-sync-conflicts/gsd/REPORT.md` and `.oto-sync-conflicts/superpowers/REPORT.md` exist after the same run; the gsd report matches /gsd/ and the superpowers report matches /superpowers/. No REPORT.md is written at the `.oto-sync-conflicts` root.
    - Test 3: single-upstream sync also namespaces — after a gsd-only fixture sync, the report is at `.oto-sync-conflicts/gsd/REPORT.md` (updated assertions in phase-09-cli.integration.test.cjs).
  </behavior>
  <action>
    RED first: create `tests/16-07-sync-all-provenance.test.cjs` implementing the behaviors above, run it, confirm it FAILS against current HEAD (the superpowers leg overwrites `.oto-sync-conflicts/oto/workflows/sample.md.md` and REPORT.md today). Commit the failing test. Then implement GREEN:

    1. `scripts/sync-upstream/merge.cjs`:
       - After the existing `--upstream` presence check (exit 2 when missing), add value validation: if `values.upstream !== 'gsd' && values.upstream !== 'superpowers'`, write `sync-merge: --upstream must be gsd or superpowers\n` to stderr and `process.exit(2)`. This prevents path injection into the namespace segment (see threat T-16.07-01).
       - Change line 58 from `const conflictsDir = path.resolve(values['conflicts-dir']);` to `const conflictsDir = path.resolve(values['conflicts-dir'], upstream);`. Everything downstream (mergeAll sidecars, `writeReport`) then lands under `.oto-sync-conflicts/<upstream>/` automatically. No other change in this file.

    2. New test `tests/16-07-sync-all-provenance.test.cjs` (node:test, CJS):
       - Replicate the helper pattern from tests/phase-09-cli.integration.test.cjs: `makeTempRoot`, `preserveFiles` over the three rebrand report paths (`reports/coverage-manifest.pre.json`, `reports/coverage-manifest.post.json`, `reports/coverage-manifest.delta.md`), `writeJson`, `runOtoSync(args, cwd)` spawning `process.execPath [REPO_ROOT/bin/install.js, 'sync', ...args]`, and `buildBareUpstream` fixture.
       - Project setup: mkdir `oto/`; write `.oto-sync/BREAKING-CHANGES-gsd.md` AND `.oto-sync/BREAKING-CHANGES-superpowers.md` headers; copy `rename-map.json`; write `decisions/sync-allowlist.json` with `oto_owned_globs: ['decisions/**', '.oto/**', '.oto-sync/**', '.oto-sync-conflicts/**']`; write `decisions/file-inventory.json` with SIX entries — for EACH of `README.md`, `oto/workflows/sample.md`, `oto/workflows/added.md` one entry with `upstream: 'gsd'` and one with `upstream: 'superpowers'` (all `verdict: 'keep'`, `target_path` = path). Covering all three fixture paths for both upstreams keeps `unclassifiedAdds` at 0 so both legs exit 0 and the aggregate exit status is 0.
       - Write local divergent `oto/workflows/sample.md` with content `# local divergent\n\nlocal body\n` (differs from fixture `# sample\n\nbody\n` → first-sync conflict sidecar on both legs).
       - Run `runOtoSync(['--upstream', 'all', '--to', 'v1.0.0', '--fixture-url', fixture.bareUrl], project)`; assert `result.status === 0` (all entries classified).
       - Assert Test 1 and Test 2 behaviors: exact sidecar paths, `upstream: gsd` / `upstream: superpowers` header lines, `# local divergent` present in BOTH sidecar bodies, both REPORT.md files, and `fs.existsSync(path.join(project, '.oto-sync-conflicts/REPORT.md')) === false`.
       - NOTE: if the superpowers rebrand rules transform the `oto/workflows/sample.md` path (check the resulting rebranded tree if the assertion fails), switch the shared target to a path untouched by rename rules (e.g. `docs/shared-note.md` added to the fixture inventory entries) — the invariant under test is per-upstream survival of an overlapping target, not the specific filename.
    3. `tests/phase-09-cli.integration.test.cjs`: update the two flat-layout assertions to the namespaced layout — line 156 and line 183 change `'.oto-sync-conflicts/REPORT.md'` to `'.oto-sync-conflicts/gsd/REPORT.md'`. No other assertion in this file changes in this task.
  </action>
  <verify>
    <automated>node --test tests/16-07-sync-all-provenance.test.cjs tests/phase-09-cli.integration.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "conflicts-dir'\], upstream" scripts/sync-upstream/merge.cjs` returns exactly one match (namespaced resolution).
    - `grep -n "must be gsd or superpowers" scripts/sync-upstream/merge.cjs` returns a match (upstream value validation before path join).
    - `grep -c "oto-sync-conflicts/gsd/REPORT.md" tests/phase-09-cli.integration.test.cjs` returns 2 (both updated assertions).
    - `tests/16-07-sync-all-provenance.test.cjs` exists, asserts the literal strings `upstream: gsd` and `upstream: superpowers` against two DIFFERENT sidecar paths under `.oto-sync-conflicts/gsd/` and `.oto-sync-conflicts/superpowers/`, and asserts `.oto-sync-conflicts/REPORT.md` (root, flat) does NOT exist.
    - `node --test tests/16-07-sync-all-provenance.test.cjs tests/phase-09-cli.integration.test.cjs` exits 0 with 0 failures.
    - Git history shows the test commit (RED) before the implementation commit (GREEN).
  </acceptance_criteria>
  <done>The exact `--upstream all` path writes each leg's sidecars and REPORT.md into its own upstream namespace, and an end-to-end test with an overlapping target proves the GSD record survives the Superpowers leg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make `--status` and `--accept` provenance-safe against the namespaced layout; document it</name>
  <files>bin/lib/sync-cli.cjs, tests/16-07-sync-all-provenance.test.cjs, tests/phase-09-cli.integration.test.cjs, docs/upstream-sync.md</files>
  <read_first>
    - bin/lib/sync-cli.cjs (the file being modified — parseSyncArgs mutual-exclusion at lines 61-63, countConflicts REPORT skip at line 165, runStatus at lines 179-199, accept dispatch at lines 216-231)
    - bin/lib/sync-accept.cjs (source of truth for helper signatures and the exported `assertSafeAcceptPath` traversal guard — reuse, do not modify)
    - tests/phase-09-cli.integration.test.cjs (status test at lines 194-224 asserting `M=0 A=1 D=1`; parser test at lines 103-117)
    - docs/upstream-sync.md (sections to update: sidecar layout at line 69, accept examples at lines 81-91, conflicts-dir mention at line 53, REPORT.md mention at line 117)
  </read_first>
  <behavior>
    - Test 1: `--status` counts sidecars in BOTH `.oto-sync-conflicts/gsd/` and `.oto-sync-conflicts/superpowers/` plus legacy flat leftovers, and skips a file named `REPORT.md` at ANY depth. With one namespaced modified sidecar + namespace REPORT.md added to the existing flat fixtures, the aggregate line reads `M=1 A=1 D=1`.
    - Test 2: `--accept <relPath>` with the sidecar present in exactly ONE namespace auto-detects that namespace, accepts, and leaves the other namespace untouched.
    - Test 3: `--accept <relPath>` with the sidecar present in BOTH namespaces exits nonzero with a message matching /both upstreams/ and /--upstream/; re-running with `--upstream gsd` accepts the gsd sidecar and leaves `.oto-sync-conflicts/superpowers/<relPath>.md` intact.
    - Test 4: parseSyncArgs accepts `--accept x --upstream gsd`, still rejects `--accept x --to v1`, and rejects `--accept x --upstream all`.
  </behavior>
  <action>
    Extend `tests/16-07-sync-all-provenance.test.cjs` with the accept/ambiguity behaviors (Tests 2-4 above; for accept legs, pre-write resolved sidecars — YAML header per `emitYamlHeader` field order with body free of `<<<<<<<`/`=======`/`>>>>>>>` markers, since `acceptConflict` refuses unresolved markers) and update the status test in `tests/phase-09-cli.integration.test.cjs` (Test 1). Then implement in `bin/lib/sync-cli.cjs`:

    1. `countConflicts` (line 165): change `else if (nextRel === 'REPORT.md') continue;` to `else if (entry.name === 'REPORT.md') continue;`. Safe: real sidecars always carry a suffix appended to the target filename (`<target>.md` / `<target>.added.md` / `<target>.deleted.md`), so a bare `REPORT.md` at any depth can only be a generated report.
    2. `runStatus`: keep the existing aggregate line format `  pending conflicts: M=${...} A=${...} D=${...}` (walking the `.oto-sync-conflicts` root already spans both namespaces once the skip rule is depth-agnostic). After it, for each upstream in `['gsd', 'superpowers']` where `.oto-sync-conflicts/<upstream>` exists, append a per-upstream breakdown line exactly: `  pending conflicts [<upstream>]: M=x A=y D=z` using `countConflicts` on that subdirectory.
    3. `parseSyncArgs`: replace the lines 61-63 rule with: accept modes (`accept`, `accept-deletion`, `keep-deleted`) may not be combined with `--to`; they MAY carry `--upstream`, whose value must be `gsd` or `superpowers` (throw `--upstream must be gsd or superpowers when used with --accept flags` for anything else, including `all`). `--status` keeps rejecting all sync target flags as today.
    4. `runSync` accept dispatch: add a module-level helper
       `function resolveAcceptDir(conflictsRoot, relPath, suffix, upstreamFlag)`:
       - First call `assertSafeAcceptPath(conflictsRoot, relPath + suffix)` (imported from `./sync-accept.cjs`) to reject traversal BEFORE any filesystem probe (threat T-16.07-01).
       - If `upstreamFlag` set: return `path.join(conflictsRoot, upstreamFlag)`.
       - Else probe `['gsd', 'superpowers']` for `fs.existsSync(path.join(conflictsRoot, u, relPath + suffix))`. Two hits → throw `'<relPath>' has pending records from both upstreams; disambiguate with --upstream gsd or --upstream superpowers`. One hit → return that namespace dir. Zero hits → return `conflictsRoot` (legacy flat-layout fallback so the helpers' own ENOENT message still fires for truly-missing sidecars).
       - Wire it: `accept` mode uses suffix `'.md'`, `accept-deletion` and `keep-deleted` use `'.deleted.md'`; pass the resolved dir as `conflictsDir` to the existing sync-accept helpers (helpers unchanged).
    5. `docs/upstream-sync.md`: update the conflict-layout description to `.oto-sync-conflicts/<upstream>/<target-path>.md` (line 69 area), state that each upstream leg writes its own `REPORT.md` at `.oto-sync-conflicts/<upstream>/REPORT.md` (so `--upstream all` preserves both reports; line 53 and 117 areas), and extend the accept section (lines 81-91): `--accept` auto-detects the upstream namespace, errors when both upstreams recorded the same path, and takes `--upstream gsd|superpowers` to disambiguate. Keep the literal string `.oto-sync-conflicts/` present (tests/phase-10-docs-presence.test.cjs asserts it).
  </action>
  <verify>
    <automated>node --test tests/16-07-sync-all-provenance.test.cjs tests/phase-09-cli.integration.test.cjs tests/phase-09-accept-helper.test.cjs tests/phase-09-merge-add-delete.test.cjs tests/phase-09-allowlist.test.cjs tests/phase-09-allowlist-corpus.integration.test.cjs tests/phase-09-merge-3way.test.cjs tests/phase-10-docs-presence.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "entry.name === 'REPORT.md'" bin/lib/sync-cli.cjs` returns a match; `grep -c "nextRel === 'REPORT.md'" bin/lib/sync-cli.cjs` returns 0.
    - `grep -n "resolveAcceptDir" bin/lib/sync-cli.cjs` shows the helper definition and its use for all three accept modes.
    - `grep -n "assertSafeAcceptPath" bin/lib/sync-cli.cjs` shows the guard is invoked before namespace probing.
    - `grep -n "pending conflicts \[" bin/lib/sync-cli.cjs` shows the per-upstream breakdown line.
    - `node bin/install.js sync --status` run at the repo root exits 0 and prints a line matching `pending conflicts: M=`.
    - `grep -n "oto-sync-conflicts/<upstream>" docs/upstream-sync.md` (or the equivalent literal per-upstream path) returns at least one match, and `grep -c "oto-sync-conflicts/" docs/upstream-sync.md` returns >= 1.
    - The focused sync suite listed in <verify> exits 0 with 0 failures — the untouched sync-merge/sync-accept library tests pass unchanged, proving the namespacing lives entirely at the stage/CLI boundary.
  </acceptance_criteria>
  <done>`oto sync --status` and all three accept flags operate correctly and provenance-safely against the per-upstream layout (with legacy flat fallback), the layout and disambiguation are documented, and the full sync-focused regression suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → filesystem paths | `--accept`/`--upstream`/`--conflicts-dir` values flow into resolved paths under the repo |
| Upstream repo content → local sidecars | Upstream target paths become sidecar paths inside `.oto-sync-conflicts/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.07-01 | Tampering (path traversal) | merge.cjs conflictsDir / sync-cli resolveAcceptDir | mitigate | Namespace segment is only ever the validated literal `gsd`/`superpowers` (merge.cjs exits 2 on any other value; parseSyncArgs rejects non-literal `--upstream` with accept flags). `resolveAcceptDir` calls the existing `assertSafeAcceptPath` on `relPath + suffix` before any filesystem probe; sidecar writes keep the existing `assertSafeOutputPath` guard in sync-merge.cjs. |
| T-16.07-02 | Tampering (artifact clobbering) | mergeAll sidecars + writeReport | mitigate | Per-upstream namespacing removes the cross-upstream same-path collision entirely; e2e test asserts the first leg's sidecar and report survive the second leg byte-verifiably (`upstream: gsd` header + body marker). |
| T-16.07-03 | Repudiation (provenance loss) | `--accept` on overlapping paths | mitigate | Ambiguity is fail-loud: two-namespace hits throw with an explicit disambiguation instruction instead of silently accepting the wrong upstream's content. |
| T-16.07-04 | Denial of service (status walk over hostile dir tree) | countConflicts recursive walk | accept | Walk is bounded to a repo-local directory the operator controls; no symlink following added; low-value target, no PII. |
</threat_model>

<verification>
- `node --test tests/16-07-sync-all-provenance.test.cjs` — the WR-01 regression test passes: both upstreams' sidecars + reports survive `--upstream all`.
- Focused sync suite (all phase-09 sync tests + phase-10 docs presence + the new test) — 0 failures.
- `npm test` — no new failures beyond the known sandboxed-DNS install-smoke case (re-run that single suite with network if it fails: `node --test tests/phase-04-mr01-install-smoke.test.cjs`).
- No file under `sdk/`, no baseline, and no golden row modified (`git status` shows changes only in this plan's `files_modified`).
</verification>

<success_criteria>
- HARD-05's blocking gap is closed: the documented milestone-close command `node bin/install.js sync --upstream all --to latest --dry-run` durably records BOTH upstreams' conflict surfaces with no overwrite, verifiable from disk after the run.
- `oto sync --status` / `--accept` / `--accept-deletion` / `--keep-deleted` are provenance-safe against the namespaced layout.
- Re-verification evidence exists as an automated e2e test, not just terminal transcription.
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-07-SUMMARY.md`
</output>
