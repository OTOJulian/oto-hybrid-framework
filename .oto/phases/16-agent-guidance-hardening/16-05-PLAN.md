---
phase: 16-agent-guidance-hardening
plan: 05
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - oto/agents/oto-debugger.md
  - oto/agents/oto-advisor-researcher.md
  - tests/16-search-reference.test.cjs
  - tests/16-transformed-tool-names.test.cjs
autonomous: true
requirements: [GUID-03, GUID-04, GUID-05]
must_haves:
  truths:
    - "oto-debugger and oto-advisor-researcher carry mcp__exa__* frontmatter and consume the shared search reference by include pointer"
    - "No deprecated Exa tool name appears in the source OR the Codex-md OR Codex-toml OR Gemini-md transformed output of any of the five agents, nor in any shipped reference"
    - "Gemini-transformed frontmatter contains zero mcp__ entries; Codex-transformed output carries the tools line so Exa access survives the transform"
  artifacts:
    - path: "tests/16-transformed-tool-names.test.cjs"
      provides: "GUID-04/05 grep-guards over real transform OUTPUT (not source)"
      min_lines: 60
  key_links:
    - from: "oto/agents/oto-debugger.md"
      to: "oto/references/search-tools.md"
      via: "include pointer"
      pattern: "@~/.claude/oto/references/search-tools\\.md"
    - from: "oto/agents/oto-advisor-researcher.md"
      to: "oto/references/search-tools.md"
      via: "include pointer"
      pattern: "@~/.claude/oto/references/search-tools\\.md"
    - from: "tests/16-transformed-tool-names.test.cjs"
      to: "bin/lib/codex-transform.cjs"
      via: "runs the real shipped transforms in-process"
      pattern: "convertClaudeAgentToCodexAgent"
---

<objective>
Extend Exa access to `oto-debugger` and `oto-advisor-researcher` via `mcp__exa__*` frontmatter plus the shared-reference include (GUID-03), then build the transform-output grep-guards proving no deprecated Exa tool names ship in any of the five agents across all runtime outputs (GUID-04) and that Codex/Gemini tool naming survives the transforms correctly (GUID-05).

Purpose: The reference now exists (16-01) so extension cannot create new drift — debugger/advisor get one include line each, never copied prose. Verification MUST run against TRANSFORMED output because the Codex `<codex_agent_role>` header and TOML instructions carry the `tools:` line verbatim, and Gemini filters `mcp__*` from frontmatter — source-only greps verify nothing.

Output: two extended agents (synced to both runtime roots), the GUID-04/05 test file, extended structural test.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/16-agent-guidance-hardening/16-RESEARCH.md

Frontmatter decision (research Open Question 1): use the WILDCARD form `mcp__exa__*` for consistency with the three researchers (which already ship it). Current docs say server-level wildcards work in subagent `tools:`; the HARD-04 checkpoint in 16-06 empirically resolves the conflicting closed issue #53865 on the installed CLI (2.1.212) — if the wildcard fails there, 16-06's contingency switches all five agents to exact 3-tool enumeration.

Deprecated-name list (VERIFIED against the exa-mcp-server@3.2.1 bundle in Phase 15 research): `crawling_exa`, `get_code_context_exa`, `deep_researcher_start`, `deep_researcher_check`, `company_research_exa`, `linkedin_search_exa`, `deep_search_exa`, `people_search_exa`.

Transform functions (verified signatures): `convertClaudeAgentToCodexAgent(content)` and `generateCodexAgentToml(agentName, agentContent, sandboxMap = {}, ...)` from `bin/lib/codex-transform.cjs`; `convertClaudeToGeminiAgent(content)` from `bin/lib/gemini-transform.cjs` (its `convertGeminiToolName` returns null for `mcp__*` — the frontmatter filter). Model test file: `tests/phase-08-codex-transform.test.cjs`.

Sync precedent: `~/.claude` straight copy; `~/.codex` edit-in-place on `.md` AND `.toml` `developer_instructions` with `~/.codex`-rooted include path; never regenerate installed .toml; `~/.gemini`: no install, skip with notice.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exa frontmatter + shared-reference include for debugger and advisor + sync</name>
  <files>oto/agents/oto-debugger.md, oto/agents/oto-advisor-researcher.md, tests/16-search-reference.test.cjs</files>
  <read_first>
    - oto/agents/oto-debugger.md (lines 1-40 — frontmatter and role block; find the closing </role> tag as the insertion anchor)
    - oto/agents/oto-advisor-researcher.md (entire file, 127 lines — tool_strategy block lines 102-117 is the insertion site)
    - oto/agents/oto-phase-researcher.md (post-16-01 state — the include-block shape to replicate)
    - tests/16-search-reference.test.cjs (16-01 state — the researcher loop to extend)
    - ~/.codex/agents/oto-debugger.md and ~/.codex/agents/oto-debugger.toml (installed Codex dialect before edit-in-place)
  </read_first>
  <action>
    **oto-debugger.md:**
    1. Frontmatter line 4: change `tools: Read, Write, Edit, Bash, Grep, Glob, WebSearch` to `tools: Read, Write, Edit, Bash, Grep, Glob, WebSearch, mcp__exa__*`.
    2. Immediately after the closing `</role>` tag, insert a new top-level block:

    ```markdown
    <search_tools>

    ## Search Tools

    @~/.claude/oto/references/search-tools.md
    </search_tools>
    ```

    **oto-advisor-researcher.md:**
    1. Frontmatter line 4: change `tools: Read, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*` to `tools: Read, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*, mcp__exa__*`.
    2. Inside `<tool_strategy>`, after the `**Context7 flow:**` numbered list (line ~114) and before `Keep research focused...`, insert:

    ```markdown
    ## Search Tools

    @~/.claude/oto/references/search-tools.md
    ```

    Do NOT copy any search prose into either agent — include pointer only (copy-pasting is how the original drift happened).

    **Extend `tests/16-search-reference.test.cjs`:** grow the agent loop to all five names; for `oto-debugger` and `oto-advisor-researcher` additionally assert the frontmatter `tools:` line matches `/^tools:.*mcp__exa__\*/m` and the include line is present.

    **Sync (same task — CLAUDE.md Runtime Sync Guardrail):**
    1. `cp` both repo agent files to `~/.claude/agents/` (after `diff`-verifying the pre-change installed copies matched pre-change repo state; else edit-in-place).
    2. `~/.codex/agents/oto-debugger.{md,toml}` and `oto-advisor-researcher.{md,toml}`: apply the same frontmatter tools-line change and body insertion edit-in-place, include path rewritten to `@~/.codex/oto/references/search-tools.md`. Codex .md frontmatter is the Codex dialect (comma-string tools) — append `, mcp__exa__*` to its tools value; the .toml has no tools key (Codex restricts by sandbox, not tools) so only its `developer_instructions` body gets the insertion (plus the `<codex_agent_role>` tools prose if present — update it to match).
    3. `~/.gemini`: no oto install — record "gemini: no install, skipped".
    4. `node scripts/check-runtime-sync.cjs` — must exit 0.
  </action>
  <verify>
    <automated>node --test tests/16-search-reference.test.cjs && node scripts/check-runtime-sync.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mcp__exa__\*' oto/agents/oto-debugger.md` == 1 (frontmatter) and `grep -c '@~/.claude/oto/references/search-tools.md' oto/agents/oto-debugger.md` == 1
    - `grep -c 'mcp__exa__\*' oto/agents/oto-advisor-researcher.md` == 1 and `grep -c '@~/.claude/oto/references/search-tools.md' oto/agents/oto-advisor-researcher.md` == 1
    - `grep -c '@~/.codex/oto/references/search-tools.md' ~/.codex/agents/oto-debugger.md` == 1 and `grep -c '@~/.codex/oto/references/search-tools.md' ~/.codex/agents/oto-debugger.toml` == 1 (same for advisor)
    - `node --test tests/16-search-reference.test.cjs` exits 0 (five-agent loop green)
    - `node scripts/check-runtime-sync.cjs` exits 0
  </acceptance_criteria>
  <done>Debugger and advisor have Exa frontmatter + include; zero copied prose; both runtime roots synced; extended structural test green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GUID-04/05 transform-output grep-guards</name>
  <files>tests/16-transformed-tool-names.test.cjs</files>
  <read_first>
    - tests/phase-08-codex-transform.test.cjs (the exact model for running transforms in-process)
    - bin/lib/codex-transform.cjs (lines 45-80 — convertClaudeAgentToCodexAgent and generateCodexAgentToml signatures/output shape)
    - bin/lib/gemini-transform.cjs (lines 10-60 — convertGeminiToolName mcp filter, convertClaudeToGeminiAgent output shape)
    - .oto/phases/16-agent-guidance-hardening/16-RESEARCH.md (Pattern 2 — the prescribed test skeleton)
  </read_first>
  <behavior>
    - For each of the 5 agents × 4 outputs (source, codex-md via convertClaudeAgentToCodexAgent, codex-toml via generateCodexAgentToml, gemini-md via convertClaudeToGeminiAgent): output contains NONE of the 8 deprecated names: crawling_exa, get_code_context_exa, deep_researcher_start, deep_researcher_check, company_research_exa, linkedin_search_exa, deep_search_exa, people_search_exa
    - Every file in oto/references/ (they ship untransformed): contains none of the 8 deprecated names
    - oto/references/search-tools.md contains zero `mcp__` tokens
    - GUID-05 Gemini: for each of the 5 agents, the frontmatter section of the gemini-md output (text between the first `---` pair) contains zero `mcp__` occurrences
    - GUID-05 Gemini body: the gemini-md BODY of each agent contains zero `mcp__exa__` tokens (runtime-neutral include means Exa names never appear in body prose)
    - GUID-05 Codex: codex-md output of oto-debugger and oto-advisor-researcher contains `mcp__exa__*` (the tools line survives the transform verbatim, proving Exa access ships to Codex)
  </behavior>
  <action>
    Create `tests/16-transformed-tool-names.test.cjs` implementing <behavior>, modeled on the research Pattern 2 skeleton: `'use strict'`, node:test, require the two transform modules from `../bin/lib/`, read the five agent sources from `oto/agents/`, generate all four outputs per agent, loop assertions. For the references sweep, walk `oto/references/` recursively for `.md` files. Use `generateCodexAgentToml(name, src)` with default remaining args (test-hermetic; sandbox/model values are irrelevant to name greps). Keep the DEPRECATED list as a top-of-file constant with the provenance comment `// VERIFIED against the exa-mcp-server@3.2.1 bundle (15-RESEARCH.md)`.

    Run: all green immediately IF Task 1 and 16-01 were done correctly — if any assertion is red, fix the offending agent/reference content (not the test) and re-sync per the guardrail.
  </action>
  <verify>
    <automated>node --test tests/16-transformed-tool-names.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/16-transformed-tool-names.test.cjs` exists; contains all 8 deprecated names as constants; contains `convertClaudeAgentToCodexAgent`, `generateCodexAgentToml`, `convertClaudeToGeminiAgent`
    - Test count >= 20 (5 agents × 4 outputs for deprecated names, plus gemini-frontmatter, gemini-body, codex-tools-line, and reference assertions)
    - `node --test tests/16-transformed-tool-names.test.cjs` exits 0
    - `npm test` exits 0 (full suite, includes the new file via the tests/*.test.cjs glob)
  </acceptance_criteria>
  <done>Deprecated-name and namespace guards run against real transform output for all five agents plus references; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent frontmatter tools: → runtime tool access | Widens debugger/advisor tool surface to the 3 Exa tools |
| repo → installed runtime roots | Edited agents cross into live sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Elevation of Privilege | mcp__exa__* grant to debugger/advisor | accept | Grants exactly 3 read-only web-search tools from a pinned server; no write/exec capability added; same surface the researchers already carry |
| T-16-05-02 | Tampering | Transform pipeline shipping wrong/deprecated tool names to Codex/Gemini | mitigate | GUID-04/05 tests run the real shipped transforms in-process and grep OUTPUT; hermetic, CI-enforced via npm test glob |
| T-16-05-03 | Spoofing | Model hallucinating tool names from stale prose | mitigate | Include-pointer-only guidance; runtime-neutral server names in the reference; zero mcp__ tokens in untransformed content |
</threat_model>

<verification>
- `node --test tests/16-search-reference.test.cjs tests/16-transformed-tool-names.test.cjs` green
- `node scripts/check-runtime-sync.cjs` exit 0
- `npm test` full suite green
</verification>

<success_criteria>
- Five agents share one guidance source; two newly-extended agents have Exa frontmatter
- Deprecated names provably absent from every shipped output form
- Codex/Gemini naming verified against transformed output, not source
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-05-SUMMARY.md`
</output>
