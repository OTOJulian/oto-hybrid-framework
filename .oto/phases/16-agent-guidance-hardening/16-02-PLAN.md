---
phase: 16-agent-guidance-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/bin/lib/init.cjs
  - oto/workflows/settings-integrations.md
  - tests/16-availability-coherence.test.cjs
  - tests/16-agent-skills-array.test.cjs
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "cmdInitNewProject reports exa/brave/firecrawl availability via detectKeySource — an empty or dangling keyfile yields available:false (agents never get told a dead tool is live)"
    - "The settings workflow persists agent_skills as a JSON array of validated names, never one comma-joined string"
    - "A two-skill agent_skills config produces two @…/SKILL.md lines from the agent-skills consumer (end-to-end)"
    - "A legacy comma-joined agent_skills string self-heals at read time in buildAgentSkillsBlock"
  artifacts:
    - path: "tests/16-availability-coherence.test.cjs"
      provides: "HARD-01 CJS regression fixtures (empty/whitespace/valid keyfile, env var)"
      min_lines: 50
    - path: "tests/16-agent-skills-array.test.cjs"
      provides: "WR-04 end-to-end two-skill injection test"
      min_lines: 40
  key_links:
    - from: "oto/bin/lib/init.cjs"
      to: "oto/bin/lib/secrets.cjs"
      via: "detectKeySource in cmdInitNewProject"
      pattern: "detectKeySource\\('exa'\\)\\.source !== null"
    - from: "oto/workflows/settings-integrations.md"
      to: "config-set agent_skills"
      via: "JSON array write"
      pattern: "agent_skills\\.<slug> '\\["
---

<objective>
Close the two CJS-side coherence gaps this phase contractually owns: (a) HARD-01's availability-gate defect — `cmdInitNewProject` uses bare env/`existsSync` so empty/dangling keyfiles report integrations as available, violating the Phase 15 locked decision "All key availability gates use detectKeySource"; (b) the WR-04 pre-task from STATE.md Pending Todos — the settings workflow persists comma-separated agent skills as ONE quoted string that the consumer treats as a single nonexistent skill path.

Purpose: Agents trust init-context booleans; HARD-01's "zero user-facing errors" floor requires those booleans to reflect usable keys. WR-04 is a deferred Phase 14 review item with a tracked Phase 16 owner — plans that omit it fail verification against STATE.md.

Output: detectKeySource-aligned `cmdInitNewProject`, JSON-array agent_skills write path + self-healing consumer, two new test files, synced workflow file.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/16-agent-guidance-hardening/16-RESEARCH.md

Verified fix sites:
- `oto/bin/lib/init.cjs:352-366` — `cmdInitNewProject` computes `hasBraveSearch/hasFirecrawl/hasExaSearch` from `!!(process.env.X || fs.existsSync(keyfile))`. Correct pattern already exists in `oto/bin/lib/config.cjs:90-92`: `detectKeySource('exa').source !== null` (commented `// OTO Phase 15 (FRESH-WR-04)`).
- Output fields at `init.cjs:446-448`: `brave_search_available`, `firecrawl_available`, `exa_search_available`.
- `oto/workflows/settings-integrations.md` step `section_3_agent_skills` (line ~354): writes `oto-sdk query config-set agent_skills.<slug> "<skill-a,skill-b,skill-c>"`. Both config-set paths JSON.parse their value argument (CJS `config.cjs:389`, SDK `config-mutation.ts:213`), so passing a JSON array literal persists a real array.
- Consumer `init.cjs:1606-1676` `buildAgentSkillsBlock`: normalizes a single string to `[string]` (line 1617) but never splits commas — `"skill-a,skill-b"` becomes one nonexistent path. CLI surface: `node oto/bin/lib/oto-tools.cjs agent-skills <agent-type>` (dispatch at oto-tools.cjs:669).
- Fixture technique (from tests/14-newproject-boolean.test.cjs): `spawnSync(process.execPath, [OTO_TOOLS, ...], { env: { ...process.env, HOME: tmpHome, EXA_API_KEY: '', BRAVE_API_KEY: '', FIRECRAWL_API_KEY: '' } })` — HOME override redirects `~/.oto` keyfile resolution; never touch the real `~/.oto` (this machine has a live exa keyfile).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detectKeySource alignment in cmdInitNewProject + availability-coherence fixtures</name>
  <files>oto/bin/lib/init.cjs, tests/16-availability-coherence.test.cjs</files>
  <read_first>
    - oto/bin/lib/init.cjs (lines 340-460 — cmdInitNewProject current state and output fields)
    - oto/bin/lib/config.cjs (lines 85-95 — the correct detectKeySource pattern to replicate, including its comment style)
    - oto/bin/lib/secrets.cjs (lines 165-195 — detectKeySource(slug, baseDir) signature; readKeyfile empty/whitespace handling)
    - tests/14-newproject-boolean.test.cjs (lines 1-60 — seedFixture/cleanEnv/spawnSync fixture technique to reuse)
    - oto/bin/lib/oto-tools.cjs (lines 875-925 — `init new-project` dispatch: argv is `init new-project`)
  </read_first>
  <behavior>
    - Test 1 (no keys): tmp HOME, all three env vars set to '' → JSON output has exa_search_available:false, brave_search_available:false, firecrawl_available:false
    - Test 2 (empty keyfile — THE regression): tmp HOME with `$HOME/.oto/exa_api_key` created containing '' → exa_search_available:false (pre-fix this is true; test must be RED before the fix)
    - Test 3 (whitespace-only keyfile): file containing "  \n" → exa_search_available:false
    - Test 4 (valid keyfile): `$HOME/.oto/brave_api_key` containing 'test-key-0123456789' (mode 0600) → brave_search_available:true
    - Test 5 (env var): EXA_API_KEY='test-env-key' in env → exa_search_available:true even with no keyfile
  </behavior>
  <action>
    First write `tests/16-availability-coherence.test.cjs` implementing the <behavior> cases: spawn `process.execPath [oto/bin/lib/oto-tools.cjs, 'init', 'new-project']` with `cwd` = tmp project dir, `HOME` = tmp home dir, and `EXA_API_KEY/BRAVE_API_KEY/FIRECRAWL_API_KEY` explicitly set to `''` (except the env-var case). Parse `JSON.parse(result.stdout)` and assert the `exa_search_available` / `brave_search_available` / `firecrawl_available` fields. Run it — Test 2 and Test 3 MUST fail against current code (RED). Commit `test(16-02): add failing availability-coherence fixtures`.

    Then fix `oto/bin/lib/init.cjs` `cmdInitNewProject`: replace the block at lines ~355-366 (the three `const ...KeyFile = path.join(homedir, '.oto', ...)` + `!!(process.env.X || fs.existsSync(...))` pairs, including the `const homedir = require('os').homedir();` line if it becomes unused) with:

    ```js
    // oto: HARD-01 — availability must use detectKeySource (Phase 15 D-15), not bare existence
    const { detectKeySource } = require('./secrets.cjs');
    const hasBraveSearch = detectKeySource('brave').source !== null;
    const hasFirecrawl = detectKeySource('firecrawl').source !== null;
    const hasExaSearch = detectKeySource('exa').source !== null;
    ```

    Keep the diff block-shaped and commented (this is a GSD-shared file — sync hygiene constraint). Check whether `homedir` is used elsewhere in the function before removing it. Re-run the test file — all green. Commit `feat(16-02): align cmdInitNewProject availability with detectKeySource (HARD-01)`.

    Note for SUMMARY: the SDK mirror `sdk/src/query/init-complex.ts:102-104` ALREADY uses `detectKeySource` (research assumption A3 resolved during planning — no SDK change needed for this defect; plan 16-03 covers the remaining SDK gaps).
  </action>
  <verify>
    <automated>node --test tests/16-availability-coherence.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "detectKeySource('exa').source !== null" oto/bin/lib/init.cjs` == 1 (and same for 'brave', 'firecrawl')
    - `grep -c "existsSync(exaKeyFile)" oto/bin/lib/init.cjs` == 0
    - `grep -c "oto: HARD-01" oto/bin/lib/init.cjs` == 1
    - `node --test tests/16-availability-coherence.test.cjs` exits 0 with >= 5 passing tests
    - `node --test tests/14-newproject-boolean.test.cjs` still exits 0 (no regression in config-new-project)
  </acceptance_criteria>
  <done>Empty/whitespace/dangling keyfiles report available:false from init new-project; valid keyfile and env-var cases report true; existing tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-04 — agent_skills JSON-array persistence + comma-split self-heal + e2e test</name>
  <files>oto/workflows/settings-integrations.md, oto/bin/lib/init.cjs, tests/16-agent-skills-array.test.cjs</files>
  <read_first>
    - oto/workflows/settings-integrations.md (lines 350-400 — section_3_agent_skills step, current write command)
    - oto/bin/lib/init.cjs (lines 1594-1695 — buildAgentSkillsBlock + cmdAgentSkills; existing string→array normalization at line 1617; validation rules at 1625-1665)
    - oto/bin/lib/oto-tools.cjs (line 669 — `agent-skills` command dispatch)
    - tests/14-settings-workflow-contract.test.cjs (workflow contract test conventions, if asserting workflow text)
  </read_first>
  <behavior>
    - Test 1 (e2e two-skill injection): tmp project with `.oto/config.json` = `{"agent_skills":{"oto-executor":["skill-a","skill-b"]}}` and both `skill-a/SKILL.md` + `skill-b/SKILL.md` created under the project root → `node oto-tools.cjs agent-skills oto-executor` stdout contains BOTH `- @skill-a/SKILL.md` AND `- @skill-b/SKILL.md`
    - Test 2 (legacy comma-string self-heal): config value `"skill-a,skill-b"` (single string) → stdout contains BOTH lines (consumer splits/trims)
    - Test 3 (whitespace trim): config value `"skill-a, skill-b "` → both lines, no whitespace in paths
    - Test 4 (workflow contract): oto/workflows/settings-integrations.md no longer contains the raw comma-string write `"<skill-a,skill-b,skill-c>"`; contains a JSON-array config-set example matching `agent_skills\.<slug> '\[`
  </behavior>
  <action>
    First write `tests/16-agent-skills-array.test.cjs` implementing <behavior> (spawnSync `oto/bin/lib/oto-tools.cjs agent-skills oto-executor` with cwd = tmp project; SKILL.md files need at least one line of content). Run — Tests 2/3/4 RED. Commit `test(16-02): add failing agent-skills array e2e (WR-04)`.

    **Fix A — consumer self-heal (`oto/bin/lib/init.cjs` buildAgentSkillsBlock):** immediately after line 1617's `if (typeof skillPaths === 'string') skillPaths = [skillPaths];`, add a block-shaped commented split:

    ```js
    // oto: WR-04 — legacy configs persisted comma-joined lists as one string; split and trim
    skillPaths = skillPaths
      .flatMap(p => (typeof p === 'string' ? p.split(',') : [p]))
      .map(p => (typeof p === 'string' ? p.trim() : p))
      .filter(p => p !== '');
    ```

    All existing per-entry validation (slug regex for `global:`, validatePath containment, SKILL.md existence) continues to run on each split entry — do NOT bypass it.

    **Fix B — workflow write path (`oto/workflows/settings-integrations.md` section_3_agent_skills):** replace the write instruction + command. New instructions: after collecting the comma-separated text input, split on `,`, trim each entry, drop empties, and validate EACH name against `^(global:)?[a-zA-Z0-9_-]+$`; if any entry fails, print the existing rejection message naming the offending entry and re-prompt (never write a partial list). Then persist a JSON array:

    ```bash
    oto-sdk query config-set agent_skills.<slug> '["skill-a","skill-b"]' ${WS_ARGS[@]+"${WS_ARGS[@]}"}
    ```

    (Both config-set paths JSON.parse the value argument, so this persists a real array.) Also update the workflow's final checklist line (~454) to say each skill NAME is validated against `[a-zA-Z0-9_-]+` (with optional `global:` prefix) before the JSON-array write.

    Run all tests green. Commit `feat(16-02): persist agent_skills as validated JSON array + comma-split self-heal (WR-04)`.

    **Sync (same task — settings-integrations.md is in the byte-checked oto/workflows tree):**
    1. `cp oto/workflows/settings-integrations.md ~/.claude/oto/workflows/settings-integrations.md`
    2. `cp oto/workflows/settings-integrations.md ~/.codex/oto/workflows/settings-integrations.md`
    3. `~/.gemini`: no oto install — record "gemini: no install, skipped"
    4. `node scripts/check-runtime-sync.cjs` — must exit 0
  </action>
  <verify>
    <automated>node --test tests/16-agent-skills-array.test.cjs && node scripts/check-runtime-sync.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "oto: WR-04" oto/bin/lib/init.cjs` == 1
    - `grep -c 'agent_skills.<slug> .\[' oto/workflows/settings-integrations.md` >= 1 (JSON-array write present)
    - `grep -c '"<skill-a,skill-b,skill-c>"' oto/workflows/settings-integrations.md` == 0 (raw comma-string write gone)
    - `node --test tests/16-agent-skills-array.test.cjs` exits 0 with >= 4 passing tests
    - `diff oto/workflows/settings-integrations.md ~/.claude/oto/workflows/settings-integrations.md` empty; same for ~/.codex
    - `node scripts/check-runtime-sync.cjs` exits 0
  </acceptance_criteria>
  <done>Two-skill config injects two SKILL.md lines end-to-end; legacy comma strings self-heal; workflow writes validated JSON arrays; workflow synced to both roots</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text input → tracked .oto/config.json | agent_skills names are user-supplied and later expanded into agent spawn prompts |
| config.json → agent spawn prompt | skill paths become @-includes read by agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Elevation of Privilege | agent_skills values (skill-injection via crafted names/paths) | mitigate | Workflow validates each name against `^(global:)?[a-zA-Z0-9_-]+$` before write; consumer re-validates via slug regex + `validatePath` symlink-escape containment (init.cjs:1646-1656) — write-path validation added, read-path validation preserved on every split entry |
| T-16-02-02 | Information Disclosure | init new-project output leaking key material | mitigate | detectKeySource returns only source/masked fields; test fixtures use synthetic keys in tmp HOME, never the real ~/.oto |
| T-16-02-03 | Denial of Service | availability=true for dead keyfile causing agent tool-call failures | mitigate | This IS the HARD-01 fix — detectKeySource rejects empty/whitespace/dangling keyfiles |
</threat_model>

<verification>
- `node --test tests/16-availability-coherence.test.cjs tests/16-agent-skills-array.test.cjs` green
- `node --test tests/14-newproject-boolean.test.cjs tests/14-settings-workflow-contract.test.cjs` still green (shared-file regression check)
- `node scripts/check-runtime-sync.cjs` exit 0
</verification>

<success_criteria>
- Empty/whitespace/dangling keyfiles can never make init report an integration available (CJS path)
- agent_skills round-trips as a validated JSON array end-to-end; legacy strings heal on read
- STATE.md WR-04 pre-task is dischargeable with test evidence
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-02-SUMMARY.md`
</output>
