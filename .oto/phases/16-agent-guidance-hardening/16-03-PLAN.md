---
phase: 16-agent-guidance-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/websearch.ts
  - sdk/src/query/websearch.test.ts
  - sdk/src/query/secret-commands.ts
  - sdk/src/query/secret-status-inheritance.test.ts
  - sdk/dist/**
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "A keyfile-only Brave user gets the Brave rung: oto-sdk query websearch resolves the key from ~/.oto/brave_api_key when BRAVE_API_KEY is unset, and still degrades to {available:false} (never throws) with no key"
    - "Workstream secret-status reflects effective root-to-workstream flags: root exa_search:true shows enabled under a selected workstream"
    - "A legacy plaintext string in the ROOT config self-heals to the keyfile even when secret-status runs with a workstream selected"
    - "secret-status output remains masked-only (no raw key bytes in structured data or raw lines)"
  artifacts:
    - path: "sdk/src/query/websearch.test.ts"
      provides: "Brave keyfile-rung + graceful-degradation vitest coverage"
      min_lines: 40
    - path: "sdk/src/query/secret-status-inheritance.test.ts"
      provides: "FRESH-CR-03 root-true and root-legacy reproductions turned regressions"
      min_lines: 50
  key_links:
    - from: "sdk/src/query/websearch.ts"
      to: "sdk/src/query/secrets.ts"
      via: "readKeyfile fallback for Brave key"
      pattern: "readKeyfile\\('brave'"
    - from: "sdk/src/query/secret-commands.ts"
      to: "sdk/src/config.ts loadConfig"
      via: "effective root+workstream merged view"
      pattern: "loadConfig\\(projectDir, workstream\\)"
    - from: "sdk/src/query/secret-commands.ts"
      to: "root config migration"
      via: "migrateLegacyIntegrationKeys on root path"
      pattern: "migrateLegacyIntegrationKeys\\(planningPaths\\(projectDir\\)\\.config\\)"
---

<objective>
Close the two SDK-side coherence gaps: (a) HARD-01 — the Brave rung in `websearch.ts` reads only the `BRAVE_API_KEY` env var and never the Phase-14 keyfile, so a keyfile-only Brave user silently loses the middle rung of the fallback ladder; (b) FRESH-CR-03 (STATE.md Phase 16 pre-task) — workstream `secret-status` bypasses inherited root flags and root-layer legacy migration: it migrates only the workstream-selected config path and computes `enabled` from a single-file read instead of the effective root+workstream merge.

Purpose: "Zero user-facing errors via the fallback ladder" requires every SDK rung to be coherent with the Phase-14 keyfile story, and the status surface the settings workflow trusts must match the effective loader. One plan owns both because both live in `sdk/src/query/` and the phase precedent mandates a single `sdk/dist` rebuild per plan-wave.

Output: keyfile-aware websearch, root-aware secretStatus, two vitest files, single dist rebuild.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.oto/phases/16-agent-guidance-hardening/16-RESEARCH.md

Verified current state:
- `sdk/src/query/websearch.ts:26-30`: `const apiKey = process.env.BRAVE_API_KEY; if (!apiKey) return { data: { available: false, reason: 'BRAVE_API_KEY not set' } };` — never reads `~/.oto/brave_api_key`.
- `sdk/src/query/secrets.ts` exports: `readKeyfile(slug, baseDir?)` → `KeyfileValue | null` (null for empty/whitespace/dangling — D-15 rules), `detectKeySource(slug, baseDir?)` → `{ source: 'env'|'keyfile'|null, masked, ... }` (masked only, no raw value).
- `sdk/src/query/secret-commands.ts:285-335` `secretStatus`: migrates ONLY `planningPaths(projectDir, workstream).config`; computes `enabled` from `readConfig(projectDir, workstream)` — a raw single-file JSON.parse with no root merge (lines 271-282).
- `sdk/src/config.ts:192` `loadConfig(projectDir, workstream)`: performs the root+workstream deep-merge (root under workstream) with both-layer scrub — the WR-09-fixed effective view secretStatus must adopt.
- `sdk/src/query/helpers.ts:428` `planningPaths(projectDir, workstream?)`: `planningPaths(projectDir)` (no workstream) yields the ROOT config path.
- SDK test framework: vitest (`cd sdk && npm test` = `vitest run`). Node `os.homedir()` on POSIX honors `process.env.HOME` — tests redirect `~/.oto` via a tmp HOME. `sdk/src/query/secrets.test.ts` and `secrets-empty-keyfile.test.ts` show existing fixture conventions.
- Build precedent (Phase 14-19 / 15-12): single `sdk/dist` rebuild per plan, `cd sdk && npm run build` (tsc), then focused vitest + full `vitest run`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brave keyfile rung in websearch.ts</name>
  <files>sdk/src/query/websearch.ts, sdk/src/query/websearch.test.ts</files>
  <read_first>
    - sdk/src/query/websearch.ts (entire file — 82 lines)
    - sdk/src/query/secrets.ts (lines 80-180 — readKeyfile signature, KeyfileValue shape, D-15 empty/whitespace/symlink rules)
    - sdk/src/query/secrets-empty-keyfile.test.ts (fixture conventions: tmp HOME, vi environment handling)
  </read_first>
  <behavior>
    - Test 1: no BRAVE_API_KEY env, no keyfile → `{ data: { available: false, reason: <mentions both env var and keyfile path> } }` — never throws
    - Test 2: no env, `$HOME/.oto/brave_api_key` containing 'kf-test-key-0123456789' (mode 0600) → fetch is called with header `X-Subscription-Token: kf-test-key-0123456789` (stub global.fetch; return ok:true with empty results) and result has available:true
    - Test 3: no env, EMPTY keyfile → available:false (readKeyfile D-15 rules apply)
    - Test 4: env BRAVE_API_KEY='env-key-0123456789' AND keyfile both present → fetch called with the ENV value (env-first precedence, matching detectKeySource ordering)
  </behavior>
  <action>
    Write `sdk/src/query/websearch.test.ts` (vitest: `describe/it/expect`, `vi.stubGlobal('fetch', ...)`, tmp HOME via `process.env.HOME` swap in beforeEach/afterEach, and `vi.stubEnv`/delete for BRAVE_API_KEY). Run `cd sdk && npx vitest run src/query/websearch.test.ts` — Tests 2/3 RED. Commit `test(16-03): add failing Brave keyfile-rung websearch coverage`.

    Then edit `sdk/src/query/websearch.ts`: replace lines 26-30 with:

    ```ts
    // oto: HARD-01 — Brave rung must honor the Phase-14 keyfile story, env-first (D-15 ordering)
    const envKey = process.env.BRAVE_API_KEY;
    const apiKey =
      typeof envKey === 'string' && envKey.trim() !== ''
        ? envKey
        : readKeyfile('brave')?.value;

    if (!apiKey) {
      return { data: { available: false, reason: 'No Brave key: set BRAVE_API_KEY or ~/.oto/brave_api_key' } };
    }
    ```

    with `import { readKeyfile } from './secrets.js';` added at the top (ESM `.js` specifier — SDK convention). Update the file's doc comment (lines 4-6 and 20-23) to mention the keyfile fallback. Do not change any other behavior (params, fetch call, error shapes). Run the focused vitest file green. Commit `feat(16-03): resolve Brave key from keyfile when env unset (HARD-01)`.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/query/websearch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readKeyfile('brave')" sdk/src/query/websearch.ts` == 1
    - `grep -c "oto: HARD-01" sdk/src/query/websearch.ts` == 1
    - `grep -c "from './secrets.js'" sdk/src/query/websearch.ts` == 1
    - `cd sdk && npx vitest run src/query/websearch.test.ts` exits 0 with >= 4 passing tests
  </acceptance_criteria>
  <done>Keyfile-only Brave users reach the Brave rung; env-first precedence preserved; no-key case degrades to structured {available:false}</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FRESH-CR-03 — root-aware secretStatus + single sdk/dist rebuild</name>
  <files>sdk/src/query/secret-commands.ts, sdk/src/query/secret-status-inheritance.test.ts, sdk/dist/**</files>
  <read_first>
    - sdk/src/query/secret-commands.ts (lines 1-40 imports, 250-336 — readConfig helper + secretStatus)
    - sdk/src/config.ts (lines 180-260 — loadConfig root+workstream merge semantics, GSDConfig shape)
    - sdk/src/query/helpers.ts (lines 420-450 — planningPaths root vs workstream config path resolution)
    - sdk/src/query/secrets.ts (migrateLegacyIntegrationKeys signature and behavior)
    - .oto/phases/14-key-storage-reconciliation/14-DISPOSITIONS.md (FRESH-CR-03 row — the exact contract this closes)
  </read_first>
  <behavior>
    - Test 1 (root-true inheritance): fixture project with ROOT `.oto/config.json` = `{"exa_search": true}`, a workstream whose config lacks exa_search → `secretStatus([], projectDir, '<ws>')` entry for exa has enabled:true (pre-fix: false)
    - Test 2 (workstream override): root `{"exa_search": true}`, workstream config `{"exa_search": false}` → enabled:false (workstream wins, matching loadConfig merge order)
    - Test 3 (root-legacy migration): root config contains `{"exa_search": "sk-legacy-0123456789abcdef"}`; run `secretStatus([], projectDir, '<ws>')` → afterwards the ROOT config file contains `"exa_search": true` (boolean), `$HOME/.oto/exa_api_key` exists containing the migrated key, and NEITHER the structured result NOR raw lines contain the substring 'sk-legacy' (masked-only)
    - Test 4 (no-workstream path unchanged): `secretStatus([], projectDir)` with root-only config behaves as today (regression guard)
  </behavior>
  <action>
    Write `sdk/src/query/secret-status-inheritance.test.ts` per <behavior>, using tmp project + tmp HOME fixtures (workstream config location: derive from `planningPaths(projectDir, ws).config` — create the directory structure it points at). Run focused — Tests 1/3 RED. Commit `test(16-03): add failing secret-status inheritance reproductions (FRESH-CR-03)`.

    Then edit `secretStatus` in `sdk/src/query/secret-commands.ts`:

    1. **Root-layer migration:** replace the single migration line (293) with a block that always migrates the ROOT config first, then the workstream config when one is selected:

    ```ts
    // oto: FRESH-CR-03 — status must heal and reflect the ROOT layer, not just the selected workstream
    try { await migrateLegacyIntegrationKeys(planningPaths(projectDir).config); } catch { /* masked display still renders */ }
    if (workstream) {
      try { await migrateLegacyIntegrationKeys(planningPaths(projectDir, workstream).config); } catch { /* masked display still renders */ }
    }
    ```

    2. **Effective flags:** replace `const config = readConfig(projectDir, workstream);` with `const config = await loadConfig(projectDir, workstream) as Record<string, unknown>;` (import `loadConfig` from `../config.js`). loadConfig performs the WR-09 root-under-workstream deep-merge with both-layer scrub, so `enabled = config[integration.configKey] === true` becomes the effective view. If `readConfig` has no remaining callers after this change, delete it; otherwise leave it.

    3. Preserve everything else byte-for-byte where possible (masked-only construction, raw line formats) — the settings workflow parses these lines verbatim (Phase 15 decision).

    Run focused vitest green, then the rebuild gate — in `sdk/`: `npm run build` (single dist rebuild for this plan), `npx vitest run` (full SDK suite), and from repo root `npm test` (CJS suite unaffected but confirms no cross-layer breakage). Commit `feat(16-03): effective root-to-workstream secret-status + root legacy migration (FRESH-CR-03)`.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/query/secret-status-inheritance.test.ts && npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "oto: FRESH-CR-03" sdk/src/query/secret-commands.ts` == 1
    - `grep -c "migrateLegacyIntegrationKeys(planningPaths(projectDir).config)" sdk/src/query/secret-commands.ts` == 1
    - `grep -c "loadConfig(projectDir, workstream)" sdk/src/query/secret-commands.ts` >= 1
    - `cd sdk && npx vitest run src/query/secret-status-inheritance.test.ts` exits 0 with >= 4 passing tests
    - `cd sdk && npx vitest run` exits 0 (full SDK suite — includes existing secrets/config-loader-parity suites)
    - `sdk/dist/query/secret-commands.js` mtime newer than `sdk/src/query/secret-commands.ts` (rebuild happened)
    - `git status --porcelain sdk/dist` shows rebuilt artifacts staged/committed with the source change
  </acceptance_criteria>
  <done>Workstream secret-status shows effective inherited flags; root legacy strings heal through the workstream status path; masked-only output preserved; dist rebuilt once</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.oto keyfiles → SDK process memory | Raw key bytes enter memory for the fetch header / migration only |
| secretStatus → workflow/agent transcript | Status output is rendered into chat transcripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Information Disclosure | secretStatus output leaking raw key after loadConfig adoption | mitigate | Masked-only construction preserved (D-10); Test 3 asserts the raw secret substring appears nowhere in structured data or raw lines; loadConfig's scrub already booleanizes non-boolean integration values on the effective view |
| T-16-03-02 | Information Disclosure | websearch sending the keyfile value anywhere but api.search.brave.com | mitigate | Key used solely in the existing X-Subscription-Token header of the existing fetch call; no logging added; error paths return status codes, never the key |
| T-16-03-03 | Tampering | Root config migration corrupting user config on partial failure | mitigate | Reuses `migrateLegacyIntegrationKeys` (Phase 14 lock-protected, compensating-rollback transaction — WR-01/CR-03 hardened); wrapped in try/catch fail-open exactly like the existing call |
| T-16-03-04 | Denial of Service | websearch throwing instead of degrading when keyfile unreadable | mitigate | readKeyfile returns null on any unusable file (D-15); guard returns structured {available:false} |
</threat_model>

<verification>
- `cd sdk && npx vitest run` — full SDK suite green
- `cd sdk && npm run build` — tsc clean, dist committed
- `npm test` (repo root) — CJS suite green
</verification>

<success_criteria>
- Brave rung works from keyfile alone; ladder degrades structurally with no key anywhere
- FRESH-CR-03 reproductions from 14-VERIFICATION.md are captured as passing regressions
- Single dist rebuild; masked-only status contract intact
</success_criteria>

<output>
After completion, create `.oto/phases/16-agent-guidance-hardening/16-03-SUMMARY.md`
</output>
