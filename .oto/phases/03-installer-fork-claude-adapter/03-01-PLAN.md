---
phase: 03
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-03-args.test.cjs
  - tests/phase-03-runtime-detect.test.cjs
  - tests/phase-03-marker.test.cjs
  - tests/phase-03-install-state.test.cjs
  - tests/phase-03-copy-files.test.cjs
  - tests/phase-03-runtime-claude.test.cjs
  - tests/phase-03-runtime-codex.test.cjs
  - tests/phase-03-runtime-gemini.test.cjs
  - tests/phase-03-no-unwanted-runtimes.test.cjs
  - tests/phase-03-no-runtime-conditionals.test.cjs
  - tests/phase-03-bin-shell.test.cjs
  - tests/phase-03-help-output.test.cjs
  - tests/phase-03-install-claude.integration.test.cjs
  - tests/phase-03-install-codex.integration.test.cjs
  - tests/phase-03-install-gemini.integration.test.cjs
  - tests/phase-03-install-all.integration.test.cjs
autonomous: true
requirements: [INS-01, INS-02, INS-03, INS-04, INS-05, INS-06]

must_haves:
  truths:
    - "All 16 phase-03 test files exist on disk and are discovered by node --test"
    - "Every test file imports node:test and node:assert/strict and contains at least one t.todo() or skipped test"
    - "Running node --test tests/phase-03-*.test.cjs exits 0 (todos do not fail)"
  artifacts:
    - path: "tests/phase-03-args.test.cjs"
      provides: "INS-03 + INS-06 arg validation skeleton"
    - path: "tests/phase-03-runtime-detect.test.cjs"
      provides: "INS-06 --all detection skeleton"
    - path: "tests/phase-03-marker.test.cjs"
      provides: "Marker inject/remove/replace skeleton"
    - path: "tests/phase-03-install-state.test.cjs"
      provides: "State schema validation skeleton"
    - path: "tests/phase-03-copy-files.test.cjs"
      provides: "INS-04 copyTree/removeTree/sha256File/walkTree unit-level skeleton"
    - path: "tests/phase-03-runtime-claude.test.cjs"
      provides: "Claude adapter contract skeleton"
    - path: "tests/phase-03-runtime-codex.test.cjs"
      provides: "Codex adapter contract skeleton"
    - path: "tests/phase-03-runtime-gemini.test.cjs"
      provides: "Gemini adapter contract skeleton"
    - path: "tests/phase-03-no-unwanted-runtimes.test.cjs"
      provides: "INS-01 / SC#4 grep-based exclusion skeleton"
    - path: "tests/phase-03-no-runtime-conditionals.test.cjs"
      provides: "INS-02 / SC#2 grep-based exclusion skeleton"
    - path: "tests/phase-03-bin-shell.test.cjs"
      provides: "INS-01 thin-shell shape skeleton"
    - path: "tests/phase-03-help-output.test.cjs"
      provides: "INS-01 / D-15 help output skeleton"
    - path: "tests/phase-03-install-claude.integration.test.cjs"
      provides: "Full install/re-install/uninstall round-trip skeleton"
    - path: "tests/phase-03-install-codex.integration.test.cjs"
      provides: "Codex best-effort install skeleton"
    - path: "tests/phase-03-install-gemini.integration.test.cjs"
      provides: "Gemini best-effort install skeleton"
    - path: "tests/phase-03-install-all.integration.test.cjs"
      provides: "INS-06 --all multi-runtime install skeleton"
  key_links:
    - from: "tests/phase-03-*.test.cjs"
      to: "node --test runner"
      via: "discovery via tests/*.test.cjs glob in npm test"
      pattern: "node:test"
---

<objective>
Create all 16 Wave 0 test scaffolds before any production code. Each file must compile, import the standard test framework, declare its requirement coverage in a top-of-file comment, and register `t.todo()` placeholders for every behavior listed in 03-VALIDATION.md. Wave 0 must commit cleanly before Wave 1 begins; this is the Nyquist contract for Phase 3.

Purpose: Lock the test surface BEFORE production code (D-21). Production tasks in Waves 1–4 fill in the assertions; they MUST NOT add new test files outside this list.
Output: 16 `.test.cjs` files in `tests/` that run green (all `t.todo()`) when invoked via `node --test tests/phase-03-*.test.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer-fork-claude-adapter/03-CONTEXT.md
@.planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md
@CLAUDE.md
@tests/phase-02-allowlist.test.cjs
@tests/phase-02-roundtrip.test.cjs

<interfaces>
<!-- Established Phase 1+2 test conventions executors must replicate -->

Standard test header (CJS, node:test, strict assert):
```javascript
'use strict';
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');
```

Tmpdir helper pattern (port from tests/phase-02-allowlist.test.cjs:16-24):
```javascript
function tmpDir(t) {
  const d = fs.mkdtempSync(path.join(os.tmpdir(), 'oto-install-test-'));
  t.after(() => fs.rmSync(d, { recursive: true, force: true }));
  return d;
}
```

Todo skeleton:
```javascript
test('INS-XX: behavior description', { todo: 'Wave N — fills assertions' }, () => {});
```

Coverage block (REQUIRED at top of every Wave 0 file, after requires):
```javascript
// Phase 3 Wave 0 scaffold.
// Covers: INS-XX (behavior summary)
// Filled by: 03-NN-PLAN.md (Wave N)
// Sources: 03-VALIDATION.md per-task verification map
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 9 unit-level test scaffolds</name>
  <files>tests/phase-03-args.test.cjs, tests/phase-03-runtime-detect.test.cjs, tests/phase-03-marker.test.cjs, tests/phase-03-install-state.test.cjs, tests/phase-03-copy-files.test.cjs, tests/phase-03-runtime-claude.test.cjs, tests/phase-03-runtime-codex.test.cjs, tests/phase-03-runtime-gemini.test.cjs, tests/phase-03-no-unwanted-runtimes.test.cjs</files>
  <read_first>
    - tests/phase-02-allowlist.test.cjs (header pattern + tmpDir helper at lines 16-24)
    - tests/phase-02-roundtrip.test.cjs (multi-test file structure)
    - .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md (per-test behaviors lines 41-62 and Wave 0 list lines 70-86)
    - .planning/phases/03-installer-fork-claude-adapter/03-CONTEXT.md (D-21, D-22 test strategy lines 195-204)
    - CLAUDE.md (zero-dep + node:test prescription)
  </read_first>
  <action>
Create nine `.test.cjs` files in `tests/`. Each MUST:

1. Begin with `'use strict';` then standard requires: `const { test } = require('node:test'); const assert = require('node:assert/strict');` plus `fs/os/path` only if the file's behaviors need them per the list below.
2. Include the exact 4-line coverage comment block (see `<interfaces>`) immediately after requires, citing the relevant INS-XX IDs and the future plan that fills it.
3. Register `t.todo()` placeholder tests per the behavior list below — one `test(...)` call per row, with the exact name pattern `'INS-XX: <behavior copied from VALIDATION.md>'`. Use `{ todo: 'Wave N — fills assertions' }` as the second arg so the test reports as TODO, not as failing.
4. NOT import any `bin/lib/*.cjs` module yet (those don't exist). The scaffolds only declare intent; Wave 1+ tasks add the imports and assertions.

Per-file todo lists (copy these names verbatim — VALIDATION.md is the source of truth):

`tests/phase-03-args.test.cjs` — fills in Wave 1 (Plan 02):
- `'INS-03: --config-dir flag wins over env var and default'`
- `'INS-03: env var wins over default when no flag'`
- `'INS-03: default = ~/.<runtime> when no flag and no env'`
- `'INS-03: expandTilde resolves ~/foo against os.homedir()'`
- `'INS-06: --all + --config-dir rejected at parse time (exit 3)'`
- `'INS-06: --all + --claude rejected (mutual exclusion)'`
- `'INS-06: parseCliArgs returns runtimes array for --claude --codex combination'`

`tests/phase-03-runtime-detect.test.cjs` — fills in Wave 1 (Plan 02):
- `'INS-06: --all detects present runtimes via dir existence'`
- `'INS-06: detectPresentRuntimes returns [] when no runtimes installed'`
- `'INS-06: detectPresentRuntimes only returns claude/codex/gemini (no other names)'`

`tests/phase-03-marker.test.cjs` — fills in Wave 1 (Plan 03):
- `'INS-04: injectMarkerBlock creates file when missing with single trailing newline'`
- `'INS-04: injectMarkerBlock replaces only content between markers when present'`
- `'INS-04: injectMarkerBlock appends block when markers absent'`
- `'INS-04: injectMarkerBlock is idempotent — second call produces byte-identical file'`
- `'INS-04: removeMarkerBlock strips block + leaves rest of file intact'`
- `'INS-04: findUpstreamMarkers returns [] for clean file'`
- `'INS-04: findUpstreamMarkers detects "<!-- GSD Configuration" and Superpowers identity'`

`tests/phase-03-install-state.test.cjs` — fills in Wave 1 (Plan 03):
- `'INS-04: writeState round-trips through readState (deep equal)'`
- `'INS-04: validateState rejects unsupported version'`
- `'INS-04: validateState rejects absolute paths in files[].path'`
- `'INS-04: validateState rejects non-64-hex sha256'`
- `'INS-04: validateState rejects unknown runtime name'`

`tests/phase-03-copy-files.test.cjs` — fills in Wave 1 (Plan 04):
- `'INS-04: copyTree returns 0 filesCopied when src absent'`
- `'INS-04: copyTree copies tree of files into dst recursively'`
- `'INS-04: copyTree-written files are not symlinks (lstat().isSymbolicLink() === false)'`
- `'INS-04: copyTree rejects source tree containing a symlink'`
- `'INS-04: sha256File returns deterministic 64-hex digest'`
- `'INS-04: walkTree returns [] for absent root'`
- `'INS-04: removeTree is no-op for absent path'`

`tests/phase-03-runtime-claude.test.cjs` — fills in Wave 2 (Plan 05):
- `'INS-02: adapter exports name === "claude"'`
- `'INS-02: adapter descriptor has configDirEnvVar === "CLAUDE_CONFIG_DIR"'`
- `'INS-02: adapter descriptor has defaultConfigDirSegment === ".claude"'`
- `'INS-02: adapter descriptor has instructionFilename === "CLAUDE.md"'`
- `'INS-02: adapter descriptor has settingsFilename === "settings.json" and settingsFormat === "json"'`
- `'INS-02: adapter descriptor has sourceDirs and targetSubdirs for commands/agents/skills/hooks'`
- `'INS-02: adapter exports lifecycle hooks renderInstructionBlock/transformCommand/transformAgent/transformSkill/mergeSettings/onPreInstall/onPostInstall'`
- `'INS-02: transformCommand/Agent/Skill are identity for Claude (canonical)'`
- `'INS-02: mergeSettings is identity at Phase 3 (no-op)'`
- `'INS-02: renderInstructionBlock output is stable for fixed oto_version (snapshot)'`

`tests/phase-03-runtime-codex.test.cjs` — fills in Wave 2 (Plan 05):
- `'INS-02: adapter exports name === "codex"'`
- `'INS-02: adapter descriptor has configDirEnvVar === "CODEX_HOME"'`
- `'INS-02: adapter descriptor has defaultConfigDirSegment === ".codex"'`
- `'INS-02: adapter descriptor has instructionFilename === "AGENTS.md"'`
- `'INS-02: adapter descriptor has settingsFilename === "config.toml" and settingsFormat === "toml"'`
- `'INS-02: transformCommand/Agent/Skill are identity at Phase 3 (TODO Phase 8 parity comment present)'`
- `'INS-02: mergeSettings is identity at Phase 3 (TODO Phase 5 hooks comment present)'`
- `'INS-05: --codex labeled best-effort until Phase 8 — adapter has // TODO Phase 8 marker in source'`

`tests/phase-03-runtime-gemini.test.cjs` — fills in Wave 2 (Plan 05):
- `'INS-02: adapter exports name === "gemini"'`
- `'INS-02: adapter descriptor has configDirEnvVar === "GEMINI_CONFIG_DIR"'`
- `'INS-02: adapter descriptor has defaultConfigDirSegment === ".gemini"'`
- `'INS-02: adapter descriptor has instructionFilename === "GEMINI.md"'`
- `'INS-02: adapter descriptor has settingsFilename === "settings.json" and settingsFormat === "json"'`
- `'INS-02: transformCommand/Agent/Skill are identity at Phase 3 (TODO Phase 8 parity comment present)'`
- `'INS-05: --gemini labeled best-effort until Phase 8 — adapter has // TODO Phase 8 marker in source'`

`tests/phase-03-no-unwanted-runtimes.test.cjs` — fills in Wave 4 (Plan 07):
- `'INS-01 / SC#4: grep -E "(--opencode|--kilo|--cursor|--windsurf|--antigravity|--augment|--trae|--qwen|--codebuddy|--cline|--copilot)" returns 0 hits in bin/ and scripts/install*.cjs (excluding foundation-frameworks/, tests/, decisions/, license files, this test file)'`
- `'INS-01: bare-name scan ("opencode", "kilocode", etc.) limited to bin/ and scripts/install*.cjs returns 0 hits'`
  </action>
  <verify>
    <automated>node --test tests/phase-03-args.test.cjs tests/phase-03-runtime-detect.test.cjs tests/phase-03-marker.test.cjs tests/phase-03-install-state.test.cjs tests/phase-03-copy-files.test.cjs tests/phase-03-runtime-claude.test.cjs tests/phase-03-runtime-codex.test.cjs tests/phase-03-runtime-gemini.test.cjs tests/phase-03-no-unwanted-runtimes.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files exist under tests/ (verify with `ls tests/phase-03-*.test.cjs | wc -l` ≥ 9)
    - Each file begins with `'use strict';` (grep first line)
    - Each file requires `node:test` and `node:assert/strict` (grep `require('node:test')` returns 9 hits)
    - Each file contains a `// Phase 3 Wave 0 scaffold.` line
    - `node --test tests/phase-03-args.test.cjs ... (9 files)` exits 0
    - Test runner output reports todo count > 0 (greppable: `# todo`)
    - Total `t.todo()` placeholders = 56 (per-file: 7+3+7+5+7+10+8+7+2)
    - No file imports `bin/lib/*.cjs` (those modules do not exist yet — `grep -l "require.*bin/lib" tests/phase-03-*.test.cjs` returns 0 hits)
  </acceptance_criteria>
  <done>
    All 9 unit test scaffolds exist, run green via `node --test`, and report all behaviors as TODO placeholders. No production-code imports.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 7 grep, shell, help, and integration test scaffolds</name>
  <files>tests/phase-03-no-runtime-conditionals.test.cjs, tests/phase-03-bin-shell.test.cjs, tests/phase-03-help-output.test.cjs, tests/phase-03-install-claude.integration.test.cjs, tests/phase-03-install-codex.integration.test.cjs, tests/phase-03-install-gemini.integration.test.cjs, tests/phase-03-install-all.integration.test.cjs</files>
  <read_first>
    - tests/phase-02-allowlist.test.cjs (lines 16-24 for `tmpDir` helper to copy)
    - tests/phase-02-bin-stub.test.cjs (the closest analogue to phase-03-bin-shell.test.cjs — shell-shape verification)
    - .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md (per-test behaviors and Wave 0 list)
    - .planning/phases/03-installer-fork-claude-adapter/03-RESEARCH.md (Pitfall A, lines 433-447 — substring collision rules for grep tests)
    - .planning/phases/03-installer-fork-claude-adapter/03-CONTEXT.md (D-15 help text content lines 169-178; D-16 exit codes line 178; D-21 integration test contract lines 198-204)
  </read_first>
  <action>
Create seven `.test.cjs` files in `tests/`. Same conventions as Task 1: `'use strict';`, `node:test` + `node:assert/strict` requires, coverage comment block, then `t.todo()` registrations.

Per-file todo lists (copy verbatim — these come from VALIDATION.md + CONTEXT.md):

`tests/phase-03-no-runtime-conditionals.test.cjs` — fills in Wave 4 (Plan 07):
- `'INS-02 / SC#2: orchestrator (bin/lib/install.cjs) has zero "if (runtime ===" branches'`
- `'INS-02 / SC#2: bin/install.js has zero "if (runtime ===" branches'`
- `'INS-02 / SC#2: bin/lib/args.cjs has zero "if (runtime ===" branches'`
- `'INS-02 / SC#2: scan covers bin/lib/ EXCLUDING bin/lib/runtime-*.cjs (those are the legitimate adapter homes)'`

`tests/phase-03-bin-shell.test.cjs` — fills in Wave 4 (Plan 07):
- `'INS-01: bin/install.js starts with #!/usr/bin/env node shebang'`
- `'INS-01: bin/install.js declares use strict on second line'`
- `'INS-01: bin/install.js line count <= 200 (thin shell per D-14)'`
- `'INS-01: bin/install.js requires bin/lib/args.cjs and bin/lib/install.cjs (no business logic inline)'`
- `'INS-01: bin/install.js exits 1 with stderr message when Node major < 22'`
- `'INS-01: bin/install.js retains executable bit (mode includes 0o111) — Pitfall #2453 / D-20'`

`tests/phase-03-help-output.test.cjs` — fills in Wave 4 (Plan 07):
- `'INS-01 / D-15: oto install --help mentions --claude, --codex, --gemini, --all, --config-dir'`
- `'INS-01 / D-15: oto install --help documents resolution order: --config-dir > <RUNTIME>_CONFIG_DIR env > ~/.<runtime>/'`
- `'INS-01 / D-15: oto install --help labels --codex and --gemini as "best-effort until Phase 8"'`
- `'INS-01 / D-15: oto install --help fits 30 lines / 80 columns (no wrap)'`
- `'INS-01 / D-15: oto install --help exits 0 to stdout (not stderr)'`
- `'INS-01 / D-15: help mentions "https://github.com/OTOJulian/oto-hybrid-framework" and oto version'`
- `'INS-01: help does NOT mention any of opencode/kilo/cursor/windsurf/antigravity/augment/trae/qwen/codebuddy/cline/copilot'`

`tests/phase-03-install-claude.integration.test.cjs` — fills in Wave 3 (Plan 06) + Wave 4 (Plan 07):
- `'INS-04 / INS-05: install --claude into tmpdir creates <configDir>/oto/.install.json'`
- `'INS-04: install --claude injects <!-- OTO Configuration --> ... <!-- /OTO Configuration --> block into CLAUDE.md'`
- `'INS-04: install preserves pre-existing user content in CLAUDE.md byte-for-byte outside marker block'`
- `'INS-04: re-install over existing install produces byte-identical state.files[] (modulo installed_at)'`
- `'INS-04: re-install does NOT duplicate marker block (exactly one open marker present)'`
- `'INS-04: uninstall --claude removes <configDir>/oto/ subdir entirely'`
- `'INS-04: uninstall restores CLAUDE.md to pre-install user content (block stripped, surrounding text intact)'`
- `'INS-04: copied files are regular files, not symlinks (lstat().isSymbolicLink() === false)'`
- `'INS-05: success message matches /^installed: claude — \\d+ files copied, marker injected, state at .+\\.install\\.json$/'`

`tests/phase-03-install-codex.integration.test.cjs` — fills in Wave 3 (Plan 06):
- `'INS-05: install --codex into tmpdir creates <configDir>/oto/.install.json with runtime === "codex"'`
- `'INS-05: install --codex injects marker block into AGENTS.md (not CLAUDE.md)'`
- `'INS-05: mergeSettings is identity — pre-existing config.toml is byte-identical after install'`
- `'INS-05: uninstall --codex round-trips clean (state file gone, AGENTS.md restored)'`

`tests/phase-03-install-gemini.integration.test.cjs` — fills in Wave 3 (Plan 06):
- `'INS-05: install --gemini into tmpdir creates <configDir>/oto/.install.json with runtime === "gemini"'`
- `'INS-05: install --gemini injects marker block into GEMINI.md'`
- `'INS-05: mergeSettings is identity — pre-existing settings.json is byte-identical after install'`
- `'INS-05: uninstall --gemini round-trips clean'`

`tests/phase-03-install-all.integration.test.cjs` — fills in Wave 3 (Plan 06):
- `'INS-06: --all with all 3 fake runtime dirs present installs to all 3'`
- `'INS-06: --all skips absent runtime dirs (only installs to ones whose ~/.<runtime>/ exists)'`
- `'INS-06: --all with no present runtimes exits 4 with stderr message containing "no runtimes detected"'`
- `'INS-06: --all + --config-dir rejected at parse time (exit 3, stderr contains "--config-dir cannot be used with --all")'`
  </action>
  <verify>
    <automated>node --test tests/phase-03-no-runtime-conditionals.test.cjs tests/phase-03-bin-shell.test.cjs tests/phase-03-help-output.test.cjs tests/phase-03-install-claude.integration.test.cjs tests/phase-03-install-codex.integration.test.cjs tests/phase-03-install-gemini.integration.test.cjs tests/phase-03-install-all.integration.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist under tests/
    - `ls tests/phase-03-*.test.cjs | wc -l` returns 16 (combined Task 1 + Task 2)
    - All files start with `'use strict';`
    - `node --test tests/phase-03-*.test.cjs` exits 0
    - All tests in these 7 files report as TODO (no `# fail`, no `# pass` of real assertions yet)
    - `grep -l "require.*bin/lib" tests/phase-03-*.test.cjs` returns 0 hits (Wave 0 has no production-code imports)
  </acceptance_criteria>
  <done>
    All 7 remaining Wave 0 test scaffolds exist, run green as TODO placeholders, and the entire `node --test tests/phase-03-*.test.cjs` glob is green with 16 files discovered.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Set Wave 0 frontmatter flag and final verification</name>
  <files>.planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md (frontmatter at lines 1-8 — fields `nyquist_compliant` and `wave_0_complete`)
  </read_first>
  <action>
Update VALIDATION.md frontmatter: change `nyquist_compliant: false` to `nyquist_compliant: true` and `wave_0_complete: false` to `wave_0_complete: true`. Also tick all 16 checkboxes in the "Wave 0 Requirements" section from `- [ ]` to `- [x]`. Do NOT change any other content. This commits the Wave 0 gate per D-21.
  </action>
  <verify>
    <automated>node --test tests/phase-03-*.test.cjs && grep -E '^(nyquist_compliant|wave_0_complete): true$' .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md | wc -l | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^nyquist_compliant: true$' .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md` returns 1 match
    - `grep '^wave_0_complete: true$' .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md` returns 1 match
    - All 16 lines in the Wave 0 Requirements section show `- [x]` (not `- [ ]`) — `grep -c '^- \[x\] \`tests/phase-03-' .planning/phases/03-installer-fork-claude-adapter/03-VALIDATION.md` returns ≥ 16
    - `node --test tests/phase-03-*.test.cjs` exits 0
  </acceptance_criteria>
  <done>
    VALIDATION.md flagged Wave 0 complete; full Phase 3 test glob green; Wave 1 unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→test files | Test scaffolds are dev-only; not shipped to users (tests/ excluded from package.json files allowlist). No external trust crossing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T (Tampering) | tests/phase-03-*.test.cjs | accept | Test scaffolds contain only `t.todo()` placeholders; no executable assertions touch real filesystem until Wave 1+. Tampering surface = source-code review at PR time (single dev project). |
| T-03-01-02 | I (Information disclosure) | test fixtures | accept | No tmpdir creation or fs.writeFile in Wave 0 scaffolds; tests are inert until production code lands. No PII, secrets, or paths leaked. |
</threat_model>

<verification>
- All 16 test files exist: `ls tests/phase-03-*.test.cjs | wc -l` returns 16
- All run green as TODO: `node --test tests/phase-03-*.test.cjs` exits 0
- No production-code imports: `grep -l "require.*bin/lib" tests/phase-03-*.test.cjs` returns 0 hits
- VALIDATION.md frontmatter shows `nyquist_compliant: true` and `wave_0_complete: true`
</verification>

<success_criteria>
- 16 Wave 0 test scaffolds in tests/ phase-03-*.test.cjs
- node --test glob exits 0
- VALIDATION.md frontmatter committed: nyquist_compliant=true, wave_0_complete=true
- Total t.todo() count across all 16 files matches the per-file lists in this plan (the production tasks in Waves 1–4 turn each todo into a real assertion)
</success_criteria>

<output>
After completion, create `.planning/phases/03-installer-fork-claude-adapter/03-01-SUMMARY.md` summarizing the test surface created (file list, total todo count, requirement coverage matrix).
</output>
</output>
