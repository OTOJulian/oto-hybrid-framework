---
milestone: v0.2.0
status: shipped
shipped: 2026-05-07
total_requirements: 32
complete: 32
note: |-
  REQUIREMENTS.md was absent for this post-v0.1.0 extension milestone.
  Requirement sets were sourced from ROADMAP.md (REQ-MIG-*) and CONTEXT.md
  decisions (D-*). This archive consolidates them retroactively from the
  Phase VERIFICATION.md traceability tables.
---

# Milestone v0.2.0 — Requirements (Archived)

## Source

This milestone shipped without a `.planning/REQUIREMENTS.md` file (deleted on v0.1.0 archive, never re-created). Requirements are reconstructed here from the canonical traceability sources used during execution:

- **REQ-MIG-01..10** — declared in `.planning/ROADMAP.md` Phase 1 entry and in each Phase 01 plan's frontmatter `requirements_satisfied` field.
- **D-01..D-22** — declared in `.planning/phases/02-build-oto-log-command-for-capturing-freeform-ad-hoc-work-ses/02-CONTEXT.md` as 22 locked decisions, treated as the requirement set for Phase 02 (no REQ-LOG-* IDs were minted).

Both phases' VERIFICATION.md files contain explicit traceability tables.

## Phase 01 — `/oto-migrate` (REQ-MIG-01..10)

| Status | ID | Description |
|--------|------|-------------|
| [x] | REQ-MIG-01 | `/oto-migrate` invokable as Claude command, `oto-tools migrate <args>`, and public `oto migrate`. |
| [x] | REQ-MIG-02 | Default `--dry-run`; only writes when `--apply` passed. Dry-run emits classified per-file report. |
| [x] | REQ-MIG-03 | Calls existing `scripts/rebrand/lib/engine.cjs` for in-file string transforms. New code limited to detection, scope-globbing, marker-block rewrite, frontmatter-key rewrite, backup/restore, conflict detection. |
| [x] | REQ-MIG-04 | Idempotent: re-running `--apply` on already-migrated tree is a zero-change diff and exits 0 with "nothing to do". |
| [x] | REQ-MIG-05 | Detects and refuses half-migrated state (both `<!-- GSD:* -->` and `<!-- OTO:* -->` markers in same file) unless `--force`. |
| [x] | REQ-MIG-06 | `.planning/` → `.oto/` directory rename is opt-in via `--rename-state-dir`. |
| [x] | REQ-MIG-07 | Timestamped backup of every modified file under `.oto-migrate-backup/<timestamp>/` (`--no-backup` opt-out). |
| [x] | REQ-MIG-08 | `--scope` flag: `planning` (default), `all`, `minimal`. |
| [x] | REQ-MIG-09 | Emits `.oto-migrate-report.{json,md}` mirroring `reports/rebrand-dryrun.{json,md}` shape. |
| [x] | REQ-MIG-10 | Does NOT touch user runtime config dirs (`~/.claude/`, `~/.codex/`, `~/.gemini/`); documented in command help text. |

## Phase 02 — `/oto-log` (D-01..D-22)

| Status | ID | Description |
|--------|------|-------------|
| [x] | D-01 | Hybrid capture model: fire-and-forget (`/oto-log <title>`) and bookmarked sessions (`/oto-log start` / `/oto-log end`). |
| [x] | D-02 | Body drafted from observable evidence (transcript + `git diff` + `git log` over bounded ref); graceful degradation when git unavailable. |
| [x] | D-03 | `--body` flag writes verbatim body through `log.main()` (skips drafting). |
| [x] | D-04 | Six-section body: Summary, What changed, What was discussed, Outcome, Files touched, Open questions. |
| [x] | D-05 | Evidence wrapped in `<DATA_START>`/`<DATA_END>` markers; injected marker literals escaped; diff capped at 8 KB. |
| [x] | D-06 | `routeSubcommand()` only treats reserved words (`start`, `end`, `list`, `show`, `promote`) as subcommands when first token. |
| [x] | D-07 | `deriveLogSlug()`: strip stop words, lowercase, keep first 4 meaningful words. |
| [x] | D-08 | Empty title returns "/oto-log requires a title" hint and non-zero exit. |
| [x] | D-09 | Same-minute duplicate writes get `-2`, `-3` collision suffixes. |
| [x] | D-10 | Log entries write to `.oto/logs/{YYYYMMDD-HHmm}-{slug}.md`. |
| [x] | D-11 | Frontmatter includes: date, title, slug, mode, phase, diff refs, files, questions, promoted. |
| [x] | D-12 | `.active-session.json` created on `start`, deleted on `end`, auto-ended on second `start`, gitignored. |
| [x] | D-13 | `/oto-progress` Recent Activity interleaves logs and summaries chronologically. |
| [x] | D-14 | Running `/oto-log` does not modify STATE.md. |
| [x] | D-15 | `/oto-resume-work` extracts newest log `## Summary` line as status hint. |
| [x] | D-16 | `/oto-resume-work` detects `.oto/logs/.active-session.json` and surfaces open-session hint. |
| [x] | D-17 | CLI subcommands shipped: `oneshot`, `start`, `end`, `list`, `show`, `promote`. |
| [x] | D-18 | Flags shipped: `--body`, `--phase`, `--since`. |
| [x] | D-19 | Entries are immutable; `edit` is not routed or advertised. |
| [x] | D-20 | `promote --to quick|todo` creates downstream artifact, guards re-promotion collisions, rejects `--to plan`. |
| [x] | D-21 | `--phase 02` writes phase frontmatter while keeping file under `.oto/logs/`. |
| [x] | D-22 | Surfaces shipped: `oto/commands/oto/log.md`, `oto-tools log`, public `oto log`, command index, runtime matrix, all three runtime adapter smoke. |

## Outcomes

| Bucket | Count | Notes |
|--------|-------|-------|
| Validated as built | 32 | All 32 requirements verified per Phase 01 and Phase 02 VERIFICATION.md. |
| Adjusted in flight | 1 | Phase 02 plan-03 dropped the literal `--to plan` rejection from the prose surface; the rejection remains in the library guard (D-20). Tracked in commit `f619b51`. |
| Dropped | 0 | — |

## Traceability Summary

| Phase | Requirements | Status |
|-------|--------------|--------|
| 01 (oto-migrate) | REQ-MIG-01..10 | 10/10 covered |
| 02 (oto-log) | D-01..D-22 | 22/22 covered |
| **Total** | **32** | **32 covered (100%)** |

---

_Audit:_ `.planning/v0.2.0-MILESTONE-AUDIT.md`
_Roadmap archive:_ `.planning/milestones/v0.2.0-ROADMAP.md`
