---
phase: quick-260713-in8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.codex/oto/workflows/execute-phase.md
  - ~/.codex/oto/workflows/plan-phase.md
  - ~/.codex/oto/references/model-calibration.md
  - ~/.codex/agents/oto-code-reviewer.md
  - ~/.codex/agents/oto-verifier.md
  - ~/.codex/agents/oto-code-reviewer.toml
  - ~/.codex/agents/oto-verifier.toml
autonomous: true
requirements: [QUICK-260713-IN8]

must_haves:
  truths:
    - "Codex execute-phase.md and plan-phase.md are byte-identical to the repo source-of-truth copies"
    - "model-calibration.md exists under ~/.codex/oto/references/ and is byte-identical to the repo copy"
    - "Both Codex review agents (.md) reference model-calibration.md via a ~/.codex path in required_reading"
    - "The 'go soft' failure-mode bullet lists are absent from both Codex agent .md files AND both .toml sidecars"
    - "No repo source file is modified — git status shows only .oto/ tracking files"
    - "~/.gemini skip is recorded (no oto install present)"
  artifacts:
    - path: "~/.codex/oto/references/model-calibration.md"
      provides: "Model calibration anchor for Codex review agents"
    - path: "~/.codex/oto/workflows/execute-phase.md"
      provides: "Bounded-convergence contract (DISPOSITIONS.md triage stop)"
    - path: "~/.codex/oto/workflows/plan-phase.md"
      provides: "ACCEPT/DEFER exclusion"
  key_links:
    - from: "~/.codex/agents/oto-code-reviewer.md"
      to: "~/.codex/oto/references/model-calibration.md"
      via: "required_reading block"
      pattern: "@~/.codex/oto/references/model-calibration\\.md"
    - from: "~/.codex/agents/oto-verifier.md"
      to: "~/.codex/oto/references/model-calibration.md"
      via: "required_reading block"
      pattern: "@~/.codex/oto/references/model-calibration\\.md"
---

<objective>
Propagate the review-machinery recalibration (quick task 260713-ffa, commit a9de198) from the repo to the Codex runtime root. The repo files are the source of truth, already correct and committed. This is propagation ONLY.

Purpose: The Codex runtime (~/.codex) still runs the pre-recalibration review agents (with the "go soft" bullet lists, without the model-calibration anchor) and pre-recalibration workflows. ~/.claude is already synced; Gemini has no oto install.

Output: Updated files under ~/.codex only, plus this quick task's .oto/ tracking artifacts.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
Repo working directory: /Users/Julian/ONE to ONE/Code/OTO Hybrid Framework/oto-hybrid-framework-main

Source-of-truth repo files (READ ONLY — never modify):
@oto/references/model-calibration.md
@oto/agents/oto-code-reviewer.md
@oto/agents/oto-verifier.md
@oto/workflows/execute-phase.md
@oto/workflows/plan-phase.md

**HARD PROHIBITION:** Do NOT modify ANY repo source file. Do NOT touch ~/.claude (already synced). All writes go to ~/.codex only. If a listed target file is missing or a write fails on permissions, STOP and report — do not work around it.

**Pre-verified ground truth (inspected 2026-07-13 during planning):**
- ~/.codex/oto/workflows/execute-phase.md and plan-phase.md exist (pre-recalibration versions).
- ~/.codex/oto/references/ exists but does NOT contain model-calibration.md.
- ~/.codex/agents/oto-code-reviewer.{md,toml} and oto-verifier.{md,toml} all exist.
- Both Codex agent .md files carry Codex-specific adaptations: quoted frontmatter without tools/color, a `<codex_agent_role>` block, `@~/.codex/...` reference paths, `.codex/skills/`, `.codexignore`. Per the task spec's conditional, the **edit-in-place branch applies** (do NOT overwrite them with repo copies).
- Both .toml sidecars embed the agent body in `developer_instructions` and CONTAIN the "go soft" bullet lists (reviewer.toml ~line 20, verifier.toml ~line 22) → the deletion applies to them too. Deletion ONLY — do not add required_reading lines to the .toml files.
- ~/.gemini has NO oto install (no ~/.gemini/oto/ directory, no oto agents) → Gemini steps are skipped; record this in the SUMMARY.
</context>

<interfaces>
Exact edit anchors, verified during planning:

**oto-code-reviewer — repo pattern to replicate (repo lines 18-21):** a `<required_reading>` block sits between `</role>` and `<adversarial_stance>`:
```
<required_reading>
@~/.claude/oto/references/model-calibration.md
</required_reading>
```
The Codex copy currently has `</role>` followed directly by `<adversarial_stance>`. Insert the same block between them, with the Codex path: `@~/.codex/oto/references/model-calibration.md`.

**oto-verifier — repo pattern to replicate (repo lines 35-37):** the existing `<required_reading>` block (which in the Codex copy currently lists `@~/.codex/oto/references/verification-overrides.md` and `@~/.codex/oto/references/gates.md`) gains a third line after gates.md: `@~/.codex/oto/references/model-calibration.md`.

**"Go soft" block to DELETE from ~/.codex/agents/oto-code-reviewer.md AND oto-code-reviewer.toml** (header + 5 bullets + trailing blank line, inside `<adversarial_stance>`, between the "FORCE stance" paragraph and "**Required finding classification:**"):
```
**Common failure modes — how code reviewers go soft:**
- Stopping at obvious surface issues (console.log, empty catch) and assuming the rest is sound
- Accepting plausible-looking logic without tracing through edge cases (nulls, empty collections, boundary values)
- Treating "code compiles" or "tests pass" as evidence of correctness
- Reading only the file under review without checking called functions for bugs they introduce
- Downgrading findings from BLOCKER to WARNING to avoid seeming harsh
```

**"Go soft" block to DELETE from ~/.codex/agents/oto-verifier.md AND oto-verifier.toml** (same position pattern):
```
**Common failure modes — how verifiers go soft:**
- Trusting SUMMARY.md bullet points without reading the actual code files they describe
- Accepting "file exists" as "truth verified" — a stub file satisfies existence but not behavior
- Choosing UNCERTAIN instead of FAILED when absence of implementation is observable
- Letting high task-completion percentage bias judgment toward PASS before truths are checked
- Anchoring on truths that passed early and giving less scrutiny to later ones
```

KEEP in all four files: the "FORCE stance" paragraph, the "Required finding classification" section, and (verifier) the "Do NOT trust SUMMARY.md" mindset text.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Straight-copy runtime-neutral files to Codex root</name>
  <files>~/.codex/oto/workflows/execute-phase.md, ~/.codex/oto/workflows/plan-phase.md, ~/.codex/oto/references/model-calibration.md</files>
  <action>
    From the repo working directory, copy three files (repo → Codex root). These are runtime-neutral markdown; the task spec mandates straight copies:
    1. `cp oto/workflows/execute-phase.md ~/.codex/oto/workflows/execute-phase.md`
    2. `cp oto/workflows/plan-phase.md ~/.codex/oto/workflows/plan-phase.md`
    3. `cp oto/references/model-calibration.md ~/.codex/oto/references/model-calibration.md` (directory already exists — do not recreate)
    Do NOT edit the repo sources. Do NOT copy anything else. If any cp fails (permissions, missing target dir), STOP and report.
  </action>
  <verify>
    <automated>diff -q oto/workflows/execute-phase.md ~/.codex/oto/workflows/execute-phase.md && diff -q oto/workflows/plan-phase.md ~/.codex/oto/workflows/plan-phase.md && diff -q oto/references/model-calibration.md ~/.codex/oto/references/model-calibration.md</automated>
  </verify>
  <done>All three diff -q comparisons exit 0 (byte-identical to repo source of truth).</done>
</task>

<task type="auto">
  <name>Task 2: Apply recalibration edits into Codex agent files and .toml sidecars</name>
  <files>~/.codex/agents/oto-code-reviewer.md, ~/.codex/agents/oto-verifier.md, ~/.codex/agents/oto-code-reviewer.toml, ~/.codex/agents/oto-verifier.toml</files>
  <action>
    The Codex agent .md files carry Codex-specific adaptations (verified during planning), so apply the two recalibration edits IN PLACE — do not overwrite with repo copies. Use the exact anchors from the &lt;interfaces&gt; block:

    A. `~/.codex/agents/oto-code-reviewer.md`:
       - Insert the `<required_reading>` block containing `@~/.codex/oto/references/model-calibration.md` between `</role>` and `<adversarial_stance>` (mirrors repo placement; MUST use the ~/.codex path, never ~/.claude).
       - Delete the "go soft" header + 5 bullets + trailing blank line from `<adversarial_stance>`.
    B. `~/.codex/agents/oto-verifier.md`:
       - Add `@~/.codex/oto/references/model-calibration.md` as a new line after `@~/.codex/oto/references/gates.md` inside the existing `<required_reading>` block.
       - Delete the verifier "go soft" header + 5 bullets + trailing blank line from `<adversarial_stance>`.
    C. `~/.codex/agents/oto-code-reviewer.toml` and `~/.codex/agents/oto-verifier.toml`:
       - Each embeds the agent body in `developer_instructions` and contains the same "go soft" block — delete it (header + 5 bullets + trailing blank line). Deletion ONLY: do NOT add required_reading lines to the .toml files, and do not change any other TOML keys (name, model, sandbox_mode, etc.).

    Preserve in all four files: FORCE stance paragraph, Required finding classification, and the verifier's don't-trust-SUMMARY.md instruction. Read each file before editing; use the Edit tool with exact string matches.
  </action>
  <verify>
    <automated>grep -q "model-calibration" ~/.codex/agents/oto-code-reviewer.md && grep -q "model-calibration" ~/.codex/agents/oto-verifier.md && ! grep -q "go soft" ~/.codex/agents/oto-code-reviewer.md ~/.codex/agents/oto-verifier.md ~/.codex/agents/oto-code-reviewer.toml ~/.codex/agents/oto-verifier.toml && ! grep -rq "claude/oto/references/model-calibration" ~/.codex/agents/</automated>
  </verify>
  <done>Both .md files reference model-calibration.md via a ~/.codex path; "go soft" absent from all four files; FORCE stance and finding classification sections intact; .toml files otherwise unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification suite and confirm scope containment</name>
  <files>(read-only verification — no files modified)</files>
  <action>
    Run the complete verification checklist from the task spec:
    1. `grep "model-calibration" ~/.codex/agents/oto-code-reviewer.md ~/.codex/agents/oto-verifier.md` → present in both.
    2. `grep "go soft" ~/.codex/agents/oto-code-reviewer.md ~/.codex/agents/oto-verifier.md ~/.codex/agents/oto-code-reviewer.toml ~/.codex/agents/oto-verifier.toml` → NO matches.
    3. `grep -c "DISPOSITIONS.md" ~/.codex/oto/workflows/execute-phase.md` → bounded-convergence contract (DISPOSITIONS.md triage stop) present; and grep for the ACCEPT/DEFER exclusion in ~/.codex/oto/workflows/plan-phase.md (`grep -iE "ACCEPT|DEFER" ~/.codex/oto/workflows/plan-phase.md`) → present.
    4. `diff -q` the three straight-copied files against repo sources → identical.
    5. `git status --porcelain` in the repo → confirm NO repo source files changed (oto/**, bin/**, etc.); only .oto/ tracking files from this quick task may appear.
    6. Confirm ~/.gemini has no oto install (`ls ~/.gemini/oto 2>/dev/null` returns nothing) and record "Gemini skipped — no oto install at ~/.gemini" in the SUMMARY.
    If ANY check fails, STOP, report the failure, and do not mark the task complete.
  </action>
  <verify>
    <automated>grep -l "model-calibration" ~/.codex/agents/oto-code-reviewer.md ~/.codex/agents/oto-verifier.md | wc -l | grep -q 2 && ! grep -q "go soft" ~/.codex/agents/oto-code-reviewer.md ~/.codex/agents/oto-verifier.md ~/.codex/agents/oto-code-reviewer.toml ~/.codex/agents/oto-verifier.toml && grep -q "DISPOSITIONS.md" ~/.codex/oto/workflows/execute-phase.md && test -z "$(git status --porcelain -- oto/ bin/ scripts/ hooks/ commands/ agents/ skills/)"</automated>
  </verify>
  <done>All six spec verification checks pass; git status shows zero changes to repo source files; Gemini skip is documented for the SUMMARY.</done>
</task>

</tasks>

<verification>
- Three straight-copied files under ~/.codex byte-match the repo (diff -q exit 0 each).
- `model-calibration` referenced (via ~/.codex path) in both Codex agent .md files; no ~/.claude path leakage anywhere under ~/.codex/agents/.
- `go soft` absent from all four Codex agent files (.md and .toml).
- Bounded-convergence contract present in ~/.codex/oto/workflows/execute-phase.md; ACCEPT/DEFER exclusion present in ~/.codex/oto/workflows/plan-phase.md.
- Repo `git status` clean of source-file changes (only .oto/ quick-task tracking files).
- Gemini skip noted in SUMMARY (no oto install at ~/.gemini).
</verification>

<success_criteria>
Codex runtime root carries the exact post-recalibration review machinery: identical workflow/reference payloads and correctly adapted agent edits, with zero repo source modifications, ~/.claude untouched, and the Gemini skip documented.
</success_criteria>

<output>
After completion, create `.oto/quick/260713-in8-propagate-review-machinery-recalibration/260713-in8-SUMMARY.md` documenting: files copied, edits applied (including .toml deletions), all verification results, and the Gemini skip rationale.
</output>
