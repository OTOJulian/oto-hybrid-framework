---
phase: 12-query-registry-workflow-consumption
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - sdk/src/golden/oto-query-smoke.integration.test.ts
  - sdk/src/golden/fixtures/oto-project/
  - oto/workflows/lib/sdk-require.md
  - tests/sdk-fallback-policy.test.cjs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SDK-03, SDK-05]
user_setup: []
must_haves:
  truths:
    - "An enumerate+fixture smoke test extracts every distinct `oto-sdk query <key>` token from oto/workflows + oto/commands, filters to tokens registry.has() accepts, dispatches each in-process against a fixture .oto/ project, and asserts structured output with zero .planning/ access."
    - "Read-only workflow query call sites carry the `2>/dev/null || echo <default>` degradation idiom; a static assertion proves the policy holds for the read-only tier (SDK-05)."
    - "Structural/stateful workflow query call sites are guarded by a hard-require check that emits ONE clear actionable error when oto-sdk is absent — not reimplemented in bash (D-05)."
    - "ROADMAP Phase 12 SC#4 and REQUIREMENTS SDK-05 text reflect the tiered model: read-only degrades to defaults; structural fails fast with a clear error (D-06)."
    - "The 9 CJS files referencing .planning are confirmed correct-by-design (route through planningRootName/planningDir or are intentional marker/migration/display literals) — no CJS logic change (D-04)."
  artifacts:
    - path: "sdk/src/golden/oto-query-smoke.integration.test.ts"
      provides: "Enumerate+fixture in-process smoke proving SDK-03 SC#1 (answers every key) + SC#2 (resolves .oto/, never .planning/)"
      contains: "createRegistry"
    - path: "sdk/src/golden/fixtures/oto-project/"
      provides: "Minimal .oto/ fixture tree (STATE.md, config.json, ROADMAP.md, REQUIREMENTS.md, one phases/NN-name/ dir); no .planning/"
      contains: ".oto"
    - path: "oto/workflows/lib/sdk-require.md"
      provides: "Documented hard-require guard idiom for the structural/stateful fallback tier (D-05)"
      contains: "command -v oto-sdk"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 SC#4 reconciled to the tiered fallback model"
      contains: "fail fast"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SDK-05 reconciled to the tiered fallback model"
      contains: "fail fast"
  key_links:
    - from: "sdk/src/golden/oto-query-smoke.integration.test.ts"
      to: "sdk/src/query/index.js createRegistry"
      via: "in-process dispatch against fixtureRoot (sidesteps stale dist + stale global oto-sdk)"
      pattern: "createRegistry\\(\\)"
    - from: "sdk/src/golden/oto-query-smoke.integration.test.ts"
      to: "sdk/src/golden/fixtures/oto-project/.oto"
      via: "fixtureRoot with no .planning/ — asserts resolution lands on .oto/"
      pattern: "\\.oto"
---

<objective>
Close the phase: (1) author the Wave 0 enumerate+fixture smoke harness proving SDK-03 SC#1/SC#2 in-process; (2) wire and statically assert the D-05 tiered fallback (read-only degrades, structural hard-requires) for SDK-05; (3) confirm the D-04 CJS audit (no rework); (4) reconcile ROADMAP SC#4 + REQUIREMENTS SDK-05 to the tiered model (D-06); then a human-verify checkpoint for the cross-binary parity that automation cannot fully self-certify.

Purpose: This is the goal-backward verification + the SDK-05 fallback policy + the mandated D-06 text reconciliation. The smoke harness dispatches IN-PROCESS via `createRegistry()` to sidestep both the stale committed-dist trap and the stale global `oto-sdk v0.1.0` on PATH (RESEARCH Runtime State Inventory). The fallback work is policy + targeted instrumentation + a static assertion — NOT a 110-file rewrite (which D-05 explicitly rejects against the cost ceiling).

Output: A passing smoke test, a documented structural-tier guard, a static fallback-policy assertion, an audit note, reconciled ROADMAP/REQUIREMENTS text, and a checkpoint confirming real `.oto/` parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-query-registry-workflow-consumption/12-CONTEXT.md
@.planning/phases/12-query-registry-workflow-consumption/12-RESEARCH.md
@.planning/phases/12-query-registry-workflow-consumption/12-VALIDATION.md
@.planning/phases/12-query-registry-workflow-consumption/12-03-SUMMARY.md

<interfaces>
<!-- In-process dispatch path. Source pattern: sdk/src/cli.ts:396-426 (VERIFIED). -->
```typescript
import { createRegistry } from '../query/index.js';
import { resolveQueryArgv } from '../query/registry.js';
import { normalizeQueryCommand } from '../query/normalize-query-command.js';

const registry = createRegistry();
const [normCmd, normArgs] = normalizeQueryCommand(key, args);
const matched = resolveQueryArgv([normCmd, ...normArgs], registry);
const result = await registry.dispatch(matched.cmd, matched.args, fixtureRoot, undefined);
// registry.has(token) filters prose false-positives from the extracted key list.
```

<!-- Key extraction (VERIFIED to produce the workflow key catalog). -->
```bash
grep -rhoE "oto-sdk query [a-zA-Z0-9._-]+" oto/workflows oto/commands \
  | sed 's/oto-sdk query //' | sort -u
# Filter each token through registry.has() in the test; drop tokens that don't resolve.
```

<!-- Existing read-only degradation idiom (extend / assert this for the read-only tier). VERIFIED 37+ uses. -->
```bash
SKIP_DISCUSS=$(oto-sdk query config-get workflow.skip_discuss 2>/dev/null || echo "false")
COMMIT_DOCS=$(oto-sdk query config-get commit_docs 2>/dev/null || echo "true")
```

<!-- Proposed structural hard-require guard (D-05; finalize wording). -->
```bash
if ! command -v oto-sdk >/dev/null 2>&1; then
  echo "ERROR: oto-sdk is required for this operation but is not on PATH." >&2
  echo "Install: npm install -g github:OTOJulian/oto-hybrid-framework" >&2
  exit 1
fi
```

<!-- D-06 reconciliation targets (VERIFIED line numbers). -->
.planning/ROADMAP.md:93 — "4. The same workflows fall back to manual file operations and complete successfully when `oto-sdk` is absent."
.planning/REQUIREMENTS.md:16 — "**SDK-05**: Workflows that call `oto-sdk query …` consume its output when present and still degrade gracefully (manual fallback) when it is absent."

<!-- D-04 CJS audit verdict (RESEARCH §CJS Stray-Hardcode Audit): all 9 files CONFIRM, no logic fix. -->
Files: core.cjs, init.cjs, state.cjs, migrate.cjs, drift.cjs, commands.cjs, milestone.cjs, verify.cjs, gsd2-import.cjs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the .oto/ fixture + enumerate-and-smoke harness (Wave 0; SDK-03 SC#1/SC#2)</name>
  <files>sdk/src/golden/oto-query-smoke.integration.test.ts, sdk/src/golden/fixtures/oto-project/.oto/STATE.md, sdk/src/golden/fixtures/oto-project/.oto/config.json, sdk/src/golden/fixtures/oto-project/.oto/ROADMAP.md, sdk/src/golden/fixtures/oto-project/.oto/REQUIREMENTS.md, sdk/src/golden/fixtures/oto-project/.oto/phases/01-sample/01-01-PLAN.md</files>
  <read_first>
    - sdk/src/cli.ts (lines 396-426 — the dispatch path to mirror in-process: createRegistry, normalizeQueryCommand, resolveQueryArgv, registry.dispatch)
    - sdk/src/query/registry.ts (resolveQueryArgv, QueryRegistry.has, QUERY_MUTATION_COMMANDS)
    - sdk/src/query/index.ts (createRegistry + the full handler registration list — to know which keys are registered)
    - sdk/vitest.config.ts (the `integration` project: include `src/**/*.integration.test.ts`, 120s timeout)
    - sdk/src/golden/golden.integration.test.ts (existing integration test style + fixture/temp-dir conventions)
    - .planning/phases/12-query-registry-workflow-consumption/12-RESEARCH.md (§Validation Architecture step 1-5; Open Question 3 — fixture minimal contents)
  </read_first>
  <behavior>
    - Extraction yields every distinct `oto-sdk query <token>` from oto/workflows + oto/commands; after registry.has() filtering, only registered keys remain (prose false-positives like "into" are dropped).
    - For each registered read-only key, in-process dispatch against the fixture .oto/ returns a structured result (object or string), NOT a thrown GSDError / {error}.
    - No dispatched read resolves a path under the fixture's (absent) .planning/ — assert every project-rooted path string in the result is under `<fixtureRoot>/.oto/`, never `<fixtureRoot>/.planning/`.
    - Structural/mutation keys (in QUERY_MUTATION_COMMANDS) are asserted to RESOLVE and target a `.oto/`-rooted path (dry-run / structural assertion) rather than committing or mutating shared state.
    - The fixture directory contains NO `.planning/` directory.
  </behavior>
  <action>
    Create the fixture and the smoke test under `sdk/src/golden/`.

    A) FIXTURE — `sdk/src/golden/fixtures/oto-project/`. Build a minimal `.oto/` tree (per RESEARCH Open Question 3):
    - `.oto/STATE.md` — frontmatter with `oto_state_version: 1.0` plus a minimal body (so resolution + state reads work).
    - `.oto/config.json` — `{ "commit_docs": true, "workflow": {} }` minimal valid config (backs config-get/agent-skills/resolve-model).
    - `.oto/ROADMAP.md` — one `### Phase 1:` section with a Requirements line (backs roadmap.* / find-phase).
    - `.oto/REQUIREMENTS.md` — one requirement line (backs requirements reads).
    - `.oto/phases/01-sample/01-01-PLAN.md` — minimal plan frontmatter (backs phases.list / phase reads / phase-plan-index).
    - Explicitly NO `.planning/` directory anywhere in the fixture.
    If a particular key family needs git (commit-class), the test should COPY the fixture into an `mkdtempSync` temp dir and `git init` there at runtime rather than committing a .git into the fixture.

    B) SMOKE TEST — `sdk/src/golden/oto-query-smoke.integration.test.ts` (the `.integration.test.ts` suffix routes it to the integration project):
    1. At test setup, run the extraction (spawn the grep|sed pipeline via `execSync`, OR read+regex the workflow/command files in JS) to collect distinct tokens from `oto/workflows` + `oto/commands` (path them relative to repo root — the test runs from `sdk/`, so resolve `../oto/workflows`).
    2. `const registry = createRegistry();` then filter tokens to those `registry.has(token)` (or `resolveQueryArgv([normCmd,...])` matches) accepts. Assert the filtered set is NON-EMPTY and includes representative members across families: at least one of each of `config-get`, `agent-skills`, `state.load`/`state-snapshot`, `roadmap.get-phase`, `phases.list`, `find-phase`, `commit`, `init.*`.
    3. Copy the fixture into a fresh `mkdtempSync(join(tmpdir(), 'oto-smoke-'))`; for commit-class keys, `git init` the temp dir.
    4. For each READ-ONLY registered key: `await registry.dispatch(matched.cmd, matched.args, fixtureRoot, undefined)` and assert the result is structured (not an error). Walk the result for any string containing the fixtureRoot path and assert it contains `/.oto/` and NOT `/.planning/`.
    5. For each MUTATION key (member of QUERY_MUTATION_COMMANDS): assert it resolves (no "unknown command") and that its target path (where determinable structurally) is `.oto/`-rooted. Do NOT require a successful mutation for keys needing extra args; a "resolves + targets .oto/" assertion is sufficient and avoids fragile arg construction.
    6. Clean up the temp dir in `afterAll`/`finally`.
    Mark the test resilient to keys that legitimately need args the smoke can't supply — such keys should still PROVE registration (registry.has true) and be allowed to return a structured "missing arg" error WITHOUT touching `.planning/`. The hard assertion is SC#2 (never `.planning/`), with SC#1 proven by `registry.has()` coverage of the enumerated set.

    Do NOT create a `.oto/` at the repo root — only inside the fixture/temp dir (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run src/golden/oto-query-smoke.integration.test.ts --project integration</automated>
  </verify>
  <acceptance_criteria>
    - `test -f sdk/src/golden/fixtures/oto-project/.oto/STATE.md && echo OK` prints OK
    - `test ! -d sdk/src/golden/fixtures/oto-project/.planning && echo NO_PLANNING` prints NO_PLANNING
    - `grep -F "oto_state_version" sdk/src/golden/fixtures/oto-project/.oto/STATE.md` matches (fixture is a migrated/.oto root)
    - `grep -c "createRegistry" sdk/src/golden/oto-query-smoke.integration.test.ts` returns >= 1
    - `grep -cE "registry.has|resolveQueryArgv" sdk/src/golden/oto-query-smoke.integration.test.ts` returns >= 1 (prose false-positives filtered)
    - `grep -F ".planning" sdk/src/golden/oto-query-smoke.integration.test.ts` shows it ASSERTS-AGAINST `.planning` (negative assertion present)
    - `cd sdk && npx vitest run src/golden/oto-query-smoke.integration.test.ts --project integration` exits 0
  </acceptance_criteria>
  <done>The fixture .oto/ tree exists (no .planning/), and the smoke test enumerates+filters workflow keys, dispatches each in-process against the fixture, and passes with structured output + zero .planning/ access assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the D-05 structural guard + statically assert the tiered fallback policy (SDK-05)</name>
  <files>oto/workflows/lib/sdk-require.md, oto/workflows/execute-phase.md, oto/workflows/autonomous.md, tests/sdk-fallback-policy.test.cjs</files>
  <read_first>
    - oto/workflows/ (sample read-only call sites already using `2>/dev/null || echo <default>` — e.g. grep for `config-get .* || echo`; and structural call sites like `oto-sdk query commit`, `oto-sdk query phase.add`, `oto-sdk query state.update`)
    - .planning/phases/12-query-registry-workflow-consumption/12-RESEARCH.md (§Tiered Fallback; §Code Examples for the guard idiom; the read-only vs structural key classification in D-05)
    - .planning/phases/12-query-registry-workflow-consumption/12-CONTEXT.md (D-05 — the exact read-only vs structural lists)
    - CLAUDE.md (personal-use cost ceiling — do NOT reimplement handlers in bash)
  </read_first>
  <action>
    SDK-05 is a POLICY + a guard idiom + a static assertion — NOT a 110-file rewrite. D-05 establishes the tiers; read-only already uses the degradation idiom in 37+ places. Deliver:

    A) STRUCTURAL GUARD DOC — create `oto/workflows/lib/sdk-require.md` documenting the single hard-require guard idiom for the structural/stateful tier, with the exact error wording. Include:
    - The guard snippet (from interfaces: `command -v oto-sdk >/dev/null 2>&1 || { echo "ERROR: oto-sdk is required …" >&2; echo "Install: npm install -g github:OTOJulian/oto-hybrid-framework" >&2; exit 1; }`).
    - The structural key list it applies to (init.*, commit, phase.add/insert/remove/complete, state mutations, roadmap.update-plan-progress, requirements mark-complete, milestone.complete).
    - The read-only tier list and its `2>/dev/null || echo <default>` idiom (config-get, resolve-model, agent-skills, state-snapshot, state.load, find-phase reads).
    - One sentence stating WHY structural ops are not reimplemented in bash (cost ceiling; Phase 11 guarantees oto-sdk on PATH so absence is an edge case).

    B) APPLY THE GUARD to the highest-traffic structural entry points so the policy is real, not just documented. Add the hard-require guard to the FIRST structural `oto-sdk query` invocation in these representative workflows (the most-used per the key inventory): the `commit` path and the `init.*`/state-mutation path. Concretely, add the guard block immediately before the first structural call in: `oto/workflows/execute-phase.md`, `oto/workflows/autonomous.md`, and any workflow whose first SDK call is a `commit`/`init.*`/`state.update`. Do NOT add it before read-only calls (those degrade). Keep edits minimal — one guard block per workflow, near the top of its SDK-using section.

    C) STATIC ASSERTION — add a node:test (CJS) test at `tests/sdk-fallback-policy.test.cjs` (root test dir; node:test per CLAUDE.md for tooling) that:
    - Greps `oto/workflows` for read-only key call sites and asserts they carry `2>/dev/null || echo` (sample the known read-only keys: config-get, resolve-model, agent-skills).
    - Asserts `oto/workflows/lib/sdk-require.md` exists and contains the `command -v oto-sdk` guard.
    - Asserts at least the representative workflows from (B) contain the guard string.
    This is a static policy check (grep-based), not a runtime PATH-stripping test — matching the VALIDATION.md "static/shell" test type.
  </action>
  <verify>
    <automated>node --test tests/sdk-fallback-policy.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f oto/workflows/lib/sdk-require.md && echo OK` prints OK
    - `grep -F "command -v oto-sdk" oto/workflows/lib/sdk-require.md` matches
    - `grep -F "2>/dev/null || echo" oto/workflows/lib/sdk-require.md` matches (read-only tier documented)
    - `grep -rlF "command -v oto-sdk" oto/workflows/execute-phase.md oto/workflows/autonomous.md` lists both files (guard applied)
    - `test -f tests/sdk-fallback-policy.test.cjs && echo OK` prints OK
    - `node --test tests/sdk-fallback-policy.test.cjs` exits 0
  </acceptance_criteria>
  <done>The structural hard-require guard is documented in sdk-require.md, applied to the representative high-traffic structural workflows, and a node:test statically asserts both tiers' policy holds — without reimplementing any handler in bash.</done>
</task>

<task type="auto">
  <name>Task 3: D-04 CJS audit confirmation note + D-06 ROADMAP/REQUIREMENTS reconciliation</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/12-query-registry-workflow-consumption/12-RESEARCH.md (§CJS Stray-Hardcode Audit — the per-file verdict table; §State of the Art — the exact D-06 edits)
    - .planning/ROADMAP.md (lines 85-94 — Phase 12 section, SC#4 at line 93)
    - .planning/REQUIREMENTS.md (lines 14-16 — SDK-03, SDK-05 at line 16)
    - oto/bin/lib/core.cjs (lines 55-73 — to confirm the marker-check literal is intentional, per D-04)
  </read_first>
  <action>
    A) D-04 CJS AUDIT (confirm, no rework). Re-run the audit grep to confirm RESEARCH's verdict still holds:
    `grep -rn "\.planning" oto/bin/lib/core.cjs oto/bin/lib/init.cjs oto/bin/lib/state.cjs oto/bin/lib/migrate.cjs oto/bin/lib/drift.cjs oto/bin/lib/commands.cjs oto/bin/lib/milestone.cjs oto/bin/lib/verify.cjs oto/bin/lib/gsd2-import.cjs`
    For each hit, classify it as one of: (a) routes through planningRootName/planningDir/planningPaths/planningRoot, (b) intentional migrated-marker check (core.cjs:56), (c) intentional migration dual-root handling (migrate.cjs / gsd2-import.cjs), (d) comment/display string. If EVERY hit falls in (a)-(d), record the confirmation in the SUMMARY (a per-file verdict table mirroring RESEARCH). Make NO CJS code change. If a hit is a genuine stray hardcode in a resolution path (none expected per RESEARCH), fix it by routing through planningRootName(cwd) and note it. Do NOT touch migrate.cjs/gsd2-import.cjs dual-root logic (it is the migration tool, intentional).

    B) D-06 RECONCILIATION (MANDATORY text edits).
    - Edit `.planning/ROADMAP.md` line 93 (Phase 12 Success Criteria #4). Replace:
      `4. The same workflows fall back to manual file operations and complete successfully when \`oto-sdk\` is absent.`
      with a tiered-model statement, e.g.:
      `4. When \`oto-sdk\` is absent, read-only queries degrade to sensible defaults and the workflow continues; structural/stateful operations fail fast with one clear, actionable error rather than silently completing.`
    - Edit `.planning/REQUIREMENTS.md` line 16 (SDK-05). Replace:
      `- [ ] **SDK-05**: Workflows that call \`oto-sdk query …\` consume its output when present and still degrade gracefully (manual fallback) when it is absent.`
      with:
      `- [ ] **SDK-05**: Workflows that call \`oto-sdk query …\` consume its output when present; when \`oto-sdk\` is absent, read-only queries degrade to sensible defaults while structural/stateful operations fail fast with one clear, actionable error (tiered fallback).`
      Keep the `- [ ]` checkbox unchecked (verification closes it later). Do NOT alter SDK-03's wording.
    Preserve all surrounding markdown structure, frontmatter, and other lines exactly.
  </action>
  <verify>
    <automated>grep -F "fail fast" .planning/ROADMAP.md && grep -F "fail fast" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "fall back to manual file operations and complete successfully" .planning/ROADMAP.md` returns NOTHING (old literal wording removed)
    - `grep -F "fail fast" .planning/ROADMAP.md` matches (SC#4 reconciled)
    - `grep -F "still degrade gracefully (manual fallback)" .planning/REQUIREMENTS.md` returns NOTHING (old SDK-05 wording removed)
    - `grep -F "tiered fallback" .planning/REQUIREMENTS.md` matches (SDK-05 reconciled)
    - `grep -F "**SDK-03**" .planning/REQUIREMENTS.md` still present and unchanged
    - SUMMARY contains the D-04 per-file audit verdict table (9 files, all CONFIRM) with no CJS code change
  </acceptance_criteria>
  <done>D-04 audit re-confirmed (9 files, no CJS logic change, recorded in SUMMARY); ROADMAP SC#4 and REQUIREMENTS SDK-05 reconciled to the tiered fallback model with the old literal wording removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm cross-binary .oto/ parity (oto-sdk query vs oto-tools.cjs)</name>
  <files>(verification only — no files modified)</files>
  <what-built>
    The SDK query registry now resolves `.oto/` (or marker-`.planning/`) via the ported `planningRootName` model, at parity with the CJS `oto-tools.cjs`. The in-process smoke harness proves every workflow key resolves against a fixture `.oto/` with zero `.planning/` access, and `sdk/dist/` is rebuilt. This checkpoint confirms the end-to-end cross-binary parity that automation samples but cannot fully self-certify (per 12-VALIDATION.md Manual-Only Verifications).
  </what-built>
  <action>
    HUMAN CHECKPOINT — no automated file changes. Pause and present the verification steps below to the user; resume only after they confirm parity or report a divergence.
  </action>
  <how-to-verify>
    Run against a throwaway `.oto/` project (NOT this repo's root — this repo stays on `.planning/` until Phase 13):

    1. Create a temp project:
       `D=$(mktemp -d); mkdir -p "$D/.oto/phases/01-sample"; printf -- "---\noto_state_version: 1.0\n---\n# State\n" > "$D/.oto/STATE.md"; printf '{"commit_docs":true}' > "$D/.oto/config.json"; (cd "$D" && git init -q)`
    2. Build is current: from the repo, `cd sdk && npm run build` (should be a no-op if Plan 03 already built).
    3. Run a representative read key with the REPO-LOCAL binary (NOT the stale global): from repo root,
       `node bin/oto-sdk.js query state-snapshot --project-dir "$D"`
       Confirm it returns structured output rooted at `$D/.oto/` and does NOT error with `command not found` or reference `.planning`.
    4. Compare against the CJS layer: `node oto/bin/lib/oto-tools.cjs state-snapshot` run from inside `$D` (or its equivalent invocation) — confirm the two produce equivalent structured output for the same layout.
    5. Confirm NO `.oto/` was created at the actual repo root: `test ! -d .oto && echo CLEAN` should print CLEAN.

    Expected: both binaries resolve `$D/.oto/`; output is structured and equivalent; no `.planning/` access; repo root unchanged.
  </how-to-verify>
  <verify>
    Human confirmation that both binaries resolve `.oto/` at parity (no automated gate; this is the manual-only verification from 12-VALIDATION.md).
  </verify>
  <done>User confirms both the repo-local `oto-sdk` and `oto-tools.cjs` resolve a throwaway `.oto/` project equivalently with no `.planning/` access, and the repo root still has no `.oto/`.</done>
  <resume-signal>Type "approved" if both binaries resolve .oto/ at parity, or describe the divergence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted workflow tokens → registry dispatch | The smoke harness feeds grep-extracted tokens into `registry.dispatch`; `registry.has()` filtering bounds them to registered commands. |
| fixture/temp dir → filesystem writes | The harness writes a fixture into a `mkdtempSync` temp dir and may `git init` it; never the repo root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | smoke harness creating directories | mitigate | Fixtures live under `sdk/src/golden/fixtures/` and runtime copies under `mkdtempSync(tmpdir())` — never the repo root. Acceptance criterion asserts `test ! -d .oto` at repo root, preventing an accidental `.oto/` that would break the goldens (RESEARCH Pitfall 2). |
| T-12-11 | Denial of Service | enumerate step spawning grep over many files | accept | Bounded one-time extraction over `oto/workflows` + `oto/commands` (~110 files) at test setup; integration project has a 120s timeout. No unbounded recursion. |
| T-12-12 | Spoofing / Tampering | structural guard error message | accept | The hard-require guard only emits a fixed error string and exits non-zero when oto-sdk is absent; it executes no user input and reduces (not increases) the chance of a structural op silently faking completion (the rejected universal-wrapper anti-pattern). |
| T-12-13 | Information Disclosure | reconciliation edits to planning docs | accept | ROADMAP/REQUIREMENTS edits are local doc text changes with no secrets; standard planning-doc maintenance. |
</threat_model>

<verification>
- `cd sdk && npx vitest run src/golden/oto-query-smoke.integration.test.ts --project integration` exits 0 (SDK-03 SC#1/SC#2 proven in-process)
- `cd sdk && npm test` exits 0 (full vitest incl. goldens — no regression from the new integration test)
- `node --test tests/sdk-fallback-policy.test.cjs` exits 0 (SDK-05 tiered policy asserted)
- root `npm test` exits 0 (CJS regression)
- ROADMAP SC#4 + REQUIREMENTS SDK-05 reconciled (old literal wording gone; tiered wording present)
- D-04 audit verdict recorded in SUMMARY; no CJS logic change
- Human checkpoint confirms cross-binary `.oto/` parity; repo root has no `.oto/`
</verification>

<success_criteria>
- Enumerate+fixture in-process smoke proves every registered workflow key resolves against `.oto/` with zero `.planning/` access (SDK-03 SC#1/SC#2)
- The D-05 tiered fallback is documented (sdk-require.md), applied to representative structural workflows, and statically asserted (SDK-05) — no handler reimplemented in bash
- D-04 confirmed: 9 CJS files correct-by-design, no rework
- D-06 satisfied: ROADMAP SC#4 + REQUIREMENTS SDK-05 reconciled to the tiered model with old literal wording removed
- Cross-binary `.oto/` parity confirmed at the human checkpoint; goldens green; repo root has no `.oto/`
</success_criteria>

<output>
After completion, create `.planning/phases/12-query-registry-workflow-consumption/12-04-SUMMARY.md`
</output>
