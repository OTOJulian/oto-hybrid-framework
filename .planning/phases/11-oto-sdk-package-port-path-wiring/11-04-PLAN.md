---
phase: 11-oto-sdk-package-port-path-wiring
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - scripts/install-smoke.cjs
autonomous: true
requirements: [SDK-01, SDK-02, SDK-04]
tags: [smoke, clean-install, integration, phase-gate]

must_haves:
  truths:
    - "scripts/install-smoke.cjs, after the existing global tarball/archive install into the temp prefix, runs <prefix>/bin/oto-sdk query generate-slug \"Phase Eleven\" and asserts exit 0 + JSON \"slug\":\"phase-eleven\""
    - "The smoke asserts a .planning/-backed key (roadmap.analyze or state.json) returns parseable JSON from a dir containing .planning/"
    - "The smoke asserts NO ERR_MODULE_NOT_FOUND appears in stderr on any oto-sdk query (the headline dependency-resolution negative guard)"
    - "The smoke asserts <prefix>/bin/oto-sdk exists and is executable (mode & 0o111)"
    - "The smoke asserts the installer prints a PATH-gated 'OTO SDK ready' on a clean install (oto-sdk callable on PATH)"
    - "No manual tsc/build step runs anywhere in the smoke — the SDK works purely from the committed prebuilt dist"
  artifacts:
    - path: "scripts/install-smoke.cjs"
      provides: "extended clean-install simulation proving SC #1-#4 end to end"
      contains: "oto-sdk"
  key_links:
    - from: "scripts/install-smoke.cjs"
      to: "<prefix>/bin/oto-sdk"
      via: "spawnSync structured-output + negative-guard assertions"
      pattern: "oto-sdk"
    - from: "scripts/install-smoke.cjs"
      to: "ERR_MODULE_NOT_FOUND negative guard"
      via: "stderr scan asserting absence"
      pattern: "ERR_MODULE_NOT_FOUND"
---

<objective>
Extend `scripts/install-smoke.cjs` (the existing npm-archive -> temp-prefix install simulation) with the load-bearing Phase 11 assertions: after the clean install, `oto-sdk query generate-slug "Phase Eleven"` resolves and emits structured JSON; a `.planning/`-backed key returns parseable JSON; NO `ERR_MODULE_NOT_FOUND` appears on any query (the dependency-resolution proof); the `oto-sdk` bin is linked + executable; and the installer prints a PATH-gated "OTO SDK ready". This is the phase gate that proves SC #1-#4 end to end with NO manual build step.

Purpose: 11-VALIDATION's central check is the clean-install simulation. The existing smoke already does npm-archive install -> assert `oto` bin -> per-runtime install. This plan adds the `oto-sdk` query + negative-guard assertions to the SAME host, turning it into the SC #1-#4 gate. This is Wave 2 because it requires the committed `sdk/dist/` (Plan 01), the bin entry + top-level deps (Plan 02), and the installer "OTO SDK ready" message (Plan 03) all present.
Output: A green `node scripts/install-smoke.cjs` that fails loudly if the SDK ever stops resolving, crashes on missing modules, or regresses the false-ready bug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-oto-sdk-package-port-path-wiring/11-CONTEXT.md
@.planning/phases/11-oto-sdk-package-port-path-wiring/11-RESEARCH.md
@.planning/phases/11-oto-sdk-package-port-path-wiring/11-VALIDATION.md
@scripts/install-smoke.cjs

<existing_smoke_shape>
scripts/install-smoke.cjs (127 lines) currently:
- mkdtemp temp prefix; spec = github-archive tarball URL for the current HEAD ref;
  `npm install -g <spec> --prefix tmpdir` (this is the clean install — it runs npm,
  installs top-level dependencies into the install tree, but NEVER runs tsc).
- asserts `<tmpdir>/bin/oto` exists + executable; runs it bare; asserts stdout has `oto v<version>`.
- runOtoInstallSmoke(binDir): per-runtime `oto install --<runtime> --config-dir <tmp>` with PATH prefixed
  by binDir; asserts state file + agents + CLAUDE.md marker.
- finally: rm -rf tmpdir.
ADD the oto-sdk assertions in the main try block AFTER the `oto v<version>` check (binPath dir is
`path.dirname(binPath)` === `<tmpdir>/bin`, where npm linked oto-sdk). The deps that oto-sdk needs were
installed by the same `npm install -g <spec>` because Plan 02 declared them top-level.
</existing_smoke_shape>

<exact_assertion_sequence>
<!-- From 11-RESEARCH "Clean-install simulation" + 11-VALIDATION test map. Copy verbatim. -->
After the existing `oto v<version>` assertion, with `binDir = path.dirname(binPath)`:

1. oto-sdk bin linked + executable:
   const sdkBin = path.join(binDir, 'oto-sdk');
   assert fs.existsSync(sdkBin); assert (fs.statSync(sdkBin).mode & 0o111) !== 0;

2. Structured slug (SC #1, pure handler — deterministic, zero FS dep):
   const r1 = spawnSync(sdkBin, ['query','generate-slug','Phase Eleven'], { encoding:'utf8' });
   assert r1.status === 0;
   const j1 = JSON.parse(r1.stdout);  // must parse
   assert j1.slug === 'phase-eleven';

3. Negative guard (the headline dependency-resolution proof — SC #1):
   const combined = (r1.stdout||'') + (r1.stderr||'');
   assert !combined.includes('ERR_MODULE_NOT_FOUND');  // deps resolved upward from sdk/dist/cli.js

4. .planning/-backed key (registry breadth — run from the repo root which HAS .planning/, or a temp
   project seeded with a minimal .planning/ROADMAP.md). Use roadmap.analyze (or state.json):
   const r2 = spawnSync(sdkBin, ['query','roadmap.analyze'], { encoding:'utf8', cwd: <dir-with-.planning> });
   assert r2.status === 0; JSON.parse(r2.stdout);  // parseable
   NOTE: pick a key from the Phase-11 "works" tier (pure or .planning/-reading). Do NOT assert state.load
   or any CJS-bridge key (Phase 12). If roadmap.analyze proves brittle against the temp dir, fall back to
   the pure `current-timestamp` key for breadth — but prefer a .planning/-reading key to demonstrate registry reach.

5. PATH-gated "OTO SDK ready" (SC #2/#4): during runOtoInstallSmoke, capture the installer stdout for the
   claude run (PATH already prefixed with binDir so oto-sdk IS callable). Assert it contains "OTO SDK ready".
   (The installer's wireOtoSdk gates this on isOtoSdkOnPath() — with binDir on PATH it resolves true.)
</exact_assertion_sequence>

<phase_boundary>
The .planning/ key assertion uses THIS repo's existing .planning/ layout (Phase 11 keeps the registry
answering against .planning/). Do NOT assert .oto/-pathed keys or state.load — Phase 12.
</phase_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add oto-sdk bin + structured-output + ERR_MODULE_NOT_FOUND negative-guard assertions</name>
  <read_first>
    - scripts/install-smoke.cjs (the full current file — extend the main try block after the `oto v<version>` check)
    - .planning/phases/11-oto-sdk-package-port-path-wiring/11-RESEARCH.md ("Clean-install simulation" exact sequence)
    - .planning/phases/11-oto-sdk-package-port-path-wiring/11-VALIDATION.md (SDK-01 / SDK-04 rows)
  </read_first>
  <files>scripts/install-smoke.cjs</files>
  <action>
    In `scripts/install-smoke.cjs`, in the main try block, AFTER the existing `if (!out.stdout.includes(`oto v${expectedVersion}`))` check and BEFORE `runOtoInstallSmoke(...)`, insert the oto-sdk assertions using the existing `binPath` (so `binDir = path.dirname(binPath)`):

    1. Assert `oto-sdk` bin linked + executable:
       - const sdkBin = path.join(path.dirname(binPath), 'oto-sdk');
       - if (!fs.existsSync(sdkBin)) -> console.error FAIL + process.exit(1)
       - if ((fs.statSync(sdkBin).mode & 0o111) === 0) -> FAIL + exit(1)

    2. Assert structured slug (SC #1 pure handler):
       - const r1 = spawnSync(sdkBin, ['query','generate-slug','Phase Eleven'], { encoding:'utf8' });
       - if (r1.status !== 0) -> FAIL (print r1.stdout + r1.stderr) + exit(1)
       - parse r1.stdout in a try/catch; on parse error -> FAIL + exit(1)
       - if (parsed.slug !== 'phase-eleven') -> FAIL + exit(1)

    3. Assert the ERR_MODULE_NOT_FOUND negative guard (headline dependency-resolution proof):
       - const combined = (r1.stdout||'') + '\n' + (r1.stderr||'');
       - if (combined.includes('ERR_MODULE_NOT_FOUND')) -> console.error('FAIL: oto-sdk crashed with ERR_MODULE_NOT_FOUND — top-level deps did not resolve from sdk/dist/cli.js') + exit(1)

    4. Print a PASS line: `console.log('PASS: oto-sdk query generate-slug -> structured JSON, deps resolve')`.

    Keep all output as console.error + process.exit(1) on failure to match the file's existing style.
    Do NOT add a tsc/build step. Do NOT modify the per-runtime loop in this task (Task 2 handles the
    .planning/ key + the "OTO SDK ready" assertion).
  </action>
  <verify>
    <automated>node -c scripts/install-smoke.cjs && grep -q "generate-slug" scripts/install-smoke.cjs && grep -q "ERR_MODULE_NOT_FOUND" scripts/install-smoke.cjs && grep -q "phase-eleven" scripts/install-smoke.cjs && echo "ok: sdk query + negative guard wired"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/install-smoke.cjs` is valid CJS (`node -c` exits 0)
    - contains `generate-slug` and the assertion `slug` / `phase-eleven`
    - contains a literal `ERR_MODULE_NOT_FOUND` string in a failure-guard check
    - constructs `oto-sdk` bin path via `path.dirname(binPath)` (or `binDir`) — the npm-linked secondary bin
    - on the structured-output failure paths it calls `process.exit(1)` (matches file style)
    - NO `tsc` / `npm run build` token added anywhere in the file
  </acceptance_criteria>
  <done>The smoke proves a clean-installed oto-sdk resolves, returns structured JSON for the pure generate-slug handler, and does NOT crash with a missing module — the SC #1 + dependency-resolution proof.</done>
</task>

<task type="auto">
  <name>Task 2: Add .planning/-backed key assertion + PATH-gated "OTO SDK ready" installer check</name>
  <read_first>
    - scripts/install-smoke.cjs (now carrying Task 1's assertions; the runOtoInstallSmoke function)
    - .planning/phases/11-oto-sdk-package-port-path-wiring/11-RESEARCH.md ("Phase 11 Query Keys" tier table — pick a .planning/-reading key; PATH-gated ready message)
    - .planning/ROADMAP.md (confirms a .planning/ROADMAP.md exists for roadmap.analyze to read in this repo)
  </read_first>
  <files>scripts/install-smoke.cjs</files>
  <action>
    Two additions to `scripts/install-smoke.cjs`:

    A. `.planning/`-backed registry-breadth assertion (SC #1 breadth). After Task 1's slug assertions, run a
       `.planning/`-reading key from a directory that HAS a `.planning/`:
       - Prefer running with `cwd: process.cwd()` IF the smoke is invoked from the repo root (which has
         `.planning/`). To be hermetic, seed a temp project: mkdtemp, create `<tmp>/.planning/ROADMAP.md`
         with a minimal valid roadmap stub (a few lines), then:
         const r2 = spawnSync(sdkBin, ['query','roadmap.analyze'], { encoding:'utf8', cwd: tmpProj });
       - if (r2.status !== 0) -> FAIL (print stdout+stderr) + exit(1)
       - JSON.parse(r2.stdout) in try/catch; on parse error -> FAIL + exit(1)
       - Negative guard again on r2: combined must NOT include 'ERR_MODULE_NOT_FOUND'.
       - rm -rf the temp project. Print `PASS: oto-sdk query roadmap.analyze -> parseable JSON`.
       If roadmap.analyze is brittle against the stub, substitute the pure `current-timestamp` key
       (still proves registry reach without a CJS bridge) — but document the substitution in the SUMMARY.
       Do NOT use state.load or any CJS-bridge key (Phase 12).

    B. PATH-gated "OTO SDK ready" assertion (SC #2/#4). In `runOtoInstallSmoke`, for the `claude` runtime
       iteration (PATH is already prefixed with binDir so oto-sdk resolves), the installer output is captured
       in `r` (spawnSync ... encoding:'utf8'). After the existing claude marker check, assert:
       - if (!r.stdout.includes('OTO SDK ready')) -> console.error('FAIL: installer did not print PATH-gated "OTO SDK ready" (oto-sdk should be callable with binDir on PATH)') + process.exit(1)
       - Print `PASS: installer reports OTO SDK ready (PATH-gated)`.
       This proves the live installer's wireOtoSdk gate fires positively when oto-sdk is genuinely on PATH.

    Do NOT add a build step. Do NOT assert .oto/ paths (Phase 12).
  </action>
  <verify>
    <automated>node -c scripts/install-smoke.cjs && grep -q "roadmap.analyze\|current-timestamp" scripts/install-smoke.cjs && grep -q "OTO SDK ready" scripts/install-smoke.cjs && echo "ok: planning-key + ready-gate assertions wired"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/install-smoke.cjs` valid CJS (`node -c` exits 0)
    - contains a `.planning/`-reading key call (`roadmap.analyze` OR the documented `current-timestamp` fallback)
    - the `.planning/` key assertion JSON.parses the stdout (grep `JSON.parse` near the r2 call)
    - contains the literal `OTO SDK ready` assertion inside `runOtoInstallSmoke` (claude branch)
    - the "OTO SDK ready" check calls `process.exit(1)` on absence
    - NO `state.load`, NO `.oto/` path token added (phase boundary)
    - NO `tsc` / `npm run build` token added
  </acceptance_criteria>
  <done>The smoke proves registry breadth via a .planning/-backed key and confirms the installer's PATH-gated "OTO SDK ready" fires when oto-sdk is callable — closing SC #1 breadth + SC #2/#4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke harness -> spawned oto-sdk | the smoke executes the freshly-installed binary with controlled args and cwd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | a regressed dist or dropped dep silently shipping | mitigate | The negative guard (assert NO ERR_MODULE_NOT_FOUND) + structured-output assertion fail the smoke (and CI) the moment deps stop resolving or dist regresses — converting a runtime crash into a build-time gate. |
| T-11-11 | Denial of Service | smoke leaves temp dirs / installs behind | accept | Existing `finally { rm -rf tmpdir }` covers the install prefix; Task 2's temp project is rm -rf'd inline. Faithful to the file's existing cleanup discipline. |
</threat_model>

<verification>
- `node scripts/install-smoke.cjs` exits 0 end to end (clean archive install -> oto-sdk query asserts -> per-runtime + ready gate).
- The smoke asserts: oto-sdk linked+executable; generate-slug -> `slug:"phase-eleven"`; NO ERR_MODULE_NOT_FOUND; a .planning/ key parses; installer prints "OTO SDK ready".
- No tsc/build runs anywhere (SC #3 / SC #4 — committed dist only).
</verification>

<success_criteria>
- The clean-install simulation proves SC #1 (resolves + structured output, no missing-module crash), SC #2 (oto-sdk on PATH + installer confirms), SC #3 (no manual build), and SC #4 (prebuilt dist ships and works) end to end.
- A future regression in deps/dist/installer fails the smoke immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/11-oto-sdk-package-port-path-wiring/11-04-SUMMARY.md`
</output>
