---
phase: 11-oto-sdk-package-port-path-wiring
plan: 01
subsystem: sdk
tags: [sdk, typescript, oto-sdk, dist, query]

requires:
  - phase: 11-oto-sdk-package-port-path-wiring
    provides: Phase 11 context and research defining the faithful SDK port boundary
provides:
  - Top-level sdk/ subpackage ported from GSD's SDK
  - Surface-rebranded oto-sdk package manifest and CLI strings
  - Prebuilt committed sdk/dist/ output from tsc
affects: [phase-11, phase-12, sdk, installer, query-registry]

tech-stack:
  added: [typescript, vitest, "@anthropic-ai/claude-agent-sdk", ws]
  patterns: [isolated ESM sdk subpackage, committed prebuilt dist, surface-only rebrand]

key-files:
  created:
    - sdk/
    - sdk/dist/
    - sdk/package-lock.json
  modified:
    - sdk/package.json
    - sdk/src/cli.ts
    - sdk/README.md

key-decisions:
  - "Ported the full upstream GSD SDK subtree rather than trimming to query-only."
  - "Kept internal GSD class and file identifiers unchanged for build-safe parity."
  - "Committed sdk/dist/ as a one-time author build; no install-time SDK build was added."
  - "Committed the generated sdk/package-lock.json so the SDK dependency tree is reproducible and no generated artifact remains untracked."

patterns-established:
  - "SDK stays isolated under sdk/ with its own package.json, tsconfig.json, and ESM dist output."
  - "Phase 11 rebrands only package/bin/user-facing CLI strings; Phase 12 owns CJS bridge and .oto path rewiring."

requirements-completed: [SDK-04, SDK-01]

duration: 8min
completed: 2026-05-25
---

# Phase 11 Plan 01: SDK Package Port Summary

**Faithful GSD SDK port with an oto-sdk surface and committed prebuilt TypeScript dist output**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-25T21:54:47Z
- **Completed:** 2026-05-25T22:02:28Z
- **Tasks:** 3
- **Files modified:** sdk subtree plus this summary

## Accomplishments

- Copied the full upstream GSD SDK source, prompts, tests, fixtures, docs, and config into top-level `sdk/`, excluding `dist`, `node_modules`, and the upstream lockfile.
- Rebranded the package and user-facing CLI surface to `@oto-build/sdk`, `oto-sdk`, and `OTO_QUERY_FALLBACK`.
- Built `sdk/dist/` from the rebranded TypeScript source and committed the compiled CLI output.
- Verified `node sdk/dist/cli.js query generate-slug "Phase Eleven"` returns JSON with `"slug": "phase-eleven"`.

## Task Commits

1. **Task 1: Copy GSD sdk/ subtree** - `55b0cc7` (feat)
2. **Task 2: Surface rebrand gsd->oto** - `49c42ca` (feat)
3. **Task 3: Build sdk/dist with tsc** - `e79365c` (feat)

## Files Created/Modified

- `sdk/package.json` - SDK subpackage manifest with `@oto-build/sdk` and `oto-sdk` bin surface.
- `sdk/src/cli.ts` - Rebranded user-facing usage, errors, version output, and fallback environment variable.
- `sdk/README.md` - Rebranded SDK prose with upstream attribution and note about retained internal GSD names.
- `sdk/dist/cli.js` - Prebuilt CLI entry generated by `tsc` and committed executable.
- `sdk/dist/query/index.js` - Compiled query registry entry.
- `sdk/package-lock.json` - Generated lockfile for the SDK subpackage dependency tree.

## Decisions Made

- Kept `BUNDLED_GSD_TOOLS_PATH` and `BUNDLED_CORE_CJS` paths unchanged per the Phase 12 boundary.
- Kept `GSD`, `GSDTools`, and related internal identifiers unchanged to avoid cosmetic churn and preserve upstream sync safety.
- Committed the SDK lockfile generated by `npm install`; it was not copied from upstream and now reflects the rebranded package.

## Verification

- `npm install --silent` in `sdk/` succeeded after dependencies were fetched.
- `npm run build` in `sdk/` succeeded.
- `node sdk/dist/cli.js query generate-slug "Phase Eleven"` parsed as structured JSON with slug `phase-eleven`.
- `find sdk/dist -name '*.test.js' -o -name '*.integration.test.js'` returned no files.
- `git check-ignore sdk/dist/cli.js` returned exit `1` (not ignored).
- `git check-ignore sdk/node_modules` returned exit `0` (ignored).
- `grep` checks confirmed `sdk/src/cli.ts` contains `Usage: oto-sdk` and `OTO_QUERY_FALLBACK`, with zero `gsd-sdk` occurrences.
- `sdk/src/gsd-tools.ts` still contains `../../get-shit-done/bin/gsd-tools.cjs`.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

- The first sandboxed `npm install` emitted no output and hung; the process was stopped, then rerun with network-capable execution and completed successfully.
- `npm install` generated `sdk/package-lock.json`. It was committed with Task 3 as intentional SDK package metadata so the worktree does not retain generated untracked files.

## Known Stubs

None. Stub scan hits were local accumulator arrays in CLI/query output handling, not placeholder data flowing to a UI or user-facing surface.

## Threat Flags

None. The new committed `sdk/dist/` and SDK dependency surface were already covered by the plan threat model.

## User Setup Required

None.

## Next Phase Readiness

Plan 11-02 can wire the top-level `bin/oto-sdk.js`, package `bin` entry, package `files` allowlist, and top-level runtime dependencies against the committed SDK payload.

## Self-Check: PASSED

- Found `.planning/phases/11-oto-sdk-package-port-path-wiring/11-01-SUMMARY.md`
- Found `sdk/package.json`
- Found `sdk/src/cli.ts`
- Found `sdk/dist/cli.js`
- Found task commits `55b0cc7`, `49c42ca`, and `e79365c`

---
*Phase: 11-oto-sdk-package-port-path-wiring*
*Completed: 2026-05-25*
