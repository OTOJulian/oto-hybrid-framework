---
phase: 10
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-10-license-attribution.test.cjs
  - tests/phase-10-action-sha-pin.test.cjs
  - tests/phase-10-workflow-shape.test.cjs
  - tests/phase-10-rebrand-snapshot.test.cjs
  - tests/phase-10-skill-auto-trigger.test.cjs
  - tests/phase-10-readme-shape.test.cjs
  - tests/phase-10-docs-presence.test.cjs
  - tests/phase-10-commands-index-sync.test.cjs
  - tests/fixtures/phase-10/rebrand-snapshots/.gitkeep
  - scripts/gen-commands-index.cjs
autonomous: true
requirements:
  - CI-04
  - CI-06
  - CI-07
  - CI-10
  - DOC-01
  - DOC-04
  - DOC-06
must_haves:
  truths:
    - "Running `npm test` exercises new phase-10-*.test.cjs files (some pass, some are t.todo() pending Wave 1 artifacts)"
    - "License-attribution test diffs THIRD-PARTY-LICENSES.md against the upstream LICENSE bodies in foundation-frameworks/ and FAILS LOUD if either MIT body drifts"
    - "Action-SHA-pin test scans .github/workflows/*.yml and FAILS if any non-local `uses:` reference is not a 40-char hex SHA"
    - "Workflow-shape test asserts presence + required jobs/triggers of test.yml, install-smoke.yml, release.yml (uses t.todo() until Wave 1 creates them)"
    - "Skill-auto-trigger runtime test spawns oto/hooks/oto-session-start with an active STATE fixture and asserts using-oto skill body does NOT appear in additionalContext"
    - "Rebrand-snapshot test classifies each tests/fixtures/rebrand/* file via engine.cjs and assert.deepEqual against committed __snapshots__ JSON, using the D-10-07 locked projection shape: { file, classifications: [{ rule, before, after, line }, ...] } sorted by file ASC then line ASC"
    - "README-shape test ships and asserts unconditionally (D-10-05): asserts README.md contains a concrete `vX.Y.Z`-shaped install URL (rejects literal `vX.Y.Z` placeholder), 'Install', 'Attribution', and a link to commands/INDEX.md. EXPECTED to fail loud after Wave 0 against the stub README and pass after Wave 1's README rewrite."
    - "Docs-presence test asserts docs/upstream-sync.md and docs/rebrand-engine.md exist with required headings (uses t.todo() until Wave 1 creates them)"
    - "Commands-index-sync test re-runs scripts/gen-commands-index.cjs and asserts the regenerated bytes equal the committed oto/commands/INDEX.md (uses t.todo() until Wave 1 commits INDEX.md)"
    - "scripts/gen-commands-index.cjs is executable and writes a sorted Markdown table to oto/commands/INDEX.md from frontmatter of all 76 commands in oto/commands/oto/*.md"
  artifacts:
    - path: "tests/phase-10-license-attribution.test.cjs"
      provides: "CI-06 byte-equality assertion against foundation-frameworks/*/LICENSE"
      min_lines: 25
    - path: "tests/phase-10-action-sha-pin.test.cjs"
      provides: "CI-10 regex lint over .github/workflows/*.yml"
      min_lines: 20
    - path: "tests/phase-10-workflow-shape.test.cjs"
      provides: "CI-01/02/03 presence + required-job assertions"
      min_lines: 40
    - path: "tests/phase-10-rebrand-snapshot.test.cjs"
      provides: "CI-04 per-fixture snapshot regression with D-10-07 locked projection shape"
      min_lines: 40
    - path: "tests/phase-10-skill-auto-trigger.test.cjs"
      provides: "CI-07 runtime hook-spawn assertion"
      min_lines: 30
    - path: "tests/phase-10-readme-shape.test.cjs"
      provides: "DOC-01 content-presence + vX.Y.Z placeholder rejection (D-10-05: ships with no whole-suite stub-bypass)"
      min_lines: 20
    - path: "tests/phase-10-docs-presence.test.cjs"
      provides: "DOC-03/04 presence + required-heading assertions"
      min_lines: 25
    - path: "tests/phase-10-commands-index-sync.test.cjs"
      provides: "DOC-06 regen-diff assertion"
      min_lines: 20
    - path: "scripts/gen-commands-index.cjs"
      provides: "DOC-06 generator (CLI: `--check` mode for regen-diff, default writes file)"
      min_lines: 50
  key_links:
    - from: "tests/phase-10-license-attribution.test.cjs"
      to: "foundation-frameworks/{get-shit-done-main,superpowers-main}/LICENSE"
      via: "fs.readFileSync + assert THIRD-PARTY-LICENSES.md.includes(LICENSE.trim())"
      pattern: "fs\\.readFileSync.*foundation-frameworks.*LICENSE"
    - from: "tests/phase-10-action-sha-pin.test.cjs"
      to: ".github/workflows/*.yml"
      via: "fs.readdirSync + regex /uses:\\s+([^@\\s]+)@([^\\s#]+)/g asserts /^[0-9a-f]{40}$/"
      pattern: "[0-9a-f]\\{40\\}"
    - from: "tests/phase-10-skill-auto-trigger.test.cjs"
      to: "oto/hooks/oto-session-start"
      via: "spawnSync('bash', [HOOK]) with cwd containing .oto/STATE.md from STATE-active fixture"
      pattern: "spawnSync.*oto-session-start"
    - from: "tests/phase-10-rebrand-snapshot.test.cjs"
      to: "scripts/rebrand/lib/engine.cjs (engine.run / engine.classify)"
      via: "require + per-fixture classify call + deepEqual against tests/fixtures/phase-10/rebrand-snapshots/<fixture>.json — D-10-07 projection shape"
      pattern: "require.*rebrand/lib/engine"
    - from: "scripts/gen-commands-index.cjs"
      to: "oto/commands/oto/*.md frontmatter"
      via: "readdirSync + parse YAML frontmatter (name, description) + sort + render Markdown table"
      pattern: "oto/commands/oto"
---

<objective>
Wave 0: lock the test surface and the auto-generator script for Phase 10. Produces 8 new node:test files plus scripts/gen-commands-index.cjs. Most tests run live now (license-attribution, action-SHA-pin where workflows already exist, rebrand-snapshot, skill-auto-trigger). Tests for artifacts that don't exist yet (workflow files, docs/, commands/INDEX.md) use `t.todo()` placeholders and are filled in by Wave 1 by writing the artifacts those tests already gate. The README-shape test (D-10-05) is the deliberate exception: it ships and asserts unconditionally, expected to fail after Wave 0 and pass after Wave 1's README rewrite.

Purpose: Wave 0 enforces the Nyquist contract — every requirement in CI-04, CI-06, CI-07, CI-10, DOC-01, DOC-04, DOC-06 has an automated assertion BEFORE the implementing artifact is written. Wave 1 cannot regress these criteria silently.

Output: 8 phase-10-*.test.cjs files, 1 fixtures placeholder dir, 1 generator script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-tests-ci-docs-v0-1-0-release/10-RESEARCH.md
@.planning/phases/10-tests-ci-docs-v0-1-0-release/10-VALIDATION.md
@CLAUDE.md
@tests/phase-01-licenses.test.cjs
@tests/phase-02-coverage-manifest.test.cjs
@tests/05-session-start-fixture.test.cjs
@tests/06-using-oto-state-gating.test.cjs

<interfaces>
<!-- Engine API surface (from scripts/rebrand/lib/engine.cjs) -->
```js
module.exports = { run, loadAndValidate, byteIdentical };
// run({ mode, target, out, force, owner, mapPath }) — mode in {'apply','dry-run','verify-roundtrip'}
//   returns { mode, target, out, summary, ... }
```

<!-- D-10-07 LOCKED projection shape (cross-plan contract). Both Plan 01 Task 1's test
     and Plan 02's regen-rebrand-snapshots.cjs MUST emit this exact shape: -->
```json
{
  "file": "<relative-path-from-fixtures-input-dir>",
  "classifications": [
    { "rule": "<rule-id>", "before": "<string>", "after": "<string>", "line": <int> }
  ]
}
```
Sort order: `file` ASC, then `line` ASC within `classifications`. NO absolute paths,
NO temp-dir refs (`/var/folders/...`, `/tmp/...`), NO SHAs. Deterministic across runs.

<!-- Existing test scaffold pattern (from tests/05-session-start-fixture.test.cjs and tests/06-using-oto-state-gating.test.cjs) -->
```js
'use strict';
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const REPO_ROOT = path.resolve(__dirname, '..');
```

<!-- Existing skill-gating fixture (from tests/06) -->
```js
const STATE_FIXTURE = path.join(__dirname, 'skills/__fixtures__/STATE-active.md');
```

<!-- Frontmatter shape from oto/commands/oto/help.md and new-project.md -->
File starts with a YAML frontmatter block delimited by triple-dash lines, containing two keys:
- name: oto:<slug>
- description: <one-line>

<!-- runtime-matrix.cjs precedent (scanCommands at scripts/render-runtime-matrix.cjs) -->
```js
const { renderMatrix, scanCommands } = require('../bin/lib/runtime-matrix.cjs');
// scanCommands(repoRoot) returns [{ name, path, referencedAgents, ... }] from oto/commands
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author CI-06 + CI-10 + CI-04 + CI-07 live test files (4 files, all run green Wave 0)</name>
  <files>
    tests/phase-10-license-attribution.test.cjs,
    tests/phase-10-action-sha-pin.test.cjs,
    tests/phase-10-rebrand-snapshot.test.cjs,
    tests/phase-10-skill-auto-trigger.test.cjs,
    tests/fixtures/phase-10/rebrand-snapshots/.gitkeep
  </files>
  <read_first>
    /Users/Julian/Desktop/oto-hybrid-framework/.planning/phases/10-tests-ci-docs-v0-1-0-release/10-RESEARCH.md (lines 267-342: license-attribution, action-SHA-pin, snapshot, skill-auto-trigger code sketches; lines 568-598: Open Questions RESOLVED — D-10-01..D-10-07 locked decisions; D-10-07 specifically locks the snapshot projection shape),
    /Users/Julian/Desktop/oto-hybrid-framework/tests/phase-01-licenses.test.cjs (existing baseline — confirms substring-only coverage; this task adds byte-equality of MIT bodies),
    /Users/Julian/Desktop/oto-hybrid-framework/foundation-frameworks/get-shit-done-main/LICENSE,
    /Users/Julian/Desktop/oto-hybrid-framework/foundation-frameworks/superpowers-main/LICENSE,
    /Users/Julian/Desktop/oto-hybrid-framework/THIRD-PARTY-LICENSES.md,
    /Users/Julian/Desktop/oto-hybrid-framework/tests/05-session-start-fixture.test.cjs (hook-spawn pattern reference),
    /Users/Julian/Desktop/oto-hybrid-framework/tests/06-using-oto-state-gating.test.cjs (STATE-active fixture path constant),
    /Users/Julian/Desktop/oto-hybrid-framework/tests/skills/__fixtures__/STATE-active.md,
    /Users/Julian/Desktop/oto-hybrid-framework/oto/hooks/oto-session-start (hook executable spawned by skill-auto-trigger test),
    /Users/Julian/Desktop/oto-hybrid-framework/scripts/rebrand/lib/engine.cjs (engine.run API),
    /Users/Julian/Desktop/oto-hybrid-framework/tests/fixtures/rebrand/ (9 fixtures to snapshot),
    /Users/Julian/Desktop/oto-hybrid-framework/tests/phase-02-coverage-manifest.test.cjs (engine usage example: engine.run({ mode:'apply', target:..., out:..., owner:'OTOJulian' }))
  </read_first>
  <behavior>
    - Test phase-10-license-attribution.test.cjs:
      - Read foundation-frameworks/get-shit-done-main/LICENSE → assert THIRD-PARTY-LICENSES.md.includes(GSD_LIC.trim())
      - Read foundation-frameworks/superpowers-main/LICENSE → assert THIRD-PARTY-LICENSES.md.includes(SUP_LIC.trim())
      - Assert THIRD-PARTY-LICENSES.md contains literal "Lex Christopherson" AND "Jesse Vincent"
      - Failure mode: any drift in upstream LICENSE bodies vs THIRD-PARTY-LICENSES.md fails loud (T-LIC-01 mitigation)
    - Test phase-10-action-sha-pin.test.cjs:
      - Walk .github/workflows/*.yml; if dir does not exist, skip via test.skip() (do NOT t.todo — the lint should silently no-op pre-Wave-1 but FAIL once any workflow is present)
      - For each `uses: <action>@<ref>` line where action does NOT start with `./`, assert /^[0-9a-f]{40}$/.test(ref)
      - Permits trailing `# vX.Y.Z` comment after SHA (RegExp captures only the @<ref> token before whitespace/`#`)
    - Test phase-10-rebrand-snapshot.test.cjs (per D-10-07 LOCKED projection shape):
      - For each file in tests/fixtures/rebrand/*: invoke engine in dry-run mode against a tmp tree containing that single fixture → derive projection: `{ file: <relative-path-from-input-dir>, classifications: [{ rule, before, after, line }, ...] }`, sorted by `file` ASC, then `line` ASC. NO abs paths, NO `/var/folders/`, `/tmp/` refs, NO SHAs.
      - deepEqual against tests/fixtures/phase-10/rebrand-snapshots/<basename>.json
      - On first run (snapshots empty), call test.todo with message 'snapshot for <fixture> not yet seeded — see Plan 02 Task 1b' (NOTE: regen-rebrand-snapshots.cjs is authored in Plan 02 Task 1b; Wave 0 ships the snapshots/ dir with .gitkeep only)
      - The test must be deterministic given a fixed engine + rename-map
      - The test SHARES the projection-shape contract with Plan 02 Task 1b's regen tool — both must construct identical projections from the same engine output. This shape is NOT executor judgment; it is locked.
    - Test phase-10-skill-auto-trigger.test.cjs:
      - mkdtempSync; create .oto/STATE.md by copying tests/skills/__fixtures__/STATE-active.md
      - spawnSync('bash', [path.join(REPO_ROOT, 'oto/hooks/oto-session-start')], { cwd, env: { PATH: process.env.PATH, HOME: cwd, CLAUDE_PLUGIN_ROOT: '/tmp/fake' }, encoding: 'utf8' })
      - Assert exit status === 0; parse stdout JSON; assert hookSpecificOutput.additionalContext does NOT contain '<!-- oto:state-gating-directive -->' marker (the using-oto skill body sentinel)
      - If the hook does not yet emit hookSpecificOutput at all, mark t.todo with diagnostic
  </behavior>
  <action>
1. Create tests/phase-10-license-attribution.test.cjs verbatim from RESEARCH.md § "License-attribution test (CI-06)" (lines 269-289). Add a third test asserting `Copyright (c) 2025 Lex Christopherson` and `Copyright (c) 2025 Jesse Vincent` (full copyright lines per THIRD-PARTY-LICENSES.md verified content). Use REPO_ROOT = path.resolve(__dirname, '..').

2. Create tests/phase-10-action-sha-pin.test.cjs verbatim from RESEARCH.md § "Action SHA-pin lint (CI-10)" (lines 293-315). Constants: WORKFLOWS_DIR = path.resolve(__dirname, '..', '.github', 'workflows'); SHA_RE = /uses:\s+([^@\s]+)@([^\s#]+)/g. Skip block if !fs.existsSync(WORKFLOWS_DIR) (returns silently — no t.todo, no failure). For each .yml/.yaml file in dir, iterate matches; if action.startsWith('./') skip; else assert.match(ref, /^[0-9a-f]{40}$/, descriptive message).

3. Create tests/phase-10-rebrand-snapshot.test.cjs **using the D-10-07 LOCKED projection shape** (NOT executor discretion):
   - require engine = require('../scripts/rebrand/lib/engine.cjs')
   - SNAPSHOTS_DIR = path.join(REPO_ROOT, 'tests/fixtures/phase-10/rebrand-snapshots')
   - FIXTURES_DIR = path.join(REPO_ROOT, 'tests/fixtures/rebrand')
   - Define a pure helper `function buildProjection(engineResult, inputDir)` that returns:
     ```js
     // D-10-07 locked shape — DO NOT modify without updating RESEARCH.md
     {
       file: <path relative to inputDir, POSIX separators>,
       classifications: [
         { rule: <string rule-id>, before: <string>, after: <string>, line: <int 1-based> },
         ...
       ]
     }
     ```
     `classifications` MUST be sorted by `line` ASC (stable sort; secondary by `rule` ASC if same line). Top-level array of projections (one per file) MUST be sorted by `file` ASC.
     Strip absolute paths, temp-dir refs (`/var/folders/...`, `/tmp/...`, `os.tmpdir()` prefix), and any SHA-like 40-char hex strings introduced by the engine's metadata. If the engine.run return shape does not surface `{rule, before, after, line}` directly, the helper must derive these from `result.summary` / per-rule diff blocks; the executor reads engine.cjs to find the right access path but the EMITTED shape is fixed.
   - Per-fixture loop: read snapshot JSON if exists → run engine in dry-run mode against a tmp tree containing that single fixture → call buildProjection(result, inputDir) → assert.deepEqual(actual, expected). If snapshot file does not exist, register `test.todo('snapshot for <fixture> not yet seeded — Plan 02 Task 1b will run regen-rebrand-snapshots.cjs')` per fixture (so the test file runs green Wave 0 with todos surfaced).
   - DO NOT MUTATE foundation-frameworks/. Use fs.mkdtempSync + cpSync of the single fixture into a synthetic input dir for engine.run.
   - Cite D-10-07 in a top-of-file comment: `// Projection shape locked by D-10-07 in 10-RESEARCH.md. Cross-plan contract with tests/regen-rebrand-snapshots.cjs (Plan 02 Task 1b).`

4. Create tests/phase-10-skill-auto-trigger.test.cjs per RESEARCH.md § "Skill auto-trigger runtime test (CI-07 gap)" (lines 327-342). Use HOOK = path.join(REPO_ROOT, 'oto/hooks/oto-session-start'); STATE_ACTIVE_FIXTURE = path.join(__dirname, 'skills/__fixtures__/STATE-active.md'). The marker scanned for is the literal `<!-- oto:state-gating-directive -->` (matches the using-oto SKILL.md gating-directive marker established in Phase 6). If the hook returns non-zero or stdout is not JSON, fail with stderr in the assertion message.

5. Create tests/fixtures/phase-10/rebrand-snapshots/.gitkeep (empty file) so the directory exists for the snapshot test to read.

6. After writing all 4 tests, run: `node scripts/run-tests.cjs --test tests/phase-10-license-attribution.test.cjs tests/phase-10-action-sha-pin.test.cjs tests/phase-10-rebrand-snapshot.test.cjs tests/phase-10-skill-auto-trigger.test.cjs`. License + action-pin + skill-auto-trigger must exit 0; rebrand-snapshot must exit 0 (every fixture surfaced as test.todo since no snapshots seeded yet).
  </action>
  <acceptance_criteria>
    - File tests/phase-10-license-attribution.test.cjs exists, contains string `foundation-frameworks/get-shit-done-main/LICENSE` AND `foundation-frameworks/superpowers-main/LICENSE` AND `Lex Christopherson` AND `Jesse Vincent`
    - File tests/phase-10-action-sha-pin.test.cjs exists, contains regex literal `/^[0-9a-f]{40}$/` AND string `WORKFLOWS_DIR`
    - File tests/phase-10-rebrand-snapshot.test.cjs exists, contains string `require('../scripts/rebrand/lib/engine.cjs')` AND `tests/fixtures/phase-10/rebrand-snapshots` AND `D-10-07` (citation comment) AND `buildProjection` (helper name) AND `classifications` (locked shape key)
    - File tests/phase-10-skill-auto-trigger.test.cjs exists, contains string `oto/hooks/oto-session-start` AND `oto:state-gating-directive` AND `STATE-active.md`
    - Directory tests/fixtures/phase-10/rebrand-snapshots/ exists with .gitkeep
    - Command `node scripts/run-tests.cjs --test tests/phase-10-license-attribution.test.cjs` exits 0 with all assertions passing
    - Command `node scripts/run-tests.cjs --test tests/phase-10-action-sha-pin.test.cjs` exits 0 (skip path: .github/workflows does not exist yet)
    - Command `node scripts/run-tests.cjs --test tests/phase-10-skill-auto-trigger.test.cjs` exits 0 (passes if hook gates correctly, OR registers t.todo if hook does not emit hookSpecificOutput)
    - Command `node scripts/run-tests.cjs --test tests/phase-10-rebrand-snapshot.test.cjs` exits 0 (all fixtures registered as test.todo until Plan 02 Task 1b seeds snapshots)
    - foundation-frameworks/ directory is unchanged (sha256 of get-shit-done-main/LICENSE and superpowers-main/LICENSE files unchanged before vs after running this task)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-tests.cjs --test tests/phase-10-license-attribution.test.cjs tests/phase-10-action-sha-pin.test.cjs tests/phase-10-rebrand-snapshot.test.cjs tests/phase-10-skill-auto-trigger.test.cjs</automated>
  </verify>
  <done>4 tests authored, all exit 0; license-attribution actively asserts byte-equality of upstream MIT bodies; action-SHA-pin asserts 40-char SHA pattern; skill-auto-trigger spawns the hook and asserts no using-oto leak; rebrand-snapshot scaffolds per-fixture todos AND emits the D-10-07 locked projection shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author 4 deferred-shape tests (workflow-shape, readme-shape D-10-05 ships unconditionally, docs-presence, commands-index-sync)</name>
  <files>
    tests/phase-10-workflow-shape.test.cjs,
    tests/phase-10-readme-shape.test.cjs,
    tests/phase-10-docs-presence.test.cjs,
    tests/phase-10-commands-index-sync.test.cjs
  </files>
  <read_first>
    /Users/Julian/Desktop/oto-hybrid-framework/.planning/phases/10-tests-ci-docs-v0-1-0-release/10-RESEARCH.md (lines 156-265 workflow shapes; lines 392-394 README shape; lines 385-391 docs shape; lines 344-383 commands-INDEX generator + regen-diff; lines 583-585 D-10-05 README-shape ships unconditionally),
    /Users/Julian/Desktop/oto-hybrid-framework/README.md (current 19-line stub — used for negative test: must reject `vX.Y.Z` literal; Wave 0 EXPECTED to fail this test),
    /Users/Julian/Desktop/oto-hybrid-framework/tests/phase-04-planning-leak.test.cjs (precedent for shipped-payload directory walks)
  </read_first>
  <behavior>
    - Test phase-10-workflow-shape.test.cjs (covers CI-01, CI-02, CI-03 presence):
      - Assert .github/workflows/test.yml exists, contains `runs-on: ${{ matrix.os }}`, `node-version: [22, 24]`, `os: [ubuntu-latest]`, `os: macos-latest`, and `npm test`
      - Assert .github/workflows/install-smoke.yml exists, contains both `npm pack` AND `npm install -g $GITHUB_WORKSPACE` (smoke-unpacked job byte-cloned from upstream pattern; mode-644 trap reproduced WITHOUT chmod 644 — see Plan 02 Task 1)
      - Assert .github/workflows/release.yml exists, contains `on:\n  push:\n    tags:` (or equivalent) AND `gh release create` AND `--generate-notes`, AND a step that asserts package.json version matches the tag
      - Implementation: when any workflow file does not exist yet, register `test.todo('Wave 1 must create <file>')`
    - Test phase-10-readme-shape.test.cjs (covers DOC-01) — D-10-05: SHIPS UNCONDITIONALLY, NO STUB-BYPASS:
      - Read README.md
      - Assert sections: `# oto`, `## Install`, `## Attribution`
      - Assert install command line matches `/npm install -g .*\\/archive\\/v\\d+\\.\\d+\\.\\d+(?:[-+][\\w.-]+)?\\.tar\\.gz/` (concrete vX.Y.Z; rejects literal `vX.Y.Z` placeholder)
      - Assert link to commands/INDEX.md (string `commands/INDEX.md` or `oto/commands/INDEX.md`)
      - Assert link to THIRD-PARTY-LICENSES.md
      - When README still contains literal `vX.Y.Z`, assertion fails loud (this is the catch-loud lint)
      - **NO substring-bypass, NO whole-suite t.todo wrap.** Per D-10-05 in RESEARCH.md (line 585): "the test must NOT silently bypass via README-stub detection — Wave 0 may run against the stub README and is expected to fail loud until Plan 02 Task 2's README rewrite lands." This is by design (Nyquist sampling).
    - Test phase-10-docs-presence.test.cjs (covers DOC-03, DOC-04):
      - Assert docs/upstream-sync.md exists; if so assert it contains `## Quick start` AND `## How sync works` AND `oto sync` AND `.oto-sync-conflicts/`; if not exists, register test.todo
      - Assert docs/rebrand-engine.md exists; if so assert it contains `## Rule classes` AND `## Allowlist` AND `--dry-run` AND `--verify-roundtrip`; if not exists, register test.todo
    - Test phase-10-commands-index-sync.test.cjs (covers DOC-06):
      - require('child_process').spawnSync(process.execPath, ['scripts/gen-commands-index.cjs', '--check']) → assert exit code 0
      - If oto/commands/INDEX.md does not exist, register t.todo('Wave 1 must run gen-commands-index.cjs and commit oto/commands/INDEX.md')
  </behavior>
  <action>
1. Create tests/phase-10-workflow-shape.test.cjs:
   - REPO_ROOT = path.resolve(__dirname, '..')
   - WORKFLOWS = path.join(REPO_ROOT, '.github/workflows')
   - For each of [test.yml, install-smoke.yml, release.yml]:
     - filepath = path.join(WORKFLOWS, name)
     - test(`<name>: present + shape correct`, (t) => { if (!fs.existsSync(filepath)) return t.todo(`Wave 1 must create ${name}`); const body = fs.readFileSync(filepath, 'utf8'); /* per-file assertions per behavior block */ })
   - test.yml asserts: contains 'os: [ubuntu-latest]', 'node-version: [22, 24]', '- os: macos-latest', 'node-version: 24', 'npm test'
   - install-smoke.yml asserts: contains 'npm pack', 'npm install -g', 'smoke-unpacked' (job name), '$GITHUB_WORKSPACE' (unpacked install path). DO NOT assert `chmod 644` — per W-2 fix and T-INST-01 disposition update, the smoke proves the install works from a fresh-clone source-mode 755, not from a deliberately broken-mode source.
   - release.yml asserts: contains "tags: ['v*", 'gh release create', '--generate-notes', 'package.json' (for version assert step)

2. Create tests/phase-10-readme-shape.test.cjs **per D-10-05 — NO substring-bypass**:
   - README = fs.readFileSync(path.join(REPO_ROOT, 'README.md'), 'utf8')
   - Top-of-file comment: `// D-10-05: This test ships and asserts unconditionally. Wave 0 EXPECTS failure against the stub README; Wave 1's README rewrite (Plan 02 Task 2) makes it pass. Per-assertion t.todo is permitted only if individually justified — whole-suite stub-bypass is FORBIDDEN.`
   - test('README has install URL with concrete vX.Y.Z'): assert.match(README, /npm install -g .+\/archive\/v\d+\.\d+\.\d+(?:[-+][\w.-]+)?\.tar\.gz/); also assert.doesNotMatch(README, /\barchive\/vX\.Y\.Z\b/)
   - test('README has Install section'): assert.match(README, /^## Install/m)
   - test('README has Attribution section'): assert.match(README, /^## Attribution/m)
   - test('README links to commands index'): assert.ok(README.includes('commands/INDEX.md'))
   - test('README links to THIRD-PARTY-LICENSES'): assert.ok(README.includes('THIRD-PARTY-LICENSES.md'))
   - DO NOT include any `if (README.includes('alpha - Phase 2 ships')) return t.todo(...)` substring-bypass logic. If you find yourself writing it, STOP — D-10-05 forbids it.

3. Create tests/phase-10-docs-presence.test.cjs:
   - SYNC_DOC = path.join(REPO_ROOT, 'docs/upstream-sync.md')
   - REBRAND_DOC = path.join(REPO_ROOT, 'docs/rebrand-engine.md')
   - test('upstream-sync.md present + sections'): if (!fs.existsSync(SYNC_DOC)) return t.todo('Wave 1 must create docs/upstream-sync.md'); body = readFileSync; assert all required substrings
   - Same shape for rebrand-engine.md

4. Create tests/phase-10-commands-index-sync.test.cjs:
   - INDEX = path.join(REPO_ROOT, 'oto/commands/INDEX.md')
   - SCRIPT = path.join(REPO_ROOT, 'scripts/gen-commands-index.cjs')
   - test('gen-commands-index.cjs --check exits 0'): if (!fs.existsSync(INDEX)) return t.todo('Wave 1 must commit oto/commands/INDEX.md'); const r = spawnSync(process.execPath, [SCRIPT, '--check'], { cwd: REPO_ROOT, encoding: 'utf8' }); assert.equal(r.status, 0, r.stderr || r.stdout)

5. Run: `node scripts/run-tests.cjs --test tests/phase-10-workflow-shape.test.cjs tests/phase-10-readme-shape.test.cjs tests/phase-10-docs-presence.test.cjs tests/phase-10-commands-index-sync.test.cjs`. Expected outcomes:
   - workflow-shape: exit 0 (all t.todo until Wave 1)
   - **readme-shape: EXPECTED TO FAIL** (Wave 0 stub README contains no `archive/v0.1.0.tar.gz`, no `## Install` section, etc.). This is by design per D-10-05 — Nyquist sampling. Wave 1's README rewrite turns it green.
   - docs-presence: exit 0 (both docs t.todo until Wave 1)
   - commands-index-sync: exit 0 (t.todo until INDEX.md exists; Plan 01 Task 3 then makes it live-green)
  </action>
  <acceptance_criteria>
    - File tests/phase-10-workflow-shape.test.cjs exists, contains strings `test.yml`, `install-smoke.yml`, `release.yml`, `smoke-unpacked`, `gh release create`. Does NOT contain `chmod 644` (W-2 fix: trap is reproduced via fresh-clone install, not deliberate mode strip).
    - File tests/phase-10-readme-shape.test.cjs exists, contains string `archive/v` AND `vX\\.Y\\.Z` (the placeholder it rejects) AND `commands/INDEX.md` AND `D-10-05` (citation comment). Does NOT contain `alpha - Phase 2 ships` (substring-bypass forbidden) or any `if.*README.*includes.*return t.todo` whole-suite bypass pattern.
    - File tests/phase-10-docs-presence.test.cjs exists, contains strings `docs/upstream-sync.md`, `docs/rebrand-engine.md`, `--verify-roundtrip`
    - File tests/phase-10-commands-index-sync.test.cjs exists, contains strings `gen-commands-index.cjs`, `--check`, `oto/commands/INDEX.md`
    - workflow-shape, docs-presence, commands-index-sync exit 0 when invoked via `node scripts/run-tests.cjs --test`. Failures must be t.todo (counted but not failing) until Wave 1 produces the artifacts.
    - readme-shape is EXPECTED to exit non-zero against the Wave 0 stub README (D-10-05 design contract). After Plan 02 Task 2's README rewrite, it MUST exit 0. Verify with: run it now, confirm it fails loud on the stub.
    - No test imports a module that does not exist (no require errors)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-tests.cjs --test tests/phase-10-workflow-shape.test.cjs tests/phase-10-docs-presence.test.cjs tests/phase-10-commands-index-sync.test.cjs</automated>
  </verify>
  <done>4 deferred-shape tests authored. workflow-shape/docs-presence/commands-index-sync use t.todo for missing artifacts. readme-shape ships unconditionally per D-10-05 and is expected to fail Wave 0 / pass after Wave 1 README rewrite (this is by design, Nyquist sampling).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author scripts/gen-commands-index.cjs (writer + --check mode)</name>
  <files>scripts/gen-commands-index.cjs</files>
  <read_first>
    /Users/Julian/Desktop/oto-hybrid-framework/.planning/phases/10-tests-ci-docs-v0-1-0-release/10-RESEARCH.md (lines 344-383: full generator implementation sketch),
    /Users/Julian/Desktop/oto-hybrid-framework/oto/commands/oto/help.md,
    /Users/Julian/Desktop/oto-hybrid-framework/oto/commands/oto/new-project.md,
    /Users/Julian/Desktop/oto-hybrid-framework/scripts/render-runtime-matrix.cjs (regen-diff precedent — Phase 8)
  </read_first>
  <behavior>
    - Default invocation `node scripts/gen-commands-index.cjs`: writes oto/commands/INDEX.md with sorted command table; prints `wrote N commands → oto/commands/INDEX.md`; exit 0
    - `--check` invocation: regenerate in-memory; compare to existing oto/commands/INDEX.md bytes; exit 0 if equal, exit 1 with diff hint if drift; print nothing on success
    - Frontmatter parser: reads YAML between `---\n...---` boundary; extracts `name:` and `description:`; tolerates quoted/unquoted values; tolerates missing description (defaults to '' but still emits row); skips files with no frontmatter at all
    - Output format: `# /oto-* Command Index\n\n_Auto-generated…_\n\n| Command | Description |\n|---------|-------------|\n| `/oto-<slug>` | <description> |\n…\n` — sorted by `name` ascending
    - Idempotent: running it twice produces zero diff
  </behavior>
  <action>
1. Create scripts/gen-commands-index.cjs based on RESEARCH.md § "commands/INDEX.md generator (DOC-06)" (lines 347-381):
   - `#!/usr/bin/env node` shebang line + `'use strict';`
   - require fs, path
   - CMD_DIR = path.resolve(__dirname, '..', 'oto', 'commands', 'oto')
   - OUT = path.resolve(__dirname, '..', 'oto', 'commands', 'INDEX.md')
   - parseFrontmatter(body) function as in RESEARCH sketch (regex `/^---\n([\s\S]*?)\n---/` then per-line `(\w[\w-]*):\s*(.*)$` with quote stripping)
   - entries = readdirSync(CMD_DIR).filter(f => f.endsWith('.md')).map(parse).sort((a,b) => a.name.localeCompare(b.name))
   - render md as in RESEARCH sketch — name in table is `/${e.name.replace(':', '-')}` so `oto:help` becomes `/oto-help`
   - CLI handling: const checkMode = process.argv.includes('--check')
   - If checkMode: const existing = fs.existsSync(OUT) ? fs.readFileSync(OUT, 'utf8') : ''; if (existing === md) process.exit(0); else { console.error(`drift: oto/commands/INDEX.md out of sync. Run \`node scripts/gen-commands-index.cjs\` and commit.`); process.exit(1); }
   - Else: fs.writeFileSync(OUT, md); console.log(`wrote ${entries.length} commands → ${path.relative(process.cwd(), OUT)}`)

2. chmod +x scripts/gen-commands-index.cjs (so it can be invoked with shebang too)

3. Run `node scripts/gen-commands-index.cjs` once to produce oto/commands/INDEX.md (this is acceptable Wave 0 output — Plan 02 will validate it stays in sync; for Plan 01 we just produce the artifact so the regen-diff test loop can verify idempotency now). Then run `node scripts/gen-commands-index.cjs --check` and assert exit 0. Then re-run the writer once more and assert the file is byte-identical (idempotency check).

4. Verify the resulting INDEX.md row count: `grep -c '^| \`/oto-' oto/commands/INDEX.md` must equal 76 (matches the 76 commands in oto/commands/oto/*.md).
  </action>
  <acceptance_criteria>
    - File scripts/gen-commands-index.cjs exists, executable bit set (mode & 0o111 !== 0), contains strings `parseFrontmatter`, `--check`, `oto/commands/INDEX.md`, `localeCompare`
    - Command `node scripts/gen-commands-index.cjs` exits 0 and writes oto/commands/INDEX.md
    - File oto/commands/INDEX.md exists, line count > 80 (header + ~76 rows)
    - Command `grep -c '^| `/oto-' oto/commands/INDEX.md` outputs `76`
    - Command `node scripts/gen-commands-index.cjs --check` exits 0 (drift check passes after self-generation)
    - Idempotency: running the writer twice in a row produces byte-identical output (sha256sum oto/commands/INDEX.md unchanged across two runs)
    - Now that INDEX.md exists, `node scripts/run-tests.cjs --test tests/phase-10-commands-index-sync.test.cjs` exits 0 with the assertion live (no longer t.todo)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/gen-commands-index.cjs && node scripts/gen-commands-index.cjs --check && node scripts/run-tests.cjs --test tests/phase-10-commands-index-sync.test.cjs</automated>
  </verify>
  <done>Generator + INDEX.md committed. Regen-diff is live and green. Setup-cost paid for Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream LICENSE bodies → THIRD-PARTY-LICENSES.md | Drift between the two breaks legal attribution (T-LIC-01) |
| .planning/ → shipped tarball | Any leak of internal planning artifacts to npm tarball or runtime config dirs is a privacy/IP leak (T-LEAK-01) |
| skill loading → runtime context | A using-oto skill body that leaks into SessionStart when STATE is active breaks the workflow-deference contract (CI-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LIC-01 | Information Disclosure / Repudiation | THIRD-PARTY-LICENSES.md vs upstream LICENSE files | mitigate | tests/phase-10-license-attribution.test.cjs asserts byte-equality of full LICENSE bodies in foundation-frameworks/ against THIRD-PARTY-LICENSES.md (Task 1) |
| T-LEAK-01 | Information Disclosure | snapshot test fixtures + .gitkeep paths | mitigate | tests/phase-10-rebrand-snapshot.test.cjs writes only to tests/fixtures/phase-10/rebrand-snapshots/ — that path is NOT in package.json `files` allowlist, so it never ships in the tarball. tests/ is not in the `files` allowlist either (verified). Per D-10-07: projection shape strips abs paths, temp-dir refs, and SHAs to keep snapshots free of host-specific leakage. |
| T-CI-01 | Tampering | (no workflows yet — covered by Wave 1 Task 1's pinning; this plan only authors the lint that gates it) | mitigate | tests/phase-10-action-sha-pin.test.cjs asserts /^[0-9a-f]{40}$/ on every non-local `uses:` ref. Once Wave 1 ships workflows, this test fails loud on tag-based pins. |
| T-INST-01 | Tampering / DoS | (workflow-shape lint only — actual mode-644 simulation is in Wave 1's install-smoke.yml) | accept-here-mitigate-Wave-1 | tests/phase-10-workflow-shape.test.cjs asserts install-smoke.yml contains `npm install -g $GITHUB_WORKSPACE` AND `smoke-unpacked` job name (the unpacked-dir install path that exercises the mode-644 trap class). Per W-2 fix: the trap is reproduced via the fresh-clone install path (source bin/install.js is 755 in repo, npm preserves source mode), NOT via deliberate `chmod 644` in the job. The smoke proves "fresh clone install works", which is the actual user-facing scenario. |
| T-REL-01 | Tampering | (out of scope for Plan 01) | defer | covered by Plan 03 |
</threat_model>

<verification>
- All 8 phase-10-*.test.cjs files exist
- `node scripts/run-tests.cjs --test tests/phase-10-license-attribution.test.cjs tests/phase-10-action-sha-pin.test.cjs tests/phase-10-rebrand-snapshot.test.cjs tests/phase-10-skill-auto-trigger.test.cjs tests/phase-10-workflow-shape.test.cjs tests/phase-10-docs-presence.test.cjs tests/phase-10-commands-index-sync.test.cjs` exits 0 (7 of 8 tests)
- `tests/phase-10-readme-shape.test.cjs` is **EXPECTED to FAIL after Wave 0 and PASS after Wave 1's README rewrite** — this is by design per D-10-05 (Nyquist sampling). Do NOT mask the failure with t.todo; the failing assertion IS the contract.
- `node scripts/run-tests.cjs` (full suite) is EXPECTED to fail on readme-shape after Wave 0; will be green after Wave 1 lands the README rewrite.
- foundation-frameworks/ unchanged (Phase 9 D-01 immutability respected — verify via git status: no modifications to foundation-frameworks/)
- scripts/gen-commands-index.cjs is executable, committed; oto/commands/INDEX.md is committed and 76-rows
- tests/fixtures/phase-10/rebrand-snapshots/.gitkeep exists
</verification>

<success_criteria>
- Wave 0 test surface complete — 8 phase-10-*.test.cjs files authored
- 4 tests run live and green (license-attribution, action-SHA-pin, skill-auto-trigger, commands-index-sync after Task 3)
- 3 tests use t.todo() until Wave 1 ships artifacts (workflow-shape, docs-presence, rebrand-snapshot per-fixture)
- 1 test ships unconditionally and is EXPECTED to fail Wave 0 / pass Wave 1 (readme-shape, per D-10-05)
- scripts/gen-commands-index.cjs + oto/commands/INDEX.md committed
- All 7 requirements (CI-04, CI-06, CI-07, CI-10, DOC-01, DOC-04, DOC-06) have an automated assertion in tests/
</success_criteria>

<output>
After completion, create `.planning/phases/10-tests-ci-docs-v0-1-0-release/10-01-SUMMARY.md`
</output>
</output>
