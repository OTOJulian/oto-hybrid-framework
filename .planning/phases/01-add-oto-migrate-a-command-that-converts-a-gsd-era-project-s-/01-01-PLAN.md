---
phase: 01
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/migrate-detect.test.cjs
  - tests/migrate-rename-map.test.cjs
  - tests/migrate-dry-run.test.cjs
  - tests/migrate-apply.test.cjs
  - tests/migrate-idempotent.test.cjs
  - tests/migrate-conflict.test.cjs
  - tests/migrate-instructions.test.cjs
  - tests/migrate-state-frontmatter.test.cjs
  - tests/migrate-cli.test.cjs
  - tests/migrate-command-md.test.cjs
  - tests/fixtures/gsd-project-minimal/.planning/STATE.md
  - tests/fixtures/gsd-project-minimal/.planning/ROADMAP.md
  - tests/fixtures/gsd-project-minimal/.planning/phases/01-example/01-PLAN.md
  - tests/fixtures/gsd-project-minimal/CLAUDE.md
  - tests/fixtures/gsd-project-full/.planning/STATE.md
  - tests/fixtures/gsd-project-full/.planning/ROADMAP.md
  - tests/fixtures/gsd-project-full/.planning/PROJECT.md
  - tests/fixtures/gsd-project-full/.planning/REQUIREMENTS.md
  - tests/fixtures/gsd-project-full/.planning/config.json
  - tests/fixtures/gsd-project-full/.planning/phases/01-foo/01-PLAN.md
  - tests/fixtures/gsd-project-full/.planning/phases/01-foo/01-SUMMARY.md
  - tests/fixtures/gsd-project-full/CLAUDE.md
  - tests/fixtures/gsd-project-full/AGENTS.md
  - tests/fixtures/gsd-project-full/GEMINI.md
autonomous: true
requirements_addressed:
  - REQ-MIG-01
  - REQ-MIG-02
  - REQ-MIG-03
  - REQ-MIG-04
  - REQ-MIG-05
  - REQ-MIG-06
  - REQ-MIG-07
  - REQ-MIG-08
  - REQ-MIG-09
  - REQ-MIG-10
must_haves:
  truths:
    - "All 10 migrate-*.test.cjs files exist and run via node --test"
    - "Each test fails loudly (red) because oto/bin/lib/migrate.cjs does not yet exist"
    - "Two fixture trees exist: tests/fixtures/gsd-project-minimal/ and tests/fixtures/gsd-project-full/"
    - "Fixtures contain authentic GSD-era signals (gsd_state_version key, <!-- GSD:project-start --> markers, /gsd-* command refs)"
  artifacts:
    - path: "tests/migrate-detect.test.cjs"
      provides: "REQ-MIG-01 detection assertions"
    - path: "tests/migrate-rename-map.test.cjs"
      provides: "REQ-MIG-02 engine-call argument shape"
    - path: "tests/migrate-dry-run.test.cjs"
      provides: "REQ-MIG-03 dry-run report shape; no writes assertion"
    - path: "tests/migrate-apply.test.cjs"
      provides: "REQ-MIG-04 apply rewrites planning artifacts in fixture"
    - path: "tests/migrate-idempotent.test.cjs"
      provides: "REQ-MIG-05 second-run is no-op (zero diff)"
    - path: "tests/migrate-conflict.test.cjs"
      provides: "REQ-MIG-06 half-migrated state aborts non-zero"
    - path: "tests/migrate-instructions.test.cjs"
      provides: "REQ-MIG-07 marker-block rewrite for CLAUDE/AGENTS/GEMINI.md"
    - path: "tests/migrate-state-frontmatter.test.cjs"
      provides: "REQ-MIG-08 gsd_state_version → oto_state_version rewrite"
    - path: "tests/migrate-cli.test.cjs"
      provides: "REQ-MIG-09 oto-tools migrate dispatch + exit codes"
    - path: "tests/migrate-command-md.test.cjs"
      provides: "REQ-MIG-10 oto/commands/oto/migrate.md frontmatter shape"
    - path: "tests/fixtures/gsd-project-minimal/"
      provides: "Smallest GSD-era project: STATE.md frontmatter + 1 phase dir"
    - path: "tests/fixtures/gsd-project-full/"
      provides: "Full GSD-era project: all artifact types + 3 instruction files + completed-phase summaries"
  key_links:
    - from: "tests/migrate-*.test.cjs"
      to: "oto/bin/lib/migrate.cjs"
      via: "require('../oto/bin/lib/migrate.cjs')"
      pattern: "require.*migrate"
    - from: "tests/migrate-apply.test.cjs"
      to: "tests/fixtures/gsd-project-minimal/"
      via: "fs.cpSync to tmpdir then call apply()"
      pattern: "gsd-project-minimal"
---

<objective>
Create Wave 0 test scaffolding: 10 named test files + 2 fixture trees that anchor REQ-MIG-01..10. All tests must fail (RED) on first run because the implementation modules they reference do not yet exist. Wave 1 turns them green.

Purpose: Goal-backward verification — every requirement gets a falsifiable, automated check before any apply-side code lands. Without Wave 0, Wave 1 has no acceptance signal beyond "compiles".

Output: 10 `tests/migrate-*.test.cjs` files + 2 fixture trees (`tests/fixtures/gsd-project-minimal/`, `tests/fixtures/gsd-project-full/`) committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-add-oto-migrate-a-command-that-converts-a-gsd-era-project-s-/01-RESEARCH.md
@.planning/phases/01-add-oto-migrate-a-command-that-converts-a-gsd-era-project-s-/01-VALIDATION.md
@CLAUDE.md
@oto/templates/instruction-file.md
@rename-map.json
@tests/05-build-hooks.test.cjs

<interfaces>
<!-- The not-yet-existing module under test. Wave 1 implements this exact surface. -->

oto/bin/lib/migrate.cjs (does NOT exist yet — this is the contract Wave 0 tests assert against):

```javascript
// SYNCHRONOUS-FRIENDLY exports the tests will require()
module.exports = {
  // Returns { isGsdEra: boolean, signals: string[], conflicts: string[] }
  // signals ⊂ { 'gsd-state-frontmatter', 'gsd-instruction-markers', 'gsd-command-refs' }
  // conflicts ⊂ { 'oto-markers-present', 'both-state-dirs' }
  detectGsdProject(projectDir),

  // Returns { mode:'dry-run', files:[{path,matches:[],frontmatter,markers}], summary, exitCode:0 }
  // No writes. Calls scripts/rebrand/lib/engine.cjs run({ mode:'dry-run', target: projectDir, ... }).
  dryRun(projectDir, opts),

  // Performs the migration. Honors opts.renameStateDir (default false), opts.noBackup (default false), opts.force (default false), opts.scope ('planning'|'all'|'minimal', default 'planning').
  // Throws on detected conflict unless opts.force.
  // Returns { mode:'apply', filesChanged:[], backupDir:string|null, exitCode:0 }
  apply(projectDir, opts),

  // CLI entrypoint dispatched from oto-tools.cjs case 'migrate':
  // main(args) parses --dry-run|--apply|--rename-state-dir|--scope|--no-backup|--force, calls dryRun/apply, returns exitCode.
  main(args, cwd)
};
```

oto/bin/lib/oto-tools.cjs dispatch (existing pattern, ~line 1269):

```javascript
case 'from-oto2': {
  const oto2Import = require('./gsd2-import.cjs');
  oto2Import.cmdFromGsd2(args.slice(1), cwd, raw);
  break;
}
```

Wave 2 will add immediately after this case:
```javascript
case 'migrate': {
  const migrate = require('./migrate.cjs');
  process.exit(migrate.main(args.slice(1), cwd));
  break;
}
```

Marker shape in instruction files (verified at oto/templates/instruction-file.md:21-222):
- Open: `<!-- GSD:<section>-start [source:<file>] -->`
- Close: `<!-- GSD:<section>-end -->`
- Sections: project, stack, conventions, architecture, skills, workflow, profile
- Migrate must rewrite `GSD:` → `OTO:` while preserving the `source:<file>` attribute and section name verbatim.

State frontmatter shape (verified at .planning/STATE.md:2):
```text
[YAML frontmatter delimiter line]
gsd_state_version: 1.0    # ← becomes oto_state_version: 1.0
milestone: ...
[YAML frontmatter delimiter line]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the two fixture trees (gsd-project-minimal + gsd-project-full)</name>
  <files>
    tests/fixtures/gsd-project-minimal/.planning/STATE.md,
    tests/fixtures/gsd-project-minimal/.planning/ROADMAP.md,
    tests/fixtures/gsd-project-minimal/.planning/phases/01-example/01-PLAN.md,
    tests/fixtures/gsd-project-minimal/CLAUDE.md,
    tests/fixtures/gsd-project-full/.planning/STATE.md,
    tests/fixtures/gsd-project-full/.planning/ROADMAP.md,
    tests/fixtures/gsd-project-full/.planning/PROJECT.md,
    tests/fixtures/gsd-project-full/.planning/REQUIREMENTS.md,
    tests/fixtures/gsd-project-full/.planning/config.json,
    tests/fixtures/gsd-project-full/.planning/phases/01-foo/01-PLAN.md,
    tests/fixtures/gsd-project-full/.planning/phases/01-foo/01-SUMMARY.md,
    tests/fixtures/gsd-project-full/CLAUDE.md,
    tests/fixtures/gsd-project-full/AGENTS.md,
    tests/fixtures/gsd-project-full/GEMINI.md
  </files>
  <read_first>
    - .planning/STATE.md (copy frontmatter shape verbatim — `gsd_state_version: 1.0`)
    - oto/templates/instruction-file.md (copy `<!-- GSD:project-start source:PROJECT.md -->` … `<!-- GSD:project-end -->` marker pairs verbatim for project + stack + workflow sections only; minimal fixture only needs project section)
    - rename-map.json (confirm which substrings the engine is expected to rewrite — `gsd_state_version`, `Get Shit Done`, `/gsd-`, `gsd-hook-version`)
    - .planning/config.json — copy structure for fixtures/gsd-project-full/.planning/config.json including a `git.phase_branch_template: "gsd/phase-{phase}-{slug}"` line so the branch-template rewrite path is exercised
  </read_first>
  <action>
    Build two self-contained fixture trees that mimic GSD-era projects. Each fixture must contain *enough* signal that detectGsdProject() returns isGsdEra=true on it.

    **gsd-project-minimal/** — smallest viable GSD-era project (≤ 4 files):
    1. `.planning/STATE.md` — frontmatter MUST contain literal lines `gsd_state_version: 1.0` and `milestone: v0.1.0`. Body is a single `# Project State` heading.
    2. `.planning/ROADMAP.md` — single phase entry: `### Phase 1: Example` with bullet `- run /gsd-execute-phase 1 to break down`. The `/gsd-execute-phase` reference is the command-rule-rewrite signal.
    3. `.planning/phases/01-example/01-PLAN.md` — frontmatter `phase: 01\nplan: 01`, body contains the literal string `/gsd-plan-phase` and `Get Shit Done` once each.
    4. `CLAUDE.md` — at project root, contains exactly one marker pair: `<!-- GSD:project-start source:PROJECT.md -->\n## Project\n\nExample fixture.\n<!-- GSD:project-end -->`. Plus a workflow-enforcement block referencing `/gsd-quick`, `/gsd-debug`, `/gsd-execute-phase`.

    **gsd-project-full/** — exercises every signal class:
    1. `.planning/STATE.md` — same frontmatter as minimal.
    2. `.planning/ROADMAP.md` — three phases, multiple `/gsd-*` references.
    3. `.planning/PROJECT.md` — body mentions `Get Shit Done` once.
    4. `.planning/REQUIREMENTS.md` — minimal table with one row.
    5. `.planning/config.json` — JSON with `{"git": {"phase_branch_template": "gsd/phase-{phase}-{slug}"}, "gsd-hook-version": "1.0"}`.
    6. `.planning/phases/01-foo/01-PLAN.md` — body references `/gsd-execute-phase`.
    7. `.planning/phases/01-foo/01-SUMMARY.md` — body references `/gsd-verify-work` (this is the historical-summary case for Open Q #4 — default behavior is to rewrite).
    8. `CLAUDE.md` — full marker set: project, stack, workflow sections, each with `<!-- GSD:<section>-start source:<file> -->` … `<!-- GSD:<section>-end -->` pairs. Workflow section references `/gsd-quick`, `/gsd-debug`, `/gsd-execute-phase`.
    9. `AGENTS.md` — same marker set, distinct prose body so tests can verify per-file rewrites.
    10. `GEMINI.md` — same marker set.

    DO NOT add `oto_state_version` or `<!-- OTO:* -->` markers anywhere — these fixtures must be pure GSD-era. Conflict detection (REQ-MIG-06) gets its own ad-hoc fixture built inside the conflict test (see Task 7).

    Make sure every file ends with a trailing newline. No symlinks. Pure ASCII / UTF-8 markdown + JSON.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m='tests/fixtures/gsd-project-minimal/.planning/STATE.md'; const f='tests/fixtures/gsd-project-full/CLAUDE.md'; const sm=fs.readFileSync(m,'utf8'); const sf=fs.readFileSync(f,'utf8'); if(!sm.includes('gsd_state_version: 1.0')) process.exit(1); if(!sf.includes('&lt;!-- GSD:project-start')) process.exit(1); if(!sf.includes('/gsd-execute-phase')) process.exit(1); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/gsd-project-minimal/.planning/STATE.md` contains literal `gsd_state_version: 1.0`
    - `tests/fixtures/gsd-project-minimal/CLAUDE.md` contains literal `&lt;!-- GSD:project-start` AND `&lt;!-- GSD:project-end --&gt;`
    - `tests/fixtures/gsd-project-full/CLAUDE.md` contains literal `&lt;!-- GSD:project-start`, `&lt;!-- GSD:stack-start`, `&lt;!-- GSD:workflow-start`, AND `/gsd-execute-phase`
    - `tests/fixtures/gsd-project-full/AGENTS.md` and `tests/fixtures/gsd-project-full/GEMINI.md` both exist and both contain `&lt;!-- GSD:project-start`
    - `tests/fixtures/gsd-project-full/.planning/config.json` parses as valid JSON and contains the literal `gsd/phase-{phase}-{slug}` value
    - No file in either fixture contains the literals `oto_state_version` or `&lt;!-- OTO:`
    - `grep -r 'oto_state_version' tests/fixtures/gsd-project-minimal tests/fixtures/gsd-project-full` returns 0 hits
  </acceptance_criteria>
  <done>Both fixture trees exist with the exact files listed in `<files>`; verify command exits 0 with `OK`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the 10 migrate-*.test.cjs files (RED scaffold)</name>
  <files>
    tests/migrate-detect.test.cjs,
    tests/migrate-rename-map.test.cjs,
    tests/migrate-dry-run.test.cjs,
    tests/migrate-apply.test.cjs,
    tests/migrate-idempotent.test.cjs,
    tests/migrate-conflict.test.cjs,
    tests/migrate-instructions.test.cjs,
    tests/migrate-state-frontmatter.test.cjs,
    tests/migrate-cli.test.cjs,
    tests/migrate-command-md.test.cjs
  </files>
  <read_first>
    - tests/05-build-hooks.test.cjs (canonical test layout: `'use strict'`, `node:test`, `node:assert/strict`, REPO_ROOT pattern)
    - .planning/phases/01-add-oto-migrate-a-command-that-converts-a-gsd-era-project-s-/01-VALIDATION.md (Per-Task Verification Map and Wave 0 list — these test filenames are load-bearing)
    - rename-map.json (do_not_rename allowlist — relevant to migrate-apply.test.cjs assertion that LICENSE files are not touched)
    - oto/bin/lib/oto-tools.cjs lines 1240-1280 (existing dispatch pattern — migrate-cli.test.cjs spawns this binary)
  </read_first>
  <action>
    Create exactly the 10 listed test files. Every file must:
    1. Open with `'use strict';` then `const test = require('node:test');` and `const assert = require('node:assert/strict');`.
    2. Define `const REPO_ROOT = path.resolve(__dirname, '..');`.
    3. Use `fs.mkdtempSync(path.join(os.tmpdir(), 'oto-migrate-'))` for any test that mutates fs; copy the fixture in via `fs.cpSync(src, tmp, {recursive:true})`.
    4. Require the not-yet-existing module via `const migrate = require(path.join(REPO_ROOT, 'oto/bin/lib/migrate.cjs'));` — this is what makes Wave 0 RED.
    5. Use `t.after(() => fs.rmSync(tmp, {recursive:true, force:true}));` for cleanup.

    **Test bodies (one per file, minimal but specific — each asserts the precise behavior its REQ-ID requires):**

    1. `migrate-detect.test.cjs` — `test('detectGsdProject returns isGsdEra=true on gsd-project-minimal fixture')`. Asserts `result.isGsdEra === true`, `result.signals` includes `'gsd-state-frontmatter'`, `result.signals` includes `'gsd-instruction-markers'`. Second sub-test: a non-GSD tmpdir (just an empty dir with a README.md) → `isGsdEra === false`.

    2. `migrate-rename-map.test.cjs` — Asserts that `migrate.dryRun(fixtureDir)` returns a result whose `.summary.engine_invocation` records `mapPath` ending in `rename-map.json` AND `target` equal to fixtureDir AND `mode === 'dry-run'`. (The implementation will record this in its return value for testability.)

    3. `migrate-dry-run.test.cjs` — Asserts `dryRun(fixture)` returns `{mode:'dry-run', files:[...], exitCode:0}` AND that NO file under fixture was modified (compare `fs.statSync(...).mtime` before/after to within 100ms; or compute SHA before/after on every file). Also asserts the report has classified rule_types — at least one of `identifier`, `command`, `frontmatter`, `marker`.

    4. `migrate-apply.test.cjs` — `apply(fixture, {})` then assert: post-apply, `STATE.md` contains `oto_state_version` (not `gsd_state_version`); `CLAUDE.md` contains `<!-- OTO:project-start` (not `<!-- GSD:project-start`); `01-PLAN.md` contains `/oto-execute-phase` (not `/gsd-execute-phase`). Also assert no file under tests/fixtures/gsd-project-minimal/ at the source (REPO_ROOT path) was mutated.

    5. `migrate-idempotent.test.cjs` — `apply(tmp1, {})` then `apply(tmp1, {})` again; second-apply result MUST have `result.filesChanged.length === 0`. Also recursive SHA-256 manifest before second run === manifest after second run.

    6. `migrate-conflict.test.cjs` — Build a half-migrated fixture inline: copy gsd-project-minimal then rewrite `CLAUDE.md` to inject one `<!-- OTO:foo-start -->` while preserving `<!-- GSD:project-start -->`. Assert `apply(tmp, {})` THROWS or returns nonzero exitCode AND error message mentions `half-migrated` or `conflict`. Assert `apply(tmp, {force:true})` succeeds.

    7. `migrate-instructions.test.cjs` — On gsd-project-full fixture, after `apply()`: `CLAUDE.md`, `AGENTS.md`, `GEMINI.md` each have ZERO `<!-- GSD:` substrings AND each have at least 3 `<!-- OTO:` substrings (project, stack, workflow markers). Each file's `source:<file>` attribute on start tags is preserved (e.g., `<!-- OTO:project-start source:PROJECT.md -->`).

    8. `migrate-state-frontmatter.test.cjs` — Read pre-apply STATE.md, assert it has `gsd_state_version: 1.0` literal. Run apply. Read post-apply, assert `oto_state_version: 1.0` literal AND zero occurrences of `gsd_state_version`. Verify the YAML frontmatter delimiter `---` lines are preserved exactly (not reformatted).

    9. `migrate-cli.test.cjs` — Spawn `node oto/bin/lib/oto-tools.cjs migrate --dry-run` with `cwd = tmpFixture`. Assert `result.status === 0` AND stdout contains some recognizable token (`dry-run` or `files` or `migrate`). Then spawn `node ... migrate --apply` and assert `result.status === 0` AND CLAUDE.md was rewritten. Then spawn `node oto/bin/lib/oto-tools.cjs migrate` (no flag) on a non-GSD tmpdir and assert `result.status !== 0`.

    10. `migrate-command-md.test.cjs` — Read `oto/commands/oto/migrate.md` and assert: file exists, frontmatter contains `name: oto:migrate`, `description:` is non-empty, `allowed-tools:` block lists at least `Read`, `Write`, `Edit`, `Bash`. Body contains `<objective>` and `<execution_context>` XML tags (matching the convention used by oto/commands/oto/import.md).

    **Important:** All 10 tests will fail on first `npm test` run because `oto/bin/lib/migrate.cjs` and `oto/commands/oto/migrate.md` do not yet exist. That is the desired Wave 0 state. Wave 1 implements the module, Wave 2 implements the command markdown.
  </action>
  <verify>
    <automated>node --test --test-concurrency=4 tests/migrate-*.test.cjs 2>&amp;1 | tail -20 ; echo "expected: failures (RED) — tests reference oto/bin/lib/migrate.cjs which does not yet exist"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 listed test files exist with non-empty bodies
    - Every test file contains the literal string `require('node:test')`
    - Every test file contains the literal string `oto/bin/lib/migrate.cjs` (or path.join equivalent resolving to it)
    - `node --test tests/migrate-*.test.cjs` exits non-zero (RED state expected; this is correct for Wave 0)
    - The failure output mentions `Cannot find module` or similar for `migrate.cjs` (proving the tests are wired against the contract)
    - tests/migrate-command-md.test.cjs references the literal `oto/commands/oto/migrate.md` path
  </acceptance_criteria>
  <done>10 test files exist; running `node --test tests/migrate-*.test.cjs` produces failures whose root cause is the missing `oto/bin/lib/migrate.cjs` module (and missing `oto/commands/oto/migrate.md` for test 10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture file writes | All writes occur under `tests/fixtures/...` (in-repo, source-controlled) and `os.tmpdir()/oto-migrate-*` (test scratch). No writes outside these prefixes. |
| Test process boundary | Tests spawn `oto/bin/lib/oto-tools.cjs` via `child_process.spawnSync` with explicit `cwd` set to the tmpdir copy of a fixture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | Fixture build script | accept | Fixtures are static markdown/JSON committed to git; reviewable by diff. No executable bits set. |
| T-01-02 | I (Information disclosure) | Tests on tmpdir fixtures | mitigate | Use `fs.mkdtempSync` (mode 0700 on POSIX); cleanup in `t.after`. No PII or secrets in fixtures. |
| T-01-03 | D (Denial of service) | RED tests blocking CI | accept | Wave 0 is intentionally RED. Wave 1+ turns it green within the same phase before merge. |

No new code paths in production-shipped artifacts; this plan only adds tests + fixtures + zero runtime imports.
</threat_model>

<verification>
1. `find tests/fixtures/gsd-project-minimal tests/fixtures/gsd-project-full -type f | wc -l` returns at least 14 (4 minimal + 10 full)
2. `ls tests/migrate-*.test.cjs | wc -l` equals 10
3. `node --test tests/migrate-*.test.cjs` exits non-zero (expected RED)
4. `npm test` does not regress non-migrate tests: `node --test tests/*.test.cjs --test-name-pattern='^(?!migrate)'` (or filter inverse) — existing 418-test baseline still passes for non-migrate files
</verification>

<success_criteria>
- 10 `tests/migrate-*.test.cjs` files exist matching the VALIDATION.md Wave 0 list verbatim
- 2 fixture trees exist matching the directory layouts in Task 1
- Tests are wired to require `oto/bin/lib/migrate.cjs` (proving the contract) and `oto/commands/oto/migrate.md` (for the surface test)
- Pre-existing 418-test baseline is unaffected
- Plan committed to git via `oto-tools commit`
</success_criteria>

<output>
After completion, create `.planning/phases/01-add-oto-migrate-a-command-that-converts-a-gsd-era-project-s-/01-01-SUMMARY.md` summarizing: which 10 test files were created, fixture-tree file counts, and the expected Wave 1 implementation surface (detectGsdProject / dryRun / apply / main).
</output>
