---
phase: 3
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/bin/lib/model-profiles.cjs
  - .github/workflows/install-smoke.yml
  - scripts/install-smoke.cjs
  - tests/phase-04-mr01-install-smoke.test.cjs
autonomous: true
requirements: [INST-03, PRTY-01]
tags: [installer, ci, install-smoke, expected-agents]

must_haves:
  truths:
    - "`EXPECTED_AGENTS` in `oto/bin/lib/model-profiles.cjs` has exactly 26 entries and includes `oto-doc-classifier`, `oto-doc-synthesizer`, and `oto-eval-auditor` alphabetically interleaved."
    - "`tests/phase-04-mr01-install-smoke.test.cjs` (which loops over `EXPECTED_AGENTS`) asserts all 26 retained agents install for Claude with no manual additions needed."
    - "`tests/phase-04-mr01-install-smoke.test.cjs` additionally asserts the two restored Claude command files (`commands/oto/ingest-docs.md`, `commands/oto/eval-review.md`) install in the Claude config dir (PRTY-01 Claude branch)."
    - "`.github/workflows/install-smoke.yml` `smoke-tarball` AND `smoke-unpacked` jobs each contain a step that installs `oto` for each of `claude`/`codex`/`gemini` into an isolated `mktemp -d` config dir and asserts the three new agent files are present per runtime."
    - "For the Codex branch, the CI step additionally asserts each new agent's `.toml` declares the correct `sandbox_mode` (classifier `read-only`, synthesizer `workspace-write`, auditor `read-only`)."
    - "`pull_request: paths:` filter in `install-smoke.yml` includes `oto/agents/**` so agent-file changes trigger the smoke."
    - "All steps fail loudly (`set -euo pipefail` + explicit `exit 1` on missing files) — a deleted agent file MUST cause CI red."
  artifacts:
    - path: "oto/bin/lib/model-profiles.cjs"
      provides: "Updated EXPECTED_AGENTS array (23 → 26 entries)"
      contains: "oto-doc-classifier"
    - path: ".github/workflows/install-smoke.yml"
      provides: "CI install-smoke step that asserts 3 new agent files per runtime"
      contains: "oto-doc-classifier"
    - path: "tests/phase-04-mr01-install-smoke.test.cjs"
      provides: "Claude parity smoke (PRTY-01 Claude branch) — asserts 2 restored command files + EXPECTED_AGENTS-driven 26 agents"
      contains: "ingest-docs.md"
    - path: "scripts/install-smoke.cjs"
      provides: "Local smoke driver that mirrors CI assertions"
      contains: "oto-doc-classifier"
  key_links:
    - from: "tests/phase-04-mr01-install-smoke.test.cjs"
      to: "oto/bin/lib/model-profiles.cjs"
      via: "require('../oto/bin/lib/model-profiles').EXPECTED_AGENTS"
      pattern: "EXPECTED_AGENTS"
    - from: ".github/workflows/install-smoke.yml"
      to: "oto install --claude/--codex/--gemini --config-dir <tmp>"
      via: "bash run step + test -f on agent files"
      pattern: "oto-doc-classifier"
---

<objective>
Land INST-03 by bumping `EXPECTED_AGENTS` from the stale 23-entry list to the correct 26-entry list, extending `install-smoke.yml` to assert the three new agent files exist in each runtime's config dir under both install paths (tarball + unpacked), and landing PRTY-01's Claude branch by adding `/oto-ingest-docs` + `/oto-eval-review` command-file assertions to `tests/phase-04-mr01-install-smoke.test.cjs`.

Purpose: Close the load-bearing INST-03 gap (research Pitfall #1 — the stale array silently passes install-smoke today) and assert at the CI YAML surface that all three runtimes ship the restored agents.
Output: One module update, one CI YAML update, one local-smoke script update, and one Claude-side parity assertion extension.

**Collision resolution note:** This plan owns `tests/phase-04-mr01-install-smoke.test.cjs` exclusively — Plan 03 (Codex + Gemini PRTY-01) does NOT touch this file. This eliminates the file collision identified in the recommended plan shape and keeps Wave 1 fully parallel.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-VALIDATION.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@.planning/phases/01-agent-ports-installer-wiring/01-VERIFICATION.md
@CLAUDE.md
@oto/bin/lib/model-profiles.cjs
@.github/workflows/install-smoke.yml
@scripts/install-smoke.cjs
@tests/phase-04-mr01-install-smoke.test.cjs
@tests/fixtures/phase-04/retained-agents.json
@bin/lib/runtime-codex.cjs
@oto/agents/oto-doc-classifier.md
@oto/agents/oto-doc-synthesizer.md
@oto/agents/oto-eval-auditor.md

<interfaces>
<!-- Target EXPECTED_AGENTS array shape (research Pattern 3, Code Examples section) -->

Current state of `oto/bin/lib/model-profiles.cjs` lines 29-53:
- `const EXPECTED_AGENTS = [ ... ];` — 23 entries, missing the 3 new agents.

Target state (26 entries, alphabetical, three new entries marked):
```javascript
const EXPECTED_AGENTS = [
  'oto-advisor-researcher',
  'oto-assumptions-analyzer',
  'oto-code-fixer',
  'oto-code-reviewer',
  'oto-codebase-mapper',
  'oto-debugger',
  'oto-doc-classifier',     // v0.3.0 Phase 1 restoration
  'oto-doc-synthesizer',    // v0.3.0 Phase 1 restoration
  'oto-doc-verifier',
  'oto-doc-writer',
  'oto-domain-researcher',
  'oto-eval-auditor',       // v0.3.0 Phase 1 restoration
  'oto-executor',
  'oto-integration-checker',
  'oto-nyquist-auditor',
  'oto-phase-researcher',
  'oto-plan-checker',
  'oto-planner',
  'oto-project-researcher',
  'oto-research-synthesizer',
  'oto-roadmapper',
  'oto-security-auditor',
  'oto-ui-auditor',
  'oto-ui-checker',
  'oto-ui-researcher',
  'oto-verifier',
];
```

Executor MUST verify alphabetic correctness by reading the existing 23-entry array first — if the existing entries differ from this expectation, preserve the existing order and insert only the 3 new entries alphabetically. The 26-entry target is authoritative on count, not on specific entries beyond the three new ones.

Codex sandbox locks (from `bin/lib/runtime-codex.cjs:90-92`, already locked in Phase 1 D-04 — DO NOT modify):
- `'oto-doc-classifier': 'read-only'`
- `'oto-doc-synthesizer': 'workspace-write'`
- `'oto-eval-auditor': 'read-only'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update EXPECTED_AGENTS (23 → 26) in oto/bin/lib/model-profiles.cjs</name>
  <files>oto/bin/lib/model-profiles.cjs</files>
  <read_first>
    - oto/bin/lib/model-profiles.cjs (lines 29-53 — the current EXPECTED_AGENTS array; read FULL existing array to preserve alphabetic ordering and any inline comments)
    - tests/fixtures/phase-04/retained-agents.json (already has the correct 26 entries — cross-reference to confirm the three new agents match the fixture exactly)
    - tests/phase-04-mr01-install-smoke.test.cjs (the consumer — confirm it iterates EXPECTED_AGENTS, so a length change propagates without further test changes)
    - oto/agents/ (directory listing — confirm the three new agent files exist: oto-doc-classifier.md, oto-doc-synthesizer.md, oto-eval-auditor.md)
  </read_first>
  <action>
    In `oto/bin/lib/model-profiles.cjs`, locate `const EXPECTED_AGENTS = [` (around line 29). Insert these three string entries in alphabetic position within the array:
    - `'oto-doc-classifier',` (alphabetically between `'oto-debugger'` and `'oto-doc-verifier'`)
    - `'oto-doc-synthesizer',` (alphabetically between `'oto-doc-classifier'` and `'oto-doc-verifier'`)
    - `'oto-eval-auditor',` (alphabetically between `'oto-domain-researcher'` and `'oto-executor'`)

    Use an inline comment on each new line: `// v0.3.0 Phase 1 restoration` so future readers see the provenance.

    Resulting array length MUST be exactly 26. Do NOT change any other entries, do NOT reorder existing entries.

    After editing, verify:
    ```bash
    node -e "console.log(require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS.length)"
    # Must print 26
    node -e "const a = require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS; ['oto-doc-classifier','oto-doc-synthesizer','oto-eval-auditor'].forEach(x => { if (!a.includes(x)) { console.error('MISSING:', x); process.exit(1); } }); console.log('ALL THREE PRESENT');"
    ```

    Then run the install-smoke test focused: `node --test tests/phase-04-mr01-install-smoke.test.cjs`. It must still pass (this test does `npm pack` + install, takes ~30-60 seconds; let it complete).
  </action>
  <verify>
    <automated>node -e "const a = require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS; if (a.length !== 26) { console.error('LEN:', a.length); process.exit(1); } ['oto-doc-classifier','oto-doc-synthesizer','oto-eval-auditor'].forEach(x => { if (!a.includes(x)) { console.error('MISSING:', x); process.exit(1); } }); console.log('OK 26');" &amp;&amp; node --test tests/phase-04-mr01-install-smoke.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS.length === 26` (verified via node -e).
    - All three new agent identifiers (`oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor`) appear in the array.
    - `grep -c "'oto-" oto/bin/lib/model-profiles.cjs | head -1` reflects 26 quoted oto-* entries inside the EXPECTED_AGENTS section.
    - `node --test tests/phase-04-mr01-install-smoke.test.cjs` exits 0.
    - The existing 23 entries are preserved (no reordering, no deletions) — verify by diffing against pre-change.
  </acceptance_criteria>
  <done>
    EXPECTED_AGENTS has exactly 26 entries with the three restored agents alphabetically placed; the install-smoke test that iterates this array passes green for all 26 agents.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/phase-04-mr01-install-smoke.test.cjs with PRTY-01 Claude command-file assertions</name>
  <files>tests/phase-04-mr01-install-smoke.test.cjs</files>
  <read_first>
    - tests/phase-04-mr01-install-smoke.test.cjs (FULL file — understand the existing `npm pack` + install flow, how `EXPECTED_AGENTS` is consumed, and the test patterns already in use; find where the loop over EXPECTED_AGENTS ends, that's the natural insertion point for new command-file assertions)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 4" + "Open Questions #2" (the per-runtime parity shape and the recommendation to extend phase-04-mr01 rather than fork a new Claude-specific smoke file)
    - oto/commands/oto/ingest-docs.md and oto/commands/oto/eval-review.md (the two files being asserted as installed)
  </read_first>
  <action>
    Locate the existing test that loops over `EXPECTED_AGENTS` and asserts each agent file installs in the Claude config dir. Append a NEW test block (sibling describe or test, matching the existing file's style) named `'PRTY-01: /oto-ingest-docs and /oto-eval-review command files install (claude)'` that:

    1. Reuses the same `--config-dir <tmp>` install dir already set up by the test (if the test currently does install setup in `before` hooks, reuse the tmp dir reference — DO NOT do a second `npm pack` + install). If the existing test does install per-test, add the new assertions as a separate test inside the same describe block so it reuses the cached install.

    2. For each of these two relative paths inside the Claude config dir:
       - `commands/oto/ingest-docs.md`
       - `commands/oto/eval-review.md`

       Assert `fs.existsSync(path.join(<tmpClaudeDir>, relativePath))` is true. Failure message must include the runtime name (Claude) and the missing path.

    3. Additionally assert these two files do NOT contain the deferral framing token `intentionally non-executable` (regression-guard that Phase 2's de-deferral propagates through install).

    Do NOT modify the existing EXPECTED_AGENTS loop or any other pre-existing assertions. The new block is purely additive.

    Verify locally: `node --test tests/phase-04-mr01-install-smoke.test.cjs`. Must pass green and include the new test in the output.
  </action>
  <verify>
    <automated>node --test tests/phase-04-mr01-install-smoke.test.cjs 2>&amp;1 | grep -E 'PRTY-01.*ingest-docs.*eval-review.*claude'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PRTY-01" tests/phase-04-mr01-install-smoke.test.cjs` exits 0.
    - `grep -q "commands/oto/ingest-docs.md" tests/phase-04-mr01-install-smoke.test.cjs` exits 0.
    - `grep -q "commands/oto/eval-review.md" tests/phase-04-mr01-install-smoke.test.cjs` exits 0.
    - `grep -q "intentionally non-executable" tests/phase-04-mr01-install-smoke.test.cjs` exits 0 (as a negative-assertion regex, NOT as the production source).
    - `node --test tests/phase-04-mr01-install-smoke.test.cjs` exits 0 with the new test included in the output count.
  </acceptance_criteria>
  <done>
    `tests/phase-04-mr01-install-smoke.test.cjs` includes a PRTY-01 Claude block asserting the two restored Claude command files install AND lack deferral framing; full test file still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend scripts/install-smoke.cjs with per-runtime agent-file assertions</name>
  <files>scripts/install-smoke.cjs</files>
  <read_first>
    - scripts/install-smoke.cjs (FULL file — current Claude-only smoke; understand how it invokes `oto install --claude --config-dir <tmp>` and what it asserts today)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 3" (two-surface install-smoke approach)
    - bin/lib/runtime-codex.cjs (lines 90-92 — Codex sandbox map for the three agents; values needed for the per-agent TOML assertions in the codex branch)
  </read_first>
  <action>
    Extend `scripts/install-smoke.cjs` so that after the existing Claude smoke install, it ALSO runs the equivalent for Codex and Gemini and asserts agent files exist per runtime. The script should:

    1. Define a constant near the top: `const NEW_AGENTS = ['oto-doc-classifier', 'oto-doc-synthesizer', 'oto-eval-auditor'];`

    2. For each runtime in `['claude', 'codex', 'gemini']`:
       a. Create a fresh tmp dir via `fs.mkdtempSync(path.join(os.tmpdir(), `oto-smoke-${runtime}-`))`.
       b. Spawn `oto install --${runtime} --config-dir ${tmp}` and require exit code 0.
       c. For each agent in `NEW_AGENTS`: assert `fs.existsSync(path.join(tmp, 'agents', `${agent}.md`))` — if missing, throw an error containing the runtime and agent name.
       d. For the `codex` runtime ONLY: additionally read `path.join(tmp, 'agents', `${agent}.toml`)`, assert it exists, and grep-equivalent for the expected `sandbox_mode = "<mode>"` per agent (classifier `read-only`, synthesizer `workspace-write`, auditor `read-only`). Use `content.match(/sandbox_mode\s*=\s*"([^"]+)"/)` and compare captured group.
       e. Clean up the tmp dir via `fs.rmSync(tmp, { recursive: true, force: true })`.

    3. Preserve all existing Claude assertions (the state file existence check, the CLAUDE.md marker check) — DO NOT remove them; just expand coverage.

    4. Make the script idempotent: if any assertion fails, throw with a clear `FAIL: <runtime> <reason>` message and exit non-zero. On success, print `PASS: 3 new agents installed across claude/codex/gemini` once at the end.

    5. If the Codex or Gemini install fails because the runtime adapter does something the Claude adapter doesn't (e.g., requires different config bootstrap), surface the spawn output verbatim — the script's job is to ASSERT, not to skip.

    Verify locally by running the script directly:
    ```bash
    node scripts/install-smoke.cjs
    ```
    It should exit 0 and print the PASS line.
  </action>
  <verify>
    <automated>node scripts/install-smoke.cjs 2>&amp;1 | grep -E 'PASS:.*claude.*codex.*gemini'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "NEW_AGENTS" scripts/install-smoke.cjs` exits 0.
    - `grep -c "oto-doc-classifier" scripts/install-smoke.cjs` >= 1.
    - `grep -q "sandbox_mode" scripts/install-smoke.cjs` exits 0 (Codex TOML assertion present).
    - `grep -q "'claude', 'codex', 'gemini'" scripts/install-smoke.cjs` OR `grep -q '"claude", "codex", "gemini"' scripts/install-smoke.cjs` exits 0 (all three runtimes iterated).
    - `node scripts/install-smoke.cjs` exits 0 and stdout contains `PASS:` (use `node scripts/install-smoke.cjs &amp;&amp; echo SCRIPT_OK`).
  </acceptance_criteria>
  <done>
    `scripts/install-smoke.cjs` covers all three runtimes, asserts each new agent file is installed, and for Codex additionally locks the per-agent sandbox_mode per Phase 1 D-04. Script exits 0 against a clean repo.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend .github/workflows/install-smoke.yml — both smoke jobs assert 3 new agents per runtime</name>
  <files>.github/workflows/install-smoke.yml</files>
  <read_first>
    - .github/workflows/install-smoke.yml (FULL file — understand current paths filter, jobs, steps, pinned action SHAs; identify the `smoke-tarball` and `smoke-unpacked` jobs and where the post-install assertion belongs)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 3" Code Example (the exact bash step shape with `set -euo pipefail` and the `for RUNTIME in claude codex gemini` loop, plus the Codex sandbox_mode grep block)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md Pitfall #7 (the loud-failure pattern — every assertion MUST `exit 1` on failure, not just echo)
  </read_first>
  <action>
    Edit `.github/workflows/install-smoke.yml`:

    Step A — Update `pull_request: paths:` filter. Locate the `paths:` array under `on: pull_request:`. Add the line `      - 'oto/agents/**'` (preserve the existing entries: `bin/**`, `package.json`, `scripts/install-smoke.cjs`, `.github/workflows/install-smoke.yml`, `.github/workflows/release.yml`). This ensures the smoke fires when agent files change.

    Step B — Add an assertion step to BOTH the `smoke-tarball` job AND the `smoke-unpacked` job, placed AFTER the existing step that installs `oto` (so `oto` is on PATH). The step body is:

    ```yaml
    - name: Assert v0.3.0 restored agents install per runtime (INST-03)
      shell: bash
      run: |
        set -euo pipefail
        AGENTS="oto-doc-classifier oto-doc-synthesizer oto-eval-auditor"
        for RUNTIME in claude codex gemini; do
          TMP=$(mktemp -d)
          oto install "--$RUNTIME" --config-dir "$TMP"
          for AGENT in $AGENTS; do
            if [ ! -f "$TMP/agents/$AGENT.md" ]; then
              echo "FAIL: $RUNTIME missing $AGENT.md at $TMP/agents/"
              ls -la "$TMP/agents/" || true
              exit 1
            fi
          done
          if [ "$RUNTIME" = "codex" ]; then
            grep -q 'sandbox_mode[[:space:]]*=[[:space:]]*"read-only"' "$TMP/agents/oto-doc-classifier.toml" || { echo "FAIL: codex classifier sandbox_mode"; exit 1; }
            grep -q 'sandbox_mode[[:space:]]*=[[:space:]]*"workspace-write"' "$TMP/agents/oto-doc-synthesizer.toml" || { echo "FAIL: codex synthesizer sandbox_mode"; exit 1; }
            grep -q 'sandbox_mode[[:space:]]*=[[:space:]]*"read-only"' "$TMP/agents/oto-eval-auditor.toml" || { echo "FAIL: codex auditor sandbox_mode"; exit 1; }
          fi
          rm -rf "$TMP"
        done
        echo "PASS: 3 new agents installed across claude/codex/gemini"
    ```

    Step C — Do NOT touch the pinned action SHAs (`actions/checkout@…`, `actions/setup-node@…`). Do NOT change job-level `runs-on` values. Do NOT add new jobs (extend existing ones).

    Step D — Validate YAML syntax locally:
    ```bash
    node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/install-smoke.yml','utf8'));"
    ```
    If `js-yaml` is not available, use any alternative YAML parser; the goal is to confirm the file still parses. If no parser is available, at minimum confirm `git diff` shows the file has clean indentation and matching `name: …` step headers.

    Step E — Run the test suite to confirm no regression elsewhere: `node --test tests/phase-04-mr01-install-smoke.test.cjs`.
  </action>
  <verify>
    <automated>grep -q "oto-doc-classifier" .github/workflows/install-smoke.yml &amp;&amp; grep -q "oto-doc-synthesizer" .github/workflows/install-smoke.yml &amp;&amp; grep -q "oto-eval-auditor" .github/workflows/install-smoke.yml &amp;&amp; grep -q "oto/agents/\*\*" .github/workflows/install-smoke.yml &amp;&amp; grep -c "Assert v0.3.0 restored agents install per runtime" .github/workflows/install-smoke.yml | grep -q "^2$" &amp;&amp; grep -q "set -euo pipefail" .github/workflows/install-smoke.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Assert v0.3.0 restored agents install per runtime" .github/workflows/install-smoke.yml` returns exactly `2` (once in each smoke job).
    - All three new agent names appear in the YAML.
    - `grep -q "oto/agents/\*\*" .github/workflows/install-smoke.yml` exits 0 (paths filter extended).
    - `grep -q "set -euo pipefail" .github/workflows/install-smoke.yml` exits 0 (loud-fail mode active).
    - `grep -q 'sandbox_mode.*workspace-write' .github/workflows/install-smoke.yml` exits 0 (Codex synthesizer assertion present).
    - `grep -q 'sandbox_mode.*read-only' .github/workflows/install-smoke.yml` exits 0 (Codex read-only assertion present).
    - The YAML parses cleanly via any YAML loader OR a `gh workflow lint` equivalent if available.
    - `node --test tests/phase-04-mr01-install-smoke.test.cjs` still exits 0 (no regression on the in-suite path).
  </acceptance_criteria>
  <done>
    `install-smoke.yml` covers all three runtimes in BOTH smoke jobs with loud-fail bash assertions, the paths filter triggers on agent file changes, Codex's per-agent sandbox_mode is locked at the CI surface, and the existing in-suite smoke test still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI bash step → installed `oto` CLI | The smoke step invokes the just-built `oto` against `mktemp -d` config dirs. Tmpdirs are isolated; no pollution of `~/.claude`/`~/.codex`/`~/.gemini`. |
| `EXPECTED_AGENTS` array → test loop | A single source of truth consumed by `tests/phase-04-mr01-install-smoke.test.cjs`. Stale = silent under-assertion. |
| `paths:` filter in PR triggers | Determines whether install-smoke runs on agent-file PRs. Too narrow = bugs ship; too wide = wasted CI minutes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Tampering | `EXPECTED_AGENTS` count drift | mitigate | Task 1 acceptance enforces `length === 26` via node -e; Task 1 also requires all three new identifiers to be present. |
| T-3-06 | Repudiation / silent pass | Bash assertion without `exit 1` | mitigate | Task 4 mandates `set -euo pipefail` + explicit `exit 1` inside each `if [ ! -f … ]` branch; acceptance greps for both. |
| T-3-07 | Information disclosure | `mktemp -d` tmp dirs leak between runners | accept | Tmp dirs are cleaned with `rm -rf`; CI runners are ephemeral; no PII or secrets in install fixtures. |
| T-3-08 | Tampering | Codex sandbox_mode regression | mitigate | Task 4 asserts each of three sandbox_mode lines in the Codex `.toml` files at CI surface; Task 3 mirrors the assertion in the local script. Two independent assertion surfaces (CI + local). |

All threats are medium severity; phase scope is CI YAML + test extension + one constant bump.
</threat_model>

<verification>
- `node -e "require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS.length"` prints `26`.
- `node scripts/install-smoke.cjs` exits 0 with `PASS:` line.
- `node --test tests/phase-04-mr01-install-smoke.test.cjs` exits 0.
- `install-smoke.yml` greps return all three agent names in 2 instances (one per job).
</verification>

<success_criteria>
- INST-03 satisfied at three independent surfaces: the EXPECTED_AGENTS array (in-suite test), the local smoke script, and the CI YAML (both tarball + unpacked jobs).
- PRTY-01 Claude branch satisfied: two restored Claude command files asserted in `tests/phase-04-mr01-install-smoke.test.cjs`.
- Codex sandbox_mode locks (classifier read-only, synthesizer workspace-write, auditor read-only) asserted at both the CI surface (Task 4) and the local script (Task 3).
- No file collision with Plan 03 (which owns only the codex + gemini integration test files).
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-install-smoke-parity-adr-15/03-02-SUMMARY.md` recording:
- The before/after `EXPECTED_AGENTS.length` numbers and the three inserted entries with their alphabetic neighbors.
- The two job names in `install-smoke.yml` that received the new step (smoke-tarball, smoke-unpacked).
- Any deviations from the research recommendations (e.g., a different sandbox_mode regex shape if the actual `.toml` files use different spacing).
- Confirmation that `tests/phase-04-mr01-install-smoke.test.cjs` still passes with both EXPECTED_AGENTS-driven and new PRTY-01 assertions.
</output>
