---
phase: 3
plan: 3
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - tests/phase-08-smoke-codex.integration.test.cjs
  - tests/phase-08-smoke-gemini.integration.test.cjs
autonomous: true
requirements: [PRTY-01]
tags: [parity, integration, codex, gemini, sandbox]

must_haves:
  truths:
    - "`tests/phase-08-smoke-codex.integration.test.cjs` asserts (on a fresh `--config-dir <tmp>` install) that both `skills/oto-ingest-docs/SKILL.md` and `skills/oto-eval-review/SKILL.md` are present."
    - "The same Codex test asserts all three new agent `.md` files install AND that each agent's `.toml` declares the correct `sandbox_mode` per Phase 1 D-04 (classifier `read-only`, synthesizer `workspace-write`, auditor `read-only`)."
    - "The same Codex test asserts that `commands/oto/ingest-docs.md` and `commands/oto/eval-review.md` are NOT present on the Codex install (Codex uses the skills/* surface — research Pitfall #4)."
    - "`tests/phase-08-smoke-gemini.integration.test.cjs` asserts the two restored commands install at `commands/oto/ingest-docs.md` AND `commands/oto/eval-review.md` (W2 lock — `.md` extension per existing in-repo Gemini test precedent at lines 60-62 asserting `commands/oto/help.md`)."
    - "Both tests gracefully self-skip when the runtime CLI is not on PATH (existing `probeCodex()` / `probeGemini()` pattern preserved)."
    - "Neither test calls `oto-sdk` (SDK-DEFER-01 — runtime smoke does file-existence checks only)."
    - "Plan 03 runs AFTER Plan 02 (`depends_on: ['03-02']`) so the full-suite `npm test` regression check in Task 3 sees the EXPECTED_AGENTS bump and doesn't race against Plan 02's in-flight changes (W3 lock)."
  artifacts:
    - path: "tests/phase-08-smoke-codex.integration.test.cjs"
      provides: "PRTY-01 Codex parity assertions (skill files + 3 agent md + 3 sandbox_mode locks)"
      contains: "oto-ingest-docs"
    - path: "tests/phase-08-smoke-gemini.integration.test.cjs"
      provides: "PRTY-01 Gemini parity assertions (2 command files + 3 agent md)"
      contains: "oto-ingest-docs"
  key_links:
    - from: "tests/phase-08-smoke-codex.integration.test.cjs"
      to: "Codex install --config-dir <tmp>"
      via: "spawnSync + fs.existsSync"
      pattern: "skills/oto-ingest-docs"
    - from: "tests/phase-08-smoke-codex.integration.test.cjs"
      to: "Phase 1 D-04 sandbox locks"
      via: "fs.readFileSync + sandbox_mode regex"
      pattern: "sandbox_mode"
    - from: "tests/phase-08-smoke-gemini.integration.test.cjs"
      to: "Gemini install --config-dir <tmp>"
      via: "spawnSync + fs.existsSync"
      pattern: "commands/oto/ingest-docs.md"
---

<objective>
Land PRTY-01's Codex and Gemini branches by extending the existing per-runtime integration smoke tests to assert that both restored commands (`/oto-ingest-docs`, `/oto-eval-review`) and all three new agents install per-runtime, and that Codex's per-agent `.toml` files declare the correct `sandbox_mode` per Phase 1 D-04.

Purpose: Close PRTY-01 at the per-runtime integration surface. The Claude branch is owned by Plan 02 (extending `tests/phase-04-mr01-install-smoke.test.cjs`) — this plan does NOT touch that file or `oto/bin/lib/model-profiles.cjs`, eliminating all file collisions with Plan 02.

**Dependency note (W3):** Plan 03 depends on Plan 02 (`depends_on: ["03-02"]`) so Task 3's full-suite `npm test` regression check sees the bumped EXPECTED_AGENTS array. Without this edge, a Plan 03 landing before Plan 02 would assert against the stale 23-entry array and force a re-run. Conceptually Plan 03 is still part of Wave 1's parity work but is sequenced after Plan 02 within the wave — call it Wave 1b. (Alternative considered: move Plan 03 to Wave 2 with `depends_on: ["03-01", "03-02"]` and bump 03-04 to Wave 3. Rejected because it doubles wall-clock and 03-04 only needs the test+install-smoke outcomes recorded in SUMMARYs, which Plan 03 doesn't produce. Wave 1b is cleaner.)

Output: Two extended integration test files. No production code changes.
</objective>

<execution_context>
@~/.claude/oto/workflows/execute-plan.md
@~/.claude/oto/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md
@.planning/phases/03-tests-install-smoke-parity-adr-15/03-VALIDATION.md
@.planning/phases/01-agent-ports-installer-wiring/01-CONTEXT.md
@.planning/phases/01-agent-ports-installer-wiring/01-VERIFICATION.md
@CLAUDE.md
@tests/phase-08-smoke-codex.integration.test.cjs
@tests/phase-08-smoke-gemini.integration.test.cjs
@bin/lib/runtime-codex.cjs
@oto/agents/oto-doc-classifier.md
@oto/agents/oto-doc-synthesizer.md
@oto/agents/oto-eval-auditor.md
@oto/commands/oto/ingest-docs.md
@oto/commands/oto/eval-review.md

<interfaces>
<!-- Per-runtime layout (from research Pattern 4 — VERIFY against actual tests before encoding) -->

Codex install layout (verified via QUICK-260505-bxx + research Pattern 4):
- Commands: `skills/oto-<name>/SKILL.md` (NOT `commands/oto/<name>.md`)
- Agents: `agents/oto-<name>.md`
- Codex extras: `agents/oto-<name>.toml` with `sandbox_mode = "<read-only|workspace-write>"`

Gemini install layout (W2 locked — `.md` extension):
- Commands: `commands/oto/<name>.md` — confirmed by existing in-repo precedent at `tests/phase-08-smoke-gemini.integration.test.cjs` lines 60-62 which asserts `commands/oto/help.md`, `commands/oto/progress.md`, `commands/oto/new-project.md`.
- Agents: `agents/oto-<name>.md`
- **W2 decision:** Use `.md` extension for `ingest-docs` and `eval-review`. The earlier recommended-plan-shape suggestion of `.toml` is REJECTED because it contradicts the established in-repo Gemini convention. Do not deviate.

Phase 1 D-04 sandbox_mode targets (authoritative — already locked in `bin/lib/runtime-codex.cjs:90-92`):
| Agent | Expected sandbox_mode |
|-------|----------------------|
| oto-doc-classifier | read-only |
| oto-doc-synthesizer | workspace-write |
| oto-eval-auditor | read-only |

Pattern reference (research "Per-runtime parity extension" code example):
- For each agent, build the expected mode via:
  `const expectedMode = agent === 'oto-doc-synthesizer' ? 'workspace-write' : 'read-only';`
  Then assert `toml` matches `new RegExp('sandbox_mode\\s*=\\s*"' + expectedMode + '"')` with a message naming the agent and the expected mode.

Probe / skip pattern (existing — preserve):
- `probeCodex()` returns true if `codex --version` resolves; tests self-skip otherwise.
- `probeGemini()` returns true if `gemini --version` resolves; tests self-skip otherwise.

NO `oto-sdk` invocations from either test — SDK-DEFER-01 binding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Codex parity smoke with PRTY-01 assertions (skills + agent md + sandbox_mode)</name>
  <files>tests/phase-08-smoke-codex.integration.test.cjs</files>
  <read_first>
    - tests/phase-08-smoke-codex.integration.test.cjs (FULL file — understand `probeCodex()`, `freshTmpDir()`, the install helper, the existing skill-file assertion loop around lines 60-67, the existing `commands/oto/` negative assertion around lines 70-74)
    - bin/lib/runtime-codex.cjs (lines 90-92 — confirm `agentSandboxes` map values; the test asserts the installed `.toml` matches this map)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 4", the "Per-runtime parity extension" code example, and Pitfall #4
    - oto/agents/oto-doc-classifier.md, oto/agents/oto-doc-synthesizer.md, oto/agents/oto-eval-auditor.md (confirm names match)
  </read_first>
  <action>
    Append a new test to `tests/phase-08-smoke-codex.integration.test.cjs` with the exact name:
    `PRTY-01: /oto-ingest-docs and /oto-eval-review skill surfaces + 3 new agents install (codex)`

    The test body MUST:

    1. Probe-skip if Codex CLI is unavailable using the EXACT pattern already at the top of the file (read first, then mirror — e.g. `if (!probeCodex()) { t.skip('codex CLI not available'); return; }`). Do not invent a new pattern.

    2. Create an isolated tmp config dir via the existing helper (e.g. `freshTmpDir('oto-smoke-codex-prty-')`) and register cleanup via `t.after(() => fs.rmSync(tmp, { recursive: true, force: true }))`.

    3. Install Codex via the existing helper and assert exit status is 0 with a failure message that includes the install stderr.

    4. Assert both skill surfaces exist by looping over `['skills/oto-ingest-docs/SKILL.md', 'skills/oto-eval-review/SKILL.md']` and calling `assert.ok(fs.existsSync(path.join(tmp, skill)), '<skill> must install for codex')`.

    5. Negative assertion (Pitfall #4 lock) — loop over `['commands/oto/ingest-docs.md', 'commands/oto/eval-review.md']` and assert each path does NOT exist, with message `'codex must NOT install <wrongPath> (uses skills/* instead per QUICK-260505-bxx)'`.

    6. Agent + sandbox_mode assertions — for each entry of `{ 'oto-doc-classifier': 'read-only', 'oto-doc-synthesizer': 'workspace-write', 'oto-eval-auditor': 'read-only' }`:
       - Assert `agents/<agent>.md` exists in `tmp`.
       - Assert `agents/<agent>.toml` exists in `tmp`.
       - Read the `.toml` contents and assert it matches `new RegExp('sandbox_mode\\s*=\\s*"' + mode + '"')` with the failure message `'<agent>.toml must declare sandbox_mode = "<mode>" per Phase 1 D-04'`.

    Do NOT touch existing tests in the file. Do NOT remove the existing skill-file loop or the existing `commands/oto/` negative assertion. The new block is purely additive.

    Verify: `node --test tests/phase-08-smoke-codex.integration.test.cjs`. If Codex is on PATH, the test passes; if not, it self-skips (skipped count +1, no failure).
  </action>
  <verify>
    <automated>grep -q "PRTY-01" tests/phase-08-smoke-codex.integration.test.cjs &amp;&amp; grep -q "sandbox_mode" tests/phase-08-smoke-codex.integration.test.cjs &amp;&amp; grep -q "workspace-write" tests/phase-08-smoke-codex.integration.test.cjs &amp;&amp; grep -q "oto-doc-synthesizer" tests/phase-08-smoke-codex.integration.test.cjs &amp;&amp; node --test tests/phase-08-smoke-codex.integration.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PRTY-01" tests/phase-08-smoke-codex.integration.test.cjs` exits 0.
    - `grep -c "skills/oto-ingest-docs/SKILL.md" tests/phase-08-smoke-codex.integration.test.cjs` >= 1.
    - `grep -c "skills/oto-eval-review/SKILL.md" tests/phase-08-smoke-codex.integration.test.cjs` >= 1.
    - All three sandbox modes encoded somewhere in the test file: `grep -q '"read-only"' tests/phase-08-smoke-codex.integration.test.cjs` AND `grep -q '"workspace-write"' tests/phase-08-smoke-codex.integration.test.cjs`.
    - All three new agent names present: `grep -q "oto-doc-classifier" tests/phase-08-smoke-codex.integration.test.cjs` AND `grep -q "oto-doc-synthesizer" tests/phase-08-smoke-codex.integration.test.cjs` AND `grep -q "oto-eval-auditor" tests/phase-08-smoke-codex.integration.test.cjs`.
    - The negative assertion against `commands/oto/ingest-docs.md` (the wrong path) is present: `grep -q "must NOT install" tests/phase-08-smoke-codex.integration.test.cjs` exits 0.
    - `node --test tests/phase-08-smoke-codex.integration.test.cjs` exits 0 (test passes or self-skips; never fails).
  </acceptance_criteria>
  <done>
    Codex parity test asserts both skill surfaces, all three agent files (md + toml), all three sandbox modes per Phase 1 D-04, and locks the Pitfall #4 negative invariant (no `commands/oto/<name>.md` on Codex). Test passes or self-skips cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Gemini parity smoke with PRTY-01 assertions (2 commands + 3 agent md) — W2 LOCKED .md extension</name>
  <files>tests/phase-08-smoke-gemini.integration.test.cjs</files>
  <read_first>
    - tests/phase-08-smoke-gemini.integration.test.cjs (FULL file — understand `probeGemini()`, `freshTmpDir()`, the install helper, the existing `commands/oto/help.md` assertion at lines 60-62 which establishes the in-repo `.md` convention)
    - The Gemini install adapter under `bin/lib/runtime-gemini.cjs` (optional confirmation read — but the W2 decision is locked by the existing test convention, not by adapter inspection)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-RESEARCH.md "Pattern 4" (Gemini row of the per-runtime command-surface table)
  </read_first>
  <action>
    Append a new test to `tests/phase-08-smoke-gemini.integration.test.cjs` with the exact name:
    `PRTY-01: /oto-ingest-docs and /oto-eval-review command surfaces + 3 new agents install (gemini)`

    The test body MUST:

    1. Probe-skip if Gemini CLI is unavailable using the EXACT existing pattern (mirror what `probeGemini()` callers in the file already do).

    2. Create an isolated tmp config dir via the existing helper and register cleanup via `t.after`.

    3. Install Gemini via the existing helper and assert exit status is 0 with a stderr-inclusive failure message.

    4. **W2 LOCKED — `.md` extension.** Assert the two restored commands install at `commands/oto/ingest-docs.md` and `commands/oto/eval-review.md`. The earlier "Option A vs Option B" branch in this plan is removed — Option A (`.md`) is the only correct path because the existing test at lines 60-62 asserts `commands/oto/help.md`, `commands/oto/progress.md`, `commands/oto/new-project.md` — that is the established convention. Loop:
       ```javascript
       for (const cmd of ['ingest-docs', 'eval-review']) {
         const cmdPath = path.join(tmp, 'commands/oto', `${cmd}.md`);
         assert.ok(fs.existsSync(cmdPath), `${cmd}.md must install for gemini at ${cmdPath}`);
       }
       ```
       DO NOT use `.toml`. DO NOT branch on adapter inspection. The extension is locked by the in-repo precedent.

    5. Assert all three new agent `.md` files install: loop over `['oto-doc-classifier', 'oto-doc-synthesizer', 'oto-eval-auditor']` and `assert.ok(fs.existsSync(path.join(tmp, 'agents', agent + '.md')), agent + '.md must install for gemini')`.

    6. Do NOT assert sandbox_mode for Gemini (Codex-specific concept — Gemini agents do not carry the same TOML sandbox declaration).

    Do NOT touch existing tests. Purely additive.

    Verify: `node --test tests/phase-08-smoke-gemini.integration.test.cjs`. Passes if Gemini is on PATH; self-skips otherwise.
  </action>
  <verify>
    <automated>grep -q "PRTY-01" tests/phase-08-smoke-gemini.integration.test.cjs &amp;&amp; grep -q "ingest-docs.md" tests/phase-08-smoke-gemini.integration.test.cjs &amp;&amp; grep -q "eval-review.md" tests/phase-08-smoke-gemini.integration.test.cjs &amp;&amp; grep -q "oto-doc-classifier" tests/phase-08-smoke-gemini.integration.test.cjs &amp;&amp; ! grep -qE 'ingest-docs\.toml|eval-review\.toml' tests/phase-08-smoke-gemini.integration.test.cjs &amp;&amp; node --test tests/phase-08-smoke-gemini.integration.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PRTY-01" tests/phase-08-smoke-gemini.integration.test.cjs` exits 0.
    - `grep -q "ingest-docs.md" tests/phase-08-smoke-gemini.integration.test.cjs` exits 0 (W2 — `.md` extension present).
    - `grep -q "eval-review.md" tests/phase-08-smoke-gemini.integration.test.cjs` exits 0 (W2 — `.md` extension present).
    - **W2 negative lock:** `grep -qE 'ingest-docs\.toml|eval-review\.toml' tests/phase-08-smoke-gemini.integration.test.cjs` exits 1 (no `.toml` extension for these two commands).
    - All three new agent names present (`oto-doc-classifier`, `oto-doc-synthesizer`, `oto-eval-auditor`) — `grep -c "oto-doc-" tests/phase-08-smoke-gemini.integration.test.cjs` >= 2 and `grep -q "oto-eval-auditor" tests/phase-08-smoke-gemini.integration.test.cjs` exits 0.
    - `grep -q "sandbox_mode" tests/phase-08-smoke-gemini.integration.test.cjs` exits 1 (sandbox_mode is NOT asserted for Gemini — it is a Codex-specific concept).
    - `node --test tests/phase-08-smoke-gemini.integration.test.cjs` exits 0 (passes or self-skips).
  </acceptance_criteria>
  <done>
    Gemini parity test asserts both restored command surfaces at the locked `.md` extension (W2) and all three agent `.md` files. Sandbox assertions intentionally absent. Test passes or self-skips cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full npm test and confirm PRTY-01 additions cause zero regressions (runs AFTER Plan 02 per depends_on)</name>
  <files>(no file modifications — regression check)</files>
  <read_first>
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-VALIDATION.md (sampling rate guidance — full suite after wave merge)
    - .planning/phases/03-tests-install-smoke-parity-adr-15/03-02-SUMMARY.md (Plan 02's recorded EXPECTED_AGENTS bump — needed because Task 3's `npm test` regression check is what catches an EXPECTED_AGENTS mismatch; W3 fix ensures Plan 02 lands first)
  </read_first>
  <action>
    **W3 precondition:** This task assumes Plan 02 has already landed (depends_on edge guarantees this). Before running `npm test`, sanity-check by inspecting `oto/bin/lib/model-profiles.cjs`:
    ```bash
    node -e "const a = require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS; if (a.length !== 26) { console.error('Plan 02 has not landed yet: EXPECTED_AGENTS.length =', a.length, 'expected 26'); process.exit(1); }"
    ```
    If this check fails, STOP and re-sequence — Plan 03 cannot run its regression check against a stale array.

    Then run `npm test` from repo root. Required outcomes:
    1. Exit code 0.
    2. `fail` count is exactly 0.
    3. `pass` count has not decreased relative to the baseline established by Plan 01's Task 3 (Plan 01 establishes the new ~591+ baseline; this plan should hold it or grow it by however many PRTY-01 tests are added — typically +2, one per integration test).
    4. `skipped` count may be +1 or +2 if Codex/Gemini CLIs are not on the executor's PATH (acceptable; the tests self-skip by design).

    Record the final summary in this plan's SUMMARY: tests, pass, fail, skipped. Cross-reference Plan 01's SUMMARY numbers to confirm no regression.

    If `fail > 0` in any pre-existing test, STOP and diagnose. Likely cause: Task 1 or Task 2 broke an existing test by editing shared helpers (`probeCodex`, `freshTmpDir`, install helpers) — undo the helper change and integrate via additive code instead.
  </action>
  <verify>
    <automated>node -e "if (require('./oto/bin/lib/model-profiles').EXPECTED_AGENTS.length !== 26) process.exit(1)" &amp;&amp; npm test 2>&amp;1 | tail -20 | tee /tmp/oto-phase3-plan03-npmtest.log &amp;&amp; grep -q '^# fail 0$' /tmp/oto-phase3-plan03-npmtest.log</automated>
  </verify>
  <acceptance_criteria>
    - W3 precondition check passes: `EXPECTED_AGENTS.length === 26` (proves Plan 02 landed first).
    - `npm test` exits 0.
    - `/tmp/oto-phase3-plan03-npmtest.log` contains `# fail 0`.
    - `/tmp/oto-phase3-plan03-npmtest.log` contains a `# pass <N>` line.
    - The PRTY-01 tests appear in the test output (run with `--test` reporter detail OR confirm via `grep -q "PRTY-01" /tmp/oto-phase3-plan03-npmtest.log` if the reporter surfaces test names).
  </acceptance_criteria>
  <done>
    Full suite stays green; PRTY-01 additions do not regress any pre-existing test; W3 precondition (EXPECTED_AGENTS.length === 26) verified before regression check; exact numbers recorded in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → installed `oto` CLI | Tests invoke `oto install --codex/--gemini --config-dir <tmp>` against `mktemp` dirs. Tmpdirs are isolated; no user-data pollution. |
| Per-runtime adapter → installed agent files | Tests file-existence-check `.md` and `.toml` outputs; if the adapter changes the install layout silently, the test fails loudly. |
| Probe helpers → external CLI | `probeCodex()` / `probeGemini()` spawn `--version`; if the CLI is missing, the test self-skips rather than failing. Tradeoff: missing CLI on a CI runner means the parity assertion does not actually fire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-09 | Tampering | Codex `.toml` sandbox_mode drift | mitigate | Task 1 asserts the exact mode per agent via regex; any drift from D-04 causes red. Mirrored in `install-smoke.yml` (Plan 02) for double-coverage. |
| T-3-10 | Spoofing | Codex installs `commands/oto/<name>.md` accidentally | mitigate | Task 1's negative assertion (`must NOT install`) hard-locks the skills/* surface. If the adapter regresses to commands/oto/* on Codex, the test fails loudly. |
| T-3-11 | Repudiation | Missing Codex/Gemini CLI on CI runner causes silent skip | accept | Documented behavior: the in-suite path self-skips; the load-bearing assertion lives in `install-smoke.yml` (Plan 02) which runs `oto install` directly without probing. Two assertion surfaces, one always fires. |
| T-3-12 | Tampering | Gemini file extension drift (.md vs .toml) | mitigate | W2 locks `.md` per existing in-repo convention; Task 2 acceptance includes a NEGATIVE grep that rejects `.toml` for these two commands. |
| T-3-W3 | Repudiation | Plan 03 regression check races Plan 02's EXPECTED_AGENTS bump | mitigate | W3 fix: `depends_on: ["03-02"]` in frontmatter; Task 3 also asserts `EXPECTED_AGENTS.length === 26` as a precondition before running `npm test`. |

All threats are medium severity. T-3-11 is the only `accept` — mitigated by the redundant CI YAML assertions in Plan 02.
</threat_model>

<verification>
- `node --test tests/phase-08-smoke-codex.integration.test.cjs` exits 0 (passes or self-skips).
- `node --test tests/phase-08-smoke-gemini.integration.test.cjs` exits 0 (passes or self-skips).
- `npm test` exits 0 with 0 failures.
- No file collision with Plan 02 (`grep -l "phase-08-smoke" .planning/phases/03-tests-install-smoke-parity-adr-15/03-02-PLAN.md` returns no hits).
- W2 lock: `grep -qE 'ingest-docs\.toml|eval-review\.toml' tests/phase-08-smoke-gemini.integration.test.cjs` exits 1.
- W3 lock: `EXPECTED_AGENTS.length === 26` precondition holds before Task 3 runs `npm test`.
</verification>

<success_criteria>
- PRTY-01 satisfied at the per-runtime integration surface for Codex and Gemini.
- Phase 1 D-04 sandbox locks doubly asserted (this plan's Codex test + Plan 02's CI YAML + Plan 02's `scripts/install-smoke.cjs`).
- No regressions in pre-existing tests.
- Wave 1 sequencing: Plan 01 + Plan 02 land before Plan 03 (W3); Plan 03 → Plan 04 in Wave 2. Plans 01 and 02 remain mutually parallel; Plan 03 is sequenced after Plan 02 within Wave 1.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-install-smoke-parity-adr-15/03-03-SUMMARY.md` recording:
- Whether Codex / Gemini CLIs were available on the executor's PATH at run time (and therefore whether the new tests actually ran or self-skipped).
- Confirmation that the Gemini command-file extension used was `.md` (W2 lock), with citation of the existing test line that justified the choice.
- W3 precondition outcome: `EXPECTED_AGENTS.length` value seen at run time (must be 26).
- Final `npm test` numbers (tests, pass, fail, skipped) for cross-reference with Plan 01.
- Any deviations from the research recommendations.
</output>
</content>
</invoke>