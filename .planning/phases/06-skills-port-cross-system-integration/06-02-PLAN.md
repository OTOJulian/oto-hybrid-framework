---
phase: 06
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - oto/skills/test-driven-development/SKILL.md
  - oto/skills/test-driven-development/testing-anti-patterns.md
  - oto/skills/systematic-debugging/SKILL.md
  - oto/skills/systematic-debugging/CREATION-LOG.md
  - oto/skills/systematic-debugging/condition-based-waiting.md
  - oto/skills/systematic-debugging/condition-based-waiting-example.ts
  - oto/skills/systematic-debugging/defense-in-depth.md
  - oto/skills/systematic-debugging/find-polluter.sh
  - oto/skills/systematic-debugging/root-cause-tracing.md
  - oto/skills/systematic-debugging/test-academic.md
  - oto/skills/systematic-debugging/test-pressure-1.md
  - oto/skills/systematic-debugging/test-pressure-2.md
  - oto/skills/systematic-debugging/test-pressure-3.md
  - oto/skills/verification-before-completion/SKILL.md
  - oto/skills/dispatching-parallel-agents/SKILL.md
  - oto/skills/using-git-worktrees/SKILL.md
  - oto/skills/writing-skills/SKILL.md
  - oto/skills/writing-skills/anthropic-best-practices.md
  - oto/skills/writing-skills/examples/CLAUDE_MD_TESTING.md
  - oto/skills/writing-skills/graphviz-conventions.dot
  - oto/skills/writing-skills/persuasion-principles.md
  - oto/skills/writing-skills/render-graphs.js
  - oto/skills/writing-skills/testing-skills-with-subagents.md
  - oto/skills/using-oto/SKILL.md
  - oto/skills/using-oto/references/codex-tools.md
  - oto/skills/using-oto/references/copilot-tools.md
  - oto/skills/using-oto/references/gemini-tools.md
  - reports/rebrand-dryrun.md
  - reports/rebrand-dryrun.json
autonomous: true
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
  - SKL-04
  - SKL-05
  - SKL-06
  - SKL-07
must_haves:
  truths:
    - "All 7 skill directories exist at oto/skills/<name>/ with the exact 27 files enumerated in 06-RESEARCH.md"
    - "Engine dry-run report shows zero unexpected matches inside non-markdown payload (find-polluter.sh, condition-based-waiting-example.ts, render-graphs.js, graphviz-conventions.dot)"
    - "find-polluter.sh retains mode 0o755 (executable bit set)"
    - "oto/skills/using-oto/SKILL.md contains the Phase 5 D-05 identity sentence 'You are using oto.', the marker-bracketed STATE.md gating directive (D-03), and zero upstream identity literals (Pitfall 15)"
    - "tests/06-skill-structure.test.cjs and tests/06-using-oto-state-gating.test.cjs and tests/06-installer-skill-copy.test.cjs exit zero (green) after this plan"
  artifacts:
    - path: "oto/skills/test-driven-development/SKILL.md"
      provides: "SKL-01 — TDD enforcement skill body"
      contains: "name: test-driven-development"
    - path: "oto/skills/systematic-debugging/SKILL.md"
      provides: "SKL-02 — root-cause-tracing skill body"
      contains: "name: systematic-debugging"
    - path: "oto/skills/systematic-debugging/find-polluter.sh"
      provides: "SKL-02 payload — executable polluter-find script"
      mode: "0o755"
    - path: "oto/skills/verification-before-completion/SKILL.md"
      provides: "SKL-03 — per-claim verification skill body"
      contains: "name: verification-before-completion"
    - path: "oto/skills/dispatching-parallel-agents/SKILL.md"
      provides: "SKL-04 — parallel agent dispatch skill body"
      contains: "name: dispatching-parallel-agents"
    - path: "oto/skills/using-git-worktrees/SKILL.md"
      provides: "SKL-05 — worktree workflow skill body"
      contains: "name: using-git-worktrees"
    - path: "oto/skills/writing-skills/SKILL.md"
      provides: "SKL-06 — meta-skill for authoring oto skills"
      contains: "name: writing-skills"
    - path: "oto/skills/using-oto/SKILL.md"
      provides: "SKL-07 — bootstrap skill body with STATE.md gating directive"
      contains: "<!-- oto:state-gating-directive -->"
  key_links:
    - from: "oto/skills/using-oto/SKILL.md"
      to: "oto/hooks/oto-session-start"
      via: "hook reads file inline at SessionStart"
      pattern: "oto/skills/using-oto/SKILL\\.md"
    - from: "oto/skills/using-oto/SKILL.md"
      to: ".oto/STATE.md status field"
      via: "prose directive instructs model to read STATE.md before ambient skill auto-fire"
      pattern: "\\.oto/STATE\\.md"
    - from: "decisions/file-inventory.json"
      to: "oto/skills/<name>/<file> output paths"
      via: "engine.cjs::outputRelPathFor consults inventory.target_path before applying rule-based path rewriting"
      pattern: "phase_owner.*6"
---

<objective>
Run the rebrand engine over `foundation-frameworks/superpowers-main/skills/` to produce engine output for the 7 retained skill directories at `oto/skills/<name>/`, then apply targeted hand-fixes to `oto/skills/using-oto/SKILL.md` only — strip upstream identity literals, insert the Phase 5 D-05 locked identity sentence, and add the marker-bracketed STATE.md gating directive (D-03). The other six skills ship as engine output (verified via inspection, not rewritten).

Purpose: Land SKL-01..07 file artifacts. The engine is the locked port mechanism (D-01); the hand-fix is the locked Pitfall-15 defense (D-02). After this plan, two of the three Wave 0 tests turn green (skill-structure + using-oto-state-gating); the installer-copy test turns green because skills now exist for the installer to copy.
Output: 27 files under `oto/skills/`, plus a dry-run report under `reports/`. No source-code identifiers in the four non-markdown payload files (`.sh`, `.ts`, `.dot`, `.js`) are touched (verified by dry-run).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md
@.planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md
@.planning/phases/06-skills-port-cross-system-integration/06-VALIDATION.md

# Wave 0 tests this plan must turn green
@tests/06-skill-structure.test.cjs
@tests/06-installer-skill-copy.test.cjs
@tests/06-using-oto-state-gating.test.cjs

# Engine + inventory inputs
@scripts/rebrand.cjs
@scripts/rebrand/lib/engine.cjs
@decisions/file-inventory.json
@rename-map.json

# Phase 5 precedent (engine + hand-fix pattern)
@.planning/phases/05-hooks-port-consolidation/05-CONTEXT.md
@oto/hooks/oto-session-start
@oto/hooks/__fixtures__/session-start-claude.json

# Upstream input (read-only — never hand-edit; engine is the only writer)
@foundation-frameworks/superpowers-main/skills/using-superpowers/SKILL.md

<phase_5_locked_identity_sentence>
<!-- The exact sentence locked in Phase 5 D-05. Phase 6 D-02 says using-oto/SKILL.md
     must align with this string at runtime so SessionStart fixture and skill body
     stay consistent. Source: oto/hooks/__fixtures__/session-start-claude.json -->

You are using oto.

Below is the full content of your 'oto:using-oto' skill — your introduction
to using oto skills. For all other skills, use the Skill tool.
</phase_5_locked_identity_sentence>

<state_md_gating_directive>
<!-- The exact prose locked by D-03. Insert verbatim in using-oto/SKILL.md
     as a "## Workflow Deference" section ahead of the Red Flags table.
     Wrap in BOTH marker comments so the D-09 test anchors hold. -->

## Workflow Deference

<!-- oto:state-gating-directive -->
Before invoking any other oto skill on suspicion, read `.oto/STATE.md`.
If the `status:` frontmatter field shows an active phase
(`execute_phase`, `plan_phase`, `debug`, or `verify`), suppress
ambient skill auto-fire — only canonical agent invocations and
explicit user `Skill()` calls fire.

If `.oto/STATE.md` is missing, malformed, or shows `status: complete`,
treat as no active workflow and let skills fire normally.
<!-- /oto:state-gating-directive -->
</state_md_gating_directive>

<phase_5_d05_identity_block>
<!-- The exact replacement for the upstream `<EXTREMELY-IMPORTANT>You have superpowers.`
     block. Insert at the same structural position the upstream block occupies in
     SKILL.md (top of body, after frontmatter, before "## Instruction Priority"). -->

<EXTREMELY_IMPORTANT>
You are using oto.

If you think there is even a 1% chance a skill might apply to what you are
doing, you ABSOLUTELY MUST invoke the skill.

IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT.

This is not negotiable. This is not optional. You cannot rationalize your
way out of this.
</EXTREMELY_IMPORTANT>
</phase_5_d05_identity_block>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine dry-run + apply on the upstream skill subtree</name>
  <files>oto/skills/test-driven-development/, oto/skills/systematic-debugging/, oto/skills/verification-before-completion/, oto/skills/dispatching-parallel-agents/, oto/skills/using-git-worktrees/, oto/skills/writing-skills/, oto/skills/using-oto/, reports/rebrand-dryrun.md, reports/rebrand-dryrun.json</files>
  <read_first>
    - scripts/rebrand.cjs (CLI entry — confirm dry-run / apply / verify-roundtrip flag names match the commands you will run)
    - scripts/rebrand/lib/engine.cjs (lines 230–241 — `outputRelPathFor` consults inventory `target_path` first; this is what drives the using-superpowers → using-oto directory rename without a rename-map change)
    - decisions/file-inventory.json (search for `"phase_owner": 6` — verify exactly 27 rows; verify each row has a `target_path` starting with `skills/`)
    - rename-map.json (confirm the `skill_ns` rule `superpowers:` → `oto:` is present; no map changes needed for this plan)
    - foundation-frameworks/superpowers-main/skills/ (treat as read-only; engine is the only writer)
    - .planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md ("Pitfall D: Payload Drift" — what to look for in dry-run report; "State of the Art" — operational note on engine target scoping)
  </read_first>
  <action>
    Step 1 — Dry-run first. Run:

    ```bash
    node scripts/rebrand.cjs --dry-run --target foundation-frameworks/superpowers-main/skills/
    ```

    Inspect the resulting `reports/rebrand-dryrun.md` (or `.json`). For EACH of the four non-markdown payload files in the report:

    - `foundation-frameworks/superpowers-main/skills/systematic-debugging/find-polluter.sh`
    - `foundation-frameworks/superpowers-main/skills/systematic-debugging/condition-based-waiting-example.ts`
    - `foundation-frameworks/superpowers-main/skills/writing-skills/render-graphs.js`
    - `foundation-frameworks/superpowers-main/skills/writing-skills/graphviz-conventions.dot`

    Verify the report shows ZERO planned identifier rewrites in their bodies. If the report flags any in-code identifier match (e.g., a `gsdSomething` symbol the engine wants to rewrite to `otoSomething`), STOP and escalate to the user — do not proceed to apply. Document what was flagged and why before deciding to allowlist.

    Also verify the report shows the inventory-driven rename `skills/using-superpowers/SKILL.md` → `skills/using-oto/SKILL.md` (and similar for the three references files). If the rename does not appear, the inventory `target_path` consultation is broken — STOP.

    Step 2 — Apply. Run:

    ```bash
    node scripts/rebrand.cjs --apply \
      --target foundation-frameworks/superpowers-main/skills/ \
      --out .oto-rebrand-out/skills/ \
      --force
    ```

    The engine emits to `.oto-rebrand-out/skills/`. Walk that directory and copy ONLY the 7 retained skill directories (`test-driven-development/`, `systematic-debugging/`, `verification-before-completion/`, `dispatching-parallel-agents/`, `using-git-worktrees/`, `writing-skills/`, `using-oto/`) into `oto/skills/`. Other directories the engine may have emitted (any of the 7 dropped Superpowers skills, etc.) are NOT copied — verify by checking `decisions/file-inventory.json` for `verdict: "drop"` rows in the skills subtree before doing the move.

    Use `cp -R` (BSD/GNU cp) or `node:fs/promises` `cp({ recursive: true, preserveTimestamps: false, dereference: false })`. The `dereference: false` flag matters because copyTree in production uses the same default and `find-polluter.sh` mode 0o755 must survive (Pitfall C).

    Step 3 — Verify mode bit on `find-polluter.sh`. After the copy, run:

    ```bash
    stat -f '%p' oto/skills/systematic-debugging/find-polluter.sh
    ```

    Output should end in `755` (e.g., `100755`). If output ends in `644`, run `chmod 755 oto/skills/systematic-debugging/find-polluter.sh` and document why the copy lost the bit (probably engine emitted with default mode — capture as a follow-up task or open issue).

    Step 4 — Round-trip verify. Run:

    ```bash
    node scripts/rebrand.cjs --verify-roundtrip --target oto/skills/
    ```

    Expected output: zero changes. If the round-trip flags any change, the engine output drifted — STOP and investigate.

    Step 5 — Run the Wave 0 structure test against the new skill tree:

    ```bash
    node --test tests/06-skill-structure.test.cjs
    ```

    Expected: blocks 1, 2, 3 GREEN (D-07 structure axes); block 4 GREEN (no command-name collision; verified during planning); blocks 5, 6, 7 still RED (agent invocations land in Plan 03). Exit code can be non-zero because of the agent blocks — that's expected; document which blocks went green vs. red in the SUMMARY.

    Do NOT commit `.oto-rebrand-out/` — it's intermediate scratch. Either delete it after the move or add to `.gitignore`. Confirm `.gitignore` already excludes the path or add an entry.
  </action>
  <verify>
    <automated>node --test tests/06-skill-structure.test.cjs --test-name-pattern="D-07" # Blocks 1–3 (structure) and block 4 (collision) must pass</automated>
  </verify>
  <acceptance_criteria>
    - Directory `oto/skills/test-driven-development/` exists and contains exactly `SKILL.md` + `testing-anti-patterns.md` (`ls oto/skills/test-driven-development/ | wc -l` returns `2`).
    - Directory `oto/skills/systematic-debugging/` exists and contains 11 files matching the inventory (`ls oto/skills/systematic-debugging/ | wc -l` returns `11`).
    - Directory `oto/skills/verification-before-completion/` exists and contains exactly `SKILL.md` (count = 1).
    - Directory `oto/skills/dispatching-parallel-agents/` exists and contains exactly `SKILL.md` (count = 1).
    - Directory `oto/skills/using-git-worktrees/` exists and contains exactly `SKILL.md` (count = 1).
    - Directory `oto/skills/writing-skills/` exists with `SKILL.md`, `anthropic-best-practices.md`, `examples/CLAUDE_MD_TESTING.md`, `graphviz-conventions.dot`, `persuasion-principles.md`, `render-graphs.js`, `testing-skills-with-subagents.md` (count = 7 including the examples/ subdir contents).
    - Directory `oto/skills/using-oto/` exists with `SKILL.md` and a `references/` subdirectory containing 3 files (`codex-tools.md`, `copilot-tools.md`, `gemini-tools.md`).
    - `stat -f '%p' oto/skills/systematic-debugging/find-polluter.sh` (macOS) or `stat -c '%a' ...` (Linux) shows `755` (or output mode contains `755`).
    - `node -e "console.log((require('fs').statSync('oto/skills/systematic-debugging/find-polluter.sh').mode & 0o111) !== 0)"` prints `true`.
    - No directory exists at `oto/skills/brainstorming/`, `oto/skills/writing-plans/`, `oto/skills/executing-plans/`, `oto/skills/subagent-driven-development/`, `oto/skills/requesting-code-review/`, `oto/skills/receiving-code-review/`, or `oto/skills/finishing-a-development-branch/` (the 7 dropped skills must not slip through).
    - `grep -rE "(superpowers|Superpowers)" oto/skills/ | wc -l` returns `0` (excluding any matches inside `examples/CLAUDE_MD_TESTING.md` if its content references Superpowers as a documentation example — if found there, escalate to user; otherwise expect 0).
    - File `reports/rebrand-dryrun.md` (or `.json`) exists from the dry-run step.
    - Round-trip command `node scripts/rebrand.cjs --verify-roundtrip --target oto/skills/` exits zero with no diff output.
    - `node --test tests/06-skill-structure.test.cjs --test-name-pattern="D-07"` exits zero (D-07 structure + collision blocks pass).
  </acceptance_criteria>
  <done>
    27 skill files committed under oto/skills/, find-polluter.sh executable bit preserved, round-trip clean, dry-run report committed, structure-test D-07 axes green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-fix oto/skills/using-oto/SKILL.md (Pitfall 15 defense + D-03 gating directive)</name>
  <files>oto/skills/using-oto/SKILL.md</files>
  <read_first>
    - oto/skills/using-oto/SKILL.md (the engine output from Task 1 — read it AS-IS before editing so you see exactly what the engine produced and what literals remain)
    - foundation-frameworks/superpowers-main/skills/using-superpowers/SKILL.md (the upstream original — diff source for what to remove)
    - oto/hooks/__fixtures__/session-start-claude.json (the locked Phase 5 D-05 identity sentence — must align)
    - oto/hooks/oto-session-start (line 37 — confirms how the SessionStart hook wraps using-oto/SKILL.md content; the identity sentence sits OUTSIDE the skill body in production runtime, but D-02 says the skill body's `<EXTREMELY_IMPORTANT>` block must STILL be the rebranded form so static readers see the right framing)
    - tests/06-using-oto-state-gating.test.cjs (the assertions you must satisfy: marker-bracketed gating directive + `.oto/STATE.md` substring + `suppress` keyword + RECOGNIZED_STATUSES values + `You are using oto.` substring + zero banned literals)
    - .planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md ("Specific Ideas" item 1: assembly order; D-02: minimal voice adaptation list of removals/preservations)
  </read_first>
  <action>
    Hand-edit `oto/skills/using-oto/SKILL.md` (in-place; do NOT create a separate file). Use the Read tool first to see the exact engine output, then use Edit tool calls to apply each change. The four required mutations:

    Mutation 1 — Replace the upstream `<EXTREMELY-IMPORTANT>` block with the Phase 5 D-05 rebranded form.

    Find the existing block (engine output may have rebranded `<EXTREMELY-IMPORTANT>` to `<EXTREMELY_IMPORTANT>` or left it; check). Replace whichever form is present with EXACTLY this verbatim text from the `<phase_5_d05_identity_block>` section above:

    ```
    <EXTREMELY_IMPORTANT>
    You are using oto.

    If you think there is even a 1% chance a skill might apply to what you are
    doing, you ABSOLUTELY MUST invoke the skill.

    IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT.

    This is not negotiable. This is not optional. You cannot rationalize your
    way out of this.
    </EXTREMELY_IMPORTANT>
    ```

    Note: `You are using oto.` (the period-terminated sentence) is the exact match the test asserts on. Underscore form `<EXTREMELY_IMPORTANT>` (not hyphen) matches the SessionStart hook's wrapping convention.

    Mutation 2 — Strip upstream contributor framing if present in engine output.

    Search the file body for any of these substrings (using grep/Read):
    - `94% PR rejection`
    - `94 percent`
    - `fabricated content`
    - `protect your human partner from PR rejection`
    - `protect your human partner`

    For each match found, delete the containing paragraph (or sentence if the framing is mid-paragraph). The CONTEXT.md upstream `using-superpowers/SKILL.md` may not contain these (the audit confirmed they live in upstream CLAUDE.md, not the skill); if grep returns 0 matches, skip this mutation and document the no-op in the SUMMARY.

    Mutation 3 — Replace remaining upstream identity self-references.

    Search for and rewrite (using Edit tool):
    - Any `using-superpowers` literal still in the body → `using-oto`
    - Any `Superpowers skills` → `oto skills`
    - Any standalone `Superpowers` (capitalized, not in code blocks) → `oto`
    - Any `superpowers` (lowercase, not in `oto:superpowers...` namespace contexts — there should be none anyway since the engine handled the namespace already) → `oto`

    Verify after each Edit by re-reading the file. If `grep -E "(superpowers|Superpowers|using-superpowers|You have superpowers)" oto/skills/using-oto/SKILL.md` returns ANY hit, repeat the mutation until clean.

    Mutation 4 — Insert the STATE.md gating directive.

    Find the section header `## Red Flags` (or whatever the engine output named the rationalization-table section). Insert IMMEDIATELY BEFORE that header the exact verbatim block from the `<state_md_gating_directive>` context above:

    ```
    ## Workflow Deference

    <!-- oto:state-gating-directive -->
    Before invoking any other oto skill on suspicion, read `.oto/STATE.md`.
    If the `status:` frontmatter field shows an active phase
    (`execute_phase`, `plan_phase`, `debug`, or `verify`), suppress
    ambient skill auto-fire — only canonical agent invocations and
    explicit user `Skill()` calls fire.

    If `.oto/STATE.md` is missing, malformed, or shows `status: complete`,
    treat as no active workflow and let skills fire normally.
    <!-- /oto:state-gating-directive -->

    ```

    The blank line after the closing marker is intentional — separates the section from `## Red Flags`.

    Preserve verbatim (do NOT rewrite without eval evidence — D-02 lock):
    - The Red Flags rationalization table
    - The 1% rule paragraph (already present in `<EXTREMELY_IMPORTANT>` block above)
    - The Instruction Priority hierarchy section
    - The "human partner" phrasing where it shapes behavior (NOT the dropped "PR rejection" framing)
    - The Skill Priority ordering (process skills before implementation skills, if present)
    - The "I can check git/files quickly" table

    After all four mutations, run the full Wave 0 test triplet locally:

    ```bash
    node --test tests/06-skill-structure.test.cjs tests/06-using-oto-state-gating.test.cjs tests/06-installer-skill-copy.test.cjs
    ```

    Expected at this point:
    - skill-structure: D-07 blocks green, SKL-08 axes 1–3 still red (Plan 03 fixes those)
    - using-oto-state-gating: ALL four blocks green
    - installer-skill-copy: byte-equality and exec-bit blocks green; install-state JSON marker block green

    Diff-review the final using-oto/SKILL.md end-to-end (per CONTEXT.md D-02 explicit instruction: "Diff-review using-oto end-to-end before lock — highest-visibility skill"). Confirm no unintended deletion of preserved sections.
  </action>
  <verify>
    <automated>node --test tests/06-using-oto-state-gating.test.cjs # All four blocks must pass after this task</automated>
  </verify>
  <acceptance_criteria>
    - File `oto/skills/using-oto/SKILL.md` exists.
    - `grep -F "<EXTREMELY_IMPORTANT>" oto/skills/using-oto/SKILL.md` returns ≥1 match (rebranded underscore form).
    - `grep -F "<EXTREMELY-IMPORTANT>" oto/skills/using-oto/SKILL.md` returns 0 matches (upstream hyphen form gone).
    - `grep -F "You are using oto." oto/skills/using-oto/SKILL.md` returns ≥1 match (Phase 5 D-05 sentence).
    - `grep -F "You have superpowers" oto/skills/using-oto/SKILL.md` returns 0 matches.
    - `grep -F "<!-- oto:state-gating-directive -->" oto/skills/using-oto/SKILL.md` returns 1 match.
    - `grep -F "<!-- /oto:state-gating-directive -->" oto/skills/using-oto/SKILL.md` returns 1 match.
    - `grep -F ".oto/STATE.md" oto/skills/using-oto/SKILL.md` returns ≥1 match.
    - `grep -Fi "suppress" oto/skills/using-oto/SKILL.md` returns ≥1 match.
    - `grep -F "execute_phase" oto/skills/using-oto/SKILL.md` returns ≥1 match.
    - `grep -F "plan_phase" oto/skills/using-oto/SKILL.md` returns ≥1 match.
    - `grep -F "## Workflow Deference" oto/skills/using-oto/SKILL.md` returns 1 match.
    - `grep -E "(superpowers|Superpowers|using-superpowers)" oto/skills/using-oto/SKILL.md` returns 0 matches.
    - `grep -F "94% PR rejection" oto/skills/using-oto/SKILL.md` returns 0 matches.
    - `grep -F "fabricated content" oto/skills/using-oto/SKILL.md` returns 0 matches.
    - `grep -F "protect your human partner from PR rejection" oto/skills/using-oto/SKILL.md` returns 0 matches.
    - `node --test tests/06-using-oto-state-gating.test.cjs` exits zero (all 4 blocks green).
    - `node --test tests/06-installer-skill-copy.test.cjs` exits zero (all 3 blocks green — installer copies the now-existing skill tree).
  </acceptance_criteria>
  <done>
    using-oto/SKILL.md hand-fixed with 4 named mutations applied; D-09 gating-directive test green; Pitfall-15 banned-literal test green; installer-copy test green; SKL-07 satisfied at content level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `foundation-frameworks/` (read-only) → engine → `oto/skills/` | Upstream content rebranded by rule-typed engine; allowlist prevents touching foundation-frameworks/ itself |
| Engine output → manual hand-fix → SKILL.md committed | Single high-visibility file; literal-string scan in test enforces fidelity to D-02 rules |
| `oto/skills/using-oto/SKILL.md` → SessionStart hook → model context | Hook injects body inline at every session; any T-15 leakage propagates to every Claude/Codex/Gemini session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering (T-15: upstream-identity literal leakage) | oto/skills/using-oto/SKILL.md | mitigate | Task 2 mutations 1–3 strip every banned literal; Wave 0 test 06-using-oto-state-gating.test.cjs block 2 enforces the seven-string banned list; acceptance criteria include 5 grep-zero asserts on banned strings. |
| T-06-09 | Tampering (Pitfall E: gating-directive drift) | oto/skills/using-oto/SKILL.md `<!-- oto:state-gating-directive -->` markers | mitigate | Task 2 mutation 4 inserts both open + close markers around exact directive prose; D-09 test asserts both markers + `.oto/STATE.md` + `suppress` + 4 status values. Future paraphrase deletion fails the test. |
| T-06-10 | Tampering (Pitfall D: payload drift) | systematic-debugging/find-polluter.sh, condition-based-waiting-example.ts, writing-skills/render-graphs.js, graphviz-conventions.dot | mitigate | Task 1 step 1 dry-run inspection requires zero unexpected matches in payload files before apply; if any flagged, STOP and escalate. |
| T-06-11 | Tampering (Pitfall C: exec-bit loss) | systematic-debugging/find-polluter.sh mode | mitigate | Task 1 step 3 stats the file post-copy; chmod 755 if engine emitted with default mode; test 06-installer-skill-copy.test.cjs block 2 catches regression at install time. |
| T-06-12 | Tampering (dropped-skill resurrection) | oto/skills/<dropped-name>/ | mitigate | Task 1 step 2 explicitly copies only the 7 retained directories; acceptance criteria assert non-existence of all 7 dropped-skill directories by name. |
| T-06-13 | Repudiation (engine output drift on re-apply) | oto/skills/ tree | mitigate | Task 1 step 4 round-trip verify must produce zero changes; if engine output drifts on second apply, the rule set has a bug — escalate. |
| T-06-14 | Information Disclosure | reports/rebrand-dryrun.{md,json} contents | accept | Reports contain only rebrand classifications, no secrets; committing them is intentional for audit trail. |
| T-06-15 | Tampering (engine identifier-rule false positive in code payload) | condition-based-waiting-example.ts identifiers | mitigate | Task 1 step 1 dry-run inspection; if a `gsdSomething`-shaped symbol shows up as a planned rewrite, STOP — the example code would be corrupted; planner must add an allowlist entry before proceeding. |
</threat_model>

<verification>
- All 27 expected files exist under `oto/skills/` (count by skill matches the inventory).
- find-polluter.sh mode 0o755 confirmed.
- Round-trip verify produces zero diff.
- `grep -rE "(superpowers|Superpowers|using-superpowers|You have superpowers|94% PR rejection|fabricated content|protect your human partner from PR rejection)" oto/skills/` returns 0 matches outside any documentation-example references in `writing-skills/examples/CLAUDE_MD_TESTING.md` that the planner has explicitly approved.
- `node --test tests/06-skill-structure.test.cjs --test-name-pattern="D-07"` exits zero.
- `node --test tests/06-using-oto-state-gating.test.cjs` exits zero (all 4 blocks).
- `node --test tests/06-installer-skill-copy.test.cjs` exits zero (all 3 blocks).
- The 3 SKL-08 blocks in skill-structure.test.cjs remain RED (turn green in Plan 03).
</verification>

<success_criteria>
- [ ] All 27 skill files exist at the inventory-specified paths under `oto/skills/`.
- [ ] No dropped-skill directory exists in `oto/skills/`.
- [ ] find-polluter.sh has its executable bit set (mode & 0o111 != 0).
- [ ] Round-trip rebrand passes (zero diff on second apply).
- [ ] using-oto/SKILL.md contains the locked Phase 5 D-05 identity sentence and the marker-bracketed STATE.md gating directive.
- [ ] using-oto/SKILL.md contains zero banned literals (Pitfall 15 defense holds).
- [ ] tests/06-skill-structure.test.cjs D-07 axes green.
- [ ] tests/06-using-oto-state-gating.test.cjs all four blocks green.
- [ ] tests/06-installer-skill-copy.test.cjs all three blocks green (skills now exist for installer to copy).
- [ ] Dry-run report committed under reports/.
- [ ] SKL-01..07 file-existence axes satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/06-skills-port-cross-system-integration/06-02-SUMMARY.md` documenting:
- Engine dry-run findings (any payload matches found, what was decided)
- Apply step output (files emitted, files copied to oto/skills/)
- Hand-fix mutations applied to using-oto/SKILL.md (which of the 4 were no-ops vs. applied; what literal strings were removed)
- Final test status: 06-skill-structure D-07 green / SKL-08 red, 06-using-oto-state-gating green, 06-installer-skill-copy green
- find-polluter.sh mode confirmation
</output>
