---
phase: 06
plan: 03
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - oto/agents/oto-executor.md
  - oto/agents/oto-verifier.md
  - oto/agents/oto-debugger.md
autonomous: true
requirements:
  - SKL-08
must_haves:
  truths:
    - "oto-executor.md body contains literal directives invoking Skill('oto:test-driven-development') before writing implementation code AND Skill('oto:verification-before-completion') after writing implementation code, before declaring done"
    - "oto-verifier.md body contains a literal directive invoking Skill('oto:verification-before-completion') at the start of its verification pass"
    - "oto-debugger.md body contains a literal directive invoking Skill('oto:systematic-debugging') when starting a debug session"
    - "Generic upstream placeholder prose (the line-58/54/41 sentences from CONTEXT.md canonical_refs) is fully replaced — no double-write, no leftover placeholder"
    - "tests/06-skill-structure.test.cjs blocks 5–7 (SKL-08 axes 1–3) exit zero"
  artifacts:
    - path: "oto/agents/oto-executor.md"
      provides: "SKL-08 axes 1 and 2 — TDD before write + verification after write canonical invocations"
      contains: "Skill('oto:test-driven-development')"
    - path: "oto/agents/oto-verifier.md"
      provides: "SKL-08 axis 3 — verification-before-completion at start of verification pass"
      contains: "Skill('oto:verification-before-completion')"
    - path: "oto/agents/oto-debugger.md"
      provides: "SKL-08 axis 4 — systematic-debugging at debug session start"
      contains: "Skill('oto:systematic-debugging')"
  key_links:
    - from: "oto/agents/oto-executor.md"
      to: "oto/skills/test-driven-development/SKILL.md, oto/skills/verification-before-completion/SKILL.md"
      via: "Skill() tool invocation directives in agent prompt body"
      pattern: "Skill\\('oto:(test-driven-development|verification-before-completion)'\\)"
    - from: "oto/agents/oto-verifier.md"
      to: "oto/skills/verification-before-completion/SKILL.md"
      via: "Skill() tool invocation directive"
      pattern: "Skill\\('oto:verification-before-completion'\\)"
    - from: "oto/agents/oto-debugger.md"
      to: "oto/skills/systematic-debugging/SKILL.md"
      via: "Skill() tool invocation directive"
      pattern: "Skill\\('oto:systematic-debugging'\\)"
---

<objective>
Wire the four canonical skill invocations from D-06 (matching SKL-08 verbatim) into the three retained spine agents. Replace the generic upstream-style placeholder prose at oto-executor.md L58, oto-verifier.md L54, oto-debugger.md L41 with explicit imperative `Skill('oto:<name>')` directives at the natural insertion points in each agent's flow.

Purpose: Cross-system integration. Skills exist after Plan 02; this plan makes the three retained agents consistently invoke them at canonical points. Inline-prose pattern (D-05) is grep-able for tests and does not require new tool surfaces.
Output: Three agent files edited in-place with concrete `Skill()` directives. SKL-08 axes 1–4 in tests/06-skill-structure.test.cjs turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md
@.planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md
@.planning/phases/06-skills-port-cross-system-integration/06-VALIDATION.md

# Wave 0 test this plan must turn green (blocks 5, 6, 7)
@tests/06-skill-structure.test.cjs

# Files modified
@oto/agents/oto-executor.md
@oto/agents/oto-verifier.md
@oto/agents/oto-debugger.md

# Architectural decisions backing the inline-prose pattern
@decisions/skill-vs-command.md

<canonical_directive_text>
<!-- The exact prose to insert. Replace the placeholder lines verbatim with these.
     Wording is normative — tests assert on the literal Skill('oto:<name>') strings;
     surrounding sentences are imperative, matching the existing tone of agent prompts.
     Source: 06-RESEARCH.md "Pattern 3: Inline Prose Skill Invocations" + 06-CONTEXT.md
     "Specific Ideas" item 3 ("directive, not a suggestion"). -->

For oto-executor.md (replaces line 58 "Follow skill rules relevant to the task you are about to commit."):
```
Before writing implementation code, invoke Skill('oto:test-driven-development') and follow the discipline it specifies. After completing implementation and before declaring the task done, invoke Skill('oto:verification-before-completion') and apply its checklist.
```

For oto-verifier.md (replaces line 54 "Apply skill rules when scanning for anti-patterns and verifying quality."):
```
At the start of the verification pass, invoke Skill('oto:verification-before-completion') and apply its checklist before scanning for anti-patterns.
```

For oto-debugger.md (replaces line 41 "Follow skill rules relevant to the bug being investigated and the fix being applied."):
```
When starting a debug session, invoke Skill('oto:systematic-debugging') and follow its root-cause-tracing methodology.
```
</canonical_directive_text>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire oto-executor canonical invocations (SKL-08 axes 1 + 2)</name>
  <files>oto/agents/oto-executor.md</files>
  <read_first>
    - oto/agents/oto-executor.md (full file — read ALL of it; specifically read line 58 in context to confirm current placeholder text matches CONTEXT.md "Existing oto code touched by Phase 6")
    - tests/06-skill-structure.test.cjs (block 5 — confirms exact assertion strings: `Skill('oto:test-driven-development')` AND `Skill('oto:verification-before-completion')` must both appear; AND placeholder `"Follow skill rules relevant to the task you are about to commit."` must be GONE)
    - .planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md (D-05/D-06: inline prose, not frontmatter; D-06 #1 + #2: TDD before write, verification after write)
    - .planning/phases/06-skills-port-cross-system-integration/06-RESEARCH.md (Pattern 3 — example block for oto-executor)
  </read_first>
  <action>
    Edit `oto/agents/oto-executor.md` in place using the Edit tool. The line number was 58 at the time CONTEXT.md was authored — confirm by Read first; if the line moved due to other edits, find by exact substring match instead.

    Replace the EXACT old string:

    ```
    - Follow skill rules relevant to the task you are about to commit.
    ```

    With this NEW string (verbatim from `<canonical_directive_text>` above; preserve the leading `- ` list-item marker so the bullet list structure is unchanged):

    ```
    - Before writing implementation code, invoke Skill('oto:test-driven-development') and follow the discipline it specifies. After completing implementation and before declaring the task done, invoke Skill('oto:verification-before-completion') and apply its checklist.
    ```

    Use the Edit tool with the exact old/new strings above (single replacement; no regex). After the edit:

    1. Re-Read the file to confirm the change landed correctly.
    2. Run `grep -F "Follow skill rules relevant to the task you are about to commit." oto/agents/oto-executor.md` — must return 0 (placeholder gone).
    3. Run `grep -F "Skill('oto:test-driven-development')" oto/agents/oto-executor.md` — must return 1.
    4. Run `grep -F "Skill('oto:verification-before-completion')" oto/agents/oto-executor.md` — must return 1.
    5. Run `node --test tests/06-skill-structure.test.cjs --test-name-pattern="SKL-08 axis 1"` — must exit zero.

    No other edits to this file. Specifically: do NOT add `required_skills:` frontmatter (D-05 lock); do NOT split the directive into multiple list items (the test asserts both Skill() calls in the same body, not their relative position).

    Note: D-06 #1 and #2 are wired in a SINGLE replacement. CONTEXT.md says "Insert at the agent's verification gate" for #2 — but the agent's existing structure puts both pre-write skill enforcement and post-write verification in the same prose block at line 58 (the line is an instruction in the project_context section that fires BEFORE both events). Embedding both directives in this single line is the most natural insertion point; the directive sentence orders them temporally ("before writing... after completing...") so the model still sees them at the right times. If the executor.md flow elsewhere has a separate, named "verification gate" section better suited for D-06 #2, the planner permits moving the second sentence there — document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>node --test tests/06-skill-structure.test.cjs --test-name-pattern="SKL-08 axis 1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Skill('oto:test-driven-development')" oto/agents/oto-executor.md` returns 1 match.
    - `grep -F "Skill('oto:verification-before-completion')" oto/agents/oto-executor.md` returns 1 match.
    - `grep -F "Follow skill rules relevant to the task you are about to commit." oto/agents/oto-executor.md` returns 0 matches.
    - `grep -F "Before writing implementation code, invoke Skill('oto:test-driven-development')" oto/agents/oto-executor.md` returns 1 match.
    - `grep -F "After completing implementation and before declaring the task done, invoke Skill('oto:verification-before-completion')" oto/agents/oto-executor.md` returns 1 match.
    - `grep -F "required_skills:" oto/agents/oto-executor.md` returns 0 matches (D-05 lock — no frontmatter mechanism).
    - `node --test tests/06-skill-structure.test.cjs --test-name-pattern="SKL-08 axis 1"` exits zero.
    - File still parses as valid markdown (no broken frontmatter, list structure intact — verify by Read).
  </acceptance_criteria>
  <done>
    oto-executor.md wires both TDD-before-write and verification-after-write canonical invocations; placeholder removed; test block 5 green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire oto-verifier canonical invocation (SKL-08 axis 3) AND oto-debugger canonical invocation (SKL-08 axis 4)</name>
  <files>oto/agents/oto-verifier.md, oto/agents/oto-debugger.md</files>
  <read_first>
    - oto/agents/oto-verifier.md (full file; specifically line 54 to confirm placeholder text)
    - oto/agents/oto-debugger.md (full file; specifically line 41 to confirm placeholder text)
    - tests/06-skill-structure.test.cjs (blocks 6 and 7 — exact assertion strings: verifier must contain `Skill('oto:verification-before-completion')` and placeholder line gone; debugger must contain `Skill('oto:systematic-debugging')` and placeholder line gone)
    - .planning/phases/06-skills-port-cross-system-integration/06-CONTEXT.md (D-06 #3 and #4)
  </read_first>
  <action>
    Two file edits, both straightforward exact-string replacements via the Edit tool.

    Edit 1 — `oto/agents/oto-verifier.md`:

    Replace the EXACT old string:
    ```
    - Apply skill rules when scanning for anti-patterns and verifying quality.
    ```

    With:
    ```
    - At the start of the verification pass, invoke Skill('oto:verification-before-completion') and apply its checklist before scanning for anti-patterns.
    ```

    Preserve leading `- ` list-item marker.

    Edit 2 — `oto/agents/oto-debugger.md`:

    Replace the EXACT old string:
    ```
    - Follow skill rules relevant to the bug being investigated and the fix being applied.
    ```

    With:
    ```
    - When starting a debug session, invoke Skill('oto:systematic-debugging') and follow its root-cause-tracing methodology.
    ```

    Preserve leading `- ` list-item marker.

    After both edits:

    1. Re-Read both files to confirm the changes landed.
    2. Run the full skill-structure test:
       ```bash
       node --test tests/06-skill-structure.test.cjs
       ```
       All 7 blocks must exit zero (D-07 axes from Plan 02 + SKL-08 axes 1, 2 from Plan 03 Task 1 + SKL-08 axes 3, 4 from this task).

    3. Run the full Phase 6 test triplet via the project runner:
       ```bash
       node scripts/run-tests.cjs --filter "06-"
       ```
       Expected: all three test files green; full Phase 6 contract satisfied.

    Do NOT add `required_skills:` frontmatter. Do NOT add a hook-based skill invocation. Stay strict to D-05 inline-prose pattern.
  </action>
  <verify>
    <automated>node scripts/run-tests.cjs --filter "06-"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Skill('oto:verification-before-completion')" oto/agents/oto-verifier.md` returns 1 match.
    - `grep -F "Apply skill rules when scanning for anti-patterns and verifying quality." oto/agents/oto-verifier.md` returns 0 matches.
    - `grep -F "At the start of the verification pass, invoke Skill('oto:verification-before-completion')" oto/agents/oto-verifier.md` returns 1 match.
    - `grep -F "Skill('oto:systematic-debugging')" oto/agents/oto-debugger.md` returns 1 match.
    - `grep -F "Follow skill rules relevant to the bug being investigated and the fix being applied." oto/agents/oto-debugger.md` returns 0 matches.
    - `grep -F "When starting a debug session, invoke Skill('oto:systematic-debugging')" oto/agents/oto-debugger.md` returns 1 match.
    - `grep -F "required_skills:" oto/agents/oto-verifier.md` returns 0 matches.
    - `grep -F "required_skills:" oto/agents/oto-debugger.md` returns 0 matches.
    - `node --test tests/06-skill-structure.test.cjs` exits zero (all 7 blocks green).
    - `node scripts/run-tests.cjs --filter "06-"` exits zero (all three Phase 6 test files green: skill-structure, installer-skill-copy, using-oto-state-gating).
  </acceptance_criteria>
  <done>
    Both verifier and debugger agents wire their canonical skill invocation; placeholders removed; all SKL-08 axes green; full Phase 6 test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent prompt body → Claude Code's Skill tool | Agents invoke skills via inline `Skill('oto:<name>')` directives the model executes; tests assert literal-string presence to prevent silent removal |
| Future agent edit → these canonical directives | Drift risk: a future edit could remove or paraphrase the directive. Wave 0 test 06-skill-structure (blocks 5–7) catches removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Tampering (canonical-directive drift) | oto/agents/oto-executor.md, oto-verifier.md, oto-debugger.md | mitigate | Tests assert presence of exact `Skill('oto:<name>')` literal strings AND absence of upstream placeholder prose. Future paraphrase-away or skill-name change fails the test. |
| T-06-17 | Repudiation (silent SKL-08 removal) | Agent prompts | mitigate | Test 06-skill-structure blocks 5–7 enforce all four canonical invocations. Removing any directive breaks one of the blocks. |
| T-06-18 | Tampering (mechanism creep — adding required_skills frontmatter or hook-based invocation) | Agent file frontmatter | mitigate | Acceptance criteria explicitly grep `required_skills:` returns 0 in both files. Locks the D-05 inline-prose pattern at test time. |
| T-06-19 | Information Disclosure | Agent file content | accept | Agents are public repo content; no secrets. |
</threat_model>

<verification>
- All three agent files contain exact canonical directive sentences and zero placeholder prose.
- `grep -rE "Skill\('oto:(test-driven-development|verification-before-completion|systematic-debugging)'\)" oto/agents/` returns 4 matches across the three files (executor: 2, verifier: 1, debugger: 1).
- No `required_skills:` field added.
- Full Phase 6 test triplet green via `node scripts/run-tests.cjs --filter "06-"`.
- Full project test suite green via `node scripts/run-tests.cjs` (smoke check that Phase 6 changes haven't regressed earlier phase tests).
</verification>

<success_criteria>
- [ ] oto-executor.md contains `Skill('oto:test-driven-development')` and `Skill('oto:verification-before-completion')` directives.
- [ ] oto-verifier.md contains `Skill('oto:verification-before-completion')` directive.
- [ ] oto-debugger.md contains `Skill('oto:systematic-debugging')` directive.
- [ ] All three placeholder lines (executor L58, verifier L54, debugger L41 generic prose) fully removed.
- [ ] No `required_skills:` frontmatter field added to any agent.
- [ ] tests/06-skill-structure.test.cjs all 7 blocks green (D-07 + SKL-08 axes 1–4).
- [ ] Full Phase 6 triplet (`scripts/run-tests.cjs --filter "06-"`) exits zero.
- [ ] Full test suite (`scripts/run-tests.cjs`) exits zero (no regressions to earlier phase tests).
</success_criteria>

<output>
After completion, create `.planning/phases/06-skills-port-cross-system-integration/06-03-SUMMARY.md` documenting:
- Three exact-string Edit operations performed and confirmed
- Final test status: all 7 blocks of 06-skill-structure green; full Phase 6 triplet green
- Confirmation that no frontmatter `required_skills:` field was introduced (D-05 pattern preserved)
- Confirmation that all four SKL-08 axes (TDD-before-write, verification-after-write, verification-at-start, systematic-debugging-at-start) are wired and tested
</output>
