---
phase: 04-core-workflows-agents-port
plan: 04
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - oto/commands/oto/ai-integration-phase.md
  - oto/workflows/ai-integration-phase.md
autonomous: true
requirements_addressed:
  - WF-28
must_haves:
  truths:
    - "oto/commands/oto/ai-integration-phase.md describes the bounded v0.1.0 state explicitly per D-20 (no false promises about dropped agent capabilities)"
    - "oto/workflows/ai-integration-phase.md retains only the Domain Research step using oto-domain-researcher, with explicit DEFERRED blocks replacing dropped-agent steps"
    - "Neither file references oto-framework-selector, oto-ai-researcher, or oto-eval-planner as executable Task calls"
    - "The /oto-ai-integration-phase command remains useful (D-17): the user can still scaffold an AI-SPEC.md, just with manual fill-in for the deferred portions"
  artifacts:
    - path: "oto/commands/oto/ai-integration-phase.md"
      provides: "WF-28 command with bounded objective explaining v0.1.0 deferral"
    - path: "oto/workflows/ai-integration-phase.md"
      provides: "WF-28 workflow that runs Domain Research live and prints DEFERRED messages for missing steps"
  key_links:
    - from: "oto/workflows/ai-integration-phase.md"
      to: "oto-domain-researcher agent"
      via: "Task(subagent_type=...)"
      pattern: "subagent_type[\"']?\\s*[:=]\\s*[\"']oto-domain-researcher"
---

<objective>
Apply the AI-integration scaffolding rewrite per D-17/D-18/D-20 and 04-RESEARCH § "AI-integration scaffolding rewrite (D-17/D-18/D-20)".

The upstream `ai-integration-phase` workflow orchestrates four agents: `gsd-framework-selector → gsd-ai-researcher → gsd-domain-researcher → gsd-eval-planner`. After the trim, only `oto-domain-researcher` survives. This plan rewrites both the command and workflow to:

1. Keep the `/oto-ai-integration-phase` command useful (D-17)
2. Reference only retained agents as executable Task calls (D-18)
3. Make the bounded scope explicit so the user is not misled (D-20)
4. NOT resurrect any dropped agents (D-19)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflows-agents-port/04-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-RESEARCH.md
@decisions/agent-audit.md
@decisions/ADR-07-agent-trim.md
@oto/commands/oto/ai-integration-phase.md
@oto/workflows/ai-integration-phase.md

<interfaces>
<!-- Surviving + dropped agents in this workflow -->

After the rebrand engine, the file uses `oto-` prefixes:
- `oto-framework-selector` → DROPPED (ADR-07; no replacement)
- `oto-ai-researcher` → DROPPED (ADR-07; no replacement)
- `oto-domain-researcher` → RETAINED (a research-role agent that produces RESEARCH.md fragments)
- `oto-eval-planner` → DROPPED (ADR-07; no replacement)

`oto-domain-researcher` frontmatter (from RESEARCH § "Agents (23 KEEP rows)"):
- Tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*
- Sandbox (per AGT-04 plan 04-06): `workspace-write`

Other references the engine emits:
- `oto-sdk query resolve-model gsd-{framework-selector,ai-researcher,eval-planner}` — these env-var lookups would resolve to `null` at runtime; remove them.
- AI-SPEC.md template path references — survive untouched (the template itself is purely documentary)

<!-- ANTI-LEAK PATTERN — CRITICAL FOR THIS PLAN -->
<!--
The Wave 3 test `phase-04-no-dropped-agents.test.cjs` (plan 04-07 Task 1) does a SUBSTRING
match across the WHOLE FILE CONTENT (including HTML comments and prose) of every shipped-
payload file. If the strings `oto-framework-selector`, `oto-ai-researcher`,
`oto-eval-planner`, or `oto-eval-auditor` appear ANYWHERE in this file — even inside
`<!-- ... -->` HTML comments — the test will fail.

Plan 04-05 Task 1 establishes the canonical defensive pattern: in DEFERRED blocks, refer
to dropped agents by their UN-PREFIXED name ("the eval-auditor agent",
"the framework-selector agent") rather than the `oto-`-prefixed substring. Mirror that
pattern here. See 04-05-PLAN.md Task 1 NOTE block for the precedent.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite oto/commands/oto/ai-integration-phase.md objective and process</name>
  <files>oto/commands/oto/ai-integration-phase.md</files>
  <read_first>
    - oto/commands/oto/ai-integration-phase.md (the entire current file — to identify the `<objective>` block, `<process>` block, and any `subagent_type=` references)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "AI-integration scaffolding rewrite" (lines ~563-577)
    - 04-05-PLAN.md Task 1 (canonical anti-leak pattern: refer to dropped agents by un-prefixed name inside DEFERRED HTML comments to avoid tripping the Wave 3 substring test)
  </read_first>
  <action>
    Open `oto/commands/oto/ai-integration-phase.md`. The engine has already rebranded `gsd-` to `oto-` in the file. We need to rewrite the parts that still reference dropped agents.

    Step A — Find all references to `oto-framework-selector`, `oto-ai-researcher`, `oto-eval-planner` (the three dropped agents). Use:
    ```bash
    grep -n -E "oto-(framework-selector|ai-researcher|eval-planner)" oto/commands/oto/ai-integration-phase.md
    ```

    Step B — Rewrite the `<objective>` block to explicitly describe v0.1.0's bounded state. Replace the existing objective text with content matching this template:

    ```
    <objective>
    Generate an AI-feature design contract (AI-SPEC.md) for a phase that adds AI-powered capability.

    **v0.1.0 status (bounded scope per ADR-07):**
    - **Live:** Domain Research step (via `oto-domain-researcher`) — gathers domain knowledge and writes RESEARCH.md fragments.
    - **Deferred:** Framework Selection, AI Research, and Evaluation Planning are deferred until eval-tooling agents return in v2 (see ADR-07 in `decisions/`). The workflow will print explicit DEFERRED blocks where these steps would run, with manual-fill instructions.

    Purpose: Keep the command discoverable in `/oto-help` and useful for partial scaffolding; do NOT pretend the deferred steps work.
    Output: Domain-research artifacts + an AI-SPEC.md skeleton with explicit TODO sections for the deferred steps.
    </objective>
    ```

    Step C — In the `<process>` block (or whatever the file calls its execution-flow section), replace any `Task(subagent_type="oto-framework-selector"...)`, `Task(subagent_type="oto-ai-researcher"...)`, or `Task(subagent_type="oto-eval-planner"...)` calls with a DEFERRED comment block.

    **CRITICAL ANTI-LEAK RULE:** The DEFERRED HTML comment template MUST NOT contain the substrings `oto-framework-selector`, `oto-ai-researcher`, or `oto-eval-planner` — even inside `<!-- ... -->` comments. The Wave 3 leak test (`phase-04-no-dropped-agents.test.cjs`, plan 04-07 Task 1) does substring matching across the whole file content, not just executable code. Refer to the dropped agents by their UN-PREFIXED name. This mirrors the canonical pattern in 04-05-PLAN.md Task 1.

    Use this template (note: NO `oto-` prefix on dropped-agent names anywhere in the comment text):

    ```
    <!-- DEFERRED step: Framework Selection (was the framework-selector agent). This agent was dropped in ADR-07 with no v0.1.0 replacement. Manual action: ask the user which framework/library will host the AI feature, record in AI-SPEC.md § Framework. See .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "AI-integration scaffolding rewrite" -->
    ```

    Apply the same pattern for the other two deferred steps. Use phrasings like:
    - "the framework-selector agent" (NOT `oto-framework-selector`)
    - "the AI researcher agent" (NOT `oto-ai-researcher`)
    - "the eval-planner agent" (NOT `oto-eval-planner`)

    Keep the rationale (ADR-07 reference) intact — only the agent-name substring is being scrubbed.

    Step D — Remove any `oto-sdk query resolve-model oto-{framework-selector,ai-researcher,eval-planner}` env-var lookups. These would resolve to `null`. If the surrounding flow depended on the resolved value, replace with a comment that this is deferred (using the un-prefixed-name convention from Step C).

    Step E — Leave any `oto-domain-researcher` references intact (it's retained).

    After the edit, run:
    ```bash
    grep -E "oto-(framework-selector|ai-researcher|eval-planner)" oto/commands/oto/ai-integration-phase.md
    ```
    Expect zero matches anywhere in the file (including within HTML comments).
  </action>
  <acceptance_criteria>
    - `grep -c "oto-framework-selector" oto/commands/oto/ai-integration-phase.md` returns 0 (zero matches anywhere, including inside HTML comments)
    - `grep -c "oto-ai-researcher" oto/commands/oto/ai-integration-phase.md` returns 0
    - `grep -c "oto-eval-planner" oto/commands/oto/ai-integration-phase.md` returns 0
    - `grep -c "DEFERRED" oto/commands/oto/ai-integration-phase.md` returns >= 1 (positive check: the deferral marker MUST be present — cannot satisfy the zero-substring checks above by simply deleting the deferred blocks entirely)
    - `grep -c "ADR-07" oto/commands/oto/ai-integration-phase.md` returns >= 1
    - The `<objective>` block explicitly mentions "v0.1.0" and "Deferred"
    - File remains valid markdown with frontmatter intact
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "oto-(framework-selector|ai-researcher|eval-planner)" oto/commands/oto/ai-integration-phase.md && [ "$(grep -c DEFERRED oto/commands/oto/ai-integration-phase.md)" -ge "1" ] && grep -q "ADR-07" oto/commands/oto/ai-integration-phase.md</automated>
  </verify>
  <done>Command file describes bounded scope; no dropped-agent substrings anywhere in file; DEFERRED markers present; ADR-07 cited as warrant</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite oto/workflows/ai-integration-phase.md to run only Domain Research and DEFER the rest</name>
  <files>oto/workflows/ai-integration-phase.md</files>
  <read_first>
    - oto/workflows/ai-integration-phase.md (the full file, to map upstream steps 5/6/7/9 to current line ranges)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "AI-integration scaffolding rewrite" point 3 (lines ~571-575) — specifies which steps to keep vs replace with DEFERRED blocks
    - 04-05-PLAN.md Task 1 (canonical anti-leak pattern: refer to dropped agents by un-prefixed name inside DEFERRED HTML comments to avoid tripping the Wave 3 substring test)
  </read_first>
  <action>
    Open `oto/workflows/ai-integration-phase.md`.

    Step A — Find every reference to dropped agents:
    ```bash
    grep -n -E "oto-(framework-selector|ai-researcher|eval-planner|eval-auditor)" oto/workflows/ai-integration-phase.md
    ```

    Step B — For step 6 (Domain Research): KEEP. The Task call should now reference `oto-domain-researcher` (the engine already rebranded). Confirm the Task block references this agent and is structurally intact.

    Step C — For steps 5 (Framework Selection), 7 (AI Research), and 9 (Eval Planning):

    Replace the agent-spawning Task block in each step with a DEFERRED block that:
    1. Documents what the dropped step was meant to do
    2. Names the dropped agent for traceability — using the UN-PREFIXED name (e.g., "the framework-selector agent") so the Wave 3 substring test passes
    3. Tells the user how to manually fill the corresponding AI-SPEC.md section
    4. Tells the workflow to print the deferred message at runtime so the user is not silently skipped

    **CRITICAL ANTI-LEAK RULE:** The DEFERRED HTML comments and any prose MUST NOT contain the substrings `oto-framework-selector`, `oto-ai-researcher`, `oto-eval-planner`, or `oto-eval-auditor`. The Wave 3 test (`phase-04-no-dropped-agents.test.cjs`, plan 04-07 Task 1) substring-matches the whole file content. Use un-prefixed names. This mirrors 04-05-PLAN.md Task 1's defensive pattern.

    Example DEFERRED block for step 5 (note: NO `oto-` prefix on the dropped agent name):
    ```
    ### Step 5 — Framework Selection (DEFERRED in v0.1.0)

    <!-- DEFERRED: was the framework-selector agent per ADR-07 (decisions/agent-audit.md, decisions/ADR-07-agent-trim.md). No retained replacement; deferred until eval-tooling agents return in v2. -->

    Print to the user:

    > Step 5 — Framework Selection — DEFERRED in oto v0.1.0.
    > Manually decide which framework/library will host the AI feature (e.g., Vercel AI SDK, LangChain, raw OpenAI client) and record under § Framework in AI-SPEC.md.
    > Tracking: ADR-07; will be re-enabled when eval-tooling agents return.

    Then proceed to Step 6 (Domain Research, live).
    ```

    Apply the same shape (un-prefixed names) to steps 7 and 9 (and 5):
    - Step 5: "the framework-selector agent" (NOT `oto-framework-selector`)
    - Step 7: "the AI researcher agent" (NOT `oto-ai-researcher`)
    - Step 9: "the eval-planner agent" (NOT `oto-eval-planner`)

    Step D — Remove any `oto-sdk query resolve-model oto-{framework-selector,ai-researcher,eval-planner}` calls. They resolve to `null` at runtime.

    Step E — If `oto-eval-auditor` also appears in this file, replace with the same DEFERRED pattern using the un-prefixed name "the eval-auditor agent" (NOT `oto-eval-auditor`). RESEARCH notes eval-auditor is part of the eval pipeline.

    After the edit, run:
    ```bash
    grep -E "oto-(framework-selector|ai-researcher|eval-planner|eval-auditor)" oto/workflows/ai-integration-phase.md
    ```
    Expect zero matches anywhere (including within HTML comments).
  </action>
  <acceptance_criteria>
    - `grep -c "oto-framework-selector" oto/workflows/ai-integration-phase.md` returns 0 (zero matches anywhere, including inside HTML comments)
    - `grep -c "oto-ai-researcher" oto/workflows/ai-integration-phase.md` returns 0
    - `grep -c "oto-eval-planner" oto/workflows/ai-integration-phase.md` returns 0
    - `grep -c "oto-eval-auditor" oto/workflows/ai-integration-phase.md` returns 0
    - `grep -c "oto-domain-researcher" oto/workflows/ai-integration-phase.md` returns >= 1 (Domain Research step retained)
    - `grep -c "DEFERRED" oto/workflows/ai-integration-phase.md` returns >= 3 (positive check: one per dropped step — cannot satisfy the zero-substring checks by deleting the deferral markers entirely)
    - `grep -c "ADR-07" oto/workflows/ai-integration-phase.md` returns >= 1
    - File remains valid markdown
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "oto-(framework-selector|ai-researcher|eval-planner|eval-auditor)" oto/workflows/ai-integration-phase.md && grep -q "oto-domain-researcher" oto/workflows/ai-integration-phase.md && [ "$(grep -c DEFERRED oto/workflows/ai-integration-phase.md)" -ge "3" ] && grep -q "ADR-07" oto/workflows/ai-integration-phase.md</automated>
  </verify>
  <done>Workflow runs only the Domain Research step live; three dropped steps replaced with explicit DEFERRED blocks using un-prefixed agent names; runtime messaging tells the user what was skipped and why; zero dropped-agent substrings anywhere in file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — markdown content edits within already-rebranded `oto/` tree) | No code execution, no external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | I (Integrity — runtime references resolve to non-existent agents) | shipped command + workflow files | mitigate | Wave 3 `phase-04-no-dropped-agents.test.cjs` asserts zero dropped-agent substrings in shipped payload. Wave 3 `phase-04-task-refs-resolve.test.cjs` asserts every `subagent_type=` value resolves to a retained agent. Both files in scope. |
| T-04-04-02 | I (User mislead by partial functionality) | `/oto-ai-integration-phase` command UX | mitigate | D-20 explicitly requires "useful but bounded": the rewrite makes the deferred state visible at runtime via printed messages so the user is not silently misled into thinking framework selection / eval planning happened. |
| T-04-04-03 | I (Anti-leak compliance — substring leak via HTML comments) | DEFERRED block templates in both files | mitigate | DEFERRED comments use un-prefixed agent names ("the framework-selector agent" not `oto-framework-selector`) per 04-05's canonical pattern. Acceptance criteria assert zero substring matches anywhere AND positive DEFERRED-marker presence, so the executor cannot satisfy the zero-substring check by deleting the deferral entirely. |
</threat_model>

<verification>
- `grep -E "oto-(framework-selector|ai-researcher|eval-planner|eval-auditor)" oto/commands/oto/ai-integration-phase.md oto/workflows/ai-integration-phase.md` returns no matches (anywhere — including inside HTML comments)
- Both files contain at least one `DEFERRED` marker referencing ADR-07
- Workflow file retains a live `Task(subagent_type="oto-domain-researcher" ...)` call for the Domain Research step
- Both files remain valid markdown with frontmatter
</verification>

<success_criteria>
- WF-28 `/oto-ai-integration-phase` is shippable: command resolves to a workflow file, workflow runs the live Domain Research step, deferred steps are explicit
- D-17 satisfied (useful), D-18 satisfied (no broken refs), D-19 satisfied (no resurrection), D-20 satisfied (bounded scope visible)
- Wave 3 anti-leak test (`phase-04-no-dropped-agents.test.cjs`) passes against both files
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflows-agents-port/04-04-SUMMARY.md` with the before/after of the `<objective>` block and the three DEFERRED blocks (so Phase 9 sync can replay).
</output>
