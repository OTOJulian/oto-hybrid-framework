---
phase: 04-core-workflows-agents-port
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - oto/commands/oto/
  - oto/agents/
  - oto/workflows/
  - oto/contexts/
  - oto/templates/
  - oto/references/
  - oto/skills/
  - oto/hooks/
autonomous: true
requirements_addressed:
  - WF-01
  - WF-02
  - WF-03
  - WF-04
  - WF-05
  - WF-06
  - WF-07
  - WF-08
  - WF-09
  - WF-10
  - WF-11
  - WF-12
  - WF-13
  - WF-14
  - WF-15
  - WF-16
  - WF-17
  - WF-18
  - WF-19
  - WF-20
  - WF-21
  - WF-22
  - WF-23
  - WF-24
  - WF-25
  - WF-28
  - WF-29
  - WF-30
must_haves:
  truths:
    - "The rebrand engine has been run with --apply against foundation-frameworks/get-shit-done-main and emitted the rebranded tree under oto/"
    - "oto/commands/oto/ contains the 28 workflow command files for WF-01..25, WF-28..30"
    - "oto/agents/ contains 23 retained agent files (Superpowers code-reviewer.md is absent because inventory marks it DROP)"
    - "oto/workflows/ contains the workflow markdown files including subdirs discuss-phase/{modes,templates}/ and execute-phase/steps/"
    - "Engine coverage manifest reports zero non-allowlisted gsd/superpowers occurrences in the rebranded tree"
    - "ultraplan-phase.md is NOT present in oto/commands/oto/ or oto/workflows/ (per REQUIREMENTS.md Out of Scope override of inventory)"
  artifacts:
    - path: "oto/commands/oto/new-project.md"
      provides: "WF-01 entry point"
    - path: "oto/commands/oto/plan-phase.md"
      provides: "WF-03 entry point (will be patched in Wave 2)"
    - path: "oto/commands/oto/execute-phase.md"
      provides: "WF-04 entry point"
    - path: "oto/commands/oto/verify-work.md"
      provides: "WF-05 entry point"
    - path: "oto/commands/oto/debug.md"
      provides: "WF-10 entry point (will be patched in Wave 2 — drops debug-session-manager refs)"
    - path: "oto/commands/oto/ai-integration-phase.md"
      provides: "WF-28 entry point (will be patched in Wave 2 per D-17/D-18/D-20)"
    - path: "oto/commands/oto/autonomous.md"
      provides: "WF-29 entry point"
    - path: "oto/commands/oto/set-profile.md"
      provides: "WF-30 entry point"
    - path: "oto/agents/oto-planner.md"
      provides: "Retained agent (engine output of agents/gsd-planner.md)"
    - path: "oto/agents/oto-executor.md"
      provides: "Retained agent"
    - path: "oto/agents/oto-verifier.md"
      provides: "Retained agent"
  key_links:
    - from: "oto/commands/oto/new-project.md"
      to: "oto/workflows/new-project.md"
      via: "@-include"
      pattern: "@.*workflows/new-project"
    - from: "oto/commands/oto/plan-phase.md"
      to: "oto/workflows/plan-phase.md"
      via: "@-include"
      pattern: "@.*workflows/plan-phase"
---

<objective>
Generate the Phase 4 baseline by running the rebrand engine in apply mode against `foundation-frameworks/get-shit-done-main/` and committing the resulting `oto/` tree. Per D-01, the engine is the primary porting path; curated fixups follow in Wave 2.

Purpose: Populates `oto/commands/`, `oto/agents/`, `oto/workflows/`, `oto/contexts/`, `oto/templates/`, `oto/references/` (and incidentally `oto/skills/` + `oto/hooks/` source trees, which are Phase 5/6 verification surfaces). This is the single largest commit in Phase 4.
Output: ~250+ markdown files under `oto/`, ready for Wave 2 fixups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-workflows-agents-port/04-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-RESEARCH.md
@CLAUDE.md
@scripts/rebrand.cjs
@rename-map.json
@decisions/file-inventory.md

<interfaces>
<!-- Engine CLI contract — already shipped in Phase 2 -->

```
node scripts/rebrand.cjs --apply --target <upstream> --out <output> [--force] [--owner OWNER]
```

- `--target` accepts a directory; engine walks it respecting `decisions/file-inventory.json` verdicts (KEEP rows produce output, DROP rows skipped)
- `--out` is the rebranded output root (engine creates this if missing)
- `--force` overwrites existing output files
- `--owner` substitutes the GitHub owner in URL rewrites (project-locked: `OTOJulian`)

Engine emits:
- Rebranded files under `<out>/`
- `<out>/.rebrand-manifest.json` summary
- `reports/coverage-manifest.{pre,post}.json` with non-allowlisted occurrence counts

Inventory rows for KEEP files have `target` paths like:
- `commands/gsd/<x>.md` → `oto/commands/oto/<x>.md` (path rule)
- `agents/gsd-<x>.md` → `oto/agents/oto-<x>.md`
- `get-shit-done/workflows/<x>.md` → `oto/workflows/<x>.md`
- `get-shit-done/contexts/<x>.md` → `oto/contexts/<x>.md`
- `get-shit-done/templates/<x>.md` → `oto/templates/<x>.md`
- `get-shit-done/references/<x>.md` → `oto/references/<x>.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run rebrand engine apply against upstream into oto/ tree</name>
  <files>oto/commands/, oto/agents/, oto/workflows/, oto/contexts/, oto/templates/, oto/references/, oto/skills/, oto/hooks/</files>
  <read_first>
    - scripts/rebrand.cjs (lines 1-50 to confirm CLI flag handling and current entry point)
    - rename-map.json (top-level structure: identifier rules, path rules, command rules, etc.)
    - decisions/file-inventory.md § "command" and § "agents" sections (target path conventions)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Pattern 1: Engine-driven port" (lines ~202-220) and § "Code Examples > Example 1" (the exact command line)
  </read_first>
  <action>
    From the repo root, run:

    ```bash
    node scripts/rebrand.cjs --apply \
      --target foundation-frameworks/get-shit-done-main \
      --out oto/ \
      --force \
      --owner OTOJulian
    ```

    If the engine emits any error or the post-coverage manifest reports >0 non-allowlisted matches, STOP and surface the error before proceeding. Do not hand-edit `oto/` to silence engine output — fix engine input or rename-map per D-04.

    Expected scale (from RESEARCH § "Phase 4 inclusion set"): ~78 commands, 23 agents, ~88 workflows + subdirs, 3 contexts, 43 templates, 51 references. The engine also emits `oto/skills/` and `oto/hooks/` sources from KEEP inventory rows but those are Phase 5/6 verification surfaces (this plan ports them as files but does NOT certify them).

    **Inventory override for `ultraplan-phase`:** Per the planning_context resolved-open-questions §1, REQUIREMENTS.md "Out of Scope" overrides inventory for `ultraplan-phase.md`. After the engine run, delete `oto/commands/oto/ultraplan-phase.md` AND `oto/workflows/ultraplan-phase.md` if either exists. This is the ONLY hand-edit allowed in this plan; document it explicitly in the SUMMARY.

    Verify the engine output looks right by spot-checking these specific files exist:
    - `oto/commands/oto/new-project.md`
    - `oto/commands/oto/plan-phase.md`
    - `oto/commands/oto/execute-phase.md`
    - `oto/commands/oto/verify-work.md`
    - `oto/commands/oto/debug.md`
    - `oto/commands/oto/ai-integration-phase.md`
    - `oto/agents/oto-planner.md`
    - `oto/agents/oto-executor.md`
    - `oto/agents/oto-verifier.md`
    - `oto/workflows/new-project.md`
    - `oto/workflows/plan-phase.md`
    - `oto/workflows/discuss-phase/modes/default.md`
    - `oto/workflows/execute-phase/steps/codebase-drift-gate.md`

    And confirm Superpowers `code-reviewer.md` did NOT land:
    - `oto/agents/code-reviewer.md` MUST NOT exist (it is a DROP row in inventory; AGT-02)
    - `oto/agents/oto-code-reviewer.md` MUST exist (it's the GSD agent rebranded)

    DO NOT run the engine more than once unnecessarily. If you must re-run, use `--force` to overwrite cleanly. Do not commit `reports/` output — it is regenerated each run.
  </action>
  <acceptance_criteria>
    - `oto/commands/oto/new-project.md` exists
    - `oto/commands/oto/plan-phase.md` exists
    - `oto/commands/oto/execute-phase.md` exists
    - `oto/commands/oto/verify-work.md` exists
    - `oto/commands/oto/debug.md` exists
    - `oto/commands/oto/ai-integration-phase.md` exists
    - `oto/agents/oto-planner.md` exists
    - `oto/agents/oto-executor.md` exists
    - `oto/agents/oto-verifier.md` exists
    - `oto/workflows/new-project.md` exists
    - `oto/workflows/plan-phase.md` exists
    - `oto/workflows/discuss-phase/modes/default.md` exists
    - `oto/workflows/execute-phase/steps/codebase-drift-gate.md` exists
    - `oto/agents/code-reviewer.md` does NOT exist (Superpowers version is DROP)
    - `oto/agents/oto-code-reviewer.md` exists (rebranded GSD version)
    - `oto/commands/oto/ultraplan-phase.md` does NOT exist (REQUIREMENTS Out of Scope override)
    - `oto/workflows/ultraplan-phase.md` does NOT exist
    - `find oto/commands/oto -name '*.md' | wc -l` returns >= 70 (78 minus any DROPs/overrides)
    - `find oto/agents -name 'oto-*.md' | wc -l` returns 23
    - `find oto/workflows -name '*.md' | wc -l` returns >= 80
    - `grep -rE '^name: code-reviewer$' oto/agents/ 2>/dev/null` returns no matches (AGT-02)
    - `grep -r '\bgsd-' oto/commands/ oto/agents/ oto/workflows/ oto/templates/ oto/references/ 2>/dev/null | grep -v 'foundation-frameworks' | wc -l` returns 0 (no un-rebranded GSD identifiers in shipped payload)
  </acceptance_criteria>
  <verify>
    <automated>test -f oto/commands/oto/new-project.md && test -f oto/agents/oto-planner.md && test ! -f oto/agents/code-reviewer.md && test ! -f oto/commands/oto/ultraplan-phase.md && [ "$(find oto/agents -name 'oto-*.md' | wc -l | tr -d ' ')" = "23" ]</automated>
  </verify>
  <done>Engine apply complete; oto/ tree populated; ultraplan files removed; spot-check counts match expected scale</done>
</task>

<task type="auto">
  <name>Task 2: Implement rebrand-smoke test body and confirm it passes</name>
  <files>tests/phase-04-rebrand-smoke.test.cjs</files>
  <read_first>
    - tests/phase-04-rebrand-smoke.test.cjs (the Wave 0 scaffold; replace t.todo() with real assertions)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Phase 4 inclusion set" (lines ~256-355 — exact expected counts)
    - tests/phase-03-install-claude.integration.test.cjs (test shape precedent)
  </read_first>
  <action>
    Replace the `t.todo()` placeholder in `tests/phase-04-rebrand-smoke.test.cjs` with real assertions that the Task 1 output is structurally correct. The test does NOT re-run the engine — it asserts on the already-committed `oto/` tree. Use:

    ```js
    'use strict';
    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');

    const REPO_ROOT = path.resolve(__dirname, '..');

    function countFiles(dir, ext = '.md') {
      const full = path.join(REPO_ROOT, dir);
      if (!fs.existsSync(full)) return 0;
      let count = 0;
      const stack = [full];
      while (stack.length) {
        const cur = stack.pop();
        for (const entry of fs.readdirSync(cur, { withFileTypes: true })) {
          const p = path.join(cur, entry.name);
          if (entry.isDirectory()) stack.push(p);
          else if (entry.isFile() && p.endsWith(ext)) count++;
        }
      }
      return count;
    }

    test('phase-04 rebrand-smoke: oto/commands/oto contains expected core commands', () => {
      const expected = [
        'oto/commands/oto/new-project.md',
        'oto/commands/oto/discuss-phase.md',
        'oto/commands/oto/plan-phase.md',
        'oto/commands/oto/execute-phase.md',
        'oto/commands/oto/verify-work.md',
        'oto/commands/oto/ship.md',
        'oto/commands/oto/debug.md',
        'oto/commands/oto/ai-integration-phase.md',
        'oto/commands/oto/autonomous.md',
        'oto/commands/oto/set-profile.md',
        'oto/commands/oto/help.md',
        'oto/commands/oto/progress.md',
        'oto/commands/oto/resume-work.md',
        'oto/commands/oto/pause-work.md',
      ];
      for (const rel of expected) {
        assert.ok(fs.existsSync(path.join(REPO_ROOT, rel)), `missing: ${rel}`);
      }
    });

    test('phase-04 rebrand-smoke: oto/agents has exactly 23 oto-* agent files', () => {
      const dir = path.join(REPO_ROOT, 'oto/agents');
      const files = fs.readdirSync(dir).filter(f => f.startsWith('oto-') && f.endsWith('.md'));
      assert.equal(files.length, 23, `expected 23 retained agents, got ${files.length}`);
    });

    test('phase-04 rebrand-smoke: oto/workflows includes nested subdirs', () => {
      assert.ok(fs.existsSync(path.join(REPO_ROOT, 'oto/workflows/discuss-phase/modes/default.md')));
      assert.ok(fs.existsSync(path.join(REPO_ROOT, 'oto/workflows/execute-phase/steps/codebase-drift-gate.md')));
    });

    test('phase-04 rebrand-smoke: contexts, templates, references populated', () => {
      assert.ok(countFiles('oto/contexts') >= 3, 'expected 3+ context files');
      assert.ok(countFiles('oto/templates') >= 30, 'expected 30+ template files');
      assert.ok(countFiles('oto/references') >= 40, 'expected 40+ reference files');
    });

    test('phase-04 rebrand-smoke: ultraplan-phase removed (REQUIREMENTS Out of Scope override)', () => {
      assert.ok(!fs.existsSync(path.join(REPO_ROOT, 'oto/commands/oto/ultraplan-phase.md')));
      assert.ok(!fs.existsSync(path.join(REPO_ROOT, 'oto/workflows/ultraplan-phase.md')));
    });
    ```

    Run the test and confirm green.
  </action>
  <acceptance_criteria>
    - `tests/phase-04-rebrand-smoke.test.cjs` no longer contains `t.todo(`
    - `node --test tests/phase-04-rebrand-smoke.test.cjs` exits 0
    - Test output contains 5 passing tests
    - The test file imports `node:test`, `node:assert/strict`, `node:fs`, `node:path`
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/phase-04-rebrand-smoke.test.cjs</automated>
  </verify>
  <done>rebrand-smoke test passes; engine output is asserted structurally correct</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → `oto/` filesystem | Engine writes ~250+ files into `oto/`. The engine's input is `foundation-frameworks/get-shit-done-main/`, `rename-map.json`, `decisions/file-inventory.json` — all repo-local, not external. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | I (Information disclosure / supply-chain integrity) | rebrand engine output in `oto/` | mitigate | Engine is verdict-aware (DROP rows skipped) and allowlist-aware (LICENSE / copyright preserved). Phase 2 round-trip test (Phase 2 SC#4) ensures engine output is deterministic. Wave 3 verification (`phase-04-no-dropped-agents.test.cjs`, `phase-04-planning-leak.test.cjs`) catches any leak the engine missed. |
| T-04-02-02 | T (Tampering — accidental hand-edit of generated tree) | `oto/` tree | mitigate | This plan permits ONE intentional hand-deletion (ultraplan-phase, per REQUIREMENTS Out of Scope) and explicitly forbids any other hand-edits. Documented in task action and SUMMARY. Wave 2 fixups apply curated patches with explicit before/after snippets so Phase 9 sync can replay them. |
</threat_model>

<verification>
- `node scripts/rebrand.cjs --apply` succeeded without error
- `oto/commands/oto/`, `oto/agents/`, `oto/workflows/` populated with expected core files
- `oto/agents/code-reviewer.md` (Superpowers) does NOT exist
- `oto/commands/oto/ultraplan-phase.md` does NOT exist (REQUIREMENTS override)
- `tests/phase-04-rebrand-smoke.test.cjs` passes (5 tests green)
</verification>

<success_criteria>
- Phase 4 baseline tree exists under `oto/`
- All success-criterion-1 commands resolved (28 listed `/oto-*` workflows have command files)
- 23 retained agents present, Superpowers code-reviewer absent
- Wave 2 has a clean tree to apply fixups against
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflows-agents-port/04-02-SUMMARY.md` documenting the engine command run, file counts produced, and the ultraplan deletion override.
</output>
