---
phase: 04-core-workflows-agents-port
plan: 05
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - oto/workflows/eval-review.md
  - oto/workflows/ingest-docs.md
  - oto/workflows/profile-user.md
autonomous: true
requirements_addressed:
  - WF-19
  - WF-24
must_haves:
  truths:
    - "oto/workflows/eval-review.md no longer references oto-eval-auditor as an executable Task call; the workflow body is marked deferred but the file remains so commands/oto/eval-review.md (KEEP) resolves per D-12"
    - "oto/workflows/ingest-docs.md no longer references oto-doc-classifier or oto-doc-synthesizer as executable Task calls; bounded-deferred per agent-audit.md"
    - "oto/workflows/profile-user.md is either deleted (per planning_context resolved-questions §2) or marked deferred with explicit header explaining no caller exists"
    - "After this plan, no references to dropped agents (other than oto-pattern-mapper / oto-debug-session-manager handled by 04-03 and the AI-integration trio handled by 04-04) remain in shipped payload"
  artifacts:
    - path: "oto/workflows/eval-review.md"
      provides: "WF-19-adjacent workflow file (commands/oto/eval-review.md @-includes it; D-12 satisfied)"
    - path: "oto/workflows/ingest-docs.md"
      provides: "Workflow file with explicit deferred body (D-11)"
  key_links:
    - from: "oto/commands/oto/eval-review.md"
      to: "oto/workflows/eval-review.md"
      via: "@-include"
      pattern: "@.*workflows/eval-review"
---

<objective>
Apply the remaining dropped-agent rewrites for the workflow trio identified in 04-RESEARCH § "Workflows referencing dropped agents in prose":

1. `oto/workflows/eval-review.md` — references `oto-eval-auditor` (DROP). Mark workflow body deferred; keep file present so the corresponding command resolves per D-12.
2. `oto/workflows/ingest-docs.md` — references `oto-doc-classifier` and `oto-doc-synthesizer` (both DROP). Bounded-deferred per RESEARCH recommendation (the doc-writer/verifier roles do not cleanly absorb classifier+synthesizer pipeline semantics).
3. `oto/workflows/profile-user.md` — references `oto-user-profiler` (DROP). The corresponding command is also DROP, so the file is inert. Per resolved-question §2, delete the workflow file (no caller exists in shipped payload).

This plan, combined with 04-03 and 04-04, eliminates every dropped-agent reference from shipped payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflows-agents-port/04-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-RESEARCH.md
@decisions/agent-audit.md
@decisions/ADR-07-agent-trim.md
@oto/workflows/eval-review.md
@oto/workflows/ingest-docs.md
@oto/workflows/profile-user.md

<interfaces>
<!-- Dropped agents in scope for this plan -->

After rebrand:
- `oto-eval-auditor` → DROPPED (ADR-07; v2 NICH-V2-* if anything)
- `oto-doc-classifier` → DROPPED (audit notes role differs from doc-writer; no clean merge)
- `oto-doc-synthesizer` → DROPPED (same)
- `oto-user-profiler` → DROPPED (NICH-V2-03; v2-deferred)

`oto-doc-writer` and `oto-doc-verifier` are RETAINED but the audit explicitly says they do NOT absorb the classifier/synthesizer pipeline semantics.

Per planning_context resolved-questions §2: `oto/workflows/profile-user.md` should be deleted (D-11 says workflows referencing dropped agents must be deferred or rewritten; with no caller, deletion is the cleanest path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Defer oto/workflows/eval-review.md body and keep file present</name>
  <files>oto/workflows/eval-review.md</files>
  <read_first>
    - oto/workflows/eval-review.md (full file — to identify the executable Task calls + the surrounding flow)
    - oto/commands/oto/eval-review.md (confirm it `@`-includes `workflows/eval-review.md` so D-12 requires the file to exist)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Category A" item 5 (lines ~487-488)
  </read_first>
  <action>
    Open `oto/workflows/eval-review.md`. Locate every reference to `oto-eval-auditor`:
    ```bash
    grep -n "oto-eval-auditor" oto/workflows/eval-review.md
    ```

    The recommended pattern (per RESEARCH): keep the file present (so `commands/oto/eval-review.md` resolves per D-12), but replace executable Task calls and the bulk of the body with a deferred header.

    Rewrite strategy:

    1. Preserve the frontmatter (name/description/etc.)
    2. Replace the body content with a deferred-state document of the form:

    ```markdown
    ---
    {existing frontmatter}
    ---

    # eval-review (DEFERRED in oto v0.1.0)

    <!-- DEFERRED: this workflow's executable steps depended on `oto-eval-auditor`, which was dropped in ADR-07 (decisions/agent-audit.md). No retained replacement; deferred until eval-tooling agents return in v2. -->

    ## Status

    The `/oto-eval-review` command remains discoverable in `/oto-help` per D-12 (every command must point to an existing workflow file), but its executable body is deferred until evaluation-tooling agents return in v2.

    ## What this workflow was meant to do

    Audit the eval results from a phase that ran AI evaluations, surfacing flaky tests, eval-set drift, and prompt-regression risk. The original implementation used `oto-eval-auditor` to walk eval reports under `.oto/evals/` and emit a structured audit.

    ## Manual approximation (until v2)

    1. Inspect any eval reports your phase produced under `.oto/evals/`
    2. Manually compare pass-rates and flag regressions
    3. Record findings in your phase's `VERIFICATION.md` § Eval Audit section

    ## Tracking

    - ADR-07 (`decisions/ADR-07-agent-trim.md`)
    - Reactivation criterion: a v2 milestone restoring eval-tooling agents (`oto-eval-auditor` or successor)
    ```

    Adjust the prose to match the actual purpose of the upstream workflow if the assumption above is wrong (read the file first to confirm).

    After the edit:
    ```bash
    grep -c "oto-eval-auditor" oto/workflows/eval-review.md   # expect 0 matches as a Task call; the comment may mention it as historical context, but if so it must be inside an HTML comment
    ```

    NOTE: The DEFERRED HTML comment may contain the dropped-agent name as a backtick-quoted reference (for traceability). The Wave 3 test `phase-04-no-dropped-agents.test.cjs` walks for any substring match of `oto-eval-auditor` — so KEEP the substring out of the file entirely. Reword the comment to say "the eval-auditor agent" without the `oto-` prefix substring. Example: `<!-- DEFERRED: this workflow's executable steps depended on the eval-tooling auditor agent which was dropped in ADR-07. -->` This avoids tripping the substring check.
  </action>
  <acceptance_criteria>
    - `grep -c "oto-eval-auditor" oto/workflows/eval-review.md` returns 0
    - `grep -c "DEFERRED" oto/workflows/eval-review.md` returns >= 1
    - `grep -c "ADR-07" oto/workflows/eval-review.md` returns >= 1
    - File still has its original frontmatter (`---` block at top intact)
    - File is still present (not deleted) — `test -f oto/workflows/eval-review.md` succeeds
    - File contains a "Manual approximation" section so the user has a fallback
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "oto-eval-auditor" oto/workflows/eval-review.md && grep -q "DEFERRED" oto/workflows/eval-review.md && test -f oto/workflows/eval-review.md</automated>
  </verify>
  <done>eval-review workflow file present; body deferred; no oto-eval-auditor substring; manual approximation documented</done>
</task>

<task type="auto">
  <name>Task 2: Defer oto/workflows/ingest-docs.md body, drop classifier/synthesizer references</name>
  <files>oto/workflows/ingest-docs.md</files>
  <read_first>
    - oto/workflows/ingest-docs.md (full file — to find every classifier/synthesizer reference and the surrounding flow)
    - oto/commands/oto/ingest-docs.md (if it exists — confirm it @-includes the workflow per D-12)
    - decisions/agent-audit.md (confirms doc-classifier and doc-synthesizer are DROP, and that doc-writer/verifier do NOT absorb their roles cleanly)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Category A" item 6 (lines ~489-490) and § "Workflows referencing dropped agents in prose" (lines ~466-467)
  </read_first>
  <action>
    Open `oto/workflows/ingest-docs.md`. Locate every reference to `oto-doc-classifier` and `oto-doc-synthesizer`:
    ```bash
    grep -n -E "oto-doc-(classifier|synthesizer)" oto/workflows/ingest-docs.md
    ```

    Rewrite strategy (bounded-deferred, similar to eval-review):

    1. Preserve frontmatter
    2. Replace body with a deferred-state document:

    ```markdown
    ---
    {existing frontmatter}
    ---

    # ingest-docs (DEFERRED in oto v0.1.0)

    <!-- DEFERRED: this workflow's pipeline depended on the doc classifier and doc synthesizer agents which were dropped in ADR-07 (decisions/agent-audit.md). The retained `oto-doc-writer` and `oto-doc-verifier` agents do NOT cleanly absorb the classifier→synthesizer pipeline semantics; merging would cargo-cult the API. Deferred until v2. -->

    ## Status

    `/oto-ingest-docs` is discoverable in `/oto-help` per D-12 but its executable body is deferred.

    ## What this workflow was meant to do

    Walk a directory of source documents, classify them by type, then synthesize a structured representation. The classifier+synthesizer pipeline is materially different from `oto-doc-writer`'s authoring role.

    ## Manual approximation (until v2)

    1. To author documentation from scratch, use `/oto-docs-update` (which runs `oto-doc-writer` + `oto-doc-verifier`)
    2. To classify a single doc manually, read it and tag it in your project's documentation index

    ## Tracking

    - ADR-07
    - Reactivation criterion: v2 milestone restoring doc-classifier / doc-synthesizer or designing a successor
    ```

    Use the same tip from Task 1: keep the dropped-agent substrings (`oto-doc-classifier`, `oto-doc-synthesizer`) out of the file entirely, even inside HTML comments. Reword as "the doc classifier and doc synthesizer agents" so the leak test passes.

    After the edit:
    ```bash
    grep -c -E "oto-doc-(classifier|synthesizer)" oto/workflows/ingest-docs.md
    ```
    Expect 0 matches.
  </action>
  <acceptance_criteria>
    - `grep -c "oto-doc-classifier" oto/workflows/ingest-docs.md` returns 0
    - `grep -c "oto-doc-synthesizer" oto/workflows/ingest-docs.md` returns 0
    - `grep -c "DEFERRED" oto/workflows/ingest-docs.md` returns >= 1
    - `grep -c "ADR-07" oto/workflows/ingest-docs.md` returns >= 1
    - File still has its frontmatter
    - File is still present
    - File mentions `/oto-docs-update` as the manual fallback for authoring (the retained doc workflow)
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "oto-doc-(classifier|synthesizer)" oto/workflows/ingest-docs.md && grep -q "DEFERRED" oto/workflows/ingest-docs.md && test -f oto/workflows/ingest-docs.md</automated>
  </verify>
  <done>ingest-docs workflow file present; body deferred; no classifier/synthesizer substrings; doc-writer fallback referenced</done>
</task>

<task type="auto">
  <name>Task 3: Delete oto/workflows/profile-user.md (no caller exists)</name>
  <files>oto/workflows/profile-user.md</files>
  <read_first>
    - oto/workflows/profile-user.md (confirm the file exists in the rebranded tree and references `oto-user-profiler`)
    - planning_context resolved-questions §2 (locks the deletion decision)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Category A" item 7 (lines ~491-492)
  </read_first>
  <action>
    Verify there is no caller for `oto/workflows/profile-user.md` in shipped payload:
    ```bash
    grep -rE "@.*workflows/profile-user|profile-user\\.md" oto/commands/ oto/workflows/ oto/agents/ 2>/dev/null
    ```

    The corresponding command `oto/commands/oto/profile-user.md` should NOT exist (it's a DROP row in inventory; the engine skipped it). If for any reason it exists, also delete it and document the cleanup in the SUMMARY.

    If the grep returns no callers (expected), delete the workflow file:
    ```bash
    rm oto/workflows/profile-user.md
    ```

    If the grep returns a caller, STOP and surface to the user — Assumption A6 in RESEARCH would be wrong and the deletion path needs reconsideration. In that case, fall back to the deferred-body pattern from Task 1/2.
  </action>
  <acceptance_criteria>
    - `test ! -f oto/workflows/profile-user.md` (file is deleted)
    - `grep -rE "@.*workflows/profile-user|profile-user\\.md" oto/commands/ oto/workflows/ oto/agents/` returns no matches (no dangling reference)
    - `grep -rc "oto-user-profiler" oto/ 2>/dev/null | grep -v ':0$'` returns no matches (the dropped-agent substring is gone from shipped payload)
  </acceptance_criteria>
  <verify>
    <automated>test ! -f oto/workflows/profile-user.md && [ "$(grep -rE 'oto-user-profiler' oto/ 2>/dev/null | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>profile-user workflow file deleted; no callers; oto-user-profiler substring absent from shipped payload</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — local file edits + one deletion within `oto/`) | No code execution, no external services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | I (Integrity — dangling references after deletion) | `oto/commands/`, `oto/workflows/` | mitigate | Task 3 grep precondition checks no caller exists before deletion. If a caller is found, the task escalates rather than orphan-references. Wave 3 `phase-04-command-to-workflow.test.cjs` is the backstop: any command @-include pointing to a missing file fails the test. |
| T-04-05-02 | I (User mislead by deferred-but-discoverable commands) | `/oto-eval-review`, `/oto-ingest-docs` | mitigate | Both deferred workflows include explicit "Status: DEFERRED" headers and "Manual approximation" sections, so users discovering them via `/oto-help` are not silently misled. D-11 satisfied. |
</threat_model>

<verification>
- All three target files updated as specified (two deferred, one deleted)
- `grep -rE "oto-(eval-auditor|doc-classifier|doc-synthesizer|user-profiler)" oto/` returns no matches
- `oto/workflows/eval-review.md` and `oto/workflows/ingest-docs.md` still exist with frontmatter intact
- `oto/workflows/profile-user.md` does not exist
</verification>

<success_criteria>
- After this plan + 04-03 + 04-04, the dropped-agent leakage from shipped payload is closed
- D-09, D-11, D-19 are all satisfied for these three files
- Wave 3 verification can run `phase-04-no-dropped-agents.test.cjs` and pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflows-agents-port/04-05-SUMMARY.md` listing the three files, the action taken on each (defer/defer/delete), and confirming the no-caller precondition for the deletion.
</output>
