---
phase: 04-core-workflows-agents-port
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-04-rebrand-smoke.test.cjs
  - tests/phase-04-frontmatter-schema.test.cjs
  - tests/phase-04-task-refs-resolve.test.cjs
  - tests/phase-04-no-dropped-agents.test.cjs
  - tests/phase-04-generic-agent-allowlist.test.cjs
  - tests/phase-04-codex-sandbox-coverage.test.cjs
  - tests/phase-04-superpowers-code-reviewer-removed.test.cjs
  - tests/phase-04-planning-leak.test.cjs
  - tests/phase-04-command-to-workflow.test.cjs
  - tests/phase-04-mr01-install-smoke.test.cjs
  - tests/fixtures/phase-04/retained-agents.json
autonomous: true
requirements_addressed: []
must_haves:
  truths:
    - "All 10 Phase 4 verification test files exist as scaffolds with `t.todo()` placeholders"
    - "The retained-agents fixture is parsed from `decisions/agent-audit.md` and stored as JSON"
    - "`node --test tests/phase-04-*.test.cjs` exits 0 (all todos, no failures) before downstream waves run"
  artifacts:
    - path: "tests/phase-04-rebrand-smoke.test.cjs"
      provides: "Wave 0 placeholder for engine-output assertions"
    - path: "tests/phase-04-frontmatter-schema.test.cjs"
      provides: "Wave 0 placeholder for AGT-03 frontmatter"
    - path: "tests/phase-04-task-refs-resolve.test.cjs"
      provides: "Wave 0 placeholder for AGT-03 Task ref resolution"
    - path: "tests/phase-04-no-dropped-agents.test.cjs"
      provides: "Wave 0 placeholder for D-09 enforcement"
    - path: "tests/phase-04-generic-agent-allowlist.test.cjs"
      provides: "Wave 0 placeholder for D-10 allowlist"
    - path: "tests/phase-04-codex-sandbox-coverage.test.cjs"
      provides: "Wave 0 placeholder for AGT-04 coverage"
    - path: "tests/phase-04-superpowers-code-reviewer-removed.test.cjs"
      provides: "Wave 0 placeholder for AGT-02"
    - path: "tests/phase-04-planning-leak.test.cjs"
      provides: "Wave 0 placeholder for D-13/D-15"
    - path: "tests/phase-04-command-to-workflow.test.cjs"
      provides: "Wave 0 placeholder for D-12"
    - path: "tests/phase-04-mr01-install-smoke.test.cjs"
      provides: "Wave 0 placeholder for MR-01 automated portion"
    - path: "tests/fixtures/phase-04/retained-agents.json"
      provides: "Single source of truth for retained agent set used by multiple tests"
  key_links:
    - from: "tests/phase-04-codex-sandbox-coverage.test.cjs"
      to: "tests/fixtures/phase-04/retained-agents.json"
      via: "require('./fixtures/phase-04/retained-agents.json')"
      pattern: "require.*retained-agents"
    - from: "tests/phase-04-task-refs-resolve.test.cjs"
      to: "tests/fixtures/phase-04/retained-agents.json"
      via: "require"
      pattern: "retained-agents"
---

<objective>
Create Wave 0 test scaffolds for Phase 4. Every Phase 4 verification test file gets created as a runnable but empty `node:test` file with `t.todo()` placeholders so downstream waves cannot ship without addressing each one. Also extract the retained-agent set from `decisions/agent-audit.md` into a JSON fixture used by multiple tests.

Purpose: Mirrors Phase 3's Wave 0 precedent (`03-01-PLAN.md`). Prevents the scenario where a downstream task ships work that lacks an automated verification check.
Output: 10 Phase 4 test files + 1 fixture file, all in `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-workflows-agents-port/04-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-RESEARCH.md
@.planning/phases/04-core-workflows-agents-port/04-VALIDATION.md
@CLAUDE.md
@decisions/agent-audit.md
@tests/phase-03-install-claude.integration.test.cjs

<interfaces>
<!-- Existing project test patterns. All Phase 4 scaffolds follow the same shape. -->

Phase 1/2/3 tests use this shape:
```js
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const REPO_ROOT = path.resolve(__dirname, '..');

test('description', (t) => {
  // assertions
});
```

For Wave 0 scaffolds use `t.todo('reason')` to make every test a green-but-deferred placeholder:
```js
test('phase-04-foo: short description', (t) => {
  t.todo('Implemented in Wave N (plan 04-NN)');
});
```

Retained-agent set (from decisions/agent-audit.md KEEP rows; 23 agents) — source for tests/fixtures/phase-04/retained-agents.json:
- oto-advisor-researcher
- oto-assumptions-analyzer
- oto-code-fixer
- oto-code-reviewer
- oto-codebase-mapper
- oto-debugger
- oto-doc-verifier
- oto-doc-writer
- oto-domain-researcher
- oto-executor
- oto-integration-checker
- oto-nyquist-auditor
- oto-phase-researcher
- oto-plan-checker
- oto-planner
- oto-project-researcher
- oto-research-synthesizer
- oto-roadmapper
- oto-security-auditor
- oto-ui-auditor
- oto-ui-checker
- oto-ui-researcher
- oto-verifier
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate retained-agents fixture from agent-audit.md</name>
  <files>tests/fixtures/phase-04/retained-agents.json</files>
  <read_first>
    - decisions/agent-audit.md (read the entire file; identify the 23 KEEP rows; the verdict column lists KEEP/DROP/MERGE)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Agents (23 KEEP rows)" (lines ~299-325 — confirms the exact 23 agent names after rebrand)
  </read_first>
  <action>
    Create `tests/fixtures/phase-04/retained-agents.json` containing the exact 23 retained agent names (after rebrand, with `oto-` prefix). The file MUST be valid JSON parseable by `JSON.parse(fs.readFileSync(...))`. Schema:

    ```json
    {
      "source": "decisions/agent-audit.md (KEEP rows)",
      "count": 23,
      "agents": [
        "oto-advisor-researcher",
        "oto-assumptions-analyzer",
        "oto-code-fixer",
        "oto-code-reviewer",
        "oto-codebase-mapper",
        "oto-debugger",
        "oto-doc-verifier",
        "oto-doc-writer",
        "oto-domain-researcher",
        "oto-executor",
        "oto-integration-checker",
        "oto-nyquist-auditor",
        "oto-phase-researcher",
        "oto-plan-checker",
        "oto-planner",
        "oto-project-researcher",
        "oto-research-synthesizer",
        "oto-roadmapper",
        "oto-security-auditor",
        "oto-ui-auditor",
        "oto-ui-checker",
        "oto-ui-researcher",
        "oto-verifier"
      ],
      "dropped_agents_for_leak_test": [
        "oto-pattern-mapper",
        "oto-doc-classifier",
        "oto-doc-synthesizer",
        "oto-debug-session-manager",
        "oto-ai-researcher",
        "oto-eval-auditor",
        "oto-eval-planner",
        "oto-framework-selector",
        "oto-intel-updater",
        "oto-user-profiler"
      ],
      "generic_allowlist": [
        "general-purpose",
        "generalPurpose",
        "general",
        "X",
        "general_purpose_task"
      ]
    }
    ```

    Use the EXACT agent list above — it was verified against decisions/agent-audit.md and 04-RESEARCH.md § "Agents (23 KEEP rows)". The dropped list and generic allowlist are also pulled from RESEARCH (§ "Pitfall 1" and § "Category C — Generic-agent allowlist (D-10)"). Create the parent directory `tests/fixtures/phase-04/` if it does not exist.
  </action>
  <acceptance_criteria>
    - File `tests/fixtures/phase-04/retained-agents.json` exists
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('tests/fixtures/phase-04/retained-agents.json','utf8')).agents.length)"` prints `23`
    - The JSON contains exactly the 23 agent names listed in the action block (no additions, no omissions)
    - `dropped_agents_for_leak_test` array has exactly 10 entries
    - `generic_allowlist` array has exactly 5 entries
    - File is valid JSON: `node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/phase-04/retained-agents.json','utf8'))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('tests/fixtures/phase-04/retained-agents.json','utf8')); if(j.agents.length!==23)throw new Error('expected 23'); if(j.dropped_agents_for_leak_test.length!==10)throw new Error('expected 10 dropped'); if(j.generic_allowlist.length!==5)throw new Error('expected 5 generic');"</automated>
  </verify>
  <done>retained-agents.json exists with 23/10/5 entries and parses as valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Create 10 Phase 4 test scaffolds with t.todo() placeholders</name>
  <files>
    tests/phase-04-rebrand-smoke.test.cjs,
    tests/phase-04-frontmatter-schema.test.cjs,
    tests/phase-04-task-refs-resolve.test.cjs,
    tests/phase-04-no-dropped-agents.test.cjs,
    tests/phase-04-generic-agent-allowlist.test.cjs,
    tests/phase-04-codex-sandbox-coverage.test.cjs,
    tests/phase-04-superpowers-code-reviewer-removed.test.cjs,
    tests/phase-04-planning-leak.test.cjs,
    tests/phase-04-command-to-workflow.test.cjs,
    tests/phase-04-mr01-install-smoke.test.cjs
  </files>
  <read_first>
    - tests/phase-03-install-claude.integration.test.cjs (current Phase 3 test shape — use as template for `'use strict'`, requires, REPO_ROOT path, test() signature)
    - .planning/phases/04-core-workflows-agents-port/04-VALIDATION.md (the per-task verification map names which test covers which requirement)
    - .planning/phases/04-core-workflows-agents-port/04-RESEARCH.md § "Wave 0 gaps" (lines ~921-933 — the canonical list of 10 test files)
  </read_first>
  <action>
    Create each of the 10 test files. Each file MUST:

    1. Start with `'use strict';`
    2. Require `node:test`, `node:assert/strict`, `node:fs`, `node:path` as needed
    3. Define `const REPO_ROOT = path.resolve(__dirname, '..');`
    4. Contain at least one `test(name, (t) => { t.todo(reason); })` call
    5. Have a top-of-file comment block describing what the test covers, which requirement(s) it backs, and which Wave-N plan will fill in the implementation

    Per-file content (the planned implementation summary belongs in a comment; the body uses `t.todo()`):

    **`tests/phase-04-rebrand-smoke.test.cjs`** (Wave 1, plan 04-02):
    Will assert: running `node scripts/rebrand.cjs --apply --target foundation-frameworks/get-shit-done-main --out <tmp>` against a temp dir produces `<tmp>/commands/oto/`, `<tmp>/agents/`, `<tmp>/workflows/` populated; spot-check counts (>= 70 commands, exactly 23 agents, >= 80 workflows). Covers WF-01..25, WF-28..30 engine output.

    **`tests/phase-04-frontmatter-schema.test.cjs`** (Wave 3, plan 04-07):
    Will walk `oto/agents/oto-*.md`, parse frontmatter (hand-rolled `---/---` split + `key: value` scan per RESEARCH § "Code Examples > Example 2"), assert each agent has `name:` and `description:` keys present (tools may be empty per Pitfall 8), and that `name:` value matches `oto-${path.basename(file, '.md')}`. Covers AGT-03 (frontmatter).

    **`tests/phase-04-task-refs-resolve.test.cjs`** (Wave 3, plan 04-07):
    Will walk `oto/commands/oto/*.md` and `oto/workflows/**/*.md`, regex `/subagent_type\s*[:=]\s*["']([a-zA-Z0-9_-]+)["']/g`, for each match assert agent is in retained set (from fixtures/phase-04/retained-agents.json `agents`) OR in `generic_allowlist`. Covers AGT-03 (Task refs) + D-09.

    **`tests/phase-04-no-dropped-agents.test.cjs`** (Wave 3, plan 04-07):
    Will walk shipped payload roots (`oto/commands`, `oto/agents`, `oto/workflows`, `oto/contexts`, `oto/templates`, `oto/references`, `bin`), for each of the 10 strings in `dropped_agents_for_leak_test`, assert zero substring matches. Covers AGT-03 (no-dropped) + D-09.

    **`tests/phase-04-generic-agent-allowlist.test.cjs`** (Wave 3, plan 04-07):
    Will walk `oto/commands/oto/*.md` and `oto/workflows/**/*.md`, regex generic `subagent_type` values, assert every occurrence is in the `generic_allowlist` set from the fixture (no other generic strings tolerated). Covers D-10.

    **`tests/phase-04-codex-sandbox-coverage.test.cjs`** (Wave 3, plan 04-07):
    Will `require('../bin/lib/runtime-codex.cjs')`, assert `agentSandboxes` is an object, `Object.keys(agentSandboxes)` length is 23, set equality with retained-agents fixture. Each value MUST be `'workspace-write'` or `'read-only'`. Covers AGT-04.

    **`tests/phase-04-superpowers-code-reviewer-removed.test.cjs`** (Wave 3, plan 04-07):
    Will assert `oto/agents/code-reviewer.md` does NOT exist (the Superpowers version), AND `oto/agents/oto-code-reviewer.md` DOES exist. Covers AGT-02.

    **`tests/phase-04-planning-leak.test.cjs`** (Wave 3, plan 04-07):
    Will walk `['oto/commands','oto/agents','oto/workflows','oto/contexts','oto/templates','oto/references','oto/skills','oto/hooks','bin','hooks']`, apply regex `/(?<![A-Za-z0-9_])\.planning(?=\/|["'`)\s$]|$)/g` per file, assert zero matches. Excluded paths (NOT walked): `foundation-frameworks/`, `.planning/`, `decisions/`, `tests/`, `scripts/`, `node_modules/`, `reports/`, `.oto-rebrand-out/`. Covers D-13/D-15/D-16.

    **`tests/phase-04-command-to-workflow.test.cjs`** (Wave 3, plan 04-07):
    Will walk `oto/commands/oto/*.md`, parse `@`-includes (lines starting with `@$HOME/...` or `@workflows/...` or similar), assert each `@`-included file path resolves to an existing file under `oto/`. Covers D-12.

    **`tests/phase-04-mr01-install-smoke.test.cjs`** (Wave 3, plan 04-07):
    Integration test mirroring `tests/phase-03-install-claude.integration.test.cjs`. Will: build a tarball via `npm pack`, install it into a tmp prefix, run `oto install --claude --config-dir <tmp>`, assert `<tmp>/oto/.install.json` exists and lists copied files including ones from `oto/commands/oto/` and `oto/agents/`. Covers MR-01 automated.

    Each scaffold body example:
    ```js
    'use strict';
    // Phase 4 Wave 0 scaffold — implemented in Wave 3 (plan 04-07)
    // Covers: AGT-03 (frontmatter validation per RESEARCH § Pitfall 8)
    // Will: walk oto/agents/, parse frontmatter, assert name: matches file stem.
    const test = require('node:test');
    const path = require('node:path');
    const REPO_ROOT = path.resolve(__dirname, '..');

    test('phase-04 frontmatter-schema: every retained agent has valid frontmatter', (t) => {
      t.todo('Implemented in Wave 3 (plan 04-07) after Wave 1 populates oto/agents/');
    });
    ```

    Use this exact shape for every file. Adjust the comment block, test name, and todo reason per file.
  </action>
  <acceptance_criteria>
    - All 10 files exist under `tests/`
    - `node --test tests/phase-04-*.test.cjs` exits 0 (all tests are todo, none fail)
    - Each file starts with `'use strict';`
    - Each file contains at least one `t.todo(` invocation
    - Each file has a header comment naming the requirement(s) it covers and the downstream plan that will implement it
    - `grep -l "t.todo" tests/phase-04-*.test.cjs | wc -l` returns 10
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-concurrency=4 tests/phase-04-*.test.cjs 2>&1 | grep -E "(# tests|# todo|# fail)" | head -5</automated>
  </verify>
  <done>10 test scaffolds exist; `node --test tests/phase-04-*.test.cjs` exits 0 with 10 todos and 0 failures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Wave 0 only writes test scaffolds and a fixture JSON. No filesystem mutation outside `tests/`, no network, no external services, no untrusted input. |

## STRIDE Threat Register

No realistic threats for this plan. Rationale: scaffolds are inert `t.todo()` placeholders + a static JSON fixture; no code paths execute, no data crosses a trust boundary, no commands invoke external processes. The threat surface is the same as committing a markdown documentation file.
</threat_model>

<verification>
- All 10 Phase 4 scaffold files exist
- `tests/fixtures/phase-04/retained-agents.json` parses as valid JSON with the expected schema
- `node --test --test-concurrency=4 tests/phase-04-*.test.cjs` exits 0 (todos do not fail the run)
- `npm test` passes (Phase 1+2+3+4 todos all green or pass)
</verification>

<success_criteria>
- 10 test scaffolds + 1 fixture file committed
- All scaffolds compile and run as `t.todo()` placeholders
- Downstream Wave 1/2/3 plans can fill in test bodies without creating new files
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflows-agents-port/04-01-SUMMARY.md`
</output>
