---
phase: 07-workstreams-workspaces-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - oto/bin/lib/workstream.cjs
  - oto/bin/lib/oto-tools.cjs
  - oto/workflows/new-workspace.md
  - oto/workflows/list-workspaces.md
  - oto/workflows/remove-workspace.md
  - oto/commands/oto/workstreams.md
  - oto/commands/oto/new-workspace.md
  - oto/commands/oto/list-workspaces.md
  - oto/commands/oto/remove-workspace.md
  - oto/references/workstream-flag.md
autonomous: true
requirements:
  - WF-26
  - WF-27
must_haves:
  truths:
    - "All 8 in-scope files have zero matches for the leak literal regex set"
    - "Every command frontmatter passes the Phase 4 frontmatter-schema test"
    - "Every `oto-sdk query <handler>` invocation in the 4 workflow/command files resolves to a real handler in oto-tools.cjs"
  artifacts:
    - path: "oto/bin/lib/workstream.cjs"
      provides: "Workstream CRUD logic (verified clean)"
      contains: "function migrateToWorkstreams"
    - path: "oto/references/workstream-flag.md"
      provides: "`--ws` resolution priority + session-key probe order"
      contains: "OTO_WORKSTREAM"
  key_links:
    - from: "oto/workflows/new-workspace.md"
      to: "oto-tools.cjs init dispatcher"
      via: "oto-sdk query init.new-workspace"
      pattern: "oto-sdk query init\\.new-workspace"
    - from: "oto/commands/oto/workstreams.md"
      to: "oto-tools.cjs workstream dispatcher"
      via: "oto-sdk query workstream.<subcommand>"
      pattern: "oto-sdk query workstream\\."
---

<objective>
Verification + hand-fixup pass for the 8 in-scope Phase-4-bulk-rebrand-output files (D-01, D-02). Re-run grep against the leak-literal regex set, confirm `oto-sdk query` invocations in the 4 workflow/command files all resolve to real `oto-tools.cjs` handlers, confirm command frontmatter passes Phase 4's existing schema test, and hand-fix any failures in place. NO re-running of `scripts/rebrand.cjs apply` (D-01 forbids it). Phase 4 output is the source of truth; Phase 7 only patches what the engine missed.

Purpose: Establish a clean baseline so downstream plans (skill edit, tests, UAT) operate on verified-clean state. WF-26 + WF-27 require these files to be live; if they leak `gsd-` / `.planning/` substrings or reference dropped handlers, the slash commands will fail at runtime.

Output: 8 files verified clean (or hand-fixed clean) in place. No new files created in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workstreams-workspaces-port/07-CONTEXT.md
@.planning/phases/04-core-workflows-agents-port/04-CONTEXT.md
@oto/references/workstream-flag.md
@tests/phase-04-frontmatter-schema.test.cjs
@tests/phase-04-planning-leak.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-scan the 8 in-scope files for leak literals; hand-fix any hits</name>
  <files>
    oto/bin/lib/workstream.cjs,
    oto/bin/lib/oto-tools.cjs,
    oto/workflows/new-workspace.md,
    oto/workflows/list-workspaces.md,
    oto/workflows/remove-workspace.md,
    oto/commands/oto/workstreams.md,
    oto/commands/oto/new-workspace.md,
    oto/commands/oto/list-workspaces.md,
    oto/commands/oto/remove-workspace.md,
    oto/references/workstream-flag.md
  </files>
  <read_first>
    .planning/phases/07-workstreams-workspaces-port/07-CONTEXT.md (D-02 + `<specifics>` regex set),
    .planning/phases/04-core-workflows-agents-port/04-CONTEXT.md (Phase 4 leak-literal rule this phase reuses),
    oto/bin/lib/workstream.cjs (full file before any edits),
    oto/bin/lib/oto-tools.cjs (full file; this is large — focus on workstream-related code paths around lines 188, 269, 287-291, 325, 911-921, 1043-1066),
    oto/workflows/new-workspace.md,
    oto/workflows/list-workspaces.md,
    oto/workflows/remove-workspace.md,
    oto/commands/oto/workstreams.md,
    oto/commands/oto/new-workspace.md,
    oto/commands/oto/list-workspaces.md,
    oto/commands/oto/remove-workspace.md,
    oto/references/workstream-flag.md,
    tests/phase-04-planning-leak.test.cjs (regex set reference)
  </read_first>
  <action>
    Run this exact grep against the 10 in-scope files (NOTE: `oto-tools.cjs` is added because CONTEXT.md `<specifics>` "Whether to hand-fix oto-tools.cjs itself if grep finds residual `gsd` literals in its workstream-related code paths — yes, in scope"; the 8-file count from the phase brief is the user-facing surface, oto-tools.cjs is the lib backing it):

    ```bash
    grep -nE '\bgsd-|\bGSD_|Get Shit Done|\bsuperpowers\b|\bSuperpowers\b|\.planning/' \
      oto/bin/lib/workstream.cjs \
      oto/bin/lib/oto-tools.cjs \
      oto/workflows/new-workspace.md \
      oto/workflows/list-workspaces.md \
      oto/workflows/remove-workspace.md \
      oto/commands/oto/workstreams.md \
      oto/commands/oto/new-workspace.md \
      oto/commands/oto/list-workspaces.md \
      oto/commands/oto/remove-workspace.md \
      oto/references/workstream-flag.md
    ```

    For each hit:
    - `\bgsd-` literal → replace with `oto-` (e.g., `gsd-tools` → `oto-tools`, `gsd-progress` → `oto-progress`)
    - `\bGSD_` literal → replace with `OTO_` (e.g., `GSD_WORKSTREAM` → `OTO_WORKSTREAM`)
    - `Get Shit Done` literal → replace with `oto`
    - `\bsuperpowers\b` (lowercase, word-bounded) → replace with `oto`
    - `\bSuperpowers\b` (capitalized, word-bounded) → replace with `oto`
    - `\.planning/` literal → replace with `.oto/` (this is the canonical state directory per ARCH-01)

    Allowlist exceptions (do NOT modify):
    - Any line inside a fenced code block whose preceding heading or comment indicates an upstream-attribution / changelog context — there should be none in these files; if encountered, leave and note in summary
    - URL fragments pointing to `github.com/.../get-shit-done` or `.../superpowers` repos are NOT in scope here (none expected; if found, leave them — Phase 4 allowlist already covers attribution)

    After hand-fixes, re-run the same grep — must return zero output.

    If `oto-tools.cjs` is huge and grep is noisy, narrow to workstream-related sections by adding `| grep -E 'workstream|workspace'` after the initial grep — but ONLY for triage; the final clean-state assertion runs the full grep without the second filter.

    Do NOT use `sed -i` for replacements — read the file with the Read tool, then use Edit tool with old_string/new_string for each hit. This preserves working code that happens to contain the literal in non-leak contexts.
  </action>
  <verify>
    <automated>grep -nE '\bgsd-|\bGSD_|Get Shit Done|\bsuperpowers\b|\bSuperpowers\b|\.planning/' oto/bin/lib/workstream.cjs oto/bin/lib/oto-tools.cjs oto/workflows/new-workspace.md oto/workflows/list-workspaces.md oto/workflows/remove-workspace.md oto/commands/oto/workstreams.md oto/commands/oto/new-workspace.md oto/commands/oto/list-workspaces.md oto/commands/oto/remove-workspace.md oto/references/workstream-flag.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The grep command above produces no output (exit code 1 = no matches found)
    - `node oto/bin/lib/oto-tools.cjs --help` still runs without throwing (basic regression: hand-fixes did not break syntax)
    - `node -e "require('./oto/bin/lib/workstream.cjs')"` returns without throwing (CJS module still parseable)
    - The `oto-tools.cjs workstream` dispatch (around line 1043) and `oto-tools.cjs init {new,list,remove}-workspace` dispatch (around lines 911-921) still exist (`grep -n "workstream" oto/bin/lib/oto-tools.cjs | wc -l` returns >= 5; `grep -n "init.*workspace" oto/bin/lib/oto-tools.cjs | wc -l` returns >= 3)
  </acceptance_criteria>
  <done>
    Zero leak literals across the 10 files. CJS modules still load. `oto-tools.cjs` workstream + workspace dispatch wiring still intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify `oto-sdk query` handlers resolve and command frontmatter passes Phase 4 schema</name>
  <files>
    oto/workflows/new-workspace.md,
    oto/workflows/list-workspaces.md,
    oto/workflows/remove-workspace.md,
    oto/commands/oto/workstreams.md,
    oto/commands/oto/new-workspace.md,
    oto/commands/oto/list-workspaces.md,
    oto/commands/oto/remove-workspace.md
  </files>
  <read_first>
    oto/workflows/new-workspace.md,
    oto/workflows/list-workspaces.md,
    oto/workflows/remove-workspace.md,
    oto/commands/oto/workstreams.md,
    oto/commands/oto/new-workspace.md,
    oto/commands/oto/list-workspaces.md,
    oto/commands/oto/remove-workspace.md,
    oto/bin/lib/oto-tools.cjs (lines 911-921 init workspace dispatch + lines 1043-1066 workstream dispatch),
    bin/oto-sdk.js (the thin shim that translates `query workstream.list` → `workstream list`),
    tests/phase-04-frontmatter-schema.test.cjs (the schema rule)
  </read_first>
  <action>
    Step A — extract every `oto-sdk query <handler>` invocation from the 7 workflow/command files:

    ```bash
    grep -nE 'oto-sdk query [a-zA-Z._-]+' \
      oto/workflows/new-workspace.md \
      oto/workflows/list-workspaces.md \
      oto/workflows/remove-workspace.md \
      oto/commands/oto/workstreams.md \
      oto/commands/oto/new-workspace.md \
      oto/commands/oto/list-workspaces.md \
      oto/commands/oto/remove-workspace.md
    ```

    For each unique handler name (e.g., `workstream.list`, `workstream.create`, `workstream.set`, `workstream.get`, `workstream.status`, `workstream.complete`, `workstream.progress`, `init.new-workspace`, `init.list-workspaces`, `init.remove-workspace`), confirm a corresponding dispatch exists in `oto/bin/lib/oto-tools.cjs`:
    - For `workstream.<sub>` handlers — confirm a case for `<sub>` in the workstream dispatcher around line 1043-1066. Expected subcommands per CONTEXT.md `<code_context>`: `create`, `list`, `status`, `complete`, `set`, `get`, `progress`.
    - For `init.<key>` handlers — confirm a case for `<key>` in the init dispatcher around line 911-921. Expected: `new-workspace`, `list-workspaces`, `remove-workspace`.

    If a handler is invoked but has no dispatch case, that is a hand-fixup target — DO NOT add new handlers (out of scope per D-01); instead, fix the workflow/command body to reference the correct existing handler. Cross-reference with `foundation-frameworks/get-shit-done-main/get-shit-done/workflows/<same-file>.md` to see the original (pre-rebrand) handler name and confirm the rebrand engine mapped it correctly.

    Step B — run the existing Phase 4 frontmatter-schema test against the whole tree (it scans `oto/commands/oto/`); confirm it still passes:

    ```bash
    node --test tests/phase-04-frontmatter-schema.test.cjs
    ```

    If it fails on any of the 4 command files (`workstreams.md`, `new-workspace.md`, `list-workspaces.md`, `remove-workspace.md`), inspect the failure and hand-fix the frontmatter (likely candidates: missing `description:` field, malformed `argument-hint:`, missing `model:` or `tools:` if the schema requires them — read the test for the exact rules).

    Step C — run the existing Phase 4 planning-leak test (sanity, it should already pass after Task 1):

    ```bash
    node --test tests/phase-04-planning-leak.test.cjs
    ```

    Both must exit 0. If either fails, hand-fix in place; do not modify the test files.
  </action>
  <verify>
    <automated>node --test tests/phase-04-frontmatter-schema.test.cjs tests/phase-04-planning-leak.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/phase-04-frontmatter-schema.test.cjs` exits 0
    - `node --test tests/phase-04-planning-leak.test.cjs` exits 0
    - Every handler name extracted by the grep in Step A appears as a dispatch case in `oto/bin/lib/oto-tools.cjs` (manual cross-check; documented in plan summary)
    - `grep -c "oto-sdk query" oto/workflows/new-workspace.md oto/workflows/list-workspaces.md oto/workflows/remove-workspace.md` reports each file has at least 1 invocation (i.e., the rebrand did not strip the invocations)
  </acceptance_criteria>
  <done>
    Both Phase 4 tests pass. Every `oto-sdk query` handler invoked from the 7 user-facing files resolves to a real dispatch case in `oto-tools.cjs`. No handler additions, only verification + workflow-body hand-fixes if needed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → workstream/workspace name | User supplies free-text names that become directory path segments under `.oto/workstreams/<name>/` |
| user input → `--config-dir` and similar path overrides | Already trust-boundary'd at install time; out of Phase 7 scope |
| concurrent Claude instances → session-pointer file | Two simultaneous sessions writing to `.oto/active-workstream` could overwrite each other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `oto/bin/lib/workstream.cjs` name validation (path traversal via `../` in workstream/workspace name) | mitigate | Existing `workstream.cjs` validation rejects names containing `/`, `..`, NUL, leading `.` (verify in Plan 03 D-06 CRUD test by attempting `workstream create ../evil` and asserting rejection) |
| T-07-02 | Information disclosure | `oto/bin/lib/workstream.cjs` migrateToWorkstreams (accidental destructive deletion outside `.oto/`) | mitigate | Existing migration only operates on paths under `path.join(planningRoot, ...)`. No new code in Phase 7; verification by re-reading `migrateToWorkstreams` (lines 24-65) in Task 1 read_first |
| T-07-03 | Tampering | session-pointer file race between concurrent Claude instances | accept | Existing `workstream-flag.md` documents session-keyed pointers (resolution level 3) as the mitigation; D-07 test in Plan 03 verifies two distinct session keys hold distinct active workstreams. Pure file-write atomicity is a known accept (single user, no adversary) |
</threat_model>

<verification>
After both tasks complete:
1. The grep regex set returns zero across the 10 files
2. `node --test tests/phase-04-frontmatter-schema.test.cjs tests/phase-04-planning-leak.test.cjs` exits 0
3. `node oto/bin/lib/oto-tools.cjs workstream list` runs without throwing (may return empty list or error about missing `.oto/` — that's behavioral, not a regression; checks the lib still loads its workstream dispatch)
</verification>

<success_criteria>
- 0 leak-literal hits across 10 files
- Phase 4 schema + leak tests still green
- Every `oto-sdk query` handler in workflow/command bodies resolves to a real dispatch case
- No new files created; no `scripts/rebrand.cjs apply` invocation
</success_criteria>

<output>
After completion, create `.planning/phases/07-workstreams-workspaces-port/07-01-SUMMARY.md` recording: files inspected, hits found per file (or "0 hits"), hand-fixes applied per file (with before/after snippets), Phase 4 test exit codes, list of handlers verified.
</output>
