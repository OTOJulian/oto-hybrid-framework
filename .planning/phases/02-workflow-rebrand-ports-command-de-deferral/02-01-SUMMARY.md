---
phase: 02-workflow-rebrand-ports-command-de-deferral
plan: 01
subsystem: workflows
tags: [rebrand, workflows, ingest-docs, eval-review]
requires:
  - phase: 01-agent-ports-installer-wiring
    provides: restored oto-doc-classifier, oto-doc-synthesizer, and oto-eval-auditor agents
provides:
  - executable oto-ingest-docs workflow body
  - executable oto-eval-review workflow body
  - SDK-tolerant workflow calls
  - read-only-agent orchestrator persistence wording
affects: [WF-ING-01, WF-ING-02, WF-ING-03, WF-ING-04, WF-EVAL-01, WF-EVAL-02]
tech-stack:
  added: []
  patterns: [rebrand-engine-output-plus-bounded-fixups]
key-files:
  created:
    - .planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-01-SUMMARY.md
  modified:
    - oto/workflows/ingest-docs.md
    - oto/workflows/eval-review.md
    - reports/rebrand-dryrun.json
    - reports/rebrand-dryrun.md
    - scripts/rebrand/lib/engine.cjs
    - scripts/rebrand/lib/report.cjs
    - tests/phase-02-dryrun-report.test.cjs
key-decisions:
  - "Kept the rebrand engine as the source of truth and limited hand edits to the three planned fixup classes."
  - "Repaired the dry-run report generator to emit inventory-backed target_path values instead of hand-editing generated report artifacts."
patterns-established:
  - "Read-only classifier and auditor workflows capture Task return content and have the orchestrator write outputs."
  - "Dry-run reports now expose both upstream path and projected target_path for inventory-backed files."
requirements-completed: [WF-ING-01, WF-ING-02, WF-ING-03, WF-ING-04, WF-EVAL-01, WF-EVAL-02]
duration: 38 min
completed: 2026-05-18
---

# Phase 2 Plan 01: Workflow Rebrand Ports Summary

**Rebrand-generated ingest-docs and eval-review workflow bodies replaced the deferral stubs, with bounded fixups for .oto prose paths, SDK fallbacks, and read-only-agent persistence.**

## Performance

- **Duration:** 38 min
- **Started:** 2026-05-18T19:10:00Z
- **Completed:** 2026-05-18T19:48:17Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Replaced `oto/workflows/ingest-docs.md` from a 27-line deferral stub to a 332-line executable workflow body.
- Replaced `oto/workflows/eval-review.md` from a 25-line deferral stub to a 155-line executable workflow body.
- Applied the three required hand-fixups: `.planning` prose paths to `.oto`, SDK fallbacks, and orchestrator persistence for read-only agents.
- Regenerated `reports/rebrand-dryrun.json` and `reports/rebrand-dryrun.md` via the plan command.
- Repaired `scripts/rebrand/lib/engine.cjs` and `scripts/rebrand/lib/report.cjs` so generated dry-run reports include projected `target_path` values, satisfying the plan's report-path acceptance gate without hand-editing generated report artifacts.

## Task Commits

This plan has one workflow-body commit plus a follow-up repair commit for the generated-report acceptance gap.

## Files Created/Modified

- `oto/workflows/ingest-docs.md` - Rebrand-ported executable `/oto-ingest-docs` workflow with `.oto` paths, SDK fallbacks, and orchestrator-persisted classifier JSON.
- `oto/workflows/eval-review.md` - Rebrand-ported executable `/oto-eval-review` workflow with SDK fallback and orchestrator-persisted eval review markdown.
- `reports/rebrand-dryrun.json` - Regenerated by `node scripts/rebrand.cjs --dry-run --target foundation-frameworks/get-shit-done-main --owner OTOJulian`; now includes `target_path` for inventory-backed rows.
- `reports/rebrand-dryrun.md` - Regenerated companion report with a `Target` column.
- `scripts/rebrand/lib/engine.cjs` - Adds `target_path` to dry-run file entries.
- `scripts/rebrand/lib/report.cjs` - Renders the dry-run `Target` column.
- `tests/phase-02-dryrun-report.test.cjs` - Locks projected target paths for the two restored workflow rows.
- `.planning/phases/02-workflow-rebrand-ports-command-de-deferral/02-01-SUMMARY.md` - This execution summary.

## Pre-Flight And Engine

- Inventory pre-flight command passed and printed `inventory pre-flight OK`.
- Apply command run:
  `node scripts/rebrand.cjs --apply --target foundation-frameworks/get-shit-done-main --owner OTOJulian --out .oto-rebrand-stage`
- Apply output:
  `engine: apply - 341 files, 5098 matches, 0 unclassified, 465ms`
- Staged workflow files existed at:
  `.oto-rebrand-stage/oto/workflows/ingest-docs.md`
  `.oto-rebrand-stage/oto/workflows/eval-review.md`
- Dry-run command run:
  `node scripts/rebrand.cjs --dry-run --target foundation-frameworks/get-shit-done-main --owner OTOJulian`
- Dry-run output:
  `engine: dry-run - 341 files, 6047 matches, 0 unclassified, 415ms`
- `.oto-rebrand-stage/` was removed and `test ! -e .oto-rebrand-stage` passed.

## Line Counts

| File | Before | After |
| --- | ---: | ---: |
| `oto/workflows/ingest-docs.md` | 27 | 332 |
| `oto/workflows/eval-review.md` | 25 | 155 |

## Hand-Fixup Pass 1

The following `.planning` prose/path contexts in `oto/workflows/ingest-docs.md` were rewritten to `.oto`:

| Original line | Context |
| ---: | --- |
| 3 | Bootstrap or merge into `.planning/`. |
| 6 | Mode default mentions `.planning/` absence/presence. |
| 64 | Overwrite warning for existing `.planning/`. |
| 166 | Classifier `OUTPUT_DIR` prompt field. |
| 171 | Abort without touching `.planning/` further. |
| 201 | Synthesizer output intel files. |
| 202 | Synthesizer `SYNTHESIS.md` path. |
| 203 | Synthesizer `INGEST-CONFLICTS.md` path. |
| 209 | Conflict gate reads `INGEST-CONFLICTS.md`. |
| 230 | Abort message preserves staged intel path. |
| 242 | New-mode synthesis source path. |
| 274 | Merge-mode existing state paths. |
| 279 | Merge-mode requirements intel path. |
| 280 | Merge-mode decisions intel path. |
| 281 | Merge-mode roadmap append path. |
| 315 | Completion output conflict report path. |

Post-fixup locks:
- `grep -c '\.planning\b' oto/workflows/ingest-docs.md` -> `0`
- `grep -c '\.planning\b' oto/workflows/eval-review.md` -> `0`

## Hand-Fixup Pass 2

Edited SDK calls:

| File:line after edit | Fallback shape |
| --- | --- |
| `oto/workflows/ingest-docs.md:55` | `INIT=$(oto-sdk query init.ingest-docs 2>/dev/null || echo "")` |
| `oto/workflows/ingest-docs.md:292` | `oto-sdk query commit ... --files 2>/dev/null \` plus final `|| true` |
| `oto/workflows/eval-review.md:16` | `INIT=$(oto-sdk query init.phase-op "${PHASE_ARG}" 2>/dev/null || echo "")` |

Already tolerant call left unchanged:
- `oto/workflows/eval-review.md:23` uses `2>/dev/null` and `|| true`.

Lock:
- `grep -n 'oto-sdk query' oto/workflows/ingest-docs.md oto/workflows/eval-review.md | grep -v '2>/dev/null\|||' | wc -l | tr -d ' '` -> `0`

## Hand-Fixup Pass 3

### Ingest Classifier

Pre-string:

```text
Collect the one-line confirmations from each classifier. If any classifier errors out, surface the error and abort without touching `.oto/` further.
```

Post-string:

```text
Spawn `Task(subagent_type="oto-doc-classifier")` and capture the agent's JSON response. The classifier is a read-only agent (Phase 1 D-04). After the Task returns, the workflow orchestrator writes the returned JSON to `.oto/intel/classifications/<doc-slug>.json` using its own Write tool. If any classifier errors out, surface the error and abort without touching `.oto/` further.
```

Confirmation:
- `grep -F -c 'Collect the one-line confirmations from each classifier. If any classifier errors out, surface the error and abort without touching `.oto/` further.' oto/workflows/ingest-docs.md` -> `0`
- `awk '/<step name="classify_parallel">/,/<\/step>/' oto/workflows/ingest-docs.md | grep -q 'orchestrator'` -> pass

### Eval Auditor

Pre-string:

```text
Spawn as Task with model `AUDITOR_MODEL`.
```

Post-string:

```text
Spawn `Task(subagent_type="oto-eval-auditor")` and capture the agent's response body. The auditor is a read-only agent (Phase 1 D-04). After the Task returns, the workflow orchestrator writes the returned markdown to `.oto/phases/<phase>/EVAL-REVIEW.md` using its own Write tool.
```

Confirmation:
- `grep -F -c 'Spawn as Task with model `AUDITOR_MODEL`.' oto/workflows/eval-review.md` -> `0`
- `grep -B5 -A20 'oto-eval-auditor' oto/workflows/eval-review.md | grep -q 'orchestrator'` -> pass

Related wording also updated:
- `Read the written EVAL-REVIEW.md.` -> `Read the orchestrator-written EVAL-REVIEW.md.`
- `EVAL-REVIEW.md written (by auditor)` -> `EVAL-REVIEW.md returned by auditor and written by the orchestrator`

## Security Checks

All required preserved-security checks passed:

- `grep -q '\*\.\.\*' oto/workflows/ingest-docs.md` -> pass
- `grep -q 'realpath' oto/workflows/ingest-docs.md` -> pass
- `grep -q 'Exit WITHOUT' oto/workflows/ingest-docs.md` -> pass

## Coverage Manifest

The generated dry-run report now records scanned upstream source paths and projected inventory target paths. The workflow rows are present as:

- `get-shit-done/workflows/ingest-docs.md` -> `oto/workflows/ingest-docs.md`
- `get-shit-done/workflows/eval-review.md` -> `oto/workflows/eval-review.md`

The report was regenerated from `scripts/rebrand.cjs --dry-run`; target strings were not hand-edited into the artifacts.

## Verification

Passed:

- `test -f oto/workflows/ingest-docs.md && test -f oto/workflows/eval-review.md`
- `wc -l oto/workflows/ingest-docs.md oto/workflows/eval-review.md` -> `332`, `155`
- `! grep -qi 'intentionally non-executable' oto/workflows/ingest-docs.md`
- `! grep -qi 'intentionally non-executable' oto/workflows/eval-review.md`
- `grep -q '<step name="discover_docs">' oto/workflows/ingest-docs.md`
- `grep -q '<step name="conflict_gate">' oto/workflows/ingest-docs.md`
- `grep -q 'oto-eval-auditor' oto/workflows/eval-review.md`
- `grep -q 'oto/workflows/ingest-docs.md' reports/rebrand-dryrun.json && grep -q 'oto/workflows/eval-review.md' reports/rebrand-dryrun.json`
- `node -e "const r=require('./reports/rebrand-dryrun.json');const o=r.orphans||{};const h=JSON.stringify(o).match(/workflows\/(ingest-docs|eval-review)/g)||[];process.exit(h.length===0?0:1);"`
- Task 2 combined acceptance command from the plan.
- `node --test tests/phase-02-dryrun-report.test.cjs` -> 4 pass, 0 fail.
- `node --test tests/phase-04-rebrand-smoke.test.cjs` -> 5 pass, 0 fail. This test rewrote `reports/rebrand-dryrun.*` to an empty-temp fixture state, so the planned dry-run command was rerun afterward to restore the Phase 2 report artifacts.

## Deviations from Plan

### Rule 2 Auto-Fix: Dry-run report target paths

- **Found during:** Task 1 acceptance verification
- **Issue:** Plan expected generated `reports/rebrand-dryrun.json` to contain literal target paths under `oto/workflows/...`; the pre-repair dry-run report schema contained only upstream source paths and match records.
- **Fix:** Added `target_path` to dry-run entries from `decisions/file-inventory.json`, rendered it in the markdown report, added a regression test, and regenerated reports.
- **Files modified:** `scripts/rebrand/lib/engine.cjs`, `scripts/rebrand/lib/report.cjs`, `tests/phase-02-dryrun-report.test.cjs`, `reports/rebrand-dryrun.json`, `reports/rebrand-dryrun.md`.
- **Verification:** Target-path grep now passes; `node --test tests/phase-02-dryrun-report.test.cjs` passes 4/4; orphan workflow check passes.

**Total deviations:** 1 auto-fixed acceptance gap.
**Impact on plan:** The generated report contract is now stronger and matches the plan's acceptance criteria.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02-03 can consume the executable workflow bodies for workflow-shape tests and fixture smoke. Plan 02-02 is independently owned by another worker and was not modified here.

---
*Phase: 02-workflow-rebrand-ports-command-de-deferral*
*Completed: 2026-05-18*
