---
phase: 09-upstream-sync-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-09-pull-puller.test.cjs
  - tests/phase-09-rebrand-sync.test.cjs
  - tests/phase-09-merge-3way.test.cjs
  - tests/phase-09-merge-add-delete.test.cjs
  - tests/phase-09-allowlist.test.cjs
  - tests/phase-09-accept-helper.test.cjs
  - tests/phase-09-report.test.cjs
  - tests/phase-09-cli.integration.test.cjs
  - tests/fixtures/phase-09/build-bare-upstream.cjs
  - tests/fixtures/phase-09/sample-inventory.json
  - tests/fixtures/phase-09/sample-allowlist.json
  - tests/fixtures/phase-09/three-version-trio/base.txt
  - tests/fixtures/phase-09/three-version-trio/current.txt
  - tests/fixtures/phase-09/three-version-trio/other.txt
  - decisions/sync-allowlist.json
  - schema/sync-allowlist.json
  - schema/last-synced-commit.json
  - .oto-sync/BREAKING-CHANGES-gsd.md
  - .oto-sync/BREAKING-CHANGES-superpowers.md
  - .gitignore
autonomous: true
requirements: [SYN-01, SYN-02, SYN-03, SYN-04, SYN-05, SYN-06, SYN-07]

must_haves:
  truths:
    - "Running `node --test tests/phase-09-*.test.cjs` lists 8 test files all using node:test, all marked t.todo() or skipping cleanly (no failures)"
    - "`decisions/sync-allowlist.json` exists and contains every glob enumerated in CONTEXT D-16"
    - "`schema/sync-allowlist.json` exists and validates the allowlist via the existing hand-rolled validator"
    - "`schema/last-synced-commit.json` exists and constrains records to {upstream in [gsd,superpowers], ref_kind, ref, sha matching ^[0-9a-f]{40}$, timestamp}"
    - "`.oto-sync/BREAKING-CHANGES-gsd.md` and `BREAKING-CHANGES-superpowers.md` exist with header content"
    - "`.gitignore` contains `.oto-sync/upstream/`, `.oto-sync/rebranded/`, `.oto-sync-conflicts/`"
    - "Test fixture builder script generates a `git init --bare` upstream with 3 tagged commits and uses `file://` URLs"
  artifacts:
    - path: "tests/phase-09-pull-puller.test.cjs"
      provides: "Test scaffolds for SYN-01, SYN-02, SYN-05, Pitfalls 9/10/12"
    - path: "tests/phase-09-rebrand-sync.test.cjs"
      provides: "Test scaffolds for SYN-03 (engine reuse byte-identity)"
    - path: "tests/phase-09-merge-3way.test.cjs"
      provides: "Test scaffolds for SYN-04 happy path + D-12 + Pitfalls 1, 4, 6"
    - path: "tests/phase-09-merge-add-delete.test.cjs"
      provides: "Test scaffolds for SYN-04 add/delete/unknown D-10/D-17"
    - path: "tests/phase-09-allowlist.test.cjs"
      provides: "Test scaffolds for D-16/D-17 + Pitfall 7 allowlist completeness"
    - path: "tests/phase-09-accept-helper.test.cjs"
      provides: "Test scaffolds for D-15 --accept, --accept-deletion, --keep-deleted"
    - path: "tests/phase-09-report.test.cjs"
      provides: "Test scaffolds for SYN-06 + D-14 REPORT.md + BREAKING-CHANGES.md"
    - path: "tests/phase-09-cli.integration.test.cjs"
      provides: "Test scaffolds for D-19 end-to-end + Pitfall 9 (git --version)"
    - path: "tests/fixtures/phase-09/build-bare-upstream.cjs"
      provides: "Reusable bare-repo fixture builder per Pattern 5; uses file:// URLs"
    - path: "tests/fixtures/phase-09/sample-inventory.json"
      provides: "Minimal inventory subset for unit tests"
    - path: "tests/fixtures/phase-09/sample-allowlist.json"
      provides: "Minimal oto_owned_globs for unit tests"
    - path: "tests/fixtures/phase-09/three-version-trio/"
      provides: "base.txt + current.txt + other.txt for mergeOneFile unit tests covering clean + conflict cases"
    - path: "decisions/sync-allowlist.json"
      provides: "v1 allowlist of oto-owned globs (D-16 enumerated)"
    - path: "schema/sync-allowlist.json"
      provides: "Hand-rolled-validator-compatible schema for sync-allowlist"
    - path: "schema/last-synced-commit.json"
      provides: "Hand-rolled-validator-compatible schema for last-synced-commit pin record"
    - path: ".oto-sync/BREAKING-CHANGES-gsd.md"
      provides: "Initial empty audit log for GSD upstream (committed)"
    - path: ".oto-sync/BREAKING-CHANGES-superpowers.md"
      provides: "Initial empty audit log for Superpowers upstream (committed)"
    - path: ".gitignore"
      provides: "Updated to exclude .oto-sync/upstream/, .oto-sync/rebranded/, .oto-sync-conflicts/"
  key_links:
    - from: "decisions/sync-allowlist.json"
      to: "schema/sync-allowlist.json"
      via: "version + oto_owned_globs + oto_diverged_paths field shape"
      pattern: '"version"\\s*:\\s*"1"'
    - from: "tests/phase-09-*.test.cjs"
      to: "tests/fixtures/phase-09/build-bare-upstream.cjs"
      via: "require('./fixtures/phase-09/build-bare-upstream.cjs')"
      pattern: "build-bare-upstream"
---

<objective>
Wave 0 foundation for Phase 9: create all 8 `phase-09-*.test.cjs` scaffolds with `t.todo()` placeholders for every behavior in 09-VALIDATION.md, build the reusable fixture infrastructure (bare-repo builder + sample inventory + sample allowlist + three-version trio), ship both schemas (`sync-allowlist.json`, `last-synced-commit.json`), commit the initial empty audit logs (`BREAKING-CHANGES-{gsd,superpowers}.md`), seed `decisions/sync-allowlist.json` with the D-16 enumerated globs, and update `.gitignore` to keep working files out of git.

Purpose: Wave 0 must exist before Wave 1 libraries are written so each library task can fill an existing test file (Phase 5/6/8 convention) rather than invent new test names mid-implementation. This plan provides every consumer (test, schema, fixture, ignore rule) the downstream waves require.

Output: 8 test scaffolds, 5 fixture artifacts, 2 schemas, 1 allowlist, 2 BREAKING-CHANGES seed files, 1 `.gitignore` patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-upstream-sync-pipeline/09-CONTEXT.md
@.planning/phases/09-upstream-sync-pipeline/09-RESEARCH.md
@.planning/phases/09-upstream-sync-pipeline/09-VALIDATION.md
@CLAUDE.md
@scripts/rebrand/lib/validate-schema.cjs
@tests/phase-08-codex-toml.test.cjs
@.gitignore

<interfaces>
<!-- Existing patterns this plan must follow. -->

From scripts/rebrand/lib/validate-schema.cjs (hand-rolled JSON Schema 2020-12 subset, zero deps):
```javascript
function validate(data, schema) → { valid: boolean, errors: string[] }
// Supports: const, enum, type, pattern, minLength, minItems, minimum, maximum,
// required, properties, additionalProperties (false), items, oneOf, allOf+if/then,
// $ref via '#/$defs/<key>'.
module.exports = { validate };
```

From tests/phase-08-codex-toml.test.cjs (Phase 5/6/8 test convention):
```javascript
'use strict';
const { test } = require('node:test');
const assert = require('node:assert/strict');
// hand-rolled assertions, t.after() for cleanup, os.tmpdir() for fixtures
```

From schema/file-inventory.json (existing hand-rolled-compatible schema example) and
schema/rename-map.json: shape uses $schema, $id, type, required, additionalProperties,
properties — no JSON Pointer features beyond what validate-schema.cjs supports.

D-16 enumerated oto_owned_globs (CONTEXT.md, all 32 entries — copy verbatim into
decisions/sync-allowlist.json::oto_owned_globs):
```
bin/lib/codex-toml.cjs
bin/lib/codex-transform.cjs
bin/lib/codex-profile.cjs
bin/lib/gemini-transform.cjs
bin/lib/instruction-file.cjs
bin/lib/runtime-matrix.cjs
bin/lib/sync-pull.cjs
bin/lib/sync-merge.cjs
bin/lib/sync-accept.cjs
oto/templates/**
CLAUDE.md
AGENTS.md
GEMINI.md
decisions/**
schema/**
rename-map.json
scripts/rebrand.cjs
scripts/rebrand/**
scripts/render-instruction-files.cjs
scripts/render-runtime-matrix.cjs
scripts/sync-upstream/**
.oto/**
.oto-sync/**
.oto-rebrand-out/**
.oto-sync-conflicts/**
tests/fixtures/**
package.json
package-lock.json
LICENSE
THIRD-PARTY-LICENSES.md
README.md
.gitignore
.github/**
foundation-frameworks/**
```
(Add `bin/lib/sync-cli.cjs` to this list — referenced in CONTEXT but missing from the 32-entry verbatim block.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas + allowlist + initial committed state files + .gitignore patch</name>
  <files>
    schema/sync-allowlist.json,
    schema/last-synced-commit.json,
    decisions/sync-allowlist.json,
    .oto-sync/BREAKING-CHANGES-gsd.md,
    .oto-sync/BREAKING-CHANGES-superpowers.md,
    .gitignore
  </files>
  <read_first>
    - schema/file-inventory.json (existing hand-rolled-compatible schema example for shape conventions)
    - schema/rename-map.json (existing schema example)
    - scripts/rebrand/lib/validate-schema.cjs (the validator these schemas must validate against — confirm only supported keywords are used)
    - .planning/phases/09-upstream-sync-pipeline/09-CONTEXT.md (D-16 verbatim 32-entry oto_owned_globs list; copy exactly)
    - .planning/phases/09-upstream-sync-pipeline/09-RESEARCH.md (Code Examples section: schema/sync-allowlist.json and schema/last-synced-commit.json templates)
    - .gitignore (current contents — patch by appending, do not replace)
  </read_first>
  <action>
    Create the following files exactly as specified:

    1. `schema/sync-allowlist.json` — Hand-rolled-validator-compatible JSON Schema. Use exactly:
       ```
       {
         "$schema": "https://json-schema.org/draft/2020-12/schema",
         "$id": "https://github.com/OTOJulian/oto-hybrid-framework/schema/sync-allowlist.json",
         "type": "object",
         "required": ["version", "oto_owned_globs", "oto_diverged_paths"],
         "additionalProperties": false,
         "properties": {
           "version": { "type": "string", "const": "1" },
           "oto_owned_globs": { "type": "array", "minItems": 1,
             "items": { "type": "string", "minLength": 1 } },
           "oto_diverged_paths": { "type": "array",
             "items": { "type": "string", "minLength": 1 } }
         }
       }
       ```

    2. `schema/last-synced-commit.json` — Use exactly:
       ```
       {
         "$schema": "https://json-schema.org/draft/2020-12/schema",
         "$id": "https://github.com/OTOJulian/oto-hybrid-framework/schema/last-synced-commit.json",
         "type": "object",
         "required": ["upstream", "ref_kind", "ref", "sha", "timestamp"],
         "additionalProperties": false,
         "properties": {
           "upstream": { "type": "string", "enum": ["gsd", "superpowers"] },
           "ref_kind": { "type": "string", "enum": ["tag", "branch", "sha", "tag-or-branch"] },
           "ref": { "type": "string", "minLength": 1 },
           "sha": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
           "timestamp": { "type": "string", "minLength": 1 }
         }
       }
       ```

    3. `decisions/sync-allowlist.json` — Object with shape `{ "version": "1", "oto_owned_globs": [...], "oto_diverged_paths": [] }`. Set `oto_owned_globs` to the EXACT 32 strings enumerated in CONTEXT D-16 (copy verbatim from .planning/phases/09-upstream-sync-pipeline/09-CONTEXT.md), AND additionally include `"bin/lib/sync-cli.cjs"` (referenced in CONTEXT but missing from the D-16 verbatim block — total 33 entries). Order: keep verbatim list first; append `bin/lib/sync-cli.cjs` after the other three sync-* lib entries.

    4. `.oto-sync/BREAKING-CHANGES-gsd.md` — File body exactly:
       ```
       # Breaking Changes — GSD upstream

       This file is the permanent audit log of upstream syncs against gsd-build/get-shit-done. Each `oto sync` run appends a dated section. Do not delete entries.

       <!-- New entries below this line -->
       ```

    5. `.oto-sync/BREAKING-CHANGES-superpowers.md` — Same body as #4 but with "Superpowers upstream" header and `obra/superpowers` URL.

    6. `.gitignore` — APPEND (do not overwrite) the following block at the end of the existing file (preserve the current 7 lines):
       ```

       # Phase 9 — upstream sync working state (metadata files exempted by negation)
       .oto-sync/upstream/
       .oto-sync/rebranded/
       .oto-sync-conflicts/
       ```
       Note: `.oto-sync/BREAKING-CHANGES-*.md` and `.oto-sync/last-synced-commit.json` are NOT under `upstream/` or `rebranded/`, so they remain trackable without explicit negation rules.

    Run `node -e "const s=require('./scripts/rebrand/lib/validate-schema.cjs'); const sch=require('./schema/sync-allowlist.json'); const data=require('./decisions/sync-allowlist.json'); console.log(s.validate(data, sch));"` and confirm `{ valid: true, errors: [] }`.
  </action>
  <verify>
    <automated>node -e "const s=require('./scripts/rebrand/lib/validate-schema.cjs'); const sch=require('./schema/sync-allowlist.json'); const data=require('./decisions/sync-allowlist.json'); const r=s.validate(data,sch); if(!r.valid){console.error(r.errors);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `schema/sync-allowlist.json` exists, `cat schema/sync-allowlist.json | python3 -m json.tool` succeeds, contains `"$id": "https://github.com/OTOJulian/oto-hybrid-framework/schema/sync-allowlist.json"`
    - File `schema/last-synced-commit.json` exists, contains `"pattern": "^[0-9a-f]{40}$"` for the sha field
    - File `decisions/sync-allowlist.json` exists, valid JSON, contains keys `version`, `oto_owned_globs`, `oto_diverged_paths`; `oto_owned_globs` array length is 33 and contains EVERY one of these substrings (grep): `bin/lib/codex-toml.cjs`, `bin/lib/sync-pull.cjs`, `bin/lib/sync-merge.cjs`, `bin/lib/sync-accept.cjs`, `bin/lib/sync-cli.cjs`, `oto/templates/**`, `decisions/**`, `schema/**`, `scripts/rebrand/**`, `scripts/sync-upstream/**`, `.oto/**`, `.oto-sync/**`, `.oto-sync-conflicts/**`, `foundation-frameworks/**`, `tests/fixtures/**`, `LICENSE`, `THIRD-PARTY-LICENSES.md`, `package.json`, `.gitignore`, `.github/**`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md` (23 substrings)
    - `.oto-sync/BREAKING-CHANGES-gsd.md` exists, contains the literal string `Breaking Changes — GSD upstream`
    - `.oto-sync/BREAKING-CHANGES-superpowers.md` exists, contains the literal string `Breaking Changes — Superpowers upstream`
    - `.gitignore` contains the literal lines `.oto-sync/upstream/`, `.oto-sync/rebranded/`, `.oto-sync-conflicts/` (verify via grep)
    - Hand-rolled validator returns `{ valid: true }` for `decisions/sync-allowlist.json` against `schema/sync-allowlist.json` (the verify command above)
  </acceptance_criteria>
  <done>
    All six files exist with the exact content specified; validator accepts the allowlist; .gitignore additions confirmed via grep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test fixtures (bare-repo builder + samples + three-version trio)</name>
  <files>
    tests/fixtures/phase-09/build-bare-upstream.cjs,
    tests/fixtures/phase-09/sample-inventory.json,
    tests/fixtures/phase-09/sample-allowlist.json,
    tests/fixtures/phase-09/three-version-trio/base.txt,
    tests/fixtures/phase-09/three-version-trio/current.txt,
    tests/fixtures/phase-09/three-version-trio/other.txt,
    tests/fixtures/phase-09/three-version-trio/conflict-base.txt,
    tests/fixtures/phase-09/three-version-trio/conflict-current.txt,
    tests/fixtures/phase-09/three-version-trio/conflict-other.txt
  </files>
  <read_first>
    - .planning/phases/09-upstream-sync-pipeline/09-RESEARCH.md (Pattern 5: Bare-Repo Test Fixture; lines around `setupFixture`)
    - .planning/phases/09-upstream-sync-pipeline/09-VALIDATION.md (Wave 0 Requirements section: fixture file list)
    - decisions/file-inventory.json (header + first 3-5 entries — to mirror the shape in sample-inventory.json)
    - decisions/sync-allowlist.json (created in Task 1 — sample-allowlist.json is a smaller subset of the same shape)
  </read_first>
  <action>
    Create reusable fixture infrastructure used by all 8 phase-09 tests.

    1. `tests/fixtures/phase-09/build-bare-upstream.cjs` — A reusable function module (NOT a test). Exports `buildBareUpstream(options)` which:
       - Accepts `{ rootDir }` where `rootDir` is a temp directory.
       - Runs `git init --bare upstream.git` inside `rootDir`.
       - Clones via `git clone file://${rootDir}/upstream.git work` (USE `file://` URL — bare paths trigger "--depth is ignored in local clones" warning per Pitfall 5).
       - In the work clone, sets `GIT_AUTHOR_NAME=t`, `GIT_AUTHOR_EMAIL=t@t.test`, `GIT_COMMITTER_NAME=t`, `GIT_COMMITTER_EMAIL=t@t.test` via env on every git invocation.
       - Creates 3 sequential commits each with a tag:
         - Tag `v1.0.0`: file `README.md` containing `line1\nline2\nline3\n`; file `oto/workflows/sample.md` containing `# sample\n\nbody\n`
         - Tag `v1.1.0`: modify `README.md` to `line1\nline2 EDIT\nline3\n` (non-overlapping with future v1.2.0 changes); add `oto/workflows/added.md` with `# added\n` (this models an upstream-added file)
         - Tag `v1.2.0`: modify `README.md` to `line1\nline2 EDIT\nline3 EDIT\n`; remove `oto/workflows/sample.md` (models an upstream deletion); modify `oto/workflows/added.md` to `# added EDIT\n`
       - Pushes `main` and all three tags to the bare repo.
       - Returns `{ bareUrl: \`file://${rootDir}/upstream.git\`, workDir, tags: ['v1.0.0','v1.1.0','v1.2.0'] }`.
       - Uses `child_process.spawnSync('git', args, { cwd, env, encoding: 'utf8' })` — throws with stderr if status !== 0.
       - Module shape: `module.exports = { buildBareUpstream };`

    2. `tests/fixtures/phase-09/sample-inventory.json` — Minimal subset matching the shape of `decisions/file-inventory.json`. Include AT LEAST these 4 entries (paths drive merge add/delete tests):
       ```
       {
         "version": "1",
         "entries": [
           { "upstream": "gsd", "path": "README.md", "verdict": "keep", "target_path": "README.md", "reason": "test fixture" },
           { "upstream": "gsd", "path": "oto/workflows/sample.md", "verdict": "keep", "target_path": "oto/workflows/sample.md", "reason": "test fixture; will be deleted upstream at v1.2.0" },
           { "upstream": "gsd", "path": "oto/workflows/conflict.md", "verdict": "merge", "target_path": "oto/workflows/conflict.md", "reason": "test fixture; same-line conflict" },
           { "upstream": "gsd", "path": "oto/workflows/clean-merge.md", "verdict": "keep", "target_path": "oto/workflows/clean-merge.md", "reason": "test fixture; non-overlapping merge" }
         ]
       }
       ```
       Note: this must validate against the existing `schema/file-inventory.json` — verify by reading that schema first to confirm field names. If `version` is `"2"` or another value in the production schema, mirror that. If field names differ, copy them.

    3. `tests/fixtures/phase-09/sample-allowlist.json` — Same shape as `decisions/sync-allowlist.json` but minimal:
       ```
       {
         "version": "1",
         "oto_owned_globs": ["oto/templates/**", "decisions/**", ".oto/**"],
         "oto_diverged_paths": []
       }
       ```

    4. `tests/fixtures/phase-09/three-version-trio/base.txt`, `current.txt`, `other.txt` — Clean-merge case (non-overlapping edits):
       - `base.txt`: `alpha\nbeta\ngamma\n`
       - `current.txt`: `alpha-EDITED\nbeta\ngamma\n` (oto changed line 1)
       - `other.txt`: `alpha\nbeta\ngamma-EDITED\n` (upstream changed line 3)
       Running `git merge-file -p current.txt base.txt other.txt` returns exit 0 with content `alpha-EDITED\nbeta\ngamma-EDITED\n`.

    5. `tests/fixtures/phase-09/three-version-trio/conflict-base.txt`, `conflict-current.txt`, `conflict-other.txt` — Same-line clash case:
       - `conflict-base.txt`: `shared\nline\n`
       - `conflict-current.txt`: `shared-OTO\nline\n`
       - `conflict-other.txt`: `shared-UPSTREAM\nline\n`
       Running `git merge-file -p conflict-current.txt conflict-base.txt conflict-other.txt` returns exit 1 with `<<<<<<<` markers around line 1.

    The fixture builder must NOT be marked as a test file (no `.test.cjs` suffix; no `node:test` import); it is a helper required to be importable from any phase-09 test.
  </action>
  <verify>
    <automated>node -e "const f = require('./tests/fixtures/phase-09/build-bare-upstream.cjs'); const os=require('node:os'),fsp=require('node:fs/promises'),p=require('node:path'); fsp.mkdtemp(p.join(os.tmpdir(),'oto-fix-')).then(async (d) => { const r = await f.buildBareUpstream({rootDir: d}); if (!r.bareUrl.startsWith('file://')) throw new Error('bareUrl must use file:// URL'); if (r.tags.length !== 3) throw new Error('expected 3 tags'); console.log('OK', r.tags); await fsp.rm(d, {recursive:true,force:true}); }).catch(e => { console.error(e); process.exit(1); });"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/phase-09/build-bare-upstream.cjs` exists, contains literal string `'file://'` (verify with grep), contains literal string `'init', '--bare'` (verify with grep), exports `buildBareUpstream` (verify by `require()` returning function)
    - Verify command above prints `OK [ 'v1.0.0', 'v1.1.0', 'v1.2.0' ]` and exits 0
    - `tests/fixtures/phase-09/sample-inventory.json` is valid JSON, has at least one entry with `verdict: "keep"` AND one with `verdict: "merge"`, paths `README.md` and `oto/workflows/sample.md` both present
    - `tests/fixtures/phase-09/sample-allowlist.json` is valid JSON, has `version: "1"`, `oto_owned_globs` is non-empty array, `oto_diverged_paths` is empty array
    - `tests/fixtures/phase-09/three-version-trio/base.txt` exists with content `alpha\nbeta\ngamma\n`; `current.txt` and `other.txt` differ on lines 1 and 3 respectively (verify with diff)
    - `tests/fixtures/phase-09/three-version-trio/conflict-{base,current,other}.txt` all exist; all three differ on the first line
    - Run `git merge-file -p tests/fixtures/phase-09/three-version-trio/current.txt tests/fixtures/phase-09/three-version-trio/base.txt tests/fixtures/phase-09/three-version-trio/other.txt` exits 0 (clean merge case)
    - Run `git merge-file -p tests/fixtures/phase-09/three-version-trio/conflict-current.txt tests/fixtures/phase-09/three-version-trio/conflict-base.txt tests/fixtures/phase-09/three-version-trio/conflict-other.txt` exits 1 (one conflict hunk)
    - Fixture builder uses `GIT_AUTHOR_NAME` and `GIT_COMMITTER_NAME` env vars (verify with grep `GIT_AUTHOR_NAME` in build-bare-upstream.cjs)
  </acceptance_criteria>
  <done>
    Bare-repo builder runs end-to-end and returns 3 tags via `file://` URL; all sample/trio fixtures exist with content that produces predictable git merge-file outcomes.
  </done>
</task>

<task type="auto">
  <name>Task 3: 8 test scaffolds with t.todo() placeholders mapping every VALIDATION row</name>
  <files>
    tests/phase-09-pull-puller.test.cjs,
    tests/phase-09-rebrand-sync.test.cjs,
    tests/phase-09-merge-3way.test.cjs,
    tests/phase-09-merge-add-delete.test.cjs,
    tests/phase-09-allowlist.test.cjs,
    tests/phase-09-accept-helper.test.cjs,
    tests/phase-09-report.test.cjs,
    tests/phase-09-cli.integration.test.cjs
  </files>
  <read_first>
    - tests/phase-08-codex-toml.test.cjs (test convention: `'use strict'`, `const { test } = require('node:test')`, `const assert = require('node:assert/strict')`, hand-rolled assertions, t.after() cleanup)
    - tests/phase-08-instruction-file-render.test.cjs (alternative convention with t.todo() placeholders)
    - .planning/phases/09-upstream-sync-pipeline/09-VALIDATION.md (Per-Task Verification Map — every row maps to one t.todo() in one of these 8 files)
    - .planning/phases/09-upstream-sync-pipeline/09-CONTEXT.md (D-21: 8 named test files; their behaviors)
    - tests/fixtures/phase-09/build-bare-upstream.cjs (created in Task 2 — referenced by pull-puller, cli.integration, allowlist, report tests)
    - tests/fixtures/phase-09/three-version-trio/ (created in Task 2 — referenced by merge-3way test)
  </read_first>
  <action>
    Create 8 test scaffolds. Each file follows this template structure:

    ```javascript
    'use strict';
    const { test } = require('node:test');
    const assert = require('node:assert/strict');
    // Other imports as needed (path, fs/promises, os)

    // Each test uses t.todo() to declare intent without failing.
    // Wave 1+ plans replace t.todo bodies with real assertions.

    test('<behavior description>', { todo: '<plan-NN will fill>' }, () => {});
    ```

    Allocate VALIDATION rows to test files as follows. For each row, create one `test('<row behavior>', { todo: 'plan-NN' }, () => {})` entry.

    File 1 — `tests/phase-09-pull-puller.test.cjs` — placeholder: `plan-02`. Tests:
      - `SYN-01: pullUpstream clones at tag into .oto-sync/upstream/gsd/current/`
      - `SYN-01: snapshot rotation — current/ becomes prior/ on second pull`
      - `SYN-01: snapshot rotation — first sync skips rotation when prior/ does not exist`
      - `SYN-01: tag-pin uses --depth 1 --branch <tag> against file:// URL`
      - `SYN-01: SHA-pin falls back to full clone + git checkout <sha>` (test the classifyRef branch)
      - `SYN-02: pullUpstream parameterized — gsd vs superpowers differ only in URL`
      - `SYN-05: writes last-synced-commit.json with both ref and 40-char SHA`
      - `SYN-05: last-synced-commit.json validates against schema/last-synced-commit.json`
      - `Pitfall 12: --main ref emits stderr warning before clone`
      - `Pitfall 10: re-pull of identical SHA short-circuits via git ls-remote`

    File 2 — `tests/phase-09-rebrand-sync.test.cjs` — placeholder: `plan-05`. Tests:
      - `SYN-03: scripts/sync-upstream/rebrand.cjs produces byte-identical output to direct engine.run({mode:'apply'})`
      - `SYN-03: sync rebrand never modifies foundation-frameworks/`

    File 3 — `tests/phase-09-merge-3way.test.cjs` — placeholder: `plan-03`. Tests (uses tests/fixtures/phase-09/three-version-trio/):
      - `SYN-04: 3-way merge — non-overlapping edits return exit 0 + clean content`
      - `SYN-04: 3-way merge — same-line clash returns exit > 0 + content with <<<<<<< markers`
      - `SYN-04 / Pitfall 1: missing input file → exit 255 → mergeOneFile throws diagnostic error (not silent treat-as-conflict)`
      - `SYN-04 / Pitfall 4: binary input (NUL byte in first 8KB) routed to sidecar — git merge-file NOT invoked`
      - `SYN-04 / Pitfall 6: -L label order — first label = current (oto), third label = other (upstream)`
      - `SYN-04 / D-12: conflict-file YAML header contains kind, upstream, prior_tag, prior_sha, current_tag, current_sha, target_path, inventory_entry, timestamp, oto_version (10 fields)`

    File 4 — `tests/phase-09-merge-add-delete.test.cjs` — placeholder: `plan-03`. Tests:
      - `SYN-04 / D-10: added file (in current, not in inventory, not in allowlist) emits .oto-sync-conflicts/<path>.added.md`
      - `SYN-04 / D-10: deleted file (in prior, not in current, oto/ has it) emits .oto-sync-conflicts/<path>.deleted.md`
      - `SYN-04 / D-17: unclassified-add path causes non-zero exit even if all merges clean`
      - `SYN-04 / D-13: sidecar suffix convention .added.md / .deleted.md uses path under .oto-sync-conflicts/`

    File 5 — `tests/phase-09-allowlist.test.cjs` — placeholder: `plan-03`. Tests:
      - `D-16: glob match — oto-owned path (e.g. bin/lib/codex-toml.cjs) is never compared against upstream`
      - `D-17 / Pitfall 7: allowlist completeness — sync against foundation-frameworks/get-shit-done-main/ produces 0 unclassified adds`
      - `D-16: glob ** suffix matches deeply nested paths (e.g. .oto/foo/bar)`
      - `Pitfall 8: allowlist is read by sync code (decisions/file-inventory.json, decisions/sync-allowlist.json themselves are oto-owned)`

    File 6 — `tests/phase-09-accept-helper.test.cjs` — placeholder: `plan-04`. Tests:
      - `D-15: oto sync --accept <path> strips YAML header, validates no <<<<<<< markers, writes to oto/<target>, deletes sidecar`
      - `D-15: oto sync --accept <path> refuses with non-zero exit if <<<<<<< markers still present`
      - `D-15: oto sync --accept-deletion <path> removes file from oto/ and updates inventory verdict to dropped_upstream`
      - `D-15: oto sync --keep-deleted <path> records oto-divergence and deletes sidecar without removing oto/<target>`
      - `Path traversal guard (T-09-04): --accept <path> outside oto/ rejected`

    File 7 — `tests/phase-09-report.test.cjs` — placeholder: `plan-05`. Tests:
      - `SYN-06 / D-14: REPORT.md regenerated each sync with auto-merged + conflict + added + deleted counts`
      - `SYN-06: BREAKING-CHANGES-{gsd,superpowers}.md appended (not overwritten) on each sync run`
      - `D-14: REPORT.md and BREAKING-CHANGES section share identical content for that sync event`

    File 8 — `tests/phase-09-cli.integration.test.cjs` — placeholder: `plan-06`. Tests:
      - `D-19: oto sync flag parser parses --upstream, --to, --apply, --dry-run, --accept, --status`
      - `D-19: bin/install.js dispatches 'oto sync ...' to bin/lib/sync-cli.cjs`
      - `SYN-07 (override): oto sync --upstream gsd --to <fixture-tag> end-to-end against bare-repo fixture (default --dry-run, no writes)`
      - `SYN-07 (override): oto sync --upstream gsd --to <fixture-tag> --apply writes to oto/`
      - `D-19: oto sync --status shows pending conflicts and last-synced refs`
      - `Pitfall 9: git --version parsing — lenient regex; fail-loud on unparseable output`
      - `Ref injection guard (T-09-03): --to '$(rm -rf /)' rejected by ref validator before passing to git`

    Each test file MUST start with `'use strict';` and end with no trailing test code that runs eagerly. Every test uses `{ todo: 'plan-NN' }` option.

    Set frontmatter `nyquist_compliant: true` in `09-VALIDATION.md` (use Edit, not Write — preserve other content).
  </action>
  <verify>
    <automated>node --test --test-concurrency=4 tests/phase-09-pull-puller.test.cjs tests/phase-09-rebrand-sync.test.cjs tests/phase-09-merge-3way.test.cjs tests/phase-09-merge-add-delete.test.cjs tests/phase-09-allowlist.test.cjs tests/phase-09-accept-helper.test.cjs tests/phase-09-report.test.cjs tests/phase-09-cli.integration.test.cjs 2>&1 | tee /tmp/p09-w0.log; grep -E '# tests [0-9]+' /tmp/p09-w0.log; grep -E '# fail 0' /tmp/p09-w0.log</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist at the listed paths
    - `node --test tests/phase-09-*.test.cjs` exits 0 (no failures, only todo)
    - The combined test output reports `# todo` count >= 30 (sum of t.todo entries across the 8 files matches the VALIDATION.md row count)
    - `tests/phase-09-pull-puller.test.cjs` contains `'SYN-05'` AND `'last-synced-commit.json'` AND `'Pitfall 12'` AND `'Pitfall 10'` (verify with grep)
    - `tests/phase-09-merge-3way.test.cjs` contains `'Pitfall 1'` AND `'Pitfall 4'` AND `'Pitfall 6'` AND `'D-12'` AND `'10 fields'` (verify with grep)
    - `tests/phase-09-merge-add-delete.test.cjs` contains `'D-10'` AND `'D-17'` AND `'.added.md'` AND `'.deleted.md'` (verify with grep)
    - `tests/phase-09-allowlist.test.cjs` contains `'foundation-frameworks/get-shit-done-main'` AND `'D-17'` AND `'Pitfall 7'` (verify with grep)
    - `tests/phase-09-accept-helper.test.cjs` contains `'--accept'` AND `'--accept-deletion'` AND `'--keep-deleted'` AND `'T-09-04'` (verify with grep)
    - `tests/phase-09-cli.integration.test.cjs` contains `'T-09-03'` AND `'Pitfall 9'` AND `'--status'` (verify with grep)
    - Every test file starts with `'use strict';` and uses `require('node:test')` (verify with grep)
    - No test file requires `bin/lib/sync-*.cjs` modules (those don't exist yet) — only require `node:test`, `node:assert/strict`, `node:path`, `node:fs/promises`, `node:os`, and the fixtures path
    - `09-VALIDATION.md` frontmatter has `nyquist_compliant: true`
  </acceptance_criteria>
  <done>
    8 phase-09-*.test.cjs scaffolds exist; combined run shows all `todo`, zero `fail`. Wave 1+ plans can fill bodies without renaming files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo state ↔ disk | `.gitignore` and committed metadata files are the contract for what reproducibility requires |
| Schema → consumers | `schema/sync-allowlist.json` and `schema/last-synced-commit.json` constrain what downstream waves can produce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 (early surface) | Tampering | `decisions/sync-allowlist.json` | mitigate | Schema enforces `additionalProperties: false`; validator runs in Task 1 verify command before any consumer reads it |
| T-09-08 | Denial of Service | `.oto-sync/upstream/` (gitignored) | accept | Single-user expectation; documented in DOC-03 (Phase 10). No lock file in v1. |
| T-09-09 | Tampering | Test fixtures (file:// URLs only) | mitigate | All test fixtures use `file://` (Pitfall 5); no real network in tests; fixture content is committed to repo |

Severity: all medium or low — Wave 0 has no high-severity surface (no subprocess invocation against user-supplied refs yet; that arrives in Wave 1).
</threat_model>

<verification>
- All schemas validate sample data via the existing hand-rolled validator
- `node --test tests/phase-09-*.test.cjs` exits 0
- `git status` shows only the new files in plan 09-01 are added (no accidental edits to existing files outside .gitignore)
- `cat .gitignore` shows the three new exclusion lines appended after the original 7 lines
</verification>

<success_criteria>
- 8 test scaffolds exist with t.todo() placeholders covering every row in 09-VALIDATION.md
- 2 schemas exist and validate via existing hand-rolled validator
- `decisions/sync-allowlist.json` exists with 33 oto_owned_globs (32 from D-16 + sync-cli.cjs)
- `.oto-sync/BREAKING-CHANGES-{gsd,superpowers}.md` exist as initial empty audit logs
- `.gitignore` excludes `.oto-sync/upstream/`, `.oto-sync/rebranded/`, `.oto-sync-conflicts/`
- Bare-repo fixture builder runs offline against `file://` URLs, producing 3 tags
- `09-VALIDATION.md` frontmatter set to `nyquist_compliant: true`
</success_criteria>

<output>
After completion, create `.planning/phases/09-upstream-sync-pipeline/09-01-SUMMARY.md`
</output>
