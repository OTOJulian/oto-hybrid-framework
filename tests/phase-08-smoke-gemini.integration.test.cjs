'use strict';
const { test } = require('node:test');

test.todo('D-17 gemini spine: install → /oto-help → /oto-progress → /oto-new-project tmpdir flow → state file written under .oto/');
test.todo('D-17 gemini per-runtime hook present in settings.json under BeforeTool / AfterTool events');
test.todo('D-17 gemini spine smoke skips with clear message if gemini --version < 0.38 or gemini not on PATH');
