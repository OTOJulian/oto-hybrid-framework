'use strict';
const { test } = require('node:test');
const assert = require('node:assert/strict');

// Phase 3 Wave 0 scaffold.
// Covers: INS-06 (--all runtime detection)
// Filled by: 03-02-PLAN.md (Wave 1)
// Sources: 03-VALIDATION.md per-task verification map

test('INS-06: --all detects present runtimes via dir existence', { todo: 'Wave 1 — fills assertions' }, () => {});
test('INS-06: detectPresentRuntimes returns [] when no runtimes installed', { todo: 'Wave 1 — fills assertions' }, () => {});
test('INS-06: detectPresentRuntimes only returns claude/codex/gemini (no other names)', { todo: 'Wave 1 — fills assertions' }, () => {});
